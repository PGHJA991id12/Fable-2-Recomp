# Generated by ReXGlue v0.10.0 on 2026-09-02 20:36:00 UTC.
#
# Migrated 2026-09-19 from XenonRecomp/fable2.toml (see that file for the
# original XenonRecomp/XenonAnalyse configuration).
#
# XenonRecomp fields with no ReXGlue equivalent (kept in XenonRecomp/fable2.toml):
#   - restgprlr_14 / savegprlr_14 / restfpr_14 / savefpr_14 / restvmx_14 /
#     savevmx_14 / restvmx_64 / savevmx_64 helper addresses
#     (register save/restore thunks found via byte-pattern scan):
#       restgprlr_14 = 0x82CA2C00   savegprlr_14 = 0x82CA2BB0
#       restfpr_14   = 0x82CA751C   savefpr_14   = 0x82CA74D0
#       restvmx_14   = 0x82FFFF68   savevmx_14   = 0x82FFFCD0
#       restvmx_64   = 0x82FFFFFC   savevmx_64   = 0x82FFFD64
#   - switch_table_file_path: ReXGlue has no separate switch-table file; the
#     XenonAnalyse tables are converted to [[switch_tables]] entries in
#     fable2_switch_tables.toml and pulled in via `includes` below.
#   - patch_file_path: none. The only available patch
#     ($SystemUpdate/su20076000_00000000) is a PIRS container, not a raw
#     XEX2 delta patch, so the recompiler runs against the unpatched
#     default.xex.

[project]
name = "fable_2"
sdk_version = "0.10.0"
game_root = "."

[entrypoint]
file_path = "default.xex"
out_directory_path = "generated/default"
includes = ["fable2_switch_tables.toml"]

# setjmp/longjmp: the recompiler redirects these to native implementations.
# longjmp (0x82CA9260) restores the full guest state from the jmp_buf and
# calls RtlUnwind (thunk 0x832B25BC) on the unwind path.
# setjmp (0x83000200) stores the full guest state (f14-f31, r13-r31, VMX,
# CR, LR) to the jmp_buf, using the same layout longjmp restores.
longjmp_address = 0x82CA9260
setjmp_address = 0x83000200

# --- Recomp-level patches via mid-asm hooks (see docs/patches.md) ---------
# Code-region Xenia patches are inert at runtime in this recomp (guest .text
# is never executed), so they are applied to the RECOMPILED code instead: a
# hook is injected at a specific guest instruction address and a C++ function
# (defined in src/fable2_hooks.cpp) rewrites a register, mirroring the patch's
# intent. Codegen emits each hook's extern prototype automatically. Hooks are
# keyed by address in codegen (a duplicate address overrides the earlier one),
# so each address appears exactly once below.
#
# 60 FPS (Xenia "60 FPS" by Margen67; guest: be8 0x82B9C8EB = 0x01): the
# state-2 case of sub_82B9C7F8 does `li r11,2` at 0x82B9C8E8; the hook fires
# right after it and overwrites r11 with 1.
[[entrypoint.midasm_hook]]
address = 0x82B9C8E8
name = "fable2_hook_60fps"
registers = ["r11"]
after_instruction = true

# Unlock Website Items (Xenia "Unlock Website Items" by Guy). sub_8256E368 is
# the "is a website/Guild-chest item available" check. It is gated by the
# player's website-registration flags on the chest object (r4): GATE 1 = bit6
# of *(r4+0x90), GATE 1b = bit0 of *(r4+0x40). Only when BOTH pass does the
# type-0xf0 item lookup run and the item get GRANTED via a vtable call
# (bctrl at 0x8256E4CC); every other path funnels to the return-0 tail at
# 0x8256E4E0. We force each gate's bit-extract result to 1 (r9 after the
# GATE-1 rlwinm, r8 after the GATE-1b clrlwi) so the function always treats the
# player as registered and performs the real grant. The getters run at save
# load, so this populates the chest inventory on load.
[[entrypoint.midasm_hook]]
address = 0x8256E384
name = "fable2_hook_website_g1"
registers = ["r9"]
after_instruction = true
[[entrypoint.midasm_hook]]
address = 0x8256E3AC
name = "fable2_hook_website_g1b"
registers = ["r8"]
after_instruction = true

# Unlock Collectors Edition Content (Xenia "Unlock Collectors Edition Content"
# by Guy). Same shape: sub_824B3528 is the "is CE-chest content available"
# check, gated by GATE 1 = bit6 of *(r4+0x90) and GATE 1b = a bit of
# *(r4+0x28); only when both pass does the type-0x39 item lookup run and the
# content get GRANTED via a vtable call (bctrl at 0x824B3678). We force each
# gate's bit-extract result to 1 (r10 after the GATE-1 rlwinm, r9 after the
# GATE-1b rlwinm) so the function always treats the player as registered.
[[entrypoint.midasm_hook]]
address = 0x824B3540
name = "fable2_hook_ce_g1"
registers = ["r10"]
after_instruction = true
[[entrypoint.midasm_hook]]
address = 0x824B3568
name = "fable2_hook_ce_g1b"
registers = ["r9"]
after_instruction = true

# --- Unlock grant methods (make the getter report each item as granted) -----
# Forcing the two registration gates alone is not enough: after the gates the
# getter finds the item and calls a per-object GRANT vtable method, and the
# getter returns THAT method's result. For an unregistered player the grant
# method returns 0 (item not granted), so the getter reports "not available"
# and the chest stays locked. We force each grant method's return to 1.
#
# Website: sub_8256D940 is the website-chest vtable[1] "is this item new?"
# check. Its final instruction is `xori r3, r9, 1` at 0x8256D9E4; hook right
# after it and set r3 = 1 so the getter reports the item as granted.
[[entrypoint.midasm_hook]]
address = 0x8256D9E4
name = "fable2_hook_website_grantnew"
registers = ["r3"]
after_instruction = true
#
# CE: sub_824ACAE0 is the CE-chest grant vtable slot called from
# GetCEContent. Its entire body is `lbz r3, 993(r3)` (returns the byte at
# item+0x3E1, the "CE content available" flag). Hook right after it and set
# r3 = 1 so the getter reports the CE content as available.
[[entrypoint.midasm_hook]]
address = 0x824ACAE0
name = "fable2_hook_ce_grantavail"
registers = ["r3"]
after_instruction = true

# Conservative settings: no register optimizations
skip_lr = false
skip_msr = false
ctr_as_local = false
xer_as_local = false
reserved_as_local = false
cr_as_local = false
non_argument_as_local = false
non_volatile_as_local = false

# Invalid instruction skips: 32-bit values (big-endian) the recompiler skips
# over during its linear .text walk instead of analyzing them as functions.
#   0x00000000      -- zero padding between functions (not a valid PPC insn)
#   0x00485645      -- end-of-.text data table at 0x832B31E0 (185 words of
#                      offset data, 0x832B31E0-0x832B34C4); skip all 740 bytes
[[entrypoint.invalid_instructions]]
data = 0x00000000
size = 4

[[entrypoint.invalid_instructions]]
data = 0x00485645
size = 740

# Manual function boundaries from the XenonRecomp config.
#
# ReXGlue rejects overlapping standalone function boundaries (XenonRecomp
# allowed them), so four entries that nested inside a larger entry were trimmed
# to end where the inner function begins. Original sizes are preserved in
# XenonRecomp/fable2.toml if these turns out to be wrong:
#   0x82BE9668: 0x08D4 -> 0x0018  (inner 0x82BE9680 starts at +0x18; both
#               originally ended at 0x82BE9F3C)
#   0x82C89840: 0x01D8 -> 0x0078  (inner 0x82C898B8 starts at +0x78)
#   0x82C898B8: 0x0154 -> 0x00A8  (inner 0x82C89960 starts at +0xA8)
#   0x82C8D3F0: 0x01E8, and the nested 0x82C8D4E8 entry was removed. With both
#               present the gap-fill extended the outer past its trimmed size and
#               codegen emitted a cross-function goto into 0x82C8D4E8 (compile
#               error). Keeping the outer at its original full size makes the
#               region one function; calls into it mid-body are handled as tail
#               calls to internal labels. Original inner entry: size 0x00F0.

[entrypoint.functions."0x8217F250"]
name = "SwitchDispatch_8217F250"
size = 0x00F0

# These are the loading screen functions when starting a new game
[entrypoint.functions."0x822142D0"]
name = "GetNewGameLoadingGlobal"
[entrypoint.functions."0x82988ED8"]
name = "LoadingScreen_Virtual43"
[entrypoint.functions."0x82988F28"]
name = "LoadingScreen_Virtual48"
[entrypoint.functions."0x8274B748"]
name = "LoadingScreen_Virtual42"
[entrypoint.functions."0x82EAA980"]
name = "LoadingScreen_Virtual36"
[entrypoint.functions."0x8274B758"]
name = "LoadingScreen_Virtual39"
[entrypoint.functions."0x8274B778"]
name = "LoadingScreen_Virtual28"
[entrypoint.functions."0x822D6678"]
name = "LoadingScreen_VectorOffset"

# After new game video plays
[entrypoint.functions."0x82988EC8"]
name = "Story_AfterNewGameVideo"

# After new game after "look little sparrow" LT button
[entrypoint.functions."0x82988F48"]
name = "Story_LookLittleSparrow"

#talking to camera man in child scene
[entrypoint.functions."0x82988F38"]
name = "Story_TalkToCameraMan"

# First Combat childhood fight
[entrypoint.functions."0x8297EB48"]
name = "Story_FirstChildCombat"

# Interacting with sister after first fight
[entrypoint.functions."0x8274B788"]
name = "Story_SisterAfterFirstFight"

# Entering the warehouse as child
[entrypoint.functions."0x8274B738"]
name = "Story_EnterWarehouseAsChild"

# Shooting Beetle in the warehouse as child
[entrypoint.functions."0x82DDC4D8"]
name = "Story_ShootBeetleInWarehouse"

# Swimming
[entrypoint.functions."0x8297EB28"]
name = "Swimming"

# Interacting with job sign (blacksmith)
[entrypoint.functions."0x823A1810"]
name = "Story_JobSignBlacksmith"

# Aiming range weapon (skill)
[entrypoint.functions."0x82988EE8"]
name = "Skill_AimRangeWeapon"

# Tatter spire right after you get there when the man says "Get Moving"
[entrypoint.functions."0x8274B768"]
name = "Story_TatterSpireGetMoving"

# === Guild chest (fable2.com website items / CE content unlock) ===
# The Guild chest holds items obtainable from the fable2.com website and the
# Collectors Edition content. An unregistered player who clicks it sees
# "Go to www.fable2.com for information on how to access the gold and items
# your heroic ancestors left behind." These are the functions on the
# chest-click path (see docs/patches.md). Names are best-effort from runtime
# tracing; refine them as behaviour is confirmed.

# Website-item getter. The Xenia "Unlock Website Items" patch targets its
# GATE2 branch (0x8256E4B8) and return-0 tail (0x8256E4E3) inside this fn.
[entrypoint.functions."0x8256E368"]
name = "GuildChest_GetWebsiteItem_8256E368"

# CE-content getter. The Xenia "Unlock CE Content" patch forces r9=1 at
# 0x824B366C (a load) inside this fn.
[entrypoint.functions."0x824B3528"]
name = "GuildChest_GetCEContent_824B3528"

# Per-item worker: called once per chest item on the click; calls the item op
# method (0x8256E4F8) through the website-object vtable.
[entrypoint.functions."0x82228830"]
name = "GuildChest_ProcessItem_82228830"

# Item-processing loop: the click handler that calls ProcessItem per item.
[entrypoint.functions."0x822DE008"]
name = "GuildChest_ProcessItems_822DE008"

# Kernel wait wrappers (KeWait* re-implemented over the Nt* syscalls)
#
# WARNING on the _Guest suffix: do NOT name these bare "KeWaitForSingleObject"
# / "KeWaitForMultipleObjects". Codegen emits the body as
# __imp__<name>, and rexruntimed.dll also exports __imp__KeWaitForSingleObject
# / __imp__KeWaitForMultipleObjects (the real xboxkrnl XAPI hooks). A local
# definition silently shadows the DLL import, so the XAPI import thunks
# (0x832B28AC / 0x832B2CDC, registered in fable_2_register.cpp) would resolve
# to the guest wrapper instead of the SDK kernel hook. The wrapper expects
# millisecond timeouts in the LpWakeTime field while XAPI callers pass kernel
# 100ns LARGE_INTEGERs (0 = infinite there!), which broke all audio/video
# worker-thread waits. Any future name must not collide with a rexruntimed.dll
# export (strip the __imp__ prefix and compare; see tools/check_manifest_collisions.py).
# Fills the NTAPI_WAIT_ENTRY LpWakeTime from a millisecond timeout:
# -1 (INFINITE) -> 0, otherwise timeoutMs * -10000 (relative, 100ns units).
# Called constantly (every alertable event/mutex wait funnels through it).
[entrypoint.functions."0x822D7508"]
name = "WaitEntrySetLpWakeTime"

# Alertable KeWaitForSingleObject (guest-image-internal): NtWaitForSingleObjectEx(Alertable=1),
# loops on STATUS_USER_APC (257) while a timeout was given.
[entrypoint.functions."0x822D74A0"]
name = "KeWaitForSingleObject_Guest"

# Alertable KeWaitForMultipleObjects (guest-image-internal): count <= 64 (KE_WAIT_VARIABLE limit),
# NtWaitForMultipleObjectsEx, loops on STATUS_USER_APC (257).
[entrypoint.functions."0x822D7528"]
name = "KeWaitForMultipleObjects_Guest"

# SDK mutex family (Xbox 360 mutexes are mutants).
#
# Lock side: 2-instruction thunk, r5 = 0 then tail-calls KeWaitForSingleObject_Guest.
# Timeout 0 -> WaitEntrySetLpWakeTime writes LpWakeTime = 0 (no timer) so the
# wait is INFINITE, not a zero-wait poll (and -1 also maps to 0, so 0 cannot
# mean "don't block" or INFINITE would be unexpressible). Blocks until the
# object is signaled; APCs are not retried (timeout==0), so STATUS_USER_APC
# (257) can be returned. r4 (wait reason) is ignored by the wrapper; callers
# still pass -1/0/flags in it. 42 call sites: audio/video/rendering worker
# threads blocking on per-object handles, SDK alert/exit loops. Paired with
# MutexUnlock_ReleaseMutant (0x83004F30) at sites such as sub_832B8DA8.
[entrypoint.functions."0x82196C58"]
name = "MutexLock_WaitInfinite"

# Unlock side: NtReleaseMutant(handle, NULL). Returns 1 on success; on
# failure logs the NTSTATUS through sub_82CC1C38 (same error logger as
# KeWaitForSingleObject_Guest) and returns 0. 22 call sites, the release half of
# the lock pairs that acquire via MutexLock_WaitInfinite.
[entrypoint.functions."0x83004F30"]
name = "MutexUnlock_ReleaseMutant"

# Create side: NtCreateMutant(outHandle, attrs, initialCount). r3 = out
# handle pointer, r4 = initial owned count, r5 = optional object-attributes
# pointer (built via sub_82CC1DF0 when non-null). Sanity-checks the returned
# handle (0x10000 check, error code 183 via sub_82CC0750) and returns it;
# logs and returns 0 on NtCreateMutant failure.
[entrypoint.functions."0x83004EA8"]
name = "MutexCreate_OutHandle"

# Time/clock family.
#
# The game's master millisecond clock, cached in a global singleton:
# returns *( *(0x82000908) + 16 ) — no arguments. 147 call sites in 89 files,
# almost all of the form "stw r3, N(obj)" storing it into per-object timestamp fields
# (lastUpdate/startTime/nextFire); a few compare against an adjacent
# previous-time field (32-bit wrap handling) or double-sample it (before/
# after bracketing, e.g. sub_8237E3E0 stores two reads 4 bytes apart).
# Millisecond scale proven by callers: rate limiters compute
# consumption = rate * (now - last) / 1000 (sub_82FFA508), poll loops use
# deadline = saved + 100 (sub_82388658), and the 2^30 jump guard in
# GetElapsedMsSinceStart below only makes sense in ms (~12.4 days).
[entrypoint.functions."0x82266070"]
name = "GetCurrentTimeMs"

# Elapsed milliseconds since first call, reset/wrap-guarded.
# X = GetCurrentTimeMs(); base A = global 0x81FFBDFC (captured from X on the
# first call, when A == 0); delta = X - A. Cached last delta B = global
# 0x81FFBE00: if (delta - B) > 0xC0000000 unsigned the update is skipped and
# B is returned instead (X wrapped or reset). 24 call sites, all in the
# 0x832B end-of-.text region, all passing the result into tick/update
# methods (e.g. sub_832B5530 -> sub_832B50C0(obj, now)).
[entrypoint.functions."0x832B9398"]
name = "GetElapsedMsSinceStart"

# Gap-region leaf with NO callers in the current code (registered by the
# gap walk over 0x832B3D6C-0x832B3DE0; the original branch source was
# re-segmented away, so it is unreachable/dead in this build). Mechanism:
# v = *(r3 - 4); if (v == r4) return 0; else return ~v
# (0xFFFFFFFF - v). Looks like a tag/checksum verifier over a 32-bit word
# stored immediately before a buffer, returning 0 on match and a
# non-zero token encoding the actual value on mismatch. Purpose not
# recoverable without the lost caller; name describes the mechanism only.
[entrypoint.functions."0x832B3D78"]
name = "CheckPrefixedWord_ReturnZeroOrComplement_832B3D78"

# SDK runtime memcpy: memcpy(dest=r3, src=r4, len=r5), returns dest
# (saved at -8(r1) on entry). ~290 insns: byte-wise dest alignment, 4/8-byte
# misaligned paths, 64-byte unrolled main loop, 128-byte dcbt prefetch for
# len >= 128, dcbtst writeback hints on the tail. 1310 call sites — the
# image's dominant copy primitive (game + SDK code). Named MemCpy_SdkRuntime
# (not bare "memcpy") to keep the guest symbol distinct from host libc in
# traces and to avoid any global-namespace ambiguity.
[entrypoint.functions."0x82CA2C60"]
name = "MemCpy_SdkRuntime_82CA2C60"

# KeWaitForMultipleObjects_Guest variant with the APC-retry flag (r7) forced to 0:
# 2-insn thunk "li r7,0; b KeWaitForMultipleObjects_Guest". With r7==0 the wrapper
# does NOT retry on STATUS_USER_APC (257 is returned to the caller) and
# passes Restart=0 to NtWaitForMultipleObjectsEx. 4 call sites: batch
# WaitAll locking of N mutex handles (sub_832B8C28 with a 1ms timeout,
# sub_832B8B80 with 2 handles + variable timeout) and single-object waits
# (sub_822BA4F0 infinite, sub_8262DDD0 variable). Note: the wrapper's
# timeout is r6 (fed to WaitEntrySetLpWakeTime), r5 is the mode
# (0=WaitAll, nonzero=WaitAny).
[entrypoint.functions."0x82179FB0"]
name = "KeWaitForMultipleObjects_NoApcRetry"

# Locked per-entry processing loop over a 388-byte-entry array. Args: obj
# (r3). Guards: tail word *(obj->280 + obj->276*388 - 40) != 0 and
# obj->692 != 0. Locks the mutex at (obj+852)->+4 via
# MutexLock_WaitInfinite (bails without processing if the lock cannot be
# taken). Loops i over obj->276 entries (array at obj->280, stride 388):
# vcall entry->348 (skip entry if it returns 0), range checks over entry
# fields 0/8/12/16/24/48/52/68, vcall entry->352 (fetch buffer info into
# stack slots r1+80/84), then a byte transform that toggles each byte's
# high bit (b+128 == b XOR 0x80, i.e. 8-bit signed<->unsigned conversion)
# between the entry buffer and the obj->12 buffer with MemCpy_SdkRuntime
# moves and entry->24 offset bookkeeping, then vcall entry->356 and
# entry->348 again. Unlocks via MutexUnlock_ReleaseMutant. 7 call sites,
# all sibling methods in the same 0x832B end-of-.text family, each gating
# the call on a different non-zero object field. The XOR-0x80 byte churn is
# the signature of 8-bit sample conversion — plausibly an audio voice/sound
# pipeline, but the vtable contents are unresolved so the name stays
# mechanism-based.
[entrypoint.functions."0x832B3700"]
name = "CriticalSection_ProcessEntryArray_832B3700"

# Vtable method of the 0x832B family (NO static call sites — reached only
# through virtual dispatch). Args: r3 = the sub-object at base+852, r4 = a
# 32-bit value. base = r3 - 852. If base->276 (the entry count) is non-zero
# it runs CriticalSection_ProcessEntryArray_832B3700(base), then unconditionally
# stores r4 into base->848 (a result/status word — no in-family reader found,
# so it is likely consumed by the same family's other vtable methods or
# exported state). ~15 insns.
[entrypoint.functions."0x832B3D90"]
name = "EntryArray_ProcessAndStoreResult_832B3D90"

# Time-gated tick of a ring-buffer object (vtable method, NO static call
# sites — invoked through virtual dispatch). Args: obj (r3). Guards:
# obj->212 == 0, obj->56 != 0, obj->208 != 0. Layout: obj->188 = capacity,
# obj->216 = write cursor, obj->228 = read cursor, obj->192 = watermark W,
# obj->184 = buffer pointer P, obj->244 = fill byte, obj->236 = last tick
# time, obj->240 = tick rate limit (ms), obj->92 = consumer sub-object S.
# Flow: now = GetElapsedMsSinceStart(); if (now - obj->236 > 12) skip the
# space check (starvation override). free = (write - read) mod capacity
# (standard formula, both branches verified). If free >= 1024 the candidate
# length is (free - 1024) & ~3 (keeps a 1024-byte reserve, 4-aligned), else
# 0. Watermark gates vs obj->192/obj->232 (flag obj->220 bypasses). When it
# passes: obj->84 = 1, then MemSet (sub_82CA3190) fills 2*W bytes at
# P + write with byte obj->244 (two chunks if it wraps capacity), and the
# write cursor advances 2*W mod capacity. Then a probe loop calls
# sub_82CD2DF8(S, &out) (three probes, looping while the out-difference is
# >= 1024) and stores the final probe result into the READ cursor (obj->228)
# plus now into obj->236; if (now - old236 >= obj->240) it re-enters the
# fill step, else re-checks free space. Returns 1 (acted) or 0 (skipped).
# Hypothesis: 8-bit audio silence padding (fill byte would be 0x80 = signed
# silence; the sibling family does XOR-0x80 sample conversion and the 12 ms
# / 1024-byte constants fit sample streaming) — but the fill byte and the
# consumer probe are runtime values, so the name stays mechanism-based.
[entrypoint.functions."0x832B68A0"]
name = "RingBuffer_TickPadWithFillByte_832B68A0"

# Priority-ordered multi-mutex arbitration loop. Args: manager M (r3).
# Layout: M->0 = state (must be 1 to accept requests), M->24 = pending-list
# head, M->28 = monotonically increasing generation counter (incremented
# once per entry). Node layout (16-byte stride vtable at node->0..20, next
# at node->8): node->4 = mutex handle, node->12 = priority, node->16 =
# "prepare" vtable slot, node->20 = "granted" vtable slot. Loop: while
# M->0 == 1 and the pending list is non-empty, drain every pending node
# (linked through node->0): priority = vcall node->16(node, M->28);
# node->12 = priority; priority 0 = skip; otherwise insert the node into an
# acquired list sorted by DESCENDING priority (higher priority toward the
# head, linked through node->8). After each drain pass, build stack arrays
# (cap 64): handles[i] = node->4, objs[i] = node, then
# ret = KeWaitForMultipleObjects_NoApcRetry(count, handles, mode=0
# (WaitAll), timeout=1ms). If ret >= count (timeout/APC) the loop drains
# again; on success (ret = 0) it runs the "granted" callback vcall
# objs[ret]->20(objs[ret]) and immediately MutexUnlock_ReleaseMutant
# (handles[ret]) — the highest-priority requester is notified and its gate
# released, while the remaining acquired mutexes stay held (released by
# sibling family methods). Sole caller: sub_832B8DA8, a sibling method that
# first locks M->8 and M->20 (MutexLock_WaitInfinite) and gates the call on
# M->24 != 0, i.e. the pending head — confirming the layout.
[entrypoint.functions."0x832B8C28"]
name = "PriorityArbitrate_PendingMutexRequests_832B8C28"

# CS-guarded virtual-call wrapper, one of ~11 uniform siblings in the
# 0x82CD2Cxx-0x82CD2Fxx block. Pattern (verified across the cluster):
# RtlEnterCriticalSection(global CS at 0x82CD0324); target = arg1->76
# (a C++ object, vptr at +0); call vtable[+60/4 = slot 15] with
# (this=target, out=arg2); RtlLeaveCriticalSection; return the result.
# The sibling slots are 28/56/60/64/72/80/92/96 (plus two that forward to
# helpers sub_82CDC180/sub_82CDCFE8) — i.e. a synchronized proxy class in
# the SDK runtime around one core object, all methods sharing the single
# global CS. 2 call sites: RingBuffer_TickPadWithFillByte_832B68A0 (polled
# 3x, looping while successive out-values differ by >= 1024 — a consumer
# progress query) and sub_832B6A98 (called after sibling slots 72/64 and a
# helper, in a fetch-and-advance sequence). The vtable contents are
# unresolved, so the name stays mechanism-based (slot offset in bytes).
[entrypoint.functions."0x82CD2DF8"]
name = "CsGuardedVcall_Slot60_82CD2DF8"

# 5-insn leaf: *r4 = (uint16_t)obj->108 * obj->120; returns 0.
# Field roles pinned down by the inverse operation sub_82CE37C0, which
# (spin-lock protected: KeRaiseIrqlToDpcLevel + KeAcquireSpinLockAtRaisedIrql
# on global 0x82CA7060, with a twllei div-by-zero trap guard) computes
# obj->120 = (*total) / obj->108 and sets obj->272 = 1 — so obj->108 is a
# unit/divisor and obj->120 the total/unit count; this function is the
# count -> total direction (unit * count, classic frameSize*frameCount /
# sampleSize*sampleCount byte-length math). Several siblings in the 0x82CE
# region write these fields (sub_82CE3BE0/sub_82CE3400/sub_82CE24B0/
# sub_82CE0C20 store the u16; sub_82CE3CF0/sub_82CE1588 update the u32).
# ZERO static call sites — vtable method of that object family.
[entrypoint.functions."0x82CE32B0"]
name = "StoreUnitTimesCount_82CE32B0"

# SDK C-runtime ctype predicate: returns bit 0 (the _UPPER flag) of the
# 256-entry u16 character-type table. Table base is runtime-loaded from
# global 0x82CCC8C4 (entry = *(base + c*2), zero-extended 16-bit). One of
# 9 uniform siblings in the 0x82CA6AB0-0x82CA6BB0 block, each extracting a
# different bit/mask of the same u16 entry: bit0 (this one, sub_82CA6AD0),
# bits 0|1|8 (sub_82CA6AB0: andi 259, sub_82CA6B90: andi 263), and single
# bits 24/26/27/28/29/30 (sub_82CA6B30/BB0/B70/B50/B10/AF0 — note: beyond
# the 16-bit load, so these read zero in the current table layout). The
# _UPPER meaning of bit 0 is pinned by its only non-cluster consumer,
# ToLower_SdkRuntime_82CA4288, which gates an A-Z -> a-z fold on it.
# 2 call sites: ToLower_SdkRuntime_82CA4288 and the dispatch table
# sub_82A25230 (which routes to the sibling predicates by index).
[entrypoint.functions."0x82CA6AD0"]
name = "Ctype_IsUpper_82CA6AD0"

# SDK C-runtime tolower, in classic CRT shape:
# if (0 <= c < 256 && Ctype_IsUpper(c)) return sub_821EE9E8(c); else return c
# where sub_821EE9E8 is plain ASCII fold (A-Z -> a-z, else c unchanged;
# 22 callers image-wide, so it is also the general tolower). The ctype
# guard is what makes this the locale-aware variant. 2 call sites, both
# string processing: sub_82448030 is a CASE-INSENSITIVE FNV-1 string hash
# (hash = tolower(byte) ^ (hash * 16777619), 16777619 = 0x01000193 = the
# 32-bit FNV prime) and sub_83002B80 converts a character to a 0-25 letter
# index (result - 96, 'a' = 97). Named ToLower_SdkRuntime (not bare
# "tolower") to keep the guest symbol distinct from host libc, matching the
# MemCpy_SdkRuntime convention.
[entrypoint.functions."0x82CA4288"]
name = "ToLower_SdkRuntime_82CA4288"

# Plain ASCII tolower, the raw fold without the ctype guard:
# if (c < 65) return c; if (c > 90) return c; return c + 32.
# i.e. A-Z -> a-z, everything else passes through unchanged. 22 call sites
# image-wide, so this is the general tolower used across the engine (the
# locale-aware ToLower_SdkRuntime_82CA4288 is a rarer variant that gates the
# same fold on Ctype_IsUpper_82CA6AD0). 6 insns, no prologue (leaf). Named
# ToLower_Ascii to distinguish it from ToLower_SdkRuntime and to keep the
# guest symbol distinct from host libc (MemCpy_SdkRuntime convention).
[entrypoint.functions."0x821EE9E8"]
name = "ToLower_Ascii_821EE9E8"

# Constant-true predicate / stub: "li r3,1; blr" — ignores every argument
# and always returns 1. 6 call sites, all treating the result as a boolean
# gate: sub_83205F58 sets up (r3=*(obj+16), r4, r6, r7) then "cmpwi r3,1;
# bne" to choose between (byte & 0x1F) and a fallback value (so it always
# takes the & 0x1F path); four 0x8304-region callers (sub_83040418,
# Func_83040FEC, Func_8304100C, Func_8304101C) share an identical
# "lwz r3,192(r31); li r5,0; li r6,0; bl" shape; sub_8301D688 gates on a
# byte == 118 ('v') before the call. The call shape (this + args, result
# used as yes/no) suggests a query method the compiler folded to a constant
# (a feature always on, an "accept all" policy, or a debug gate compiled
# out) — the original semantic is not recoverable without the lost
# vtable/branch source, so the name describes the mechanism only.
[entrypoint.functions."0x82C43198"]
name = "ConstTrue_Predicate_82C43198"

# The game's SIMD-optimized byte-copy kernel (a second memcpy, distinct from
# the SDK C-runtime MemCpy_SdkRuntime_82CA2C60). Convention: copy(dst=r3,
# src=r4, len=r5), returns dst (r26). Forward-only, NO overlap/direction
# handling (plain memcpy semantics). Two paths: len < 16 takes a small path
# (optional 8-byte head if src is 8-aligned, then an aligned 4-byte chunk via
# sub_82CA30E8, then a 4-byte remainder loop); len >= 16 takes a vectorized
# path using 128-bit SIMD (lvx128/vperm128/stvx128) for 16-byte main-loop
# moves with dcbt (source) and dcbzl (destination) 128-byte prefetch, chunked
# at 1024. 34 call sites across the engine. Named MemCpy_Vectorized to
# distinguish it from the SDK memcpy and to keep the guest symbol distinct
# from host libc (the MemCpy_SdkRuntime convention).
[entrypoint.functions."0x822085D0"]
name = "MemCpy_Vectorized_822085D0"

# 1-instruction tail-branch thunk: "b 0x822085d0" — a plain (unswapped)
# alias to the vectorized memcpy kernel MemCpy_Vectorized_822085D0, passing
# (dst=r3, src=r4, len=r5) through unchanged. Single caller: sub_830FB4E8,
# which sets up (r3=r23=dst, r4=r31=src, r5=*(r25)=len) — a straightforward
# forward copy. Its 8-byte neighbor sub_82B93040 is the SWEAP-direction
# sibling (swaps r3/r4 then tail-calls the same kernel, single caller
# sub_83045C00), so the pair offers both argument orders of the same SIMD
# copy kernel. Named as a thunk/trampoline to the vectorized memcpy.
[entrypoint.functions."0x82B93038"]
name = "MemCpy_Vectorized_Thunk_82B93038"

# Spin-lock acquire (test-and-set with spin). Args: addr (r3) -> a u32
# flag where 0 = free / 1 = held. Behavior: read *addr; if 0, atomically
# CAS it to 1 (claim) and return; if non-zero (held), spin re-reading
# *addr until it reads 0, then claim it. Uses an lwarx/stwcx. atomic pair
# inside the recompiler's global-lock region (mfmsr/mtmsrd MSR sync), with
# an lwsync after the claim, and a db16cyc delay hint once the spin counter
# passes 40 (bus-contention back-off). Returns void. Sits in the wait/
# sync infrastructure (immediately before KeWaitForSingleObject at
# 0x822D74A0). 15 call sites, all in a data-processing subsystem: each
# loads a global object, adds an offset (a per-object lock word at +12/+32/
# +36/+40/-7828 etc.), and calls this BEFORE reading that object's fields —
# i.e. a spin-lock guard. Named SpinLock_Acquire (the matching release that
# sets the flag back to 0 is a separate function).
[entrypoint.functions."0x822D7408"]
name = "SpinLock_Acquire_822D7408"

# Vtable method of the 0x82B49 object family (ZERO static call sites —
# reached only through virtual dispatch). One of the two largest methods in
# the region (291 insns; near-identical twin sub_82B49320 at 290). Args:
# obj (r3), src (r4, a pointer to a u64), len (r5). obj->16 is a 64-bit
# read position (it has a public getter, sub_82B495F8: "ld r3,16(r3)");
# obj->24 a 64-bit bound; obj->32 a size; obj->36 an index into a 32-byte-
# stride entry array. Flow: bounds-check (position + len <= bound, else
# return the 0x80004005 failure code), then a loop that (a) reads a value
# from src, (b) dispatches to a handler from a GLOBAL FUNCTION-POINTER
# TABLE (r24) indexed by the high 32 bits of *src, passing (bound,
# computed_ptr, chunk_len), (c) advances the position (obj->16) by the
# chunk, and (d) calls sibling sub_82B49730(obj) for a status (low byte
# checked) and sub_82B49678(obj, flag) on the overflow path. Returns 0 on
# success, a 0x8000xxxx status on failure. Mechanism is clear (bounds-
# checked, position-advancing, handler-table-dispatched chunk processing —
# a buffer/stream reader of some kind) but the object identity and handler
# table are unresolved, so the name describes the mechanism only and keeps
# the address suffix.
[entrypoint.functions."0x82B49110"]
name = "ProcessChunk_TableDispatch_82B49110"

# Pure no-op stub: "blr" — does nothing, returns whatever was in r3.
# 396 call sites (one of the hottest functions in the image). The dominant
# caller pattern is "call a helper (sub_82317E50 / sub_82317F98 / ...), then
# addi r3,r1,N; bl sub_829CE870" — i.e. it is invoked on a stack slot (often
# twice, at two different offsets) right after constructing/using an object,
# and a second common shape is "mr r3,obj; bl sub_829CE870" (called on an
# object, then another op follows). A no-op called this uniformly on
# objects/stack slots is the signature of an EMPTY virtual method — most
# likely a no-op destructor / release / delete for an object type whose
# teardown does nothing in this build (the call is still emitted for ABI/
# dispatch correctness). The exact interface slot is not resolvable without
# the vtables, so the name states the observable fact: it is a no-op.
[entrypoint.functions."0x829CE870"]
name = "NoOp_Stub_829CE870"

# Spin-locked pool / free-list node acquisition. Args: obj (r3) — selected
# by the 4 callers by indexing an array (base = caller's obj->4, 4-aligned
# index) to pick a per-slot object. Layout: obj->16 = head of a node list,
# obj->4 = step/size, obj->24 = lower window bound, obj->28 = upper window
# bound, obj+36 = the spin-lock word (guarded by SpinLock_Acquire_822D7408).
# Flow: acquire the spin-lock; if the list is empty call sub_83230568(obj)
# to produce a node (if that also yields nothing, release and return 0);
# otherwise take the head node, advance its internal chain pointer
# (node->0 = *node->0), increment a counter (node->4 += 1), and if the
# chain is exhausted (*node->0 == 0) unlink the node from the list (via the
# node->8 next pointers, fixing the head if needed); then advance the
# allocation window (obj->24 += obj->4, obj->28 -= obj->4); release the
# lock; return the new chain head (node->0) or 0. A synchronized block/node
# dispense from a pool. The object identity is unresolved (it is one of the
# per-slot objects in the data-processing subsystem), so the name describes
# the mechanism and keeps the address suffix.
[entrypoint.functions."0x83230688"]
name = "Pool_AcquireNode_83230688"

# ---------------------------------------------------------------------------
# MAIN SIZE-BUCKETED ALLOCATOR (the game's primary "malloc", 3725 call sites)
#
# This is a SEPARATE, higher-level allocator from the low-level custom heap
# (Heap_AllocateBlock_82238828 / Heap_FreeBlock_822394F0, which carve blocks
# out of NtAllocateVirtualMemory regions). Three functions form the entry
# chain:
#
#   sub_8221F388 (3725 callers) -- the "safe" wrapper everyone calls:
#       r31 = sub_8221F3F0(size);            // allocate
#       if (r31 == 0) {                       // OOM:
#           if (*(global_fnptr @ ~0x82000B0C)) { r4 = 0; call it }  // OOM error-report callback
#           sub_82CBBB58();                   // = __imp__DbgBreakPoint (1-insn thunk)
#       }
#       return r31;
#   sub_8221F3F0 (142 callers) -- 1-insn thunk: "b 0x83231d20" -> the core.
#   sub_83231D20 (2 callers)   -- the core size-bucketed allocator.
#
# So sub_8221F388 = "allocate(size); on OOM report + debug-break". OOM is
# treated as fatal (DbgBreakPoint), which is why this is the dominant
# allocation path: game code allocates through it and never checks for NULL
# (the wrapper guarantees non-NULL or a breakpoint).
# ---------------------------------------------------------------------------

# The core size-bucketed allocator. Arg: size (r3). Returns a pointer, or 0
# if the size can't be satisfied. Flow:
#   1. Lazy init: if an init-flag byte (game data) is 0, call sub_83231B60().
#   2. FAST PATH: linear-scan a 6-entry table of cached sizes; if `size` is
#      one of them, jump to a per-size fast path.
#   3. Otherwise dispatch by size to a pool:
#        size <= 64        -> index (size+3) into a pre-built node pool and
#                            Pool_AcquireNode_83230688 the selected node
#                            (returns 0 if size > that pool's max entry).
#        64  < size <= 512 -> spin-locked pool: SpinLock_Acquire(pool+40),
#                            sub_83231590(pool, size), release (->40 = 0).
#        512 < size <= 10240 -> a second spin-locked pool (same shape).
#        10240 < size <= 10485760 (10 MB) -> a third spin-locked pool.
#        size > 10 MB        -> sub_83230900(size) (large-allocation path).
#   Each pool has a max-size cap (checked via a global); exceeding it returns 0.
# A size-bucketed allocator: small sizes use a fixed-node pool, mid/large sizes
# use dedicated spin-locked pools, and huge sizes fall through to a large path.
[entrypoint.functions."0x83231D20"]
name = "Allocate_SizeBucketed_83231D20"

# 1-insn thunk: "b 0x83231d20" -- forwards (size) to the core allocator
# Allocate_SizeBucketed_83231D20 unchanged and returns its pointer. 142
# callers use this entry directly (they handle the 0/NULL result themselves
# rather than going through the OOM-crashing wrapper sub_8221F388).
[entrypoint.functions."0x8221F3F0"]
name = "Allocate_SizeBucketed_Thunk_8221F3F0"

# The game's PRIMARY allocation entry point (3725 call sites -- the dominant
# "malloc"). Arg: size (r3). Calls Allocate_SizeBucketed_Thunk_8221F3F0(size)
# (-> Allocate_SizeBucketed_83231D20) to get a pointer. If the result is 0
# (OOM): read a global function pointer (~0x82000B0C), and if non-zero call it
# with (r4 = 0) as an OOM error-report callback, then call sub_82CBBB58 which
# is a 1-insn thunk to __imp__DbgBreakPoint (debug breakpoint). Returns the
# pointer. Net effect: "allocate(size); on failure report + debug-break", i.e.
# OOM is fatal and callers never need to NULL-check. Sits one layer above the
# low-level custom heap (Heap_AllocateBlock_82238828).
[entrypoint.functions."0x8221F388"]
name = "Allocate_OrDbgBreak_8221F388"

# 2-insn pointer getter: "lwz r3, 32(r3); blr" -- returns the u32/pointer
# stored at offset +32 of the object in r3. 6 callers (all in the 0x830F-0x831B
# region); the result is passed as the first arg to various functions, and one
# caller null-checks it ("cmplwi r3,0"), so offset 32 holds a NULL-able pointer
# (a sub-object / back-reference). The owning class isn't resolved, so the name
# states the mechanism (get the pointer field at offset 32) + address suffix.
[entrypoint.functions."0x824C23D0"]
name = "GetPtr_Field32_824C23D0"

# ---------------------------------------------------------------------------
# FREE SIDE of the size-bucketed allocator (the game's primary "free",
# 4498 call sites -- even hotter than the allocate entry at 3725).
#
#   sub_8221BE68 (4498 callers) -- the main free() entry: 1-insn thunk
#       "b 0x83231be8" -> Free_SizeBucketed_83231BE8.
#   sub_83231BE8 (2 callers)    -- the core free(): given a pointer, try to
#       release it from each size pool in turn; return 1 if freed, 0 if not.
#       - not initialized (same init byte as the allocator) -> return 0
#       - ptr == NULL                                        -> return 1 (no-op ok)
#       - pool <=64   : Pool_FreeByAddress_832304E8(ptr, pool)  -> freed?
#       - pool 64-512 : Pool_FreeByAddress_832304E8(ptr, pool)  -> freed?
#       - pool 512-10240 / 10240-10MB / one more : SpinLock +
#         sub_83231518(pool, ptr) (spin-locked pool free)        -> freed?
#       - else (large): sub_83230848(ptr) (large-path free)     -> freed?
#   sub_832304E8 (4 callers)    -- find the block containing `ptr` in a pool
#       (walk the pool's block list r4->28; base r4->20) and free it; return
#       1 if the pointer was in this pool, 0 otherwise. Used by the two small
#       pools of Free_SizeBucketed.
#   sub_83230320 (1 caller)     -- the spin-locked single-block free: push the
#       address back onto its block's free list, decrement the block's in-use
#       count, advance the pool window; if the block empties, unlink the whole
#       block from the pool's free-block list. Called only by sub_832304E8.
#
# This is the exact counterpart of the allocate side (Allocate_OrDbgBreak / 
# Allocate_SizeBucketed): free walks the same size buckets the allocator used.
# ---------------------------------------------------------------------------

# 1-insn thunk: "b 0x83231be8" -- forwards the pointer to Free_SizeBucketed_
# 83231BE8. The game's PRIMARY free() entry point (4498 call sites -- the
# dominant free, called more often than the allocate entry). Plain free(ptr):
# returns 1 if the pointer was released (or was NULL), 0 if it was not owned
# by this allocator. Sits one layer above the low-level custom heap
# (Heap_FreeBlock_822394F0).
[entrypoint.functions."0x8221BE68"]
name = "Free_SizeBucketed_Thunk_8221BE68"

# The core size-bucketed free(). Arg: pointer (r3). Returns 1 if freed, 0 if
# not owned. Flow: (1) if the allocator's init byte is 0, return 0. (2) NULL
# -> return 1 (freeing NULL is a valid no-op). (3) Try the small pools first
# via Pool_FreeByAddress_832304E8(ptr, pool) for the <=64 and 64-512 pools;
# if either reports the pointer was freed, return 1. (4) Then the spin-locked
# pools (512-10240, 10240-10MB, and one more) via SpinLock_Acquire(pool+40) +
# sub_83231518(pool, ptr) + release; return 1 if one frees it. (5) Otherwise
# the large path: sub_83230848(ptr); return the freed bit. The exact mirror of
# Allocate_SizeBucketed_83231D20's size buckets. 2 callers (the thunk and
# sub_82BEAB88).
[entrypoint.functions."0x83231BE8"]
name = "Free_SizeBucketed_83231BE8"

# Find-and-free by address for a pool's small blocks. Args: ptr (r3), pool
# (r4). Walks the pool's block list (head at r4->28; each node ->0 = start
# offset, ->12 = next; base at r4->20) looking for the block whose range
# [node_start, base + node_start) contains ptr. If found: align ptr down to
# the pool's granularity (r4->0), load the block descriptor at the aligned
# address + 12, call Pool_FreeNode_83230320(desc, ptr) to release it, and
# return 1. If no block contains ptr, return 0. 4 callers: the two small pools
# of Free_SizeBucketed_83231BE8, plus sub_828456C8 and sub_8240DAA8.
[entrypoint.functions."0x832304E8"]
name = "Pool_FreeByAddress_832304E8"

# The spin-locked single-block free for a pool. Args: pool (r31), ptr (r4).
# SpinLock_Acquire(pool+36). Aligns ptr to the block granularity, pushes the
# address onto its block's free list (head at block->0, in-use count at
# block->4, decrementing it), and advances the pool's window (pool->24 /
# pool->28, step pool->4). If the block's in-use count reaches 0, unlinks the
# whole block from the pool's free-block list (head pool->16, via block->8
# next). Calls sub_83235FC0(pool->20) as a helper, recomputes the window, then
# releases the lock (pool+36 = 0). The release counterpart of
# Pool_AcquireNode_83230688. Single caller: Pool_FreeByAddress_832304E8.
[entrypoint.functions."0x83230320"]
name = "Pool_FreeNode_83230320"

# ---------------------------------------------------------------------------
# REFERENCE-COUNTED RESOURCE RELEASE (the game's main "Release()", 2794
# call sites) plus two sub-object vtable-slot-3 delegates.
# ---------------------------------------------------------------------------

# The game's primary reference-counted Release(). Arg: wrapper object (r3) that
# holds a reference-counted resource at wrapper->4. Flow: if wrapper->4 is NULL,
# just clear the wrapper and return. Otherwise ATOMICALLY decrement the
# resource's reference count (*(wrapper->4)) using an lwarx/stwcx. loop under the
# global lock (mfmsr/mtmsrd). If the count is still > 0, another holder remains
# -- just clear the wrapper's fields and return. If it reaches 0 (last reference):
# call the resource's destructor callback (resource->4, invoked with resource->8
# as the object) and then free the resource via Free_SizeBucketed_Thunk_8221BE68
# (the main free). Finally clear wrapper->0 and wrapper->4. A classic COM-style
# Release(): drop one reference, and if this was the last, destroy + free.
# 2794 call sites -- the dominant reference-release path.
[entrypoint.functions."0x829FF648"]
name = "Release_RefCounted_829FF648"

# A vtable method (0 DIRECT callers -- reached only via virtual dispatch) that
# delegates to a sub-object's vtable slot 3 and returns a boolean. Args: obj
# (r3), arg1 (r4). Loads the sub-object at obj->16, builds a 2-word arg block
# on the stack {arg1, 0}, and calls sub_obj->vtable[3](sub_obj, &argblock, 4)
# (vtable = *(sub_obj->0); slot at vtable offset 12 / index 3). Reduces the
# call's result to a 0/1 boolean via a clz-based bit test and returns it. The
# interface method (slot 3) is not resolvable without the vtable, so the name
# states the mechanism: a delegating vtable method over the +16 sub-object that
# returns a bool.
[entrypoint.functions."0x82C01E78"]
name = "SubObject_VcallSlot3_Bool_82C01E78"

# Conditionally delegates to a sub-object's vtable slot 3 and stores a 32-bit
# result. Args: obj (r3), dest (r4). Reads a flag byte at obj->5 and, when a
# clz-based bit test on it is set, loads the sub-object at obj->0, builds a
# 2-word arg block {dest, 0} on the stack, and calls
# sub_obj->vtable[3](sub_obj, &argblock, 4) (same vtable slot 3 / offset 12 as
# SubObject_VcallSlot3_Bool_82C01E78). Stores the call's 32-bit result into
# *dest. 17 callers (sub_82C680F8, sub_82372108, sub_82A4AF78, ...). The
# interface method (slot 3) isn't resolvable without the vtable, so the name
# states the mechanism: a flag-gated vtable-slot-3 delegate over the +0
# sub-object that writes a u32 result.
[entrypoint.functions."0x82C842C8"]
name = "SubObject_VcallSlot3_GatedStore32_82C842C8"

# ---------------------------------------------------------------------------
# A word-aligned memcpy (SDK region, sibling of memset sub_82CA3190), a
# triple-vcall gate, and a table-dispatched chunk writer.
# ---------------------------------------------------------------------------

# A word-aligned memcpy: memcpy(dst=r3, src=r4, len=r5), returns nothing
# (void). Classic PPC byte-align -> word-copy -> tail-bytes structure: a byte
# loop aligns dst to a 4-byte boundary, then a 4-byte word loop (with a separate
# unaligned-source branch) copies the bulk, then a final byte loop copies the
# <4 remainder. Forward-only, no overlap handling (plain memcpy semantics).
# Sits in the SDK runtime region immediately before memset (sub_82CA3190).
# 34 DIRECT call sites (e.g. `r4=src; r5=496; bl`), AND it is the aligned-chunk
# helper that MemCpy_Vectorized_822085D0 calls for its small-path (len<16) word
# copy. Named MemCpy_Aligned to distinguish it from MemCpy_SdkRuntime_82CA2C60
# (the ~1.2 KB unrolled CRT memcpy) and MemCpy_Vectorized_822085D0 (SIMD), and
# to keep the guest symbol distinct from host libc.
[entrypoint.functions."0x82CA30E8"]
name = "MemCpy_Aligned_82CA30E8"

# A triple-vcall gate: makes THREE virtual calls in sequence and returns 1 only
# if all three succeed (nonzero), else 0. Args: obj (r3), arg (r5). Reads the
# vtable at *(obj->0) and, in order: (1) calls vtable slot 6 (offset 24);
# if it returns 0, bail with 0. (2) calls vtable slot 8 (offset 32) passing the
# arg; if it returns 0, bail with 0. (3) calls vtable slot 7 (offset 28); the
# low byte of its result is the return value (1 = all three succeeded, 0 = some
# step failed). 40 callers, all in the 0x82C0-0x82C4 region (sub_82C033D8,
# sub_82C49680, sub_82C2D040, ...). The three interface methods (slots 6/8/7)
# aren't resolvable without the vtable, so the name states the mechanism: a
# three-step all-or-nothing virtual sequence.
[entrypoint.functions."0x82C011E8"]
name = "VcallTriple_AllSuccess_82C011E8"

# A table-dispatched chunked write/emit (the write-side sibling of
# ProcessChunk_TableDispatch_82B49110, same 0x82B48/0x82B49 data-processing
# family and the same 0x80004005 failure code). Args: obj (r3), a pointer to a
# 64-bit descriptor (r4), and a total length (r5). The descriptor's high 32
# bits select a handler from a global function-pointer table (index = (hi<<2)
# & ~3) and its low 32 bits is the data pointer being emitted. Guards: if the
# descriptor is 0, return 0x80004005. Then loops: computes the chunk length as
# min(obj->24 - obj->20, remaining) (calling sub_82B48A40(obj) first when
# obj->24 is 0, returning 0x80004005 if that fails), looks up the handler,
# calls it with (data, obj->16 + position, chunk_len, ...), and advances the
# position (obj->24) and a 64-bit byte offset (obj->40); repeats until the
# length is exhausted. Returns 0 on success, 0x80004005 on failure. Single
# caller: sub_82B48898.
[entrypoint.functions."0x82B486D0"]
name = "WriteChunk_TableDispatch_82B486D0"

# ---------------------------------------------------------------------------
# A general CS enter/try-enter (270 callers), a CS-guarded handler-dispatch
# vtable method, and a position-advancing vtable-slot-3 write.
# ---------------------------------------------------------------------------

# A general critical-section acquire: enters (blocking) or try-enters
# (non-blocking) a CS and records whether it succeeded. Args: lock-state obj
# (r3), CS pointer (r4), mode flag (r5). Stores the CS pointer at obj->0. If
# the flag (low byte of r5) is 0: call __imp__RtlEnterCriticalSection(CS)
# (blocking). If nonzero: call __imp__RtlTryEnterCriticalSection(CS) (non-
# blocking); if it returns != 1 (failed to acquire), set the acquired flag to 0.
# On success set obj->4 = 1 (acquired), on try-enter failure obj->4 = 0.
# 270 call sites -- a widely-used lock-acquire helper (the matching release is
# a direct __imp__RtlLeaveCriticalSection call at the callers). The obj is a
# small lock-state wrapper: obj->0 = CS pointer, obj->4 = acquired flag.
[entrypoint.functions."0x82200688"]
name = "CriticalSection_EnterOrTry_82200688"

# A CS-guarded handler-table dispatch -- a VTABLE METHOD (0 direct callers,
# reached only via virtual dispatch) in the 0x82B43 data-processing family
# (same 0x80004005 failure code as ProcessChunk_TableDispatch / 
# WriteChunk_TableDispatch). Args: obj (r3), a pointer (r4), an offset (r5),
# and a value (r6). Guards (else return 0x80004005): obj->12 != 0, *r4 != 0,
# and (r6_low32 + r5) <= obj->32 (a 64-bit bounds check). On success: acquires
# a CS via CriticalSection_EnterOrTry_82200688 on the wrapper at obj+40, makes
# a virtual call on obj->36 (vtable slot 4) plus sub_82C6F8E8, and if that
# returns 1, computes a handler-table index and dispatches to a global
# function-pointer table with (*r4, r5 + obj->12, r6, ...); releases the CS
# (__imp__RtlLeaveCriticalSection) and returns 0. Any failed guard/step returns
# 0x80004005. The interface method and the handler-table entries aren't
# resolvable without the vtables, so the name states the mechanism: a
# bounds-checked, CS-protected dispatch.
[entrypoint.functions."0x82B43CF0"]
name = "CsGuarded_HandlerDispatch_82B43CF0"

# A position-advancing write via vtable slot 3 (a member of the same slot-3
# interface as SubObject_VcallSlot3_Bool_82C01E78 and 
# SubObject_VcallSlot3_GatedStore32_82C842C8, but over a +8 sub-object). Args:
# obj (r3), arg (r4), count (r5). Guards (else return): count > 0 and obj->8 !=
# 0. Loads the sub-object at obj->8 and calls sub_obj->vtable[3](sub_obj, 
# &{arg, 0}, sign_extend(obj->12), count) (vtable slot at offset 12 / index 3),
# then advances the position counter obj->12 += count. Returns void. Single
# caller: sub_829FCBE0. The slot-3 interface method isn't resolvable without
# the vtable, so the name states the mechanism: a slot-3 write that advances a
# position by the count.
[entrypoint.functions."0x82C62EB0"]
name = "VcallSlot3_AdvancePosition_82C62EB0"

# ---------------------------------------------------------------------------
# A signed-byte strcmp (169 callers), two vtable methods (a slot-3 product
# write and a global-callback accumulator), and a value/state processor.
# ---------------------------------------------------------------------------

# A signed-byte string compare (strcmp with sign-extended bytes). Args: str1
# (r3), str2 (r4). Loop: load *str1 as a SIGNED byte (lbz then extsb) into r10;
# if *str1 != 0 go to compare; else (str1 hit its 0 terminator) load *str2 and
# if *str2 == 0 return 0 (both empty / equal), else fall through to compare
# (where 0 < *str2 so str1 is shorter). Compare: load *str2 as a signed byte
# into r11; if r10 < r11 return -1, if r10 > r11 return 1, else advance both
# and loop. Returns -1 (str1 < str2), 0 (equal), 1 (str1 > str2). The sign-
# extension (extsb) makes this a SIGNED byte compare (bytes >= 0x80 sort as
# negative), distinct from an unsigned strcmp. 169 call sites. Named
# StrCmp_Signed to convey the signed-byte ordering and to keep the guest symbol
# distinct from host libc.
[entrypoint.functions."0x8226D7A8"]
name = "StrCmp_Signed_8226D7A8"

# A vtable method (0 direct callers, reached only via virtual dispatch) that
# computes a 32-bit product and writes it via vtable slot 3. Args: arg (r3),
# factor a (r4), factor b (r5), and a pointer-to-object (r6). Computes
# product = (int32)a * (int32)b, dereferences r6 to get the object, and calls
# object->vtable[3](r6, arg, product, 0) (vtable slot at offset 12 / index 3 --
# the same slot-3 interface as the other SubObject_VcallSlot3_* wrappers, but
# here the object is a dereferenced pointer and the product is passed in). 
# Returns the product. A "unit x count, write it" shape (cf.
# StoreUnitTimesCount_82CE32B0). The slot-3 method isn't resolvable without the
# vtable, so the name states the mechanism: a slot-3 write carrying a product.
[entrypoint.functions."0x82305270"]
name = "VcallSlot3_WithProduct_82305270"

# A vtable method (0 direct callers, reached only via virtual dispatch) that
# accumulates the results of calling a global function-pointer callback in a
# loop. Args: obj (r3), base (r4), count (r5). Guards: if the two byte fields
# obj->8 == obj->9, or count == 1, take a single-iteration/special path. 
# Otherwise loop (count-1) times: each iteration calls the global callback (a
# fixed function pointer in game data) with (base + descending_counter, 1,
# obj->4, 1) and adds the return to an accumulator. Returns the accumulated
# sum. The callback's identity and the object class aren't resolvable without
# more context, so the name states the mechanism: a looped global-callback
# accumulator.
[entrypoint.functions."0x82BC30D0"]
name = "AccumulateGlobalCallback_82BC30D0"

# A value/state processor that returns the non-zero-ness of its value argument.
# Args: obj (r3), value (r4), flag (r5). Every exit path evaluates the idiom
# (r11 = 0 - value; r10 = -1 + carry; return r10 & 1), which is 1 iff value !=
# 0 and 0 iff value == 0 -- so the RETURN is simply (value != 0). The real work
# is value-dependent SIDE EFFECTS along the way: if value > -2 call
# sub_82CD11D0(); if obj->24 < value call sub_821A9170(obj, value, obj->20);
# else (obj->24 >= value), if flag != 0 and value < 16 and value < obj->20 call
# sub_822F1DB0(obj, 1, value), and if value == 0 and obj->20 < 16 store 0 to
# obj->20 and a byte to obj->4. 8 callers, all consuming the low byte of the
# result as a boolean gate. The object class and helper identities aren't
# resolvable, so the name states the observable contract: process a value
# (with side effects) and return whether it was non-zero.
[entrypoint.functions."0x821A9090"]
name = "ProcessValue_FlagNonZero_821A9090"

# ---------------------------------------------------------------------------
# Nine functions: two hot 0x822F value/buffer operators, a hot bounded buffer
# copy with error codes, a heap carve, a lazy getter, two vtable-slot-3
# methods, an all-success vcall chain, a vtable thunk, and a return-0 stub.
# ---------------------------------------------------------------------------

# A lazy-initialized (memoized) getter for a 64-bit field -- a VTABLE METHOD
# (0 direct callers, reached only via virtual dispatch). Args: obj (r3). Loads
# obj->40; if it is already nonzero, return it immediately. If it is 0: call
# sub_82CC1460(obj->8, &out) to compute the value, cache the result at obj->40,
# then return it. A classic "compute once, cache, return" memoized getter for
# the obj->40 resource. The cached value is a 64-bit pointer/resource handle
# (ld/stw of the full 64 bits). Named for the mechanism: a lazy getter of the
# +40 field.
[entrypoint.functions."0x82B4F958"]
name = "LazyGet_Field40_82B4F958"

# A bounded buffer operation that copies or zero-fills and raises an error
# code on the failure paths. 61 call sites (a hot SDK-region routine at 0x82CA).
# Args (r3, r4, r5, r6): guards on r6 != 0 (else return 0) and r3 != 0 (else
# raise error 22). Compares two length fields (r4 vs r6): when the first is >=
# the second it copies via MemCpy_SdkRuntime_82CA2C60; when r5 == 0 it
# zero-fills via the SDK memset (sub_82CA3190) and then raises error 22 (or 34
# on one length-branch). The error is raised via a sub_82CAB770() call, a
# store of the code (22 or 34) into an out-slot, then sub_82CAB630() -- the
# SDK exception/bug path. Returns the error code (22 or 34) on failure paths
# and 0 otherwise. The exact class/args aren't resolvable (the recompiler reuses
# r3), so the name states the observable behavior: a length-checked buffer copy
# (or zero) that raises an error code on the failure paths.
[entrypoint.functions."0x82CA3730"]
name = "Buffer_CopyOrZero_Error_82CA3730"

# A vtable method (0 direct callers) -- the fifth known member of the recurring
# vtable-slot-3 interface, this one over the +16 sub-object (same sub-object
# offset and 0x82C01E region as SubObject_VcallSlot3_Bool_82C01E78). Args:
# wrapper (r3), arg (r4). Loads the sub-object at wrapper->16 and calls
# sub_obj->vtable[3](sub_obj, &{arg, 0}, 1) (vtable slot at offset 12 / index
# 3). From the returned value it derives a boolean through a
# clz/clz/rotate/xor-1 bit idiom and returns 0 or 1 (a negated/derived bit of
# the slot-3 result). The slot-3 method isn't resolvable without the vtable, so
# the name states the mechanism: a slot-3 vcall that returns a derived bool.
[entrypoint.functions."0x82C01E20"]
name = "VcallSlot3_Bool_82C01E20"

# A low-level custom-heap block carve/split in the 0x8323 allocator (sibling of
# the named Pool_*/Allocate_SizeBucketed family). Args: obj (r3), a key/value
# (r4), a pointer to the sorted-list head (r5), and a pointer to an offset (r6).
# Walks a SORTED linked list (node->0 = key, node->12 = next) to find the first
# node whose key is >= the value (else return 0). On a hit it runs alignment/
# mask arithmetic (obj->0 is an alignment mask, 16-byte block overhead), calls
# sub_83230D00 to carve/split the block, then UNLINKS the node from the list
# (fixing the predecessor's ->12 and the head) and rewrites the block's
# next/size fields. Single caller: sub_83231590 (the spin-locked pool carve). 
# Named for the mechanism: carving an aligned block out of a sorted free-list
# node.
[entrypoint.functions."0x83231058"]
name = "Heap_CarveBlock_83231058"

# A trivial 2-instruction stub: `li r3,0; blr` (returns 0). 20 call sites, all
# in the SDK region (sub_82CAB7E0, sub_82C00C90, sub_82CADF00, ...). This is a
# no-op default method / interface fallback (the same shape as NoOp_Stub_829CE870
# but returning 0 instead of void). Named for the mechanism: a return-zero stub.
[entrypoint.functions."0x831FD318"]
name = "Stub_ReturnZero_831FD318"

# A pure vtable thunk (a vtable_x forwarder, Section G). Args: wrapper (r3).
# Dereferences r3 to get the object, loads vtable slot 14 (offset 56 / 4), and
# `bctrl`s straight into it, passing the args through unmodified. Single caller:
# sub_82C14F38 (as the middle step of that all-success chain). Named per the
# Section G convention: VtableSlotX<index>.
[entrypoint.functions."0x82C01090"]
name = "VtableSlotX14_82C01090"

# A chained all-success vcall gate -- a sibling of VcallTriple_AllSuccess_82C011E8
# (same "run a sequence of virtual calls, short-circuit on the first failure, 
# return 1 only if every step succeeds" pattern). Args: wrapper (r3), arg (r5).
# (1) vcall (*wrapper)->vtable[6] (offset 24); if it returns 0, return 0. (2)
# Otherwise call VtableSlotX14_82C01090(wrapper, arg); if it returns 0, return
# 0. (3) Otherwise vcall (*wrapper)->vtable[7] (offset 28); return 1 if nonzero,
# else 0. So it returns 1 iff all three virtual steps succeed. 5 callers, all in
# the 0x82C1-0x82C3 region. Named for the mechanism: an all-success vcall chain.
[entrypoint.functions."0x82C14F38"]
name = "VcallChain_AllSuccess_82C14F38"

# A value/state processor -- a SIBLING of ProcessValue_FlagNonZero_821A9090
# (same structure, different thresholds and helpers). Args: obj (r3), value
# (r4), flag (r5). Every exit path evaluates the (r11 = 0 - value; r10 = -1 +
# carry; return r10 & 1) idiom, so the RETURN is simply (value != 0). The real
# work is value-dependent side effects: if value > 32767 call sub_82CD11D0();
# if obj->24 < value call sub_8217A4F8(obj, value, obj->20); else (obj->24 >=
# value), if flag != 0 and value < 8 and value < obj->20 call a helper and do
# bookkeeping. 7 callers (sub_826C3AA8, sub_822F6CB0, sub_822F52A8, ...), all
# consuming the low byte as a boolean gate. Named parallel to its 0x821A sibling:
# process a value (with side effects) and return whether it was non-zero.
[entrypoint.functions."0x8217A5E0"]
name = "ProcessValue_FlagNonZero_8217A5E0"

# A hot (51 callers) range/clamp + object-update operator in the 0x822F
# value/buffer subsystem (the same region as the ProcessValue_FlagNonZero_*
# pair, which it calls). Args: obj a (r3), obj b (r4), offset (r5), value (r6).
# If (b->20 < offset) call sub_82CD12C8() (a grow/extend); then compute a
# CLAMPED amount = min(value, b->20 - offset). If obj a == obj b: call
# sub_822F6E18 twice to update in place. Otherwise (obj a != obj b): call
# ProcessValue_FlagNonZero_8217A5E0(obj a, clamped, 0) and update both objects'
# fields (->4 / ->24), using a small-buffer inline-vs-pointer indirection (when
# the size field is < 8, treat the field as an inline 4-byte slot; otherwise
# dereference it). The object class isn't resolvable, so the name states the
# observable behavior: clamp a value to a range (growing if needed) and update
# the objects.
[entrypoint.functions."0x822F6380"]
name = "ClampAndUpdate_822F6380"

# A load-from-source (copy-assign) for the 0x822F object class.

# Initializes an object from a source: sets obj->20 = 0, obj->24 = 7 (a fixed
# type/size constant), and the 16-bit obj->4 = 0; then calls
# ClampAndUpdate_822F6380(obj, src, 0, -1) to load the variable-length buffer
# part from src; then copies the tail fields src->28 -> obj->28 and
# src->32 -> obj->32. Returns void. 9 callers (sub_823725E8, sub_82376A90,
# sub_825F2E10, ...). The "init to fixed defaults + clamp/copy the buffer + copy
# the tail" shape is a load-from / copy-assign for this class. The class itself
# isn't resolvable, so the name states the observable behavior: load this object
# from a source (initializing the fixed fields first).
[entrypoint.functions."0x822F2398"]
name = "LoadFrom_Source_822F2398"

# A recursive tree/list destructor that frees its nodes.

# Args: obj (r3, the owning object, passed down unchanged), node (r4). Reads
# node->17 (a byte flag): if it is nonzero, STOP (this node is a leaf/sentinel).
# Otherwise loop: (1) recurse on the child node->8 -- sub_82859340(obj, node->8);
# (2) FREE the current node via Free_SizeBucketed_Thunk_8221BE68(node); (3)
# advance node = node->0 (next) and continue while the new node->17 is 0. So it
# recursively destroys the child subtree at +8, frees each node on the way, and
# walks the +0 chain, stopping at a node whose +17 flag is set. 19 callers
# (sub_829FDAE8, sub_825F0248, sub_824FF8B8, ...). The node class isn't
# resolvable, so the name states the mechanism: a recursive destroy that frees
# the nodes of this tree/list.
[entrypoint.functions."0x82859340"]
name = "DestroyTreeNodes_82859340"

# A write-a-range-at-position operation on the 0x821A/0x822F small-buffer
# object (the same class as LoadFrom_Source_822F2398 / ClampAndUpdate_822F6380 /
# ProcessValue_FlagNonZero_*).

# Args: obj (r3), position (r4), length (r5). Uses the small-buffer
# inline-vs-pointer indirection: the buffer base is *obj->4 (external) when
# obj->24 >= 16, else &obj->4 (inline). If the position lies within
# [base, base + obj->20) it updates IN PLACE via sub_8218EA38(obj, position -
# base, length). Otherwise (position out of the current range) it routes through
# ProcessValue_FlagNonZero_821A9090(obj, length, 0) and does follow-up bookkeeping
# (a grow/realloc-style path). Returns void. 32 callers (sub_82CD1A98,
# sub_82C694C0, sub_82C83900, ...). Named for the mechanism: write a range at a
# position, in place when it fits, else via the grow path.
[entrypoint.functions."0x821A8F68"]
name = "WriteRange_AtPos_821A8F68"

# The game's PRIMARY 32-bit string hash -- FNV-1 (case-SENSITIVE). 2167 call
# sites, making it one of the hottest functions in the binary.

# Args: str (r3, a null-terminated byte string), seed (r4, the initial hash
# value). First scans str to find its length (stop at the 0 byte); if the string
# is empty, return immediately (returning the seed). Otherwise run the FNV-1
# loop over each byte: hash = (hash * 0x01000193) ^ byte, where 0x01000193
# (16777619) is the FNV-1 32-bit prime. Returns the 32-bit FNV-1 hash. The
# callers pass seed = 0x811C9DC5 (the standard FNV-1 32-bit offset basis), so
# in practice this is the canonical FNV-1 32 hash of a string. This is the
# case-SENSITIVE variant; the case-insensitive FNV-1 is sub_82448030. Named
# FNV1Hash to convey the exact algorithm.
[entrypoint.functions."0x821F3C28"]
name = "FNV1Hash_821F3C28"

# A lazy/memoized getter for the data pointer (obj->28) of the 0x822F/0x821A
# small-buffer object (same class as LoadFrom_Source_822F2398 / 
# WriteRange_AtPos_821A8F68).

# Arg: obj (r3). If obj->28 (the cached data pointer) is already set, return it.
# Otherwise, if obj->20 (length) is 0, return obj->28 (which is 0). Else resolve
# the buffer base using the small-buffer indirection -- *obj->4 (external) when
# obj->24 >= 8, else &obj->4 (inline) -- call sub_822CA580(base), cache the
# result in obj->28, and return it. A "compute once, cache, return" getter for
# the object's data pointer. 19 callers (sub_825ADB40, sub_825B27D0, ...). Named
# for the mechanism: a lazy getter of the data pointer.
[entrypoint.functions."0x822F2088"]
name = "LazyGetData_822F2088"

# An insert-at-position (with possible growth) on a 12-byte-stride dynamic
# array in the 0x8237 region.

# Args: obj (r3), a value (r4), a count/offset (r5), and a data triple (r6, a
# 3-word struct). The obj holds a 12-byte-stride array: start = obj->4, current
# position = obj->8, end/capacity = obj->12. It computes element indices as
# (obj->12 - obj->4)/12 (total) and (obj->8 - obj->4)/12 (current), clamps the
# requested index/insertion point against these, and -- when the array must
# grow -- reallocates (frees via Free_SizeBucketed_Thunk_8221BE68 and calls
# sub_82684B38 for the new block, with sub_8264EFE0 as a helper). Returns void.
# 2 callers (sub_8236FDD8, sub_82B423F0). The exact element semantics aren't
# resolvable, so the name states the mechanism: insert at a position in a
# 12-byte-stride dynamic array, growing it as needed.
[entrypoint.functions."0x82370A98"]
name = "Array_InsertAt_82370A98"

# A validated single-element insert into the 12-byte-stride dynamic array --
# the count==1 wrapper around Array_InsertAt_82370A98 (same 0x8236/0x8237 array
# family).

# Args: obj (r3), array (r4), value (r5), data triple (r6). Validates the array
# preconditions with debug assertions (ppc_trap 22 on violation): array->4 (start)
# ordering vs array->8 (current), and the data pointer. Computes the current
# element index (array->8 - array->4)/12, then calls Array_InsertAt_82370A98
# (array, value, count=1, data) to insert ONE element. Returns void. 3 callers
# (sub_8236F0F0, sub_8236E1B0, sub_82B41738). Named for the mechanism: a
# single-element (count==1) insert with precondition checks.
[entrypoint.functions."0x8236FDD8"]
name = "Array_InsertOne_8236FDD8"

# A major refcount Release (690 callers) -- a sibling of 
# Release_RefCounted_829FF648, but over the obj->0 resource.

# Arg: obj (r3). Loads obj->0 (a reference-counted resource pointer); if it is
# 0, return. Otherwise atomically decrement the refcount at (obj->0 + 12) using
# an lwarx/stwcx. retry loop under the global lock (mfmsr/mtmsrd + 
# REX_ENTER/LEAVE_GLOBAL_LOCK). If the new count is still > 0, return (not the
# last reference). If it reaches <= 0 (last reference), call the destructor
# sub_82B39A30(obj->0) and then Free_SizeBucketed_Thunk_8221BE68(obj->0). Returns
# void. 690 callers (sub_824E7BA0, sub_8328C2E0, sub_828592B0, ...) -- one of
# the hottest release paths in the binary. The COM-style "decrement, destroy +
# free on zero" contract is the same as Release_RefCounted_829FF648, except the
# resource lives at obj->0 (not obj->4) and its refcount at resource+12.
[entrypoint.functions."0x821C67D8"]
name = "Release_RefCounted_821C67D8"

# A free-forwarding thunk (743 callers) -- a single `b` to the primary free.

# 1-instruction body: `b Free_SizeBucketed_Thunk_8221BE68`. It forwards its
# argument straight to the game's primary free(). 743 callers (sub_82C0D9D0,
# sub_82C2F000, sub_82C748D0, ...) -- a distinct free entry point (most likely a
# vtable operator-delete / destructor-tail for a specific class) that simply
# delegates to the size-bucketed free. Named for the mechanism: a free thunk.
[entrypoint.functions."0x824FE010"]
name = "Free_Thunk_824FE010"

# A pure vtable thunk (a vtable_x forwarder, Section G) -- the offset-52 sibling
# of VtableSlotX14_82C01090.

# Arg: wrapper (r3). Dereferences r3 to get the object, loads vtable slot 13
# (offset 52 / 4), and `bctr`s straight into it, passing the args through
# unmodified. 2 callers (sub_82C15068, sub_82C4A410). Named per the Section G
# convention: VtableSlotX<index>.
[entrypoint.functions."0x82C010B0"]
name = "VtableSlotX13_82C010B0"

# A chained all-success vcall gate -- a sibling of VcallChain_AllSuccess_82C14F38
# (same "run a sequence of virtual calls, short-circuit on the first failure, 
# return 1 only if every step succeeds" pattern; only the middle thunk differs).

# Args: wrapper (r3), arg (r5). (1) vcall `(*wrapper)->vtable[6]` (offset 24);
# if it returns 0, return 0. (2) Otherwise call VtableSlotX13_82C010B0(wrapper,
# arg) (vtable slot 13 / offset 52); if it returns 0, return 0. (3) Otherwise
# vcall `(*wrapper)->vtable[7]` (offset 28); return 1 if nonzero, else 0. So it
# returns 1 iff all three virtual steps succeed. 16 callers, all in the
# 0x82C1-0x82C4 region. Named parallel to its 0x82C14F38 sibling (which uses
# VtableSlotX14 in the middle instead of VtableSlotX13).
[entrypoint.functions."0x82C15068"]
name = "VcallChain_AllSuccess_82C15068"

# A string setter / copy-constructor for the 0x822F/0x821A small-buffer object
# (same class as LoadFrom_Source_822F2398 / WriteRange_AtPos_821A8F68).

# 330 callers (sub_82AB77A8, sub_82A665B8, sub_82370060, ...) -- a major string-
# setting operation. Args: obj (r3), src (r4, a null-terminated byte string).
# Initializes the object: obj->20 = 0 (length), obj->24 = 15 (the max inline
# size, matching the < 16 inline threshold of the small-buffer indirection), and
# clears the inline byte at obj->4. Computes strlen(src) by scanning to the 0
# byte, then copies the string in via WriteRange_AtPos_821A8F68(obj, src,
# strlen). Returns void. So it resets this object and makes it hold a copy of
# the C string. Named for the mechanism: set this (small-buffer) object to a
# string.
[entrypoint.functions."0x822F2020"]
name = "SetString_822F2020"

# A name-hash initializer: computes the FNV-1 hash of a fixed global string and
# stores it in obj->0.

# Arg: obj (r3). Loads a fixed global string (via a pointer at ~0x81FF7B98),
# copies it into a temporary small-buffer on the stack via SetString_822F2020,
# resolves the buffer (inline vs external per the small-buffer indirection), and
# computes its FNV-1 32 hash via FNV1Hash_821F3C28 (seed = 0x811C9DC5, the offset
# basis). Stores the hash in obj->0. If the temp buffer was external it frees it
# (Free_SizeBucketed_Thunk_8221BE68). Finally clears bit 0 of obj->4 (obj->4 &=
# 0x1FFFFFFF). Returns void. 3 callers (sub_8236E1B0, sub_8236D6A8, 
# sub_8236E348). So it initializes obj->0 to the FNV-1 hash of a known global
# name and clears a flag bit in obj->4. Named for the mechanism: initialize the
# name hash from a global string.
[entrypoint.functions."0x8236D358"]
name = "InitNameHash_8236D358"

# A case-INSENSITIVE string compare (stricmp). 211 call sites.

# Args: str1 (r3), str2 (r4). Walks both strings byte-by-byte (lbzu
# pre-increment). At each step compute diff = str1byte - str2byte; if str2byte is
# the 0 terminator, return (0 if the strings are equal, else the running diff);
# if diff == 0 continue. On the FIRST difference, fold both bytes to lowercase
# (if a byte is in 'A'..'Z', OR in bit 5 / 32) and recompute diff; if the
# folded diff is 0 (they differ only by case) keep scanning, else return the
# signed folded difference. So it returns 0 when the strings are equal ignoring
# case, and a nonzero signed value otherwise. Named StrCmpIgnoreCase to convey
# the case-insensitive ordering (and keep the guest symbol distinct from host
# libc); the signed-byte variant is StrCmp_Signed_8226D7A8.
[entrypoint.functions."0x82CA6320"]
name = "StrCmpIgnoreCase_82CA6320"

# A push_back / append to a u32 dynamic array (4-byte stride).

# Args: obj (r3, the array), value (r4). The array layout: obj->0 = base
# pointer, obj->8 = current index/count, obj->4 = capacity. If (obj->4 - obj->8)
# < 1 (no room), it calls sub_82C21C90 (an ensure-capacity/grow) with a new
# size. Then stores value at base[obj->8] (a 4-byte-stride slot: *(obj->0 +
# obj->8 * 4) = value) and increments obj->8. Returns void. 38 callers, all in
# the 0x82C2-0x82C3 region. Named for the mechanism: append a u32 to a dynamic
# array (grow as needed, then store + bump the index).
[entrypoint.functions."0x82C21E08"]
name = "Array_PushBack_82C21E08"

# A binary search (lower_bound-style) over a sorted array of keyed objects,
# returning the index or insertion point plus a found flag.

# Args: obj (r3, the array: obj->0 = base pointer, obj->8 = count), a key
# pointer (r4, the target key is at r4->0), and an out flag (r5). Sets *out = 0
# (not found). If count == 0, return 0. Otherwise binary-search: low = 0, high =
# count-1; while low <= high: mid = (low+high)/2, elem = base[mid] (4-byte
# stride), key = elem->0; if key > target, high = mid-1; if key == target, set
# *out = 1 and RETURN mid (found); if key < target, low = mid+1. If the loop
# exits without a match, RETURN the insertion point (mid or mid+1) with *out
# still 0. So it's a lower_bound: exact index when found (flag set), else the
# insertion position. 11 callers (sub_82C0DEA8, sub_82C1ACD8, sub_82224FE0,
# ...). Named for the mechanism: a key-based binary search with a found flag.
[entrypoint.functions."0x82C0BC08"]
name = "BinarySearch_82C0BC08"

# The primary refcounted copy-assignment (operator=) -- 2524 callers, one of
# the hottest functions in the binary. The AddRef counterpart to
# Release_RefCounted_821C67D8.

# Args: obj (r3, this), src (r4, other). Sets obj->0 = 0. Atomically increments
# a global counter (lwarx/stwcx. retry under the global lock) -- a side-effecting
# generation/sequence counter. If src == obj, return (self-assign guard). Then:
# (1) RELEASE the old -- if obj->0 != 0, call Release_RefCounted_821C67D8(obj);
# (2) ACQUIRE the new -- if src->0 != 0, copy it into obj->0 (obj->0 = src->0)
# and atomically increment the new resource's refcount at (src->0 + 12) via the
# same lwarx/stwcx. retry loop. Returns obj (r3). So this is the classic
# refcounted copy-assign: Release(old), copy the pointer, AddRef(new). 2524
# callers (sub_82690588, sub_82594EA0, sub_824B2540, ...).
[entrypoint.functions."0x821F0108"]
name = "CopyAssign_RefCounted_821F0108"

# A push of a 36-byte element (a small-buffer object) into a 36-byte-stride
# array (the 0x8237 array family; the elements are the small-buffer class that
# LoadFrom_Source_822F2398 operates on).

# Args: obj (r3, the array: obj->4 = start, obj->8 = current, obj->12 = end,
# 36-byte stride), source element (r4). Computes the element counts (obj->8 -
# obj->4)/36 and (obj->12 - obj->4)/36. If there is room (current element index
# < end element index): copy the source element into the slot at obj->8 via
# LoadFrom_Source_822F2398, then advance obj->8 by 36 (one element). Otherwise
# (array full) it routes to sub_82376118 (a grow path). Returns void. 7 callers
# (sub_82B3EDD0, sub_82B4B0E0, sub_82375270, ...). Named for the mechanism: push
# a 36-byte element into the array (copy in, bump the position, grow if full).
[entrypoint.functions."0x823758C0"]
name = "Array_PushElement_823758C0"

# A bounds-checked copy that validates its parameters and delegates to 
# sub_82CAA2E0 (a later TODO function) -- a sibling of 
# Buffer_CopyOrZero_Error_82CA3730 (same 0x82CA region, same error codes).

# Args: dst (r3), capacity (r4), src (r5), len (r6). Guards (else raise an
# error): len != 0, dst != 0, and src != 0 -- any null raises error 22 (via the
# sub_82CAB770() + store-code + sub_82CAB630() SDK exception path). Then if
# capacity < len, raise error 34 (buffer too small). Otherwise call
# sub_82CAA2E0(dst, src, len) to perform the copy and return 0. So it's a
# validated copy: check dst/src non-null and capacity >= len, then copy. 
# Returns 0 on success, 22 (null pointer) or 34 (insufficient capacity) on
# failure. 137 callers (sub_82B39B48, sub_82A4D7F0, sub_829BBE00, ...). Named
# for the mechanism: a bounds-checked copy.
[entrypoint.functions."0x82CA3808"]
name = "CheckedCopy_82CA3808"

# A SIMD (vector) memcpy KERNEL -- the 128-bytes-per-iteration vectorized loop
# that a vectorized memcpy dispatches to for large blocks.

# Args: dst (r3), src (r4), length (r5). For length >= 128 the main loop loads
# 8 x 128-bit vectors from src (lvx at offsets 0/16/32/48/63/64/80/96 with vperm
# alignment), stores them to dst (stvx at the same offsets), then advances dst
# and src by 128 and length -= 128, looping while length >= 128 (with dcbt/dcbzl
# cache-block hints for the large case). A scalar path (lvsl shift + tail store)
# handles length < 128. Single caller: MemCpy_Vectorized_822085D0 (it calls this
# as its large-block kernel). So it is the raw SIMD copy kernel. Named for the
# mechanism: a SIMD (128-byte-unrolled) memcpy kernel.
[entrypoint.functions."0x82CC9DA0"]
name = "MemCpy_SIMDKernel_82CC9DA0"

# A validated tree-node removal: destroys the node's subtree and rewires the
# links to take the node out of the structure.

# Args: obj (r3), node (r4), and two pointers (r5, r6). Validates preconditions
# with `trap 22` assertions (r5 and r6 must be nonzero, not equal to node, and
# must match node's fields). On success it destroys the node's subtree via
# DestroyTreeNodes_82859340(node, node->4), then rewires: obj->0 = node, clears
# node->8, sets the node's self-reference fields, and copies node->4->0 into
# obj->4 (splicing the node out). Returns void. 11 callers (sub_82A6D9F0,
# sub_82A56CA0, sub_82B150D8, ...). Named for the mechanism: remove a node from
# a tree (destroy its subtree, rewire the links).
[entrypoint.functions."0x826A4A20"]
name = "Tree_RemoveNode_826A4A20"

# A specialized allocation entry point: allocates `size` bytes via the primary
# malloc, with conditional setup for size >= 2.

# Args: size (r3), flag (r4, always 0 in the callers). If size == 0 or size == 1
# (detected via (-1 / size) >= 1), skip the setup. Otherwise (size >= 2) it runs
# a setup that references two fixed global strings (~0x81FF563C / 0x81FF5630) and
# calls sub_822F1F00 (a local-stack preparation). Then it always calls
# Allocate_OrDbgBreak_8221F388(size) (the game's primary malloc) and returns the
# allocated pointer. So it's a wrapper around the primary allocator that does
# some conditional initialization first. 4 callers (sub_821A9170, sub_82A1DA08,
# sub_82A1DC28, sub_826C0D38). Named for the mechanism: allocate (with
# conditional init) via the primary malloc.
[entrypoint.functions."0x821A9258"]
name = "Allocate_Init_821A9258"

# A buffer grow/realloc for the 0x821A/0x822F small-buffer object -- the grow
# path called by ProcessValue_FlagNonZero_821A9090.

# Args: obj (r3), new size request (r4), new length (r5). Computes the new size
# (r4 | 15, i.e. rounded up toward a 16-multiple, or a combined size derived from
# obj->24 for small values), allocates a new buffer of (new_size + 1) bytes via
# Allocate_Init_821A9258, and if the length (r5) is nonzero copies the existing
# data into the new buffer via Buffer_CopyOrZero_Error_82CA3730 (resolving the
# old base via the small-buffer inline-vs-external indirection). Frees the old
# external buffer if obj->24 >= 16 (Free_SizeBucketed_Thunk_8221BE68). Then
# updates obj->4 (new buffer pointer), obj->24 (new size), obj->20 (new length),
# and null-terminates the external buffer. Returns void. Single caller:
# ProcessValue_FlagNonZero_821A9090 (its grow path). So it's a data-preserving
# realloc of the small-buffer object's storage. Named for the mechanism: grow the
# buffer (allocate larger, copy, free old, update fields).
[entrypoint.functions."0x821A9170"]
name = "GrowBuffer_821A9170"

# A clamped range update -- a SIBLING of ClampAndUpdate_822F6380 (same
# structure, different update helper), and the in-place core that
# WriteRange_AtPos_821A8F68 calls.

# Args: obj a (r3), pos/obj b (r4), length (r5), extra (r6). If (b->20 < length)
# call sub_82CD12C8 (a grow/extend); then compute a CLAMPED amount = min(extra,
# b->20 - length). If obj a == obj b: update IN PLACE via sub_822F1F60 twice
# (with (clamped + length, -1) then (0, length)). Otherwise (obj a != obj b):
# a cross-object update path (loc_8218EAAC). Returns void. 41 callers (sub_82C6A248,
# sub_82C84920, sub_82C83900, ...). Parallel to ClampAndUpdate_822F6380 (which
# uses sub_822F6E18 for the in-place update); here the in-place helper is
# sub_822F1F60. Named for the mechanism: a clamped range update (grow if
# needed), in place or across objects.
[entrypoint.functions."0x8218EA38"]
name = "RangeUpdate_8218EA38"

# A case-INSENSITIVE FNV-1 32 string hash -- the ToLower-per-byte sibling of
# FNV1Hash_821F3C28.

# Args: str (r3, null-terminated), seed (r4, initial hash). Scans str for its
# length (stop at 0); if empty, return the seed. Otherwise run the FNV-1 loop
# over each byte, but FIRST lowercase it via ToLower_SdkRuntime_82CA4288, then
# hash = lowercased_byte ^ (hash * 0x01000193), where 0x01000193 (16777619) is
# the FNV-1 32-bit prime. Returns the 32-bit case-insensitive FNV-1 hash. The
# case-SENSITIVE variant is FNV1Hash_821F3C28. 2 callers (sub_821BC7E8,
# sub_82C67878). Named to convey the exact algorithm + case-insensitivity.
[entrypoint.functions."0x82448030"]
name = "FNV1Hash_CaseInsensitive_82448030"

# A vtable-slot-3 caller with a loop/single mode, selected by a flag bit in
# obj->5 -- a member of the recurring slot-3 interface (over the *obj->0
# sub-object).

# Args: obj (r3), value (r4), count (r5). A bit test on obj->5 (via
# clz/rotate/xor) selects the mode. MODE A (loop): if count != 0, loop `count`
# times -- each iteration vcall `(*obj->0)->vtable[3](obj->0, &{value++, 0}, 1)`
# (vtable slot at offset 12 / index 3), incrementing value each pass -- then
# return 1. MODE B (single): one vcall `(*obj->0)->vtable[3](obj->0, &{value, 0},
# count)`, returning a bit derived from the result (clz/rotate/xor). So it calls
# the slot-3 method either `count` times or once, depending on the obj->5 flag.
# 1 caller: sub_82C669C0. The slot-3 method isn't resolvable without the vtable,
# so the name states the mechanism: a slot-3 vcall in loop or single mode.
[entrypoint.functions."0x82C66330"]
name = "VcallSlot3_LoopOrSingle_82C66330"

# A stateful object processor in the 0x82C6 region that orchestrates a gated
# slot-3 store, a buffer update, and a slot-3 loop/single call, returning a byte
# flag.

# Args: (r3), obj a (r4), obj b (r5). If obj a->4 (a byte) != 0: call
# SubObject_VcallSlot3_GatedStore32_82C842C8(obj a, &out) and store the result
# back into obj a->4. Reads the out value, and updates a buffer via sub_822F1F60
# (if out-1 > obj b->20) or sub_826C2FF8 (else, with out-1-obj b->20). Resolves
# obj b's buffer base via the small-buffer indirection (obj b->24 >= 16 ? *obj
# b->4 : &obj b->4), then calls VcallSlot3_LoopOrSingle_82C66330(obj a, base,
# out). Finally returns the byte obj a->4. 2 callers (sub_82C85250, 
# sub_82C67878). The object class isn't resolvable, so the name states the
# observable behavior: process the object (gated slot-3 store + buffer update +
# slot-3 call), returning a byte flag.
[entrypoint.functions."0x82C669C0"]
name = "ProcessObject_82C669C0"

# A push of a 12-byte element into a 12-byte-stride array (with growth) -- a
# sibling of Array_PushElement_823758C0 (36-byte) and Array_PushBack_82C21E08
# (u32), but here the element is a raw 12-byte struct copied verbatim.

# Args: obj (r3, the array: obj->4 = start, obj->8 = current, obj->12 = end,
# 12-byte stride), src (r4, a 12-byte struct). Computes current element count
# (obj->8 - obj->4)/12 and total (obj->12 - obj->4)/12. If there is room (current
# < total): copy the 12 bytes from src into the slot at obj->8 (obj->8+0/+4/+8 =
# src->0/+4/+8), then advance obj->8 by 12. Otherwise (full): validate (obj->4 <=
# obj->8, else `trap 22`) and grow via sub_82C66BE0, then push. Returns void.
# Single caller: sub_82C67878. Named for the mechanism: push a 12-byte element
# into the array (copy in, bump the position, grow if full).
[entrypoint.functions."0x82C66E00"]
name = "Array_Push12_82C66E00"

# A 5-way state dispatch (state-machine switch) on the byte obj->16->21
# (values 0-4) -- a switch/jump-table dispatcher.

# Arg: obj (r3). Reads the state byte at obj->16->21. If it is > 4, jump to a
# default path. Otherwise switch on it via a function-pointer table:
#   case 0: sub_82BCB7E8(obj), return 0
#   case 1: (obj->16->36 related path)
#   case 2/3/4: other 0x82BC handler paths
# (each case calls a distinct handler -- sub_82BC9860, sub_82BCB0A8, 
# sub_82BCB610, sub_82BCB6E0, sub_82BCB8D0, sub_82BCD698, sub_8227BB58, ...).
# 2 callers (sub_82BC62B8, sub_8227B8B8). The individual states/handlers aren't
# resolvable, so the name states the mechanism: a 5-way dispatch on a state byte.
[entrypoint.functions."0x82BCBB40"]
name = "StateDispatch_82BCBB40"

# A binary search with a VIRTUAL (polymorphic) comparison -- a sibling of
# BinarySearch_82C0BC08, but the element-vs-target comparison is done by a
# vtable method instead of a direct field compare.

# Args: obj (r3, the array: obj->0 = base, obj->8 = count), a target (r4), and an
# out flag (r5). Sets *out = 0. If count == 0, return 0. Otherwise binary-search:
# low = 0, high = count-1; while low <= high: mid = (low+high)/2, element =
# base[mid] (4-byte stride), and call (element->4)->vtable[1] (a virtual compare
# method) to compare the element to the target; narrow low/high on the result. 
# Returns the index/insertion point and sets *out on a match (like a lower_bound).
# Single caller: sub_82C17350. The element class (and thus the compare method)
# isn't resolvable, so the name states the mechanism: a binary search whose
# comparison is a virtual method.
[entrypoint.functions."0x82C15850"]
name = "BinarySearch_VirtualCmp_82C15850"

# A lookup-by-key that binary-searches an array (wrapper->4) with a virtual
# comparison and returns the found element's field 4 -- the caller of
# BinarySearch_VirtualCmp_82C15850.

# Args: wrapper (r3, whose member at +4 is the array), key (r4). Calls
# BinarySearch_VirtualCmp_82C15850(wrapper->4, key, &found) to binary-search the
# array. If found (found != 0) and the element at the returned index is non-null:
# return element->4 (a sub-object). Otherwise return 0. So it's a find-by-key that
# returns the matched element's ->4 member (or 0 if not found). 0 direct callers
# (a virtual/dead method). Named for the mechanism: look up a key via the
# virtual-compare binary search and return the found element's field 4.
[entrypoint.functions."0x82C17350"]
name = "Lookup_ByVirtualCmp_82C17350"

# A tree insertion: walk a binary tree to find the insertion point for a key, then
# insert a node (with a fixup call).

# Args: root (r3, whose ->4 is the first node), key wrapper (r4, *r4 = the key).
# The nodes carry: ->8 = right child, ->0 = parent, ->12 = node key, ->21 = a
# state byte (0 / nonzero). The search loop walks the tree -- if node->12 >= key,
# step up to the parent (*node); else step right (node->8) -- until it reaches a
# node with a nonzero state byte (the insertion point). It then performs the
# insertion (splicing a new node in) and calls sub_82419280 (a tree fixup/rebalance
# step). 7 callers (sub_829FF798, sub_823715E8, sub_8236DFC0, sub_8226A650,
# sub_829FFC48, sub_82A000F8, sub_82509E40). The exact tree type (e.g. red-black)
# isn't confirmed from the fields, so the name states the observable behavior: a
# tree insert (find the point, insert, fix up).
[entrypoint.functions."0x82A00620"]
name = "Tree_Insert_82A00620"

# A constant-return stub: returns the integer 4 (a 2-insn function: li r3,4; blr).
# 0 direct callers, so it is almost certainly a VIRTUAL method (invoked through a
# vtable) -- likely a property/type-code/size accessor whose value is the constant
# 4. The exact meaning of 4 needs the vtable + class. Named for what it does: a
# stub returning the constant 4.
[entrypoint.functions."0x826A0520"]
name = "Stub_Return4_826A0520"

# A binary search (lower_bound) over a 12-byte-stride array, comparing each
# element's field 0 to a key -- a sibling of BinarySearch_82C0BC08 (4-byte) and
# BinarySearch_VirtualCmp_82C15850 (virtual cmp).

# Args: obj (r3, the array: obj->4 = start, obj->8 = current, obj->12 = end, 12-byte
# stride), key wrapper (r4, *r4 = the key). Validates the ranges (obj->8 <= obj->12,
# obj->4 <= obj->8, else `trap 22`). Binary-searches: mid, element = base + mid*12;
# compare element->0 (the element's key field) to the key -- if element->0 < key, move
# right (base += 12, shrink the range); else move left. On completion, validate the
# found position (several `trap 22` range checks) and return the insertion point / match.
# 2 callers (sub_8236DFC0, sub_82372108). The element class (a 12-byte struct keyed on
# its first word) isn't resolvable, so the name states the mechanism: a 12-byte-stride
# binary search by element->0.
[entrypoint.functions."0x8236F0F0"]
name = "BinarySearch_12Stride_8236F0F0"

# A RECURSIVE linked-list destroy: walk the list, releasing each node's ->0
# sub-object and freeing the node.

# Args: obj (r3, an owner/context), node (r4, the current list node; node->8 = next
# link, node->0 = a refcounted payload, node+16 = a refcount slot, node->25 = a flag
# byte). Recursively calls itself on node->8 (the next link); for the current node it
# releases node->0 via Release_RefCounted_829FF648 (at node+16) and frees the node via
# Free_SizeBucketed_Thunk_8221BE68, then advances to the next node (node->0) and loops
# while the node->25 flag is 0. Returns void. So it recursively tears down a linked
# list of refcounted nodes. 6 callers (sub_82443110, sub_823D46F8, sub_83218A40,
# sub_8247E448, sub_82A121D8, sub_83218350). The exact list type isn't resolvable, so
# the name states the mechanism: a recursive destroy of a linked list (release + free
# each node).
[entrypoint.functions."0x82443440"]
name = "DestroyList_Recursive_82443440"

# A string/buffer load into obj with a conditional FAST and SLOW path, selected by
# a field on a control object (r4->20).

# Args: obj (r3, the destination small-buffer object), control (r4, ->20 selects the
# path), source (r5, the source small-buffer object). If control->20 == 0 (fast path):
# just LoadFrom_Source_822F2398(obj, source). Otherwise (slow path): resolve the
# source buffer via the small-buffer indirection (source->24 >= 8 ? *source->4 : 
# &source->4), build a local buffer, and copy through ClampAndUpdate_822F6380 plus
# sub_8217AA20 / sub_8217AB30 / sub_822F5208 / sub_822F6CB0, freeing temporaries via
# Free_SizeBucketed_Thunk_8221BE68. 6 callers (sub_823735B0, sub_82B4FBD0, 
# sub_823728E0, sub_82B41738, sub_825F2E10, sub_8260B9E0). The exact semantics of the
# control object and the slow-path transform aren't fully resolvable, so the name
# states the observable structure: load a string/buffer, fast path when control->20
# is 0, otherwise the slower multi-step path.
[entrypoint.functions."0x823725E8"]
name = "LoadString_FastOrSlow_823725E8"

# A SIMPLE 12-byte-stride binary search (comparing element->0 to a key over [start,
# end)) -- a simpler sibling of BinarySearch_12Stride_8236F0F0 (no trap-22
# validation), storing the result into *out.

# Args: out (r3, receives the found position), start (r4), end (r5), key wrapper
# (r6, *r6 = the key). count = (end - start)/12 (12-byte element stride). Binary-
# searches: element = start + mid*12; if element->0 >= key move left, else move right
# (start += 12). On completion, stores the result into *out. Returns void. 2 callers
# (sub_82B40BD0, sub_82B41738). The element class (a 12-byte struct keyed on its first
# word) isn't resolvable, so the name states the mechanism: a simple 12-byte-stride
# binary search by element->0.
[entrypoint.functions."0x82B429A0"]
name = "BinarySearch_12Stride_Simple_82B429A0"

# An identity/passthrough: returns its 2nd argument (r4) unchanged (a 2-insn
# function: mr r3,r4; blr).

# The 3 callers (sub_82EE53D8, sub_82FFE460, sub_82EE95D8) each pass a constant in
# r3 (4, 752, or 312 -- an opcode/type-ish value) and a value in r4, and take back
# r4 (the value, unchanged). So the observable behavior is an identity on the second
# argument. This shape (pass a code + a value, get the value back) is the classic
# default/identity implementation of a VIRTUAL method (a base handler that returns
# its input unchanged), or an inlining artifact. The exact class/vtable isn't
# resolvable, so the name states what it does: return arg2 unchanged.
[entrypoint.functions."0x82C2CE58"]
name = "Identity_ReturnArg2_82C2CE58"

# A process-elements loop: walk a linked structure and, per element, dispatch 4
# vmethods (slots 0-3) on a target object.

# Args: obj (r3, ->0 = a vtable, ->4 = the end marker), target (r4, an object whose
# ->0 is a vtable), plus two values (r5, r6). Loop while the current element (r29,
# starting at *obj->0) != obj->4: each iteration calls four methods on target via its
# vtable -- target->vtable[0](target, elem->16), target->vtable[1](target, r5),
# target->vtable[2](target, prevResult), target->vtable[3](target, r6) -- then
# advances the element to elem->0 or elem->4 (chosen by the call results). When the
# element reaches obj->4, stop and return 0. 24 callers (sub_82C53B88, sub_82C14688,
# sub_82C02730, sub_82C17460, ...). The object class and the four methods aren't
# resolvable, so the name states the mechanism: process the elements, each with a
# 4-vcall chain on the target.
[entrypoint.functions."0x82C46C10"]
name = "ProcessElements_Vcall4_82C46C10"

# A constructor/initializer for a 12-byte (three u32 field) object from a value,
# with a small/large distinction (a tagged-value init).

# Args: obj (r3, zero-initializes obj->4, obj->8, obj->12), value (r4). If value ==
# 0, return 0 (an empty object). Otherwise, if value <= 0x3FFFFFFF (fits in 30 bits, a
# small/inline value): the small-value init path. Else (value > 0x3FFFFFFF, a pointer/
# large value): build a local string via SetString_822F2020 (from a global string) and
# convert it via sub_826C3EF0 into the object's fields. Returns a value the callers
# mask to a byte (clrlwi r3, 24 -- a status/size). 3 callers (sub_82B4BFD8, 
# sub_82B4B2B8, sub_82B4D300), all in the 0x82B4 data-processing region. The object
# class and the exact meaning of the three fields aren't resolvable, so the name
# states the mechanism: construct/init the object from a value (inline if small,
# converted if large).
[entrypoint.functions."0x82B4CB58"]
name = "Construct_FromValue_82B4CB58"

# A 7-way state dispatch (state-machine switch) on the byte (obj->4 - 4) (values
# 0-6) -- a switch/jump-table dispatcher, a sibling of StateDispatch_82BCBB40.

# Args: obj (r3), state obj (r4, whose ->4 is the switch key and ->5 a status byte).
# Clears the low 2 bits of obj->5 (stb obj->5 = obj->5 & 0xFFFFFFFC), computes the
# key = obj->4 - 4, and (if key is in 0..6) switches on it via a function-pointer
# table; each case (0-6) performs inline register operations (no external sub-calls --
# the case bodies manipulate obj's fields directly). 17 callers (sub_822DF280, 
# sub_82BCC6F0, sub_82BD3A90, sub_82BC56F8, sub_82BCB7E8, ...). The individual states
# and their effects aren't resolvable, so the name states the mechanism: a 7-way
# dispatch on a state byte.
[entrypoint.functions."0x82BCA7D8"]
name = "StateDispatch_82BCA7D8"

# A memmove (overlap-safe copy): copy `size` bytes from src to dst, handling the
# case where the regions overlap.

# Args: dst (r3), src (r4), size (r5). If dst == src, return (nothing to do). If dst
# < src (no dangerous overlap), delegate to the forward SDK copy -- 
# MemCpy_SdkRuntime_82CA2C60(dst, src, size). Otherwise (dst >= src, a backward
# overlap): copy BACKWARDS so the source isn't clobbered -- first byte-by-byte until
# dst is 4-byte aligned (from the end: *--dst = *--src), then 4-byte word-by-word
# backwards. Returns void. 80 callers (sub_82171010, sub_8319BA60, Func_82CB4CB8,
# sub_82BAF318, Exception_82CC031C, ...). This is the classic memmove: a copy that is
# safe when src and dst overlap. Sits next to MemCpy_SdkRuntime in the memory family.
[entrypoint.functions."0x82CAA2E0"]
name = "MemMove_SdkRuntime_82CAA2E0"

# A SIGNED 3-way comparator of two 32-bit values: returns -1 if a < b, 0 if a == b,
# 1 if a > b (where a = *r4 and b = *r5).

# Args: (r3), a wrapper (r4, a = *r4), b wrapper (r5, b = *r5). Compares a and b
# (signed, cmplw/bge): if a < b, return -1. Otherwise (a >= b): compute the borrow of
# (b - a) via subfc/subfe and return its LSB -- which is 0 when a == b (no borrow) and
# 1 when a > b (b - a underflows, borrow set). So the return is -1 / 0 / 1 for
# less-than / equal / greater-than -- a standard 3-way signed compare (like std::cmp / 
# the sign of a - b). 0 direct callers, so it is most likely a VIRTUAL comparison
# method (a comparator used by a sort/search). The operand class (a 4-byte value
# deref'd from r4/r5) isn't resolvable, so the name states the mechanism: a signed
# 3-way compare of two 32-bit values.
[entrypoint.functions."0x82C13458"]
name = "Compare3Way_Signed32_82C13458"

# A vtable-slot-12 call wrapper: deref obj, load vtable offset 48 (slot 12), and call
# it with obj as the sole argument, returning the result.

# Arg: obj (r3, whose ->0 is a vtable). Loads *(obj->0) = the vtable, then vtable[48]
# (slot 12, 48/4), and calls it with obj (bctr). Returns the callee's result. 1 caller:
# sub_82C14E08. The method (slot 12) isn't resolvable without the vtable, so the name
# states the mechanism: call the object's vtable slot 12.
[entrypoint.functions."0x82C010D0"]
name = "VcallSlot12_82C010D0"

# A 3-step vcall chain (slots 6 -> 12 -> 7), each step gated on the previous result's
# low byte being nonzero -- a sibling of VcallTriple_AllSuccess (6,8,7) and
# VcallChain_AllSuccess (6,14,7).

# Args: obj (r3, whose ->0 is a vtable), (r4), value (r5). Step 1: vcall
# obj->vtable[6] (offset 24); if the result's low byte == 0, return 0. Step 2: vcall
# slot 12 via VcallSlot12_82C010D0(obj, value); if the result's low byte == 0, return
# 0. Step 3: vcall obj->vtable[7] (offset 28); return (result's low byte != 0). So it's
# an all-or-nothing chain of three virtual calls (6, 12, 7), returning 1 only if all
# three report success (nonzero byte). 2 callers (sub_82C1E360, sub_82C15100). The
# methods (slots 6/12/7) aren't resolvable without the vtable, so the name states the
# mechanism: a 3-step vcall chain, slots 6 -> 12 -> 7, all-or-nothing.
[entrypoint.functions."0x82C14E08"]
name = "VcallChain_6_12_7_82C14E08"

# A vtable-slot-0 call with a null check and a flag of 1 -- the classic virtual
# destructor-call shape (obj->vtable[0](obj, 1)).

# Arg: obj (r3). If obj == null, return (no-op). Otherwise load *(obj->0) = the vtable
# and call vtable[0] (offset 0 / slot 0) with obj and r4 = 1 (bctr). Returns the
# callee's result. 0 direct callers, so it is most likely invoked VIRTUALLY (a
# destructor/dispose entry) -- vtable slot 0 is typically the destructor in the Itanium
# C++ ABI, and the r4 = 1 is plausibly a "deleting"/"full" flag. The exact class and
# whether slot 0 is the destructor aren't resolvable here, so the name states the
# mechanism: null-check, then call the object's vtable slot 0 with a flag of 1.
[entrypoint.functions."0x829681C0"]
name = "VcallSlot0_Flag1_829681C0"

# A refcounted value setter that also builds an owning 12-byte wrapper: release the
# current obj->0, store the new value, and (if non-null) allocate a refcounted wrapper.

# Args: obj (r3, an 8-byte object: obj->0 = the value, obj->4 = a wrapper slot), value
# (r4). First releases the current obj->0 via Release_RefCounted_829FF648, then sets
# obj->0 = value. If value == null, set obj->4 = 0 and stop. Otherwise allocate a 12-byte
# block via Allocate_OrDbgBreak_8221F388(12) and initialize it as a wrapper -- wrapper->0
# = 1 (a refcount), wrapper->4 = a fixed constant (a type/offset), wrapper->8 = value --
# then set obj->4 = that wrapper (or 0 if the allocation failed). Returns void. 171
# callers (sub_8290DA78, sub_8246A048, sub_82807978, ...). The wrapper's exact role (the
# constant and what obj->4 is used for) isn't resolvable, so the name states the
# mechanism: set a refcounted value and, for a non-null value, allocate + populate a
# 12-byte owning wrapper.
[entrypoint.functions."0x8238A848"]
name = "SetRefcountedWrapper_8238A848"

# A vtable-slot-3 call on the sub-object obj->0 (offset 12 / slot 3) that passes a
# value pointer and returns a BIT derived from the result -- a member of the recurring
# slot-3 interface.

# Args: obj (r3, whose ->0 is the sub-object and ->5 a flag byte), value (r4). A bit
# test on obj->5 (via clz/rotate/xor) selects which stack slot holds the value pointer
# (a 2-element struct {value, 0}). Then vcall `(*obj->0)->vtable[3](obj->0, &{value, 0},
# 1)` (vtable slot at offset 12). Finally it returns a single bit derived from the
# result (clz of the result, then rotate-right-27 and mask 1). 8 callers (sub_8236D3C8,
# sub_82C85538, sub_82C84838, sub_82A4AF78, sub_82C680F8, sub_82B483D0, sub_82C85758,
# sub_82365458). The slot-3 method isn't resolvable without the vtable, so the name
# states the mechanism: a slot-3 vcall that returns a bit.
[entrypoint.functions."0x82372268"]
name = "VcallSlot3_ReturnBit_82372268"

# A get-or-create-by-name: hash a name, binary-search a 12-byte-stride array for it,
# and (if not found) insert a new element.

# Args: obj (r3, whose member at +4 is the array: start/current/end, 12-byte stride),
# name (r4, a string). First hashes the name via InitNameHash_8236D358 into a local. 
# Validates the array ranges (start <= current, current <= end, else `trap 22`). Then
# binary-searches the array (12-byte stride) comparing each element's key (element->0,
# the stored name hash) to the computed hash. On a miss it inserts a new element via
# Array_InsertOne_8236FDD8. So it's a find-or-create keyed on the name's hash. 2
# callers (sub_82371820, sub_8236DD48). The element class (a 12-byte struct holding a
# name hash + the name) isn't fully resolvable, so the name states the mechanism:
# get-or-create an entry by name hash.
[entrypoint.functions."0x8236E1B0"]
name = "GetOrCreateByNameHash_8236E1B0"

# A constant-return global getter: loads and returns a fixed global address (a
# 2-insn function: lis r11, <hi>; addi r3, r11, <lo>; blr -- ~0x83654CE0).

# Takes no meaningful arguments and always returns the same global pointer. 0 direct
# callers, so it is most likely a VIRTUAL method (a getter for a global/singleton, or
# an accessor whose value is that fixed address) -- the exact global's meaning needs
# the vtable + class. The name states what it does: return a fixed global pointer.
[entrypoint.functions."0x82C1E350"]
name = "GetGlobal_82C1E350"

# The SDK memset: fill `size` bytes at dst with the byte value (r3 = dst, r4 = value,
# r5 = size).

# Implementation: fill byte-by-byte (*r6++ = r4) until dst is 4-byte aligned; then build
# a 32-bit word from the byte (rlwimi r4 = 0xNNNNNNNN, the byte replicated 4 times) and
# fill word-by-word in 16-byte chunks (four stw per iteration); finally handle the
# remaining tail bytes. Returns void. 479 callers (MemFree_82CBFD74, sub_831D9228,
# sub_831CE8D8, ...). This is the SDK's memset -- the memory-family sibling of
# MemCpy_SdkRuntime / MemMove_SdkRuntime. Named MemSet_SdkRuntime to keep the guest
# symbol distinct from the host libc memset.
[entrypoint.functions."0x82CA3190"]
name = "MemSet_SdkRuntime_82CA3190"

# A remove-and-free tree-node wrapper: remove obj->4 from the tree, then free it and
# clear the slots.

# Arg: obj (r3, an 8-byte object: obj->4 = the tree node, obj->8 = a related field). 
# Calls Tree_RemoveNode_826A4A20(obj, obj->4, &out1, &out2) to detach the node from the
# tree (the two out pointers are stack slots receiving the removal result), then frees
# the node via Free_SizeBucketed_Thunk_8221BE68(obj->4), then sets obj->4 = 0 and obj->8
# = 0. Returns void. So it's a detach-and-destroy of the node stored in obj->4. 20
# callers (sub_832AB150, sub_8281FB68, sub_82441E90, sub_832AF290, sub_82724AE8, ...). 
# The node/tree class isn't resolvable, so the name states the mechanism: remove the
# node from the tree and free it.
[entrypoint.functions."0x826A4978"]
name = "RemoveAndFreeNode_826A4978"

# A tree find-and-process: walk the tree to locate the node / insertion point for a
# key, then process it -- a sibling of Tree_Insert_82A00620 (same node layout: ->8 right,
# ->0 parent, ->12 key, ->25 state byte).

# Args: obj (r3, ->4 = first node), key wrapper (r4, *r4 = the key). Walks the tree --
# if node->12 < key step right (node->8), else step up to the parent (*node) -- until a
# node with a nonzero ->25 state byte (the target). Then it processes the located node:
# compares it to the key, and (depending on the result) calls sub_82A122F8 and
# Release_RefCounted_829FF648 (releasing a node's payload). 166 lines, 1 caller:
# sub_83218598. The exact find-vs-remove semantics and the node class aren't fully
# resolvable, so the name states the observable mechanism: walk the tree to find the
# node for a key, then process it (release + sub_82A122F8).
[entrypoint.functions."0x82A120B0"]
name = "Tree_FindNode_82A120B0"

# A large (364-line) tree-node processor: the slow-path handler called by
# Tree_FindNode_82A120B0, with a fast path (sub_82A128B0) and a validated slow path.

# Args: (r3), obj (r4, an object with ->4 and ->8 fields), plus r5 and r6. If obj->8 ==
# 0 (fast path): call sub_82A128B0(obj->4, r6, 1) and return. Otherwise (slow path):
# validate (obj->4 != 0 and a pointer-equality check on r5, else `trap 22`) and perform
# the heavier node processing (sub_823D4F20, sub_82498700, sub_82A126D0, sub_82A128B0).
# 1 caller: Tree_FindNode_82A120B0. The node/tree class and the exact processing aren't
# resolvable, so the name states the observable role: process a tree node (fast or slow
# path). It is the deep handler behind the Tree_FindNode walk.
[entrypoint.functions."0x82A122F8"]
name = "ProcessTreeNode_82A122F8"

# A large (329-line) node string-processing handler in the 0x82A1 tree cluster -- the
# fast-path / deep handler behind ProcessTreeNode_82A122F8.

# Args: (r3), obj (r4, ->8 = a size/length field), plus r5, r6, r7. Compares obj->8 to
# a constant (0x01555554): if below it, one path (loc_82A12928). Otherwise: build a local
# string via SetString_822F2020 (from a global string), convert it via sub_826C3EF0, then
# further process it via sub_822F1F00 and sub_826C3FA8, and (in the longer path) call
# sub_82171810, sub_827C6448, sub_827C64C0, and sub_82A12B90. 2 callers (ProcessTreeNode_
# 82A122F8, sub_82A126D0). The node class and the exact string transform aren't fully
# resolvable, so the name states the observable role: process a node's string (a
# build/convert/format path selected by obj->8).
[entrypoint.functions."0x82A128B0"]
name = "ProcessNodeString_82A128B0"

# A 28-byte node allocation + initialization (a "make a node" operation) -- the
# allocation step called by ProcessNodeString_82A128B0.

# Args: (r3), a (r4), b (r5), c (r6), src (r7). Allocates 28 bytes via
# Allocate_OrDbgBreak_8221F388(28). On success, initializes the block: block->0 = a,
# block->4 = b, block->8 = c, block->12 = src->0, block->16 = src->4, block->20 = src->8.
# If src->8 (a linked field) is nonzero, it acquires the global lock (REX_ENTER_GLOBAL_
# LOCK) and does an atomic load (lwarx) on src->8 (a lock-free linked-list append/splice
# under the global lock). Returns the allocated block (or null on allocation failure).
# 1 caller: ProcessNodeString_82A128B0. The node class isn't resolvable, so the name
# states the mechanism: allocate a 28-byte node and initialize it from the args + a
# source object.
[entrypoint.functions."0x82A12B90"]
name = "AllocateNode28_82A12B90"

# A 20-byte object constructor: set the vtable (obj->0) and allocate + zero a 20-byte
# payload.

# Arg: obj (r3). Sets obj->0 to a fixed global (~0x82C3BBC0, plausibly a vtable/
# type-object pointer). Then allocates 20 bytes via Allocate_OrDbgBreak_8221F388(20) and
# zeroes the block (writes 0 to each 4-byte slot at alloc+0/+4/+8/... -- with null and
# wraparound guards on the pointer arithmetic). Returns void. 21 callers (sub_83215B98,
# sub_832159E0, sub_83215D98, sub_83215768, ...), all in the 0x83215 constructor cluster. 
# The object class and the vtable's meaning aren't resolvable, so the name states the
# mechanism: construct a 20-byte object (install the vtable, allocate + zero the 
# payload).
[entrypoint.functions."0x83215640"]
name = "Construct20Byte_83215640"

# The DESTRUCTOR counterpart of Construct20Byte_83215640 (same vtable ~0x82C3BBC0):
# destroy the tree and free the node.

# Arg: obj (r3). Sets obj->0 to the fixed global (~0x82C3BBC0, the same vtable the
# constructor installs). Then destroys the object's tree: DestroyTreeNodes_82859340(
# obj->8, obj->8->4) (recursively destroy the tree rooted at obj->8), performs cleanup
# (obj->12 = 0 and some self-reference rewiring of the obj->8 node), then calls
# RemoveAndFreeNode_826A4978(obj+4) to detach and free the node. Returns void. 31
# callers (sub_83215A70, sub_82A146C0, sub_82A14720, sub_83215D08, sub_82A14600, ...). 
# The object class isn't resolvable, so the name states the role: the destructor for
# the 20-byte tree-owning object -- install the vtable, destroy the tree, remove +
# free the node.
[entrypoint.functions."0x832156E0"]
name = "Destruct20Byte_832156E0"

# A refcounted value setter that updates a tree node's value: release the old value,
# store the new one, and increment the new value's refcount.

# Args: obj (r3, an object; the tree is at obj+12), value wrapper (r4, ->0 = a source,
# ->4 = the new value). First makes a vcall on *r4->0 (vtable slot 1 / offset 4). Then
# finds the target node in the tree via Tree_FindNode_82A120B0(obj+12, ...). If the node's
# ->4 field differs from the new value (r4->4): release the old value via
# Release_RefCounted_829FF648, set node->0 = r4->0 and node->4 = r4->4, and (if the new
# value is non-null) increment its refcount (value->0 += 1). So it's a COM-style
# refcounted set on a tree node: drop the old reference, take a new one. 3 callers
# (sub_83218630, sub_83219588, sub_83218C10), all in the 0x83218 cluster. The node/tree
# class isn't resolvable, so the name states the mechanism: update a tree node's
# refcounted value (release old, store new, inc refcount).
[entrypoint.functions."0x83218598"]
name = "UpdateTreeNodeValue_83218598"

# The in-place buffer-range update core for the small-buffer object (0x822F) -- grow if
# needed, clamp the length, shift the buffer, resize, and null-terminate.

# Args: obj (r3, a small-buffer object: obj->4 = inline buffer / external pointer,
# obj->20 = current length, obj->24 = size field used for the indirection), pos (r4), len
# (r5). If obj->20 < pos, grow via sub_82CD12C8 (the grow helper). Clamp len to
# (obj->20 - pos). If len == 0, do nothing. Otherwise resolve the buffer base via the
# small-buffer indirection (obj->24 >= 16 ? *obj->4 : &obj->4), then shift the buffer via
# CheckedCopy_82CA3808, decrease obj->20 by len (obj->20 -= len), and null-terminate at
# the new end. The observable effect (shift + shrink + null-terminate) is consistent with
# an in-place range erase / truncate. 3 callers (sub_82BED0C8, ProcessObject_82C669C0,
# RangeUpdate_8218EA38) -- it is the in-place core behind WriteRange_AtPos / RangeUpdate.
# The exact semantics (erase vs truncate vs replace) aren't fully resolvable, so the name
# states the mechanism: update the buffer in place over a range.
[entrypoint.functions."0x822F1F60"]
name = "UpdateBufferRange_822F1F60"

# A byte-append to a growable buffer (SDK region): decrement the remaining capacity,
# write the byte at the current position, and advance.

# Args: byte (r3), buffer obj (r4, ->0 = current write pointer, ->4 = remaining capacity,
# ->8 = a secondary pointer, ->12 = a flags word with a bit-5 flag), plus r5. Checks a
# flag bit (bit 5 of obj->12) that selects an alternate path (obj->8). In the main path it
# decrements obj->4 (remaining capacity); if capacity remains (obj->4 >= 0), writes the
# byte to *obj->0 and advances the write pointer (obj->0 += 1). Returns void. So it's a
# push/append of a single byte into the buffer with a capacity guard. 3 callers (sub_
# 8223F990, sub_82CAE4A8, sub_82CAE410 -- the last two are sibling SDK buffer functions). 
# The exact buffer class isn't resolvable, so the name states the mechanism: append a
# byte to the buffer (decrement capacity, store, advance).
[entrypoint.functions."0x82CAE378"]
name = "Buffer_PushByte_82CAE378"

# A build-object-from-array: derive a size from an array range, construct the object,
# and (if the range is non-empty) copy the array data in.

# Args: obj (r3, the object to build), arr (r4, a range: arr->4 = start, arr->8 = end). 
# Computes the element count (arr->8 - arr->4) >> 2 (i.e. the range's byte count / 4 --
# a count of 4-byte elements; 0 if arr->4 == 0). Calls Construct_FromValue_82B4CB58(obj,
# count) to construct obj with that size. If the construction reports a non-zero low byte
# (obj was built), validates the range (arr->4 <= arr->8, else `trap 22`) and copies the
# array data into obj via CheckedCopy_82CA3808. So it's a "populate obj from the arr
# range": derive the count, construct, then copy the bytes in. 5 callers (sub_82B4B9A8,
# sub_82B4C1C0, sub_82B4C7A0, sub_82B4B2B8, sub_82B4BF70), all in the 0x82B4B data-
# processing cluster. The object/array class isn't resolvable, so the name states the
# mechanism: construct the object from an array range (derive count, construct, copy).
[entrypoint.functions."0x82B4BFD8"]
name = "ConstructFromArray_82B4BFD8"

# A 28-byte-stride array push (sibling of Array_Push12_82C66E00): if there is room,
# initialize a new 28-byte element at the array's current position and advance.

# Args: obj (r3, the array: obj->4 = start, obj->8 = current, obj->12 = end, 28-byte
# stride). Computes the current element count (obj->8 - obj->4)/28 and the capacity
# (obj->12 - obj->4)/28. If there is no room (count >= capacity), return. Otherwise, at
# obj->8, initialize a new 28-byte element (zeroes some fields, sets a few constants --
# e.g. element->20 = 0) and sets up the element's data via RangeUpdate_8218EA38 and
# sub_82C643F0, then advances obj->8 by 28. Returns void. 2 callers (sub_82C83378,
# sub_82C67878). The element class (a 28-byte struct) isn't resolvable, so the name
# states the mechanism: push a 28-byte element into the array (room check, init, advance).
[entrypoint.functions."0x82C644D0"]
name = "Array_Push28_82C644D0"

# A ctype predicate: look up a character in the u16 ctype table and return a specific
# bit (bit 2 / 0x4) -- a member of the 0x82CA6AB0-0x82CA6BB0 ctype cluster.

# Arg: code (r3, a character/code). Loads the table base from a global (~0x82CA70C8),
# then loads the u16 entry table[code] (lhzx at base + code*2), and returns bit 2 of it
# (r3 = entry & 0x4). So it's a "is this char [property]" test where the property is
# bit 2 of the ctype table. Sibling sub_82CA6AF0 returns a different bit (the cluster
# has one function per ctype bit, e.g. bit 0 = _UPPER). 48 callers (sub_82A256D0,
# sub_82A17518, sub_82F6DF40, ...), each loading a byte from a string and testing it. The
# exact property bit 2 denotes isn't resolvable without the table definition, so the name
# states the mechanism: a ctype predicate returning bit 2 of the table entry.
[entrypoint.functions."0x82CA6B10"]
name = "Ctype_Pred_Bit2_82CA6B10"

# A self-contained tree find (12 callers, no external callees -- all processing inline)
# with the same node layout and walk as Tree_FindNode_82A120B0 and Tree_Insert_82A00620
# (->8 right, ->0 parent, ->12 key, ->25 state byte).

# Args: (r3), obj (r4, ->4 = first node), key wrapper (r5, *r5 = the key). Walks the tree
# -- if node->12 < key step right (node->8), else step up to the parent (*node) -- until a
# node with a nonzero ->25 state byte (the target). Then it processes the located node
# entirely inline (no external sub-calls): compares it to the key and rewrites the
# surrounding node pointers. Returns the located/adjusted node. 12 callers (sub_828D9110,
# sub_828D9FE8, sub_828DA098, sub_823385D0, sub_83234418, ...). The node class and the
# exact find-vs-rebalance semantics aren't resolvable, so the name states the mechanism:
# a self-contained tree find (walk to the insertion point, process inline) -- a sibling
# of Tree_FindNode_82A120B0.
[entrypoint.functions."0x82A12230"]
name = "Tree_FindNode2_82A12230"

# A chain-root finder: validate, then walk up a chain (via node->0) to the topmost node
# (the one whose byte-17 flag is set) and store it in obj->4.

# Arg: obj (r3, obj->0 = a base, obj->4 = a starting node). Validates (obj->0 != 0, else
# `trap 22`; the head node's byte-17 == 0, else `trap 22`). Then walks up the chain via
# node->0 (the parent link) until it reaches a node whose byte-17 flag is nonzero (the
# root/top of the chain), and stores that node in obj->4. There are two entry paths
# (depending on an intermediate node->17 check), but both converge on the same walk-up. 
# Returns void. 62 callers (sub_82B6A088, sub_83233B40, sub_825D3100, sub_82C7DC58, ...). 
# The node/chain class and the exact meaning of the byte-17 flag aren't resolvable, so
# the name states the mechanism: walk up the chain to find the root node and store it.
[entrypoint.functions."0x82A596F0"]
name = "FindChainRoot_82A596F0"

# A refcounted copy-assignment (operator=) -- a second variant, a sibling of
# CopyAssign_RefCounted_821F0108 (the primary, 2524 callers). This one's refcount lives
# at value->12 and is incremented atomically under the global lock.

# Args: dst (r3, dst->0 = the stored value), src (r4, src->0 = the stored value). If
# dst == src (self-assign), do nothing. Otherwise: if dst->0 != 0, release it via
# Release_RefCounted_821C67D8. Then if src->0 != 0: set dst->0 = src->0 and atomically
# increment src->0's refcount at (src->0 + 12) (lwarx/stwcx under REX_ENTER_GLOBAL_LOCK). 
# So it's a COM-style refcounted operator=: drop the old reference, take a new one. 593
# callers (sub_82B46E78, sub_8230DFF0, sub_825A20A8, ...). The wrapper class isn't
# resolvable, so the name states the mechanism: a refcounted copy-assign (release old,
# copy, inc refcount) -- the "2" distinguishes it from CopyAssign_RefCounted_821F0108.
[entrypoint.functions."0x82265160"]
name = "CopyAssign_RefCounted2_82265160"

# A refcounted object DESTRUCTOR (one of the hottest functions in the game, 5636
# callers): release the object's value and atomically decrement a global live-object
# counter.

# Arg: obj (r3). Releases obj->0 via Release_RefCounted_821C67D8 (drops the reference to
# the stored value). Then atomically decrements a global counter at a fixed address
# (~0x82C489E0) via lwarx/stwcx under REX_ENTER_GLOBAL_LOCK -- a global live-object /
# allocation count. Returns void. 5636 callers (sub_832A62A0, sub_82492428, 
# sub_82663CA0, ...). The object class and the exact meaning of the global counter aren't
# resolvable, so the name states the mechanism: destruct a refcounted object (release its
# value, decrement the global count).
[entrypoint.functions."0x82214F08"]
name = "DestructRefCounted_82214F08"

# A single-character tolower (ASCII): lowercase a u16 char if it is A-Z.

# Arg: char (r3, a u16 code). If the char == 0xFFFF, return it unchanged. If it is in the
# range A-Z (65-90), add 32 (A-Z -> a-z) and return. Otherwise return the char unchanged.
# So it's a basic ASCII tolower for one character (no table lookup). A simpler sibling of
# ToLower_SdkRuntime_82CA4288 (which uses the ctype table). 10 callers (sub_82F70138,
# sub_82F701F8, sub_8238E1E8, sub_82F705E8, ...), mostly in the 0x82F7 region. Named
# ToLower_Ascii to distinguish the table-less single-char version from the SDK table-
# based one.
[entrypoint.functions."0x8217A980"]
name = "ToLower_Ascii_8217A980"

# A hash-table lookup (linear probing / open addressing): find a key in a flat hash
# table.

# Args: table (r3, ->0 = the bucket array base, ->4 = bucket count), key (r4). Computes
# the starting bucket as (key & (count - 1)) * 8 (8-byte slots), then linearly probes
# (wrapping around the table) comparing each stored entry to the key; stops on a match
# (entry == key) or an empty slot (entry == 0). Returns the found slot/index (or a
# not-found marker). So it's a find-in-hash-table with open addressing. 1 caller:
# sub_829FAC30 (a sibling, plausibly the insert / get-or-create counterpart). The table
# key type isn't resolvable, so the name states the mechanism: a linear-probing hash
# table lookup.
[entrypoint.functions."0x829FAD30"]
name = "HashTable_Find_829FAD30"

# A RECURSIVE structure processor: walk a linked structure of nodes (node->4 = a type
# byte, node+84 = a sub-node), looping over a count and recursing on sub-nodes.

# Args: obj (r3, ->16 = a context with a ->20 flag byte), node (r4, the current node),
# count (r5). Reads the context flag (obj->16->20, xored with 3). Loops over `count`
# elements: for each, if the node's type byte (node->4) == 8, recurse on the sub-node
# (sub_82BCB610(obj, node+84, -3)) -- processing nested structure; otherwise handle it
# inline (sub_82BCB480). Returns void. 119 lines. 2 external callers (sub_82BC7D80,
# StateDispatch_82BCBB40 -- a state dispatch) plus its own recursion. The node/structure
# class and the meaning of the type byte (8 = has-sub-node) aren't resolvable, so the
# name states the mechanism: a recursive processor over a linked structure of nodes.
[entrypoint.functions."0x82BCB610"]
name = "ProcessStructure_Recursive_82BCB610"

# A trivial field getter: return obj->4 (a 2-insn function: lwz r3, 4(r3); blr).

# Arg: obj (r3). Returns the u32 at obj->4. 23 callers (sub_82C80EB8, sub_824D4190,
# sub_82397348, sub_82C81578, ...). It's a plain accessor for the object's field at
# offset 4; the field's meaning needs the object class. Named for what it does: get the
# field at offset 4.
[entrypoint.functions."0x82A46DE8"]
name = "GetField4_82A46DE8"

# A vtable-slot-3 call on the sub-object obj->0 (offset 12 / slot 3) with a flag of 4
# (r5 = 4) -- a member of the recurring slot-3 interface, a sibling of
# VcallSlot3_ReturnBit_82372268 (which uses r5 = 1).

# Args: obj (r3, ->0 = the sub-object, ->4 = a gate byte, ->5 = a flag byte), value
# (r4). If obj->4 == 0, take an alternate path. Otherwise a bit test on obj->5 (via
# clz/rotate/xor) selects which stack slot holds the value pointer (a 2-element struct
# {value, 0}), then vcall `(*obj->0)->vtable[3](obj->0, &{value, 0}, 4)` (vtable slot at
# offset 12, with the 3rd arg = 4 instead of the sibling's 1). 2 callers (sub_82372108,
# sub_823715E8). The slot-3 method isn't resolvable without the vtable, so the name
# states the mechanism: a slot-3 vcall on the sub-object with a flag of 4.
[entrypoint.functions."0x82372070"]
name = "VcallSlot3_Flag4_82372070"

# A constructor/copy for a struct holding three floats plus an optional 40-byte
# payload: copy the three floats and (if the source has a payload) allocate + copy a new
# one.

# Args: dst (r3, ->0 = a payload pointer, ->4/->8/->12 = three floats), src (r4, same
# layout). Sets dst->0 = 0. Copies the three floats (src->4/8/12 -> dst->4/8/12). If
# src->0 (the payload pointer) is non-null: allocate 40 bytes via
# Allocate_OrDbgBreak_8221F388(40) and copy the source payload into it (dst->0 = the new
# payload). Returns void. 179 lines. 4 callers (sub_82A0BAA0, sub_82A0B5C8, sub_82A0B300
# -- all in the 0x82A0B cluster -- plus sub_8321F968). The struct's purpose (the three
# floats -- plausibly a vector/position -- and the 40-byte payload) aren't resolvable, so
# the name states the mechanism: construct the struct from a source (copy 3 floats, plus
# an optional 40-byte payload).
[entrypoint.functions."0x82A0C0A0"]
name = "Construct3FloatPayload_82A0C0A0"

# A vtable-slot-5 call wrapper: deref obj, load vtable offset 20 (slot 5), and call it
# with obj as the sole argument, returning the result.

# Arg: obj (r3, whose ->0 is a vtable). Loads *(obj->0) = the vtable, then vtable[20]
# (slot 5, 20/4), and calls it with obj (bctr). Returns the callee's result. 0 direct
# callers, so it is most likely a VIRTUAL method (a wrapper that forwards to the
# object's own vtable slot 5 -- a delegating thunk). The method (slot 5) isn't
# resolvable without the vtable, so the name states the mechanism: call the object's
# vtable slot 5.
[entrypoint.functions."0x82C1DC40"]
name = "VcallSlot5_82C1DC40"

# A string comparator (3-way): compare two string-wrapper objects (their ->0 holds the
# string pointer) using the signed string compare.

# Args: (r3), a (r4, a->0 = a pointer to a's string), b (r5, b->0 = a pointer to b's
# string). If a->0 == b->0, return 0 (equal). If a->0 == 0, return 1 (a is null). If
# b->0 == 0, return 0 (b is null). Otherwise both are non-null: StrCmp_Signed_8226D7A8
# (*a->0, *b->0) and return the sign bit of the result (negative/zero/positive -- a
# 3-way ordering). 42 callers (sub_825D8308, sub_822B84F0, sub_825D7718, ...). A
# string-ordering predicate for the string-wrapper class. Named CompareStrings to state
# the mechanism: compare two (wrapper) strings and return the ordering.
[entrypoint.functions."0x8228B5A0"]
name = "CompareStrings_8228B5A0"

# A doubly-linked-list splice: reorder / remove a node in a list by rewiring the
# ->0 / ->4 / ->8 links.

# Args: obj (r3, ->4 = a list context), node (r4, the node to splice; ->0 prev, ->4
# next, ->8 a link). Reads node->8's target, checks a ->25 flag on it, and rewires the
# node's prev/next pointers (and the neighboring nodes' links) to splice the node into
# or out of the list -- a classic doubly-linked-list manipulation. Returns void. 67
# lines, 0 external callees (all inline). 30 callers (sub_8234DB40, sub_82366668,
# sub_8278C580, ..., ProcessNodeString_82A128B0, ...). The list element class isn't
# resolvable, so the name states the mechanism: splice a node in a doubly-linked list.
[entrypoint.functions."0x827C6448"]
name = "List_SpliceNode_827C6448"

# A vtable-slot-8 call wrapper: deref obj, load vtable offset 32 (slot 8), and call it
# with obj as the sole argument, returning the result.

# Arg: obj (r3, whose ->0 is a vtable). Loads *(obj->0) = the vtable, then vtable[32]
# (slot 8, 32/4), and calls it with obj (bctr). Returns the callee's result. A sibling
# of VcallSlot5_82C1DC40 (same wrapper shape, different slot). 1 caller:
# sub_82C4A410. The method (slot 8) isn't resolvable without the vtable, so the name
# states the mechanism: call the object's vtable slot 8.
[entrypoint.functions."0x82C1DC70"]
name = "VcallSlot8_82C1DC70"

# A vtable-slot-1 call on an embedded sub-object: deref obj->4, treat (obj->4 + 12) as
# `this`, and call its vtable slot 1.

# Arg: obj (r3, ->4 = a container). Sets this = obj->4 + 12 (an embedded object at
# offset 12 within the container). Loads the vtable at (obj->4 + 12)->0, then vtable[4]
# (slot 1), and calls it with this (bctr). Returns the callee's result. 0 direct
# callers, so it is most likely a VIRTUAL method (a delegating thunk on the embedded
# sub-object). The method (slot 1) and the container class aren't resolvable, so the
# name states the mechanism: call slot 1 on the sub-object embedded at obj->4 + 12.
[entrypoint.functions."0x82C14D40"]
name = "VcallSlot1_SubObj12_82C14D40"

# A binary-search-tree walk (search/insert point): walk the tree from the root, comparing
# the key at each node, following the child pointer indicated by the comparison.

# Args: obj (r3, ->0 = the root node, ->4 = the key), key (r4), dst (r5, a 16-byte
# scratch/result). Sets dst->0 = dst->4 = obj->4 (the key). Starting from the root
# (obj->0), loads each node and compares the key to the node's value via sub_82C46A30
# (the comparator); follows the appropriate child pointer (node->0 or node->8) based on
# the comparison result, until it reaches the target / null. Returns the found node
# (or the insertion point). 104 lines. 1 caller: sub_82C46CF8 (a sibling, also in the
# TODO -- plausibly the get-or-create wrapper). The tree key type and the comparator
# (sub_82C46A30) aren't resolvable, so the name states the mechanism: a BST search walk.
[entrypoint.functions."0x82C46AF0"]
name = "Tree_Search_82C46AF0"

# A tree get-or-create (insert): search for an entry in the tree and, if absent, create
# and insert a new node at a computed offset.

# Args: obj (r3, the tree container), src (r4, a source / key wrapper, ->0 = a vtable
# object), offset (r5). Makes a vcall on src (vtable slot 4, offset 16), computes the
# destination as (obj + offset), then calls Tree_Search_82C46AF0 (sub_82C46AF0) to locate
# the entry; afterwards it initializes the resulting node (sets flags at ->12, ->16, and
# links it) -- so it's a get-or-create: find the key in the tree, creating a node if not
# present. Returns the node. 355 lines. 18 callers (sub_82C13F50, sub_82C50270,
# sub_82C1E2E0, ...). The tree key type and node layout aren't resolvable, so the name
# states the mechanism: search the tree and create a node if the entry is missing.
[entrypoint.functions."0x82C46CF8"]
name = "Tree_GetOrCreate_82C46CF8"

# A large (449-line) self-contained tree walk: traverse a BST, rewiring node links and
# setting a boolean flag (node->12) -- part of the 0x82C46 tree subsystem.

# Args: obj (r3, ->4 = a value), node (r4, a tree node with ->0 / ->4 / ->8 child links
# and a ->12 flag). Two entry paths (depending on whether node->12 is set). Walks the
# tree (a loop at loc_82C46FF4) comparing the running pointer to the target, following
# the child links (node->8 / ->4 / ->0), and rewiring the links as it goes. At the end it
# sets node->12 to 0 or 1 (a boolean flag). Returns void. 449 lines, 0 external callees
# (the whole tree manipulation is inline). 1 caller: sub_82C47318 (a sibling, also in the
# TODO). A core operation of the 0x82C46 BST subsystem (siblings: Tree_Search_82C46AF0,
# Tree_GetOrCreate_82C46CF8). The exact operation (walk + rewire + set a flag) is the
# resolvable mechanism; the node/key class isn't.
[entrypoint.functions."0x82C46FD8"]
name = "Tree_Walk_82C46FD8"

# A tree node-locate-and-process operation: find the target node in the tree (fast path
# or a search) and then process/rewire it -- part of the 0x82C46 tree subsystem.

# Args: obj (r3, ->4 = a value/key), node (r4, a starting node). If node->0 or node->4
# matches obj->4, use that node directly; otherwise call sub_82C46BA0 (a tree search) to
# locate the node. Then process the located node: compare it to the value, set up the
# child pointer (node->4), and call Tree_Walk_82C46FD8 (sub_82C46FD8) to rewire the
# surrounding tree. Returns void. 195 lines. 1 caller: sub_82C47470 (a sibling, also in
# the TODO). Siblings in the 0x82C46 BST subsystem: Tree_Search_82C46AF0,
# Tree_GetOrCreate_82C46CF8, Tree_Walk_82C46FD8. The node/key class and whether the
# rewire is specifically a removal aren't fully resolvable, so the name states the
# mechanism: locate a tree node and process (rewire) it.
[entrypoint.functions."0x82C47318"]
name = "Tree_ProcessNode_82C47318"

# A thin tree-process wrapper: make a vcall on the source, then dispatch to
# Tree_ProcessNode_82C47318 -- the 17-caller entry point of the 0x82C46 tree-process path.

# Args: obj (r3, the tree container), src (r4, a source wrapper, ->0 = a vtable object),
# offset (r5). Makes a vcall on src (vtable slot 4, offset 16), computes (src + offset),
# then calls Tree_ProcessNode_82C47318 (sub_82C47318) with obj and (src + offset). Returns
# void. 17 callers (sub_82C55F10, sub_82C103E8, sub_82C0CE28, ...). A wrapper in the
# 0x82C46 BST subsystem that prepares the call (vcall) and then processes the tree node.
# Named for what it does: a wrapper that dispatches to the tree-process path.
[entrypoint.functions."0x82C47470"]
name = "Tree_Process_Wrapper_82C47470"

# A reset/clear: zero four fields of an object (a 5-insn function).

# Arg: obj (r3). Sets obj->0, obj->4, obj->8, and obj->16 all to 0 (note it leaves
# offset 12 alone). Returns void. 18 callers, all in the 0x82C0-0x82C5 tree region
# (sub_82C0AF00, sub_82C0CC98, sub_82C0E5D8, sub_82C50270, sub_82C13F50, ...), so this
# clears a tree-node / tree-object back to its empty state. The object class isn't
# resolvable, so the name states the mechanism: reset the (tree) object's fields to zero.
[entrypoint.functions."0x82C474C8"]
name = "ResetTreeObj_82C474C8"

# A vtable-slot-10 call wrapper: deref obj, load vtable offset 40 (slot 10), and call it
# with obj as the sole argument, returning the result.

# Arg: obj (r3, whose ->0 is a vtable). Loads *(obj->0) = the vtable, then vtable[40]
# (slot 10, 40/4), and calls it with obj (bctr). Returns the callee's result. A sibling
# of VcallSlot12_82C010D0 (0x30 bytes away, slot 12) in the 0x82C010 vcall-wrapper family.
# 3 callers (sub_82C14EA0 -- also in the TODO -- plus sub_82EE42B8, sub_82EE3F70). The
# method (slot 10) isn't resolvable without the vtable, so the name states the mechanism:
# call the object's vtable slot 10.
[entrypoint.functions."0x82C010A0"]
name = "VcallSlot10_82C010A0"

# A vtable-slot-3 call on the sub-object at obj->16 (offset 12 / slot 3) with a flag of
# 2 (r5 = 2), returning a boolean derived from the result's sign -- a member of the
# recurring slot-3 interface.

# Args: obj (r3, ->16 = the sub-object), value (r4). Builds a 2-element struct {value, 0}
# on the stack, then vcall `(*obj->16)->vtable[3](obj->16, &{value, 0}, 2)` (vtable slot
# at offset 12, with the 3rd arg = 2). Takes the result, does a clz-based sign test, and
# returns (sign ^ 1) -- a 0/1 boolean. 0 direct callers, so it is most likely a VIRTUAL
# method. A slot-3 sibling (compare VcallSlot3_Bool_82C01E78 / _Bool_82C01E20,
# VcallSlot3_Flag4_82372070). The slot-3 method isn't resolvable without the vtable, so
# the name states the mechanism: a slot-3 vcall on obj->16 that returns a boolean.
[entrypoint.functions."0x82C01DC8"]
name = "VcallSlot3_Bool_82C01DC8"

# A multi-step vcall chain on obj: slot 6, then (if the result is nonzero) slot 10 via
# VcallSlot10_82C010A0, then (if that result is nonzero) slot 7 -- each step gated on the
# previous result's byte 3.

# Args: obj (r3, ->0 = a vtable), (r4), value (r5). Makes a vcall on obj (vtable slot 6,
# offset 24); if the result's top byte is nonzero, calls VcallSlot10_82C010A0 (sub_82C010A0,
# slot 10) with obj and value; if that result's top byte is nonzero, makes another vcall
# on obj (vtable slot 7, offset 28). Returns void. 86 lines. 1 caller: sub_82C15100 (a
# sibling, also in the TODO). A gated vcall chain (slots 6, 10, 7); a sibling of
# VcallChain_6_12_7_82C14E08 and VcallChain_AllSuccess. The slots aren't resolvable
# without the vtable, so the name states the mechanism: a gated slot-6 -> 10 -> 7 vcall
# chain.
[entrypoint.functions."0x82C14EA0"]
name = "VcallChain_6_10_7_82C14EA0"

# A complex vcall dispatcher (271 lines): make an initial vcall on the source, then
# orchestrate a sequence of vcall chains (VcallTriple_AllSuccess, VcallChain_AllSuccess,
# VcallChain_6_12_7, VcallChain_6_10_7, ...) based on each result.

# Args: obj (r3, a tree/object container), src (r4, a wrapper, ->0 = a vtable object),
# value (r5). Makes a vcall on src (vtable slot 5, offset 20); depending on the result it
# branches into a sequence of higher-level operations: VcallTriple_AllSuccess_82C011E8,
# VcallChain_AllSuccess_82C14F38, VcallChain_6_12_7_82C14E08, VcallChain_6_10_7_82C14EA0,
# plus DestructRefCounted_82214F08, sub_821F7798, sub_8228A4C0, and sub_82C14D70. So it's
# the top-level dispatcher that drives the object's virtual operation sequence. 271 lines.
# 1 caller: sub_82C168E0 (a sibling, also in the TODO). The object class and the exact
# semantics of each vcall chain aren't resolvable, so the name states the mechanism: a
# dispatcher that runs a gated sequence of vcall chains.
[entrypoint.functions."0x82C15100"]
name = "Dispatch_VcallSequence_82C15100"

# A multi-step vcall chain on obj: slot 6, then (if the result is nonzero)
# Dispatch_VcallSequence_82C15100, then (if that result is nonzero) slot 7 -- each step
# gated on the previous result's byte 3. Structurally like VcallChain_6_10_7_82C14EA0, but
# the middle step is the full dispatcher.

# Args: obj (r3, ->0 = a vtable), (r4), value (r5). Makes a vcall on obj (vtable slot 6,
# offset 24); if the result's top byte is nonzero, calls Dispatch_VcallSequence_82C15100
# (sub_82C15100) with value and obj; if that result's top byte is nonzero, makes another
# vcall on obj (vtable slot 7, offset 28). Returns void. 86 lines. 1 caller:
# sub_82C17E48 (a higher-level function). A gated vcall chain (slot 6 -> dispatch -> slot
# 7); a sibling of VcallChain_6_10_7_82C14EA0. The slots aren't resolvable without the
# vtable, so the name states the mechanism: a gated slot-6 -> dispatcher -> slot-7 vcall
# chain.
[entrypoint.functions."0x82C168E0"]
name = "VcallChain_6_Dispatch_7_82C168E0"

# A get-or-create for a 24-byte element: search for the element (via
# ProcessElements_Vcall4_82C46C10) and, if absent, allocate + initialize a new 24-byte
# node.

# Args: obj (r3, ->8 = a container), value (r4). Calls ProcessElements_Vcall4_82C46C10
# (sub_82C46C10) to search the container for the element; if it is found (result nonzero),
# returns 0 (the existing element). Otherwise it allocates 24 bytes via
# Allocate_OrDbgBreak_8221F388(24), resets it (ResetTreeObj_82C474C8), and inserts it
# (Tree_GetOrCreate_82C46CF8). So it's a get-or-create: find the 24-byte element, creating
# one if missing. 95 lines. 1 caller: sub_82C174D8 (a sibling, also in the TODO). Part of
# the 0x82C17/0x82C46 tree+element subsystem. The element class isn't resolvable, so the
# name states the mechanism: get-or-create a 24-byte element.
[entrypoint.functions."0x82C173C0"]
name = "GetOrCreate24Element_82C173C0"

# A process-element operation: search for the element (via ProcessElements_Vcall4_82C46C10)
# and then process it (via Tree_Process_Wrapper_82C47470) -- the "process" sibling of
# GetOrCreate24Element_82C173C0 (same caller, sub_82C174D8).

# Args: obj (r3, ->8 = a container), value (r4). Calls ProcessElements_Vcall4_82C46C10
# (sub_82C46C10) to search the container for the element; then calls
# Tree_Process_Wrapper_82C47470 (sub_82C47470) to process the located element. On some
# paths it frees (Free_Thunk_824FE010) or no-ops (NoOp_Stub_829CE870). 70 lines. 1 caller:
# sub_82C174D8 (a sibling, also in the TODO). Part of the 0x82C17/0x82C46 tree+element
# subsystem. The element class isn't resolvable, so the name states the mechanism: search
# for and process a 24-byte element.
[entrypoint.functions."0x82C17460"]
name = "Process24Element_82C17460"

# A binary-search lookup-and-handle for a 24-byte element: find the index (BinarySearch),
# then either create the element (GetOrCreate24Element) or process it (Process24Element).

# Args: obj (r3, ->4 = a container), key (r4). Calls BinarySearch_82C0BC08 (sub_82C0BC08)
# to find the index of the key in the sorted container (obj->4 + 52); then indexes a
# parallel array to fetch the associated element pointer, and dispatches to
# GetOrCreate24Element_82C173C0 (sub_82C173C0) or Process24Element_82C17460 (sub_82C17460)
# depending on the element's state. Returns the element (or null). 102 lines. 1 caller:
# sub_82C1ACD8. Part of the 0x82C17 tree+element subsystem (siblings: GetOrCreate24Element,
# Process24Element, BinarySearch_82C0BC08). The element class isn't resolvable, so the name
# states the mechanism: look up a 24-byte element by binary search and handle it.
[entrypoint.functions."0x82C174D8"]
name = "Lookup24Element_82C174D8"

# A binary search (4-byte stride over an array of pointers, comparing the dereferenced
# values): find the target and write a result byte.

# Args: obj (r3, ->0 = the array base, ->8 = the count), target (r4, ->0 = the target
# value), result (r5, an output byte). Sets *result = 0. Binary-searches the array
# (4-byte slots; each slot holds a pointer, and the comparison is on *slot): mid =
# (lo + hi) / 2, val = *array[mid]; if val > target, hi = mid - 1; if val == target, the
# result byte is set; else lo = mid + 1. Returns the index (or 0 if empty). 102 lines, 0
# external callees (all inline). 22 callers (sub_82C402B8, sub_82C26518, sub_82C29328,
# ...). A member of the binary-search family (sibling of BinarySearch_12Stride,
# BinarySearch_82C0BC08) -- the 4-byte pointer-array variant.
[entrypoint.functions."0x82C40EC0"]
name = "BinarySearch_4Stride_82C40EC0"

# A 5-way switch dispatch (a state/command dispatcher in the 0x82BCB region): set a flag
# bit, index a function-pointer table by (obj->36->4 - 5), and dispatch to one of five
# handlers.

# Arg: obj (r3, ->36 = a sub-object). Sets bit 2 of (obj->36)->5 (a flag). Computes an
# index = (obj->36)->4 - 5; if the index > 4, take the default path. Otherwise it loads a
# function pointer from a global table (indexed by the index) and calls it (bctr), then
# switches on the index (cases 0-4, each calling one of sub_82BCAA58 / sub_82BCAC98 /
# sub_82BCAE28 / sub_82BCAF40). So it's a state/command dispatcher keyed on the
# sub-object's byte-4 state. 271 lines. 2 callers (StateDispatch_82BCBB40 -- a state
# dispatcher in the same region -- plus sub_82BCB2A0). The states/commands aren't
# resolvable, so the name states the mechanism: a 5-way state dispatch (sibling of
# StateDispatch_82BCBB40 / StateDispatch_82BCA7D8).
[entrypoint.functions."0x82BCB0A8"]
name = "StateDispatch_82BCB0A8"

# A polynomial rolling hash of a C-string: a simple string hash/checksum.

# Arg: buffer (r3, a byte string). If buffer[0] == 0 (empty), return 0. Otherwise compute
# a rolling hash: crc = 0; for each byte (up to 100, or until a null byte): crc = crc * 
# 0x0001003F (65635) + signed_byte. Returns the hash. So it's a polynomial (base-65635)
# rolling hash of a null-terminated string, capped at 100 chars. 282 callers (sub_8328CC90,
# sub_8328E5F0, sub_8328D780, sub_823F75A0, ...), many in the 0x8328 region -- a high-value
# string-hash primitive. Named HashCstring to state the mechanism: hash a C-string with a
# polynomial roll.
[entrypoint.functions."0x82188CF0"]
name = "HashCstring_82188CF0"

# A trivial two-level getter: return (obj->0)->4, or 0 if obj->0 is null (a 5-insn
# function).

# Arg: obj (r3, ->0 = a pointer). If obj->0 == 0, return 0. Otherwise return *(obj->0 + 4)
# (the u32 at field 4 of the pointed-to object). 20 callers (sub_821EE858, sub_82A1A360,
# sub_82B3A9A8, ...). A plain accessor for a nested object's field at offset 4; the field's
# meaning needs the object class. Named for what it does: get field 4 of the object pointed
# to by obj->0.
[entrypoint.functions."0x821EEA78"]
name = "GetSubObjField4_821EEA78"

# A source-evaluation operation: conditionally run two slot-3 vcalls on the source object
# (updating its byte-4 flag) and copy a resulting value into the destination.

# Args: dst (r3), src (r4, ->4 = a byte flag). If src->4 != 0, calls
# SubObject_VcallSlot3_GatedStore32_82C842C8 (sub_82C842C8) with src and a stack slot, and
# stores the result back to src->4. Copies a stack value to dst->0. If src->4 is still
# nonzero, calls VcallSlot3_ReturnBit_82372268 (sub_82372268) and stores the result to
# src->4. So it evaluates the source (two slot-3 vcalls, gated on the flag) and writes a
# value to the destination. Returns void. 103 lines. 1 caller: sub_82371820. The object
# class and the slot-3 method semantics aren't resolvable, so the name states the
# mechanism: evaluate a source object (via two slot-3 vcalls) and copy out a value.
[entrypoint.functions."0x8236D3C8"]
name = "EvaluateSource_8236D3C8"

# A gated vcall sequence: call VcallTriple_AllSuccess_82C011E8 on the source, and (if the
# result's top byte is nonzero) call it again with obj+4 as the 3rd argument.

# Args: obj (r3), src (r4). Calls VcallTriple_AllSuccess_82C011E8 (sub_82C011E8) with src,
# a global (a 3rd-arg constant ~0x82C0F0C0), and obj; if the result's top byte is nonzero,
# calls VcallTriple_AllSuccess_82C011E8 again with src, a second global, and (obj + 4). So
# it's a gated two-step vcall sequence driving the source object. Returns the final result
# byte. 75 lines. 1 caller: sub_82C16AA8 (a sibling, also in the TODO). The vcall-triple
# slots aren't resolvable, so the name states the mechanism: a gated sequence of two
# VcallTriple_AllSuccess calls.
[entrypoint.functions."0x82C15358"]
name = "VcallTriple_Sequence_82C15358"

# A multi-step vcall chain on obj: slot 6, then (if the result is nonzero)
# VcallTriple_Sequence_82C15358, then (if that result is nonzero) slot 7 -- each step
# gated on the previous result's byte 3. Same shape as VcallChain_6_10_7_82C14EA0 and
# VcallChain_6_Dispatch_7_82C168E0, but the middle step is the vcall-triple sequence.

# Args: obj (r3, ->0 = a vtable), (r4), value (r5). Makes a vcall on obj (vtable slot 6,
# offset 24); if the result's top byte is nonzero, calls VcallTriple_Sequence_82C15358
# (sub_82C15358) with value and obj; if that result's top byte is nonzero, makes another
# vcall on obj (vtable slot 7, offset 28). Returns void. 86 lines. 1 caller:
# sub_82C188A8. A gated vcall chain (slot 6 -> vcall-triple sequence -> slot 7); a sibling
# of the other 6-?-7 chains in the 0x82C15/0x82C16 dispatcher cluster. The slots aren't
# resolvable without the vtable, so the name states the mechanism.
[entrypoint.functions."0x82C16AA8"]
name = "VcallChain_6_Triple_7_82C16AA8"

# A large (408-line) complex vcall dispatcher: make an initial pair of vcalls on the
# source (slots 5 and 0), then orchestrate a long gated sequence of vcall chains
# (VcallTriple_AllSuccess, VcallChain_AllSuccess x2, VcallChain_6_12_7, ...) -- the bigger
# sibling of Dispatch_VcallSequence_82C15100.

# Args: obj (r3, a tree/object container), src (r4, a wrapper, ->0 = a vtable object),
# value (r5). Makes a vcall on src (vtable slot 5, offset 20), then another vcall on src
# (vtable slot 0, offset 0), then VcallTriple_AllSuccess_82C011E8; depending on the results
# it branches through VcallChain_AllSuccess_82C14F38, VcallChain_AllSuccess_82C15068,
# VcallChain_6_12_7_82C14E08, plus DestructRefCounted_82214F08, sub_821F7798, sub_82BFD780,
# and sub_82C14D70. So it's the top-level dispatcher that drives a long virtual-operation
# sequence on the source object. 408 lines. 1 caller: sub_82C1EAF8 (a sibling, also in the
# TODO). The object class and the exact semantics of each vcall chain aren't resolvable, so
# the name states the mechanism: a dispatcher that runs a long gated sequence of vcall
# chains (the "2" distinguishes it from Dispatch_VcallSequence_82C15100).
[entrypoint.functions."0x82C1E360"]
name = "Dispatch_VcallSequence2_82C1E360"

# A lookup via vcall + binary search: obtain a value from a vtable slot-1 call on the
# source, then binary-search for it (BinarySearch_4Stride_82C40EC0).

# Args: (r3), src (r4, ->0 = a vtable object), value (r5). Makes a vcall on src (vtable
# slot 1, offset 4) passing src, a global (~0x82C24C60), and value; if the result is 0,
# returns 0. Otherwise it processes the result (result + 4) and calls
# BinarySearch_4Stride_82C40EC0 (sub_82C40EC0) to look the value up in a sorted array.
# Returns the lookup result. 83 lines. 2 callers (sub_82C1E7F0 -- also in the TODO -- plus
# sub_82C1E878). Part of the 0x82C1E lookup cluster. The object class and the slot-1 method
# aren't resolvable, so the name states the mechanism: obtain a value via a vcall and
# binary-search for it.
[entrypoint.functions."0x82C1E760"]
name = "LookupByVcall_82C1E760"

# A boolean predicate: call LookupByVcall_82C1E760 and return 0/1 based on the result's
# sign (a "does it exist / is it valid" test).

# Args: (r3), src (r4), value (r5). Calls LookupByVcall_82C1E760 (sub_82C1E760) with the
# same arguments, then does a clz-based sign test on the result and returns (sign ^ 1) --
# a 0/1 boolean. So it's a predicate built on the lookup: true (1) when the lookup returns
# a "nonzero-sign" result. 0 direct callers, so it is most likely a VIRTUAL method (or
# currently dead). Part of the 0x82C1E lookup cluster. The lookup semantics aren't fully
# resolvable, so the name states the mechanism: a boolean predicate over the vcall+binary-
# search lookup.
[entrypoint.functions."0x82C1E7F0"]
name = "LookupPredicate_82C1E7F0"

# A multi-step vcall chain on obj: slot 6, then (if the result is nonzero)
# Dispatch_VcallSequence2_82C1E360, then (if that result is nonzero) slot 7 -- each step
# gated on the previous result's byte 3. Same shape as VcallChain_6_Dispatch_7_82C168E0,
# but the middle step is the second (larger) dispatcher.

# Args: obj (r3, ->0 = a vtable), (r4), value (r5). Makes a vcall on obj (vtable slot 6,
# offset 24); if the result's top byte is nonzero, calls Dispatch_VcallSequence2_82C1E360
# (sub_82C1E360) with value and obj; if that result's top byte is nonzero, makes another
# vcall on obj (vtable slot 7, offset 28). Returns void. 86 lines. 1 caller: sub_82C1EBE8.
# A gated vcall chain (slot 6 -> dispatcher2 -> slot 7); a sibling of
# VcallChain_6_Dispatch_7_82C168E0 / VcallChain_6_Triple_7_82C16AA8. The slots aren't
# resolvable without the vtable, so the name states the mechanism.
[entrypoint.functions."0x82C1EAF8"]
name = "VcallChain_6_Dispatch2_7_82C1EAF8"

# A vcall + direct array index: make a vtable-slot-1 call on the source, then index a
# 4-byte-stride array (src->4) at the given index and return the pointed-to element.

# Args: (r3), src (r4, ->0 = a vtable object, ->4 = an array base), index (r5). Makes a
# vcall on src (vtable slot 1, offset 4) passing src and a global (~0x82C24C60) as the 2nd
# arg. Then computes slot = index * 4, loads the element pointer from src->4[slot], and
# returns *element. So it's a "call slot 1, then fetch array[index]" accessor. 50 lines,
# 0 external callees (the vcall + array index are inline). 0 direct callers, so it is most
# likely a VIRTUAL method (or currently dead). Part of the 0x82C1E lookup cluster (a
# sibling of LookupByVcall_82C1E760, but with a direct index instead of a binary search).
# The slot-1 method and array element class aren't resolvable, so the name states the
# mechanism.
[entrypoint.functions."0x82C1EB90"]
name = "VcallAndIndexArray_82C1EB90"

# A circular linked-list destroy: detach the list rooted at obj->4, walk it freeing each
# node (Free_SizeBucketed_Thunk), then clear the container's flag.

# Arg: obj (r3, ->4 = the head of a circular doubly-linked list, ->8 = a count/flag). Detaches
# the list (head->0 = head, head->4 = head, obj->8 = 0). Then walks the circular list (each
# node's ->0 is the next node) freeing every node via Free_SizeBucketed_Thunk_8221BE68 until
# it returns to the head. Returns void. 66 lines. 135 callers (sub_8290F760, sub_82882DB8,
# sub_8266CFD8, ...) -- a high-value list-destructor. The list element class isn't
# resolvable, so the name states the mechanism: destroy (free all nodes of) a circular
# linked list.
[entrypoint.functions."0x82A0F5C0"]
name = "DestroyCircularList_82A0F5C0"

# A refcounted object CONSTRUCTOR (the construct counterpart of DestructRefCounted_82214F08,
# one of the hottest functions in the game, 5200 callers): initialize the object and
# atomically increment the global live-object counter.

# Args: obj (r3, ->0 = the stored value), value (r4). Sets obj->0 = 0. Atomically
# increments the global counter at ~0x82C489E0 (lwarx/stwcx under REX_ENTER_GLOBAL_LOCK) --
# the SAME global live-object count that DestructRefCounted_82214F08 decrements. If value
# (r4) is non-null it does additional setup (Release_RefCounted_821C67D8 on the old value,
# sub_822079D8). So it's the COM-style refcounted operator=/constructor: take a reference
# and bump the global live count. 104 lines. 5200 callers (sub_827CD5F0, sub_8325D140,
# sub_83260CC0, ...). The object class isn't resolvable, so the name states the mechanism:
# construct a refcounted object (init + increment the global count).
[entrypoint.functions."0x8222CF18"]
name = "ConstructRefCounted_8222CF18"

# A chain-root finder (the byte-25-flag variant): validate, then walk up a chain (via
# node->0) to the topmost node (the one whose byte-25 flag is set) and store it in obj->4.
# Structurally identical to FindChainRoot_82A596F0 (which uses byte 17), but uses byte 25
# and validates more aggressively.

# Arg: obj (r3, ->0 = a base, ->4 = a starting node). Validates (obj->0 != 0, else `trap 22`;
# the head node's byte-25 == 0, else `trap 22`). Then walks up the chain via node->0 (the
# parent link) until it reaches a node whose byte-25 flag is nonzero (the root/top), and
# stores that node in obj->4. Returns void. 93 lines, 0 external callees (all inline). 108
# callers (sub_8267D818, sub_825D6FE8, sub_82367898, ...) -- a high-value chain-root lookup.
# A sibling of FindChainRoot_82A596F0 (the byte-17 variant); the "2" distinguishes the two.
# The node/chain class and the byte-25 flag's meaning aren't resolvable.
[entrypoint.functions."0x823D4F20"]
name = "FindChainRoot2_823D4F20"

# A tree find-and-remove: search a BST for the key (walk node->12 vs key, left = node->0,
# right = node->8, leaf flag = node->17), then remove the located node (Tree_RemoveNode).

# Args: obj (r3, ->4 = a container, ->4->4 = the head node), key (r4, *r4 = the key). Walks
# the tree from the head: at each node, if key >= node->12 go left (node->0), else go right
# (node->8), stopping at a leaf (node->17 != 0). Then it calls FindChainRoot_82A596F0 and
# Tree_RemoveNode_826A4A20 (sub_826A4A20) to detach/remove the located node. Returns the
# removed node (or a marker). 153 lines. 3 callers (sub_83218420, sub_83215610, sub_83218350
# -- the 0x83218/0x83215 constructor cluster). Part of the 0x82A14 tree cluster. The key
# type and node class aren't resolvable, so the name states the mechanism: find a node by
# key in the tree and remove it.
[entrypoint.functions."0x82A14AE0"]
name = "Tree_FindAndRemove_82A14AE0"

# A lazy-init singleton getter: check a global flag and, on first call only, initialize a
# global object, then return it.

# No real args (operates on globals). Checks bit 0 of a global flag (~0x82C73F98); if it is
# already set, skip to the return. Otherwise set the flag, initialize the global object at
# ~0x82C78640 (sub_82BA8BB0), and call sub_82CA3700 (a string/format routine) with another
# global (~0x82C38A80). Returns the global object. So it's a "get the singleton, initialize
# on first use" accessor. 72 lines. 94 callers, mostly in the 0x82BA/0x82BB region
# (sub_82BA8800, sub_82BA9938, sub_82BB1398, ...) -- a high-value subsystem getter. The
# object class isn't resolvable, so the name states the mechanism: get the (lazy-init)
# singleton.
[entrypoint.functions."0x82BA8718"]
name = "GetSingleton_82BA8718"

# A range/counter advance: consume a delta by incrementing counters and advancing several
# 12-byte range pointers (offsets 32/44 and 40/44) by the delta, with clamping.

# Args: obj (r3, a multi-range object: ->0/->12 = counters, ->8 = a clamped counter,
# ->32/->40/->44 = a range triple, and (obj + 32) = another range triple), delta (r4). If
# delta <= 0, return (no-op). Otherwise: obj->0 += 1, obj->12 += 1; clamp obj->8 to
# min(obj->8, obj->12); advance the ranges (obj->32 += delta, obj->44 += delta, and the
# (obj + 32) range) with similar clamping. So it's a "consume/advance N" operation over a
# multi-buffer / multi-range object. Returns void. 108 lines, 0 external callees (all
# inline). 2 callers (sub_82BAA4A8, sub_82BA8F18 -- both in the TODO). The object class and
# the exact range semantics aren't resolvable, so the name states the mechanism: advance the
# range pointers by a delta.
[entrypoint.functions."0x82BAA6F0"]
name = "AdvanceRanges_82BAA6F0"

# A value-at-position processor (in the 0x826C value-processing cluster): guard the
# position, process the value via ProcessValue_FlagNonZero, and memset as needed.

# Args: obj (r3, ->20 = a length), pos (r4), (r5). Guards the position (calls
# sub_82CD11D0 -- the high-value guard helper -- if pos is out of range). If pos == 0,
# returns. Otherwise it calls ProcessValue_FlagNonZero_821A9090 (sub_821A9090) with obj and
# (obj->20 + pos); if the result's top byte is nonzero it does further processing (memset via
# MemSet_SdkRuntime_82CA3190, and more). So it's a "process the value at a given position in
# the buffer" operation. 130 lines. 4 callers (ProcessObject_82C669C0 -- a processor in the
# 0x82C66 cluster -- plus sub_82C76100, sub_82C7AE40, sub_826C2250). The buffer class and
# exact value semantics aren't resolvable, so the name states the mechanism.
[entrypoint.functions."0x826C2FF8"]
name = "ProcessValueAtPos_826C2FF8"

# A hash-table lookup (linear probing, 4-byte stride): find a key in a flat open-addressing
# hash table and return the located slot -- a sibling of HashTable_Find_829FAD30 (which
# uses 8-byte stride).

# Args: table (r3, ->0 = the bucket count, ->4 = the array base), key (r4). Computes the
# starting bucket as (key & (count - 1)), then linearly probes (4-byte slots, wrapping
# around) comparing each entry's ->0 (the stored key) to the key; stops on a match or an
# empty slot (entry == 0). Returns the located slot (slot + 4) if found, else 0. 60 lines,
# 0 external callees (all inline). 35 callers (sub_828907F8, sub_823AAC18, sub_829FC6A0 --
# also in the TODO -- ...). The table key type isn't resolvable, so the name states the
# mechanism: a linear-probing hash table lookup (the "2" distinguishes it from
# HashTable_Find_829FAD30).
[entrypoint.functions."0x829FC630"]
name = "HashTable_Find2_829FC630"

# A processing loop: iterate `count` times over a sub-object, calling sub_82BCD8A8 each
# time (updating the counters) and copying data via MemCpy_SdkRuntime.

# Args: obj (r3, ->4 = a sub-object with ->0 = a counter, ->4 = a remaining count), (r4),
# count (r5). Loops `count` times: if sub->0 == 0, calls sub_82BCD8A8 (returns -1 or a
# value) and updates sub->0 += 1, sub->4 -= 1; then processes/copies data (MemCpy_SdkRuntime,
# sub_82BC8870, sub_82BCCB88). So it's a "process `count` elements" loop. 131 lines. 4
# callers, all in the 0x82BDD cluster (sub_82BDD1E0, sub_82BDD518, sub_82BDDA98, sub_82BDD000
# -- some also in the TODO). The element class and the per-element operation aren't
# resolvable, so the name states the mechanism: a loop that processes a count of elements.
[entrypoint.functions."0x82BDCF20"]
name = "ProcessElements_82BDCF20"

# A buffer reassignment: free the old buffer, allocate a new one of the requested size, set
# the length/size fields, and null-terminate -- a "reset the buffer to a new size" operation.

# Args: obj (r3, ->0 = the buffer, ->4 = the length, ->8 = a size field with a flag bit),
# (r4), pos (r5). If obj->0 != 0, free the old buffer (Free_SizeBucketed_Thunk_8221BE68) and
# clear obj->0. Then computes size = (pos + 4) aligned to 4, allocates a new buffer
# (Allocate_OrDbgBreak_8221F388), and sets obj->0 = buffer, obj->4 = pos, obj->8 = (size << 1)
# | (flag bit). Null-terminates at pos (stb). May also copy (MemCpy_Vectorized) or zero
# (MemSet_SdkRuntime). So it's a buffer reassign/resize. 88 lines. 7 callers (sub_822079D8 --
# a callee of ConstructRefCounted_8222CF18 and in the TODO -- plus sub_8222E840, sub_82275368,
# ...). The buffer class isn't resolvable, so the name states the mechanism: reassign the
# buffer to a new size.
[entrypoint.functions."0x822088B0"]
name = "ReassignBuffer_822088B0"

# A state handler (one of the five cases dispatched by StateDispatch_82BCB0A8): conditionally
# dispatch via StateDispatch_82BCA7D8, then loop over a sub-array checking elements.

# Args: obj (r3), state (r4, ->12 = a sub-object with a ->5 flag byte, ->6 and ->7 = gate
# bytes, ->20 = a sub-array). If (state->12->5) & 0x3 != 0, calls StateDispatch_82BCA7D8
# (sub_82BCA7D8). If state->6 != 0 and state->7 != 0, it loops over the sub-array at
# (state + 20) checking each element (element->4 == 4) and processing it. So it's a specific
# state/command handler in the 0x82BCA state cluster. 157 lines. 1 caller:
# StateDispatch_82BCB0A8 (a state dispatcher, also in this batch). The state semantics aren't
# resolvable, so the name states the mechanism: a state handler (one of the dispatcher's
# cases).
[entrypoint.functions."0x82BCAE28"]
name = "StateHandler_82BCAE28"

# A trivial pointer adjustment: return obj + 492 (a 2-insn function: addi r3, r3, 492; blr).

# Arg: obj (r3). Returns (obj + 492) -- a pointer to a sub-field / embedded object at offset
# 492 within the object. 0 direct callers, so it is most likely a VIRTUAL method (a vtable
# accessor) or currently dead. It's a plain "get the embedded object at offset 492" accessor;
# the field's meaning needs the object class. Named for what it does: return obj + 492.
[entrypoint.functions."0x82BAA138"]
name = "GetSubObj_492_82BAA138"

# A 2D region operation: compute a rectangle's width/height (in 4-byte units), derive a step
# via the Euclidean (gcd) algorithm, then traverse the 2D region.

# Args: base (r3), p1 (r4), p2 (r5) -- plausibly a base point and two offsets/points. Computes
# dx = p1 - base, dy = p2 - base; width = dx / 4, height = dy / 4. Runs the Euclidean algorithm
# (gcd of width and height via divw/mod in a loop). If the gcd is nonzero and < height, it
# traverses the 2D region (4-byte stride, a nested loop over the rectangle). Returns void. 122
# lines, 0 external callees (all inline). 10 callers (sub_82C2BB20, sub_82C289E8 -- in the TODO
# -- sub_82C2E000, sub_82C196E8, sub_82C204F0 -- in the TODO -- ...). The exact operation (the
# gcd-derived step suggests a strided/diagonal 2D traversal) isn't fully resolvable, so the name
# states the mechanism: process a 2D region (with a gcd-derived step).
[entrypoint.functions."0x82C3C010"]
name = "Process2DRegion_82C3C010"

# A 16-byte allocation + self-referential init: allocate a 16-byte block, make it point at
# itself (sentinel self-references), and store it in the container.

# Arg: obj (r3, ->4 = the block pointer, ->8 = a flag). Allocates a 16-byte block via
# Allocate_OrDbgBreak_8221F388(16). If the block is non-null, sets block->0 = block (a
# self-reference) and block->4 = block (another self-reference). Stores obj->4 = block and
# obj->8 = 0. So it's a constructor for a small (16-byte) self-referential object. Returns
# void. 56 lines. 48 callers (sub_828514D8, sub_82755FD0, sub_82646D18, ...) -- a high-value
# small-object constructor. The object class (the self-references are likely sentinels for a
# small buffer / string / node) isn't resolvable, so the name states the mechanism: allocate a
# 16-byte self-referential block.
[entrypoint.functions."0x821EE1B8"]
name = "Construct16SelfRef_821EE1B8"

# A very large (533-line) structure-processing operation: make a vcall on the source, copy
# variable-length byte data, and orchestrate a long sequence of build/teardown operations
# (DestroyCircularList, Construct16SelfRef, Free, plus the 0x82A0F/0x82A11/0x82A1B node ops).

# Args: obj (r3), src (r4, ->0 = a vtable object, ->4/->12 = data, ->20 = a size), (r5).
# Makes a vcall on src (vtable slot 2, offset 8). If src->20 >= 4, copies a variable-length
# byte array from src->12 (byte-by-byte to the stack). Then it branches into a long sequence
# of sub-operations: DestroyCircularList_82A0F5C0, Free_SizeBucketed_Thunk_8221BE68,
# Construct16SelfRef_821EE1B8, sub_82A0F0A0, sub_82A0F6B8, sub_82A118B0, sub_82A1B480, and
# sub_8322EFB0. So it's a top-level "process the data structure" routine (parse + build /
# teardown). 533 lines. 1 caller: sub_832206E8 (a sibling, also in the TODO). The data-structure
# class and the exact operation sequence aren't resolvable, so the name states the mechanism:
# a large structure-processing routine.
[entrypoint.functions."0x82A0DD10"]
name = "ProcessDataStructure_82A0DD10"

# A block reassign / cleanup (198 lines): free the old block and, on the larger path, allocate
# a new one and reinitialize it -- a "reassign the object's data block" operation.

# Args: obj (r3, ->0 = a data block pointer), gate (r4, ->8 = a count/flag), (r5). If gate->8
# <= 1, it frees obj->0 (Free_SizeBucketed_Thunk_8221BE68, if non-null) and clears obj->0
# (a teardown of a small object). Otherwise (gate->8 > 1) it allocates a new block
# (Allocate_OrDbgBreak_8221F388), reinitializes it, and calls sub_82A0FB80 (a further setup
# routine). So it's a reassign/realloc of the object's data block (free old, allocate new).
# Returns void. 198 lines. 2 callers (sub_82A0DD10 -- ProcessDataStructure_82A0DD10 -- plus
# sub_82A0E0C0). Part of the 0x82A0F structure cluster. The block class isn't resolvable, so
# the name states the mechanism: reassign the object's data block.
[entrypoint.functions."0x82A0F6B8"]
name = "ReassignBlock_82A0F6B8"

# A large (393-line) structure constructor / build: allocate self-referential blocks, build a
# multi-element structure, and orchestrate teardown (destroy/free) as needed.

# Args: obj (r3, ->8 = a count/flag), (r4). If obj->8 < 2, takes a short path. Otherwise it
# allocates 16-byte self-referential blocks (Allocate_OrDbgBreak_8221F388, each block set to
# point at itself -- the same pattern as Construct16SelfRef_821EE1B8) and builds a multi-element
# structure in a loop (r29 = 25 elements, plus further Allocate_SizeBucketed_Thunk_8221F3F0
# calls). It also orchestrates DestroyCircularList_82A0F5C0, Free_SizeBucketed_Thunk_8221BE68,
# sub_82A0F120, sub_82A0F290, and sub_82CBBB58. So it's a top-level "build the structure"
# routine. 393 lines. 3 callers (sub_82A0E0C0, sub_82A11500, sub_82A0DD10 --
# ProcessDataStructure_82A0DD10). Part of the 0x82A11 structure cluster. The structure class
# isn't resolvable, so the name states the mechanism: a large structure-construction routine.
[entrypoint.functions."0x82A118B0"]
name = "BuildStructure_82A118B0"

# A float-data processing routine (in the 0x8322 cluster): load global floats and a float
# array (indexed), build a stack struct, then process it via ProcessDataStructure.

# Args: obj (r3), (r4), index (r5), (r6), (r7). Loads several global floats (and a float from
# a global float array at index * 4), building a small stack struct (stack[80..92]). Then it
# calls ProcessDataStructure_82A0DD10 (sub_82A0DD10) and sub_82A0B5C8 (a constructor in the
# 0x82A0B cluster), and frees (Free_SizeBucketed_Thunk_8221BE68). So it's a "process float
# data" routine that feeds the data-structure processor. 127 lines. 8 callers, in the
# 0x8323/0x8321 region (sub_8323AE00, sub_8323F5B0, sub_83217688, ...). The float data's
# meaning (plausibly geometry / transform / animation values) and the object class aren't
# resolvable, so the name states the mechanism: process float data.
[entrypoint.functions."0x832206E8"]
name = "ProcessFloatData_832206E8"

# A 2D strided-processing routine (in the 0x82BDD cluster): call ProcessElements with
# count * stride, then dispatch on the stride (an 8-way switch) for stride-specific handling.

# Args: obj (r3, ->16 = a flag), (r4), count (r5), stride (r6), (r7). Computes (count * stride)
# and calls ProcessElements_82BDCF20 (sub_82BDCF20) to process the elements. If obj->16 != 0,
# it does an 8-way switch on (stride - 1) (cases 0-7) for stride-specific processing (via
# sub_82BC8870 / sub_82BCCB88). So it's a "process 2D data, with per-stride handling" routine.
# 213 lines. 5 callers, all in the 0x82BDD cluster (sub_82BDD518, sub_82BDD170, sub_82BDD1E0,
# sub_82BDD8A8, sub_82BDD258). The data class and the per-stride operations aren't resolvable,
# so the name states the mechanism: process 2D strided data.
[entrypoint.functions."0x82BDD000"]
name = "Process2DStrided_82BDD000"

# A range/counter advance (the sibling of AdvanceRanges_82BAA6F0): if delta > 0, update the
# counters and advance the range pointers -- but in the opposite (shrink/consume) direction
# (obj->36 += delta while obj->44 -= delta, etc.).

# Args: obj (r3, a multi-range object: ->4/->12 = counters, ->20/->28, ->36/->44, ->52/->60 =
# range triples), delta (r4). If delta <= 0, return (no-op). Otherwise: obj->12 -= 1, obj->4 +=
# 1, obj->20 += 1, obj->28 -= 1; advance the ranges (obj->36 += delta while obj->44 -= delta,
# obj->52 += delta while obj->60 -= delta) -- so it shrinks the ranges from both ends. Returns
# void. 64 lines, 0 external callees (all inline). 3 callers (sub_82BA9068, sub_82BAA570 -- in
# the TODO -- sub_82BA8D90). A sibling of AdvanceRanges_82BAA6F0 (same multi-range object,
# opposite direction); the "2" distinguishes the two. The object class and exact range
# semantics aren't resolvable.
[entrypoint.functions."0x82BAA7B8"]
name = "AdvanceRanges2_82BAA7B8"

# A string allocation: compute the string length (if pos == -1) or use the given length, then
# allocate the string's buffer (ReassignBuffer).

# Args: (r3), src (r4, a C-string), pos (r5, a length, or -1 to auto-compute). If pos == -1,
# it allocates a 16-byte block (zeroed: block->0/->4/->8 = 0) and computes the string length
# (a strlen loop over src until a null byte). Otherwise it uses the given length. Then it
# calls ReassignBuffer_822088B0 (sub_822088B0) to (re)allocate the string's data buffer at the
# computed length. So it's a "create/allocate a string object from a C-string" routine. 107
# lines. 3 callers (sub_821E3950, ConstructRefCounted_8222CF18 -- which calls it to set up a
# string value -- plus sub_82B3A0E8). The string class isn't resolvable, so the name states the
# mechanism: allocate a string (compute length, allocate buffer).
[entrypoint.functions."0x822079D8"]
name = "AllocateString_822079D8"

# A dispose / destructor for a specific object type: free the object's block, clear its
# value, and preserve a flag bit -- called by Release_RefCounted_821C67D8 when the refcount
# hits zero.

# Arg: obj (r3, ->0 = a data block, ->4 = a value, ->8 = a field with a flag bit). If obj->0
# != 0, frees it (Free_SizeBucketed_Thunk_8221BE68) and sets obj->0 = 0. Sets obj->4 = 0.
# Preserves bit 0 of obj->8 (obj->8 &= 1), clearing the rest (the size/length bits). So it's a
# dispose: release the block, reset the value, keep the flag. Returns void. 53 lines. 1 caller:
# Release_RefCounted_821C67D8 (the refcounted release -- this is the object-type-specific
# dispose it dispatches to when the reference count reaches zero). The object class isn't
# resolvable, so the name states the mechanism: dispose (free + reset, keep a flag).
[entrypoint.functions."0x82B39A30"]
name = "Dispose_82B39A30"

# A chain-root finder (the byte-33-flag variant): validate, then walk up a chain (via
# node->0) to the topmost node (the one whose byte-33 flag is set) and store it in obj->4.
# A third variant, structurally identical to FindChainRoot_82A596F0 (byte 17) and
# FindChainRoot2_823D4F20 (byte 25), but uses byte 33.

# Arg: obj (r3, ->0 = a base, ->4 = a starting node). Validates (obj->0 != 0, else `trap 22`;
# the head node's byte-33 == 0, else `trap 22`). Then walks up the chain via node->0 (the
# parent link) until it reaches a node whose byte-33 flag is nonzero (the root/top), and
# stores that node in obj->4. Returns void. 93 lines, 0 external callees (all inline). 64
# callers (sub_8278C8E0, sub_8292C840, sub_823B0DB8, ...) -- a high-value chain-root lookup.
# Siblings: FindChainRoot_82A596F0 (byte 17), FindChainRoot2_823D4F20 (byte 25); the "3"
# distinguishes this byte-33 variant. The node/chain class and the byte-33 flag's meaning
# aren't resolvable.
[entrypoint.functions."0x82A962B0"]
name = "FindChainRoot3_82A962B0"

# A linear search (8-byte stride): scan an array of 8-byte elements, comparing a masked 27-bit
# field of each to the target, and return the located slot offset.

# Args: obj (r3, ->28 = a u16 with a flag bit, ->31 = the count, ->32/->68 = the array base),
# target (r4). Selects the array base as (obj + 68) if bit 0 of (obj->28) is set, else (obj +
# 32). Reads count = obj->31; if 0, returns 0. Otherwise scans the array (8-byte slots): for
# each element, masks it to 27 bits (element & 0x7FFFFFF) and compares to target; on a match,
# returns the slot offset. Returns 0 if not found. 60 lines, 0 external callees (all inline).
# 13 callers (sub_821BD468, sub_823F75A0, sub_82283ED0, ...). The element class and the target
# type aren't resolvable, so the name states the mechanism: a linear search over an 8-byte
# array (masked-field comparison).
[entrypoint.functions."0x82284448"]
name = "LinearSearch_82284448"

# A string-key lookup: walk a node structure, comparing node keys via StrCmp_Signed_8226D7A8,
# and set a 0/1 result.

# Args: obj (r3, ->4 ->4 = a starting node, node->17 = a flag, node->12 = a key), src (r4,
# ->0 = the target). Reads the starting node (obj->4->4); if the node's byte-17 flag is 0, it
# walks the node structure comparing each node's key (node->12) to the target via
# StrCmp_Signed_8226D7A8, setting a 0/1 result (found/not) in r11. Returns that result. 100
# lines. 1 caller: sub_825D7688 (a sibling, also in the TODO). The node/key class (a string-key
# lookup over a tree/list of nodes) isn't resolvable, so the name states the mechanism: a
# string-key lookup (walk + StrCmp compare, return a 0/1 result).
[entrypoint.functions."0x822ADCB8"]
name = "LookupString_822ADCB8"

# A string-key lookup (the larger sibling of LookupString_822ADCB8): walk a node structure,
# comparing node keys via StrCmp_Signed_8226D7A8 / CompareStrings_8228B5A0, and set a 0/1
# result.

# Args: obj (r3, ->4 ->4 = a starting node, node->17 = a flag, node->12 = a key), (r4), src
# (r5, ->0 = the target). Reads the starting node (obj->4->4); if the node's byte-17 flag is 0,
# it walks the node structure comparing each node's key (node->12) to the target via
# StrCmp_Signed_8226D7A8 and CompareStrings_8228B5A0, setting a 0/1 result (found/not). Also
# calls sub_8246CF80 and sub_824708F8 (further lookup helpers). Returns the 0/1 result. 220
# lines. Many callers (sub_82514818, sub_825C7278, sub_8291FC20, ...). A larger sibling of
# LookupString_822ADCB8 (same walk + string-key compare + 0/1 result shape, but with extra
# helpers and a longer body); the "2" distinguishes the two. The node/key class isn't
# resolvable, so the name states the mechanism: a string-key lookup.
[entrypoint.functions."0x8246C010"]
name = "LookupString2_8246C010"

# A lookup-and-compare: call LookupString_822ADCB8 to find the node, validate it, then confirm
# the match with CompareStrings_8228B5A0.

# Args: obj (r3), target-obj (r4), src (r5). Calls LookupString_822ADCB8 (sub_822ADCB8) to
# find the matching node. If the result is 0, raises `trap 22` (an invalid/missing-node
# assertion). Otherwise it reads the node's key (result->4) and, if it doesn't already equal the
# lookup result, confirms the match with CompareStrings_8228B5A0 (sub_8228B5A0, comparing the
# node's +12 string to src). Returns the 0/1 match result. 86 lines. Many callers (sub_82B71020,
# sub_825C6368, sub_825C79D8, ...). It's a thin "find the node, then verify the string matches"
# wrapper around LookupString + CompareStrings. The node class and exact comparison semantics
# aren't resolvable, so the name states the mechanism: lookup and compare.
[entrypoint.functions."0x825D7688"]
name = "LookupAndCompare_825D7688"

# A register/init with a global counter: initialize the object, atomically increment a global
# counter (under the global lock), and run a couple of string lookups.

# Args: obj (r3, ->0 = a value, ->4 = a field), (r4), (r5). Sets obj->0 = r5 and obj->4 = 0
# (initializing the object). Then it atomically increments a global counter (lwarx/stwcx CAS
# loop under the global lock, REACQUIRE until success) -- the classic "register this object / assign
# it an ID" pattern. It then calls LookupString2_8246C010 (sub_8246C010), LookupAndCompare_825D7688
# (sub_825D7688), and CopyAssign_RefCounted2_82265160. Returns. 112 lines. 4 callers, in the
# 0x82B7 cluster (sub_82B5C8B0, sub_82B745A0, sub_82B714A0, sub_82B80D70). The object class and
# the exact registry semantics aren't resolvable, so the name states the mechanism: register/init
# the object and bump a global counter.
[entrypoint.functions."0x82B71020"]
name = "RegisterWithCounter_82B71020"

# A sorting network / bubble-sort over a small set of wrapper pointers: compare pairs
# (a,b), (b,c), ... and swap the pointers so they come out in order.

# Args: a (r3), b (r4), c (r5), (r6) -- three (or four) pointers to wrapper objects, where each
# wrapper's ->0 is a value object and value->0 / value->4 are the two sort keys. For each pair,
# it compares (value->0 of A vs value->0 of B); if A > B it swaps the two pointers; if equal it
# tiebreaks on (value->4). It walks the pairs (a,b), (b,c), ... performing swaps, producing an
# ordered sequence. Returns void. 89 lines, 0 external callees (all inline compares/swaps). 1
# caller: sub_82C170D0. The element class and the exact number of inputs aren't resolvable, so
# the name states the mechanism: a pointer-sorting (comparison/swap) network.
[entrypoint.functions."0x82C280C0"]
name = "SortPointers_82C280C0"

# A bit-flag processing routine: inspect bit 13 of the incoming flags and of obj->28, compute a
# parity bit (via a leading-zero count), and dispatch further flag handling.

# Args: (r3), obj (r4, ->28 = a u16 flag field), flags (r5), (r6). Tests bit 13 (0x1000) of the
# incoming flags; if it's clear, jumps to a fast path. Otherwise it inspects obj->28 (its upper
# bits and its bit 13) and, in one branch, computes a parity bit as (32 - clz(obj->28 & 0xFF)) &
# 1 (an odd/even bit-count test). It then calls sub_82284810 (a further flag handler). Returns.
# 110 lines. 0 direct callers (it's reached via a dispatch/vcall, not a direct call). The flag
# semantics (what bit 13 and the parity select) and the object class aren't resolvable, so the
# name states the mechanism: process bit flags.
[entrypoint.functions."0x82284750"]
name = "ProcessFlags_82284750"

# A comparator-based binary search over a 4-byte-stride array: linear scan for small ranges
# (<= 32 elements), otherwise binary search using sub_82C42828 as the comparator.

# Args: low (r3, array start), high (r4, array end), count (r5), (r6). Computes the element
# span as ((high - low) >> 2) (4-byte stride). If the span is <= 32, it takes a linear-scan path.
# Otherwise it binary-searches: computes a mid (rounding up), calls sub_82C42828 (the comparator,
# with (low, high, ...) args) to decide which half to recurse into, and narrows the range. It
# also calls sub_82C16D20, sub_82C273A8, sub_82C289E8 (further search/insert helpers) and recurses
# (self-call). Returns the located position. 176 lines. 8 callers, in the 0x82C1-0x82C2 cluster
# (sub_82C2E968, sub_82C3ADF0, sub_82C1B998, ...). A sibling of BinarySearch_4Stride_82C40EC0
# (same 4-byte-stride array search, but with an explicit comparator + linear fallback); the
# "Cmp" distinguishes it. The array element class and the comparator's ordering aren't
# resolvable.
[entrypoint.functions."0x82C39988"]
name = "BinarySearch_Cmp_82C39988"

# A bubble-sort over a range of 4-byte-stride wrapper pointers: walk the (start, end) range,
# compare adjacent wrappers (via ->0->0), and swap them into order.

# Args: start (r3, array start), end (r4, array end) -- a range of wrapper pointers, each
# wrapper's ->0 a value object (value->0 the sort key). If start == end (or start + 4 == end,
# i.e. fewer than 2 elements), returns immediately (nothing to sort). Otherwise it walks the
# range, comparing adjacent elements ((start)->0->0 vs (start+4)->0->0) and swapping them when
# out of order (a bubble sort). It also calls Process2DRegion_82C3C010 on one path. Returns
# void. 113 lines. 1 caller: BinarySearch_Cmp_82C39988 (sub_82C39988). A range-based sibling of
# SortPointers_82C280C0 (same compare/swap sorting of wrapper pointers, but over a start/end
# range); the name states the mechanism: bubble-sort the pointer range. The element class
# isn't resolvable.
[entrypoint.functions."0x82C289E8"]
name = "BubbleSortPointers_82C289E8"

# A heapify / priority-queue sift over a 4-byte-stride array: compare the root against the
# elements at the mid/child indices and swap them to maintain heap order.

# Args: heap (r3, ->0 = the root element, then a 4-byte-stride array), (r4), end (r5), (r6). Computes
# the element count as ((end - heap) >> 2). If the count is <= 40, it takes a linear path
# (loc_82C3C454). Otherwise it does a heap sift: computes a mid index (roughly (count + 1) / 8,
# rounded up) and compares the root (heap->0->0) against the elements at the mid and 2*mid
# positions (binary-heap parent/child indices); when out of order it swaps the elements
# (rewiring the pointer slots). 298 lines, 0 external callees (all inline compares/swaps). 1
# caller: sub_82C42828 (the same routine BinarySearch_Cmp_82C39988 uses as its comparator --
# so this is the sift it invokes). The element class and the heap's ordering (min/max) aren't
# resolvable, so the name states the mechanism: a heap sift.
[entrypoint.functions."0x82C3C298"]
name = "HeapSift_82C3C298"

# A heap-range processor: compute the mid index of a 4-byte-stride range, sift the left half
# (HeapSift_82C3C298), then continue processing / recursing over the range.

# Args: (r3), start (r4, array start), end (r5, array end), (r6) -- a range of 4-byte-stride
# elements (a heap). Computes the element count as ((end - start) >> 2) and the mid index
# (roughly count / 2, rounded up). It calls HeapSift_82C3C298 (sub_82C3C298) to sift the left
# half (start .. start + mid), then continues walking/processing the range (comparing and
# reordering elements). Returns. 310 lines, 1 callee (HeapSift_82C3C298). 1 caller:
# BinarySearch_Cmp_82C39988 (sub_82C39988) -- so it's the heap routine the comparator invokes
# during its search. The element class and the exact heap invariant (min/max) aren't
# resolvable, so the name states the mechanism: process the heap range (sift + recurse).
[entrypoint.functions."0x82C42828"]
name = "HeapProcess_82C42828"

# A recursive tree / structure destructor: recurse into the child subtree (child->8), free each
# node's data block (child->20), and clear the node's fields.

# Args: node (r3), child (r4), (r5). If the child's byte-33 flag is 0, it recurses into the
# child subtree: calls itself with (node, child->8) (the next child), frees child->20
# (Free_SizeBucketed_Thunk_8221BE68, a size-bucketed block, if non-null), advances to the next
# node (child->0), and zeroes the node's fields ((child + 16)->4/->8/->12 = 0). So it's a
# recursive teardown of a tree (free each node's block, recurse on the children). Returns void.
# 75 lines. 2 callers (sub_82B4CC70, sub_82371958). A sibling of DestroyList_Recursive (same
# recursive-free pattern, but for a tree with data blocks). The node class and the child link
# semantics aren't resolvable, so the name states the mechanism: a recursive tree destructor.
[entrypoint.functions."0x82B4D218"]
name = "DestroyTree_Recursive_82B4D218"

# An indexed-region processor: index a 16-byte-stride array in a large object (index clamped to
# 0-511), update the region's counters, and advance the ranges (AdvanceRanges2_82BAA7B8).

# Args: obj (r3, a large object with a 16-byte-stride array starting at offset 44), gate (r4,
# a non-zero check), index (r5, clamped to 511), (r6). If gate == 0, returns. Clamps index to
# 511. Accesses the region at (obj + 44 + (index + 196) * 16) and updates its counters (region->4
# += 1, region->12 -= 1), then walks the region (a loop over obj + 44) and calls
# AdvanceRanges2_82BAA7B8 (sub_82BAA7B8) and sub_82BAA570. So it's a "process the region at the
# clamped index" routine. 196 lines. Many callers, in the 0x82BB/0x82BA cluster (sub_82BB7700,
# sub_82BAE500, sub_823F8F40, ...). The region object and the exact counter semantics aren't
# resolvable, so the name states the mechanism: process an indexed region.
[entrypoint.functions."0x82BA9068"]
name = "ProcessIndexedRegion_82BA9068"

# A trivial constant-return stub: always returns 2.

# Args: (none used). Sets r3 = 2 and returns. 2 lines. 0 direct callers -- so it's most likely a
# virtual method reached via a vtable (a type-ID / small-value getter that always yields 2), or
# otherwise-dead code. The constant 2's meaning (a type tag, an element count, a mode, ...) can't
# be determined from the body alone. The name states the mechanism: a stub that returns the
# constant 2.
[entrypoint.functions."0x823314B8"]
name = "ReturnConstant2_823314B8"

# A trivial global getter: returns a fixed global address (0x83334D40).

# Args: (none used). Loads a fixed 32-bit address via lis/addi (0x83334D40, in the tail of the
# .text/.data region) into r3 and returns it. 2 lines. 0 direct callers -- so it's most likely a
# virtual method reached via a vtable (a "get the global object/pointer" accessor) or otherwise-
# dead code. The global it points at (0x83334D40) and its meaning can't be determined from the
# body alone. The name states the mechanism: a stub returning a fixed global pointer.
[entrypoint.functions."0x82C27FA0"]
name = "GetGlobalPtr_82C27FA0"

# A trivial global getter (the sibling of GetGlobalPtr_82C27FA0): returns a fixed global
# address (0x83334D28, 24 bytes before GetGlobalPtr_82C27FA0's 0x83334D40).

# Args: (none used). Loads a fixed 32-bit address via lis/addi (0x83334D28) into r3 and returns
# it. 2 lines. 0 direct callers -- most likely a virtual method reached via a vtable (a "get the
# global object/pointer" accessor) or otherwise-dead code. Sibling of GetGlobalPtr_82C27FA0
# (same shape, a nearby global in the same region); the "2" distinguishes the two. The global's
# meaning can't be determined from the body alone. The name states the mechanism: a stub
# returning a fixed global pointer.
[entrypoint.functions."0x82C23D00"]
name = "GetGlobalPtr2_82C23D00"

# An indexed-region processor (the sibling of ProcessIndexedRegion_82BA9068): index a
# 16-byte-stride array in a large object (index clamped to 0-511), update the region's counters,
# and advance the ranges (AdvanceRanges_82BAA6F0).

# Args: obj (r3, a large object with a 16-byte-stride array), index (r4, clamped to 511), (r5),
# (r6), (r7). Clamps index to 511. Accesses the region at (obj + (index + 196) * 16) and updates
# its counters (region->0 += 1, region->12 += 1), clamps region->8 to the new region->12, then
# walks the region and calls AdvanceRanges_82BAA6F0 (sub_82BAA6F0) and sub_82BAA4A8. So it's a
# "process the region at the clamped index" routine. 194 lines. Many callers, in the 0x82BB/
# 0x82BC/0x82BA cluster (sub_82BBDC90, sub_82BAFBE8, sub_82BC4040, ...). A sibling of
# ProcessIndexedRegion_82BA9068 (same 16-byte-stride array + clamped-index shape, but increments
# ->0/->12 and calls AdvanceRanges rather than AdvanceRanges2); the "2" distinguishes the two.
# The region object and the exact counter semantics aren't resolvable.
[entrypoint.functions."0x82BA8F18"]
name = "ProcessIndexedRegion2_82BA8F18"

# A range-advance + dispatch: call AdvanceRanges_82BAA6F0 to advance the object's ranges, then
# dispatch (a vcall via a global function pointer / sub_82BAA688).

# Args: obj (r3), (r4), (r5), (r6). Calls AdvanceRanges_82BAA6F0 (sub_82BAA6F0) with a global
# base pointer + obj + the range args to advance the ranges. Then it loads a global function
# pointer (from a global table) and dispatches a call through it (bctrl) with obj, and also calls
# sub_82BAA688 (a further advance/range helper). It also loads/compares a 2-field global
# (a low/high pair). So it's an "advance the ranges, then dispatch" routine. 116 lines. 1 caller:
# ProcessIndexedRegion2_82BA8F18 (sub_82BA8F18). Part of the 0x82BAA range/advance cluster. The
# object class and the dispatched call's meaning aren't resolvable, so the name states the
# mechanism: advance the ranges and dispatch.
[entrypoint.functions."0x82BAA4A8"]
name = "AdvanceAndDispatch_82BAA4A8"

# A large structure-node processor: branch on a child's flag, and on the null-child path call a
# helper (sub_82B4C1C0); otherwise process the node (FindChainRoot3_82A962B0 + helpers).

# Args: obj (r3), child (r4, ->4 = a value, ->8 = a flag), (r5), (r6). If child->8 == 0, it calls
# sub_82B4C1C0 (a leaf helper, with child->4 / 1 / r6) and returns. Otherwise it processes the
# node: calls FindChainRoot3_82A962B0 (sub_82A962B0) to find the chain root, plus sub_828836E8 and
# sub_82B4C080 (further structure helpers). So it's a "process the structure node" routine. 306
# lines. 1 caller: sub_82B4B2B8. Part of the 0x82B4B structure cluster (siblings of
# DestroyTree_Recursive_82B4D218, sub_82B4C080, sub_82B4C1C0). The node class and the exact
# processing aren't resolvable, so the name states the mechanism: process a structure node.
[entrypoint.functions."0x82B4BB10"]
name = "ProcessStructureNode_82B4BB10"

# A large node-value constructor (in the 0x82B4C cluster): check a type/code field, build a
# string, allocate, and construct a value (ConstructFromArray + the 0x826C3 helpers).

# Args: obj (r3), child (r4, ->8 = a type/code), (r5), (r6), (r7). Tests child->8 against a code
# (0x0CCE03); on the matching path it builds a string (SetString_822F2020 with a global string,
# then sub_826C3EF0 -- a format op), allocates (Allocate_OrDbgBreak_8221F388), and constructs a
# value (ConstructFromArray_82B4BFD8, a sibling in the 0x82B4B cluster, plus sub_822F1F00,
# sub_8234D600, sub_826C3FA8, sub_8292CC88, sub_82171810). So it's a "construct the node's value"
# routine (branch on the type, build the string, allocate, construct). 349 lines. 2 callers
# (sub_82B4BB10 -- ProcessStructureNode_82B4BB10 -- and sub_82B4C080). The node/value class and
# the 0x0CCE03 code's meaning aren't resolvable, so the name states the mechanism: construct a
# node value.
[entrypoint.functions."0x82B4C1C0"]
name = "ConstructNodeValue_82B4C1C0"

# A chain-root finder (a byte-33-flag variant): validate, then walk a chain (via node->8 / node->0)
# to the topmost node (byte-33 flag set) and store it in obj->4.

# Arg: obj (r3, ->0 = a base, ->4 = a starting node). Validates (obj->0 != 0, else `trap 22`).
# Reads the head node (obj->4): if the head's byte-33 flag is set, it advances to the next node
# (obj->4 = head->8) and returns if that node's byte-33 flag is 0 (else `trap 22`). Otherwise it
# walks up the chain via node->0 / node->8 until it reaches a node whose byte-33 flag is set (the
# root/top), and stores that node in obj->4. Returns void. 112 lines, 0 external callees (all
# inline). Many callers (sub_82883068, sub_822FF270, sub_82616CA0, ...). Siblings:
# FindChainRoot_82A596F0 (byte 17), FindChainRoot2_823D4F20 (byte 25), FindChainRoot3_82A962B0
# (byte 33, via node->0); this one also uses byte 33 but walks via node->8/node->0, hence the
# "4". The node/chain class and the byte-33 flag's meaning aren't resolvable.
[entrypoint.functions."0x828836E8"]
name = "FindChainRoot4_828836E8"

# A hash table insert / get-or-insert (the sibling of HashTable_Find2_829FC630): look up the
# key; if not present, increment the count, compute the value's length, allocate, and insert.

# Args: table (r3, ->24 = a count/size), key (r4), value (r5, a C-string). Calls
# HashTable_Find2_829FC630 (sub_829FC630) to look up the key. If it's already present, returns it.
# Otherwise it increments the count (table->24 += 1), computes the value's length (a strlen loop),
# allocates (Allocate_OrDbgBreak_8221F388) and copies the value (MemCpy_SdkRuntime_82CA2C60), then
# inserts the entry into the open-addressing table. So it's a "get or insert into the hash table"
# routine. 148 lines. 1 caller: sub_829FC7A8. Sibling of HashTable_Find2_829FC630 (the lookup
# this insert uses); the table's key/value class (a string-key -> string-value map, plausibly) isn't
# fully resolvable, so the name states the mechanism: insert into the hash table.
[entrypoint.functions."0x829FC6A0"]
name = "HashTable_Insert_829FC6A0"

# A hash table insert with auto-grow: call HashTable_Insert_829FC6A0, then check whether the
# table is at its load threshold and grow/rehash (sub_829FAB50) if needed.

# Args: table (r3, ->0 = a size/mask, ->24 = a count), (r4, a key/index), (r5). Calls
# HashTable_Insert_829FC6A0 (sub_829FC6A0) to insert the entry. Then it checks the table's load
# (count = table->24 against a threshold derived from the size table->0, a (3/4)-fill-style
# threshold); if it's at the threshold it grows the table by calling sub_829FAB50 (a rehash,
# with the new size table->0 << 1). It then recomputes the probe index ((size - 1) & key). So it's
# an "insert, and grow the table if it's getting full" routine. 101 lines. 1 caller: sub_829FC858.
# Part of the 0x829FC hash-table cluster (HashTable_Find2_829FC630, HashTable_Insert_829FC6A0,
# sub_829FAB50). The table's key/value class isn't resolvable, so the name states the mechanism:
# insert with auto-grow.
[entrypoint.functions."0x829FC7A8"]
name = "HashTable_InsertGrow_829FC7A8"

# A ctype predicate (a sibling of the 0x82CA6AB0-0x82CA6BB0 char-classification cluster): look up
# the u16 char table and return bit 3 (0x8) of the entry.

# Arg: c (r3, a char). Computes the table index as (c * 2) (a u16 per character), loads the u16
# entry from the global char-classification table (a fixed base near the .data region, the same
# table the 0x82CA6AB0-BB0 ctype siblings use), masks it to bit 3 (0x8) (rlwinm ... 28,28), and
# returns that single bit. So it's a character-classification predicate: "does char c have the
# bit-3 class set?". 5 lines, 0 external callees (all inline table lookup). 13 callers
# (sub_82C47A50, sub_83045EC0, sub_82B47610, ...). Siblings in the same cluster:
# Ctype_Pred_Bit2_82CA6B10 (bit 2), and the bit-0 (_UPPER) predicates; the "Bit3" identifies which
# class bit this one returns. The exact class (bit 3's meaning in the table) isn't resolvable from
# the body alone.
[entrypoint.functions."0x82CA6B50"]
name = "Ctype_Pred_Bit3_82CA6B50"

# A trivial constant-return stub: always returns 8.

# Args: (none used). Sets r3 = 8 and returns. 2 lines. 0 direct callers -- so it's most likely a
# virtual method reached via a vtable (a "get the size / count / type" getter that always yields
# 8), or otherwise-dead code. The constant 8's meaning (an element size, a count, a type tag, ...)
# can't be determined from the body alone. The name states the mechanism: a stub that returns the
# constant 8.
[entrypoint.functions."0x826EB210"]
name = "ReturnConstant8_826EB210"

# A float-to-int conversion + process: if a pair field matches, scale a float by an integer,
# convert the result to an int (fctiwz), and process it (sub_823F9AE0).

# Args: obj (r3, ->36/->40 = a pair, ->44 = a float), (r4). If obj->40 == obj->36, it does a
# float-to-int conversion: loads obj->44 (a float) and obj->36 (extended to a double), multiplies
# them, converts the product to an int (fctiwz, with the standard NaN/overflow saturation), and
# calls sub_823F9AE0 with that int (+ 1). So it's a "scale a float to an int and process" routine
# (a normalized-value -> index/count conversion). 94 lines. Many callers, in the 0x82BB/0x82BA/
# 0x82BC cluster plus ProcessFlags_82284750 (sub_82284750). The float's meaning (a scale factor,
# a probability, a normalized coordinate) and sub_823F9AE0's role aren't resolvable, so the name
# states the mechanism: float-to-int conversion and process.
[entrypoint.functions."0x82284810"]
name = "FloatToIntProcess_82284810"

# A trivial constant-return stub: always returns 16.

# Args: (none used). Sets r3 = 16 and returns. 2 lines. 0 direct callers -- so it's most likely a
# virtual method reached via a vtable (a "get the size / count / type" getter that always yields
# 16, e.g. a 16-byte element size), or otherwise-dead code. The constant 16's meaning (an element
# size, a count, a type tag, ...) can't be determined from the body alone. The name states the
# mechanism: a stub that returns the constant 16.
[entrypoint.functions."0x8269B5F8"]
name = "ReturnConstant16_8269B5F8"

# A structure validator + chain-root finder: validate obj->32 (non-null, not self-referential,
# else trap 22), then find the chain root (FindChainRoot_82A596F0).

# Args: obj (r3, ->28 = a field, ->32 = a pointer), (r4). Reads obj->32 and validates it (it must
# be non-null and must not equal obj + 28 -- a self-reference check -- else it raises `trap 22`).
# Then it walks/validates the structure (a loop checking obj->32 against obj + 28 and a stored
# value) and calls FindChainRoot_82A596F0 (sub_82A596F0) to find the chain root. So it's a
# "validate the structure, then find its chain root" routine. 90 lines. 0 direct callers (it's
# reached via a dispatch/vcall, not a direct call). Part of the 0x83217 constructor cluster. The
# structure class and the exact validation invariants aren't resolvable, so the name states the
# mechanism: validate and find the root.
[entrypoint.functions."0x83217AF8"]
name = "ValidateAndFindRoot_83217AF8"

# A trivial global getter (the third in the GetGlobalPtr series): returns a fixed global address
# (0x8332ECF0).

# Args: (none used). Loads a fixed 32-bit address via lis/addi (0x8332ECF0, in the .data region)
# into r3 and returns it. 2 lines. Most likely a virtual method reached via a vtable (a "get the
# global object/pointer" accessor) or otherwise-dead code. Siblings: GetGlobalPtr_82C27FA0 (returns
# 0x83334D40) and GetGlobalPtr2_82C23D00 (returns 0x83334D28); the "3" distinguishes this one. The
# global's meaning can't be determined from the body alone. The name states the mechanism: a stub
# returning a fixed global pointer.
[entrypoint.functions."0x82240578"]
name = "GetGlobalPtr3_82240578"

# A line-length scan (strcspn-like): scan a string for a line ending ('\n' = 10, '\r' = 13, or
# null) and return the length of the line.

# Arg: obj (r3, ->0 = a string base, ->32 = a byte, ->36 = an offset into the string). Computes
# the start as (obj->0 + obj->36) and scans forward byte-by-byte (sign-extended) until it hits a
# null, a newline (10), or a carriage return (13). It then returns the length of the line (the
# offset from the start to the line ending, i.e. r10 - start). If the line is empty, it returns 0
# (subf. ... beqlr). 117 lines, 0 external callees (all inline byte scanning). Many callers
# (sub_825F45A8, sub_826939A0, sub_82466098, ...). A string-line scanner ("how long is this line
# before the next newline?"), likely part of a text/log/line-based reader. The string's context
# isn't resolvable, so the name states the mechanism: scan a line and return its length.
[entrypoint.functions."0x82B477F0"]
name = "ScanLineLength_82B477F0"

# A chain lookup: walk a node chain (via node->8 / node->0, byte-25 flag) comparing each node's
# key (node->12) to the target, and locate the matching node.

# Args: obj (r3, ->4 ->4 = a starting node, node->25 = a flag, node->12 = a key), src (r4, ->0 =
# the target). Reads the starting node (obj->4->4); if the node's byte-25 flag is 0, it walks the
# chain: compares node->12 to the target, and depending on the comparison follows node->8 (next)
# or node->0 (parent), continuing while the byte-25 flag is 0. It then locates the matching node
# and calls Release_RefCounted_829FF648 (a refcounted release) and sub_82550F90 (a further
# lookup/processing helper). Returns the located node. 144 lines. 7 callers (sub_8323A1E8,
# sub_824426F0, sub_826898E8, ...). The byte-25 flag matches FindChainRoot2_823D4F20's, but this
# one walks + compares (a lookup) rather than just finding the root. The node/key class isn't
# resolvable, so the name states the mechanism: a chain lookup (walk + compare).
[entrypoint.functions."0x82550638"]
name = "ChainLookup_82550638"

# A struct copy: if src->20 >= 4, copy a variable-length byte array from src->12 (byte-by-byte to
# the stack) and advance src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size). If src->20 <
# 4, takes a short path. Otherwise it copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop
# to the stack), advances src->12 by 4, and continues processing (calling sub_82A1B480 and
# sub_8323A110). So it's a "copy the struct's data" routine (a variable-length byte-array copy,
# the same shape as Construct3FloatPayload_82A0C0A0's payload handling). 126 lines. 4 callers, in
# the 0x8321-0x8323 cluster (sub_83221548, sub_83238090, sub_8321FC38, sub_83218C10). The struct
# class and the exact field semantics aren't resolvable, so the name states the mechanism: copy
# the struct's data.
[entrypoint.functions."0x83235608"]
name = "CopyStructData_83235608"

# A node lookup / insert: call sub_82C343A0 to look up the node, validate the result (trap 22 if
# invalid), and run a chain lookup (ChainLookup_82550638).

# Args: obj (r3, ->4 = a field, ->8 = a value), (r4), gate (r5, ->4 = a value), (r6). If r4 ==
# gate->4, returns (already present). Otherwise it calls sub_82C343A0 (a lookup, with (obj + 4)
# args), validates the result (it must be non-null and must not equal obj + 4 -- a self-reference
# check -- else it raises `trap 22`), then reads the result's fields and calls
# ChainLookup_82550638 (sub_82550638), Release_RefCounted_829FF648, and sub_82A10A70 (further
# node helpers). So it's a "look up / insert the node" routine. 124 lines. 1 caller:
# CopyStructData_83235608 (sub_83235608). Part of the 0x8323 constructor cluster. The node class
# and the exact lookup/insert semantics aren't resolvable, so the name states the mechanism: look
# up / insert a node.
[entrypoint.functions."0x8323A110"]
name = "LookupAndInsertNode_8323A110"

# A buffer write / append: advance the offset and append bytes to a buffer via
# Buffer_PushByte_82CAE378 (in a loop), gated on the buffer's "done"/"in-use" flags.

# Args: src (r3, ->0 = a byte to append), count (r4), buffer (r5, ->8 = an in-use flag, ->12 = a
# flag with a "done" bit), pos (r6, ->0 = an offset), (r7). If bit 6 of buffer->12 is set (a
# "done"/"sealed" flag) or buffer->8 != 0 (in use), it returns without appending. Otherwise it
# advances the offset (pos->0 += count) and appends the byte(s) in a loop, calling
# Buffer_PushByte_82CAE378 (sub_82CAE378) and sub_82CAB770 (a further buffer helper). So it's an
# "append to the buffer (if it's still open)" routine. 92 lines. 2 callers (sub_82CAE4A8, a sibling
# in the same 0x82CAE buffer cluster, plus sub_8223F990). The buffer class and the exact flag
# semantics aren't resolvable, so the name states the mechanism: append bytes to a buffer.
[entrypoint.functions."0x82CAE410"]
name = "Buffer_Append_82CAE410"

# A trivial constant-return stub: always returns 0x8000 (32768).

# Args: (none used). Loads 0 into r3, ORs in 0x8000 (32768), and returns. 3 lines. 0 direct
# callers -- so it's most likely a virtual method reached via a vtable (a type-ID / flag getter
# that always yields 0x8000, the high bit of a 16-bit type/flag word), or otherwise-dead code. The
# constant 0x8000's meaning (a type tag, a capability bit, a default value, ...) can't be
# determined from the body alone. The name states the mechanism: a stub that returns the constant
# 0x8000.
[entrypoint.functions."0x82A11FF0"]
name = "ReturnConstant0x8000_82A11FF0"

# A sorting network over 12-byte records: compare pairs (a,b), (b,c), ... by record->0 and swap
# the full 12-byte records to order them.

# Args: a (r3), b (r4), c (r5), (r6) -- three 12-byte records (->0, ->4, ->8). For each pair, it
# compares the records' ->0 field; if the second is smaller, it swaps the two full 12-byte records
# (rewriting ->0/->4/->8 for both). It walks the pairs (a,b), (b,c), ... performing swaps,
# producing an ordered sequence. Returns void. 104 lines, 0 external callees (all inline
# compares/swaps). 1 caller: sub_82B42E28. A sibling of SortPointers_82C280C0 (same
# compare/swap sorting shape, but this one sorts 12-byte records directly by ->0 rather than
# wrapper pointers); the name states the mechanism: sort the records. The record class isn't
# resolvable.
[entrypoint.functions."0x82B43280"]
name = "SortRecords_82B43280"

# A hash lookup / get-or-create: hash the string (HashCstring_82188CF0), combine the hash with a
# stored value, then do a linear search (LinearSearch_82284448) to find or create the entry.

# Args: obj (r3, ->80 = a stored hash), (r4), (r5), src (r6, ->0 = a string, or 0 to use a global
# string). If r4 or r5 is 0, returns (no-op). Otherwise it hashes the string (HashCstring_82188CF0,
# sub_82188CF0), merges the hash bits into obj->80 (rlwimi/rlwinm bit manipulation), and does a
# linear search (LinearSearch_82284448, sub_82284448) to find the matching entry; if not found it
# creates one (ConstructRefCounted_8222CF18, a refcounted object). So it's a "hash the key, look up
# (or create) the entry" routine. 122 lines. 6 callers (sub_828152D0, sub_828C9360, sub_823F9620,
# ...). The entry class and the exact hash combination aren't resolvable, so the name states the
# mechanism: a hash lookup / get-or-create.
[entrypoint.functions."0x823F74D0"]
name = "HashLookup_823F74D0"

# A value setter / constructor: zero the object's 12-byte field, and if the incoming value is
# non-null, validate it (error if out of range) and construct it (sub_823093F8).

# Args: obj (r3, ->4/->8/->12 = a 12-byte field), value (r4). Zeros the field (obj->4 = obj->8 =
# obj->12 = 0). If value is 0, returns 0 (a null/empty result). Otherwise it validates the value
# (if value > a limit -- a 0x3FFF-style bound -- it calls sub_82684B38, an error/assert; else it
# calls sub_823093F8 to construct/store the value). Returns the result. 85 lines. 3 callers
# (sub_8236ED00, sub_8236EC48, sub_82B1BF08). A "set the value (validating it)" routine for a
# small value object. The value class and the exact bound aren't resolvable, so the name states
# the mechanism: set the value (zero, validate, construct).
[entrypoint.functions."0x8236FCE8"]
name = "SetValue_8236FCE8"

# A character-skip scan (the sibling of ScanLineLength_82B477F0): walk a string (from obj->0 +
# obj->36), advancing past characters that have the bit-3 ctype class (Ctype_Pred_Bit3_82CA6B50) or
# a newline, stopping at null.

# Arg: obj (r3, ->0 = a string base, ->36 = an offset). Computes the start as (obj->0 + obj->36)
# and scans forward: for each byte (sign-extended), if it has the bit-3 character class
# (Ctype_Pred_Bit3_82CA6B50, sub_82CA6B50) or is a newline (10), it advances; it stops at a null
# byte. So it's a "skip the leading characters of the bit-3 class" routine (e.g. skip leading
# whitespace / separators). 142 lines, 1 callee (Ctype_Pred_Bit3_82CA6B50). 8 callers (sub_82359BE8,
# sub_82B478C0, sub_826939A0, ...). The bit-3 ctype class's exact meaning (whitespace, separators,
# ...) and the string's context aren't resolvable from the body alone, so the name states the
# mechanism: skip characters matching the bit-3 ctype class.
[entrypoint.functions."0x82B47610"]
name = "SkipCtypeChars_82B47610"

# A quote-parity scan: walk a string (from obj->0 + obj->36, for a given length), count the
# quotes (double-quote " = 34, apostrophe ' = 39), and store the parity (odd/even) in obj->32.

# Args: obj (r3, ->0 = a string base, ->32 = a byte flag, ->36 = an offset), length (r4). Scans the
# (length) bytes starting at (obj->0 + obj->36); for each byte, if it's a double-quote (34) or an
# apostrophe (39), it toggles the parity (recomputes (32 - clz(obj->32 & 0xFF)) & 1 -- an odd/even
# quote-count). It stores the resulting parity in obj->32. So it's a "count the quotes and store
# the parity" routine (a quote-balance / unterminated-string detector for a text/log/line reader).
# 62 lines, 0 external callees (all inline byte scanning). Many callers (sub_82B3BAC0, sub_82607260,
# sub_82B475A0, ...). The string's context (a log line, a quoted string, ...) isn't resolvable,
# so the name states the mechanism: scan for quotes and store the parity.
[entrypoint.functions."0x82B47A40"]
name = "ScanQuoteParity_82B47A40"

# A region grow: if the incoming value exceeds the current size (obj->36), grow the region
# (ProcessIndexedRegion_82BA9068 / ProcessIndexedRegion2_82BA8F18) and zero/copy the new space
# (MemSet / MemCpy).

# Args: obj (r3, ->36 = a current size/limit, ->40 = a field), value (r4, the requested size),
# (r5). If value <= obj->36, the region is already large enough (a fast path). Otherwise it gets
# the global singleton (GetSingleton_82BA8718, sub_82BA8718), grows the region by calling
# ProcessIndexedRegion2_82BA8F18 (sub_82BA8F18, with (value * 4) and a constant 141) or
# ProcessIndexedRegion_82BA9068 (sub_82BA9068), and zeros/copies the newly added space
# (MemSet_SdkRuntime_82CA3190 / MemCpy_SdkRuntime_82CA2C60). So it's a "grow the region to fit the
# value" routine (a dynamic-array / region grow). 88 lines. Many callers (sub_82BB7700,
# sub_823F9398, ... plus FloatToIntProcess_82284810 -- sub_82284810). Part of the 0x82BA8 region/
# grow cluster. The region class and the 141 constant's meaning aren't resolvable, so the name
# states the mechanism: grow the region.
[entrypoint.functions."0x823F9AE0"]
name = "GrowRegion_823F9AE0"

# A u16 / UTF-16 string compare (a 16-bit memcmp): compare count 16-bit elements, return 0 if
# equal, else (a - b) as a 3-way result.

# Args: a (r3, a u16 array), b (r4, a u16 array), count (r5, the number of u16 elements). If count
# is 0, returns 0 (equal). Otherwise it walks the arrays (2 bytes at a time), comparing each u16;
# if all are equal it returns 0. On the first difference it computes (a_u16 - b_u16) (a
# sign-extended 3-way comparison result: negative if a < b, positive if a > b, 0 if equal) and
# returns that. So it's a "memcmp for 16-bit (UTF-16) strings" routine. 55 lines, 0 external
# callees (all inline u16 compares). 3 callers (sub_822F6480, sub_8217A858, sub_82377FD8 -- in the
# TODO). Part of the string-compare family (StrCmp_Signed_8226D7A8, CompareStrings_8228B5A0); this
# one operates on 16-bit elements. The string's context (UTF-16 text, a locale string, ...) isn't
# resolvable, so the name states the mechanism: compare u16 elements.
[entrypoint.functions."0x822F85C8"]
name = "CompareU16_822F85C8"

# A clamped range write / update: grow the object if needed (sub_82CD12C8), clamp the write
# length (to the available space and a max), then write the range.

# Args: obj (r3, ->20 = a size, ->24 = a field), pos (r4, the write position), len (r5, the
# requested length), max (r6, a max length), (r7). If obj->20 < pos, it grows the object by
# calling sub_82CD12C8 (a grow/extend helper). It then clamps the write length: to (obj->20 -
# pos) (the available space) and to max (whichever is smaller). It then writes the range (calling
# sub_822F85C8 -- CompareU16_822F85C8 -- to compare/write the 16-bit data). So it's a "write a
# range, growing and clamping as needed" routine. 107 lines. Many callers, in the 0x82C7 cluster
# (sub_82C71F00, sub_82C7EF10, sub_82C70750, ...). The object class (a string/buffer with a size
# field) and the exact write semantics aren't resolvable, so the name states the mechanism: a
# clamped range write.
[entrypoint.functions."0x82377FD8"]
name = "WriteRangeClamped_82377FD8"

# A 4-byte-stride array constructor: given a size, do an overflow check, run a setup step
# (sub_822F1F00), and allocate size * 4 bytes.

# Args: size (r3), (r4). If size is 0, skips the setup. Otherwise it does a size-overflow check
# ((0xFFFFFFFF / size) >= 4 -- guarding against size * 4 overflowing 32 bits); if it passes, it
# calls sub_822F1F00 (a setup, with a global base). Then it allocates (size * 4) bytes via
# Allocate_OrDbgBreak_8221F388 (a 4-byte-stride array) and returns the allocation. So it's a
# "construct a 4-byte-stride array of the given size" routine. 66 lines. Many callers (sub_82308690,
# sub_82370688, ... plus SetValue_8236FCE8 -- sub_8236FCE8 -- which uses it to build the value).
# The array's element class (32-bit values / pointers) isn't resolvable, so the name states the
# mechanism: construct a 4-byte-stride array.
[entrypoint.functions."0x823093F8"]
name = "ConstructArray4_823093F8"

# An error / exception handler: check a bound, and if the value is out of range, build an error
# message (SetString + a format) and raise it (sub_822F1F00).

# Args: obj (r3, ->8 = a value/bound), value (r4). Computes a bound (0x1FFF3FFF, a 0x1FFF-style
# limit) and checks (obj->8 - bound) >= value; if it passes, it returns normally (in range). If it
# fails (out of range), it builds an error message: SetString_822F2020 (a global error string),
# sub_826C3EF0 (a format op), and sub_826C3FA8 (a further format op), then stores a global error
# code and calls sub_822F1F00 (the exception raiser). So it's an "assert / raise on
# out-of-range" routine. 81 lines. 4 callers (sub_82A0F0A0, sub_82A0F290, sub_825180F0,
# sub_82461990). The exact error message and the bound's meaning aren't resolvable from the body
# alone, so the name states the mechanism: raise if out of range.
[entrypoint.functions."0x82461DA0"]
name = "RaiseIfOutOfRange_82461DA0"

# A trivial constant-return stub: always returns 512.

# Args: (none used). Sets r3 = 512 and returns. 2 lines. 0 direct callers -- so it's most likely a
# virtual method reached via a vtable (a "get the size / capacity / default" getter that always
# yields 512, e.g. a default buffer/capacity size), or otherwise-dead code. The constant 512's
# meaning (a default capacity, an element count, a type tag, ...) can't be determined from the body
# alone. The name states the mechanism: a stub that returns the constant 512.
[entrypoint.functions."0x82A145E8"]
name = "ReturnConstant512_82A145E8"

# A struct copy (the sibling of CopyStructData_83235608): if src->20 >= 4, copy a
# variable-length byte array from src->12 (byte-by-byte to the stack) and advance src->12, then
# process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size). If src->20 <
# 4, takes a short path. Otherwise it copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop
# to the stack), advances src->12 by 4, and continues processing (calling sub_82A1B480, a sibling
# in the 0x82A1B cluster). So it's a "copy the struct's data" routine. 216 lines. 8 callers (sub
# _82A93850, sub_82B25930, sub_83212DC0, ...). A sibling of CopyStructData_83235608 (same
# variable-length byte-array copy shape); the "2" distinguishes the two. The struct class and the
# exact field semantics aren't resolvable, so the name states the mechanism: copy the struct's
# data.
[entrypoint.functions."0x82A1BD30"]
name = "CopyStructData2_82A1BD30"

# A thin guard + dispatch: if both arguments are null, return 0; otherwise load a global and
# tail-call sub_8240DAA8.

# Args: (r3), a (r4), (r5), b (r6). If both a and b are 0, it returns 0 (nothing to do). Otherwise
# it loads a global base (from a fixed .data address) and tail-calls sub_8240DAA8 (the actual
# work). So it's a "guard (both-null fast path) then dispatch to sub_8240DAA8" thunk. 24 lines.
# 0 direct callers (it's reached via a dispatch/vcall, not a direct call). The global's meaning and
# sub_8240DAA8's role (the real operation) aren't resolvable from this thunk alone, so the name
# states the mechanism: a guard that dispatches.
[entrypoint.functions."0x8227CB70"]
name = "GuardDispatch_8227CB70"

# A string compare (with two cases): if a->0 is null, compare b byte-by-byte against a global
# charset string; otherwise compare a->0 to b via StrCmp_Signed_8226D7A8.

# Args: a (r3, ->0 = a string or a null flag), b (r4, a string). If a->0 is 0 (null), it compares
# b against a global charset string byte-by-byte (a per-byte difference test). Otherwise it
# compares a->0 (the string) to b via StrCmp_Signed_8226D7A8 (sub_8226D7A8). It then derives a
# small result (via a leading-zero count of the comparison result) and returns it. So it's a
# "compare the string (charset match, or a signed compare)" routine. 74 lines, 1 callee
# (StrCmp_Signed_8226D7A8). Many callers (sub_824389E0, sub_82357550, sub_828214A8, ...). A
# sibling of CompareStrings_8228B5A0 and StrCmp_Signed_8226D7A8 (the string-compare family); this
# one adds a charset branch. The string/charset context isn't resolvable, so the name states the
# mechanism: compare the string.
[entrypoint.functions."0x8229AD78"]
name = "CompareString_8229AD78"

# A line / value parser: skip the leading characters (SkipCtypeChars_82B47610), then inspect the
# current character (a ctype-bit-2 class or a minus sign '-' = 45) and parse a value.

# Args: obj (r3, ->0 = a string base, ->36 = an offset), (r4). Calls SkipCtypeChars_82B47610 (sub
# _82B47610) to skip the leading characters of the bit-3 ctype class. It then reads the current
# character (obj->0 + obj->36), and if it has the bit-2 ctype class (Ctype_Pred_Bit2_82CA6B10,
# sub_82CA6B10) or is a minus sign (45), it parses the (possibly signed) value (checking the
# following character too, and calling sub_82B47AB0, sub_82B47C70, sub_82B47D68, sub_82CA6AB0 --
# the 0x82B47 line/number-parse helpers). So it's a "parse a value from the line" routine (likely
# a signed integer/float from a log/property line). 111 lines. 9 callers (sub_825F5238, sub_82B47148,
# sub_82B473F0, ...). The value type and the line's context (a log, a config, a save) aren't
# resolvable, so the name states the mechanism: parse a value from the line.
[entrypoint.functions."0x82B46D40"]
name = "ParseLineValue_82B46D40"

# A number / decimal value parser: scan the numeric part of the line (the bit-2 ctype class --
# digits -- plus a decimal point '.' = 46), and construct a refcounted value object.

# Args: obj (r3, ->0 = a string base, ->36 = an offset), (r4). Reads the current character (obj->0
# + obj->36) and scans the numeric part: it walks while the characters have the bit-2 ctype class
# (Ctype_Pred_Bit2_82CA6B10, sub_82CA6B10 -- plausibly a digit class) and handles a decimal point
# ('.' = 46). It then constructs a refcounted value object (ConstructRefCounted_8222CF18,
# sub_8222CF18), checks the quote parity (ScanQuoteParity_82B47A40, sub_82B47A40), and calls
# sub_82B47F98 (a further parse helper) and DestructRefCounted_82214F08 (sub_82214F08). So it's a
# "parse a numeric value from the line and build the value object" routine (a float/decimal
# parser for a log/property/line reader). 256 lines. 1 caller: ParseLineValue_82B46D40 (sub_82B46D40).
# The value type (float/decimal) and the line's context aren't resolvable, so the name states the
# mechanism: parse a numeric value.
[entrypoint.functions."0x82B47AB0"]
name = "ParseNumberValue_82B47AB0"

# A trivial constant-return stub: always returns 64.

# Args: (none used). Sets r3 = 64 and returns. 2 lines. 0 direct callers -- so it's most likely a
# virtual method reached via a vtable (a "get the size / capacity / default" getter that always
# yields 64, e.g. a 64-byte element/capacity size), or otherwise-dead code. Part of a small cluster
# of adjacent constant-return stubs (sub_82A145F0 returns 16384; siblings ReturnConstant512_82A145E8,
# ReturnConstant16_8269B5F8, ReturnConstant8_826EB210). The constant 64's meaning (an element size,
# a count, a type tag, ...) can't be determined from the body alone. The name states the mechanism:
# a stub that returns the constant 64.
[entrypoint.functions."0x82A145D0"]
name = "ReturnConstant64_82A145D0"

# A trivial constant-return stub: always returns 128.

# Args: (none used). Sets r3 = 128 and returns. 2 lines. 0 direct callers -- most likely a virtual
# method reached via a vtable (a "get the size / capacity / default" getter that always yields 128,
# e.g. a 128-byte element/capacity size), or otherwise-dead code. Part of the small cluster of
# adjacent constant-return stubs (sub_82A145D0 = 64, this one = 128, sub_82A145E8 = 512,
# sub_82A145F0 = 16384; siblings ReturnConstant512_82A145E8, etc.). The constant 128's meaning (an
# element size, a count, a type tag, ...) can't be determined from the body alone. The name states
# the mechanism: a stub that returns the constant 128.
[entrypoint.functions."0x82A145D8"]
name = "ReturnConstant128_82A145D8"

# A range-advance + dispatch (the sibling of AdvanceAndDispatch_82BAA4A8): run a setup (sub_82BAA5D8),
# advance the ranges (AdvanceRanges2_82BAA7B8), then dispatch a vcall.

# Args: obj (r3), (r4), (r5). If obj is null, it returns. Otherwise it calls sub_82BAA5D8 (a setup,
# passing obj), then AdvanceRanges2_82BAA7B8 (sub_82BAA7B8, with a global base and obj -- the second
# range-advance variant), and then dispatches a vcall (loading a global function pointer and
# bctrl'ing on it, passing obj). So it's a "set up, advance the ranges, and dispatch a vcall" routine
# (the 0x82BAA indexed-region / range subsystem). 61 lines. 1 caller: ProcessIndexedRegion_82BA9068
# (sub_82BA9068). The vcall's target and the range object's meaning aren't resolvable, so the name
# states the mechanism: advance the ranges and dispatch.
[entrypoint.functions."0x82BAA570"]
name = "AdvanceRangesDispatch_82BAA570"

# A global-array search: linearly search a global 20-byte-stride array for a value; on a match,
# compact the array (MemMove_SdkRuntime_82CAA2E0) -- i.e. find-and-remove.

# Args: value (r3). Loads a global base and its count (from base + 10244). If the count is > 0, it
# linearly scans the array (20-byte stride), comparing each element's ->0 field to value. On a match
# it computes the position (count - index) and compacts the array by calling MemMove_SdkRuntime_82CAA2E0
# (sub_82CAA2E0) to shift the trailing elements down (removing the matched one). So it's a "find the
# value in the global array and remove it" routine (part of the 0x82BAA indexed-region / range
# subsystem). 97 lines. 2 callers: AdvanceRangesDispatch_82BAA570 (sub_82BAA570) and sub_82BA8D90.
# The array's element class (20-byte records) and the value's meaning aren't resolvable, so the name
# states the mechanism: find the value in the global array.
[entrypoint.functions."0x82BAA5D8"]
name = "FindInGlobalArray_82BAA5D8"

# A trivial constant-return stub: always returns 256.

# Args: (none used). Sets r3 = 256 and returns. 2 lines. 0 direct callers -- most likely a virtual
# method reached via a vtable (a "get the size / capacity / default" getter that always yields 256,
# e.g. a 256-byte element/capacity size), or otherwise-dead code. Part of the small cluster of
# adjacent constant-return stubs (sub_82A145D0 = 64, sub_82A145D8 = 128, this one = 256,
# sub_82A145E8 = 512, sub_82A145F0 = 16384; siblings ReturnConstant512_82A145E8, etc.). The constant
# 256's meaning (an element size, a count, a type tag, ...) can't be determined from the body alone.
# The name states the mechanism: a stub that returns the constant 256.
[entrypoint.functions."0x82A145E0"]
name = "ReturnConstant256_82A145E0"

# A 16-byte value constructor: allocate 16 bytes and set up a 16-byte struct (from a global base and
# a source), with an out-of-range check.

# Args: obj (r3), value (r4), src (r5). Allocates 16 bytes (Allocate_OrDbgBreak_8221F388, sub_8221F388),
# then sets up the 16-byte struct: ->0 = a global base, ->4 = the global's ->4, ->8 = src->0 (a copied
# field), and further fields (the body continues past line 80). It also calls RaiseIfOutOfRange_82461DA0
# (sub_82461DA0, an error handler) to validate the value. So it's a "construct a 16-byte value object"
# routine. 80 lines. 4 callers: ProcessDataStructure_82A0DD10 (sub_82A0DD10), sub_82A0E0C0, sub_82A11500,
# sub_82870460. The struct's class (a 16-byte value/pair) and the field semantics aren't resolvable,
# so the name states the mechanism: construct a 16-byte value.
[entrypoint.functions."0x82A0F0A0"]
name = "Construct16ByteValue_82A0F0A0"

# A struct copy (a third sibling of CopyStructData_83235608 / CopyStructData2_82A1BD30): if obj->20 >=
# 4, copy a variable-length byte array from obj->12 (byte-by-byte to the stack) and advance obj->12,
# then process further.

# Args: obj (r3, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r4). If obj->20 < 4, takes a
# short path. Otherwise it copies (obj->20 - 4) bytes from obj->12 (a byte-by-byte loop to the stack),
# advances obj->12 by 4, and continues processing (calling sub_82A1B480, the sibling in the 0x82A1B
# cluster that CopyStructData2_82A1BD30 also calls). So it's a "copy the struct's data" routine. 124
# lines. 1 caller: ProcessDataStructure_82A0DD10 (sub_82A0DD10). The struct class and the exact field
# semantics aren't resolvable, so the name states the mechanism: copy the struct's data. The "3"
# distinguishes this from CopyStructData_83235608 and CopyStructData2_82A1BD30 (the same shape).
[entrypoint.functions."0x8322EFB0"]
name = "CopyStructData3_8322EFB0"

# A chain-root finder (the sibling of the FindChainRoot family): validate the object, then walk the
# chain (via node->8, checking the flag at node->17) to find the root node.

# Args: obj (r3, ->0 = a value, ->4 = the current node). Validates obj->0 (a trap-22 on a null value,
# the same invariant the FindChainRoot family checks). It then walks the chain: it reads the current
# node (obj->4), checks a flag byte (node->17); while the flag is set, it advances to the next node
# (node->8) and updates obj->4, until the flag is clear (the root). So it's a "walk the chain to the
# root" routine (the 0x8247 / node-chain traversal, a sibling of FindChainRoot_82B1D0F0 and its
# variants). 112 lines, no callees (pure data manipulation). 8 callers: LookupString2_8246C010
# (sub_8246C010), sub_825BF630, sub_82BFC138, sub_822FF960, ... The node class (->8 = next, ->17 = a
# flag) and the object's meaning aren't resolvable, so the name states the mechanism: find the chain
# root. The "5" distinguishes it from FindChainRoot, FindChainRoot2, FindChainRoot3, and
# FindChainRoot4.
[entrypoint.functions."0x824708F8"]
name = "FindChainRoot5_824708F8"

# A count-compare + dispatch (the 0x82A0B / float-data cluster): compute two 16-byte-stride counts
# from the object, compare them, and dispatch (to Construct3FloatPayload_82A0C0A0).

# Args: obj (r3, ->4 = a base, ->8 = an end, ->12 = another end). Computes two 16-byte-stride counts:
# ((obj->12 - obj->4) >> 4) and ((obj->8 - obj->4) >> 4) (each >> 4 divides a byte span into 16-byte
# elements). It compares the two counts; when one is larger, it dispatches (calling
# Construct3FloatPayload_82A0C0A0, sub_82A0C0A0 -- the 3-float payload builder -- and sub_82A0BAA0, a
# sibling in the 0x82A0B cluster). So it's a "compare the float-data counts and dispatch" routine
# (part of the float-data processing, the 0x82A0B/0x82A0C cluster). 96 lines. 2 callers:
# ProcessFloatData_832206E8 (sub_832206E8) and sub_832207B8. The object's class (a float-data buffer)
# and the field semantics aren't resolvable, so the name states the mechanism: compare the float-data
# counts.
[entrypoint.functions."0x82A0B5C8"]
name = "CompareFloatCounts_82A0B5C8"

# A trivial global-getter stub: return a pointer to a fixed global (0x82CCC8C0 + 19788 = 0x82D0388C).

# Args: (none used). Loads the address 0x82D0388C (a fixed .data global) into r3 and returns. 2 lines.
# 0 direct callers (most likely a virtual method / a "get the global singleton" accessor). A sibling
# of GetGlobalPtr_82C27FA0, GetGlobalPtr2_82C23D00, and GetGlobalPtr3_82240578 (all trivial global-
# pointer getters). The global's meaning (a singleton, a type table, a global object) can't be
# determined from the body alone. The name states the mechanism: get a global pointer (the "4" is the
# series index).
[entrypoint.functions."0x82C21AD0"]
name = "GetGlobalPtr4_82C21AD0"

# A trivial field getter: return *(r3 + 48) -- the field at offset 48.

# Args: obj (r3). Loads the 32-bit field at offset 48 of obj and returns it. 8 lines (no callees).
# 0 direct callers (most likely a virtual method / accessor reached via a vtable, e.g. a "get field 48"
# getter). The field's meaning (a size, a count, a pointer, a type) can't be determined from the body
# alone -- only that it's a 32-bit field at offset 48 of the object. A sibling of other trivial field
# getters (GetField4_8320F978). The name states the mechanism: get the field at offset 48.
[entrypoint.functions."0x82C6F840"]
name = "GetField48_82C6F840"

# A byte-value check: mask a value to a byte, look it up (sub_82CC2028), and return 192 (0xC0) if the
# lookup yields 192, else 0.

# Args: (r3), value (r4). Masks value to its low byte (r4 & 0xFF), looks it up via sub_82CC2028 (a
# byte-value lookup / table probe), and checks the result: if it is 192 (0xC0), it returns 192; else
# it returns 0. So it's a "check whether this byte value maps to 0xC0 (return 0xC0 or 0)" routine. 35
# lines, 1 callee (sub_82CC2028). Many callers (sub_8236C520, sub_82C7E7B8, sub_82C10B90, ...). The
# byte's meaning and the 0xC0 sentinel (a category, a flag, a special code) aren't resolvable from the
# body alone, so the name states the mechanism: check a byte value (and return 0xC0 on a match).
[entrypoint.functions."0x82CBC678"]
name = "ByteValueCheck_82CBC678"

# A delay / sleep: compute a millisecond delay as a 100ns-tick relative time and call
# __imp__KeDelayExecutionThread (a wait) in a loop.

# Args: value (r3, a duration in milliseconds), flag (r4, ->0 = a flag byte). If value is -1, it uses a
# fixed 64-bit timeout (0xFFFFFFFF_00000000). Otherwise it computes value * -10000 (a negative 100ns-
# tick relative time: 10000 ticks = 1 ms, so value ms). It then loops, calling
# __imp__KeDelayExecutionThread (a kernel delay, passing the 100ns-tick time and flag), until the flag
# byte (value & 0xFF of the flag arg) is 0. So it's a "delay for value milliseconds (polling a flag)"
# routine. 83 lines. 2 callers: ByteValueCheck_82CBC678 (sub_82CBC678) and sub_82CBC6B0. The flag's
# meaning (an "interrupted" / "abort" flag) isn't resolvable, so the name states the mechanism: delay
# for a duration in milliseconds.
[entrypoint.functions."0x82CC2028"]
name = "DelayMilliseconds_82CC2028"

# A 16-byte-stride array constructor (the sibling of ConstructArray4_823093F8): given a size, do an
# overflow check, run a setup step (sub_822F1F00), and allocate size * 16 bytes.

# Args: (r3), size (r4), (more). If size is 0, skips the setup. Otherwise it does a size-overflow check
# ((0xFFFFFFFF / size) >= 16 -- guarding against size * 16 overflowing 32 bits); if it passes, it calls
# sub_822F1F00 (a setup, with a global base). Then it allocates (size * 16) bytes via
# Allocate_OrDbgBreak_8221F388 (a 16-byte-stride array) and returns the allocation. So it's a "construct
# a 16-byte-stride array of the given size" routine. 66 lines. Many callers (sub_82358250, sub_82442A78,
# sub_82A97298, ...). A sibling of ConstructArray4_823093F8 (sub_823093F8, the 4-byte-stride version);
# the "16" marks this 16-byte variant. The array's element class (16-byte records) isn't resolvable,
# so the name states the mechanism: construct a 16-byte-stride array.
[entrypoint.functions."0x82512640"]
name = "ConstructArray16_82512640"

# A string insert / append: if len is -1, compute strlen(src); check the object's capacity (obj->6);
# then copy the string in (MemCpy) and update the region (ProcessIndexedRegion / ProcessIndexedRegion2).

# Args: obj (r3, ->0 = a data pointer, ->6 = a u16 capacity), src (r4), len (r5). If len is -1, it
# computes the length of src (a strlen: walk to the null byte). Otherwise it uses len as the length.
# It then compares the object's capacity (obj->6, a u16) to the length; if there's room, it copies the
# string (MemCpy_SdkRuntime_82CA2C60, sub_82CA2C60) and updates the indexed region (ProcessIndexedRegion
# _82BA9068, ProcessIndexedRegion2_82BA8F18, GetSingleton_82BA8718 -- the 0x82BA8 indexed-region
# subsystem). So it's a "insert / append a string into the region" routine. 135 lines. Many callers (all
# in the 0x82BA/0x82BB region: sub_82BBE220, sub_82BB0D78, sub_82BAA310, ...). The region object's
# meaning (a string buffer / log) isn't resolvable, so the name states the mechanism: insert a string.
[entrypoint.functions."0x82BA8850"]
name = "StringInsert_82BA8850"

# A value-set wrapper (around SetValue_8236FCE8): compute a 4-byte element count from the source, call
# SetValue, and check the result.

# Args: obj (r3), src (r4, ->4 = a start, ->8 = an end). Computes a 4-byte element count: ((src->8 -
# src->4) >> 2) (a byte span divided into 4-byte elements). It then calls SetValue_8236FCE8 (sub_8236FCE8,
# the value setter) with the count, and checks the result (a byte: if non-zero, it compares src->4 and
# src->8 and continues processing -- calling sub_82371378, a sibling in the 0x8236/0x8237 cluster). So it's
# a "set the value from the source array" routine (a thin wrapper around SetValue_8236FCE8). 105 lines.
# 7 callers (sub_82C67088, sub_82C67220, sub_82C673D8, sub_82370EF8, ...). The value's type and the src
# array's meaning aren't resolvable, so the name states the mechanism: set the value (the "2" is the
# series index, distinct from SetValue_8236FCE8).
[entrypoint.functions."0x8236EC48"]
name = "SetValue2_8236EC48"

# A list node splice (the sibling of List_SpliceNode_8292CC00): manipulate a doubly-linked list (via
# ->0 = prev, ->4 = next, ->8) to insert/unlink a node.

# Args: list (r3, ->4 = the head), node (r4, ->0 = prev, ->4 = next). Reads node->8 (the next node),
# then manipulates the linked list: it unlinks/links the node (updating the prev/next pointers of the
# neighbors, and checking a flag byte at offset 33 of a neighbor, ->33). It walks the list (comparing
# node->4 and neighbor->4 to node->0) and re-splices the nodes (updating ->0 / ->4). So it's a "splice a
# node into / out of the doubly-linked list" routine. 67 lines, no callees (pure data manipulation).
# Many callers: ConstructNodeValue_82B4C1C0 (sub_82B4C1C0), sub_82308D70, sub_82323550, sub_823B0DB8, ... A
# sibling of List_SpliceNode_8292CC00 (sub_8292CC00, the same doubly-linked-list splice shape); the "2"
# distinguishes the two. The node's class (->33 = a flag) and the list's meaning aren't resolvable, so
# the name states the mechanism: splice a list node.
[entrypoint.functions."0x8292CC88"]
name = "List_SpliceNode2_8292CC88"

# An array copy with refcounts: copy 4-byte-stride elements from src to dst, incrementing each
# element's refcount (atomic lwarx/stwcx under the global lock).

# Args: dst (r3, the destination), pos (r4), end (r5), src (r6, the source). If pos == end, returns
# (nothing to copy). Otherwise it walks the 4-byte-stride elements (dst->0 = the element, dst->4 = the
# element's refcount) and copies each from src to dst. For each non-null element (dst->4 != 0), it
# atomically increments the element's refcount (an lwarx/stwcx compare-and-swap under the global lock,
# retrying on contention). So it's a "copy an array of refcounted elements (taking a reference on each)"
# routine (a reference-counted array insert/copy). 69 lines, no callees (pure data manipulation with
# atomic refcount ops). Many callers (sub_8260B008, sub_827F1AB0, sub_82AB4058, ...). The element's class
# (a refcounted value, ->4 = the refcount) isn't resolvable, so the name states the mechanism: copy a
# refcounted array.
[entrypoint.functions."0x827F2010"]
name = "CopyRefcountedArray_827F2010"

# A hash-lookup + compare: look up a value (HashLookup_823F74D0), compare the string
# (CompareString_8229AD78), and handle the refcounted result (CopyAssign / Destruct), recursing as needed.

# Args: obj (r3, the hash table / container), value (r4). If obj is null, returns. Otherwise it looks up
# the value (HashLookup_823F74D0, sub_823F74D0 -- the hash-table get-or-create), then compares the
# string (CompareString_8229AD78, sub_8229AD78). Depending on the comparison result, it handles the
# refcounted object: it may copy-assign (CopyAssign_RefCounted_821F0108, sub_821F0108) or destructure
# (DestructRefCounted_82214F08, sub_82214F08) the value, and recurses (sub_823F9620, plus sub_823F8C30 and
# sub_823F8C90, siblings in the 0x823F9 cluster). So it's a "look up a value in the hash table and
# compare it" routine. 116 lines. 2 callers: sub_82817D28 and sub_828152D0. The table's key/value class
# (strings) and the exact match semantics aren't resolvable, so the name states the mechanism: hash-
# lookup and compare.
[entrypoint.functions."0x823F9620"]
name = "HashLookupCompare_823F9620"

# A float-data processor (the sibling of CompareFloatCounts_82A0B5C8): compute 16-byte-stride counts
# from the object, build a 3-float payload (Construct3FloatPayload_82A0C0A0) and 16-byte arrays
# (ConstructArray16_82512640), and free intermediates.

# Args: obj (r3, ->4 = a base, ->8 = an end, ->12 = another end), (r4), (r5), (r6). Calls
# Construct3FloatPayload_82A0C0A0 (sub_82A0C0A0, the 3-float payload builder). It then computes two
# 16-byte-stride counts: ((obj->12 - obj->4) >> 4) and ((obj->8 - obj->4) >> 4) (each >> 4 divides a byte
# span into 16-byte elements). It processes the float data, allocating 16-byte arrays (ConstructArray16
# _82512640, sub_82512640) and freeing intermediates (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68), and
# calling sub_82A0F950 and sub_82A97648 (siblings in the 0x82A0B/0x82A97 clusters). So it's a "process the
# float data (build the payload and arrays)" routine (part of the float-data / animation-data cluster).
# 447 lines. 1 caller: CompareFloatCounts_82A0B5C8 (sub_82A0B5C8). The data's class (animation/float
# samples) and the field semantics aren't resolvable, so the name states the mechanism: process the float
# data. The "2" distinguishes it from ProcessFloatData_832206E8 (sub_832206E8).
[entrypoint.functions."0x82A0BAA0"]
name = "ProcessFloatData2_82A0BAA0"

# A struct copy (a fourth sibling of CopyStructData_83235608 / CopyStructData2_82A1BD30 /
# CopyStructData3_8322EFB0): if src->20 >= 4, copy a variable-length byte array from src->12 (byte-by-
# byte to the stack) and advance src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size). If src->20 < 4, takes
# a short path. Otherwise it copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop to the stack),
# advances src->12 by 4, and continues processing (calling sub_82A1B480, the sibling in the 0x82A1B cluster
# that the other CopyStructData* variants also call). So it's a "copy the struct's data" routine. 345 lines.
# Many callers (sub_82B250E8, sub_82B20180, sub_82B31CA0, ...). The struct class and the exact field
# semantics aren't resolvable, so the name states the mechanism: copy the struct's data. The "4"
# distinguishes this from CopyStructData, CopyStructData2, and CopyStructData3 (the same shape).
[entrypoint.functions."0x82A1BEA8"]
name = "CopyStructData4_82A1BEA8"

# A virtual-method dispatch + position adjust: call a vtable method on a sub-object, conditionally
# insert (sub_82BCCB88), then adjust a position field (obj->16->68 += delta - pos).

# Args: obj (r3, ->16 = a sub-object), (r4), pos (r5), delta (r6). Reads the sub-object (obj->16) and
# calls one of its virtual methods (loading obj->16->12 as the method address and bctrl'ing, with
# obj->16->16 as a first arg). If the method returns non-zero and delta is non-zero, it inserts (sub
# _82BCCB88, an insert, passing obj and the constant 4). Then it adjusts a position field:
# obj->16->68 = obj->16->68 - pos + delta (a position shift). So it's a "dispatch a virtual method and
# adjust the position" routine (part of the 0x82BC state-dispatch / position subsystem). 60 lines, 1
# callee (sub_82BCCB88). Many callers, all in the state-dispatch cluster: StateDispatch_82BCBB40 (sub
# _82BCBB40), sub_82BDE878, sub_82BCD7B0, sub_82BC9FD0, ... The sub-object's class (->12 = a vtable
# method, ->16 = an arg, ->68 = a position) and the dispatch's meaning aren't resolvable, so the name
# states the mechanism: dispatch and adjust the position.
[entrypoint.functions."0x8227BB58"]
name = "DispatchAndAdjustPos_8227BB58"

# A trivial global-getter stub: return a pointer to a fixed global (0x82CCC8C0 + 19776 = 0x82D03880).

# Args: (none used). Loads the address 0x82D03880 (a fixed .data global) into r3 and returns. 2 lines.
# 0 direct callers (most likely a virtual method / a "get the global singleton" accessor). A sibling of
# GetGlobalPtr_82C27FA0, GetGlobalPtr2_82C23D00, GetGlobalPtr3_82240578, and GetGlobalPtr4_82C21AD0 (all
# trivial global-pointer getters). Notably, GetGlobalPtr4_82C21AD0 returns 0x82D0388C, just 12 bytes above
# this one -- so this is a getter for a neighboring field of the same global object. The global's meaning
# (a singleton, a type table, a global object) can't be determined from the body alone. The name states
# the mechanism: get a global pointer (the "5" is the series index).
[entrypoint.functions."0x82C1F258"]
name = "GetGlobalPtr5_82C1F258"

# A line parser (the 0x82B47 line-reader cluster): atomically increment a global line counter, parse the
# line (ParseLineValue_82B46D40), and handle the refcounted result.

# Args: obj (r3, the line reader), (r4), (more). Atomically increments a global counter (from a fixed
# .data address, 0x82CCC8C0 + 28344 -- a line/record counter, incremented via an lwarx/stwcx compare-and-
# swap under the global lock, retrying on contention). It then parses the line (ParseLineValue_82B46D40,
# sub_82B46D40) and handles the result: it constructs a refcounted object (ConstructRefCounted_8222CF18,
# sub_8222CF18) and destructures it on a path (DestructRefCounted_82214F08, sub_82214F08), and calls
# sub_82B47E40 (a further line helper). So it's a "parse a line (incrementing the line counter and
# building the value object)" routine (a log/property line reader). 162 lines. 5 callers (sub_828152D0,
# sub_829DD350, sub_825F5238, sub_82817D28, sub_82466098). The line's content (a log, a config, a save)
# isn't resolvable, so the name states the mechanism: parse a line.
[entrypoint.functions."0x82B47218"]
name = "ParseLine_82B47218"

# A virtual-method dispatch thunk: call the method at offset 36 of the sub-object (obj->0->36), passing
# (obj, arg).

# Args: obj (r3, ->0 = a sub-object), arg (r4). Reads the sub-object (*obj), loads its method pointer
# (sub-object->36, a vtable slot at offset 36), and indirect-calls it (bctrl) with (obj, arg), returning
# the result. So it's a "dispatch to the sub-object's slot-36 virtual method" thunk. 13 lines, no direct
# callees (the target is an indirect vcall). 1 caller: sub_82C14FD0. The sub-object's class (->36 = the
# vtable slot) and the method's behavior (it depends on the concrete object's vtable) aren't resolvable
# from this thunk alone, so the name states the mechanism: dispatch to slot 36 of the sub-object.
[entrypoint.functions."0x82C010C0"]
name = "DispatchSlot36_82C010C0"

# A virtual-method dispatch triple: call the sub-object's slot-24 method; if non-zero, call slot 36
# (DispatchSlot36_82C010C0); if still non-zero, call slot 28.

# Args: obj (r3, ->0 = a sub-object), (r4), arg (r5). Reads the sub-object (*obj) and calls its slot-24
# virtual method (obj->0->24, bctrl) with (obj, arg), checking the result (a byte). If non-zero, it calls
# DispatchSlot36_82C010C0 (sub_82C010C0, the slot-36 dispatch) with (obj, arg), checking that result too.
# If still non-zero, it calls another sub-object virtual method (obj->0->28, bctrl). So it's a "dispatch a
# triple of virtual methods (slots 24, 36, 28), each gated on the previous non-zero result" routine (a
# virtual callback chain on the sub-object). 86 lines, 1 callee (DispatchSlot36_82C010C0). 3 callers
# (sub_82C29550, sub_82C161D8, sub_82C1F380). The sub-object's class (->24, ->28, ->36 = vtable slots) and
# the methods' behavior (concrete-vtable-dependent) aren't resolvable, so the name states the mechanism:
# dispatch a triple of vcalls. Sibling of Dispatch_VcallSequence_82BCB6E0 / Dispatch_VcallSequence2_82BCCF50
# (vcall-sequence dispatchers); the "Triple" marks this 3-slot variant.
[entrypoint.functions."0x82C14FD0"]
name = "DispatchVcallTriple_82C14FD0"

# A virtual-method dispatch sequence: call the source sub-object's slot-20 and slot-0 methods, then run
# a sequence of vcall-chain / vcall-triple dispatches.

# Args: obj (r3, ->4), src (r4, ->0 = a sub-object), (more). Reads the source sub-object (*src) and calls
# its slot-20 virtual method (src->0->20, bctrl, with src), then its slot-0 method (src->0->0, bctrl, with
# src). It then runs a sequence of vcall dispatches, calling VcallTriple_AllSuccess_82C011E8 (sub_82C011E8)
# with (src, a global, obj->4, ...), and (later in the body) VcallChain_AllSuccess_82C14F38, VcallChain
# _AllSuccess_82C15068, DispatchVcallTriple_82C14FD0 (sub_82C14FD0), DestructRefCounted_82214F08 (sub
# _82214F08), plus sub_821F7798, sub_82BFD780, sub_82C14D70. So it's a "dispatch a long sequence of virtual
# methods (a vcall chain) on the source sub-object" routine (a virtual callback / visitor chain). 467 lines.
# 1 caller: sub_82C1FBD0. The sub-object's class (->0, ->20 = vtable slots) and the methods' behavior
# (concrete-vtable-dependent) aren't resolvable, so the name states the mechanism: dispatch a sequence of
# vcalls. Sibling of Dispatch_VcallSequence_82BCB6E0 / Dispatch_VcallSequence2_82BCCF50 (vcall-sequence
# dispatchers); the "3" marks this variant.
[entrypoint.functions."0x82C1F380"]
name = "DispatchVcallSequence3_82C1F380"

# A virtual-method dispatch sequence: call the sub-object's slot-24 method; if non-zero, dispatch the
# vcall sequence (DispatchVcallSequence3_82C1F380); if still non-zero, call slot 28.

# Args: obj (r3, ->0 = a sub-object), (r4), arg (r5). Reads the sub-object (*obj) and calls its slot-24
# virtual method (obj->0->24, bctrl) with (obj, arg), checking the result (a byte). If non-zero, it calls
# DispatchVcallSequence3_82C1F380 (sub_82C1F380, the long vcall-sequence dispatcher) with (obj, arg),
# checking that result too. If still non-zero, it calls another sub-object virtual method (obj->0->28,
# bctrl). So it's a "dispatch slot 24, then the vcall sequence, then slot 28 (each gated on the previous
# non-zero result)" routine (a virtual callback chain on the sub-object). 86 lines, 1 callee
# (DispatchVcallSequence3_82C1F380). 1 caller: sub_82C1FF48. A structural sibling of
# DispatchVcallTriple_82C14FD0 (sub_82C14FD0, same shape but calling slot 36 in the middle instead of the
# vcall sequence). The sub-object's class (->24, ->28 = vtable slots) and the methods' behavior (concrete-
# vtable-dependent) aren't resolvable, so the name states the mechanism: dispatch a sequence of vcalls.
[entrypoint.functions."0x82C1FBD0"]
name = "DispatchVcallSequence4_82C1FBD0"

# A virtual-method dispatch + array lookup: call the sub-object's slot-4 method, then read an element
# from an indexed array (obj->4[index]->4).

# Args: (r3), obj (r4, ->0 = a sub-object, ->4 = an indexed array), index (r5). Reads the sub-object
# (*obj) and calls its slot-4 virtual method (obj->0->4, bctrl, with obj and a global). It then does an
# indexed array lookup: r8 = obj->4 (the array base), r7 = index * 4, r6 = *(r8 + r7) (the array
# element), and returns *(r6 + 4) (the element's ->4 field). So it's a "dispatch a vcall, then read the
# ->4 of the array element at the given index" routine. 50 lines, no direct callees (the vcall target is
# indirect). 0 direct callers (reached via a dispatch). The sub-object's class (->4 = a vtable slot) and
# the array's element class (->4 = a field) aren't resolvable, so the name states the mechanism: dispatch
# and index the array.
[entrypoint.functions."0x82C1FEF0"]
name = "DispatchAndIndex_82C1FEF0"

# A struct initializer / constructor: zero the pointer fields, load float fields from globals, and set a
# global pointer.

# Args: obj (r3), (r4 = 2), (r5 = -1), (r6 = a global), (more). Initializes the struct: it sets the
# pointer fields to 0 (obj->4, ->8, ->12, ->16, ->20, ->24 = 0), loads float fields from fixed global
# .data addresses (obj->28, ->32, ->36, ->44, ->48 = global floats), and sets obj->0 to a global pointer.
# So it's an "initialize a struct (zero the pointers, fill the floats from globals, set the base pointer)"
# constructor. 83 lines, no callees (pure data setup). 2 callers: sub_82C1FF48 (the caller of
# DispatchVcallSequence4_82C1FBD0) and sub_82C1E878. The struct's class (a 52+ byte value with pointer and
# float fields) and the float fields' meaning aren't resolvable, so the name states the mechanism: init
# the struct.
[entrypoint.functions."0x82C4DAC8"]
name = "InitStruct_82C4DAC8"

# A dispose / destroy: call the sub-object dispose (sub_8233E380), free the sub-object (obj->4), and zero
# the fields.

# Args: obj (r3, ->4 = a sub-object, ->8 = a field). Calls sub_8233E380 (a dispose, passing obj, obj->4,
# and the sub-object's ->0 element) to run the sub-object's teardown. It then frees the sub-object (obj->4)
# via Free_SizeBucketed_Thunk_8221BE68 (sub_8221BE68). Finally it zeros the fields (obj->4 = 0, obj->8 = 0)
# to mark the object empty. So it's a "dispose / destroy the object (tear down the sub-object, free it,
# zero the pointers)" routine. 61 lines, 2 callees (sub_8233E380, Free_SizeBucketed_Thunk_8221BE68). Many
# callers (sub_82878DA8, sub_82851DF8, sub_8256EAF8, ...). The object's class (->4 = a sub-object, ->8 = a
# field) and the sub-object's teardown aren't resolvable, so the name states the mechanism: dispose the
# object. The "2" distinguishes it from Dispose_82B39A30 (sub_82B39A30).
[entrypoint.functions."0x8233DD70"]
name = "Dispose2_8233DD70"

# A dispose (with validation + chain-root lookup): validate the value (trap-22), compare it to the
# element, look up the chain root (FindChainRoot_82A596F0), and tear down.

# Args: obj (r3), value (r4), (r5), (r6), (r7), element (r8). Validates the value: if value is 0 or
# value != element, it traps (trap-22, the same invariant the FindChainRoot family checks). It then
# compares the element to value (bailing on a mismatch) and tears down: it looks up the chain root
# (FindChainRoot_82A596F0, sub_82A596F0), and calls sub_8234C988 and sub_826A4C60 (siblings in the
# 0x8233/0x8234/0x826A clusters) to run the teardown. So it's a "dispose the sub-object (validate, find
# the chain root, tear down)" routine. 137 lines, 3 callees (FindChainRoot_82A596F0, sub_8234C988,
# sub_826A4C60). Many callers: Dispose2_8233DD70 (sub_8233DD70), sub_8322BED0, sub_8268BBA0, ... The
# object's class and the teardown's steps aren't fully resolvable, so the name states the mechanism:
# dispose the sub-object. The "3" distinguishes it from Dispose_82B39A30 and Dispose2_8233DD70.
[entrypoint.functions."0x8233E380"]
name = "Dispose3_8233E380"

# A tree-node destroy wrapper: call DestroyTreeNodes_82859340 on the sub-object's nodes, then clean up
# the object's fields.

# Args: obj (r3, ->4 = a sub-object, ->8 = a field). Calls DestroyTreeNodes_82859340 (sub_82859340, the
# tree-node destroy, passing obj->4->4) to tear down the tree nodes. It then cleans up the object's
# fields: obj->4->4 = obj->4, obj->8 = 0, obj->4->0 = obj->4, obj->4->8 = obj->4 (setting the sub-object's
# pointers to itself / zeroing, a teardown/cleanup pattern). So it's a "destroy the tree nodes and clean
# up the object" routine. 50 lines, 1 callee (DestroyTreeNodes_82859340). 3 callers: Dispose3_8233E380
# (sub_8233E380), sub_827CCDD0, sub_82878F08. The tree's node class (->4 = the nodes) and the cleanup's
# semantics aren't resolvable, so the name states the mechanism: destroy the tree nodes. The "2"
# distinguishes it from DestroyTreeNodes_82859340 (the actual node-destroy) and DestroyTree_Recursive_82B4D218.
[entrypoint.functions."0x8234C988"]
name = "DestroyTreeNodes2_8234C988"

# A 20-byte node constructor: allocate 20 bytes, set up the node (zero the pointer fields, set the flag
# bytes ->16 = 1 and ->17 = 0), and store it in obj->4.

# Args: obj (r3, ->4 = a node slot). Allocates 20 bytes (Allocate_OrDbgBreak_8221F388, sub_8221F388), then
# sets up the node: zeroes the pointer fields (->0, ->4, ->8 = 0), sets a flag byte to 1 (->16) and another
# to 0 (->17), and stores the node pointer in obj->4. So it's a "construct a 20-byte node and store it in
# the object" routine. 83 lines, 1 callee (Allocate_OrDbgBreak_8221F388). Many callers (sub_82474760,
# sub_8249C690, sub_825C0658, ...). The node's class (a 20-byte record, ->16 = a flag, ->17 = a flag) and
# its role (a list/tree node) aren't resolvable, so the name states the mechanism: construct a 20-byte
# node. The "20" marks the node size (20 bytes), distinguishing it from other node constructors.
[entrypoint.functions."0x82A12B08"]
name = "ConstructNode20_82A12B08"

# A struct copy (a fifth sibling of CopyStructData_83235608 / CopyStructData2_82A1BD30 / CopyStructData3
# _8322EFB0 / CopyStructData4_82A1BEA8): if obj->20 >= 4, copy a variable-length byte array from obj->12
# (byte-by-byte to the stack) and advance obj->12, then process further.

# Args: obj (r3, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r4). If obj->20 < 4, takes a
# short path. Otherwise it copies (obj->20 - 4) bytes from obj->12 (a byte-by-byte loop to the stack),
# advances obj->12 by 4, and continues processing (calling sub_82A1B480, the sibling in the 0x82A1B cluster
# that the other CopyStructData* variants also call). So it's a "copy the struct's data" routine. 122
# lines. 3 callers (sub_83225E80, sub_83218300, sub_83225FD0). The struct class and the exact field
# semantics aren't resolvable, so the name states the mechanism: copy the struct's data. The "5"
# distinguishes this from CopyStructData, CopyStructData2, CopyStructData3, and CopyStructData4 (the same
# shape).
[entrypoint.functions."0x83217EC8"]
name = "CopyStructData5_83217EC8"

# A chain validation + find/remove: validate the pointers (trap-22), compare the node to the value,
# then look up the chain root (FindChainRoot2_823D4F20) and find/remove the node (Tree_FindAndRemove_82A14AE0).

# Args: obj (r3, ->12 = a sub-pointer, ->16 = a node). Validates the pointers (a trap-22 if a pointer is
# null or mismatched -- the same invariant the FindChainRoot family checks). It compares the node (obj->16)
# to a value, and on a match tears down / finds / removes: it looks up the chain root (FindChainRoot2_823D4F20,
# sub_823D4F20), finds and removes the node (Tree_FindAndRemove_82A14AE0, sub_82A14AE0), and destroys the
# list (DestroyList_Recursive_82443440, sub_82443440). So it's a "validate the node, then find/remove it from
# the tree" routine. 120 lines, 3 callees (FindChainRoot2_823D4F20, Tree_FindAndRemove_82A14AE0,
# DestroyList_Recursive_82443440). 2 callers: sub_83218C10 and sub_83219DF8. The node's class (->16 = a node,
# ->12 = a sub-pointer) and the tree's meaning aren't resolvable, so the name states the mechanism: validate
# and find/remove the node.
[entrypoint.functions."0x83218350"]
name = "ValidateAndFindRemove_83218350"

# A tree-node processor (a large routine, 1310 lines): validate the node (ValidateAndFindRemove_83218350),
# copy its struct data, construct 20-byte nodes, handle the refcounted value, and update the tree-node value.

# Args: obj (r3), value (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r5, r6, ... flags). It
# calls ValidateAndFindRemove_83218350 (sub_83218350, the validate + find/remove) first. It then does a struct
# copy (from value->12, the same shape as the CopyStructData* family -- calling sub_82A1B480), constructs
# 20-byte nodes (Construct20Byte_83215640, sub_83215640), handles the refcounted value (Release_RefCounted
# _829FF648, SetRefcountedWrapper_8238A848), and updates the tree-node value (UpdateTreeNodeValue_83218598,
# sub_83218598). It also calls a long sequence of sibling functions (sub_83215768 through sub_83215DF0, the
# 0x83215 cluster) and Allocate_OrDbgBreak_8221F388. So it's a "process / update a tree node (validate, copy,
# construct, and update)" routine (the big tree-node processor of the 0x83215/0x83218 cluster). 1310 lines --
# one of the largest functions in the recompile. 0 direct callers (reached via a dispatch / vcall). The node's
# class (->4, ->12, ->20 = data/size) and the tree's meaning (likely a game data structure: an animation/prop
# tree) aren't resolvable, so the name states the mechanism: process the tree node.
[entrypoint.functions."0x83218C10"]
name = "ProcessTreeNode_83218C10"

# A trivial field getter: return *(r4 + 12) -- the field at offset 12.

# Args: (r3), obj (r4). Loads the 32-bit field at offset 12 of obj (r4) and returns it. 8 lines (no
# callees). 0 direct callers (most likely a virtual method / accessor reached via a vtable, e.g. a "get
# field 12" getter). The field's meaning (a size, a count, a pointer, a type) can't be determined from the
# body alone -- only that it's a 32-bit field at offset 12. A sibling of other trivial field getters
# (GetField4_8320F978, GetField48_82C6F840; the adjacent sub_82284200 returns field 24). The name states
# the mechanism: get the field at offset 12.
[entrypoint.functions."0x82283EC8"]
name = "GetField12_82283EC8"

# A lookup + dispatch: look up a value (sub_823FE8F8), set a flag (0/1), and dispatch a virtual method
# (value->0->32).

# Args: value (r3), flag (r4), (more). Loads a global (from a fixed .data address) and, if flag is 0, looks
# up the value (sub_823FE8F8, a lookup). It sets a result flag (0 or 1, based on whether the lookup found
# something). If the flag is set (non-zero), it dispatches a virtual method on the value (loading value->0->32
# as the method address and bctrl'ing). So it's a "look up the value, and if found, dispatch its virtual
# method" routine (part of the 0x823F8/0x823FE hash/lookup cluster). 93 lines, 1 callee (sub_823FE8F8). 1
# caller: HashLookupCompare_823F9620 (sub_823F9620). The value's class (->0->32 = a vtable slot) and the
# lookup's meaning aren't resolvable, so the name states the mechanism: look up and dispatch.
[entrypoint.functions."0x823F8C90"]
name = "LookupAndDispatch_823F8C90"

# A virtual-method dispatch sequence: call VcallTriple_AllSuccess_82C011E8 repeatedly (with a different
# global base each time), gated on a non-zero result.

# Args: obj (r3, ->4), arg (r4). Calls VcallTriple_AllSuccess_82C011E8 (sub_82C011E8, the 3-slot vcall
# dispatcher) with (arg, a global base, obj), checking the result (a byte). If non-zero, it calls it again
# (with a different global base and obj->4), checking the result too, and continues the sequence (the body
# repeats this pattern with several different global bases). So it's a "dispatch a sequence of vcall
# triples (each with a different global base), gated on the previous non-zero result" routine (a virtual
# callback / visitor chain on the argument). 92 lines, 1 callee (VcallTriple_AllSuccess_82C011E8). 1 caller:
# sub_82C26AF0. The argument's class and the vcall targets' behavior (concrete-vtable-dependent) aren't
# resolvable, so the name states the mechanism: dispatch a sequence of vcalls. Sibling of
# Dispatch_VcallSequence_82BCB6E0, Dispatch_VcallSequence2_82BCCF50, DispatchVcallSequence3_82C1F380, and
# DispatchVcallSequence4_82C1FBD0; the "5" marks this variant.
[entrypoint.functions."0x82C269E8"]
name = "DispatchVcallSequence5_82C269E8"

# A virtual-method dispatch sequence: call the sub-object's slot-24 method; if non-zero, dispatch the vcall
# sequence (DispatchVcallSequence5_82C269E8); if still non-zero, call slot 28.

# Args: obj (r3, ->0 = a sub-object), (r4), arg (r5). Reads the sub-object (*obj) and calls its slot-24
# virtual method (obj->0->24, bctrl) with (obj, arg), checking the result (a byte). If non-zero, it calls
# DispatchVcallSequence5_82C269E8 (sub_82C269E8, the repeated VcallTriple_AllSuccess dispatcher) with (obj,
# arg), checking that result too. If still non-zero, it calls another sub-object virtual method (obj->0->28,
# bctrl). So it's a "dispatch slot 24, then the vcall sequence, then slot 28 (each gated on the previous
# non-zero result)" routine (a virtual callback chain on the sub-object). 86 lines, 1 callee
# (DispatchVcallSequence5_82C269E8). 1 caller: sub_82C26CA0. A structural sibling of DispatchVcallTriple
# _82C14FD0 and DispatchVcallSequence4_82C1FBD0 (same shape: slot 24, a middle dispatcher, slot 28). The
# sub-object's class (->24, ->28 = vtable slots) and the methods' behavior (concrete-vtable-dependent)
# aren't resolvable, so the name states the mechanism: dispatch a sequence of vcalls. The "6" marks this
# variant in the DispatchVcallSequence family.
[entrypoint.functions."0x82C26AF0"]
name = "DispatchVcallSequence6_82C26AF0"

# A 24-byte array copy (the plain, non-refcounting sibling of CopyRefcountedArray_827F2010): copy
# 24-byte-stride elements from src to dst (a straight memcpy-style element copy, no refcount increments).

# Args: dst (r3, the destination), pos (r4), end (r5), src (r6, the source). If pos == end, returns
# (nothing to copy). Otherwise it walks the 24-byte-stride elements, copying each one (a 24-byte element:
# six 32-bit words plus some float fields, copied word-by-word / float-by-float) from src to dst. Unlike
# CopyRefcountedArray_827F2010 (sub_827F2010, the 4-byte element copy that takes a reference on each
# element), this one does a plain copy (no refcount increments). So it's a "copy a 24-byte array of
# elements (plain, no refcounting)" routine. 82 lines, no callees (pure data copy). 1 caller: sub_82A13D30.
# The element's class (a 24-byte record: pointers + floats) isn't resolvable, so the name states the
# mechanism: copy a 24-byte array. The "24" marks the element size (24 bytes).
[entrypoint.functions."0x82A13FE0"]
name = "CopyArray24_82A13FE0"

# A dispose / destroy (with an optional free): set a vtable / type pointer, run the 20-byte destructor
# (Destruct20Byte_832156E0), and free the object (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 7072). It then runs the 20-byte destructor
# (Destruct20Byte_832156E0, sub_832156E0). If the flag's bit 0 is set (the object is owned), it frees the
# object (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68); otherwise it leaves the memory (a
# destruct-but-don't-free path). So it's a "dispose the object (set the type, run the destructor, free if
# owned)" routine. 56 lines, 2 callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). 0 direct
# callers (reached via a dispatch / vcall, likely a type's destructor or dispose method). The object's class
# (a 20-byte object, ->0 = a vtable) and the free flag's meaning aren't resolvable, so the name states the
# mechanism: dispose the object (with an optional free). The "4" distinguishes it from Dispose_82B39A30,
# Dispose2_8233DD70, and Dispose3_8233E380.
[entrypoint.functions."0x82A146C0"]
name = "Dispose4_82A146C0"

# A struct copy (a sixth sibling of the CopyStructData family): if src->20 >= 4, copy a variable-length
# byte array from src->12 (byte-by-byte to the stack) and advance src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r5). If src->20 < 4,
# takes a short path. Otherwise it copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop to the
# stack), advances src->12 by 4, and continues processing (calling sub_82A1B480, the sibling in the 0x82A1B
# cluster that the other CopyStructData* variants also call, plus sub_83211D48, a sibling in the 0x83211
# cluster). So it's a "copy the struct's data" routine. 128 lines. 0 direct callers (reached via a dispatch
# / vcall). The struct class and the exact field semantics aren't resolvable, so the name states the
# mechanism: copy the struct's data. The "6" distinguishes this from CopyStructData, CopyStructData2, 3, 4,
# and 5 (all the same shape).
[entrypoint.functions."0x83215018"]
name = "CopyStructData6_83215018"

# A 20-byte node constructor: call Construct20Byte_83215640 (the base 20-byte constructor), then set a
# vtable / type pointer and a float field.

# Args: obj (r3, ->0 = a vtable / type pointer, ->16 = a float). Calls Construct20Byte_83215640 (sub_83215640,
# the base 20-byte constructor) to allocate and zero the 20-byte node. It then sets the node's vtable / type
# pointer (obj->0 = a fixed global, 0x82CCC8C0 + 7072 -- the same type pointer Dispose4_82A146C0 uses) and a
# float field (obj->16 = a fixed global float). So it's a "construct a 20-byte node and set its type and a
# default float" routine (a typed 20-byte node constructor, part of the 0x83215 cluster). 48 lines, 1 callee
# (Construct20Byte_83215640). 3 callers: ProcessTreeNode_83218C10 (sub_83218C10), sub_83219588, sub_83218630.
# The node's class (a 20-byte typed record, ->0 = a type, ->16 = a float) isn't resolvable, so the name
# states the mechanism: construct a 20-byte node. Sibling of ConstructNode20_82A12B08 (sub_82A12B08, the
# plain 20-byte node); this one adds a type and a float.
[entrypoint.functions."0x83215D98"]
name = "Construct20ByteNode_83215D98"

# A node insert (with a chain lookup): if the node is empty, insert it (sub_82551330); otherwise look up
# the chain root (FindChainRoot2_823D4F20) and process the node.

# Args: obj (r3, the container), node (r4, ->4 = data, ->8 = a "filled" field), (r5, r6 = args). If node->8
# is 0 (the node is empty / not yet filled), it inserts the node (sub_82551330, an insert, passing obj,
# node->4, 1, and the args) and returns obj. Otherwise it processes the node: it looks up the chain root
# (FindChainRoot2_823D4F20, sub_823D4F20) and calls sub_82498700 and sub_825511F0 (siblings in the 0x8249/0x8255
# cluster). So it's a "insert the node if empty, otherwise find the chain root and process" routine (a
# node insertion with a chain lookup, part of the 0x8255 chain/lookup cluster). 306 lines, 4 callees (sub
# _82551330, FindChainRoot2_823D4F20, sub_82498700, sub_825511F0). 1 caller: ChainLookup_82550638 (sub_82550638).
# The node's class (->4 = data, ->8 = a filled flag) and the container's meaning aren't resolvable, so the
# name states the mechanism: insert the node (with a chain lookup).
[entrypoint.functions."0x82550F90"]
name = "InsertNodeChain_82550F90"

# A node insert with an overflow check: check a bound (node->8), raise an error if it's below the bound
# (an overflow), otherwise allocate and splice the node in (List_SpliceNode_827C6448).

# Args: obj (r3, the container), node (r4, ->8 = a size/counter), (r5 = a flag), (r6, r7 = args). Checks a
# bound: if node->8 is below a fixed limit (0x55B4555C, a high water mark), it builds an error message
# (SetString_822F2020, a global error string, plus sub_826C3EF0 and sub_826C3FA8, format ops) and raises it
# (sub_822F1F00, the exception raiser -- the same pattern as RaiseIfOutOfRange_82461DA0). Otherwise it
# inserts: it allocates (Allocate_OrDbgBreak_8221F388, sub_8221F388) and splices the node in
# (List_SpliceNode_827C6448, sub_827C6448, plus sub_827C64C0, a sibling). So it's a "check the overflow
# bound, raise on overflow, and insert the node" routine (a guarded node insert, part of the 0x8255
# chain/insert cluster). 381 lines, 7 callees (List_SpliceNode_827C6448, Allocate_OrDbgBreak_8221F388,
# SetString_822F2020, sub_822F1F00, ...). 4 callers: InsertNodeChain_82550F90 (sub_82550F90), sub_825511F0,
# sub_8267F338, sub_8247D6C8. The node's class (->8 = a size) and the container's meaning aren't resolvable,
# so the name states the mechanism: insert with an overflow check.
[entrypoint.functions."0x82551330"]
name = "InsertWithOverflowCheck_82551330"

# A chain traversal / find: walk a chain (via node->4, node->8, node->12, node->25) and find the node
# whose ->12 field matches / is at the given value.

# Args: obj (r3, ->4 = a start, ->8 = a node), value (r4). Reads the start node (obj->8) and walks the chain:
# for each node, it checks a flag (node->25); while the flag is clear, it compares the node's ->12 field to
# value (bailing when node->12 >= value), and advances to the next node (node->8). On a match / at the
# end, it returns the found node (and compares it to the object's ->4). So it's a "walk the chain to find
# the node at / for the given value" routine (a linear chain search keyed on node->12). 100 lines, no callees
# (pure data manipulation). Many callers (sub_83221A60, sub_82A0D630, sub_82A0C960, ...). The node's class
# (->12 = a key, ->25 = a flag, ->8 = next, ->4 = start) and the chain's meaning (a sorted/ordered list) aren't
# resolvable, so the name states the mechanism: walk the chain to find the node.
[entrypoint.functions."0x83239FA8"]
name = "WalkChainFind_83239FA8"

# A chain-lookup + assign: look up the value (ChainLookup_82550638), release the old value if it changed
# (Release_RefCounted_829FF648), assign the new value (src->0, src->4), and take a reference on it.

# Args: obj (r3, ->4 = a value slot), src (r4, ->0 = a new value, ->4 = a new value's refcount). Looks up
# the value (ChainLookup_82550638, sub_82550638, passing obj+4 and src->0->4). If the looked-up value's ->4
# differs from src->4, it releases the old value (Release_RefCounted_829FF648, sub_829FF648), assigns the new
# value (result->0 = src->0, result->4 = src->4), and takes a reference on the new value (if src->4 is non-
# null, it increments its refcount). So it's a "look up the value, and assign the new one (releasing the old
# and refcounting the new)" routine (a reference-counted value assignment via a chain lookup, part of the
# 0x8323A cluster). 79 lines, 2 callees (ChainLookup_82550638, Release_RefCounted_829FF648). 4 callers
# (sub_83225E00, sub_83227478, sub_83225538, sub_832259A0). The value's class (->0 = the value, ->4 = the
# refcount) isn't resolvable, so the name states the mechanism: look up and assign.
[entrypoint.functions."0x8323A1E8"]
name = "LookupAndAssign_8323A1E8"

# A flag-bit test: AND a flag word (obj->28) with a mask, count leading zeros, and return a (inverted) bit
# derived from the count.

# Args: (r3), obj (r4, ->28 = a u16 flag word), mask (r5). Loads the flag word (obj->28) and ANDs it with the
# mask. It then counts the leading zeros of the result (cntlzw) and extracts bit 5 of that count (the
# clz result shifted right by 5, masked to a single bit), and inverts it (xori with 1) to return the result
# (0 or 1). So it's a "test a flag bit (derive a boolean from the masked flag word's bit count)" routine. 16
# lines, no callees (pure bit manipulation). 0 direct callers (reached via a dispatch / vcall). The flag
# word's meaning (->28 = a u16 of flags) and the mask's meaning (a category / type) aren't resolvable, so
# the name states the mechanism: test a flag bit.
[entrypoint.functions."0x821D2C58"]
name = "TestFlagBit_821D2C58"

# A size-bucketed pool allocator: check the size against thresholds (64, 256, ...), and acquire a node
# from a pool (Pool_AcquireNode_83230688); free via Pool_FreeByAddress_832304E8 on a path.

# Args: (r3), (r4), (r5), size (r6), (more). Loads a global pool base (from a fixed .data address). If size
# is 0, returns. It then checks the size against thresholds: if size <= 64, it acquires a node from the pool
# (loading the pool's freelist and calling Pool_AcquireNode_83230688, sub_83230688); if size <= 256, it takes
# another path (a different pool / bucket); otherwise it takes the large-allocation path (calling sub_83231518
# and sub_83231590). It also frees a node (Pool_FreeByAddress_832304E8, sub_832304E8) and copies data
# (MemCpy_SdkRuntime_82CA2C60, sub_82CA2C60) in some paths. So it's a "size-bucketed pool allocation" routine
# (allocate a block of the given size from a pool, choosing the bucket by size). 171 lines, 5 callees (Pool
# _AcquireNode_83230688, Pool_FreeByAddress_832304E8, MemCpy_SdkRuntime_82CA2C60, sub_83231518, sub_83231590).
# 1 caller: GuardDispatch_8227CB70 (sub_8227CB70). The pool's class (a size-bucketed freelist) and the size
# thresholds' meaning aren't resolvable, so the name states the mechanism: allocate from the pool (size-
# bucketed).
[entrypoint.functions."0x8240DAA8"]
name = "PoolAllocate_8240DAA8"

# A range / region update: validate a value against the region's bounds (obj->16 to obj->4 + obj->20), and
# update the region (if in range) or reset it (if out of range / empty).

# Args: obj (r3, ->4 = a base, ->12 = a count, ->16 = a min, ->20 = a size), value (r4). If obj->12 is 0
# (the region is empty), or value < obj->16, or value > obj->4 + obj->20 (out of bounds), it resets the
# region (obj->4 = value, obj->12 = 0, obj->16 = 0, obj->20 = 0). Otherwise it updates the region:
# obj->4 = value (the new base), obj->20 = (obj->4 + obj->20) - value (the adjusted size), and
# obj->12 = (obj->12 - obj->4) + value (the adjusted count). So it's a "update the region to the new base
# (if the value is in range), else reset it" routine (a region/range update with bounds). 53 lines, no callees
# (pure data manipulation). 4 callers (sub_82B28398, sub_822C6DE8, sub_82A1CDD8, sub_82B7E050). The region's
# meaning (a buffer/region with a base, size, min, and count) isn't resolvable, so the name states the
# mechanism: update the region.
[entrypoint.functions."0x82175DE0"]
name = "UpdateRegion_82175DE0"

# A dispose / destroy (with an optional free) -- the sibling of Dispose4_82A146C0 (a different type
# pointer): set a vtable / type pointer, run the 20-byte destructor (Destruct20Byte_832156E0), and free the
# object (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 7096 -- a different type than Dispose4_82A146C0's 7072).
# It then runs the 20-byte destructor (Destruct20Byte_832156E0, sub_832156E0). If the flag's bit 0 is set
# (the object is owned), it frees the object (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68); otherwise it
# leaves the memory (a destruct-but-don't-free path). So it's a "dispose the object (set the type, run the
# destructor, free if owned)" routine. 56 lines, 2 callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk
# _8221BE68). 0 direct callers (reached via a dispatch / vcall, likely a type's destructor / dispose method).
# A structural sibling of Dispose4_82A146C0 (sub_82A146C0) -- same shape, different type pointer (7096 vs
# 7072). The object's class (a 20-byte object, ->0 = a vtable) and the free flag's meaning aren't resolvable,
# so the name states the mechanism: dispose the object (with an optional free). The "5" distinguishes it from
# Dispose_82B39A30, Dispose2_8233DD70, Dispose3_8233E380, and Dispose4_82A146C0.
[entrypoint.functions."0x82A14720"]
name = "Dispose5_82A14720"

# A struct copy (a seventh sibling of the CopyStructData family, and one of the largest -- 804 lines): if
# src->20 >= 4, copy a variable-length byte array from src->12 (byte-by-byte to the stack) and advance
# src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r5). If src->20 < 4,
# takes a short path. Otherwise it copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop to the stack),
# advances src->12 by 4, and continues processing (calling sub_82A1B480, the sibling in the 0x82A1B cluster
# that the other CopyStructData* variants also call, plus sub_83211D48, a sibling in the 0x83211 cluster). So
# it's a "copy the struct's data" routine. 804 lines -- one of the largest functions in the recompile (the
# body has a long tail of additional processing beyond the initial copy). 0 direct callers (reached via a
# dispatch / vcall). The struct class and the exact field semantics aren't resolvable, so the name states the
# mechanism: copy the struct's data. The "7" distinguishes this from CopyStructData, CopyStructData2, 3, 4,
# 5, and 6 (all the same shape).
[entrypoint.functions."0x83214990"]
name = "CopyStructData7_83214990"

# A node constructor (the sibling of Construct20ByteNode_83215D98): call Construct20Byte_83215640 (the base
# 20-byte constructor), then set a vtable / type pointer, some counter fields (to 1), and float fields from
# globals.

# Args: obj (r3, ->0 = a vtable / type pointer, ->16, ->20, ->24, ->28, ->32, ->36, ->37, ->40). Calls
# Construct20Byte_83215640 (sub_83215640, the base 20-byte constructor) to allocate and zero the node. It then
# sets the node's vtable / type pointer (obj->0 = a fixed global, 0x82CCC8C0 + 7096 -- the same type pointer
# Dispose5_82A14720 uses), counter fields (obj->20 = 1, obj->24 = 1, obj->16 = 0, obj->28 = 0, obj->36 = 0,
# obj->37 = 0), and float fields (obj->32 and obj->40 = a fixed global float). So it's a "construct a 20-byte
# node and set its type, counters, and default floats" routine (a typed node constructor, part of the 0x83215
# cluster). 67 lines, 1 callee (Construct20Byte_83215640). 3 callers: ProcessTreeNode_83218C10 (sub_83218C10),
# sub_83218630, sub_83219588. A sibling of Construct20ByteNode_83215D98 (sub_83215D98) -- same base
# (Construct20Byte), different type / field layout. The node's class (a 20-byte typed record) isn't
# resolvable, so the name states the mechanism: construct a 20-byte node. The "2" distinguishes it from
# Construct20ByteNode_83215D98.
[entrypoint.functions."0x83215D18"]
name = "Construct20ByteNode2_83215D18"

# A hash computation: compute a hash of the data (a loop mixing the data's bytes with a seed), then use the
# hash (via obj->16).

# Args: obj (r3, ->16 = a hash-table / bucket), len (r4), seed (r5), data (r6), (more). Computes a hash of the
# data: it walks the data bytes (a loop, mixing each byte with the seed via a sequence of shifts, adds, and
# XORs -- r6 = r6 ^ (byte + (seed >> 5) + (seed >> 30))), producing a 32-bit hash. It then uses the hash: it
# loads obj->16 (a hash-table / bucket base) and its ->0 and ->8 fields, and (later in the body) calls sub
# _82BCD7B0 (a sibling in the 0x82BC cluster) to look up / insert the hashed entry. So it's a "compute a hash
# of the data and use it (a hash-table lookup/insert)" routine. 164 lines, 1 callee (sub_82BCD7B0). Many
# callers (sub_82BDDB88, sub_8219CDA8, sub_82BC7328, ...). The data's type (a string / buffer) and the hash
# table's meaning aren't resolvable, so the name states the mechanism: compute a hash.
[entrypoint.functions."0x8227BA30"]
name = "ComputeHash_8227BA30"

# A hash-table lookup: compute a bucket (a bit index + a mask), walk the bucket's chain, and return the
# entry whose key matches (or a default).

# Args: table (r3, ->7 = a bit index, ->16 = the bucket base), key (r4, ->8 = a bitmask / key), (more). Computes
# a bucket index: it uses the table's bit index (table->7) to form a mask (1 << index), ANDs it with the key's
# bitmask (key->8), and indexes into the table (bucket = table->16 + (mask + mask * 4)). It then walks the
# bucket's chain (a linked list via ->16): for each entry, if entry->12 == 4 (a type / size marker) and entry->8
# == key (a key match), it returns the entry. Otherwise it advances to the next entry (entry->16). If it
# reaches the end (null), it returns a default (a fixed global). So it's a "look up a key in the hash table
# (a bucketed chain search)" routine. 52 lines, no callees (pure data manipulation). Many callers (sub_82BCC038,
# sub_82BCCFF8, sub_82BCA948, ...). The table's key/value class (->8 = a key, ->16 = next, ->12 = a marker) isn't
# resolvable, so the name states the mechanism: hash-table find. The "3" distinguishes it from HashTable_Find
# _829FC630 and HashTable_Find2_829FC630.
[entrypoint.functions."0x821CF1D0"]
name = "HashTable_Find3_821CF1D0"

# A value-type dispatch: dispatch on the value's type field (obj->4) and handle the different value types
# (integers, floats, hash-table lookups, switch dispatches).

# Args: (r3), obj (r4, ->0 = a value, ->4 = a type), (more). Dispatches on the value's type (obj->4):
# - 0: a default / null path
# - 3: a float -- it converts obj->0 (a float) to an integer (fctiwz, the float-to-int conversion) and uses
#   it
# - 4: a keyed lookup -- it calls HashTable_Find3_821CF1D0 (sub_821CF1D0) with the value
# - otherwise: another path (calling SwitchDispatch_82BC8260, sub_82BC8260, SwitchDispatch_82BC9B28, sub
#   _82BC9B28, and sub_821E1498 in the various branches)

# So it's a "dispatch on a value's type and process it" routine (a value handler for a heterogeneous value
# type -- integers, floats, and keyed lookups). 139 lines, 4 callees (HashTable_Find3_821CF1D0, SwitchDispatch
# _82BC8260, SwitchDispatch_82BC9B28, sub_821E1498). Many callers (sub_82BD0A78, sub_82BC6F70, sub_82BB9C50,
# ...). The value's class (->0 = the value, ->4 = the type, a tagged union) isn't resolvable, so the name
# states the mechanism: dispatch on the value's type.
[entrypoint.functions."0x821CF230"]
name = "DispatchValue_821CF230"

# A dispose / destroy (with an optional free) -- the sibling of Dispose4_82A146C0 and Dispose5_82A14720 (a
# different type pointer, 7120): set a vtable / type pointer, run the 20-byte destructor (Destruct20Byte
# _832156E0), and free the object (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 7120 -- a different type than Dispose4_82A146C0's 7072
# and Dispose5_82A14720's 7096). It then runs the 20-byte destructor (Destruct20Byte_832156E0, sub_832156E0).
# If the flag's bit 0 is set (the object is owned), it frees the object (Free_SizeBucketed_Thunk_8221BE68, sub
# _8221BE68); otherwise it leaves the memory (a destruct-but-don't-free path). So it's a "dispose the object
# (set the type, run the destructor, free if owned)" routine. 56 lines, 2 callees (Destruct20Byte_832156E0, Free
# _SizeBucketed_Thunk_8221BE68). 0 direct callers (reached via a dispatch / vcall, likely a type's destructor /
# dispose method). A structural sibling of Dispose4_82A146C0 (sub_82A146C0) and Dispose5_82A14720 (sub_82A14720)
# -- same shape, different type pointer (7120 vs 7072 / 7096). The object's class (a 20-byte object, ->0 = a
# vtable) and the free flag's meaning aren't resolvable, so the name states the mechanism: dispose the object
# (with an optional free). The "6" distinguishes it from Dispose_82B39A30, Dispose2_8233DD70, Dispose3_8233E380,
# Dispose4_82A146C0, and Dispose5_82A14720.
[entrypoint.functions."0x82A14780"]
name = "Dispose6_82A14780"

# A copy-and-process: copy the struct data (CopyStructData2_82A1BD30) to the stack, read the copied values
# into the object (obj->16, obj->20), and process them (sub_83211D48) if a flag is set.

# Args: obj (r3, ->16, ->20), (r4), flag (r5, low byte = a "process" flag). Calls CopyStructData2_82A1BD30
# (sub_82A1BD30) to copy the struct data to the stack. It then reads the copied values (stack[80]->0 -> obj
# ->16, stack[80]->4 -> obj->20). If the flag's low byte is non-zero (a process flag), it processes the object
# (calling sub_83211D48, a sibling in the 0x83211 cluster). So it's a "copy the struct data, store the key
# values, and process the object (if flagged)" routine (a copy-and-process for the 0x83214 cluster). 58 lines,
# 2 callees (CopyStructData2_82A1BD30, sub_83211D48). 0 direct callers (reached via a dispatch / vcall). The
# object's class (->16, ->20 = copied values) and the flag's meaning (a process flag) aren't resolvable, so
# the name states the mechanism: copy and process the struct data.
[entrypoint.functions."0x83214828"]
name = "CopyAndProcess_83214828"

# A node constructor (the sibling of Construct20ByteNode_83215D98 / Construct20ByteNode2_83215D18): call
# Construct20Byte_83215640 (the base 20-byte constructor), then set a vtable / type pointer (7120 -- the
# Dispose6_82A14780 type) and two float fields from globals.

# Args: obj (r3, ->0 = a vtable / type pointer, ->16 = a float, ->20 = a float). Calls Construct20Byte_83215640
# (sub_83215640, the base 20-byte constructor) to allocate and zero the node. It then sets the node's vtable /
# type pointer (obj->0 = a fixed global, 0x82CCC8C0 + 7120 -- the same type pointer Dispose6_82A14780 uses)
# and two float fields (obj->16 and obj->20 = a fixed global float). So it's a "construct a 20-byte node and
# set its type and default floats" routine (a typed node constructor, part of the 0x83215 cluster). 51 lines,
# 1 callee (Construct20Byte_83215640). 3 callers: sub_83219588, ProcessTreeNode_83218C10 (sub_83218C10), sub
# _83218630. A sibling of Construct20ByteNode_83215D98 (sub_83215D98) and Construct20ByteNode2_83215D18 (sub
# _83215D18) -- same base (Construct20Byte), different type / field layout (type 7120, floats at ->16 and ->20).
# The node's class (a 20-byte typed record) isn't resolvable, so the name states the mechanism: construct a 20-
# byte node. The "3" distinguishes it from Construct20ByteNode_83215D98 and Construct20ByteNode2_83215D18.
[entrypoint.functions."0x83215CB8"]
name = "Construct20ByteNode3_83215CB8"

# An index validation: set a result field (obj->0) to 0, then validate an index against a container (a bounds
# check), raising an assertion (trap 22) if the index is out of range.

# Args: obj (r3, ->0 = a result), index (r4), container (r5, ->4 = a base, ->20 = a count, ->24 = a capacity). Sets
# obj->0 to 0 (the result). If the container is null or the index is 0, it skips the validation (an early return).
# Otherwise it validates the index: it reads the container's base (container->4, or container->4->0 if the
# container is large), its count (container->20), and capacity (container->24), computes a bound, and checks the
# index against it. If the index is out of range (index > bound), it raises an assertion (twi 31, r0, 22 -- a
# trap 22, the game's debug-assert instruction). So it's a "validate an index (assert it is in range)" routine.
# 63 lines, no callees (pure data manipulation). 3 callers (sub_822F2238, sub_8217AA20, sub_8217A7F8). The
# container's class (a container with a base, count, and capacity) and the index's meaning aren't resolvable,
# so the name states the mechanism: validate an index (assert bounds).
[entrypoint.functions."0x822F6EE8"]
name = "ValidateIndex_822F6EE8"

# A trivial offset getter: return (r3 + 732), i.e. a sub-object / field embedded at offset 732 of the
# containing object.

# Args: obj (r3, a containing object). Returns obj + 732 (0x2DC) -- the address of a sub-object / field
# embedded at offset 732 of the containing object. 2 lines, no callees. 0 direct callers (a leaf thunk / vcall
# target). The containing object's class (a large object with an embedded sub-object at offset 732) and the
# sub-object's meaning aren't resolvable, so the name states the mechanism: get the offset-732 field.
[entrypoint.functions."0x82BAA148"]
name = "GetOffset732_82BAA148"

# A flag-bit test: return bit 12 of a flag word (obj->28) -- a simple one-bit test.

# Args: (r3), obj (r4, ->28 = a u16 flag word). Loads the flag word (obj->28) and returns its bit 12 (the flag
# word rotated/shifted so bit 12 becomes the low bit, then masked to a single bit, 0 or 1). So it's a "test a
# single flag bit" routine (a predicate: is bit 12 of the object's flag word set?). 10 lines, no callees (pure
# bit manipulation). 0 direct callers (reached via a dispatch / vcall, likely a type's flag predicate method).
# The flag word's meaning (->28 = a u16 of flags) and bit 12's specific meaning (a particular flag) aren't
# resolvable, so the name states the mechanism: test a flag bit. The "2" distinguishes it from TestFlagBit_821D
# 2C58 (a more involved bit test); this one is a plain single-bit extraction.
[entrypoint.functions."0x82284208"]
name = "TestFlagBit2_82284208"

# A 2D-strided process (the sibling of Process2DStrided_82BDD000): call Process2DStrided_82BDD000 to compute
# a 2D-strided value, then process it (a hash + element processing), or return 0 if empty.

# Args: obj (r3, ->0, ->8), (more). Calls Process2DStrided_82BDD000 (sub_82BDD000) to compute a 2D-strided value
# (into the stack). If the result is 0 (empty), it returns 0. Otherwise it processes the result: it calls sub
# _82BCD930 (a sibling in the 0x82BCD cluster, with obj->0, obj->8, and the result), then ProcessElements_82BDCF
# 20 (sub_82BDCF20, with obj and the result), and also ComputeHash_8227BA30 (sub_8227BA30) in a path. So it's a
# "process a 2D-strided region (compute the value, then process it)" routine (a higher-level 2D-strided process
# that builds on Process2DStrided, part of the 0x82BDD cluster). 73 lines, 4 callees (Process2DStrided_82BDD000,
# ProcessElements_82BDCF20, ComputeHash_8227BA30, sub_82BCD930). 2 callers (sub_82BDD258, sub_82BDD8A8). The
# region's class (a 2D-strided data region) and the processed result's meaning aren't resolvable, so the name
# states the mechanism: process a 2D-strided region. The "2" distinguishes it from Process2DStrided_82BDD000.
[entrypoint.functions."0x82BDD1E0"]
name = "Process2DStrided2_82BDD1E0"

# A state dispatch: check a state object's flag bytes (ctx->8) and dispatch on its state (StateDispatch_82BCA7D8
# and other handlers).

# Args: obj (r3, ->172 = a field), ctx (r4, ->8 = a state object). Checks the state object (ctx->8): if it is
# non-null and its byte at offset 5 has the low 2 bits set (a state / type flag), it calls StateDispatch_82BCA7
# D8 (sub_82BCA7D8) to dispatch on that state. It then checks the state object's byte at offset 6 (a flag, bit
# 3) and, if that flag is clear, continues processing (loading obj->172 and a constant, and calling sub_82BCD5E0
# and sub_82CA3980 in the various branches). So it's a "check a state object's flags and dispatch on its
# state" routine (a state-machine handler, part of the 0x82BCA cluster). 319 lines, 3 callees (StateDispatch
# _82BCA7D8, sub_82BCD5E0, sub_82CA3980). 1 caller: StateDispatch_82BCB0A8 (sub_82BCB0A8). The state object's
# class (->5, ->6 = flag bytes) and the state's meaning (a game / animation / physics state) aren't resolvable,
# so the name states the mechanism: dispatch on the state. The "2" distinguishes it from StateDispatch_82BCB0A8
# and StateDispatch_82BCA7D8.
[entrypoint.functions."0x82BCAA58"]
name = "StateDispatch2_82BCAA58"

# A trivial offset getter: return (r3 + 684), i.e. a sub-object / field embedded at offset 684 of the
# containing object -- the sibling of GetOffset732_82BAA148.

# Args: obj (r3, a containing object). Returns obj + 684 (0x2AC) -- the address of a sub-object / field
# embedded at offset 684 of the containing object. 2 lines, no callees. 0 direct callers (a leaf thunk / vcall
# target). The containing object's class (a large object with embedded sub-objects at offsets 684 and 732) and
# the sub-object's meaning aren't resolvable, so the name states the mechanism: get the offset-684 field. A
# sibling of GetOffset732_82BAA148 (sub_82BAA148) -- same shape (addi + blr), different offset (684 vs 732), and
# the two sit adjacent in the binary (0x82BAA148 and 0x82BAA150), so they likely return two different sub-
# objects of the same large containing object.
[entrypoint.functions."0x82BAA150"]
name = "GetOffset684_82BAA150"

# A very large (1793-line) buffer-building / serialization routine: build a buffer (Buffer_Append, Buffer
# _PushByte) and call many SDK / runtime functions to process the data.

# Args: obj (r3, ->12 = a flag), (r4), (r6), (more). This is one of the largest routines in the recompile
# (1793 lines, a 1328-byte stack frame). It initializes a large local struct (zeroing several fields on the
# stack), checks obj for null, and (if non-null) checks a flag bit (obj->12, bit 6). It then makes a series of
# calls into sub_82CAF6C8 (a getter / accessor, called repeatedly) and sub_82CAB630 / sub_82CAB770 (SDK / runtime
# functions), building a buffer (Buffer_Append_82CAE410, Buffer_PushByte_82CAE378) and calling many other sub
# _82CA* SDK / runtime functions (sub_82CA2B88, sub_82CA3C68, sub_82CA5DC0, sub_82CAACC0, sub_82CB6A88, ...). So
# it's a "build a buffer / serialize the data" routine (a large serialization / encoding routine that assembles
# a buffer from the object's data, calling the SDK / runtime to read the fields). 1793 lines, 13 callees (Buffer
# _Append_82CAE410, Buffer_PushByte_82CAE378, GetGlobalPtr3_82240578, and 10 sub_82C* SDK / runtime functions).
# 7 callers (sub_82286918, Func_82CA71C8, sub_82CA2A78, ...). The object's class (a large object with a flag at
# ->12) and the buffer's format (a serialized / network / save format) aren't resolvable, so the name states the
# mechanism: build a buffer (serialize the data).
[entrypoint.functions."0x8223F990"]
name = "BuildBuffer_8223F990"

# A vcall-sequence dispatch (a sibling of the DispatchVcallSequence family): make a series of vcall calls
# (via the object's vtable, at slots ->32, ->44, ->48, and others).

# Args: obj (r3, ->4 = a sub-object), ctx (r4). Makes a series of vcall calls: it calls the vtable method at
# obj->4->0->32 (passing obj->4), then the method at obj->4->0->44, then the method at obj->4->0->48, and more
# (6 vcall dispatches total, all via the object's vtable). So it's a "dispatch a sequence of virtual calls"
# routine (a vcall-sequence dispatcher, a sibling of DispatchVcallSequence_82B25628 and the DispatchVcall
# Sequence2-6 family). 215 lines, 6 vcall dispatches, no direct callees (all vtable calls). 1 caller (sub_82BA
# FCE0). The object's class (a sub-object at ->4, with a vtable at ->4->0) and the vcall slots' meanings (->32,
# ->44, ->48 = particular virtual methods) aren't resolvable, so the name states the mechanism: dispatch a
# sequence of virtual calls. The "7" distinguishes it from DispatchVcallSequence_82B25628, DispatchVcall
# Sequence2_82284538, DispatchVcallSequence3_8243D0B8, DispatchVcallSequence4_82C25F18, DispatchVcallSequence5
# _82B8D250, and DispatchVcallSequence6_82C26AF0.
[entrypoint.functions."0x82BB0148"]
name = "DispatchVcallSequence7_82BB0148"

# A serialization wrapper: call the SDK / runtime functions (sub_82CAB770, sub_82CAB630, sub_82CAB7E0) and the
# buffer-builder (BuildBuffer_8223F990), and set an error / result code (obj->0 = 22) on a failure path.

# Args: obj (r3, ->0 = a result / error code), (r4), (r5), (more, r6-r10 saved to the stack). This is a common
# entry point (many callers). It saves several arguments (r6-r10) to the stack, and (if r5 is null) it calls sub
# _82CAB770 (an SDK / runtime function), sets obj->0 to 22 (an error / failure code, 0x16), and calls sub_82CAB630
# (another SDK / runtime function) to handle the error. In the main path (r5 non-null) it processes the data,
# calling sub_82CAB7E0 (an SDK / runtime function) and BuildBuffer_8223F990 (sub_8223F990, the buffer-builder /
# serialization routine). So it's a "serialize / encode the data (via the SDK / runtime and the buffer-builder)
# and report a result code" routine (a serialization entry point that wraps the SDK / runtime). 156 lines, 4
# callees (BuildBuffer_8223F990, sub_82CAB630, sub_82CAB770, sub_82CAB7E0). Many callers (sub_82C86D20, sub
# _82BB39F8, sub_826CAFA8, ...). The data's type and the serialization format aren't resolvable, so the name
# states the mechanism: serialize the data (via the SDK / runtime).
[entrypoint.functions."0x82CA3EB8"]
name = "Serialize_82CA3EB8"

# A dispose / destroy (with an optional free) -- the sibling of Dispose4_82A146C0, Dispose5_82A14720, and
# Dispose6_82A14780 (a different type pointer, 7264): set a vtable / type pointer, run the 20-byte destructor
# (Destruct20Byte_832156E0), and free the object (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 7264 -- a different type than Dispose4_82A146C0's 7072,
# Dispose5_82A14720's 7096, and Dispose6_82A14780's 7120). It then runs the 20-byte destructor (Destruct20Byte
# _832156E0, sub_832156E0). If the flag's bit 0 is set (the object is owned), it frees the object (Free_Size
# Bucketed_Thunk_8221BE68, sub_8221BE68); otherwise it leaves the memory (a destruct-but-don't-free path). So
# it's a "dispose the object (set the type, run the destructor, free if owned)" routine. 56 lines, 2 callees
# (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). 0 direct callers (reached via a dispatch / vcall,
# likely a type's destructor / dispose method). A structural sibling of Dispose4_82A146C0 (sub_82A146C0), Dispose
# 5_82A14720 (sub_82A14720), and Dispose6_82A14780 (sub_82A14780) -- same shape, different type pointer (7264 vs
# 7072 / 7096 / 7120). The object's class (a 20-byte object, ->0 = a vtable) and the free flag's meaning aren't
# resolvable, so the name states the mechanism: dispose the object (with an optional free). The "7" distinguishes
# it from Dispose_82B39A30, Dispose2_8233DD70, Dispose3_8233E380, Dispose4_82A146C0, Dispose5_82A14720, and
# Dispose6_82A14780.
[entrypoint.functions."0x82A149C0"]
name = "Dispose7_82A149C0"

# A count process: zero the object's fields (obj->4, ->8, ->12), compute a count from a size difference
# ((src->8 - src->4) / 12), and process it (sub_8282EFD8, with a refcounted copy-assign).

# Args: obj (r3, ->4, ->8, ->12 = result fields), src (r4, ->4 = a start, ->8 = an end), (more). Zeros the
# object's result fields (obj->4, obj->8, obj->12). It then computes a count: (src->8 - src->4) / 12 (the size
# difference of the source, divided by 12 -- an element size of 12 bytes). If the count is non-zero, it calls sub
# _8282EFD8 (a sibling in the 0x8282 cluster, with the object) to process it, and (in a path) CopyAssign_Ref
# Counted_821F0108 (sub_821F0108, a refcounted copy-assign). So it's a "compute a count from a size difference
# (a 12-byte element count) and process it" routine. 123 lines, 2 callees (sub_8282EFD8, CopyAssign_RefCounted
# _821F0108). 2 callers (sub_82B722E8, sub_82B82C68). The object's class (->4, ->8, ->12 = result fields) and
# the source's meaning (a 12-byte-element range) aren't resolvable, so the name states the mechanism: process a
# count.
[entrypoint.functions."0x82B728D8"]
name = "ProcessCount_82B728D8"

# A dispose / destroy (with an optional free) -- the sibling of Dispose4-7 (a different type pointer, 6724):
# set a vtable / type pointer, run the 20-byte destructor (Destruct20Byte_832156E0), and free the object
# (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 6724 -- a different type than Dispose4_82A146C0's 7072,
# Dispose5_82A14720's 7096, Dispose6_82A14780's 7120, and Dispose7_82A149C0's 7264). It then runs the 20-byte
# destructor (Destruct20Byte_832156E0, sub_832156E0). If the flag's bit 0 is set (the object is owned), it frees
# the object (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68); otherwise it leaves the memory (a destruct-but-
# don't-free path). So it's a "dispose the object (set the type, run the destructor, free if owned)" routine. 56
# lines, 2 callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). 0 direct callers (reached via a
# dispatch / vcall, likely a type's destructor / dispose method). A structural sibling of Dispose4_82A146C0, Dispose
# 5_82A14720, Dispose6_82A14780, and Dispose7_82A149C0 -- same shape, different type pointer (6724). The object's
# class (a 20-byte object, ->0 = a vtable) and the free flag's meaning aren't resolvable, so the name states the
# mechanism: dispose the object (with an optional free). The "8" distinguishes it from Dispose_82B39A30, Dispose2
# _8233DD70, Dispose3_8233E380, Dispose4_82A146C0, Dispose5_82A14720, Dispose6_82A14780, and Dispose7_82A149C0.
[entrypoint.functions."0x82A12000"]
name = "Dispose8_82A12000"

# A struct copy (an eighth sibling of the CopyStructData family): if src->20 >= 4, copy a variable-length byte
# array from src->12 (byte-by-byte to the stack) and advance src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r5). If src->20 >= 4, it
# copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop to the stack), advances src->12 by 4, and
# continues processing (calling sub_82A1B480, the sibling in the 0x82A1B cluster that the other CopyStructData*
# variants also call, plus sub_83211D48, a sibling in the 0x83211 cluster). So it's a "copy the struct's data"
# routine. 128 lines, 2 callees (sub_82A1B480, sub_83211D48). 0 direct callers (reached via a dispatch / vcall).
# The struct class and the exact field semantics aren't resolvable, so the name states the mechanism: copy the
# struct's data. The "8" distinguishes this from CopyStructData, CopyStructData2, 3, 4, 5, 6, and 7 (all the same
# shape).
[entrypoint.functions."0x83212620"]
name = "CopyStructData8_83212620"

# A node constructor (the sibling of Construct20ByteNode_83215D98 / Construct20ByteNode2_83215D18 / Construct
# 20ByteNode3_83215CB8): call Construct20Byte_83215640 (the base 20-byte constructor), then set a vtable / type
# pointer (6724 -- the Dispose8_82A12000 type) and a float field from a global.

# Args: obj (r3, ->0 = a vtable / type pointer, ->16 = a float). Calls Construct20Byte_83215640 (sub_83215640,
# the base 20-byte constructor) to allocate and zero the node. It then sets the node's vtable / type pointer
# (obj->0 = a fixed global, 0x82CCC8C0 + 6724 -- the same type pointer Dispose8_82A12000 uses) and a float field
# (obj->16 = a fixed global float). So it's a "construct a 20-byte node and set its type and default float"
# routine (a typed node constructor, part of the 0x83215 cluster). 48 lines, 1 callee (Construct20Byte_83215640).
# 2 callers: sub_83219588, ProcessTreeNode_83218C10 (sub_83218C10). A sibling of Construct20ByteNode_83215D98,
# Construct20ByteNode2_83215D18, and Construct20ByteNode3_83215CB8 -- same base (Construct20Byte), different type
# / field layout (type 6724, a float at ->16). The node's class (a 20-byte typed record) isn't resolvable, so
# the name states the mechanism: construct a 20-byte node. The "4" distinguishes it from Construct20ByteNode_83215
# D98, Construct20ByteNode2_83215D18, and Construct20ByteNode3_83215CB8.
[entrypoint.functions."0x832158B0"]
name = "Construct20ByteNode4_832158B0"

# A 12-byte object constructor: allocate a 12-byte object (Allocate_OrDbgBreak_8221F388), set up its fields
# (from the source and a global), and set a string (SetString_822F2020).

# Args: obj (r3), (r4), src (r5, ->0 = a value), (more). Allocates a 12-byte object (Allocate_OrDbgBreak_8221F388,
# sub_8221F388). If the allocation succeeds, it sets up the object's fields: ->0 (a value from a global), ->4 (a
# value from the global's ->4), and ->8 (src->0, a value from the source). It continues setting up the object
# (copying more fields) and, in a path, sets a string (SetString_822F2020, sub_822F2020) and calls sub_822F1F00
# (an error / setup function), sub_82171810, sub_826C3EF0, and sub_826C3FA8. So it's a "construct a 12-byte
# object (allocate it, set its fields, and set a string)" routine. 127 lines, 6 callees (Allocate_OrDbgBreak
# _8221F388, SetString_822F2020, sub_822F1F00, sub_82171810, sub_826C3EF0, sub_826C3FA8). Many callers (sub
# _82890AF8, sub_82361728, sub_8256C580, ...). The object's class (a 12-byte object with a string) and the
# source's meaning aren't resolvable, so the name states the mechanism: construct a 12-byte object.
[entrypoint.functions."0x825F7B10"]
name = "Construct12Byte_825F7B10"

# A vcall + check: make a vcall (obj->0->4, passing the object and a value), check the result against flags,
# and (in a path) process a float (FloatToIntProcess_82284810).

# Args: obj (r3, ->0 = a sub-object with a vtable at ->0->4), src (r4, ->20 = a value), flag1 (r5), flag2 (r6).
# Loads src->20 (a value). If it is 0, takes an early path. Otherwise it makes a vcall (obj->0->4, passing the
# object and the value). It then checks the result (the low byte of the return) against the flags: if the result
# is 0 and flag2 is 0, it takes one path; otherwise it continues (calling FloatToIntProcess_82284810, sub
# _82284810, in a path). So it's a "dispatch a vcall and check the result (against flags)" routine (a gated vcall
# dispatcher, part of the 0x82BAC cluster). 77 lines, 1 callee (FloatToIntProcess_82284810), 1 vcall dispatch.
# 0 direct callers (reached via a dispatch / vcall). The object's class (a sub-object at ->0, with a vtable at
# ->0->4) and the vcall slot's meaning (->4 = a particular virtual method) aren't resolvable, so the name states
# the mechanism: dispatch a vcall and check the result.
[entrypoint.functions."0x82BACCE0"]
name = "DispatchAndCheck_82BACCE0"

# A vcall + process: load a global value, make a vcall (via the value's vtable, at slot ->32), and process the
# result.

# Args: obj (r3), flag (r4), value (r5), (more). Loads a global value (from a fixed .data address, the ->56
# field of a global struct). If the value argument is null, it calls sub_823FE8F8 (a sibling in the 0x823FE
# cluster) to get a value. It then makes a vcall (via the value's vtable, at slot ->32, i.e. value->0->32,
# passing the value) and processes the result (2 vcall dispatches total, in the various branches). So it's a
# "dispatch a vcall (with a global / looked-up value) and process the result" routine (a vcall processor, part of
# the 0x823F8 cluster). 102 lines, 1 callee (sub_823FE8F8), 2 vcall dispatches. Many callers (sub_823F9398, sub
# _827CE468, sub_823F8F40, ...). The value's class (->0 = a vtable, ->32 = a particular virtual method) and the
# global's meaning aren't resolvable, so the name states the mechanism: make a vcall and process the result.
[entrypoint.functions."0x823F8D30"]
name = "VcallProcess_823F8D30"

# A size-checked allocation: check the size (an overflow check: size != 0 and -1 / size < 12), compute the
# allocation size (size * 12), and allocate (Allocate_OrDbgBreak_8221F388); raise an error (sub_822F1F00) on a
# failure path.

# Args: (r3), size (r4), (more). Checks the size: if size is 0, or -1 / size >= 12 (an overflow check -- the
# allocation size would overflow), it takes an error path. Otherwise it computes the allocation size (size * 3 * 4
# = size * 12) and allocates it (Allocate_OrDbgBreak_8221F388, sub_8221F388). On a failure path it calls sub
# _822F1F00 (an error / setup function, with a string at a fixed .data address). So it's a "check the size (an
# overflow check), compute the allocation size (size * 12), and allocate (with an error on failure)" routine (a
# size-checked allocation of 12-byte elements). 70 lines, 2 callees (Allocate_OrDbgBreak_8221F388, sub_822F1F00).
# Many callers (sub_82A96618, sub_82688FD8, sub_82B420E0, ...). The 12-byte element's meaning (a record / node) and
# the size's source aren't resolvable, so the name states the mechanism: check the size and allocate (size * 12).
[entrypoint.functions."0x8264EFE0"]
name = "AllocateChecked_8264EFE0"

# A size-checked allocation (the sibling of AllocateChecked_8264EFE0, but with a 2x multiplier): check the size
# (an overflow check: size != 0 and -1 / size < 2), compute the allocation size (size * 2), and allocate
# (Allocate_OrDbgBreak_8221F388); raise an error (sub_822F1F00) on a failure path.

# Args: size (r3), (more). Checks the size: if size is 0, or -1 / size >= 2 (an overflow check -- the allocation
# size would overflow), it takes an error path. Otherwise it computes the allocation size (size * 2) and allocates
# it (Allocate_OrDbgBreak_8221F388, sub_8221F388). On a failure path it calls sub_822F1F00 (an error / setup
# function, with a string at a fixed .data address). So it's a "check the size (an overflow check), compute the
# allocation size (size * 2), and allocate (with an error on failure)" routine (a size-checked allocation). 66
# lines, 2 callees (Allocate_OrDbgBreak_8221F388, sub_822F1F00). 4 callers (sub_82C73A58, sub_8217A4F8, sub
# _82C73E00, sub_82B39E78). The element's meaning (a 2x-sized record) and the size's source aren't resolvable, so
# the name states the mechanism: check the size and allocate (size * 2). The "2" distinguishes it from
# AllocateChecked_8264EFE0 (which uses a 12x multiplier).
[entrypoint.functions."0x8217A488"]
name = "AllocateChecked2_8217A488"

# A size-computed allocation (the sibling of AllocateChecked2_8217A488, a grow / resize): round the size up to
# an 8-byte boundary (size | 7), check it against a max and the object's capacity (obj->24), compute the new size,
# and allocate (AllocateChecked2_8217A488); free the old buffer (Free_SizeBucketed_Thunk_8221BE68) on a path.

# Args: obj (r3, ->24 = a capacity), size (r4), (r5), (more). Rounds the size up to an 8-byte boundary (size | 7).
# It then checks the size against a max (0x7FFFFFFE) and the object's capacity (obj->24): if the size exceeds the
# max, or if size / 3 is not less than the capacity (a capacity / overflow check), it uses the original size
# (or the capacity-based size, r11 + (r11 & 0x7FFFFFFF)). It then allocates the new size (AllocateChecked2_8217A4
# 88, sub_8217A488), copies / zeros the data (Buffer_CopyOrZero_Error_82CA3730, sub_82CA3730), and (on a path) frees
# the old buffer (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68). So it's a "compute the new size (round to 8 bytes,
# check the capacity), allocate it, copy the data, and free the old buffer" routine (a grow / resize allocation,
# a sibling of AllocateChecked2_8217A488). 133 lines, 3 callees (AllocateChecked2_8217A488, Buffer_CopyOrZero_Error
# _82CA3730, Free_SizeBucketed_Thunk_8221BE68). 1 caller: ProcessValue_FlagNonZero_8217A5E0 (sub_8217A5E0). The
# object's class (->24 = a capacity) and the buffer's meaning aren't resolvable, so the name states the mechanism:
# compute the size and allocate (a resize / grow).
[entrypoint.functions."0x8217A4F8"]
name = "AllocateResized_8217A4F8"

# A clamp-and-dispatch: clamp a value (to 32, if it is out of range), and dispatch (DispatchAndAdjustPos_8227BB58
# with the clamped value).

# Args: (r3), obj (r4, ->0, ->8 = a max / bound), value (r5), (more). Compares the value against obj->8 (a max /
# bound) and 32: if value <= obj->8, or value >= 32, it clamps the value to 32. It then dispatches: it calls
# DispatchAndAdjustPos_8227BB58 (sub_8227BB58, a vcall + position-adjust, the state-dispatch cluster) with obj->0
# and the (clamped) value. In a path it also calls sub_82BC9860 (a sibling in the 0x82BC9 cluster). So it's a
# "clamp a value to a bound (32) and dispatch on it" routine (a gated dispatch with a clamped argument, part of
# the 0x82BCD cluster). 81 lines, 2 callees (DispatchAndAdjustPos_8227BB58, sub_82BC9860). 3 callers (sub_822CE098,
# Process2DStrided2_82BDD1E0, sub_82BDD518). The object's class (->0, ->8 = a max / bound) and the value's
# meaning (an index / position) aren't resolvable, so the name states the mechanism: clamp the value and dispatch.
[entrypoint.functions."0x82BCD930"]
name = "ClampAndDispatch_82BCD930"

# A struct copy (a ninth sibling of the CopyStructData family): if src->20 >= 4, copy a variable-length byte
# array from src->12 (byte-by-byte to the stack) and advance src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r5). If src->20 >= 4, it
# copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop to the stack), advances src->12 by 4, and
# continues processing (calling sub_82A1B480, the sibling in the 0x82A1B cluster that the other CopyStructData*
# variants also call, plus sub_83211D48, a sibling in the 0x83211 cluster). So it's a "copy the struct's data"
# routine. 269 lines, 2 callees (sub_82A1B480, sub_83211D48). 0 direct callers (reached via a dispatch / vcall).
# The struct class and the exact field semantics aren't resolvable, so the name states the mechanism: copy the
# struct's data. The "9" distinguishes this from CopyStructData, CopyStructData2, 3, 4, 5, 6, 7, and 8 (all the
# same shape).
[entrypoint.functions."0x83213958"]
name = "CopyStructData9_83213958"

# A node constructor (the sibling of Construct20ByteNode_83215D98 / Construct20ByteNode2_83215D18 / Construct
# 20ByteNode3_83215CB8 / Construct20ByteNode4_832158B0): call Construct20Byte_83215640 (the base 20-byte
# constructor), then set a vtable / type pointer (7264 -- the Dispose7_82A149C0 type) and two float fields from
# globals.

# Args: obj (r3, ->0 = a vtable / type pointer, ->16 = a float, ->20 = a float, ->24 = a flag). Calls
# Construct20Byte_83215640 (sub_83215640, the base 20-byte constructor) to allocate and zero the node. It then
# sets the node's vtable / type pointer (obj->0 = a fixed global, 0x82CCC8C0 + 7264 -- the same type pointer
# Dispose7_82A149C0 uses), two float fields (obj->16 and obj->20 = a fixed global float), and a flag (obj->24 = 0).
# So it's a "construct a 20-byte node and set its type, default floats, and flag" routine (a typed node
# constructor, part of the 0x83215 cluster). 55 lines, 1 callee (Construct20Byte_83215640). 3 callers: sub
# _83219588, ProcessTreeNode_83218C10 (sub_83218C10), sub_83218630. A sibling of Construct20ByteNode_83215D98,
# Construct20ByteNode2_83215D18, Construct20ByteNode3_83215CB8, and Construct20ByteNode4_832158B0 -- same base
# (Construct20Byte), different type / field layout (type 7264, floats at ->16 and ->20, a flag at ->24). The
# node's class (a 20-byte typed record) isn't resolvable, so the name states the mechanism: construct a 20-byte
# node. The "5" distinguishes it from Construct20ByteNode_83215D98, 2, 3, and 4.
[entrypoint.functions."0x83215A80"]
name = "Construct20ByteNode5_83215A80"

# A trivial global getter: return a fixed global (0x82CCC8C0 + 19812) -- a sibling of the GetGlobalPtr family.

# Args: (none -- it ignores its arguments). Returns a fixed global (0x82CCC8C0 + 19812), i.e. the address of a
# particular global variable / structure. 3 lines, no callees. 0 direct callers (reached via a dispatch / vcall,
# likely a type's global / singleton getter). The global's meaning (a particular global variable / structure) isn't
# resolvable, so the name states the mechanism: get a global pointer. The "6" distinguishes it from GetGlobalPtr
# _82C27FA0, GetGlobalPtr2_82C23D00, GetGlobalPtr3_82240578, GetGlobalPtr4_82C21AD0, and GetGlobalPtr5 (an earlier
# entry).
[entrypoint.functions."0x82C26870"]
name = "GetGlobalPtr6_82C26870"

# An element-list process: clear a flag (src->32->5, the low 2 bits), then loop over a list of elements
# (src->8, count src->40) and dispatch on each (StateDispatch_82BCA7D8).

# Args: obj (r3), src (r4, ->8 = an element list, ->32 = a state object, ->40 = a count), (more). If src->32 is
# non-null, it clears a flag in the state object (src->32->5 &= ~3, clearing the low 2 bits of a flag byte). It
# then loops over the element list (src->8, count src->40): for each element (src->8 + i), it checks the element's
# ->4 field (a size / type, must be >= 4), loads the element's ->0 field, and (in a path) calls StateDispatch
# _82BCA7D8 (sub_82BCA7D8, a state dispatch) to process it. So it's a "clear a state flag and process a list of
# elements (dispatching on each)" routine (an element-list processor, part of the 0x82BCA cluster). 222 lines, 1
# callee (StateDispatch_82BCA7D8). 1 caller: StateDispatch_82BCB0A8 (sub_82BCB0A8). The element's class (->0, ->4
# = a size / type) and the state object's meaning (->5 = a flag byte) aren't resolvable, so the name states the
# mechanism: process a list of elements. Distinguished from ProcessElements_82BDCF20 (a different element
# processor in the 0x82BCD cluster).
[entrypoint.functions."0x82BCAC98"]
name = "ProcessElementList_82BCAC98"

# An atomic counter increment: atomically increment a counter (obj->8) using a load-linked / store-conditional
# loop (lwarx / stwcx) under the global lock, and return the new value.

# Args: obj (r3, ->8 = a counter). Atomically increments the counter at obj->8: it takes the global lock (REX
# _ENTER_GLOBAL_LOCK, disabling interrupts via the MSR), reads the counter (lwarx), increments it, and writes it
# back (stwcx, a compare-and-swap); if the store fails (another thread modified the counter), it retries (a
# load-linked / store-conditional loop). It releases the global lock and returns the new value. So it's an "atomic
# increment of a shared counter" routine (a thread-safe counter increment, part of the 0x822C cluster). 37 lines,
# no callees (a pure atomic operation, using the global lock). 1 caller (sub_82B4F368). The counter's meaning
# (a shared counter at obj->8, a frame / resource / allocation counter) isn't resolvable, so the name states the
# mechanism: atomic increment.
[entrypoint.functions."0x822CBEB8"]
name = "AtomicIncrement_822CBEB8"

# A dispose / destroy (with an optional free) -- the sibling of Dispose4-8 (a different type pointer, 7192):
# set a vtable / type pointer, run the 20-byte destructor (Destruct20Byte_832156E0), and free the object
# (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 7192 -- a different type than Dispose4_82A146C0's 7072,
# Dispose5_82A14720's 7096, Dispose6_82A14780's 7120, Dispose7_82A149C0's 7264, and Dispose8_82A12000's 6724).
# It then runs the 20-byte destructor (Destruct20Byte_832156E0, sub_832156E0). If the flag's bit 0 is set (the
# object is owned), it frees the object (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68); otherwise it leaves the
# memory (a destruct-but-don't-free path). So it's a "dispose the object (set the type, run the destructor, free if
# owned)" routine. 56 lines, 2 callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). 0 direct
# callers (reached via a dispatch / vcall, likely a type's destructor / dispose method). A structural sibling of
# Dispose4_82A146C0, Dispose5_82A14720, Dispose6_82A14780, Dispose7_82A149C0, and Dispose8_82A12000 -- same shape,
# different type pointer (7192). The object's class (a 20-byte object, ->0 = a vtable) and the free flag's meaning
# aren't resolvable, so the name states the mechanism: dispose the object (with an optional free). The "9"
# distinguishes it from Dispose_82B39A30, Dispose2_8233DD70, Dispose3_8233E380, Dispose4_82A146C0, Dispose5
# _82A14720, Dispose6_82A14780, Dispose7_82A149C0, and Dispose8_82A12000.
[entrypoint.functions."0x82A148A0"]
name = "Dispose9_82A148A0"

# A struct copy (a tenth sibling of the CopyStructData family): if src->20 >= 4, copy a variable-length byte
# array from src->12 (byte-by-byte to the stack) and advance src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r5). If src->20 >= 4, it
# copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop to the stack), advances src->12 by 4, and
# continues processing (calling sub_82A1B480, the sibling in the 0x82A1B cluster that the other CopyStructData*
# variants also call, plus sub_83211D48, a sibling in the 0x83211 cluster). So it's a "copy the struct's data"
# routine. 121 lines, 2 callees (sub_82A1B480, sub_83211D48). 0 direct callers (reached via a dispatch / vcall).
# The struct class and the exact field semantics aren't resolvable, so the name states the mechanism: copy the
# struct's data. The "10" distinguishes this from CopyStructData, CopyStructData2, 3, 4, 5, 6, 7, 8, and 9 (all
# the same shape).
[entrypoint.functions."0x832141C0"]
name = "CopyStructData10_832141C0"

# A dispose / destroy (with an optional free) -- the sibling of Dispose4-9 (a different type pointer, 7216):
# set a vtable / type pointer, run the 20-byte destructor (Destruct20Byte_832156E0), and free the object
# (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 7216 -- a different type than Dispose4_82A146C0's 7072,
# Dispose5_82A14720's 7096, Dispose6_82A14780's 7120, Dispose7_82A149C0's 7264, Dispose8_82A12000's 6724, and
# Dispose9_82A148A0's 7192). It then runs the 20-byte destructor (Destruct20Byte_832156E0, sub_832156E0). If the
# flag's bit 0 is set (the object is owned), it frees the object (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68);
# otherwise it leaves the memory (a destruct-but-don't-free path). So it's a "dispose the object (set the type, run
# the destructor, free if owned)" routine. 56 lines, 2 callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk
# _8221BE68). 0 direct callers (reached via a dispatch / vcall, likely a type's destructor / dispose method). A
# structural sibling of Dispose4_82A146C0, Dispose5_82A14720, Dispose6_82A14780, Dispose7_82A149C0, Dispose8
# _82A12000, and Dispose9_82A148A0 -- same shape, different type pointer (7216). The object's class (a 20-byte
# object, ->0 = a vtable) and the free flag's meaning aren't resolvable, so the name states the mechanism: dispose
# the object (with an optional free). The "10" distinguishes it from Dispose_82B39A30, Dispose2_8233DD70, Dispose3
# _8233E380, Dispose4_82A146C0, Dispose5_82A14720, Dispose6_82A14780, Dispose7_82A149C0, Dispose8_82A12000, and
# Dispose9_82A148A0.
[entrypoint.functions."0x82A14900"]
name = "Dispose10_82A14900"

# A struct copy (an eleventh sibling of the CopyStructData family): if src->20 >= 4, copy a variable-length
# byte array from src->12 (byte-by-byte to the stack) and advance src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r5). If src->20 >= 4, it
# copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop to the stack), advances src->12 by 4, and
# continues processing (calling sub_82A1B480, the sibling in the 0x82A1B cluster that the other CopyStructData*
# variants also call, plus sub_83211D48, a sibling in the 0x83211 cluster). So it's a "copy the struct's data"
# routine. 121 lines, 2 callees (sub_82A1B480, sub_83211D48). 0 direct callers (reached via a dispatch / vcall).
# The struct class and the exact field semantics aren't resolvable, so the name states the mechanism: copy the
# struct's data. The "11" distinguishes this from CopyStructData, CopyStructData2, 3, 4, 5, 6, 7, 8, 9, and 10 (all
# the same shape).
[entrypoint.functions."0x83213F68"]
name = "CopyStructData11_83213F68"

# A dispose / destroy (with an optional free) -- the sibling of Dispose4-10 (a different type pointer, 7240):
# set a vtable / type pointer, run the 20-byte destructor (Destruct20Byte_832156E0), and free the object
# (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 7240 -- a different type than Dispose4_82A146C0's 7072,
# Dispose5_82A14720's 7096, Dispose6_82A14780's 7120, Dispose7_82A149C0's 7264, Dispose8_82A12000's 6724,
# Dispose9_82A148A0's 7192, and Dispose10_82A14900's 7216). It then runs the 20-byte destructor (Destruct20Byte
# _832156E0, sub_832156E0). If the flag's bit 0 is set (the object is owned), it frees the object (Free_Size
# Bucketed_Thunk_8221BE68, sub_8221BE68); otherwise it leaves the memory (a destruct-but-don't-free path). So it's
# a "dispose the object (set the type, run the destructor, free if owned)" routine. 56 lines, 2 callees
# (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). 0 direct callers (reached via a dispatch / vcall,
# likely a type's destructor / dispose method). A structural sibling of Dispose4-10 -- same shape, different type
# pointer (7240). The object's class (a 20-byte object, ->0 = a vtable) and the free flag's meaning aren't
# resolvable, so the name states the mechanism: dispose the object (with an optional free). The "11" distinguishes
# it from Dispose_82B39A30, Dispose2_8233DD70, Dispose3_8233E380, and Dispose4-10.
[entrypoint.functions."0x82A14960"]
name = "Dispose11_82A14960"

# A struct copy (a twelfth sibling of the CopyStructData family): if src->20 >= 4, copy a variable-length byte
# array from src->12 (byte-by-byte to the stack) and advance src->12, then process further.

# Args: obj (r3), src (r4, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (r5). If src->20 >= 4, it
# copies (src->20 - 4) bytes from src->12 (a byte-by-byte loop to the stack), advances src->12 by 4, and
# continues processing (calling sub_82A1B480, the sibling in the 0x82A1B cluster that the other CopyStructData*
# variants also call, plus sub_83211D48, a sibling in the 0x83211 cluster). So it's a "copy the struct's data"
# routine. 121 lines, 2 callees (sub_82A1B480, sub_83211D48). 0 direct callers (reached via a dispatch / vcall).
# The struct class and the exact field semantics aren't resolvable, so the name states the mechanism: copy the
# struct's data. The "12" distinguishes this from CopyStructData, CopyStructData2, 3, 4, 5, 6, 7, 8, 9, 10, and 11
# (all the same shape).
[entrypoint.functions."0x83213D10"]
name = "CopyStructData12_83213D10"

# A chain / tree traversal and search: walk the chain (a binary-tree search, via node->0, node->8, node->12,
# node->25) and find the node whose ->12 key is at / for the given value (src->0).

# Args: (r3), obj (r4, ->4 = a tree root), src (r5, ->0 = a target key), (more). Loads the tree root (obj->4->4).
# It then walks the tree: for each node, it checks a flag (node->25); if the flag is set, it bails (a found /
# terminal path). Otherwise it compares the node's ->12 key to the target (src->0): if node->12 < target, it
# advances to node->8 (the right child); otherwise it advances to node->0 (the left child). It continues until it
# finds a node (or reaches the end), returning the found node. So it's a "walk the tree to find the node at / for
# the given key" routine (a binary-tree search keyed on node->12, part of the 0x82C34 cluster). 87 lines, no
# callees (pure data manipulation). Many callers (sub_8288D810, sub_827EF058, LookupAndInsertNode_8323A110, ...).
# The node's class (->0 = left, ->8 = right, ->12 = key, ->25 = a flag) and the tree's meaning (a sorted /
# ordered structure) aren't resolvable, so the name states the mechanism: walk the tree to find the node. The "2"
# distinguishes it from Tree_Search (an earlier entry in the same family).
[entrypoint.functions."0x82C343A0"]
name = "Tree_Search2_82C343A0"

# A validate-and-search: zero the result fields (obj->0, obj->4), search the tree (Tree_Search2_82C343A0), and
# validate the result (raising an assertion, trap 22, if it is invalid).

# Args: obj (r3, ->0, ->4 = result fields), src (r4, ->4, ->8), (r5). Zeros the result fields (obj->0, obj->4).
# It then searches the tree: it calls Tree_Search2_82C343A0 (sub_82C343A0, a binary-tree search, passing the
# source). It validates the result: if the result's ->0 field is 0, or differs from src->4 (a source / parent
# pointer), it raises an assertion (twi 31, r0, 22 -- a trap 22, the game's debug-assert instruction). In a path it
# also calls ChainLookup_82550638 (sub_82550638, a chain lookup) and Release_RefCounted_829FF648 (sub_829FF648, a
# refcounted release). So it's a "search the tree (Tree_Search2) and validate the result (assert it is valid)"
# routine (a validated tree search, part of the 0x8323A cluster). 109 lines, 3 callees (Tree_Search2_82C343A0,
# ChainLookup_82550638, Release_RefCounted_829FF648). 4 callers (sub_83225920, sub_832254B8, sub_83225D80, sub
# _832273F8). The tree's class (a binary tree, a node's ->0 / ->4 = parent / source) and the search's meaning
# aren't resolvable, so the name states the mechanism: validate and search the tree.
[entrypoint.functions."0x8323A058"]
name = "ValidateAndSearch_8323A058"

# A dispose / destroy (with an optional free) -- the sibling of Dispose4-11 (a different type pointer, 7168):
# set a vtable / type pointer, run the 20-byte destructor (Destruct20Byte_832156E0), and free the object
# (Free_SizeBucketed_Thunk_8221BE68) if a flag is set.

# Args: obj (r3, ->0 = a vtable / type pointer), flag (r4, bit 0 = an "owned / free" flag). Sets obj->0 to a
# fixed global (a vtable / type pointer, 0x82CCC8C0 + 7168 -- a different type than the other Dispose variants).
# It then runs the 20-byte destructor (Destruct20Byte_832156E0, sub_832156E0). If the flag's bit 0 is set (the
# object is owned), it frees the object (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68); otherwise it leaves the
# memory (a destruct-but-don't-free path). So it's a "dispose the object (set the type, run the destructor, free if
# owned)" routine. 56 lines, 2 callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). 0 direct
# callers (reached via a dispatch / vcall, likely a type's destructor / dispose method). A structural sibling of
# Dispose4-11 -- same shape, different type pointer (7168). The object's class (a 20-byte object, ->0 = a vtable)
# and the free flag's meaning aren't resolvable, so the name states the mechanism: dispose the object (with an
# optional free). The "12" distinguishes it from Dispose_82B39A30, Dispose2_8233DD70, Dispose3_8233E380, and
# Dispose4-11.
[entrypoint.functions."0x82A14840"]
name = "Dispose12_82A14840"

# A copy-and-process (the sibling of CopyAndProcess_83214828, using 128-bit vector ops): copy the struct data
# (CopyStructData4_82A1BEA8) to the stack, read the copied values with 128-bit vector loads, store them to the
# object (obj->16, plus a float multiply into obj->16+16), and process (sub_83211D48) if a flag is set.

# Args: obj (r3, ->16 = a 128-bit field), (r4), flag (r5, low byte = a "process" flag). Calls CopyStructData4
# _82A1BEA8 (sub_82A1BEA8) to copy the struct data to the stack. It then reads the copied values with 128-bit
# vector loads (lvx128), and stores them to the object: obj->16 = the copied value (a 128-bit store, stvx128), and
# obj->16+16 = the value squared (a 128-bit float multiply, vmulfp128, of the value by itself -- a float4 squared).
# If the flag's low byte is non-zero (a process flag), it processes the object (calling sub_83211D48, a sibling in
# the 0x83211 cluster). So it's a "copy the struct data, store the key value and its square (a float4 and a
# float4-squared, via 128-bit vector ops), and process the object (if flagged)" routine (a copy-and-process for the
# 0x83214 cluster, a sibling of CopyAndProcess_83214828). 66 lines, 2 callees (CopyStructData4_82A1BEA8, sub
# _83211D48). 0 direct callers (reached via a dispatch / vcall). The object's class (->16 = a float4 value) and the
# flag's meaning (a process flag) aren't resolvable, so the name states the mechanism: copy and process the struct
# data. The "2" distinguishes it from CopyAndProcess_83214828 (which uses scalar 32-bit stores instead of 128-bit
# vector ops).
[entrypoint.functions."0x83214418"]
name = "CopyAndProcess2_83214418"

# A node constructor (the sibling of Construct20ByteNode_83215D98 / Construct20ByteNode2_83215D18 / Construct
# 20ByteNode3_83215CB8 / Construct20ByteNode4_832158B0 / Construct20ByteNode5_83215A80): call Construct20Byte
# _83215640 (the base 20-byte constructor), then set a vtable / type pointer (7168 -- the Dispose12_82A14840 type)
# and a 128-bit vector field from a global float.

# Args: obj (r3, ->0 = a vtable / type pointer, ->16 = a 128-bit vector field). Calls Construct20Byte_83215640
# (sub_83215640, the base 20-byte constructor) to allocate and zero the node. It then sets the node's vtable /
# type pointer (obj->0 = a fixed global, 0x82CCC8C0 + 7168 -- the same type pointer Dispose12_82A14840 uses) and a
# 128-bit vector field (obj->16 = a fixed global float, replicated into a 128-bit vector via vspltw and stored with
# stvx128). So it's a "construct a 20-byte node and set its type and default vector field" routine (a typed node
# constructor, part of the 0x83215 cluster). 60 lines, 1 callee (Construct20Byte_83215640). 3 callers: sub_83218630,
# sub_83219588, ProcessTreeNode_83218C10 (sub_83218C10). A sibling of Construct20ByteNode_83215D98, Construct20
# ByteNode2_83215D18, Construct20ByteNode3_83215CB8, Construct20ByteNode4_832158B0, and Construct20ByteNode5
# _83215A80 -- same base (Construct20Byte), different type / field layout (type 7168, a 128-bit vector field at ->16).
# The node's class (a 20-byte typed record) isn't resolvable, so the name states the mechanism: construct a 20-byte
# node. The "6" distinguishes it from Construct20ByteNode_83215D98, 2, 3, 4, and 5.
[entrypoint.functions."0x83215BF0"]
name = "Construct20ByteNode6_83215BF0"

# A vcall + struct-copy (with teardown): make a vcall (src->0->8, passing the source), then copy the source's
# data (CopyStructData4_82A1BEA8), and process / tear it down (DestroyCircularList_82A0F5C0, Free_SizeBucketed
# _Thunk_8221BE68).

# Args: obj (r3), src (r4, ->0 = a vtable, ->4 = data, ->12 = a byte-array pointer, ->20 = a size), (more). Makes
# a vcall (src->0->8, passing the source). If src->20 >= 4, it copies (src->20 - 4) bytes from src->12 (a byte-by-
# byte loop to the stack, advancing src->12 by 4). It then processes / tears down the data: it calls CopyStructData4
# _82A1BEA8 (sub_82A1BEA8, a struct copy), DestroyCircularList_82A0F5C0 (sub_82A0F5C0, a circular-list destroy),
# Free_SizeBucketed_Thunk_8221BE68 (sub_8221BE68, a free), and several sub_82A0* siblings (sub_82A0ED98, sub_82A0EDF0,
# sub_82A0F328, sub_82A0F800) and sub_82A1B480. So it's a "dispatch a vcall, copy the source's data, and process /
# tear it down (a circular list and a free)" routine (a vcall + struct-copy + teardown, part of the 0x82A0E
# cluster). 541 lines -- a large routine (the body has a long tail of processing beyond the initial vcall and copy).
# 8 callees, 2 vcall dispatches. 1 caller (sub_83220548). The source's class (->0 = a vtable, a byte-array at ->12)
# and the processed data's meaning (a circular list) aren't resolvable, so the name states the mechanism: dispatch a
# vcall and copy the struct's data.
[entrypoint.functions."0x82A0E470"]
name = "DispatchAndCopy_82A0E470"

# A 48-byte node constructor: allocate a 48-byte object (Allocate_OrDbgBreak_8221F388), set up self-references
# (result->0 = result, result->4 = result -- a circular-list / self-referential node), and store the result to the
# caller (obj->4 = result, obj->8 = 0).

# Args: obj (r3, ->4 = the new node, ->8 = a value), (more). Allocates a 48-byte object (Allocate_OrDbgBreak
# _8221F388, sub_8221F388). If the allocation succeeds, it sets up the node's self-references: result->0 = result
# and result->4 = result (the node points to itself, a circular-list / self-referential pattern). It then stores
# the node to the caller: obj->4 = result (the new node), and obj->8 = 0 (a value / count). So it's a "construct a
# 48-byte self-referential node (allocate it, set up its self-references, and return it)" routine (a circular-list
# node constructor, part of the 0x82A0E cluster). 56 lines, 1 callee (Allocate_OrDbgBreak_8221F388). 2 callers: sub
# _82A0E470 (DispatchAndCopy_82A0E470), sub_82A0E838. The node's class (a 48-byte self-referential / circular-list
# node, ->0 and ->4 = self-pointers) isn't resolvable, so the name states the mechanism: construct a 48-byte node.
[entrypoint.functions."0x82A0ED98"]
name = "Construct48ByteNode_82A0ED98"

# A 48-byte node constructor (the sibling of Construct48ByteNode_82A0ED98, a larger variant that loops): if
# obj->8 >= 2, allocate 48-byte objects (Allocate_OrDbgBreak_8221F388), set up self-references (result->0 = result,
# result->4 = result), and loop (allocating more), with a circular-list destroy (DestroyCircularList_82A0F5C0) and
# frees (Free_SizeBucketed_Thunk_8221BE68).

# Args: obj (r3, ->8 = a count), (more). If obj->8 < 2, takes an early path. Otherwise it allocates 48-byte objects
# (Allocate_OrDbgBreak_8221F388, sub_8221F388) and sets up their self-references (result->0 = result, result->4 =
# result -- a circular-list / self-referential pattern). It then loops (allocating more 48-byte objects, and calling
# Allocate_SizeBucketed_Thunk_8221F3F0, sub_8221F3F0, for larger allocations). It also destroys a circular list
# (DestroyCircularList_82A0F5C0, sub_82A0F5C0) and frees objects (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68) in
# the teardown paths, plus several sub_82A0* siblings (sub_82A0F3B8, sub_82A0F528) and sub_82CBBB58. So it's a
# "construct 48-byte self-referential nodes (allocate them, set up self-references, and loop; with teardown)"
# routine (a larger 48-byte node constructor, part of the 0x82A0E cluster). 393 lines, 7 callees (Allocate_OrDbgBreak
# _8221F388, Allocate_SizeBucketed_Thunk_8221F3F0, DestroyCircularList_82A0F5C0, Free_SizeBucketed_Thunk_8221BE68,
# sub_82A0F3B8, sub_82A0F528, sub_82CBBB58). 2 callers: sub_82A0E838, DispatchAndCopy_82A0E470 (sub_82A0E470). The
# node's class (a 48-byte self-referential / circular-list node, ->0 and ->4 = self-pointers) isn't resolvable, so
# the name states the mechanism: construct a 48-byte node. The "2" distinguishes it from Construct48ByteNode_82A0ED98
# (a smaller single-node variant).
[entrypoint.functions."0x82A0EDF0"]
name = "Construct48ByteNode2_82A0EDF0"

# A sub-object dispose: if a condition holds (src->8 <= 1) and obj->0 is non-null, free the sub-object (Free_Size
# Bucketed_Thunk_8221BE68) and zero the field (obj->0 = 0); otherwise take another path (with allocations).

# Args: obj (r3, ->0 = a sub-object), src (r4, ->8 = a count), (more). If src->8 <= 1 (a small count) and obj->0 is
# non-null, it frees the sub-object (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68) and zeros the field (obj->0 =
# 0). Otherwise it takes another path (a larger count), which (in a path) allocates (Allocate_OrDbgBreak_8221F388,
# sub_8221F388) and calls sub_82A0FCA8 (a sibling in the 0x82A0F cluster). So it's a "dispose a sub-object (free it
# and zero the field, or take a larger path)" routine (a sub-object teardown, part of the 0x82A0F cluster). 203
# lines, 3 callees (Free_SizeBucketed_Thunk_8221BE68, Allocate_OrDbgBreak_8221F388, sub_82A0FCA8). 2 callers: sub
# _82A0E838, DispatchAndCopy_82A0E470 (sub_82A0E470). The sub-object's class (->0 = a sub-object, a 48-byte / 20-byte
# node) and the count's meaning (src->8 = a count) aren't resolvable, so the name states the mechanism: dispose a
# sub-object.
[entrypoint.functions."0x82A0F800"]
name = "DisposeSubObj_82A0F800"

# A build-and-dispatch: build a stack struct (global floats and a 128-bit vector), then dispatch (DispatchAndCopy
# _82A0E470); free a result (Free_SizeBucketed_Thunk_8221BE68) on a path.

# Args: obj (r3), (r4), value (r5), (more). Builds a stack struct: it loads global floats (into stack[84] and
# stack[88]), zeros a field (stack[80] = 0), and loads a 128-bit vector (lvx128, from an indexed global array based
# on the value). It then dispatches: it calls DispatchAndCopy_82A0E470 (sub_82A0E470, a vcall + struct-copy, passing
# the object and the stack struct). In a path it also frees a result (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68)
# and calls sub_82A0B930 (a sibling in the 0x82A0B cluster). So it's a "build a stack struct (floats and a 128-bit
# vector) and dispatch on it (a vcall + struct-copy)" routine (a build-and-dispatch, part of the 0x83220 cluster).
# 129 lines, 3 callees (DispatchAndCopy_82A0E470, Free_SizeBucketed_Thunk_8221BE68, sub_82A0B930). 3 callers (sub
# _83216BB0, sub_83217688, sub_8323EB40). The stack struct's meaning (a parameter block with floats and a vector)
# and the value's source (an index) aren't resolvable, so the name states the mechanism: build a struct and dispatch.
[entrypoint.functions."0x83220548"]
name = "BuildAndDispatch_83220548"

# An append-to-a-global-array: append a record (5 words) to a global array (count at ->10244, capacity at ->10240),
# with a capacity check (return early if the array is full).

# Args: (r3), a (r4), b (r5), c (r6), (more). Loads a global array (a fixed .data base): the count (->10244) and
# capacity (->10240). If count == capacity (the array is full), it returns early. Otherwise it appends a record: it
# copies 5 words (a, b, c, and 2 more from the stack, saved at stack[32]) to the array at the count's offset (a
# 5-word record, stored word-by-word), and increments the count (count++). So it's a "append a 5-word record to a
# global array (with a capacity check)" routine (a global array appender, part of the 0x82BAA cluster). 56 lines,
# no callees (pure data manipulation). 1 caller: AdvanceAndDispatch_82BAA4A8 (sub_82BAA4A8). The array's meaning
# (a global list of 5-word records, a log / registry / event list) and the record's fields (a, b, c) aren't
# resolvable, so the name states the mechanism: append to a global array.
[entrypoint.functions."0x82BAA688"]
name = "AppendToGlobalArray_82BAA688"

# A process (with a chain lookup): branch on a condition (src->8), and process the data (sub_82370060, with chain
# lookups via FindChainRoot3_82A962B0 / FindChainRoot4_828836E8).

# Args: obj (r3), src (r4, ->4, ->8 = a condition), flag (r5), value (r6), (more). Branches on src->8: if it is 0,
# it calls sub_82370060 (sub_82370060, a sibling in the 0x8237 cluster, with src->4, a constant, and the value). In
# the other path (src->8 non-zero), it processes the data, calling sub_8236FF20 (a sibling in the 0x8236 cluster)
# and the chain-root finders (FindChainRoot3_82A962B0, sub_82A962B0, and FindChainRoot4_828836E8, sub_828836E8) to
# look up a chain root. So it's a "process the data (dispatch on a condition, with a chain lookup)" routine (a
# process with a chain lookup, part of the 0x8236F cluster). 298 lines, 4 callees (sub_82370060, sub_8236FF20,
# FindChainRoot3_82A962B0, FindChainRoot4_828836E8). 2 callers (sub_82371958, sub_8236E4A0). The object's class
# (->4, ->8 = a condition) and the data's meaning (a chain / tree) aren't resolvable, so the name states the
# mechanism: process the data (with a chain lookup).
[entrypoint.functions."0x8236F398"]
name = "ProcessChainLookup_8236F398"

# A validation + error: check a value (src->8 against a threshold), and (if it is invalid) build an error string
# (SetString_822F2020 + a format, sub_826C3EF0) and raise an error (sub_822F1F00); splice a list (List_SpliceNode2
# _8292CC88) in a path.

# Args: obj (r3), src (r4, ->8 = a value), value (r5), (r6), (r7), (more). Checks the value (src->8) against a
# threshold (0x20000000 | 52427): if src->8 < the threshold, it takes one path; otherwise it builds an error string
# (SetString_822F2020, sub_822F2020, with a string at a fixed .data address, and a format via sub_826C3EF0, sub
# _826C3EF0) and raises an error (sub_822F1F00, an error / raise function). In a path it also splices a list (List
# _SpliceNode2_8292CC88, sub_8292CC88) and calls sub_82171810, sub_8234D600, sub_82370EF8, and sub_826C3FA8. So it's
# a "validate a value (check it against a threshold) and raise an error (build an error string and raise) if it is
# invalid" routine (a validated operation with an error path, part of the 0x82370 cluster). 329 lines, 8 callees
# (SetString_822F2020, sub_822F1F00, List_SpliceNode2_8292CC88, sub_826C3EF0, sub_826C3FA8, sub_82171810, sub
# _8234D600, sub_82370EF8). 2 callers (sub_8236F398, sub_8236FF20). The value's meaning (src->8 = a value, an index
# / count) and the error's meaning (a particular error / assertion) aren't resolvable, so the name states the
# mechanism: raise an error if the value is invalid.
[entrypoint.functions."0x82370060"]
name = "RaiseIfInvalid_82370060"

# A 36-byte object constructor: allocate a 36-byte object (Allocate_OrDbgBreak_8221F388) and set up its fields
# (a, b, c, src->0, a sub-value via SetValue2_8236EC48, and zero flags at ->32 and ->33).

# Args: (r3), a (r4), b (r5), c (r6), src (r7, ->0 = a value), (more). Allocates a 36-byte object (Allocate_OrDbg
# Break_8221F388, sub_8221F388). If the allocation succeeds, it sets up the object's fields: ->0 = a, ->4 = b, ->8
# = c, ->12 = src->0 (a value from the source), ->16 = a sub-value (SetValue2_8236EC48, sub_8236EC48, with the
# object+16 and src+4), and zero flags (->32 = 0, ->33 = 0). So it's a "construct a 36-byte object (allocate it
# and set its fields)" routine. 66 lines, 2 callees (Allocate_OrDbgBreak_8221F388, SetValue2_8236EC48). 1 caller:
# sub_82370060 (RaiseIfInvalid_82370060). The object's class (a 36-byte object, a record with a, b, c, a sub-value,
# and flags) and the source's meaning aren't resolvable, so the name states the mechanism: construct a 36-byte
# object.
[entrypoint.functions."0x82370EF8"]
name = "Construct36Byte_82370EF8"

# A block-size computation: clamp a value (against obj->12), then compute a size (integer arithmetic, shifts,
# divisions, and squares), with traps (twllei) on a zero-division.

# Args: obj (r3, ->8, ->12, ->16, ->28 = parameters), value (r4), (more). Clamps the value: if value < obj->12,
# it uses obj->12 (a minimum). It then computes a size: it reads the object's parameters (obj->8, obj->16, obj->28,
# a shift amount), and performs a sequence of integer operations (shifts, multiplications/squares, subtractions, and
# divisions). It traps (twllei r8, 0 and twllei r6, 0) on a zero-division (a guard against dividing by zero). So it's
# a "compute a block size (clamp the value, and compute the size via integer arithmetic)" routine (a size /
# layout computation, part of the 0x829FD cluster). 65 lines, no callees (pure integer arithmetic). 2 callers: sub
# _83231240, Heap_CarveBlock_83231058 (sub_83231058, a heap block carver). The object's class (->8, ->12, ->16, ->28
# = parameters, a shift amount) and the size's meaning (a heap block size / layout) aren't resolvable, so the name
# states the mechanism: compute a block size.
[entrypoint.functions."0x829FD1C8"]
name = "ComputeBlockSize_829FD1C8"

# A size-checked allocation (the sibling of AllocateChecked_8264EFE0 / AllocateChecked2_8217A488, with a 4x
# multiplier): check the size (an overflow check: size != 0 and -1 / size < 4), compute the allocation size (size *
# 4), and allocate (Allocate_OrDbgBreak_8221F388); raise an error (sub_822F1F00) on a failure path.

# Args: (r3), size (r4), (more). Checks the size: if size is 0, or -1 / size >= 4 (an overflow check -- the
# allocation size would overflow), it takes an error path. Otherwise it computes the allocation size (size * 4) and
# allocates it (Allocate_OrDbgBreak_8221F388, sub_8221F388). On a failure path it calls sub_822F1F00 (an error /
# setup function, with a string at a fixed .data address). So it's a "check the size (an overflow check), compute
# the allocation size (size * 4), and allocate (with an error on failure)" routine (a size-checked allocation of 4-
# byte elements). 66 lines, 2 callees (Allocate_OrDbgBreak_8221F388, sub_822F1F00). Many callers (sub_82AAFB08, sub
# _82C09F00, sub_8247D908, ...). The 4-byte element's meaning (a record / node) and the size's source aren't
# resolvable, so the name states the mechanism: check the size and allocate (size * 4). The "3" distinguishes it
# from AllocateChecked_8264EFE0 (12x) and AllocateChecked2_8217A488 (2x).
[entrypoint.functions."0x82333F78"]
name = "AllocateChecked3_82333F78"

# A value store (the sibling of SetValue_8236FCE8 / SetValue2_8236EC48): compute counts ((obj->12 - obj->4) >> 2,
# a capacity, and (obj->8 - obj->4) >> 2, a used count), and (if there is room) store a value (a float, obj->8->0 =
# the value) and advance the write pointer (obj->8 += 4).

# Args: obj (r3, ->4 = a base, ->8 = a write pointer, ->12 = an end), value (r4, a float source), (more). Computes
# the capacity ((obj->12 - obj->4) >> 2, a count of 4-byte elements) and the used count ((obj->8 - obj->4) >> 2).
# If the used count is less than the capacity (there is room), it stores the value: obj->8->0 = the value (a float,
# loaded from the source), and advances the write pointer (obj->8 += 4). If there is no room, it takes another path
# (a grow / full condition, calling sub_82A0A040, a sibling in the 0x82A0A cluster). So it's a "store a value into a
# 4-byte-stride array (with a bounds check, advancing the write pointer)" routine (a value setter for a growing /
# ring buffer, part of the 0x82A09 cluster). 87 lines, 1 callee (sub_82A0A040). 2 callers (sub_832402B8, sub
# _83221378). The buffer's class (->4 = a base, ->8 = a write pointer, ->12 = an end, a 4-byte-stride array of
# floats) and the value's meaning aren't resolvable, so the name states the mechanism: store a value (with a bounds
# check). The "3" distinguishes it from SetValue_8236FCE8 and SetValue2_8236EC48.
[entrypoint.functions."0x82A09FB0"]
name = "SetValue3_82A09FB0"

# A trivial offset getter: return (obj->12 + index * 8) -- a base plus an index into an 8-byte-stride array.

# Args: obj (r3, ->12 = a base), index (r4), (more). Returns (obj->12 + (index << 3)) = (obj->12 + index * 8) -- a
# base (obj->12) plus an index into an 8-byte-stride array. 3 lines, no callees. A sibling of GetOffset732_82BAA148
# and GetOffset684_82BAA150 (which compute (index + constant) * 16 from a base); this one computes (base + index * 8)
# (an 8-byte stride). It is registered (in the function table, fable_2_init.cpp / fable_2_register.cpp) but has no
# direct callers in the recompiled code (a registered-but-uncalled getter, likely invoked only from code outside the
# traced region, or unused). The array's meaning (an 8-byte-stride array) and the base (obj->12) aren't resolvable,
# so the name states the mechanism: get an offset (base + index * 8). The "8" distinguishes it from the other
# GetOffset* getters.
[entrypoint.functions."0x82BABC60"]
name = "GetOffset8_82BABC60"

# A store-with-grow (the sibling of SetValue3_82A09FB0, the grow path it calls): compute a count ((obj->12 - obj->4)
# >> 2), store values (a float, into a 4-byte-stride array), and (on a grow / full path) reallocate (Allocate
# Checked3_82333F78) and copy the data (CheckedCopy_82CA3808 / MemCpy_Aligned_82CA30E8), freeing the old buffer (Free
# _SizeBucketed_Thunk_8221BE68).

# Args: obj (r3, ->4 = a base, ->8 = a write pointer, ->12 = an end), (r4), count (r5), float (r6), (more). Computes
# the count ((obj->12 - obj->4) >> 2, a count of 4-byte elements). It stores values (a float, into the 4-byte-stride
# array, advancing the write pointer). On a grow / full path (when there is no room), it reallocates (Allocate
# Checked3_82333F78, sub_82333F78, a size-checked allocation of 4-byte elements) and copies the data (CheckedCopy
# _82CA3808, sub_82CA3808, and MemCpy_Aligned_82CA30E8, sub_82CA30E8), freeing the old buffer (Free_SizeBucketed
# _Thunk_8221BE68, sub_8221BE68). It also calls sub_82A97648 (a sibling in the 0x82A97 cluster). So it's a "store
# values into a 4-byte-stride array (with a grow / reallocate path: allocate a larger buffer and copy the data)"
# routine (a store with a grow, part of the 0x82A0A cluster, the grow path of SetValue3_82A09FB0). 394 lines, 5
# callees (AllocateChecked3_82333F78, CheckedCopy_82CA3808, MemCpy_Aligned_82CA30E8, Free_SizeBucketed_Thunk_8221BE
# 68, sub_82A97648). 2 callers: sub_82AB7168, SetValue3_82A09FB0 (sub_82A09FB0). The buffer's class (->4 = a base,
# ->8 = a write pointer, ->12 = an end, a 4-byte-stride array of floats) and the values' meaning aren't resolvable,
# so the name states the mechanism: store values (with a grow / reallocate).
[entrypoint.functions."0x82A0A040"]
name = "StoreValueGrow_82A0A040"

# A size-checked allocation (the sibling of AllocateChecked3_82333F78, with an 8x multiplier): check the size (an
# overflow check: size != 0 and -1 / size < 8), compute the allocation size (size * 8), and allocate (Allocate_OrDbg
# Break_8221F388); raise an error (sub_822F1F00) on a failure path.

# Args: (r3), size (r4), (more). Checks the size: if size is 0, or -1 / size >= 8 (an overflow check -- the
# allocation size would overflow), it takes an error path. Otherwise it computes the allocation size (size * 8) and
# allocates it (Allocate_OrDbgBreak_8221F388, sub_8221F388). On a failure path it calls sub_822F1F00 (an error /
# setup function, with a string at a fixed .data address). So it's a "check the size (an overflow check), compute
# the allocation size (size * 8), and allocate (with an error on failure)" routine (a size-checked allocation of 8-
# byte elements). 66 lines, 2 callees (Allocate_OrDbgBreak_8221F388, sub_822F1F00). Many callers (sub_82A58C68, sub
# _82343560, sub_8260B278, ...). The 8-byte element's meaning (a record / node / pointer) and the size's source
# aren't resolvable, so the name states the mechanism: check the size and allocate (size * 8). The "4" distinguishes
# it from AllocateChecked_8264EFE0 (12x), AllocateChecked2_8217A488 (2x), and AllocateChecked3_82333F78 (4x).
[entrypoint.functions."0x82A87220"]
name = "AllocateChecked4_82A87220"

# An atomic increment (the sibling of AtomicIncrement_822CBEB8): atomically increment a value at (ptr + 4), using an
# lwarx/stwcx. loop under the global lock (mfmsr/mtmsrd to disable interrupts), and return the new value.

# Args: ptr (r3, the target is ptr + 4), (more). Atomically increments the 32-bit value at (ptr + 4): it enters the
# global lock (REX_ENTER_GLOBAL_LOCK, with mfmsr/mtmsrd to disable interrupts), does a load-and-reserve / store-
# conditional (lwarx/stwcx.) to read the value, increment it by 1, and write it back (retrying on a store-conditional
# failure), leaves the global lock, and returns the new value (r3 = the incremented value). So it's an "atomically
# increment a value (at ptr + 4, under the global lock) and return the new value" routine (an atomic increment, the
# sibling of AtomicIncrement_822CBEB8, sub_822CBEB8). 24 lines (to the next function), no callees (pure atomic
# arithmetic). It is registered (in the function table, fable_2_init.cpp / fable_2_register.cpp) but has no direct
# callers in the recompiled code (a registered-but-uncalled atomic increment, likely invoked only from code outside
# the traced region, or unused). The value's meaning (a counter at ptr + 4, a reference count / global counter) isn't
# resolvable, so the name states the mechanism: atomically increment a value (at ptr + 4). The "2" distinguishes it
# from AtomicIncrement_822CBEB8.
[entrypoint.functions."0x82375F28"]
name = "AtomicIncrement2_82375F28"

# A virtual call: load a function pointer from a vtable (obj->8 -> ->24) and call it (bctr), passing the object
# (r3 = obj->8, the vtable-derived this) and the other arguments.

# Args: obj (r3, ->8 = a vtable), (r4), (more). Loads the vtable (obj->8), loads a function pointer from it (vtable->
# 24), sets the count register (ctr = the function pointer), and calls it (bctr -- a virtual call). It passes the
# object (r3 = obj->8, the vtable-derived this) and the other arguments (r4, ...). So it's a "virtual call (dispatch
# via a vtable, calling a virtual method at vtable->24)" routine (a virtual-call thunk, part of the 0x826C0 cluster).
# 4 lines (to the next function), no callees (a single indirect call). It is registered (in the function table, fable
# _2_init.cpp / fable_2_register.cpp) but has no direct callers in the recompiled code (a registered-but-uncalled
# virtual-call thunk, likely invoked only from code outside the traced region, or unused). The vtable's meaning (a
# class, a method at vtable->24) isn't resolvable, so the name states the mechanism: a virtual call (dispatch via a
# vtable). The address suffix distinguishes it from other VirtualCall* thunks.
[entrypoint.functions."0x826C0388"]
name = "VirtualCall_826C0388"

# A trivial field getter: return (obj->16).

# Args: obj (r3, ->16 = a value), (more). Returns (obj->16) -- a field at offset 16. 2 lines (to the next function),
# no callees (pure data access). A sibling of the GetField*/GetGlobalPtr* trivial getters (e.g., GetField12). It is
# registered (in the function table, fable_2_init.cpp / fable_2_register.cpp) but has no direct callers in the
# recompiled code (a registered-but-uncalled getter, likely invoked only from code outside the traced region, or
# unused). The field's meaning (obj->16, a sub-object / value / pointer) isn't resolvable, so the name states the
# mechanism: get a field (obj->16). The "13" distinguishes it from GetField12 and other GetField* getters.
[entrypoint.functions."0x82B495F8"]
name = "GetField13_82B495F8"

# A double-virtual-call: load a vtable (obj->16), call two virtual methods (vtable->24 and vtable->28, via bctrl),
# and return their difference.

# Args: obj (r3, ->16 = a vtable), (more). Loads the vtable (obj->16). It then makes two virtual calls (via bctrl):
# first, it calls the virtual method at vtable->24 (saving the result); second, it calls the virtual method at
# vtable->28 (with the object as the this). It returns the difference (result1 - result2). So it's a "call two
# virtual methods (vtable->24 and vtable->28) and return their difference" routine (a double-virtual-call, part of
# the 0x82C01 cluster). 59 lines, no direct callees (two indirect calls via bctrl, which the callee scan does not
# count). Many callers (sub_82C17AE8, sub_82C18550, sub_82C2B430, ... -- all in the 0x82C1/0x82C2 region). The
# vtable's meaning (a class, two virtual methods at vtable->24 and vtable->28) and the difference's meaning (a
# time / length / size difference) aren't resolvable, so the name states the mechanism: call two virtual methods
# (and return their difference).
[entrypoint.functions."0x82C01D60"]
name = "VirtualCallPair_82C01D60"

# A value store (the sibling of SetValue3_82A09FB0, with an 8-byte stride): compute counts ((obj->12 - obj->4) >> 3,
# a capacity, and (obj->8 - obj->4) >> 3, a used count, in 8-byte units), and (if there is room) store a value (8
# bytes, obj->8->0 and obj->8->4 = the value) and advance the write pointer (obj->8 += 8).

# Args: obj (r3, ->4 = a base, ->8 = a write pointer, ->12 = an end), value (r4, ->0 and ->4 = the value), (more).
# Computes the capacity ((obj->12 - obj->4) >> 3, a count of 8-byte elements) and the used count ((obj->8 - obj->4)
# >> 3). If the used count is less than the capacity (there is room), it stores the value: obj->8->0 = value->0 and
# obj->8->4 = value->4 (an 8-byte value, two words), and advances the write pointer (obj->8 += 8). If there is no
# room, it takes another path (a grow / full condition, calling sub_8260B008, a sibling in the 0x8260B cluster --
# the grow path, as in SetValue3_82A09FB0 which calls StoreValueGrow_82A0A040). So it's a "store an 8-byte value
# into an 8-byte-stride array (with a bounds check, advancing the write pointer)" routine (a value setter for a
# growing buffer, part of the 0x829FD cluster). 122 lines, 1 callee (sub_8260B008). Many callers (sub_8322C450, sub
# _82A08EB0, sub_83221548, ...). The buffer's class (->4 = a base, ->8 = a write pointer, ->12 = an end, an 8-byte-
# stride array) and the value's meaning (an 8-byte record / pointer / pair) aren't resolvable, so the name states
# the mechanism: store a value (8 bytes, with a bounds check). The "4" distinguishes it from SetValue3_82A09FB0
# (4 bytes).
[entrypoint.functions."0x829FD7B8"]
name = "SetValue4_829FD7B8"

# A store-with-grow (the sibling of StoreValueGrow_82A0A040, the 8-byte grow path of SetValue4_829FD7B8): atomically
# increment a counter (src->4, an lwarx/stwcx loop under the global lock), then process (grow the buffer: allocate
# via AllocateChecked4_82A87220, copy the data, and manage refcounts via Release_RefCounted_829FF648 / Copy
# RefcountedArray_827F2010), freeing the old buffer (Free_SizeBucketed_Thunk_8221BE68).

# Args: obj (r3), (r4), src (r6, ->0, ->4 = a counter), (more). If src->4 (a counter) is non-zero, it atomically
# increments it (an lwarx/stwcx loop under the global lock, with mfmsr/mtmsrd to disable interrupts). It then
# processes the data (a grow / reallocate): it allocates (AllocateChecked4_82A87220, sub_82A87220, a size-checked
# allocation of 8-byte elements), copies the data, and manages refcounts (Release_RefCounted_829FF648, sub_829FF648,
# and CopyRefcountedArray_827F2010, sub_827F2010), freeing the old buffer (Free_SizeBucketed_Thunk_8221BE68, sub
# _8221BE68). It also calls sub_8284B558, sub_829FA6A0, and sub_82A97648 (siblings in the 0x8284B/0x829FA/0x82A97
# clusters). So it's a "store values into an 8-byte-stride array (with a grow / reallocate path: atomically update a
# counter, allocate a larger buffer, and manage refcounts)" routine (a store with a grow, part of the 0x8260B cluster,
# the 8-byte grow path of SetValue4_829FD7B8). 369 lines, 7 callees (AllocateChecked4_82A87220, Release_RefCounted
# _829FF648, CopyRefcountedArray_827F2010, Free_SizeBucketed_Thunk_8221BE68, sub_8284B558, sub_829FA6A0, sub_82A97648
# ). Many callers (sub_8234FBE0, sub_82607260, sub_82769838, ...). The buffer's class (->4 = a base, ->8 = a write
# pointer, an 8-byte-stride array) and the counter's meaning (src->4 = a refcount / counter) aren't resolvable, so
# the name states the mechanism: store values (with a grow / reallocate, atomically updating a counter). The "2"
# distinguishes it from StoreValueGrow_82A0A040 (the 4-byte version).
[entrypoint.functions."0x8260B008"]
name = "StoreValueGrow2_8260B008"

# A copy-with-refcount-increments (the sibling of CopyRefcountedArray_827F2010): loop (count times), copy 8-byte
# values (src->0 and src->4 -> dst), and (if the value's refcount, src->4, is non-zero) atomically increment it (an
# lwarx/stwcx loop under the global lock).

# Args: (r3), dst (r4, a write pointer), count (r5), src (r6, ->0 and ->4 = a value with a refcount), (more). Loops
# (count times): it copies an 8-byte value (src->0 -> dst->0 and src->4 -> dst->4), and advances the write pointer
# (dst += 8). If the value's refcount (src->4) is non-zero, it atomically increments it (an lwarx/stwcx loop under
# the global lock, with mfmsr/mtmsrd to disable interrupts). If the write pointer (dst) is null, it breaks (a
# bounds check). So it's a "copy 8-byte values (with a refcount increment: atomically increment the refcount of each
# copied value)" routine (a copy with refcount increments, part of the 0x829FA cluster). 69 lines, no direct callees
# (the atomic increment is inline, which the callee scan does not count). 5 callers (sub_8260B008, sub_827F1AB0, sub
# _82B63FC0, sub_824E02A8, sub_82925A20). The value's class (an 8-byte value, ->0 = a pointer, ->4 = a refcount)
# and the source/destination's meaning (arrays of refcounted values) aren't resolvable, so the name states the
# mechanism: copy values (with refcount increments).
[entrypoint.functions."0x829FA6A0"]
name = "CopyRefcountedValues_829FA6A0"

# A floating-point computation (a periodic / wrapped value): compute a wrapped value from a float (fabs, an integer
# conversion (fctidz/fcfid), the fractional part, and a series of fsel selects, against two float constants).

# Args: (r3), (r4), value (f1), (more). Computes a wrapped value from the input (value, in f1): it converts the value
# to an integer and back (fctidz/fcfid, an integer part / floor), computes the absolute value (fabs), the fractional
# part (value - floor), and two constants (loaded from fixed .data addresses, 0x82...3248 and 0x82...3288). It then
# applies a series of fsel (floating-point select) operations (picking a result based on the sign of intermediate
# values), and returns the result in f1. So it's a "compute a wrapped value (a periodic / wrapped floating-point
# value, using fabs, the fractional part, and selects against two constants)" routine (a floating-point math helper,
# part of the 0x8222C cluster). 35 lines, no callees (pure floating-point arithmetic). Many callers (sub_8249A300,
# sub_82F11B70, sub_82F7F4A8, ... -- many in the 0x82F/0x830 rendering / math region). The exact computation (the two
# float constants at 0x82...3248 and 0x82...3288, and the wrap period) aren't resolvable from the code alone, so the
# name states the mechanism: compute a wrapped value (a periodic / wrapped float).
[entrypoint.functions."0x8222C3E8"]
name = "ComputeWrapped_8222C3E8"

# A call-and-free: call sub_83232128 (with the object and a flag), and (if the flag's low bit is set) free a
# resource (Free_SizeBucketed_Thunk_8221BE68).

# Args: obj (r3), flag (r4), (more). Calls sub_83232128 (a sibling in the 0x83232 cluster, with the object and the
# flag). If the flag's low bit is set (flag & 1), it frees a resource (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68,
# a size-bucketed free). So it's a "call a function (sub_83232128, with the object and a flag) and (if the flag's low
# bit is set) free a resource" routine (a call-and-free, part of the 0x82A13 cluster). 45 lines (to the next function
# ), 2 callees (sub_83232128, Free_SizeBucketed_Thunk_8221BE68). It is registered (in the function table, fable
# _2_init.cpp / fable_2_register.cpp) but has no direct callers in the recompiled code (a registered-but-uncalled
# call-and-free, likely invoked only from code outside the traced region, or unused). The object's class and the
# flag's meaning (a free-after-call flag) aren't resolvable, so the name states the mechanism: call a function (and
# free a resource conditionally).
[entrypoint.functions."0x82A13C50"]
name = "CallAndFree_82A13C50"

# A search-and-assign (the sibling of LookupAndAssign_8323A1E8): call ValidateAndSearch_8323A058 (a validated tree
# search), and (if the found value differs from the object's current value) release the old value (Release_RefCounted
# _829FF648) and assign the new one (obj->0 and obj->4 = the found value).

# Args: obj (r3, ->0 and ->4 = a refcounted value), value (r4), (more). Zeros the object (obj->0 = 0, obj->4 = 0),
# then calls ValidateAndSearch_8323A058 (sub_8323A058, a validated tree search with a trap 22 assertion, with a stack
# buffer and the value). It compares the found value (obj->4, from the search) against the object's current value
# (stack[84]): if they differ, it releases the old value (Release_RefCounted_829FF648, sub_829FF648, a refcounted
# release), and assigns the new value (obj->0 = the found pointer, obj->4 = the found refcount). So it's a "search
# for a value (a validated tree search) and assign it (a refcounted assign: release the old value and assign the new
# one)" routine (a search-and-assign, part of the 0x83225 cluster). 75 lines, 2 callees (ValidateAndSearch_8323A058,
# Release_RefCounted_829FF648). 2 callers (sub_83225E80, sub_83225F18). The object's class (->0 = a refcounted value,
# ->4 = a refcount) and the tree's meaning (a searched tree) aren't resolvable, so the name states the mechanism:
# search for a value (and assign it, a refcounted assign). A sibling of LookupAndAssign_8323A1E8 (a chain-lookup +
# refcounted assign).
[entrypoint.functions."0x83225D80"]
name = "SearchAndAssign_83225D80"

# A process (with a search-and-assign): copy a struct (CopyStructData5_83217EC8), search and assign a refcounted
# value (SearchAndAssign_83225D80), and (depending on the result) release a value (Release_RefCounted_829FF648) and
# call sibling operations (sub_83218B40, sub_823BE9C0).

# Args: obj (r3, ->0 = a refcounted value), value (r4), flag (r5), (more). It copies a struct (CopyStructData5_83217
# EC8, sub_83217EC8, a byte-by-byte variable-length copy, with the value and the flag), then searches and assigns a
# refcounted value (SearchAndAssign_83225D80, sub_83225D80, a validated tree search + refcounted assign, with the
# object and the copied struct). If obj->0 (the assigned value) is non-null, it takes one path; otherwise it releases
# a value (Release_RefCounted_829FF648, sub_829FF648, a refcounted release) and calls sibling operations (sub_83218B
# 40 and sub_823BE9C0). So it's a "process (copy a struct, search and assign a refcounted value, and handle the
# result: release a value and call sibling operations)" routine (a process with a search-and-assign, part of the
# 0x83225 cluster). 91 lines, 5 callees (CopyStructData5_83217EC8, SearchAndAssign_83225D80, Release_RefCounted_829
# FF648, sub_83218B40, sub_823BE9C0). 1 caller: sub_83232200. The object's class (->0 = a refcounted value) and the
# value's meaning (a searched tree) aren't resolvable, so the name states the mechanism: process (copy a struct,
# search and assign a refcounted value, and handle the result).
[entrypoint.functions."0x83225E80"]
name = "ProcessSearchAndAssign_83225E80"

# A virtual call (the sibling of VirtualCall_826C0388): load a function pointer from a vtable (obj->20 -> ->20) and
# call it (bctr), passing the object (r3 = obj->20, the vtable-derived this) and the other arguments.

# Args: obj (r3, ->20 = a vtable), (more). Loads the vtable (obj->20), loads a function pointer from it (vtable->20),
# sets the count register (ctr = the function pointer), and calls it (bctr -- a virtual call). It passes the object
# (r3 = obj->20, the vtable-derived this) and the other arguments. So it's a "virtual call (dispatch via a vtable,
# calling a virtual method at vtable->20)" routine (a virtual-call thunk, part of the 0x83231 cluster). 4 lines (to
# the next function), no callees (a single indirect call). It is registered (in the function table, fable_2_init.cpp
# / fable_2_register.cpp) but has no direct callers in the recompiled code (a registered-but-uncalled virtual-call
# thunk, likely invoked only from code outside the traced region, or unused). The vtable's meaning (a class, a method
# at vtable->20) isn't resolvable, so the name states the mechanism: a virtual call (dispatch via a vtable). The "2"
# distinguishes it from VirtualCall_826C0388.
[entrypoint.functions."0x83231E88"]
name = "VirtualCall2_83231E88"

# A destructor (a tree owner): set a type pointer (obj->0 = a constant), destroy the tree nodes (DestroyTreeNodes_82
# 859340), zero the object's fields (obj->12->4, obj->16, and the circular self-references at obj->12->0 and ->8),
# and release the refcounted value (Release_RefCounted_829FF648, with a dispose via Dispose2_8233DD70).

# Args: obj (r3, ->0 = a type pointer, ->12 = a tree, ->16, ->20 = a refcounted value), (more). Sets the type pointer
# (obj->0 = a constant, a vtable / type pointer at a fixed .data address). It then destroys the tree nodes (Destroy
# TreeNodes_82859340, sub_82859340, with the tree's root). It zeros the object's fields (obj->12->4 = 0, obj->16 =
# 0, and the circular self-references at obj->12->0 and obj->12->8), and releases the refcounted value (Release_Ref
# Counted_829FF648, sub_829FF648, a refcounted release, with the object+20). It also disposes a value (Dispose2_82
# 33DD70, sub_8233DD70, a 20-byte node dispose). So it's a "destructor (set a type pointer, destroy the tree nodes,
# zero the fields, and release the refcounted value)" routine (a destructor for a tree-owning object, part of the
# 0x83232 cluster). 74 lines, 3 callees (DestroyTreeNodes_82859340, Release_RefCounted_829FF648, Dispose2_8233DD70).
# 1 caller: sub_82A13C50 (CallAndFree_82A13C50). The object's class (->0 = a type pointer, ->12 = a tree, ->20 = a
# refcounted value, a tree-owning object) and the tree's meaning aren't resolvable, so the name states the mechanism:
# a destructor (destroys tree nodes and releases a refcounted value).
[entrypoint.functions."0x83232128"]
name = "DestructorTree_83232128"

# A process (with a copy and search): do a virtual call (src->0 -> ->8), and (if src->20 >= 4) copy a struct (byte-by
# byte, from src->12), then search and assign (ProcessSearchAndAssign_83225E80), releasing a value (Release_Ref
# Counted_829FF648).

# Args: obj (r3), src (r4, ->0, ->4, ->12, ->20 = a count), (r5), (more). It does a virtual call (via bctrl, calling
# the virtual method at src->0->8, with src as the this). If src->20 is >= 4 (a count), it copies a struct (byte-by
# byte, from src->12 into a stack buffer, a variable-length copy, calling sub_82A1B480, a copy helper). It then
# searches and assigns a refcounted value (ProcessSearchAndAssign_83225E80, sub_83225E80, a copy + validated tree
# search + refcounted assign). It also releases a value (Release_RefCounted_829FF648, sub_829FF648, a refcounted
# release). So it's a "process (do a virtual call, copy a struct, and search and assign a refcounted value, releasing
# a value)" routine (a process with a copy and search, part of the 0x83232 cluster). 390 lines, 3 callees (Process
# SearchAndAssign_83225E80, Release_RefCounted_829FF648, sub_82A1B480). It is registered (in the function table, fable
# _2_init.cpp / fable_2_register.cpp) but has no direct callers in the recompiled code (a registered-but-uncalled
# process, likely invoked only from code outside the traced region, or unused). The object's class (->0, ->12, ->20
# = a count, a struct with a virtual method and a variable-length payload) and the source's meaning aren't
# resolvable, so the name states the mechanism: process (a virtual call, copy a struct, and search and assign).
[entrypoint.functions."0x83232200"]
name = "ProcessCopyAndSearch_83232200"

# A bubble sort (the sibling of BubbleSortPointers_82C3C...): sort an array (a bubble-sort-like loop, comparing
# adjacent elements and swapping them if they are out of order).

# Args: a (r3, an array), end (r4), (more). If a == end (an empty array), it returns (nothing to sort). Otherwise it
# does a bubble-sort-like sort: it walks the array (from a to end, in 4-byte strides), comparing adjacent elements
# (a->0 and a+4->0, and on a tie, a+4->4 and a->4, a two-key comparison), and swaps them (a bubble-sort swap) if
# they are out of order. It also calls Process2DRegion_82C3C010 (sub_82C3C010, a 2D-region processor, in a path). So
# it's a "sort an array (a bubble sort, comparing adjacent elements by two keys and swapping them if out of order)"
# routine (a bubble sort, part of the 0x82C28 cluster). 135 lines, 1 callee (Process2DRegion_82C3C010). 1 caller: sub
# _82C28D60 (the next function in the TODO). The array's element type (a two-key record, ->0 = a primary key, ->4 = a
# secondary key) and the sort's meaning (a sorted array of records) aren't resolvable, so the name states the
# mechanism: a bubble sort (comparing adjacent elements by two keys and swapping them). The "2" distinguishes it from
# BubbleSortPointers_82C3C... (an earlier bubble sort in the 0x82C3C cluster).
[entrypoint.functions."0x82C28900"]
name = "BubbleSort2_82C28900"

# A quick sort (the caller of BubbleSort2_82C28900): if the array is small ((end - a) >> 2 <= 32 elements), use a
# bubble sort (BubbleSort2_82C28900); otherwise do a quicksort-like partition (a loop, count times, calling sub
# _82C19F30 for a partition, and recursing).

# Args: a (r3, an array), end (r4), count (r5), (r6), (more). If the array is small ((end - a) >> 2 <= 32 elements,
# a 32-element threshold), it uses a bubble sort (BubbleSort2_82C28900, sub_82C28900, a two-key bubble sort). If the
# array is large, it does a quicksort-like partition: it loops (count times, a depth / recursion limit), calling sub
# _82C19F30 (a partition function, with the array, the range, and a stack buffer), and recurses (a recursive call to
# itself, sub_82C28D60). It also calls sub_82C1A2D0 and sub_82C28B48 (siblings in the 0x82C1A/0x82C28 clusters). So
# it's a "sort an array (a quicksort: partition and recurse, with a bubble sort for small arrays)" routine (a quick
# sort, part of the 0x82C28 cluster). 176 lines, 5 callees (sub_82C19F30, BubbleSort2_82C28900, sub_82C1A2D0, sub
# _82C28B48, and a recursive call to itself). 2 callers (sub_82C28EF0, sub_82C28E80). The array's element type (a two
# -key record, a sorted array of records) and the count's meaning (a depth / recursion limit) aren't resolvable, so
# the name states the mechanism: a quicksort (partition and recurse, with a bubble sort for small arrays). The "2"
# distinguishes it from an earlier quicksort (if any).
[entrypoint.functions."0x82C28D60"]
name = "QuickSort2_82C28D60"

# A partition sort (the callee of the QuickSort2 partition function, sub_82C19F30): if the array is large (a 40-
# element threshold), compute a partition size ((count + 1) / 8, with offsets), and sort a partition (SortPointers_82
# C280C0).

# Args: a (r3, an array), end (r4), (r5 = a range end), (r6), (more). If the array is small ((r5 - a) >> 2 <= 40
# elements, a 40-element threshold), it takes one path (a small partition). If the array is large, it computes a
# partition size ((count + 1) / 8, with offsets at 8-byte strides), and sorts a partition (SortPointers_82C280C0, sub
# _82C280C0, a pointer sort, with the array, a partition range, and the offsets). So it's a "sort a partition (compute
# a partition size and sort it, via SortPointers)" routine (a partition sort, part of the 0x82C17 cluster, the callee
# of the QuickSort2 partition function sub_82C19F30). 105 lines, 1 callee (SortPointers_82C280C0). 1 caller: sub
# _82C19F30 (the QuickSort2 partition function, which is called by QuickSort2_82C28D60). The array's element type (a
# pointer array, a sorted array of pointers) and the partition's meaning (a quicksort partition) aren't resolvable,
# so the name states the mechanism: sort a partition (compute a partition size and sort it, via SortPointers).
[entrypoint.functions."0x82C170D0"]
name = "SortPartition_82C170D0"

# A quicksort partition (the callee of QuickSort2_82C28D60): compute a midpoint, sort a partition (SortPartition_82
# C170D0), and do a 3-way partition (a loop, comparing and moving elements around a pivot).

# Args: a (r3, an array), start (r4), end (r5), (r6), (more). It computes a midpoint ((end - start) / 4 / 2, a
# midpoint offset, with 8-byte strides), and sorts a partition (SortPartition_82C170D0, sub_82C170D0, a partition sort
# via SortPointers_82C280C0, with the array, the start, the midpoint, and the offsets). It then does a 3-way
# partition: it loops (comparing elements against a pivot, and moving elements to the left, right, or keeping them in
# the middle, a 3-way quicksort partition). So it's a "partition (compute a midpoint, sort a partition, and do a 3-way
# quicksort partition)" routine (a quicksort partition, part of the 0x82C19 cluster, the callee of QuickSort2_82C28D
# 60). 398 lines, 1 callee (SortPartition_82C170D0). 1 caller: sub_82C28D60 (QuickSort2_82C28D60, the quicksort that
# calls this partition function). The array's element type (a pointer / two-key record array) and the pivot's meaning
# aren't resolvable, so the name states the mechanism: a quicksort partition (compute a midpoint, sort a partition, and
# do a 3-way partition). The "2" distinguishes it from an earlier partition function (if any).
[entrypoint.functions."0x82C19F30"]
name = "Partition2_82C19F30"

# A validate-and-dispatch: validate (a trap 22 debug assertion: obj->4 <= obj->8, checked twice), build a stack
# struct (obj, obj->8, obj->4), and dispatch (sub_82BFA510).

# Args: obj (r3, ->4, ->8), (more). Validates: it checks that obj->4 <= obj->8 (a debug assertion, a trap 22 (ppc_trap
# (ctx, base, 22)) if the assertion fails -- a bounds / index check). It builds a stack struct (obj, obj->8, obj->4,
# saved at stack[80], stack[88], and stack[92]), re-validates (another trap 22), and dispatches: it calls sub_82BFA5
# 10 (a sibling in the 0x82BFA cluster, with the stack struct). So it's a "validate (a trap 22 debug assertion: obj->
# 4 <= obj->8) and dispatch (a call with a stack struct)" routine (a validated dispatch, part of the 0x8236F cluster).
# 58 lines, 1 callee (sub_82BFA510). 4 callers (sub_825C4EE8, sub_823722E0, sub_8236ED00, sub_82B1BF08). The object's
# class (->4 = an index, ->8 = a count / capacity, an object with a bounds check) and the dispatch's meaning (a
# particular operation) aren't resolvable, so the name states the mechanism: validate (a trap 22 assertion) and
# dispatch (a call). A sibling of ValidateAndSearch_8323A058 (a validated tree search with a trap 22 assertion).
[entrypoint.functions."0x8236FC88"]
name = "ValidateAndDispatch_8236FC88"

# A store-with-grow (the sibling of StoreValueGrow_82A0A040 / StoreValueGrow2_8260B008): compute a count ((obj->12 -
# obj->4) >> 2), store values (a value, into a 4-byte-stride array), and (on a grow / full path) reallocate (Construct
# Array4_823093F8) and copy the data (CheckedCopy_82CA3808), freeing the old buffer (Free_SizeBucketed_Thunk_8221BE68
# ).

# Args: obj (r3, ->4 = a base, ->8 = a write pointer, ->12 = an end), (r4), count (r5), value (r6, ->0 = a value),
# (more). Computes the count ((obj->12 - obj->4) >> 2, a count of 4-byte elements). It stores values (a value, into
# the 4-byte-stride array, advancing the write pointer). On a grow / full path (when there is no room), it
# reallocates (ConstructArray4_823093F8, sub_823093F8, a 4-byte array constructor) and copies the data (CheckedCopy
# _82CA3808, sub_82CA3808), freeing the old buffer (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68). It also calls
# sub_82684B38 (a sibling in the 0x82684 cluster). So it's a "store values into a 4-byte-stride array (with a grow /
# reallocate path: allocate a larger buffer and copy the data)" routine (a store with a grow, part of the 0x82370
# cluster). 443 lines, 4 callees (ConstructArray4_823093F8, CheckedCopy_82CA3808, Free_SizeBucketed_Thunk_8221BE68,
# sub_82684B38). 2 callers (sub_8236FBA8, sub_823723F0). The buffer's class (->4 = a base, ->8 = a write pointer,
# ->12 = an end, a 4-byte-stride array) and the values' meaning aren't resolvable, so the name states the mechanism:
# store values (with a grow / reallocate). The "3" distinguishes it from StoreValueGrow_82A0A040 and StoreValueGrow2
# _8260B008.
[entrypoint.functions."0x82370688"]
name = "StoreValueGrow3_82370688"

# A grow-and-copy (the callee of SetValue2_8236EC48): compute a count ((b - c) >> 2, a count of 4-byte elements), and
# (if non-zero) copy the data (CheckedCopy_82CA3808, a size-checked copy) to the new location.

# Args: obj (r3), a (r4, a source), b (r5, a write pointer), c (r6, a base), (more). Computes the count ((b - c) >> 2
# , a count of 4-byte elements). If the count is non-zero, it copies the data (CheckedCopy_82CA3808, sub_82CA3808, a
# size-checked copy, with the base, the count * 4, and the new location). So it's a "grow (compute a count) and copy
# the data (a size-checked copy to the new location)" routine (a grow-and-copy, part of the 0x82371 cluster, the
# callee of SetValue2_8236EC48 -- the grow path that copies the data to a new, larger buffer). 55 lines, 1 callee
# (CheckedCopy_82CA3808). 1 caller: SetValue2_8236EC48 (sub_8236EC48, a value setter, which calls this to copy the
# data on a grow). The buffer's class (a 4-byte-stride array, a source and a base) and the copy's meaning (a grow /
# reallocate copy) aren't resolvable, so the name states the mechanism: grow (compute a count) and copy the data (a
# size-checked copy).
[entrypoint.functions."0x82371378"]
name = "GrowAndCopy_82371378"

# A process (with a validate-and-dispatch and a vcall): call ValidateAndDispatch_8236FC88 (a validated dispatch), and
# (if obj->4 != 0) do a vcall (SubObject_VcallSlot3_GatedStore32_82C842C8) and store the result (obj->4 = the result);
# (if the dispatch's result is non-null) process (a bit check on obj->5).

# Args: (r3), obj (r4, ->4 = a flag, ->5 = a bit field), value (r5), (more). It calls ValidateAndDispatch_8236FC88
# (sub_8236FC88, a validated dispatch with a trap 22 assertion, with the value). If obj->4 (a flag) is non-zero, it
# does a vcall (SubObject_VcallSlot3_GatedStore32_82C842C8, sub_82C842C8, a virtual call with a gated 32-bit store,
# with the object and a stack buffer), and stores the result (obj->4 = the result). If the dispatch's result (stack
# [80]) is non-null, it processes: it checks a bit (obj->5, a bit field, with a count-leading-zeros (cntlzw) and a bit
# test), and calls sibling operations (sub_8236EED8 and sub_823723F0). So it's a "process (a validate-and-dispatch, a
# vcall (a gated store), and a bit check)" routine (a process with a validate-and-dispatch and a vcall, part of the
# 0x82372 cluster). 159 lines, 4 callees (ValidateAndDispatch_8236FC88, SubObject_VcallSlot3_GatedStore32_82C842C8,
# sub_8236EED8, sub_823723F0). 2 callers (sub_82C67878, sub_82371958). The object's class (->4 = a flag, ->5 = a bit
# field, an object with a gated store and a bit field) and the value's meaning aren't resolvable, so the name states
# the mechanism: process (a validate-and-dispatch, a vcall, and a bit check).
[entrypoint.functions."0x823722E0"]
name = "ProcessDispatchAndVcall_823722E0"

# A value store (the sibling of SetValue3_82A09FB0 / SetValue4_829FD7B8, with an index and a grow): compute a count
# ((obj->8 - obj->4) >> 2) and an offset ((obj->4 - obj->8) >> 2), and store a value (at the offset, into a 4-byte-
# stride array), with a grow (StoreValueGrow3_82370688) on a full path.

# Args: obj (r3, ->4, ->8 = a 4-byte-stride array), index (r4), value (r5), (more). Computes the count ((obj->8 -
# obj->4) >> 2, a count of 4-byte elements). If the count is less than the index (out of range), it takes a grow /
# error path. Otherwise it computes an offset ((obj->4 - obj->8) >> 2) and stores the value (at the offset, into the
# 4-byte-stride array). On a grow / full path, it calls StoreValueGrow3_82370688 (sub_82370688, a store with a grow,
# which reallocates and copies the data). It also calls sub_82370A48 and sub_82BFA510 (siblings in the 0x82370/0x82BFA
# clusters). So it's a "store a value (with an index, at an offset, into a 4-byte-stride array, with a grow)" routine
# (a value setter for a growing array, part of the 0x82372 cluster). 164 lines, 3 callees (StoreValueGrow3_82370688,
# sub_82370A48, sub_82BFA510). 2 callers: sub_823722E0 (ProcessDispatchAndVcall_823722E0), sub_827BFCD8. The
# buffer's class (->4, ->8 = a 4-byte-stride array) and the value's meaning aren't resolvable, so the name states the
# mechanism: store a value (with an index, at an offset, with a grow). The "5" distinguishes it from SetValue3_82A09
# FB0 (4 bytes) and SetValue4_829FD7B8 (8 bytes).
[entrypoint.functions."0x823723F0"]
name = "StoreValue5_823723F0"

# A validate-and-copy: validate (a trap 22 debug assertion: count == 0 or count == index), and (if the source differs
# from the destination) copy the data (CheckedCopy_82CA3808, a size-checked copy).

# Args: a (r3, a source), obj (r4, ->8), count (r5), index (r6), (more). Validates: it checks that count is 0, or
# count == index (a debug assertion, a trap 22 (ppc_trap (ctx, base, 22)) if the assertion fails -- a bounds / index
# check). If the source (a) differs from the destination (r5), it computes a count ((obj->8 - r5) >> 2, a count of
# 4-byte elements), and copies the data (CheckedCopy_82CA3808, sub_82CA3808, a size-checked copy, with the source, the
# count * 4, and the destination). So it's a "validate (a trap 22 debug assertion: count == 0 or count == index) and
# copy the data (a size-checked copy)" routine (a validated copy, part of the 0x82BFA cluster). 80 lines, 1 callee
# (CheckedCopy_82CA3808). 4 callers (sub_8236FC88, sub_823723F0, sub_82BF96E8, sub_82628158). The source/destination's
# meaning (a 4-byte-stride array, a copy) and the count/index's meaning (a bounds check) aren't resolvable, so the
# name states the mechanism: validate (a trap 22 assertion) and copy the data (a size-checked copy). A sibling of
# ValidateAndDispatch_8236FC88 (a validated dispatch with a trap 22 assertion).
[entrypoint.functions."0x82BFA510"]
name = "ValidateAndCopy_82BFA510"

# An atomic increment (the sibling of AtomicIncrement_822CBEB8 / AtomicIncrement2_82375F28): zero a field (obj->0 =
# 0), and atomically increment a global (an lwarx/stwcx loop under the global lock).

# Args: obj (r3, ->0 = a field), (more). It zeros a field (obj->0 = 0). It then atomically increments a global (a
# fixed .data address, 0x82...28344): it enters the global lock (REX_ENTER_GLOBAL_LOCK, with mfmsr/mtmsrd to disable
# interrupts), does a load-and-reserve / store-conditional (lwarx/stwcx.) to read the global, increment it by 1, and
# write it back (retrying on a store-conditional failure), and leaves the global lock. So it's a "zero a field (obj->0
# = 0) and atomically increment a global (under the global lock)" routine (an atomic increment with a field zeroing,
# part of the 0x821F7 cluster). 41 lines (to the next function), no direct callees (the atomic increment is inline,
# which the callee scan does not count). Many callers (DispatchVcallSequence3_82C1F380, sub_82BFE700, Dispatch_Vcall
# Sequence2_82C1E360, ...). The field's meaning (obj->0 = a field, a reset / cleared flag) and the global's meaning (a
# global counter, a count of an operation) aren't resolvable, so the name states the mechanism: zero a field (and
# atomically increment a global). The "3" distinguishes it from AtomicIncrement_822CBEB8 and AtomicIncrement2_82375F
# 28.
[entrypoint.functions."0x821F7798"]
name = "AtomicIncrement3_821F7798"

# A process (with a dispatch): call DispatchValue_821CF230 (a value dispatch), zero a field (src->6 = 0), and (if the
# result matches a constant) process (a constant check on value->4, and sibling operations sub_82BC9860 / sub_82BCA
# 3E8).

# Args: obj (r3), src (r4, ->6 = a flag), value (r5, ->4 = a value), (more). It calls DispatchValue_821CF230 (sub
# _821CF230, a value dispatch, with the source and the value), and zeros a field (src->6 = 0, a flag / state byte).
# If the dispatch's result matches a constant (a specific value, at a fixed .data address), it processes: it checks a
# constant (value->4, against 0 and 3, a small integer), and calls sibling operations (sub_82BC9860 and sub_82BCA3E8
# , in the 0x82BC9/0x82BCA cluster -- state-dispatch siblings). So it's a "process (a value dispatch, zero a flag,
# and (if the result matches a constant) a constant check and sibling operations)" routine (a process with a dispatch
# , part of the 0x8229A cluster). 91 lines, 3 callees (DispatchValue_821CF230, sub_82BC9860, sub_82BCA3E8). 5 callers
# (sub_82BC9FD0, sub_822A5E98, sub_82BCA3E8, sub_82BE1C40, sub_8229A518). The object's class (->6 = a flag, ->4 = a
# small integer, an object with a dispatch and a flag) and the value's meaning aren't resolvable, so the name states
# the mechanism: process (a value dispatch, zero a flag, and a constant check).
[entrypoint.functions."0x8229A488"]
name = "ProcessDispatchValue_8229A488"

# A list splice (the sibling of List_SpliceNode2_8292CC88): splice / insert a node into a doubly-linked list
# (manipulating the ->4, ->8, and ->33 pointers, with comparisons to place the node correctly).

# Args: list (r3, ->4 = a node), node (r4, ->0, ->4, ->8, ->33 = a flag), (more). It splices / inserts the node into a
# doubly-linked list: it reads the list's nodes (list->4, a node), reads the node's fields (node->0, a next pointer,
# node->4 and node->8, the doubly-linked list pointers, and node->33, a flag byte), and manipulates the pointers
# (with comparisons, placing the node correctly in the list). If a flag (node->33) is zero, it does one splice; if a
# next pointer matches, it does another. So it's a "splice / insert a node into a doubly-linked list (manipulating the
# ->4, ->8, and ->33 pointers)" routine (a list splice, part of the 0x8234D cluster). 67 lines, no callees (pure data
# manipulation). Many callers (sub_82B5FEB0, sub_824DFEA0, sub_8234C9E0, ConstructNodeValue_82B4C1C0, ...). The list's
# element type (a doubly-linked list node, ->4 and ->8 = the next/prev pointers, ->33 = a flag) and the list's
# meaning (a particular list) aren't resolvable, so the name states the mechanism: a list splice (insert a node into a
# doubly-linked list). The "3" distinguishes it from List_SpliceNode2_8292CC88.
[entrypoint.functions."0x8234D600"]
name = "List_SpliceNode3_8234D600"

# A complex computation (a clamped / min-max computation): compute clamped values from the object's fields (obj->0,
# obj->20), a, and b (subtractions, andc, additions, and min/max comparisons), and store the results (out->0 and
# out2->0).

# Args: obj (r3, ->0, ->20), a (r4), b (r5), out (r6, ->0), out2 (r7, ->0), (r8), (more). It computes clamped values
# from the object's fields (obj->0, a count, and obj->20, a base), a, and b: it performs a sequence of subtractions
# (obj->0 - 1, b - obj->0, a + obj->0 - 1), andc (bitwise-and-not, a clamp / mask), additions (obj->20 + obj->0 - 1,
# a + ...), and min/max comparisons (comparing the computed values and storing the min / max to out->0 and out2->0).
# So it's a "compute clamped values (a complex min-max computation, with subtractions, andc, and additions) and store
# the results" routine (a clamped computation, part of the 0x83230 cluster). 70 lines, no callees (pure integer
# arithmetic). 2 callers (sub_83230C88, sub_83230D00). The object's class (->0 = a count, ->20 = a base, an object
# with a count and a base) and the values' meaning (clamped / min-max values, an index / range computation) aren't
# resolvable, so the name states the mechanism: a clamped computation (min-max, with subtractions, andc, and
# additions).
[entrypoint.functions."0x83230A48"]
name = "ComputeClamped_83230A48"

# A 16-byte object constructor: if the source (src) is non-null and *src is non-zero, allocate a 16-byte object
# (Allocate_OrDbgBreak_8221F388), zero it (result->0, ->4, ->8 = 0), and process (a loop, with a reassign via Reassign
# Buffer_822088B0 and a release via Release_RefCounted_821C67D8).

# Args: obj (r3), src (r4, ->0 = a count), (more). If src is null, or *src (a count) is zero, it takes an early path
# (no allocation). Otherwise it allocates a 16-byte object (Allocate_OrDbgBreak_8221F388, sub_8221F388), zeros it
# (result->0, result->4, result->8 = 0), and processes (a loop, over the source, with a reassign via ReassignBuffer
# _822088B0, sub_822088B0, a buffer reassign, and a release via Release_RefCounted_821C67D8, sub_821C67D8, a
# refcounted release). So it's a "construct a 16-byte object (allocate it, zero it, and process a source, with a
# reassign and a release)" routine (a 16-byte object constructor, part of the 0x82275 cluster). 105 lines, 3 callees
# (Allocate_OrDbgBreak_8221F388, ReassignBuffer_822088B0, Release_RefCounted_821C67D8). Many callers (sub_822BA5F8,
# sub_82341D48, sub_82991220, ...). The object's class (a 16-byte object, ->0, ->4, ->8 = three 32-bit fields) and the
# source's meaning (a count, a source to process) aren't resolvable, so the name states the mechanism: construct a 16-
# byte object (allocate it, zero it, and process a source).
[entrypoint.functions."0x82275368"]
name = "Construct16Byte_82275368"

# A state dispatch (the sibling of StateDispatch_82BCA7D8 / StateDispatch2_82BCAA58): call SwitchDispatch_82BC9B28
# (a switch dispatch), and (depending on the result) process (a loop, decrementing value->20, with a mutual recursion
# via ProcessDispatchValue_8229A488).

# Args: obj (r3), value (r4, ->16, ->20), count (r5), (more). It calls SwitchDispatch_82BC9B28 (sub_82BC9B28, a switch
# dispatch, with the value and the count). If the result's ->4 is 0 and the result matches a constant (a specific
# value, at a fixed .data address), it takes one path. Otherwise, if value->20 > value->16 (a range check), it loops
# (decrementing value->20 by 20, a 20-byte stride). It also calls sibling operations (sub_82BCA180, in the 0x82BCA
# cluster) and has a mutual recursion with ProcessDispatchValue_8229A488 (sub_8229A488 -- they call each other). So
# it's a "process (a switch dispatch, and (depending on the result) a loop (decrementing value->20) and sibling
# operations)" routine (a state dispatch, part of the 0x82BCA cluster). 233 lines, 3 callees (SwitchDispatch_82BC9B28,
# ProcessDispatchValue_8229A488, sub_82BCA180). 6 callers (sub_82BCCFF8, ProcessDispatchValue_8229A488, sub_82BCA588,
# sub_822DF280, sub_82BC9FD0, sub_82BCA5F8). The object's class (->16, ->20 = a range, an object with a switch dispatch
# and a range) and the value's meaning (a state / event) aren't resolvable, so the name states the mechanism: a state
# dispatch (a switch dispatch, and a loop (decrementing value->20)). The "3" distinguishes it from StateDispatch_82
# BCA7D8 and StateDispatch2_82BCAA58.
[entrypoint.functions."0x82BCA3E8"]
name = "StateDispatch3_82BCA3E8"

# A constant stub: return the constant 8192 (0x2000, a power of 2 -- a capacity / size / max).

# Args: (none). Returns the constant 8192 (li r3, 8192; blr). 2 lines (to the next function), no callees (a constant
# return). A sibling of the ReturnConstant* constant stubs (e.g., ReturnConstant2/8/16/64/128/256/512/0x8000) and a
# likely virtual "capacity / size / max" getter (a method that returns a fixed constant, 8192). It is registered (in
# the function table, fable_2_init.cpp / fable_2_register.cpp) but has no direct callers in the recompiled code (a
# registered-but-uncalled constant stub, likely invoked only from code outside the traced region, or unused). The
# constant's meaning (8192, a capacity / size / max -- a power of 2, a buffer size / block count / max count) isn't
# resolvable from the code alone, so the name states the mechanism: return the constant 8192. The "8192" distinguishes
# it from the other ReturnConstant* stubs.
[entrypoint.functions."0x82A145F8"]
name = "ReturnConstant8192_82A145F8"

# A pointer getter (a branch on a field): if (obj->24 >= 8), return (obj->4); otherwise return (obj + 4).

# Args: obj (r3, ->4 = a pointer, ->24 = a flag / count), (more). Branches on obj->24: if it is >= 8 (a threshold), it
# returns (obj->4, a pointer field); otherwise it returns (obj + 4, a pointer to a fixed offset within the object). So
# it's a "get a pointer (branch on a field: obj->24 >= 8 -> obj->4, else obj + 4)" routine (a pointer getter, part of
# the 0x822F5 cluster). 19 lines (to the next function), no callees (pure data access). 2 callers (sub_82EE2A48, sub
# _822F6480). The object's class (->4 = a pointer, ->24 = a flag / count, an object with a pointer field and a flag)
# and the pointer's meaning (a sub-object / value / a fixed offset) aren't resolvable, so the name states the
# mechanism: get a pointer (branch on a field).
[entrypoint.functions."0x822F5458"]
name = "GetPointer_822F5458"

# A virtual call (the sibling of VirtualCall_826C0388 / VirtualCall2_83231E88): load a function pointer from a vtable
# (obj->0 -> ->60) and call it (bctr), passing the object (the vtable-derived this) and the other arguments.

# Args: obj (r3, ->0 = a vtable), (more). Loads the vtable (obj->0), loads a function pointer from it (vtable->60),
# sets the count register (ctr = the function pointer), and calls it (bctr -- a virtual call). It passes the object
# (the vtable-derived this) and the other arguments. So it's a "virtual call (dispatch via a vtable, calling a virtual
# method at vtable->60)" routine (a virtual-call thunk, part of the 0x82214 cluster). 4 lines (to the next function),
# no direct callees (a single indirect call via bctr, which the callee scan does not count). 1 caller: sub_82C14D70.
# The vtable's meaning (a class, a method at vtable->60) isn't resolvable, so the name states the mechanism: a virtual
# call (dispatch via a vtable). The "3" distinguishes it from VirtualCall_826C0388 and VirtualCall2_83231E88.
[entrypoint.functions."0x822142C0"]
name = "VirtualCall3_822142C0"

# A process (with a virtual call and VirtualCallPair): do a virtual call (obj->0 -> ->32, with a stack buffer), and
# (if the result's low byte is non-zero) call VirtualCallPair_82C01D60 (a double-virtual-call) and process (a
# comparison, with allocations via Allocate_OrDbgBreak_8221F388 / Construct16Byte_82275368 and frees via Free_Thunk
# _824FE010).

# Args: obj (r3, ->0 = a vtable), value (r4), (more). It does a virtual call (via bctrl, calling the virtual method at
# obj->0->32, with a stack buffer). If the result's low byte is non-zero (a flag / state), it calls VirtualCallPair_8
# 2C01D60 (sub_82C01D60, a double-virtual-call, with the object) and processes: it compares the stack buffer's value
# against the result, and (if a condition holds) calls VirtualCallPair_82C01D60 again, and processes further (with
# allocations via Allocate_OrDbgBreak_8221F388, sub_8221F388, and Construct16Byte_82275368, sub_82275368, a 16-byte
# object constructor, and frees via Free_Thunk_824FE010, sub_824FE010). It also calls sub_82BFF7D8 and sub_82BFF848
# (siblings in the 0x82BFF cluster). So it's a "process (a virtual call, and (if a flag) VirtualCallPair and further
# processing (allocations and frees))" routine (a process with a virtual call, part of the 0x82C02 cluster). 204 lines
# , 6 callees (VirtualCallPair_82C01D60, Allocate_OrDbgBreak_8221F388, Construct16Byte_82275368, Free_Thunk_824FE010
# , sub_82BFF7D8, sub_82BFF848). It is registered (in the function table, fable_2_init.cpp / fable_2_register.cpp) but
# has no direct callers in the recompiled code (a registered-but-uncalled process, likely invoked only from code
# outside the traced region, or unused). The object's class (->0 = a vtable, an object with a virtual method at
# vtable->32) and the value's meaning aren't resolvable, so the name states the mechanism: process (a virtual call, and
# (if a flag) VirtualCallPair and further processing).
[entrypoint.functions."0x82C02250"]
name = "ProcessVirtualCall_82C02250"

# A process (with a virtual-call sequence): do a virtual call (obj->0 -> ->24), and (if the result's low byte is non-
# zero) call VirtualCall3_822142C0 (a virtual call, with the object and a value), and (if the result's low byte is non
# -zero again) do another virtual call (obj->0 -> ->28).

# Args: obj (r3, ->0 = a vtable), (r4), value (r5), (more). It does a virtual call (via bctrl, calling the virtual
# method at obj->0->24, with the object). If the result's low byte is non-zero (a flag / state), it calls VirtualCall3
# _822142C0 (sub_822142C0, a virtual call, with the object and the value). If the result's low byte is again non-zero,
# it does another virtual call (via bctrl, calling the virtual method at obj->0->28, with the object). So it's a
# "process (a virtual-call sequence: call obj->0->24, and (if a flag) VirtualCall3 and another virtual call (obj->0->
# 28))" routine (a process with a virtual-call sequence, part of the 0x82C14 cluster). 86 lines, 1 callee (VirtualCall
# 3_822142C0). Many callers (Dispatch_VcallSequence_82C15100, Dispatch_VcallSequence2_82C1E360, sub_82C30C30, ... --
# many in the 0x82C1/0x82C3 region). The object's class (->0 = a vtable, an object with virtual methods at vtable->24
# and vtable->28) and the value's meaning aren't resolvable, so the name states the mechanism: a process (a virtual-
# call sequence, calling obj->0->24, VirtualCall3, and obj->0->28). The "3" distinguishes it from an earlier virtual-
# call sequence process (if any).
[entrypoint.functions."0x82C14D70"]
name = "ProcessVcallSequence3_82C14D70"

# A copy loop (the sibling of the CopyStructData family): loop (count times), copying 32-byte records (8 words, a
# 32-byte stride) from src to dst (advancing both pointers by 32 bytes each iteration).

# Args: (r3), dst (r4, a write pointer), count (r5), src (r6, a read pointer), (more). If count is 0, it takes an early
# path (no copy). Otherwise it loops (count times): it copies a 32-byte record (8 words, from src to dst -- src->0,
# src->4, ..., src->32 -> dst->-24, dst->-20, ..., dst->0, dst->4, dst->8, a 32-byte record, including a float at
# src->32), and advances both pointers (dst += 32, src += 32). So it's a "copy 32-byte records (a loop, copying 8-word
# records from src to dst, advancing both pointers)" routine (a copy loop, part of the 0x82A13 cluster, a sibling of
# the CopyStructData family). 87 lines, no callees (pure data manipulation). 2 callers (sub_82A13D30, sub_8323FF48).
# The record's meaning (a 32-byte record, 8 words, a data record / node / value) and the source/destination's meaning
# (arrays of 32-byte records) aren't resolvable, so the name states the mechanism: copy 32-byte records (a loop,
# copying 8-word records).
[entrypoint.functions."0x82A13CA0"]
name = "CopyRecords_82A13CA0"

# A complex computation (with 128-bit vector ops): compute a value (loading global floats, 128-bit vector operations
# -- vspltisw, vcfux, lvlx, vspltw, stvx128, a SIMD computation), and store the result (a float, to out->0).

# Args: out (r3, ->0 = a float), (more). It computes a value: it loads global floats (from fixed .data addresses,
# 0x82...-27456 and 0x82...-12, constants), and performs a sequence of 128-bit vector operations (vspltisw, vcfux, lvlx
# , vspltw, stvx128 -- a SIMD computation, with a 128-bit load/store and a splat / shuffle). It stores the result (a
# float, to out->0). So it's a "compute a value (a complex SIMD computation, loading global floats and 128-bit vector
# operations) and store the result (a float)" routine (a complex computation with vector ops, part of the 0x83211
# cluster). 173 lines, no direct callees (pure computation, 128-bit vector ops which the callee scan does not count).
# 1 caller: sub_832351B0. The value's meaning (a computed float, a particular math / transformation) and the global
# floats' meaning (constants, a particular computation) aren't resolvable from the code alone, so the name states the
# mechanism: a complex computation (with 128-bit vector ops) and store the result.
[entrypoint.functions."0x832119D8"]
name = "ComputeVector_832119D8"

# A complex float computation (one of the largest routines, ~2020 lines): compute a value (a large sequence of float
# operations, with a 432-byte stack frame and a global flag check -- if a global flag is set, it calls sub_823D2790,
# a setup / initialization function), releasing a refcounted value (Release_RefCounted_829FF648) in a path.

# Args: obj (r3), (r4), a (r5), b (r6), c (r7), value (f1), (more). It checks a global flag (a fixed .data address,
# 0x82...13760): if the flag's low bit is set, it sets the flag (a one-time initialization) and calls sub_823D2790 (a
# setup / initialization function, with a string at a fixed .data address). It then performs a large sequence of float
# operations (a complex computation, with a 432-byte stack frame, saving and restoring f29/f30/f31, and a value (f1,
# a float, in f29)). In a path it releases a refcounted value (Release_RefCounted_829FF648, sub_829FF648, a refcounted
# release) and calls sibling operations (sub_825BF630 and sub_82CA3700, in the 0x825BF/0x82CA3 clusters). So it's a
# "compute a value (a large, complex float computation, with a one-time initialization (a global flag check) and a
# refcounted release)" routine (a complex float computation, part of the 0x83234 cluster). ~2020 lines (at the end of
# the file), 4 callees (Release_RefCounted_829FF648, sub_823D2790, sub_825BF630, sub_82CA3700). 1 caller: sub_82A13220
# . The object's class (a float computation, a particular math / transformation) and the value's meaning (a computed
# float, a particular result) aren't resolvable from the code alone, so the name states the mechanism: a complex float
# computation (with a one-time initialization and a refcounted release).
[entrypoint.functions."0x83234418"]
name = "ProcessFloat_83234418"

# A size-checked allocation (the sibling of the AllocateChecked family, with a 40x multiplier): check the size (an
# overflow check: size != 0 and -1 / size < 40), compute the allocation size (size * 40, via (size * 5) << 3), and
# allocate (Allocate_OrDbgBreak_8221F388); raise an error (sub_822F1F00) on a failure path.

# Args: (r3), size (r4), (more). Checks the size: if size is 0, or -1 / size >= 40 (an overflow check -- the
# allocation size would overflow), it takes an error path. Otherwise it computes the allocation size ((size * 5) << 3
# = size * 40) and allocates it (Allocate_OrDbgBreak_8221F388, sub_8221F388). On a failure path it calls sub_822F1F00
# (an error / setup function, with a string at a fixed .data address). So it's a "check the size (an overflow check),
# compute the allocation size (size * 40), and allocate (with an error on failure)" routine (a size-checked allocation
# of 40-byte elements). 70 lines, 2 callees (Allocate_OrDbgBreak_8221F388, sub_822F1F00). 4 callers (sub_829175B8,
# sub_829184D8, sub_8246F6B8, sub_82A13D30). The 40-byte element's meaning (a record / node) and the size's source
# aren't resolvable, so the name states the mechanism: check the size and allocate (size * 40). The "5" distinguishes
# it from AllocateChecked_8264EFE0 (12x), AllocateChecked2_8217A488 (2x), AllocateChecked3_82333F78 (4x), and
# AllocateChecked4_82A87220 (8x).
[entrypoint.functions."0x824709C0"]
name = "AllocateChecked5_824709C0"

# A process (with a copy of records): copy a 32-byte record (from src to a stack buffer), and (depending on obj->4)
# process (copy records via CopyRecords_82A13CA0 / CopyArray24_82A13FE0, grow via AllocateChecked5_824709C0, and free
# via Free_SizeBucketed_Thunk_8221BE68).

# Args: obj (r3, ->4 = a count), (r4), src (r6, a 32-byte record: ->0, ->4, ->8, ->12, ->16, ->24, ->32, ->36), (more)
# . It copies a 32-byte record (from src to a stack buffer, at stack[80], copying src->0, src->4, src->8, src->12, src
# ->16, src->24, src->32, src->36, a 32-byte record, including two floats). If obj->4 (a count) is 0, it takes one
# path (an empty state). Otherwise it processes: it copies records (CopyRecords_82A13CA0, sub_82A13CA0, a copy loop
# for 32-byte records, and CopyArray24_82A13FE0, sub_82A13FE0, a 24-byte array copy), grows (AllocateChecked5_824709C
# 0, sub_824709C0, a size-checked allocation of 40-byte elements), and frees (Free_SizeBucketed_Thunk_8221BE68, sub
# _8221BE68, a size-bucketed free). It also calls sub_82A97648 (a sibling in the 0x82A97 cluster). So it's a "process
# (copy a 32-byte record, and (depending on the count) copy records, grow, and free)" routine (a process with a copy
# of records, part of the 0x82A13 cluster). 391 lines, 5 callees (CopyRecords_82A13CA0, CopyArray24_82A13FE0, Allocat
# eChecked5_824709C0, Free_SizeBucketed_Thunk_8221BE68, sub_82A97648). 1 caller: sub_8323FF48. The object's class
# (->4 = a count, an object with a count) and the record's meaning (a 32-byte record, a data record / node / value)
# aren't resolvable, so the name states the mechanism: process (copy a record, and (depending on the count) copy
# records, grow, and free).
[entrypoint.functions."0x82A13D30"]
name = "ProcessCopyRecords_82A13D30"

# A copy loop (the sibling of CopyRecords_82A13CA0 / CopyArray24_82A13FE0): loop (while a != b), copying 20-byte
# records (a word, two floats, and four bytes) from a to dst (advancing both pointers by 20 bytes each iteration).

# Args: (r3), a (r4, a read pointer), b (r5, an end), dst (r6, a write pointer), (more). If a == b (an empty range),
# it returns (nothing to copy). Otherwise it loops (while a != b): it copies a 20-byte record (from a to dst -- a
# word (a->-8), two floats (a->-4 and a->0, stored with a scaled index (stfsx)), and four bytes (a->8, a->9, a->10,
# a->11), a 20-byte record), and advances both pointers (a += 20, dst += 20). So it's a "copy 20-byte records (a
# loop, copying a word, two floats, and four bytes from a to dst, advancing both pointers)" routine (a copy loop,
# part of the 0x82A13 cluster, a sibling of CopyRecords_82A13CA0 (32-byte) and CopyArray24_82A13FE0 (24-byte)). 76
# lines, no callees (pure data manipulation). 1 caller: sub_82A12EC0. The record's meaning (a 20-byte record, a word,
# two floats, and four bytes, a data record / node / value) and the source/destination's meaning (arrays of 20-byte
# records) aren't resolvable, so the name states the mechanism: copy 20-byte records (a loop, copying a word, two
# floats, and four bytes). The "20" distinguishes it from CopyRecords_82A13CA0 (32-byte) and CopyArray24_82A13FE0
# (24-byte).
[entrypoint.functions."0x82A13138"]
name = "CopyRecords20_82A13138"

# A sorted list insert (the callee of Heap_CarveBlock_83231058): walk a sorted doubly-linked list (a two-key
# comparison: node->0 and node->4 against the list's nodes, following the ->12 next pointers), and insert the node
# (r8->12 = node and node->8 = r8, or at the head if no match).

# Args: list (r3, ->0 = a head), node (r4, ->0 and ->4 = two keys, ->8 and ->12 = the doubly-linked list pointers),
# (more). It walks the sorted doubly-linked list (from the head, following the ->12 next pointers): it compares the
# node's keys (node->0 and node->4, a primary and a secondary key) against the list's nodes (r11->0 and r11->4), and
# walks to the next node (r11->12) until it finds the right place (a node where the node's keys are <= the node's
# keys, or the end). It then inserts the node: if it found a place (r8, the found node), it sets r8->12 = node and
# node->8 = r8 (splicing the node after the found node); otherwise (no match), it inserts at the head (list->0 = node
# and node->8 = r8). So it's a "insert a node into a sorted doubly-linked list (walk the list by a two-key comparison
# and insert the node, or at the head if no match)" routine (a sorted list insert, part of the 0x829FD cluster). 65
# lines, no callees (pure data manipulation). 2 callers: Heap_CarveBlock_83231058 (sub_83231058, a heap block carver)
# , sub_83231240. The list's element type (a sorted doubly-linked list node, ->0 and ->4 = two keys, ->8 and ->12 =
# the next/prev pointers) and the list's meaning (a sorted list of blocks / nodes, a free list / sorted index) aren't
# resolvable, so the name states the mechanism: insert a node into a sorted doubly-linked list (by a two-key
# comparison). The "Sorted" distinguishes it from an unsorted list insert (if any).
[entrypoint.functions."0x829FD158"]
name = "List_InsertSorted_829FD158"

# A float value parse (the callee of ParseNumberValue_82B47AB0): store a constant (out->4 = a constant), and (if
# count >= 4) loop (processing bytes, with a float computation, parsing a floating-point number from a string).

# Args: out (r3, ->4 = a float), (r4, a pointer), count (r5), (more). It stores a constant (out->4 = a constant, a
# float, from a fixed .data address). If count is < 4 (a short value), it takes one path (a small number). If count
# is >= 4, it loops (processing bytes, reading from a string (r4, a pointer), with a float computation -- a
# floating-point number parse, accumulating a value from the bytes, with a loop over the digits, and a float
# conversion). So it's a "parse a floating-point number (store a constant, and (if the value is long) loop over the
# digits, accumulating a float)" routine (a float value parse, part of the 0x82B47 cluster, the callee of ParseNumber
# Value_82B47AB0 -- a number parser). 439 lines, no direct callees (pure computation, float ops and a loop which the
# callee scan does not count). 1 caller: ParseNumberValue_82B47AB0 (sub_82B47AB0, a number parser, which calls this to
# parse a floating-point number). The value's meaning (a floating-point number, a parsed float) and the string's
# meaning (a number string, a decimal number with a fractional part) aren't resolvable, so the name states the
# mechanism: parse a floating-point number (store a constant, and (if the value is long) loop over the digits,
# accumulating a float).
[entrypoint.functions."0x82B47F98"]
name = "ParseFloatValue_82B47F98"

# A bubble sort (the sibling of BubbleSort2_82C28900): compare adjacent elements (a->4 and b->4, a single-key
# comparison), and (if out of order) swap them (copying 12 bytes, a bubble-sort swap).

# Args: a (r3, ->0, ->4, ->8), b (r4, ->0, ->4, ->8), (more). If a == b (the same element), it takes one path. If a->4
# (a key) is > b->4 (a key), it swaps them (a bubble-sort swap): it copies a 12-byte record (a->0, a->4, a->8) to a
# temporary, and copies b's record (b->0, b->4, b->8) to a, and the temporary to b (a 12-byte swap). If a->4 <= b->4
# (in order), it does not swap. So it's a "sort (a bubble sort: compare adjacent elements by a single key (a->4 and
# b->4) and swap them (copying 12 bytes) if out of order)" routine (a bubble sort, part of the 0x82C16 cluster). 95
# lines, 2 callees (sub_82C15CE0, sub_82C21C90 -- siblings in the 0x82C15/0x82C21 clusters). Many callers (sub_82C1
# B180, sub_82C4B0F0, sub_82C33140, ... -- many in the 0x82C1/0x82C2/0x82C3/0x82C4 region). The element's class (a 12
# -byte record, ->0, ->4, ->8, a key at ->4) and the sort's meaning (a sorted array of 12-byte records) aren't
# resolvable, so the name states the mechanism: a bubble sort (compare adjacent elements by a single key and swap
# them (copying 12 bytes) if out of order). The "3" distinguishes it from BubbleSort2_82C28900 (a two-key bubble
# sort).
[entrypoint.functions."0x82C16C78"]
name = "BubbleSort3_82C16C78"

# A chain search (walking a chain by a comparison): load a node (obj->4->4), and (if the node's flag, node->21, is
# zero) walk the chain (comparing node->12 against a value, and following the node's next pointers, node->8 / node->0
# ), searching for a match.

# Args: obj (r3, ->4 = a node), value (r4, ->0 = a value), (more). It loads a node (obj->4->4, a node in a chain). If
# the node's flag (node->21, a byte) is zero, it takes one path (reading a value from r4->0). It then walks the chain
# (a loop): it compares the node's field (node->12, a value) against the target value (r7, from r4->0), and (depending
# on the comparison) follows the node's next pointer (node->8, a next) or the node's head (node->0, a head / prev),
# walking to the next node (node->4->21, a flag). So it's a "search a chain (walk the chain by a comparison, node->12
# against a value, following the next pointers)" routine (a chain search, part of the 0x82B3D cluster). 148 lines, 1
# callee (sub_824AEBB8). Many callers (sub_82670E90, sub_82B3D6F0, sub_83223FB0, sub_8281A568, sub_83223E18, sub
# _82B3D680, sub_83224FD0, sub_825F1D78, sub_83224818). The chain's element type (a node, ->4 = a next, ->8 = a next,
# ->12 = a value, ->21 = a flag, a doubly-linked / circular chain) and the value's meaning (a searched value, a
# particular match) aren't resolvable, so the name states the mechanism: a chain search (walk the chain by a
# comparison, node->12 against a value).
[entrypoint.functions."0x82B3D880"]
name = "ChainSearch_82B3D880"

# A dispose (the sibling of Dispose8_82A12000 / Dispose12_82A14840): call a destructor (sub_83218A40), and (if the
# flag's low bit is set) free the object (Free_SizeBucketed_Thunk_8221BE68).

# Args: obj (r3), flag (r4), (more). It calls a destructor (sub_83218A40, a sibling in the 0x83218 cluster, a 20-byte
# node destructor, with the object). If the flag's low bit is set (flag & 1, a free flag), it frees the object (Free
# _SizeBucketed_Thunk_8221BE68, sub_8221BE68, a size-bucketed free). So it's a "dispose an object (call a destructor,
# and (if a flag is set) free the object)" routine (a dispose, part of the 0x82A12 cluster, a sibling of the Dispose
# family). 45 lines (to the next function), 2 callees (sub_83218A40, Free_SizeBucketed_Thunk_8221BE68). It is
# registered (in the function table, fable_2_init.cpp / fable_2_register.cpp) but has no direct callers in the
# recompiled code (a registered-but-uncalled dispose, likely invoked only from code outside the traced region, or
# unused). The object's class (a 20-byte node, a refcounted object with a destructor) and the flag's meaning (a free
# flag) aren't resolvable, so the name states the mechanism: dispose an object (call a destructor and free
# conditionally). The "13" distinguishes it from the other Dispose* variants (Dispose through Dispose12).
[entrypoint.functions."0x82A12060"]
name = "Dispose13_82A12060"

# A dispose (the sibling of Dispose13_82A12060 / Dispose12_82A14840): destroy a list (DestroyList_Recursive_82443440,
# on obj->4->4), then zero out the object's fields (self-referential: node->4 = node, node->0 = node, node->8 = node
# , and obj->8 = 0).

# Args: obj (r3, ->4 = a node, ->8 = a field), (more). It destroys a list (DestroyList_Recursive_82443440, sub
# _82443440, a recursive list destroy, on obj->4->4 -- the object's nested list). It then zeros out the object's
# fields (self-referential: node->4 = node, node->0 = node, node->8 = node, and obj->8 = 0, a zero -- a cleanup / free
# -marking, so the object's fields point to itself and are zeroed). So it's a "dispose an object (destroy a nested
# list, and zero out the object's fields (self-referential))" routine (a dispose, part of the 0x82A12 cluster, a
# sibling of the Dispose family). 40 lines (to the next function), 1 callee (DestroyList_Recursive_82443440). 1
# caller: sub_82A12580. The object's class (a node, ->4 = a nested list, ->8 = a field, a refcounted object with a
# nested list) and the list's meaning (a nested list, a particular sub-structure) aren't resolvable, so the name
# states the mechanism: dispose an object (destroy a nested list and zero out the fields (self-referential)). The
# "14" distinguishes it from the other Dispose* variants (Dispose through Dispose13).
[entrypoint.functions."0x82A121D8"]
name = "Dispose14_82A121D8"

# A process (with a validation and a detach): validate (child != 0 and child != parent, a trap if invalid), and (if a
# match, r8 == parent->4->0) detach (a chain-root find via FindChainRoot2_823D4F20 and a dispose via Dispose14_82A121
# D8).

# Args: obj (r3, r28), parent (r4, r31, ->4 = a node), child (r5), (r6), (more). It validates: it checks that the
# child is non-zero (child != 0) and that the child is not the parent (child != parent); if either fails, it raises
# an error (a trap, 22). If the match (r8, a value, == parent->4->0, the parent's node's field), it processes: it
# finds a chain root (FindChainRoot2_823D4F20, sub_823D4F20, a chain-root finder, on the parent's node) and disposes
# (Dispose14_82A121D8, sub_82A121D8, a dispose, on the child). It also calls sub_8247E028 (a sibling in the 0x8247E
# cluster). So it's a "process (validate a child, and (if a match) detach it: find a chain root and dispose it)"
# routine (a process with a validation and a detach, part of the 0x82A12 cluster). 137 lines, 3 callees (FindChainRoo
# t2_823D4F20, sub_8247E028, Dispose14_82A121D8). 1 caller: sub_82A12668. The object's class (obj, r28, a node) and
# the parent's class (parent, r31, ->4 = a node, an object with a nested node) and the child's meaning (a child
# object, a node to detach) aren't resolvable, so the name states the mechanism: process (validate a child and (if a
# match) detach it: find a chain root and dispose it).
[entrypoint.functions."0x82A12580"]
name = "ProcessDetach_82A12580"

# A dispose (the sibling of Dispose14_82A121D8 / Dispose13_82A12060): detach (ProcessDetach_82A12580, on the object
# and its fields), free the object's field (Free_SizeBucketed_Thunk_8221BE68, on obj->4), and zero out the fields
# (obj->4 = 0 and obj->8 = 0).

# Args: obj (r3, ->4 = a field, ->8 = a field), (more). It detaches (ProcessDetach_82A12580, sub_82A12580, a process
# with a validation and a detach, on the object and its fields -- the object's nested node). It frees the object's
# field (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a size-bucketed free, on obj->4). It then zeros out the
# object's fields (obj->4 = 0 and obj->8 = 0, a cleanup). So it's a "dispose an object (detach it: validate and
# detach, free the object's field, and zero out the fields)" routine (a dispose, part of the 0x82A12 cluster, a
# sibling of the Dispose family). 50 lines (to the next function), 2 callees (ProcessDetach_82A12580, Free_SizeBucke
# ted_Thunk_8221BE68). 1 caller: sub_83218A40 (a sibling in the 0x83218 cluster, a destructor -- the callee of
# Dispose13_82A12060). The object's class (obj, ->4 = a field, ->8 = a field, a refcounted object with a field) and
# the field's meaning (a field, a particular sub-structure) aren't resolvable, so the name states the mechanism:
# dispose an object (detach it, free the field, and zero out the fields). The "15" distinguishes it from the other
# Dispose* variants (Dispose through Dispose14).
[entrypoint.functions."0x82A12668"]
name = "Dispose15_82A12668"

# A 28-byte constructor (the sibling of Construct20ByteNode_82A12F00 / Construct16Byte_82275368): allocate 28 bytes
# (Allocate_OrDbgBreak_8221F388), zero out the object's fields (->0, ->4, ->8 = 0, and ->24 = 1, ->25 = 0), and store
# the new object into obj->4.

# Args: obj (r3, ->4 = a field), (more). It allocates 28 bytes (Allocate_OrDbgBreak_8221F388, sub_8221F388, a
# size-bucketed allocation, of 28 bytes). It then zeros out the object's fields (->0, ->4, ->8 = 0, three words, and
# ->24 = 1, a one, and ->25 = 0, a zero -- a byte flag). It then stores the new object into obj->4 (obj->4 = the new
# 28-byte object). So it's a "construct a 28-byte object (allocate 28 bytes, zero out the fields (and set ->24 = 1,
# ->25 = 0), and store it into obj->4)" routine (a 28-byte constructor, part of the 0x82A12 cluster). 83 lines, 1
# callee (Allocate_OrDbgBreak_8221F388). Many callers (sub_82893C28, sub_82C340B0, sub_83290210, sub_82C660F8, sub
# _8288E310, sub_82892198, sub_82891B48, sub_8288E478, sub_828919F0, sub_83218AE8 -- many in the 0x8288E/0x82891/0x
# 82892/0x82893 region). The object's class (a 28-byte object, ->0, ->4, ->8 = words, ->24 = a flag, ->25 = a flag,
# an object with a flag) and the parent's class (obj, ->4 = a field, an object with a 28-byte object) aren't
# resolvable, so the name states the mechanism: a 28-byte constructor (allocate 28 bytes and zero out the fields).
# The "28" distinguishes it from the other constructor sizes (Construct16Byte_82275368, Construct20ByteNode_82A12F0
# 0, Construct36Byte_82370EF8, Construct48ByteNode_82A0ED98).
[entrypoint.functions."0x82A12828"]
name = "Construct28Byte_82A12828"

# A dispose (a complex destructor, the sibling of Dispose14_82A121D8 / Dispose13_82A12060): store a constant
# (obj->0 = a constant, a class id / a marker), destroy a list (DestroyList_Recursive_82443440) and a tree
# (DestroyTreeNodes_82859340), zero out the object's fields (self-referential), and dispose nested objects
# (Dispose15_82A12668 and Dispose2_8233DD70).

# Args: obj (r3, ->0 = a field, ->16 = a node, ->20 = a field, ->28 = a field, ->32 = a node), (more). It stores a
# constant (obj->0 = a constant, a class id / a marker, from a fixed .data address -- a cleanup marking). It destroys
# a list (DestroyList_Recursive_82443440, sub_82443440, a recursive list destroy, on obj->16->4 -- the object's nested
# list) and a tree (DestroyTreeNodes_82859340, sub_82859340, a tree-node destroy, on the object's nested tree). It
# zeros out the object's fields (self-referential: node->4 = node, node->0 = node, node->8 = node, and obj->20 = 0,
# a zero). It then disposes nested objects (Dispose15_82A12668, sub_82A12668, a dispose, on the object's nested
# object, and Dispose2_8233DD70, sub_8233DD70, a dispose, on another nested object). So it's a "dispose an object (a
# complex destructor: store a constant, destroy a list and a tree, zero out the fields, and dispose nested objects)"
# routine (a complex destructor, part of the 0x83218 cluster). 95 lines, 4 callees (DestroyList_Recursive_82443440,
# DestroyTreeNodes_82859340, Dispose15_82A12668, Dispose2_8233DD70). 1 caller: Dispose13_82A12060 (sub_82A12060, a
# dispose, which calls this to destroy the object's fields). The object's class (obj, ->0 = a field, ->16 = a node,
# ->20 = a field, ->28 = a field, ->32 = a node, an object with a nested list, a tree, and nested objects) isn't
# resolvable, so the name states the mechanism: a complex destructor (store a constant, destroy a list and a tree,
# zero out the fields, and dispose nested objects). The "16" distinguishes it from the other Dispose* variants
# (Dispose through Dispose15).
[entrypoint.functions."0x83218A40"]
name = "Dispose16_83218A40"

# A node constructor (the sibling of Construct20ByteNode_82A12F00 / Construct28Byte_82A12828): initialize the
# object's fields (obj->4 = a value, obj->8 = 0, obj->0 = a constant), then construct a 28-byte object
# (Construct28Byte_82A12828, on obj+12) and a 20-byte node (ConstructNode20_82A12B08, on obj+28).

# Args: obj (r3, ->0 = a field, ->4 = a value, ->8 = a field, +12, +28), value (r4), (more). It initializes the
# object's fields (obj->4 = a value, obj->8 = 0, a zero, and obj->0 = a constant, a class id / a marker, from a fixed
# .data address). It then constructs a 28-byte object (Construct28Byte_82A12828, sub_82A12828, a 28-byte constructor,
# on obj+12 -- the object's embedded 28-byte object) and a 20-byte node (ConstructNode20_82A12B08, sub_82A12B08, a
# 20-byte node constructor, on obj+28 -- the object's embedded 20-byte node). So it's a "construct a node (initialize
# the fields, then construct an embedded 28-byte object and an embedded 20-byte node)" routine (a node constructor,
# part of the 0x83218 cluster). 40 lines (to the next function), 2 callees (Construct28Byte_82A12828,
# ConstructNode20_82A12B08). 1 caller: sub_83218B40 (a sibling in the 0x83218 cluster). The object's class (obj, ->0
# = a field, ->4 = a value, ->8 = a field, +12 = an embedded 28-byte object, +28 = an embedded 20-byte node, an
# object with embedded objects) and the value's meaning (a value, a particular field) aren't resolvable, so the name
# states the mechanism: construct a node (initialize the fields and construct embedded objects). The "28" refers to
# the 28-byte embedded object.
[entrypoint.functions."0x83218AE8"]
name = "ConstructNode28_83218AE8"

# A node constructor (with a chain find): find a chain root (WalkChainFind_83239FA8), and (depending on the result)
# construct a node (ConstructNode28_83218AE8) or allocate 40 bytes (Allocate_OrDbgBreak_8221F388), wrapping a
# refcounted value (SetRefcountedWrapper_8238A848).

# Args: obj (r3, ->0 = a field, ->4 = a field), value (r4), (more). It finds a chain root (WalkChainFind_83239FA8,
# sub_83239FA8, a chain finder, on the value and a global). If the result's low byte is zero (result & 0xFF == 0, an
# empty / not-found), it takes one path (allocating 40 bytes, Allocate_OrDbgBreak_8221F388, and constructing a node,
# ConstructNode28_83218AE8, sub_83218AE8, a node constructor). Otherwise (a found value), it takes another path
# (releasing a refcounted value, Release_RefCounted_829FF648, sub_829FF648, a refcounted release). It also wraps a
# refcounted value (SetRefcountedWrapper_8238A848, sub_8238A848, a refcounted wrapper) and calls sub_83225E00 (a
# sibling in the 0x83225 cluster). So it's a "construct a node (find a chain root, and (depending on the result)
# construct a new node or wrap a found refcounted value)" routine (a node constructor with a chain find, part of the
# 0x83218 cluster). 137 lines, 6 callees (WalkChainFind_83239FA8, ConstructNode28_83218AE8, Allocate_OrDbgBreak_8221
# F388, Release_RefCounted_829FF648, SetRefcountedWrapper_8238A848, sub_83225E00). 3 callers: ProcessSearchAndAssign
# _83225E80 (sub_83225E80, a search-and-assign process), sub_83225F18, sub_83225FD0 (siblings in the 0x83225 cluster
# ). The object's class (obj, ->0 = a field, ->4 = a field, a refcounted wrapper object) and the value's meaning (a
# value, a particular key) aren't resolvable, so the name states the mechanism: construct a node (find a chain root
# and (depending on the result) construct a new node or wrap a found value).
[entrypoint.functions."0x83218B40"]
name = "ConstructWithChain_83218B40"

# A process (a refcounted assign, the sibling of SearchAndAssign_83225D80 / ProcessSearchAndAssign_83225E80): if
# obj->4 is non-zero, release a refcounted value (Release_RefCounted_829FF648) and increment a counter (obj->4->0 +=
# 1), then look up and assign (LookupAndAssign_8323A1E8).

# Args: obj (r3, ->0 = a value, ->4 = a node), (more). If obj->4 (a node) is non-zero, it releases a refcounted value
# (Release_RefCounted_829FF648, sub_829FF648, a refcounted release, on the node) and increments a counter (obj->4->0
# += 1, a use count). It then looks up and assigns (LookupAndAssign_8323A1E8, sub_8323A1E8, a lookup-and-assign,
# on the value and a global). So it's a "process (release a refcounted value, increment a counter, and look up and
# assign)" routine (a refcounted assign, part of the 0x83225 cluster, a sibling of SearchAndAssign_83225D80). 73
# lines, 2 callees (Release_RefCounted_829FF648, LookupAndAssign_8323A1E8). 1 caller: sub_83218B40 (ConstructWithCh
# ain_83218B40, a node constructor, which calls this to look up and assign). The object's class (obj, ->0 = a value,
# ->4 = a node, a refcounted wrapper object) and the value's meaning (a value, a particular key) aren't resolvable,
# so the name states the mechanism: a refcounted assign (release a refcounted value, increment a counter, and look up
# and assign).
[entrypoint.functions."0x83225E00"]
name = "ProcessRefAssign_83225E00"

# A double virtual call (the sibling of VirtualCallPair_82C01D60): if value->0 is non-zero, do a virtual call (on
# value->0->0->4, a vtable entry); and if obj->0 is non-zero, do another virtual call (on obj->0->0->8, a vtable
# entry).

# Args: obj (r3, ->0 = a node), value (r4, ->0 = a node), (more). If value->0 (a node) is non-zero, it does a virtual
# call (a vtable dispatch: r3 = value->0, r11 = r3->0, r10 = r11->4, a vtable entry, mtctr r10, bctrl -- a virtual
# call on value->0->0->4, vtable->4). If obj->0 (a node) is non-zero, it does another virtual call (a vtable dispatch:
# r3 = obj->0, r11 = r3->0, r10 = r11->8, a vtable entry, mtctr r10, bctrl -- a virtual call on obj->0->0->8,
# vtable->8). So it's a "double virtual call (if value->0 is non-zero, call a virtual method (vtable->4) on it; and if
# obj->0 is non-zero, call another virtual method (vtable->8) on it)" routine (a double virtual call, part of the
# 0x8260C cluster, a sibling of VirtualCallPair_82C01D60). 71 lines, no direct callees (pure virtual calls via
# REX_CALL_INDIRECT_FUNC). Many callers (sub_82B5D3A0, sub_82B68128, sub_82C6DE30, sub_82B43E30, sub_822F7B70, sub
# _8260B9E0, sub_82C630E0, sub_822F7870, sub_82C638C0, sub_82369958 -- many across the codebase). The node's class (a
# node, ->0 = a node, ->4 and ->8 = vtable entries, an object with virtual methods) and the vtable's meaning (a
# virtual method, a particular operation) aren't resolvable, so the name states the mechanism: a double virtual call
# (call a virtual method on two nodes). The "4" distinguishes it from the other VirtualCall* variants (VirtualCall_
# 826C0388, VirtualCall2_83231E88, VirtualCall3_822142C0).
[entrypoint.functions."0x8260C130"]
name = "VirtualCall4_8260C130"

# A tree/list destructor (a recursive destructor, the sibling of DestructorTree_83232128): loop (while node->21 is
# zero), recursively call itself (sub_82367AE8, on obj and node->8), free the node (Free_SizeBucketed_Thunk_8221BE68),
# and walk to the next node (node->0).

# Args: obj (r3, r30), node (r4, r31, ->0 = a next, ->8 = a field, ->21 = a flag), (more). If node->21 (a flag, a
# byte) is non-zero, it takes one path (a leaf / a done node). If node->21 is zero, it loops: it recursively calls
# itself (sub_82367AE8, on obj and node->8 -- a recursive destroy of the node's nested node), frees the node (Free
# _SizeBucketed_Thunk_8221BE68, sub_8221BE68, a size-bucketed free, on node->0), and walks to the next node (node->0
# , a next pointer), continuing the loop (while the next node's flag, node->21, is zero). So it's a "destroy a tree
# / list (loop over the nodes, recursively destroy each node's nested node, free the node, and walk to the next
# node)" routine (a recursive tree/list destructor, part of the 0x82367 cluster, a sibling of DestructorTree_832321
# 28). 50 lines (to the next function), 2 callees (sub_82367AE8 -- itself, a recursive call, Free_SizeBucketed_Thunk
# _8221BE68). Many callers (sub_826728C0, sub_8267ED70, sub_827C3598, sub_824686C8, sub_823F4A40, sub_82B7CD18, sub
# _8246E8B0, sub_824A7D48, sub_826B16B0, sub_826842E8 -- many across the codebase). The node's class (a node, ->0 =
# a next, ->8 = a field, ->21 = a flag, a tree/list node) and the tree's meaning (a tree/list of nodes, a particular
# structure) aren't resolvable, so the name states the mechanism: a recursive tree/list destructor (loop over the
# nodes, recursively destroy and free each). The "2" distinguishes it from DestructorTree_83232128.
[entrypoint.functions."0x82367AE8"]
name = "DestructorTree2_82367AE8"

# A process (a divide-and-conquer, with a recursive call): compute a range ((b - a) / 12, the number of 12-byte
# records), and (if the range is large, > 32) loop (recursively calling itself, sub_82B42A80, a divide-and-conquer,
# plus sub_82B42E28 and sub_82B43348, sibling operations).

# Args: a (r3, r31, a start pointer), b (r4, r26, an end pointer), count (r5, r30, a count), (more). It computes the
# range ((b - a) / 12, the number of 12-byte records in the range [a, b)). If the range is <= 32 (a small range), it
# takes one path (a base case, a linear process). If the range is large (> 32), it loops (a divide-and-conquer): it
# recursively calls itself (sub_82B42A80, on sub-ranges, a divide-and-conquer), and calls sibling operations (sub
# _82B42E28, sub_82B43348, in the 0x82B42 cluster). So it's a "process (a divide-and-conquer: compute the range, and
# (if large) recursively process sub-ranges)" routine (a divide-and-conquer, part of the 0x82B42 cluster). 340 lines,
# 3 callees (sub_82B42A80 -- itself, a recursive call, sub_82B42E28, sub_82B43348). 1 caller: sub_82B41738. The
# record's class (a 12-byte record, a data record / node) and the range's meaning (a range of 12-byte records, a
# particular collection) aren't resolvable, so the name states the mechanism: a divide-and-conquer (compute the range
# and (if large) recursively process sub-ranges).
[entrypoint.functions."0x82B42A80"]
name = "ProcessDivideConquer_82B42A80"

# A process (a 2D strided operation, the sibling of Process2DStrided_82BDD000): call a 2D strided operation
# (Process2DStrided_82BDD000, on the object, with constants r6 = 4 and r5 = 1), and (if the result is negative, an
# error path) do virtual calls (sub_82BC8870 and sub_82BCCB88, on the object's field, obj->0, with obj->12 and a
# constant).

# Args: obj (r3, ->0 = a field, ->12 = a field), (more). It calls a 2D strided operation (Process2DStrided_82BDD000,
# sub_82BDD000, a 2D strided operation, on the object, with constants r6 = 4 and r5 = 1, and a stack buffer at r4 =
# stack[112]). If the result is negative (an error / a negative value), it does virtual calls (sub_82BC8870, on the
# object's field (obj->0), with obj->12 and a constant; and sub_82BCCB88, on the object's field (obj->0), with a
# constant (3)). So it's a "process (a 2D strided operation, and (if the result is negative) do virtual calls on the
# object's field)" routine (a 2D strided process, part of the 0x82BDD cluster, a sibling of Process2DStrided_82BDD000
# ). 65 lines, 3 callees (Process2DStrided_82BDD000, sub_82BC8870, sub_82BCCB88). 3 callers: sub_82BDD518, sub
# _82BDD258, sub_82BDD8A8 (siblings in the 0x82BDD cluster). The object's class (obj, ->0 = a field, ->12 = a field,
# an object with a field) and the 2D operation's meaning (a 2D strided operation, a particular transform / filter)
# aren't resolvable, so the name states the mechanism: a 2D strided process (call a 2D strided operation and (if the
# result is negative) do virtual calls). The "2" distinguishes it from Process2DStrided_82BDD000.
[entrypoint.functions."0x82BDD170"]
name = "Process2DStrided2_82BDD170"

# A process (a divide-and-conquer helper, the sibling of ProcessDivideConquer_82B42A80): compute a range ((b - a) / 12
# , the number of 12-byte records), then perform a complex computation (computing midpoints, with a loop, a divide-and
# -conquer helper, calling SortRecords_82B43280 to sort the records).

# Args: out (r3, r24, an output pointer), a (r4, r26, a start pointer), b (r5, r25, an end pointer), (more). It
# computes the range ((b - a) / 12, the number of 12-byte records in the range [a, b)). It then performs a complex
# computation (a divide-and-conquer helper, computing midpoints, with a loop, a complex computation over the range,
# with a 40-element threshold). In a path it calls SortRecords_82B43280, sub_82B43280, a records sort, on the
# records. So it's a "process (a divide-and-conquer helper: compute the range, perform a complex computation
# (computing midpoints, with a loop), and sort the records)" routine (a divide-and-conquer helper, part of the
# 0x82B42 cluster, a sibling of ProcessDivideConquer_82B42A80). 459 lines, 1 callee (SortRecords_82B43280). 1 caller:
# ProcessDivideConquer_82B42A80 (sub_82B42A80, a divide-and-conquer, which calls this as a helper). The record's
# class (a 12-byte record, a data record / node) and the computation's meaning (a complex computation over the range,
# a particular divide-and-conquer) aren't resolvable, so the name states the mechanism: a divide-and-conquer helper
# (compute the range, perform a complex computation (computing midpoints, with a loop), and sort the records). The
# "2" distinguishes it from ProcessDivideConquer_82B42A80.
[entrypoint.functions."0x82B42E28"]
name = "ProcessDivideConquer2_82B42E28"

# A copy loop (the sibling of CopyRecords20_82A13138 / CopyRecords_82A13CA0): loop (while c != 0), copying 12-byte
# records (a float and a refcounted value, via CopyAssign_RefCounted_821F0108), advancing the pointers by 12 bytes
# each iteration.

# Args: a (r3, a read pointer), b (r4, r28, an end), c (r5, r30, a write pointer), (more). If a == b (an empty
# range), it takes one path. Otherwise it loops (while c != 0): it copies a 12-byte record (from a to c -- a float
# (a->-8, stored to c->0) and a refcounted value (via CopyAssign_RefCounted_821F0108, sub_821F0108, a refcounted
# copy-assign, on the record's refcounted field, a 12-byte record)), and advances the pointers (a += 12, c += 12).
# So it's a "copy 12-byte records (a loop, copying a float and a refcounted value (via a refcounted copy-assign) from
# a to c, advancing the pointers)" routine (a copy loop, part of the 0x82519 cluster, a sibling of CopyRecords20_82A
# 13138 and CopyRecords_82A13CA0). 72 lines, 1 callee (CopyAssign_RefCounted_821F0108). 4 callers: sub_821E3B38, sub
# _821F01B8, sub_829EABB8, sub_826EEC00. The record's meaning (a 12-byte record, a float and a refcounted value, a
# data record / node / value) and the source/destination's meaning (arrays of 12-byte records) aren't resolvable, so
# the name states the mechanism: copy 12-byte records (a loop, copying a float and a refcounted value (via a
# refcounted copy-assign)). The "12" distinguishes it from CopyRecords20_82A13138 (20-byte) and CopyRecords_82A13CA
# 0 (32-byte).
[entrypoint.functions."0x82519F28"]
name = "CopyRecords12_82519F28"

# A process (a hash table operation, with a validation): validate a size (r29->8 >= 0x00800002, a minimum-size check
# ; if too small, raise an error with a string, via SetString_822F2020, and a trap), and (on the normal path)
# allocate (Allocate_OrDbgBreak_8221F388) and perform a complex computation (a hash table operation, part of the
# 0x829FC cluster).

# Args: obj (r3, r24), (r4, a global, ->8 = a size), a (r5, r26), b (r6, r31), c (r7, r28), (more). It validates a
# size (r29->8, a size, from r4->8, a global): if the size is < 0x00800002 (a minimum size, a too-small check), it
# raises an error (SetString_822F2020, sub_822F2020, a string setter, with a message, and a call to sub_826C3EF0,
# and a trap). On the normal path (size >= 0x00800002), it allocates (Allocate_OrDbgBreak_8221F388, sub_8221F388, a
# size-bucketed allocation, for the hash table) and performs a complex computation (a hash table operation, with a
# large stack frame (224 bytes), and calls to sub_82171810, sub_822F1F00, sub_826C3EF0, sub_826C3FA8, sub_82C239D0,
# sub_82C23A48 -- siblings in the 0x82171/0x822F1/0x826C3/0x82C23 clusters). So it's a "process (a hash table
# operation: validate a size, and (on the normal path) allocate and perform a complex computation)" routine (a hash
# table process, part of the 0x829FC cluster). 344 lines, 8 callees (Allocate_OrDbgBreak_8221F388, SetString_822F2020
# , sub_82171810, sub_822F1F00, sub_826C3EF0, sub_826C3FA8, sub_82C239D0, sub_82C23A48). 7 callers (sub_824EDE70, sub
# _824AEBB8, sub_82366FB0, sub_8246E758, sub_82419280, sub_823660D0, sub_829FC180). The object's class (obj, r24, an
# object with a hash table) and the size's meaning (a size, a minimum-size check) aren't resolvable, so the name
# states the mechanism: a hash table process (validate a size and (on the normal path) allocate and perform a complex
# computation).
[entrypoint.functions."0x829FC3C0"]
name = "ProcessHashTable_829FC3C0"

# A chain/list operation (with a validation): validate obj->0 is non-zero (a trap if zero), and (depending on obj->4
# ->21, a flag) process the chain (walk the chain by obj->4->8 and obj->4->0, with a ->21 flag, and a trap on an
# invalid state).

# Args: obj (r3, ->0 = a field, ->4 = a node, ->4->0 = a head, ->4->8 = a next, ->4->21 = a flag), (more). It
# validates obj->0 is non-zero (obj->0 != 0, a trap, 22, if zero -- an invalid state). It then checks the flag (obj
# ->4->21, a byte): if it is zero, it walks the chain (obj->4->0, the head, and its ->21 flag and ->8 next, a
# chain/list walk); if it is non-zero, it processes obj->4->8 (a next) and (if that's also in a valid state, ->21 is
# zero) returns, else raises a trap (an invalid state). So it's a "process a chain (validate obj->0, and (depending
# on a flag) walk the chain by the next pointers, with a trap on an invalid state)" routine (a chain/list operation,
# part of the 0x8250A cluster). 112 lines, no direct callees (pure data manipulation, with traps). Many callers (sub
# _82AB7620, sub_82AA7F40, sub_8267EAD0, sub_82C82EA8, sub_8218E328, sub_8246E758, sub_82858CA0, sub_82344470, sub
# _8267F630, sub_823775B8 -- many across the codebase). The object's class (obj, ->0 = a field, ->4 = a node, an
# object with a node) and the chain's element type (a node, ->0 = a head, ->8 = a next, ->21 = a flag, a chain/list
# node) aren't resolvable, so the name states the mechanism: a chain/list operation (validate and process the chain
# by the next pointers). The "2" distinguishes it from a first chain process (if any).
[entrypoint.functions."0x8250AF60"]
name = "ProcessChain2_8250AF60"

# A process (a vector/SIMD computation): load a float constant (from a fixed .data address), perform a vector/SIMD
# computation (many vector operations, lvlx, a vector load, and float stores to the stack), and return a computed
# value (a vector result, stored to the stack).

# Args: (r3, a pointer), (many -- a large number of float and vector arguments), (more). It loads a float constant
# (from a fixed .data address, a magic constant, a particular value) and performs a vector/SIMD computation (many
# vector operations, lvlx, a vector load, and float stores to the stack -- a complex vector computation, with a
# 16-byte vector, a 128-bit SIMD operation, and float arithmetic). It stores the computed value (a vector result, a
# float, to the stack, a return value). So it's a "process (a vector/SIMD computation: load a float constant, perform
# a vector computation (many vector operations), and return the computed value)" routine (a vector/SIMD computation,
# part of the 0x82A3B cluster). 118 lines, no direct callees (pure vector/SIMD computation, which the callee scan does
# not count). 1 caller: sub_82A3B330 (a sibling in the 0x82A3B cluster). The value's meaning (a vector result, a
# computed float / vector, a particular value) and the constant's meaning (a magic constant, a particular value) are
# n't resolvable, so the name states the mechanism: a vector/SIMD computation (load a float constant, perform a vector
# computation, and return the computed value).
[entrypoint.functions."0x82A3B1E8"]
name = "ProcessVector_82A3B1E8"

# A list splice (the sibling of List_SpliceNode3_8234D600 / List_SpliceNode2_8292CC88): splice a node into a list
# (using the node's ->0, ->4, and ->8 pointers, with a ->21 flag, and a comparison to place the node in the right
# position).

# Args: obj (r3, ->4 = a node), node (r4, ->0 = a head, ->4 = a next, ->8 = a next, ->21 = a flag), (more). It
# splices the node into a list: it loads a node (r11 = r4->8, a next), loads its head (r10 = r11->0, a head), and
# updates r4->8 (r4->8 = r10). If the head's flag (r10->21, a byte) is zero, it sets r10->4 = r4 (a splice). It then
# updates the next pointers (r11->4 = r4->4, a next) and, (depending on a comparison, obj->4->4 == r4), performs a
# splice (r10 = obj->4, r10->4 = r11, r11->0 = r4, r4->4 = r11 -- a splice of the node into the list). So it's a
# "splice a node into a list (using the node's ->0, ->4, and ->8 pointers, with a ->21 flag, and a comparison to
# place the node in the right position)" routine (a list splice, part of the 0x82C23 cluster, a sibling of List_Splic
# eNode3_8234D600). 67 lines, no direct callees (pure data manipulation). Many callers (sub_82344608, sub_8246FFC8, sub
# _8218E0B0, sub_82C06478, sub_8233DE80, sub_82A42F70, sub_825D8968, ProcessHashTable_829FC3C0, sub_82496FB0, sub
# _82C82A80 -- many across the codebase, including a hash table process). The node's class (a node, ->0 = a head, ->
# 4 = a next, ->8 = a next, ->21 = a flag, a doubly-linked list node) and the list's meaning (a list of nodes, a
# particular structure) aren't resolvable, so the name states the mechanism: a list splice (splice a node into a list
# by the ->0, ->4, and ->8 pointers). The "4" distinguishes it from List_SpliceNode3_8234D600 and List_SpliceNode2_
# 8292CC88.
[entrypoint.functions."0x82C239D0"]
name = "List_SpliceNode4_82C239D0"

# A dispose (a destructor with an atomic decrement, the sibling of the Dispose family): atomically decrement a
# counter (obj->4->4, via lwarx/stwcx and a global lock, a lock-free atomic decrement), and (if the result is zero,
# the last reference) process a destructor (on obj->0, the object's field).

# Args: obj (r3, ->0 = a field, ->4 = a node), (more). It atomically decrements a counter (obj->4->4, a refcount /
# use count): it enters a global lock (REX_ENTER_GLOBAL_LOCK, a thread fence, disabling interrupts), does a lock-free
# atomic decrement (lwarx/stwcx, a compare-and-swap, on obj->4->4, decrementing it by one, and retrying on a CAS
# failure), and exits the global lock (REX_LEAVE_GLOBAL_LOCK). If the result (the decremented counter) is zero (the
# last reference), it processes a destructor (on obj->0, the object's field, a cleanup). So it's a "dispose an object
# (atomically decrement a counter, and (if it is zero, the last reference) process a destructor on the object's
# field)" routine (a destructor with an atomic decrement, part of the 0x82B6A cluster, a sibling of the Dispose
# family). 80 lines, no direct callees (pure data manipulation, with a lock-free atomic decrement and a global lock --
# the callee scan does not count the atomic / lock operations). Many callers (sub_82420D50, sub_82A580D8, sub_8233DE8
# 0, sub_82B5D930, sub_8223F1E0, sub_8218E5F0, sub_82B827D8, sub_82B83808, sub_82A5A310, sub_8233E2A0 -- many across
# the codebase). The object's class (obj, ->0 = a field, ->4 = a node, a refcounted object) and the field's meaning
# (a field, a particular sub-structure) aren't resolvable, so the name states the mechanism: a destructor with an
# atomic decrement (atomically decrement a counter and (if zero) process a destructor). The "17" distinguishes it
# from the other Dispose* variants (Dispose through Dispose16).
[entrypoint.functions."0x82B6AB20"]
name = "Dispose17_82B6AB20"

# A virtual call (a vtable dispatch, the sibling of VirtualCall4_8260C130 / VirtualCallPair_82C01D60): do a virtual
# call (on obj->0->4, a vtable entry), then perform an indexed load (obj->4[index * 4]->0, a field of the
# object's array, indexed by a count).

# Args: (r3, a node), obj (r4, ->0 = a node, ->4 = an array), index (r5, a count), (more). It does a virtual call (a
# vtable dispatch: r11 = obj->0, a node, r9 = r11->4, a vtable entry, mtctr r9, bctrl -- a virtual call on obj->0->
# 4, vtable->4). It then performs an indexed load (r8 = r3->4, a node's array, r7 = index * 4, a scaled index, r6 =
# r8[r7], an indexed load, r3 = r6->0, a field of the indexed element). So it's a "virtual call (call a virtual
# method (vtable->4) on obj->0, and load a field from the object's array, indexed by a count)" routine (a virtual
# call with an indexed load, part of the 0x82C26 cluster, a sibling of VirtualCall4_8260C130). 45 lines (to the next
# function), no direct callees (a pure virtual call via REX_CALL_INDIRECT_FUNC, which the callee scan does not count
# ). It is registered (in the function table, fable_2_init.cpp / fable_2_register.cpp) but has no direct callers in
# the recompiled code (a registered-but-uncalled virtual call, likely invoked only from code outside the traced
# region, or unused). The node's class (a node, ->0 = a node, ->4 = an array, an object with a virtual method and an
# array) and the vtable's meaning (a virtual method, a particular operation) aren't resolvable, so the name states
# the mechanism: a virtual call (call a virtual method on a node and load a field from the object's array). The "5"
# distinguishes it from the other VirtualCall* variants (VirtualCall_826C0388, VirtualCall2_83231E88, VirtualCall3_
# 822142C0, VirtualCall4_8260C130).
[entrypoint.functions."0x82C26818"]
name = "VirtualCall5_82C26818"

# A process (a vcall sequence, the sibling of ProcessVcallSequence3_82C14D70): call a vcall triple
# (VcallTriple_AllSuccess_82C011E8, on the object and a constant), and (if the result's low byte is non-zero, a
# condition) call another vcall sequence (ProcessVcallSequence3_82C14D70, on obj+4 and a constant).

# Args: obj (r3, r31, +4 = a field), value (r4, r30), (more). It calls a vcall triple (VcallTriple_AllSuccess_82C011
# E8, sub_82C011E8, a vcall triple, on the object (r5 = obj) and a constant (r4, a vtable / a descriptor, from a fixed
# .data address), with the value (r3 = value)). If the result's low byte is non-zero (result & 0xFF != 0, a condition
# / a success flag), it calls another vcall sequence (ProcessVcallSequence3_82C14D70, sub_82C14D70, a vcall sequence,
# on obj+4 (r5 = obj + 4) and a constant (r4, a vtable / a descriptor), with the value (r3 = value)). So it's a
# "process (a vcall sequence: call a vcall triple, and (if a condition) call another vcall sequence)" routine (a vcall
# sequence, part of the 0x82C26 cluster, a sibling of ProcessVcallSequence3_82C14D70). 109 lines, 2 callees (VcallTri
# ple_AllSuccess_82C011E8, ProcessVcallSequence3_82C14D70). 1 caller: sub_82C26940 (a sibling in the 0x82C26 cluster).
# The object's class (obj, r31, +4 = a field, an object with a field) and the value's meaning (a value, a particular
# argument) aren't resolvable, so the name states the mechanism: a vcall sequence (call a vcall triple and (if a
# condition) call another vcall sequence). The "4" distinguishes it from ProcessVcallSequence3_82C14D70.
[entrypoint.functions."0x82C26880"]
name = "ProcessVcallSequence4_82C26880"

# A process (a vcall sequence, the sibling of ProcessVcallSequence4_82C26880 / ProcessVcallSequence3_82C14D70):
# do a virtual call (on obj->0->24, a vtable entry), and (if the result's low byte is non-zero, a condition) call a
# vcall sequence (ProcessVcallSequence4_82C26880, on the object and a value) and do another virtual call (on obj->0->
# 28, a vtable entry).

# Args: obj (r3, r31, ->0 = a node, ->0->24 and ->0->28 = vtable entries), (r4), value (r5, r30), (more). It does a
# virtual call (a vtable dispatch: r11 = obj->0, a node, r10 = r11->24, a vtable entry, mtctr r10, bctrl -- a virtual
# call on obj->0->24, vtable->24). If the result's low byte is non-zero (result & 0xFF != 0, a condition / a success
# flag), it calls a vcall sequence (ProcessVcallSequence4_82C26880, sub_82C26880, a vcall sequence, on the object
# (r4 = obj) and a value (r3 = value)), and (if that also succeeds) does another virtual call (a vtable dispatch: r11
# = obj->0, r10 = r11->28, a vtable entry, mtctr r10, bctrl -- a virtual call on obj->0->28, vtable->28). So it's a
# "process (a vcall sequence: do a virtual call (vtable->24), and (if a condition) call a vcall sequence and do another
# virtual call (vtable->28))" routine (a vcall sequence, part of the 0x82C26 cluster, a sibling of ProcessVcallSequenc
# e4_82C26880). 86 lines, 1 direct callee (ProcessVcallSequence4_82C26880 -- the virtual calls via REX_CALL_INDIRECT_
# FUNC are not counted). 1 caller: sub_82C26FB8 (a sibling in the 0x82C26 cluster). The node's class (a node, ->0 = a
# node, ->24 and ->28 = vtable entries, an object with virtual methods) and the vtable's meaning (a virtual method, a
# particular operation) aren't resolvable, so the name states the mechanism: a vcall sequence (do a virtual call and
# (if a condition) call a vcall sequence and do another virtual call). The "5" distinguishes it from ProcessVcallSeq
# uence4_82C26880 and ProcessVcallSequence3_82C14D70.
[entrypoint.functions."0x82C26940"]
name = "ProcessVcallSequence5_82C26940"

# A queue/ring-buffer push (with a size check): compute the capacity ((obj->12 - obj->4) >> 2, in 4-byte units) and
# the current size ((obj->8 - obj->4) >> 2, in 4-byte units), and (if the current size is in range, less than the
# capacity) store a value (obj->8->0 = value->0, a 4-byte store) and advance the write pointer (obj->8 += 4).

# Args: obj (r3, ->4 = a base, ->8 = a write pointer, ->12 = a capacity), value (r4, r6, ->0 = a value), (more). It
# computes the capacity ((obj->12 - obj->4) >> 2, the number of 4-byte slots in the buffer, from the base (obj->4)
# to the capacity (obj->12)) and the current size ((obj->8 - obj->4) >> 2, the number of 4-byte slots used, from the
# base (obj->4) to the write pointer (obj->8)). If the current size (r8) is less than the capacity (r9, an in-range
# check), it stores a value (obj->8->0 = value->0, a 4-byte store, a push) and advances the write pointer (obj->8 +=
# 4, a write pointer advance). If the current size is >= the capacity (an overflow), it takes one path (an error / a
# full buffer, calling sub_822F7530, a sibling in the 0x822F7 cluster). So it's a "push to a queue / ring buffer (compute
# the capacity and the current size, and (if in range) store a value and advance the write pointer)" routine (a queue
# / ring-buffer push, part of the 0x822F5 cluster). 83 lines, 1 callee (sub_822F7530). Many callers (sub_827D4588, sub
# _82545C30, sub_82A72590, sub_824489A0, sub_82B27418, sub_82A6CEF0, sub_82A83420, sub_82363628, sub_823419E0, sub
# _8235F430 -- many across the codebase). The object's class (obj, ->4 = a base, ->8 = a write pointer, ->12 = a
# capacity, a queue / ring buffer with a base, a write pointer, and a capacity) and the value's meaning (a value, a 4
# -byte slot, a particular data item) aren't resolvable, so the name states the mechanism: a queue / ring-buffer push
# (compute the capacity and the current size, and (if in range) store a value and advance the write pointer).
[entrypoint.functions."0x822F5C68"]
name = "QueuePush_822F5C68"

# A process (a clamped computation, the sibling of ComputeClamped_83230A48 / the callee of Heap_CarveBlock_83231058
# ): compute a clamped value (ComputeClamped_83230A48, on a value, with a constant), and (if the result is non-zero)
# process (a computation with obj->0 and constants, 0x10000 and 0x1000, a size / a block-size computation).

# Args: obj (r3, r31, ->0 = a value), (r4), (r5), value (r6, r8), (more). It computes a clamped value (ComputeClamped
# _83230A48, sub_83230A48, a clamped computation, on the value (r6 = value) and a constant (r7 = stack[80], a
# min/max), with a result at stack[80]). If the result (stack[80]) is zero (an empty / a zero), it takes one path. If
# the result is non-zero, it processes: it loads a value (r10 = obj->0, a value), compares it against constants (0x100
# 00 = 1 << 16, and 0x1000 = 1 << 12, a size / a block-size check), and (depending on the comparison) computes a value
# (a size, a block-size computation, with a 4-byte stride, r6 = 4). So it's a "process (a clamped computation: compute
# a clamped value, and (if the result is non-zero) compute a size / a block size (with obj->0 and constants))" routine
# (a clamped computation, part of the 0x83230 cluster, a sibling of ComputeClamped_83230A48, a callee of
# Heap_CarveBlock_83231058). 92 lines, 2 callees (ComputeClamped_83230A48, sub_82CC0CA0). 1 caller: Heap_CarveBlock_8
# 3231058 (sub_83231058, a heap block carver, which calls this to compute a clamped value / a block size). The
# object's class (obj, r31, ->0 = a value, an object with a value) and the value's meaning (a value, a size / a block
# size, a particular dimension) aren't resolvable, so the name states the mechanism: a clamped computation (compute a
# clamped value and (if the result is non-zero) compute a size / a block size). The "2" distinguishes it from Compute
# Clamped_83230A48.
[entrypoint.functions."0x83230D00"]
name = "ComputeClamped2_83230D00"

# A heap allocation (the callee of Allocate_SizeBucketed_83231D20 / PoolAllocate_8240DAA8): clamp the size (min(size,
# obj->16, a max size), default to 16 if zero), align it to 16 bytes ((size + 31) & ~0xF), and (if a condition, obj
# ->24 is zero) carve a block (Heap_CarveBlock_83231058) and perform a heap operation (sub_83230DA0).

# Args: obj (r3, r31, ->16 = a max size, ->24 = a field), size (r4), (more). It clamps the size (min(size, obj->16,
# a max size): if size >= obj->16, it sets size = obj->16). If size is zero, it defaults to 16 (a minimum size). It
# aligns the size to 16 bytes ((size + 31) & ~0xF, a 16-byte alignment, a block alignment). If obj->24 (a field, a
# free-list pointer) is zero (an empty state), it performs a heap operation (sub_83230DA0, a heap operation, on the
# object) and (if the result is non-zero) carves a block (Heap_CarveBlock_83231058, sub_83231058, a heap block
# carver, on the object, with the aligned size). So it's a "allocate from a heap (clamp and align the size, and (if a
# condition) carve a block and perform a heap operation)" routine (a heap allocation, part of the 0x83231 cluster, a
# callee of Allocate_SizeBucketed_83231D20 and PoolAllocate_8240DAA8). 196 lines, 2 callees (Heap_CarveBlock_83231058
# , sub_83230DA0). 4 callers: sub_82A20528, Allocate_SizeBucketed_83231D20 (sub_83231D20, a size-bucketed
# allocation), PoolAllocate_8240DAA8 (sub_8240DAA8, a pool allocation), sub_828456C8. The object's class (obj, r31, ->
# 16 = a max size, ->24 = a field, a heap object with a max size and a free-list pointer) and the size's meaning (a
# size, a particular allocation size) aren't resolvable, so the name states the mechanism: a heap allocation (clamp
# and align the size, and (if a condition) carve a block and perform a heap operation).
[entrypoint.functions."0x83231590"]
name = "HeapAllocate_83231590"

# A copy (a simple 8-byte copy): copy two words (src->124 and src->128, an 8-byte value) to dst->0 and dst->4 (a
# simple 8-byte copy, from an offset in the source to the start of the destination).

# Args: dst (r3, ->0 and ->4, a destination), src (r4, ->124 and ->128, a source), (more). It copies two words (src
# ->124 to dst->0, and src->128 to dst->4, an 8-byte copy, from an offset (124) in the source to the start (0) of the
# destination). So it's a "copy 8 bytes (copy two words, src->124 and src->128, to dst->0 and dst->4)" routine (a
# simple 8-byte copy, part of the 0x82B56 cluster). 10 lines, no callees (pure data manipulation). It is registered (in
# the function table, fable_2_init.cpp / fable_2_register.cpp) but has no direct callers in the recompiled code (a
# registered-but-uncalled copy, likely invoked only from code outside the traced region, or unused). The source's class
# (src, ->124 and ->128, an object with an 8-byte value at an offset) and the destination's class (dst, ->0 and ->4,
# a destination for the 8-byte value) aren't resolvable, so the name states the mechanism: a simple 8-byte copy (copy
# two words, src->124 and src->128, to dst->0 and dst->4).
[entrypoint.functions."0x82B56820"]
name = "Copy8Bytes_82B56820"

# A 24-byte constructor (the sibling of Construct28Byte_82A12828 / Construct16Byte_82275368): allocate 24 bytes
# (Allocate_OrDbgBreak_8221F388), zero out the object's fields (->0, ->4, ->8 = 0, and ->21 = 0, a zero), and set a
# flag (->20 = 1, a one, a byte flag).

# Args: obj (r3, r31, a parent), (more). It allocates 24 bytes (Allocate_OrDbgBreak_8221F388, sub_8221F388, a
# size-bucketed allocation, of 24 bytes). It then zeros out the object's fields (->0, ->4, ->8 = 0, three words, and
# ->21 = 0, a zero -- a byte flag, and ->20 = 1, a one -- a byte flag). It then (presumably) stores the new object into
# the parent (obj, r31). So it's a "construct a 24-byte object (allocate 24 bytes, zero out the fields (and set ->20 =
# 1, ->21 = 0), and store it into the parent)" routine (a 24-byte constructor, part of the 0x8246C cluster). 87 lines,
# 1 callee (Allocate_OrDbgBreak_8221F388). Many callers (sub_823F9C10, sub_82B722E8, sub_827ED030, sub_8235E7C0, sub
# _8324E200, sub_8249C690, sub_825CC450, sub_823FF7E8, sub_8322C840, sub_825C4E80 -- many across the codebase). The
# object's class (a 24-byte object, ->0, ->4, ->8 = words, ->20 = a flag, ->21 = a flag, an object with flags) and the
# parent's class (obj, r31, an object with a 24-byte object) aren't resolvable, so the name states the mechanism: a 24
# -byte constructor (allocate 24 bytes and zero out the fields). The "24" distinguishes it from the other constructor
# sizes (Construct16Byte_82275368, Construct20ByteNode_82A12F00, Construct28Byte_82A12828, Construct36Byte_82370EF8,
# Construct48ByteNode_82A0ED98).
[entrypoint.functions."0x8246C600"]
name = "Construct24Byte_8246C600"

# A copy (a 20-byte copy, the sibling of Copy8Bytes_82B56820 / CopyRecords20_82A13138): copy 20 bytes (four words
# (src->0, ->4, ->8, ->12) and a float (src->16)) from src to dst, then call a process (ProcessCount_82B728D8, on dst
# + 20 and src + 20, the objects' tails).

# Args: dst (r3, r31, +20 = a tail), src (r4, r11, +20 = a tail), (more). It copies 20 bytes (from src to dst -- four
# words: src->0 to dst->0, src->4 to dst->4, src->8 to dst->8, src->12 to dst->12, and a float: src->16 to dst->16, a
# 20-byte copy). It then calls a process (ProcessCount_82B728D8, sub_82B728D8, a count process, on the objects' tails
# (dst + 20 and src + 20, the 20-byte objects' tails)). So it's a "copy a 20-byte object (copy four words and a float
# from src to dst, and process the objects' tails)" routine (a 20-byte copy, part of the 0x82B82 cluster, a sibling of
# Copy8Bytes_82B56820). 50 lines (to the next function), 1 callee (ProcessCount_82B728D8). 1 caller: sub_82B828C8 (a
# sibling in the 0x82B82 cluster). The object's class (a 20-byte object, ->0, ->4, ->8, ->12 = words, ->16 = a float, ->
# 20 = a tail, an object with a head and a tail) and the source/destination's meaning (a 20-byte object, a data object
# / node / value) aren't resolvable, so the name states the mechanism: a 20-byte copy (copy four words and a float from
# src to dst, and process the objects' tails). The "20" distinguishes it from Copy8Bytes_82B56820 (8-byte).
[entrypoint.functions."0x82B82C68"]
name = "Copy20Bytes_82B82C68"

# A process (a critical section operation, the sibling of the 0x82B5C cluster): enter a critical section
# (CriticalSection_EnterOrTry_82200688), call a process (sub_82B5D548, on obj->0 and a constant, with a result at
# stack[96]), validate the result (stack[96] is non-zero and != a constant, a trap / an error if invalid), and (on a
# success path) perform further processing (sub_82B5D690, a sibling in the 0x82B5D cluster).

# Args: obj (r3, r31, ->0 = a value), value (r4, r30), (more). It enters a critical section (CriticalSection_EnterOrT
# ry_82200688, sub_82200688, a critical section enter-or-try, on a global, with a constant). It then calls a process
# (sub_82B5D548, a process, on obj->0 (stack[80], a value, copied from obj->0) and a constant (stack[96], a result
# buffer), with the value (r30)). It validates the result (stack[96], a value): if it is zero or equal to a constant (a
# fixed .data address, a sentinel), it takes an error path (a trap / an error). On a success path (stack[96] is
# non-zero and != the sentinel), it performs further processing (sub_82B5D690, a sibling in the 0x82B5D cluster). So
# it's a "process (a critical section operation: enter a critical section, call a process, validate the result, and
# (on success) perform further processing)" routine (a critical section process, part of the 0x82B5C cluster). 218
# lines, 3 callees (CriticalSection_EnterOrTry_82200688, sub_82B5D548, sub_82B5D690). 1 caller: sub_82B82248 (a
# sibling in the 0x82B82 cluster). The object's class (obj, r31, ->0 = a value, an object with a value) and the value
# 's meaning (a value, a particular argument) aren't resolvable, so the name states the mechanism: a critical section
# process (enter a critical section, call a process, validate the result, and (on success) perform further processing
# ).
[entrypoint.functions."0x82B5C758"]
name = "ProcessCriticalSection_82B5C758"

# A chain search (the sibling of ChainSearch_82B3D880): walk a chain (a two-key comparison: node->12 against a
# value, following the node's next pointers, node->8 / node->0, with a ->21 flag), searching for a match, and process
# the found node (with the list).

# Args: (r3), value (r4, ->0 = a value), (more). It loads a list (a global, a fixed .data address, a list of nodes),
# and walks the chain (from the list's node, following the ->4, ->8, and ->0 pointers): it compares the node's field
# (node->12, a value) against the target value (r7, from r4->0), and (depending on the comparison) follows the node's
# next pointer (node->8, a next) or the node's head (node->0, a head / prev), walking to the next node (node->4->21,
# a flag). If the node's flag (node->21, a byte) is zero, it continues the walk; if it is non-zero, it stops (a found
# node / a match). It then processes the found node (r9, the found node) and the list (r8, the list), storing them to
# the stack (a result, for the caller). So it's a "search a chain (walk the chain by a comparison, node->12 against a
# value, following the next pointers, and process the found node)" routine (a chain search, part of the 0x82B5D
# cluster, a sibling of ChainSearch_82B3D880). 91 lines, no direct callees (pure data manipulation). 4 callers: sub
# _827F0358, ProcessCriticalSection_82B5C758 (sub_82B5C758, a critical section process), sub_82B80C18, sub_822001F8
# . The chain's element type (a node, ->4 = a next, ->8 = a next, ->12 = a value, ->21 = a flag, a chain/list node)
# and the value's meaning (a searched value, a particular match) aren't resolvable, so the name states the mechanism:
# a chain search (walk the chain by a comparison and process the found node). The "2" distinguishes it from ChainSear
# ch_82B3D880.
[entrypoint.functions."0x82B5D548"]
name = "ChainSearch2_82B5D548"

# A chain search (the sibling of ChainSearch2_82B5D548 / ChainSearch_82B3D880): walk a chain (a two-key comparison:
# node->12 against a value, following the node's next pointers, node->8 / node->0, with a ->21 flag), searching for a
# match, and (in a path) dispose (Dispose17_82B6AB20) and call a sibling (sub_82B5E330).

# Args: (r3), value (r4, ->0 = a value), (more). It loads a list (a global, a fixed .data address, a list of nodes),
# and walks the chain (from the list's node, following the ->4, ->8, and ->0 pointers): it compares the node's field
# (node->12, a value) against the target value (r7, from r4->0), and (depending on the comparison) follows the node's
# next pointer (node->8, a next) or the node's head (node->0, a head / prev), walking to the next node (node->4->21,
# a flag). If the node's flag (node->21, a byte) is zero, it continues the walk; if it is non-zero, it stops (a found
# node / a match). In a path it disposes (Dispose17_82B6AB20, sub_82B6AB20, a destructor with an atomic decrement) and
# calls a sibling (sub_82B5E330, a sibling in the 0x82B5E cluster). So it's a "search a chain (walk the chain by a
# comparison, node->12 against a value, following the next pointers, and (in a path) dispose and call a sibling)"
# routine (a chain search, part of the 0x82B5D cluster, a sibling of ChainSearch2_82B5D548). 135 lines, 2 callees
# (Dispose17_82B6AB20, sub_82B5E330). 1 caller: ProcessCriticalSection_82B5C758 (sub_82B5C758, a critical section
# process). The chain's element type (a node, ->4 = a next, ->8 = a next, ->12 = a value, ->21 = a flag, a chain/list
# node) and the value's meaning (a searched value, a particular match) aren't resolvable, so the name states the
# mechanism: a chain search (walk the chain by a comparison and (in a path) dispose). The "3" distinguishes it from
# ChainSearch2_82B5D548 and ChainSearch_82B3D880.
[entrypoint.functions."0x82B5D690"]
name = "ChainSearch3_82B5D690"

# A process (a chain/list operation, the sibling of ProcessChain2_8250AF60 / the callee of ChainSearch3_82B5D690):
# (if a list's field, list->8, is zero) call a process (sub_82B5E558, on the list, with a flag), and (otherwise)
# process the list (call ProcessChain2_8250AF60 and sibling operations, sub_82B5D7F0 and sub_8273F9D8).

# Args: obj (r3, r30), (r4), a (r5, r26), b (r6, r29), (more). It loads a list (a global, a fixed .data address, a
# list of nodes). If the list's field (list->8, a field, a free-list pointer / a head) is zero (an empty state), it
# calls a process (sub_82B5E558, on the list (r4 = list), with the list's field (r6 = list->4), a value (r7 = b), and
# a flag (r5 = 1), a list initialization / a setup). Otherwise (the list is non-empty), it processes the list (call
# ProcessChain2_8250AF60, sub_8250AF60, a chain operation, and sibling operations, sub_82B5D7F0 and sub_8273F9D8 --
# siblings in the 0x82B5D/0x8273F clusters). So it's a "process (a chain/list operation: (if the list is empty) call a
# process, and (otherwise) process the list (call a chain operation and siblings))" routine (a chain/list process,
# part of the 0x82B5E cluster, a callee of ChainSearch3_82B5D690). 311 lines, 4 callees (ProcessChain2_8250AF60,
# sub_82B5D7F0, sub_82B5E558, sub_8273F9D8). 1 caller: ChainSearch3_82B5D690 (sub_82B5D690, a chain search, which
# calls this to process the found list). The list's element type (a node, a list of nodes, a particular structure)
# and the value's meaning (a value, a particular argument) aren't resolvable, so the name states the mechanism: a
# chain/list process ((if the list is empty) call a process, and (otherwise) process the list). The "3" distinguishes
# it from ProcessChain2_8250AF60.
[entrypoint.functions."0x82B5E330"]
name = "ProcessChain3_82B5E330"

# A process (a chain/list operation, the sibling of ProcessChain3_82B5E330 / the callee of ProcessChain3_82B5E330):
# validate a size (list->8 >= 0x00800002, a minimum-size check; if too small, raise an error with a string, via
# SetString_822F2020, and a trap), and (on the normal path) allocate (Allocate_OrDbgBreak_8221F388) and perform a
# complex computation (a chain/list operation, part of the 0x82B5E cluster).

# Args: list (r3, r24, a list), (r4), a (r5, r26), b (r6, r31), c (r7, r28), (more). It validates a size (list->8, a
# size, from a global, a fixed .data address): if the size is < 0x00800002 (a minimum size, a too-small check), it
# raises an error (SetString_822F2020, sub_822F2020, a string setter, with a message, and a call to sub_826C3EF0, and
# a trap). On the normal path (size >= 0x00800002), it allocates (Allocate_OrDbgBreak_8221F388, sub_8221F388, a
# size-bucketed allocation, for the list) and performs a complex computation (a chain/list operation, with a large
# stack frame (224 bytes), and calls to sub_82171810, sub_822F1F00, sub_826C3EF0, sub_826C3FA8, sub_82B5E7F8,
# sub_82B5E878 -- siblings in the 0x82171/0x822F1/0x826C3/0x82B5E clusters). So it's a "process (a chain/list
# operation: validate a size, and (on the normal path) allocate and perform a complex computation)" routine (a chain
# / list process, part of the 0x82B5E cluster, a sibling of ProcessChain3_82B5E330). 382 lines, 8 callees (Allocat
# e_OrDbgBreak_8221F388, SetString_822F2020, sub_82171810, sub_822F1F00, sub_826C3EF0, sub_826C3FA8, sub_82B5E7F8, sub
# _82B5E878). 2 callers: ProcessChain3_82B5E330 (sub_82B5E330, a chain process, which calls this when the list is
# empty), sub_82B5D7F0 (a sibling in the 0x82B5D cluster). The list's element type (a node, a list of nodes, a
# particular structure) and the size's meaning (a size, a minimum-size check) aren't resolvable, so the name states the
# mechanism: a chain/list process (validate a size and (on the normal path) allocate and perform a complex computatio
# n). The "4" distinguishes it from ProcessChain3_82B5E330 and ProcessChain2_8250AF60.
[entrypoint.functions."0x82B5E558"]
name = "ProcessChain4_82B5E558"

# A 32-byte constructor (the sibling of Construct24Byte_8246C600 / Construct28Byte_82A12828): initialize the
# object's fields (obj->4 = 1, obj->8/->12/->16/->20/->24 = 0, obj->0 = a constant, obj->28 = value->0), and (if
# value->0 is non-zero) loop (a complex float computation, calling Construct24Byte_8246C600 and ProcessCount_82B728D8
# ).

# Args: obj (r3, r31, ->0 = a field, ->4 = a count, ->8/->12/->16/->20/->24 = fields, ->28 = a field), value (r4, ->0
# = a value), a (r5, r29), float (f1), (more). It initializes the object's fields (obj->4 = 1, a count / a one, obj
# ->8/->12/->16/->20/->24 = 0, five zeros, obj->0 = a constant, a class id / a marker, from a fixed .data address, and
# obj->28 = value->0, a value). If value->0 (a value) is zero (an empty state), it takes one path. If value->0 is
# non-zero, it loops (a complex float computation, with a float (f1, a float, in f31), and calls to Construct24Byte
# _8246C600, sub_8246C600, a 24-byte constructor, and ProcessCount_82B728D8, sub_82B728D8, a count process). So it's a
# "construct a 32-byte object (initialize the fields, and (if a value is non-zero) loop (a complex float computation,
# constructing nested 24-byte objects and processing counts))" routine (a 32-byte constructor, part of the 0x82B72
# cluster). 117 lines, 2 callees (Construct24Byte_8246C600, ProcessCount_82B728D8). 3 callers: sub_82515540, sub
# _82B82248, sub_82B5C8B0. The object's class (obj, ->0 = a field, ->4 = a count, ->8/->12/->16/->20/->24 = fields, ->
# 28 = a field, a 32-byte object with nested objects) and the value's meaning (a value, a particular argument) aren't
# resolvable, so the name states the mechanism: a 32-byte constructor (initialize the fields and (if a value is non-
# zero) loop (a complex float computation, constructing nested objects)). The "32" distinguishes it from the other
# constructor sizes (Construct16Byte_82275368, Construct20ByteNode_82A12F00, Construct24Byte_8246C600, Construct28B
# yte_82A12828, Construct36Byte_82370EF8, Construct48ByteNode_82A0ED98).
[entrypoint.functions."0x82B722E8"]
name = "Construct32Byte_82B722E8"

# A process (an atomic increment, the sibling of AtomicIncrement3_821F7798 / AtomicIncrement2_82375F28): load a float
# constant (from a fixed .data address), and atomically increment a global counter (via lwarx/stwcx and a global lock,
# a lock-free atomic increment), and (in a path) destruct a refcounted value (DestructRefCounted_82214F08) and call
# sibling operations.

# Args: obj (r3, r31), (r4, a value), (more). It loads a float constant (from a fixed .data address, a magic constant,
# a particular value). It then atomically increments a global counter (a fixed .data address, a counter): it enters a
# global lock (REX_ENTER_GLOBAL_LOCK, a thread fence, disabling interrupts), does a lock-free atomic increment (lwarx/
# stwcx, a compare-and-swap, on the global counter, incrementing it by one, and retrying on a CAS failure), and exits
# the global lock (REX_LEAVE_GLOBAL_LOCK). In a path it destructs a refcounted value (DestructRefCounted_82214F08, sub
# _82214F08, a refcounted destruct, on a value) and calls sibling operations (sub_821E3B38, sub_82518D30, sub_829EAE38
# -- siblings in the 0x821E3/0x82518/0x829EA clusters). So it's a "process (an atomic increment: load a float constant
# , and atomically increment a global counter (via a global lock), and (in a path) destruct a refcounted value)" routin
# e (an atomic increment, part of the 0x82B82 cluster, a sibling of AtomicIncrement3_821F7798). 180 lines, 4 callees
# (DestructRefCounted_82214F08, sub_821E3B38, sub_82518D30, sub_829EAE38). 1 caller: sub_82B80D70 (a sibling in the 0x
# 82B80 cluster). The counter's meaning (a global counter, a particular count / a statistic) and the value's meaning
# (a value, a particular argument) aren't resolvable, so the name states the mechanism: an atomic increment (load a
# float constant, atomically increment a global counter, and (in a path) destruct a refcounted value). The "4"
# distinguishes it from the other AtomicIncrement* variants (AtomicIncrement_822CBEB8, AtomicIncrement2_82375F28,
# AtomicIncrement3_821F7798).
[entrypoint.functions."0x82B82440"]
name = "AtomicIncrement4_82B82440"

# A chain search (the sibling of ChainSearch3_82B5D690 / ChainSearch2_82B5D548 / ChainSearch_82B3D880): walk a chain
# (a two-key comparison: node->12 against a value, following the node's next pointers, node->8 / node->0, with a ->29
# flag), searching for a match, and (in a path) call sibling operations (sub_823074B8, sub_82308C28, sub_8235E278,
# sub_8264EDF8).

# Args: obj (r3, r31, ->4 = a node), value (r4, r30, ->0 = a value), (more). It loads a node (obj->4->4, a node in a
# chain). If the node's flag (node->29, a byte) is zero, it takes one path (reading a value from r30->0). It then walks
# the chain (a loop): it compares the node's field (node->12, a value) against the target value (r9, from r30->0), and
# (depending on the comparison) follows the node's next pointer (node->8, a next) or the node's head (node->0, a head / 
# prev), walking to the next node (node->4->29, a flag). In a path it calls sibling operations (sub_823074B8, sub
# _82308C28, sub_8235E278, sub_8264EDF8 -- siblings in the 0x82307/0x82308/0x8235E/0x8264E clusters). So it's a "search a
# chain (walk the chain by a comparison, node->12 against a value, following the next pointers, and (in a path) call
# sibling operations)" routine (a chain search, part of the 0x82306 cluster, a sibling of ChainSearch3_82B5D690). 158
# lines, 4 callees (sub_823074B8, sub_82308C28, sub_8235E278, sub_8264EDF8). 1 caller: sub_8281A568. The chain's
# element type (a node, ->4 = a next, ->8 = a next, ->12 = a value, ->29 = a flag, a chain/list node) and the value's
# meaning (a searched value, a particular match) aren't resolvable, so the name states the mechanism: a chain search
# (walk the chain by a comparison and (in a path) call sibling operations). The "4" distinguishes it from ChainSearch
# 3_82B5D690, ChainSearch2_82B5D548, and ChainSearch_82B3D880.
[entrypoint.functions."0x82306B80"]
name = "ChainSearch4_82306B80"

# A process (a recursive construct, with a complex computation): construct a 12-byte object (Construct12Byte_825F7B10
# , on a value and value->4), load a float constant (from a fixed .data address), and perform a complex computation (a
# large stack frame, storing constants and floats, calling GetSingleton_82BA8718, GrowRegion_823F9AE0,
# ProcessIndexedRegion_82BA9068, Serialize_82CA3EB8, VcallProcess_823F8D30, and recursively calling itself).

# Args: obj (r3, r31), a (r4, r28), value (r5, r29, ->4 = a field), (more). It constructs a 12-byte object
# (Construct12Byte_825F7B10, sub_825F7B10, a 12-byte constructor, on the value (r3 = value) and a field (r4 = value->
# 4)). It loads a float constant (from a fixed .data address, a magic constant, a particular value). It then performs a
# complex computation (a large stack frame (192 bytes), storing constants and floats, and calls to GetSingleton_82BA8
# 718, sub_82BA8718, a singleton getter; GrowRegion_823F9AE0, sub_823F9AE0, a region grower; ProcessIndexedRegion_82B
# A9068, sub_82BA9068, an indexed region process; Serialize_82CA3EB8, sub_82CA3EB8, a serialize operation;
# VcallProcess_823F8D30, sub_823F8D30, a vcall process; and sub_823F8F40 -- itself, a recursive call). So it's a
# "process (a recursive construct: construct a 12-byte object, load a float constant, and perform a complex computatio
# n (calling a singleton getter, a region grower, an indexed region process, a serialize operation, a vcall process,
# and recursively calling itself))" routine (a recursive construct, part of the 0x823F8 cluster). 148 lines, 7 callees
# (Construct12Byte_825F7B10, GetSingleton_82BA8718, GrowRegion_823F9AE0, ProcessIndexedRegion_82BA9068,
# Serialize_82CA3EB8, VcallProcess_823F8D30, sub_823F8F40 -- itself, a recursive call). 1 caller: sub_8281A568. The
# object's class (obj, r31, an object with a value) and the value's meaning (a value, a particular argument) aren't
# resolvable, so the name states the mechanism: a recursive construct (construct a 12-byte object, load a float
# constant, and perform a complex computation (recursively calling itself)). The "Recursive" refers to the self-call.
[entrypoint.functions."0x823F8F40"]
name = "ProcessRecursive_823F8F40"

# A process (a hash table operation, the sibling of ProcessHashTable_829FC3C0 / the callee of ChainSearch_82B3D880):
# (if a list's field, list->8, is zero) call a hash table operation (ProcessHashTable_829FC3C0, on the list's field
# (list->4) and a value, with a flag), and (otherwise) process the list (call ProcessChain2_8250AF60 and sibling
# operations, sub_8246E758 and sub_8273F9D8).

# Args: obj (r3, r29), list (r4, r30, ->4 = a field, ->8 = a field), a (r5, r31), b (r6, r28), (more). It checks the
# list's field (list->8, a field, a free-list pointer / a head): if it is zero (an empty state), it calls a hash table
# operation (ProcessHashTable_829FC3C0, sub_829FC3C0, a hash table process, on the list's field (r6 = list->4), a
# value (r7 = b), and a flag (r5 = 1), a hash table initialization / a setup). Otherwise (the list is non-empty), it
# processes the list (call ProcessChain2_8250AF60, sub_8250AF60, a chain operation, and sibling operations, sub
# _8246E758 and sub_8273F9D8 -- siblings in the 0x8246E/0x8273F clusters). So it's a "process (a hash table operation:
# (if the list is empty) call a hash table operation, and (otherwise) process the list (call a chain operation and
# siblings))" routine (a hash table process, part of the 0x824AE cluster, a sibling of ProcessHashTable_829FC3C0, a
# callee of ChainSearch_82B3D880). 372 lines, 4 callees (ProcessHashTable_829FC3C0, ProcessChain2_8250AF60, sub
# _8246E758, sub_8273F9D8). 2 callers: ChainSearch_82B3D880 (sub_82B3D880, a chain search), sub_824AE2E8 (a sibling in
# the 0x824AE cluster). The list's element type (a node, a list of nodes, a hash table / a particular structure) and
# the value's meaning (a value, a particular argument) aren't resolvable, so the name states the mechanism: a hash
# table process ((if the list is empty) call a hash table operation, and (otherwise) process the list). The "2"
# distinguishes it from ProcessHashTable_829FC3C0.
[entrypoint.functions."0x824AEBB8"]
name = "ProcessHashTable2_824AEBB8"

# A process (a hash table lookup, with a construct and a comparison, and a recursive call): construct a refcounted
# value (ConstructRefCounted_8222CF18), call a hash lookup (HashLookup_823F74D0, on a value and obj->20), destruct the
# refcounted value (DestructRefCounted_82214F08), compare strings (CompareString_8229AD78), and (in a path) perform a
# complex computation (a large stack frame, calling GetSingleton_82BA8718, GrowRegion_823F9AE0, ProcessIndexedRegion
# _82BA9068, Serialize_82CA3EB8, VcallProcess_823F8D30, and recursively calling itself).

# Args: obj (r3, r29, ->20 = a field), value (r4, r31, a value), (more). It constructs a refcounted value
# (ConstructRefCounted_8222CF18, sub_8222CF18, a refcounted constructor, on a key (r5 = -1, a key, a hash), and a
# string (r4, a constant, from a fixed .data address, a particular key)). It calls a hash lookup (HashLookup_823F74D0
# , sub_823F74D0, a hash table lookup, on the refcounted value (r6), a field (r5 = obj->20), and the value (r4 = value
# ), storing the result to the stack (r3)). It destructs the refcounted value (DestructRefCounted_82214F08, sub
# _82214F08, a refcounted destruct, on the value). It compares strings (CompareString_8229AD78, sub_8229AD78, a string
# comparison, on a constant (r4) and the result (r3), comparing the looked-up string against a particular key). In a
# path it performs a complex computation (a large stack frame (192 bytes), and calls to GetSingleton_82BA8718, sub
# _82BA8718, a singleton getter; GrowRegion_823F9AE0, sub_823F9AE0, a region grower; ProcessIndexedRegion_82BA9068,
# sub_82BA9068, an indexed region process; Serialize_82CA3EB8, sub_82CA3EB8, a serialize operation; VcallProcess
# _823F8D30, sub_823F8D30, a vcall process; and sub_826CAFA8 -- itself, a recursive call). So it's a "process (a hash
# table lookup: construct a refcounted value, call a hash lookup, destruct the value, compare strings, and (in a path)
# perform a complex computation (recursively calling itself))" routine (a hash lookup process, part of the 0x826CA
# cluster). 262 lines, 16 callees (ConstructRefCounted_8222CF18, HashLookup_823F74D0, DestructRefCounted_82214F08,
# CompareString_8229AD78, Allocate_OrDbgBreak_8221F388, GetSingleton_82BA8718, GrowRegion_823F9AE0, ProcessIndexedReg
# ion_82BA9068, Serialize_82CA3EB8, VcallProcess_823F8D30, Release_RefCounted_821C67D8, Release_RefCounted_829FF648,
# SetValue4_829FD7B8, sub_8254C780, sub_826CAFA8 -- itself, a recursive call, sub_828C9450). 4 callers: sub_826C69B0,
# sub_82378FA0, sub_826C63D8, sub_826CAF30 (siblings in the 0x826CA/0x826C6/0x82378 clusters). The value's meaning (a
# value, a particular lookup key) and the key's meaning (a key, a particular string) aren't resolvable, so the name
# states the mechanism: a hash lookup process (construct a refcounted value, call a hash lookup, destruct the value,
# compare strings, and (in a path) perform a complex computation (recursively calling itself)).
[entrypoint.functions."0x826CAFA8"]
name = "ProcessHashLookup_826CAFA8"

# A process (a vector/SIMD computation, the sibling of ProcessVector_82A3B1E8): load a float constant (from a fixed
# .data address), and perform a vector/SIMD computation (vector loads and stores, a vector AND (vand), and other
# vector operations, on a value and a vector constant).

# Args: a (r3, a value), (more). It loads a float constant (from a fixed .data address, a magic constant, a particular
# value). It loads a vector constant (lvx128 v0, from a fixed .data address, a vector of constants, a SIMD constant).
# It then performs a vector/SIMD computation (lvlx v13, a vector load, on a value, from the stack; vand v12, a vector
# AND, combining the vector value (v13) and the vector constant (v0); stvx128 v12, a vector store, storing the result
# to r3, a value). So it's a "process (a vector/SIMD computation: load a float constant and a vector constant, and
# perform a vector/SIMD computation (a vector AND and a vector store))" routine (a vector/SIMD process, part of the
# 0x8223B cluster, a sibling of ProcessVector_82A3B1E8). 106 lines, no direct callees (pure data manipulation). 10+
# callers: sub_821B5848, sub_8248B500, sub_822961A0, sub_82A5D430, sub_82881778, sub_82A3A420, sub_826B7D70, sub
# _826E9AB8, sub_8243FAE8, sub_826CD3A8 (a widely-called vector process, part of the game's vector/SIMD subsystem).
# The value's meaning (a value, a SIMD value, a particular vector) and the vector constant's meaning (a vector
# constant, a particular SIMD constant) aren't resolvable, so the name states the mechanism: a vector/SIMD process
# (load a float constant and a vector constant, and perform a vector/SIMD computation). The "2" distinguishes it from
# ProcessVector_82A3B1E8.
[entrypoint.functions."0x8223BD98"]
name = "ProcessVector2_8223BD98"

# A process (a heap allocation operation, the callee of Pool_AcquireNode_83230688 / the sibling of Heap_CarveBlock
# _83231058): call a process (sub_83236230, on obj->20, a size), and (if the result is non-zero) perform a size
# computation (a divide, rounding up to a multiple, a heap allocation size), storing the result to a value.

# Args: obj (r3, r31, ->4 = a field, ->8 = a size, ->12 = a size, ->20 = a size), (more). It calls a process (sub
# _83236230, on the object's field (obj->20, a size), a pool / a bucket). If the result (r3) is zero (an empty state),
# it returns. If the result is non-zero, it performs a size computation (a divide: it loads the object's field (obj
# ->12, a size, a block size / a stride), adds 15 to it (a rounding, a + 15, for alignment), divides (obj->12 + 15)
# / obj->12, rounding up to a multiple of obj->12, a block count, and stores the result (the block count * the block
# size + the offset, a heap allocation size) to the value (r3->0). It then loads the object's fields (obj->4, a field
# , and obj->8, a size, a count) and continues (a heap allocation, carving a block from the pool, part of the 0x83230
# heap cluster). So it's a "process (a heap allocation: call a process (on a size), and (if the result is non-zero)
# perform a size computation (a divide, rounding up to a multiple, a heap allocation size))" routine (a heap
# allocation process, part of the 0x83230 heap cluster, a callee of Pool_AcquireNode_83230688, a sibling of Heap
# _CarveBlock_83231058). 157 lines, 1 callee (sub_83236230, sub_83236230, a pool / a bucket process). 1 caller: Pool
# _AcquireNode_83230688 (sub_83230688, a pool node acquirer). The object's class (obj, ->4 = a field, ->8 = a size,
# ->12 = a block size, ->20 = a size, a pool / a heap object) and the size's meaning (a size, a heap allocation size)
# aren't resolvable, so the name states the mechanism: a heap allocation process (call a process (on a size) and (if
# the result is non-zero) perform a size computation (a divide, rounding up to a multiple, a heap allocation size)).
[entrypoint.functions."0x83230568"]
name = "ProcessHeapAllocate_83230568"

# A process (a pool/bucket acquisition, with a spinlock, the callee of ProcessHeapAllocate_83230568 / the sibling of
# Pool_AcquireNode_83230688): acquire a spinlock (SpinLock_Acquire_822D7408, on obj->32), and (if obj->12 is zero) call
# a process (sub_8322FF90, on obj->16), and (otherwise) process the pool (increment obj->8, a count, and call sibling
# operations, sub_829FD240 and sub_8322FB30, on a global pool).

# Args: obj (r3, r31, ->8 = a count, ->12 = a field, ->16 = a field, ->32 = a spinlock), (more). It acquires a
# spinlock (SpinLock_Acquire_822D7408, sub_822D7408, a spinlock acquisition, on the object's field (obj->32, a
# spinlock), serializing access to the pool). If the object's field (obj->12, a field, a free-list pointer / a head)
# is zero (an empty state), it calls a process (sub_8322FF90, on the object's field (obj->16, a field, a pool / a
# bucket), a pool allocation / a bucket acquisition); if the result is zero (a failure), it returns; otherwise it
# continues. If obj->12 is non-zero (a non-empty state), it processes the pool (increments the object's field (obj->8,
# a count, a block count), and calls sibling operations (sub_829FD240, sub_8322FB30 -- siblings in the 0x829FD/0x8322F
# clusters, on a global pool, a fixed .data address, a pool / a free list). So it's a "process (a pool/bucket
# acquisition: acquire a spinlock, and (if obj->12 is zero) call a process, and (otherwise) process the pool (increment
# a count and call sibling operations, on a global pool))" routine (a pool acquisition process, part of the 0x83236
# heap cluster, a callee of ProcessHeapAllocate_83230568, a sibling of Pool_AcquireNode_83230688). 156 lines, 4
# callees (SpinLock_Acquire_822D7408, sub_829FD240, sub_8322FB30, sub_8322FF90). 1 caller: ProcessHeapAllocate
# _83230568 (sub_83230568, a heap allocation process, which calls this to acquire a pool node). The object's class
# (obj, ->8 = a count, ->12 = a field, ->16 = a field, ->32 = a spinlock, a pool / a heap object) and the field's
# meaning (a field, a particular pool state) aren't resolvable, so the name states the mechanism: a pool acquisition
# process (acquire a spinlock and (if obj->12 is zero) call a process, and (otherwise) process the pool (increment a
# count and call sibling operations)).
[entrypoint.functions."0x83236230"]
name = "ProcessPoolAcquire_83236230"

# A process (a double-vcall, the sibling of VirtualCall4_8260C130): (if a value is non-zero) perform a double-vcall
# (call obj->4->0->32, a virtual method, and then obj->4->0->76, another virtual method, on a node), and (depending on
# a flag, r3 & 0xFF) take a path.

# Args: obj (r3, r31, ->4 = a node, ->24 = a field), value (r4, r30, a value), a (r5, r29), b (r6, r28), (more). It
# checks the value (r30, a value): if it is zero (an empty state), it takes one path. If it is non-zero, it performs a
# double-vcall (call obj->4->0->32, a virtual method, on the node (obj->4, a node, a refcounted object with a vtable at
# ->0), and then obj->4->0->76, another virtual method, on the node). It then checks a flag (r3 & 0xFF, a low-byte
# flag, a state / a marker): if it is zero (a state), it takes one path; if it is non-zero, it continues (a complex
# computation, with a large stack frame (128 bytes), and a call to ComputeClamped2_83230D00, sub_83230D00, a clamp
# computation). So it's a "process (a double-vcall: (if a value is non-zero) perform a double-vcall (call two virtual
# methods on a node), and (depending on a flag) take a path)" routine (a double-vcall process, part of the 0x82BB0
# cluster, a sibling of VirtualCall4_8260C130). 121 lines, no direct callees (pure data manipulation, with indirect
# vcalls). 0 callers: it is registered in the function table but not called from any recompiled guest function (a
# registered-but-uncalled function, part of the 0x82BB0 cluster, a double-vcall operation). The node's class (obj->4,
# a node, a refcounted object with a vtable at ->0, ->32 and ->76 = virtual methods) and the value's meaning (a value,
# a particular argument) aren't resolvable, so the name states the mechanism: a double-vcall process ((if a value is
# non-zero) perform a double-vcall (call two virtual methods on a node) and (depending on a flag) take a path).
[entrypoint.functions."0x82BB06D8"]
name = "ProcessDoubleVcall_82BB06D8"

# A 32-byte node constructor (the sibling of Construct32Byte_82B722E8 / ConstructNode28_83218AE8): grow the object
# (obj->84 += 32, a 32-byte allocation, carving a new node from the object's pool), initialize the new node's fields (a
# 32-byte object: node->8/->12/->16/->20/->24 = 0, node->28 = 0 (a 16-bit), node->31 = 0 (a byte), node->0 = a, node
# ->4 = b, node->30 = c (a byte)), and (if a value is non-zero) continue (a complex computation, with the value's
# field, value->20).

# Args: obj (r3, r30, ->84 = a pointer, a pool / a free list), value (r4, ->20 = a field), a (r5), b (r6), c (r8),
# (more). It grows the object (obj->84 += 32, a 32-byte allocation, carving a new node from the object's pool, a fixed
# 32-byte node size). It then initializes the new node's fields (the node at obj->84 - 32, a 32-byte object: node->8/
# ->12/->16/->20/->24 = 0, five zeros, node->28 = 0 (a 16-bit), node->31 = 0 (a byte), node->0 = a (r5, a value, a
# field), node->4 = b (r6, a value, a field), node->30 = c (r8, a byte, a flag / a marker)). If the value (r4, a value
# ) is non-zero, it continues (a complex computation, with the value's field (value->20, a field, a size / a count),
# and (if it is non-zero) further processing). So it's a "construct a 32-byte node (grow the object, obj->84 += 32, a
# 32-byte allocation, and initialize the new node's fields, and (if a value is non-zero) continue (a complex
# computation))" routine (a 32-byte node constructor, part of the 0x82BAC cluster, a sibling of Construct32Byte_82B722
# E8). 136 lines, no direct callees (pure data manipulation). 1 caller: sub_82BC1968. The object's class (obj, ->84 =
# a pointer, a pool / a free list of 32-byte nodes) and the value's meaning (a value, a particular argument) aren't
# resolvable, so the name states the mechanism: a 32-byte node constructor (grow the object and initialize the new
# node's fields). The "32" distinguishes it from the other node sizes (Construct16Byte_82275368, Construct20ByteNod
# e_82A12F00, Construct24Byte_8246C600, Construct28Byte_82A12828, ConstructNode28_83218AE8, Construct36Byte_82370EF8
# , Construct48ByteNode_82A0ED98, Construct32Byte_82B722E8).
[entrypoint.functions."0x82BACFB0"]
name = "ConstructNode32_82BACFB0"

# A process (a line parse, with an atomic increment and a call, the caller of ParseLineValue_82B46D40 / the sibling of
# ParseNumberValue_82B470B8 / ParseFloatValue_82B47F98): atomically increment a global counter (via lwarx/stwcx and a
# global lock, a lock-free atomic increment), and call a line parse (ParseLineValue_82B46D40, on a stack value and an
# object), and (in a path) construct and destruct a refcounted value.

# Args: obj (r3, r31, an object), (more). It atomically increments a global counter (a fixed .data address, a counter,
# a line / a value count): it enters a global lock (REX_ENTER_GLOBAL_LOCK, a thread fence, disabling interrupts), does
# a lock-free atomic increment (lwarx/stwcx, a compare-and-swap, on the global counter, incrementing it by one, and
# retrying on a CAS failure), and exits the global lock (REX_LEAVE_GLOBAL_LOCK). It calls a line parse (ParseLineValue
# _82B46D40, sub_82B46D40, a line value parser, on a stack value (r4, a stack buffer, a parsed value) and the object
# (r3, an object, a line / a value object), parsing a line and storing the result to the stack). In a path it
# constructs a refcounted value (ConstructRefCounted_8222CF18, sub_8222CF18, a refcounted constructor) and destructs a
# refcounted value (DestructRefCounted_82214F08, sub_82214F08, a refcounted destruct). So it's a "process (a line
# parse: atomically increment a global counter, call a line parse, and (in a path) construct and destruct a refcounted
# value)" routine (a line parse process, part of the 0x82B47 line-reader cluster, a caller of ParseLineValue_82B46D40,
# a sibling of ParseNumberValue_82B470B8). 130 lines, 4 callees (ConstructRefCounted_8222CF18, DestructRefCounted
# _82214F08, ParseLineValue_82B46D40, sub_82B47E40). 8 callers: sub_82466098, sub_82691A70, sub_825F5238, sub
# _824E3FA0, sub_828152D0, sub_82359BE8, sub_82817D28, sub_8281A568 (a widely-called line parse process, part of the
# game's line-reader subsystem). The object's class (obj, an object, a line / a value object, a particular structure)
# and the counter's meaning (a global counter, a line / a value count) aren't resolvable, so the name states the
# mechanism: a line parse process (atomically increment a global counter, call a line parse, and (in a path) construct
# and destruct a refcounted value).
[entrypoint.functions."0x82B47148"]
name = "ProcessLineParse_82B47148"

# A constant stub: return a constant (0x00010000, 65536).

# Args: none. It loads a constant (0x00010000, 65536, a power of two, a particular size / a count / a flag) into r3
# (the return value register) and returns (blr, a return, returning the constant). So it's a "return a constant (0x00
# 010000, 65536)" routine (a constant stub, part of the 0x82A14 cluster, a sibling of ReturnConstant8192_82B5C730
# / ReturnConstant0x8000_82364B18). 4 lines, no direct callees (a trivial constant stub). 0 callers: it is registered
# in the function table but not called from any recompiled guest function (a registered-but-uncalled function, part of
# the 0x82A14 cluster, a constant stub). The constant's meaning (0x00010000, 65536, a particular size / a count / a
# flag) isn't resolvable, so the name states the mechanism: a constant stub returning 0x00010000. The "0x10000"
# distinguishes it from the other constant stubs (ReturnConstant2_822490B8, ReturnConstant8_82A141C8,
# ReturnConstant16_82C1D8E0, ReturnConstant64_83233840, ReturnConstant128_83233860, ReturnConstant256_83233880,
# ReturnConstant0x8000_82364B18, ReturnConstant8192_82B5C730).
[entrypoint.functions."0x82A14660"]
name = "ReturnConstant_0x10000_82A14660"

# A getter (a string length, the UTF-16 variant of StrLen_822F1DE0 / the sibling of StrLen_822F1DE0): scan a UTF-16
# string (a 2-byte character loop, looking for a null terminator), and return the length (in characters, minus 1).

# Args: a string (r3, a pointer to a UTF-16 string, a 2-byte character array). It scans the string (a loop: it reads a
# 16-bit value (a UTF-16 character, lhz, a 16-bit load) from the string, advances by 2 bytes (a UTF-16 character
# stride, addi r11, r11, 2), and continues until it finds a null terminator (a 16-bit zero, a UTF-16 null character)).
# It then computes the length (in bytes, r11 - r3, the difference between the end and the start, a length in bytes),
# divides by 2 (a length in characters, srawi r11, r11, 1, a right shift by 1, a division by 2, converting bytes to
# UTF-16 characters), and returns the length minus 1 (addi r3, r11, -1, a length minus 1, a particular convention, a
# one-based / a zero-based offset). So it's a "get a string length (scan a UTF-16 string (a 2-byte character loop,
# looking for a null terminator) and return the length (in characters, minus 1))" routine (a string length getter, the
# UTF-16 variant of StrLen_822F1DE0, part of the 0x8217A cluster, a sibling of StrLen_822F1DE0). ~20 lines, no direct
# callees (pure data manipulation). 10+ callers: sub_82C72640, sub_82BFD228, sub_82BF1CA0, sub_82B39260, sub_82C75340,
# sub_82B4DA88, sub_822F8620, sub_82CBD5A0, sub_82C7F458, sub_82CFA1B0 (a widely-called string length getter, part of
# the game's string subsystem). The string's meaning (a string, a particular UTF-16 string, a particular text) isn't
# resolvable, so the name states the mechanism: a string length getter (scan a UTF-16 string (a 2-byte character loop,
# looking for a null terminator) and return the length (in characters, minus 1)). The "UTF16" refers to the 2-byte
# character stride (a UTF-16 string, as opposed to StrLen_822F1DE0's byte-wise scan, a UTF-8 / a byte string).
[entrypoint.functions."0x8217AB30"]
name = "StrLenUTF16_8217AB30"

# A 16-byte constructor (the sibling of Construct16Byte_82275368): initialize the object's fields (obj->4/->8/->12 = 0
# , three zeros, a 16-byte object), compute a count ((value->8 - value->4) / 12, a range divided by 12, a block count
# / a stride), and (if the count is non-zero) call a process (sub_8282EFD8, on the object, a setup / a grow).

# Args: obj (r3, r31, ->4 = a field, ->8 = a field, ->12 = a field), value (r4, r29, ->4 = a field, ->8 = a field),
# (more). It initializes the object's fields (obj->4 = 0, a field, a count / a size, obj->8 = 0, a field, a count / a
# size, obj->12 = 0, a field, a count / a size, three zeros, a 16-byte object). It computes a count (a range (value
# ->8 - value->4, a range, a size), divided by 12 (a stride, a 12-byte element size), a block count / a stride). If the
# count is zero (an empty state), it returns zero. If the count is non-zero, it calls a process (sub_8282EFD8, on the
# object, r3 = obj, a setup / a grow, part of the 0x8282E cluster). So it's a "construct a 16-byte object (initialize
# the fields, obj->4/->8/->12 = 0, compute a count, (value->8 - value->4) / 12, and (if the count is non-zero) call a
# process (on the object, a setup / a grow))" routine (a 16-byte constructor, part of the 0x82A11 cluster, a sibling
# of Construct16Byte_82275368). 117 lines, 1 callee (sub_8282EFD8, sub_8282EFD8, a setup / a grow process). 2 callers:
# sub_82A10DA8, sub_82A10E80 (siblings in the 0x82A10 cluster). The object's class (obj, ->4 = a field, ->8 = a field,
# ->12 = a field, a 16-byte object, a particular structure) and the value's meaning (a value, a range, a particular
# argument) aren't resolvable, so the name states the mechanism: a 16-byte constructor (initialize the fields, compute
# a count, and (if the count is non-zero) call a process). The "2" distinguishes it from Construct16Byte_82275368.
[entrypoint.functions."0x82A11180"]
name = "Construct16Byte2_82A11180"

# A thunk (a string comparison, the callee of StrCmpIgnoreCase_82CA6320): dereference b->0 (and if it is zero, use a
# default, a constant from a fixed .data address, a default string), and dereference a->0, then call a string
# comparison (StrCmpIgnoreCase_82CA6320, on the dereferenced values, a case-insensitive string comparison).

# Args: (r3), a (r4, r9, ->0 = a value), b (r5, ->0 = a value), (more). It dereferences b->0 (a value, a pointer): if
# b->0 is non-zero, it reads b->0->0 (a dereference, a string, a particular string); if b->0 is zero (a null state),
# it uses a default (a constant, from a fixed .data address, a default string, a particular string). It dereferences
# a->0 (a value, a pointer): if a->0 is non-zero, it reads a->0->0 (a dereference, a string, a particular string); if
# a->0 is zero, it uses the default. It calls a string comparison (StrCmpIgnoreCase_82CA6320, sub_82CA6320, a case-
# insensitive string comparison, on the dereferenced values, comparing the two strings, case-insensitively). So it's a
# "thunk (a string comparison: dereference b->0 (and if it is zero, use a default), dereference a->0, and call a string
# comparison (on the dereferenced values, a case-insensitive string comparison))" routine (a string comparison thunk,
# part of the 0x82C1D cluster, a callee of StrCmpIgnoreCase_82CA6320). ~30 lines, 1 callee (StrCmpIgnoreCase_82CA6320
# , sub_82CA6320, a case-insensitive string comparison). 0 callers: it is registered in the function table but not
# called from any recompiled guest function (a registered-but-uncalled function, part of the 0x82C1D cluster, a string
# comparison thunk). The values' meaning (a value, a string, a particular string, a key / a value) aren't resolvable,
# so the name states the mechanism: a string comparison thunk (dereference both values (using a default if zero) and
# call a string comparison (a case-insensitive string comparison)).
[entrypoint.functions."0x82C1DC98"]
name = "StrCmpThunk_82C1DC98"

# A compute (a math function, a special-function approximation, the sibling of the math cluster): take the magnitude
# (fabs, a float absolute value, of the input), load a table of float constants (from a fixed .data address, a table of
# polynomial coefficients, a set of 12 constants), and perform a polynomial evaluation (a Horner's method, a series of
# float multiplications and additions, with float-to-int conversions, a special-function approximation, a math
# function, computing a particular value from the input).

# Args: a float (f1, a value, a particular input). It takes the magnitude (fabs f0, f1, a float absolute value, a
# magnitude, handling both positive and negative inputs, a particular convention). It loads a table of float constants
# (from a fixed .data address, a table of polynomial coefficients, 12 constants: f13, f11, f10, f9, f8, f7, f6, f5, f4,
# f3, f2, f1, a set of coefficients, a particular set of magic constants). It performs a polynomial evaluation (a
# Horner's method: it computes f12 = f13 + f0, a coefficient (a float), and f13 = table[8] * f12, a multiplication (a
# float), and f13 = fctid(f13), a conversion (a float to int, a rounding), and f11 = f13 - f11, a subtraction (a float),
# and f13 = fctidz(f13), a conversion (a float to int, a rounding toward zero), a series of float operations, a
# polynomial evaluation, a special-function approximation, computing a particular value from the input). So it's a
# "compute (a math function: take the magnitude, load a table of float constants, and perform a polynomial evaluation
# (a Horner's method, a special-function approximation, computing a particular value from the input))" routine (a math
# function, a special-function approximation, part of the 0x82239 cluster, a sibling of the math cluster). 125 lines,
# no direct callees (pure data manipulation). 10+ callers: sub_826E2A28, sub_821DAF50, sub_82A27648, sub_82F51298,
# sub_82FD34B8, sub_82AA5FC8, sub_82FB79E8, sub_82A77DC8, sub_823997D0, sub_82BD1290 (a widely-called math function,
# part of the game's math subsystem). The input's meaning (a float, a value, a particular input, a particular quantity)
# and the output's meaning (a float, a value, a particular output, a particular quantity) aren't resolvable, so the
# name states the mechanism: a math function (take the magnitude, load a table of float constants, and perform a
# polynomial evaluation (a Horner's method, a special-function approximation)).
[entrypoint.functions."0x82239E88"]
name = "ComputeSpecialFunction_82239E88"

# A compute (a math function, a special-function approximation, the sibling of ComputeSpecialFunction_82239E88): take
# the magnitude (fabs, a float absolute value, of the input), load a table of float constants (from a fixed .data
# address, a table of polynomial coefficients, a set of constants), and perform a polynomial evaluation (a Horner's
# method, a series of float multiplications and additions, with float-to-int conversions, a special-function
# approximation, a math function, computing a particular value from the input).

# Args: a float (f1, a value, a particular input). It takes the magnitude (fabs f0, f1, a float absolute value, a
# magnitude, handling both positive and negative inputs, a particular convention). It loads a table of float constants
# (from a fixed .data address, a table of polynomial coefficients, a set of constants: f30, f11, f12, f10, f9, f8, f7,
# f6, f5, f4, f3, f2, f31, a set of coefficients, a particular set of magic constants). It performs a polynomial
# evaluation (a Horner's method: it computes f13 = f11 * f0, a multiplication (a float), and f11 = fctid(f13), a
# conversion (a float to int, a rounding), and continues (a series of float operations, a polynomial evaluation, a
# special-function approximation, computing a particular value from the input). So it's a "compute (a math function:
# take the magnitude, load a table of float constants, and perform a polynomial evaluation (a Horner's method, a
# special-function approximation, computing a particular value from the input))" routine (a math function, a
# special-function approximation, part of the 0x82239 cluster, a sibling of ComputeSpecialFunction_82239E88). 124
# lines, no direct callees (pure data manipulation). 10+ callers: sub_8322AF80, sub_824D8638, sub_82FD3088, sub
# _82DBB8D0, sub_821BE068, sub_829C1A80, sub_827ECDA0, sub_8294C460, sub_8322AFE8, sub_82ED1E88 (a widely-called math
# function, part of the game's math subsystem). The input's meaning (a float, a value, a particular input, a particular
# quantity) and the output's meaning (a float, a value, a particular output, a particular quantity) aren't resolvable,
# so the name states the mechanism: a math function (take the magnitude, load a table of float constants, and perform
# a polynomial evaluation (a Horner's method, a special-function approximation)). The "2" distinguishes it from
# ComputeSpecialFunction_82239E88.
[entrypoint.functions."0x82239F68"]
name = "ComputeSpecialFunction2_82239F68"

# A chain search (the sibling of ChainSearch4_82306B80 / ChainSearch3_82B5D690 / ChainSearch2_82B5D548 / ChainSearch
# _82B3D880): walk a chain (a two-key comparison: node->12 against a value, following the node's next pointers, node->8
# / node->0, with a ->33 flag), searching for a match, and (in a path) free (Free_SizeBucketed_Thunk_8221BE68) and call
# sibling operations (ProcessChainLookup_8236F398, SetValue2_8236EC48).

# Args: obj (r3, r30, ->4 = a node), value (r4, ->0 = a value), (more). It loads a node (obj->4->4, a node in a chain).
# If the node's flag (node->33, a byte) is zero, it takes one path (reading a value from r4->0). It then walks the chain
# (a loop): it compares the node's field (node->12, a value) against the target value (r9, from r4->0), and (depending
# on the comparison) follows the node's next pointer (node->8, a next) or the node's head (node->0, a head / prev),
# walking to the next node (node->4->33, a flag). In a path it frees (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a
# size-bucketed free, on a value) and calls sibling operations (ProcessChainLookup_8236F398, sub_8236F398, a chain
# lookup, and SetValue2_8236EC48, sub_8236EC48, a value setter). So it's a "search a chain (walk the chain by a
# comparison, node->12 against a value, following the next pointers, and (in a path) free and call sibling operations)
# " routine (a chain search, part of the 0x8236E cluster, a sibling of ChainSearch4_82306B80). 181 lines, 3 callees
# (Free_SizeBucketed_Thunk_8221BE68, ProcessChainLookup_8236F398, SetValue2_8236EC48). 1 caller: sub_8236DD48. The
# chain's element type (a node, ->4 = a next, ->8 = a next, ->12 = a value, ->33 = a flag, a chain/list node) and the
# value's meaning (a searched value, a particular match) aren't resolvable, so the name states the mechanism: a chain
# search (walk the chain by a comparison and (in a path) free and call sibling operations). The "5" distinguishes it
# from ChainSearch4_82306B80, ChainSearch3_82B5D690, ChainSearch2_82B5D548, and ChainSearch_82B3D880.
[entrypoint.functions."0x8236E4A0"]
name = "ChainSearch5_8236E4A0"

# A process (a compare-and-dispatch, with a size computation, the sibling of DispatchAndCopy_82A0E128 / the caller of
# ValidateAndDispatch_8236FC88): compare two values (a and b, r3 and r4), and (if they differ) perform a size
# computation (a range, b->8 - b->4, divided by 4, a count, a 4-byte element count), and call a validation/dispatch
# (ValidateAndDispatch_8236FC88, on a), and (in paths) copy (CheckedCopy_82CA3808), free (Free_SizeBucketed_Thunk
# _8221BE68), and set a value (SetValue_8236FCE8).

# Args: a (r3, r29, ->4 = a field), b (r4, r30, ->4 = a pointer, ->8 = a pointer), (more). It compares a (r3, r29) and
# b (r4, r30): if they are equal (a match, a same-value check), it takes one path (a no-op / a return, a particular
# state). If they differ, it performs a size computation (a range (b->8 - b->4, a range, a size), divided by 4 (a 4-byte
# element count, srawi r10, r11, 2, a right shift by 2, a division by 4), a count, a particular size / a count). If the
# count is non-zero, it calls a validation/dispatch (ValidateAndDispatch_8236FC88, sub_8236FC88, a validation and
# dispatch, on a (r3, r29), a value, a particular state), and (in paths) copies (CheckedCopy_82CA3808, sub_82CA3808, a
# checked copy), frees (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a size-bucketed free), and sets a value
# (SetValue_8236FCE8, sub_8236FCE8, a value setter). So it's a "process (a compare-and-dispatch: compare two values (a
# and b), and (if they differ) perform a size computation (a range divided by 4, a count), and call a validation
# /dispatch (on a), and (in paths) copy, free, and set a value)" routine (a compare-and-dispatch process, part of the
# 0x8236E cluster, a sibling of DispatchAndCopy_82A0E128, a caller of ValidateAndDispatch_8236FC88). 283 lines, 5
# callees (CheckedCopy_82CA3808, Free_SizeBucketed_Thunk_8221BE68, SetValue_8236FCE8, ValidateAndDispatch_8236FC88, sub
# _822F8488). 3 callers: sub_82C673D8, sub_8236DD48, sub_82C66F20 (siblings in the 0x82C67/0x8236D/0x82C66 clusters).
# The values' meaning (a and b, a and b, particular values, a state / a particular pair) and the size's meaning (a
# size, a count, a particular size) aren't resolvable, so the name states the mechanism: a compare-and-dispatch
# process (compare two values (a and b) and (if they differ) perform a size computation (a range divided by 4, a count)
# and call a validation/dispatch (on a), and (in paths) copy, free, and set a value).
[entrypoint.functions."0x8236ED00"]
name = "ProcessCompareDispatch_8236ED00"

# A process (a dispatch, with a range check, the callee of ComputeHash_8227BA30 / the sibling of DispatchAndCheck
# _82BCD5D0): (if a value, value + 1, is <= -19, a range check, a below-threshold check) call a process (sub_82BC9860
# , on a constant, a particular value), and call a dispatch (DispatchAndAdjustPos_8227BB58, on an object, with two
# zeros and a value (value + 17, a particular offset)), and (in a path) copy (MemCpy_SdkRuntime_82CA2C60, a memory
# copy).

# Args: obj (r3, r29, an object), a (r4, r26, a value), value (r5, r30, a value), b (r6, r28, a value), (more). It
# checks a range (value + 1, a value, against -19, a threshold, a below-threshold check): if it is <= -19 (below the
# threshold), it calls a process (sub_82BC9860, on a constant, r4, a particular value, a setup / a particular
# operation). It calls a dispatch (DispatchAndAdjustPos_8227BB58, sub_8227BB58, a dispatch and adjust-position, on the
# object (r3, r29), with two zeros (r5 = 0, r4 = 0, two particular arguments), and a value (r6 = value + 17, a
# particular offset, a particular position), a dispatch, a state transition, storing the result to r31, a value). In a
# path it copies (MemCpy_SdkRuntime_82CA2C60, sub_82CA2C60, a memory copy, on a value, a particular copy). So it's a
# "process (a dispatch: (if a value is <= -19, a range check) call a process (on a constant), call a dispatch (on an
# object, with two zeros and a value, a particular offset), and (in a path) copy)" routine (a dispatch process, part
# of the 0x82BCD cluster, a callee of ComputeHash_8227BA30, a sibling of DispatchAndCheck_82BCD5D0). 139 lines, 4
# callees (DispatchAndAdjustPos_8227BB58, MemCpy_SdkRuntime_82CA2C60, sub_82BC9860, sub_82BCD698). 1 caller:
# ComputeHash_8227BA30 (sub_8227BA30, a hash computation, which calls this to dispatch a state / a position). The
# object's class (obj, an object, a particular structure, a state / a value object) and the value's meaning (a value, a
# particular argument, a position) aren't resolvable, so the name states the mechanism: a dispatch process ((if a
# value is <= -19, a range check) call a process (on a constant), call a dispatch (on an object, with two zeros and a
# value, a particular offset), and (in a path) copy). The "2" distinguishes it from the other dispatch variants.
[entrypoint.functions."0x82BCD7B0"]
name = "ProcessDispatch2_82BCD7B0"

# A getter (a pointer/index getter, with a bounds check and special values, the sibling of GetPointer_82C23858 /
# GetField13_82375C28): (if a value is positive) compute a pointer (obj->12 + value * 8, an 8-byte element stride, with
# a bounds check, obj->8, a limit, and a particular offset (minus 8)), and (if the value is in a special range,
# -10000 / -10001 / -10002, a set of special values) compute a pointer (obj->8 + value * 8, a negative offset, a
# particular pointer), returning the pointer.

# Args: obj (r3, r11, ->8 = a limit, ->12 = a base, a pointer), value (r4, a value, a particular index / a particular
# value), (more). It checks the value (r4, a value, a particular index / a particular value): if it is positive (a
# positive index, a forward offset), it computes a pointer (obj->12 + value * 8, a base (r9, obj->12) plus a stride (r1
# 0, value * 8, an 8-byte element stride, rlwinm r10, r4, 3, 0, 28, a left shift by 3, a multiplication by 8, a 8-byte
# element stride), a pointer, a particular element), and performs a bounds check (obj->8, a limit, a particular bound
# , and a particular offset (r3 = r11 - 8, a pointer, minus 8, a particular adjustment), if the pointer is below the
# limit, it returns a constant (from a fixed .data address, a particular default, a null / a particular value)). If the
# value is in a special range (-10000 / -10001 / -10002, a set of special values, a particular set of negative offsets)
# , it computes a pointer (obj->8 + value * 8, a base (r11, obj->8) plus a stride (r10, value * 8, a negative offset, a
# particular pointer), a pointer, a particular element). So it's a "get a pointer/index (a pointer/index getter: (if a
# value is positive) compute a pointer (obj->12 + value * 8, an 8-byte element stride, with a bounds check), and (if
# the value is in a special range, a set of special values) compute a pointer (obj->8 + value * 8, a negative offset))"
# routine (a pointer/index getter, part of the 0x82BC5 cluster, a sibling of GetPointer_82C23858). 111 lines, no direct
# callees (pure data manipulation). 10+ callers: sub_822A5E98, sub_82BC68F0, sub_82BD2768, sub_82BC59D0, sub_821E1548,
# sub_821931E8, sub_82BC6718, sub_82BC5C88, sub_82BC5FF0, sub_82BC5B30 (a widely-called pointer/index getter, part of
# the game's data subsystem). The object's class (obj, ->8 = a limit, ->12 = a base, a pointer, a particular structure
# , a table / a region) and the value's meaning (a value, a particular index / a particular value) aren't resolvable, so
# the name states the mechanism: a pointer/index getter ((if a value is positive) compute a pointer (obj->12 + value * 8
# , an 8-byte element stride, with a bounds check), and (if the value is in a special range) compute a pointer (obj->8
# + value * 8, a negative offset)).
[entrypoint.functions."0x82BC5448"]
name = "GetIndexedPointer_82BC5448"

# A process (a conversion, with a range check and a byte manipulation, the callee of sub_82BCA180 / the sibling of
# ParseFloatValue_82B47F98): (if a flag, obj->4, is 3, a type / a flag, a particular type) perform a float-to-int
# conversion (with a range check, 0 < the value <= 1024, a 0-1023 range, a particular range), and perform a byte
# manipulation (a loop, with byte operations, a particular conversion, an 8-byte stride, a byte packing / a byte
# extraction).

# Args: obj (r3, ->0 = a float, ->4 = a type / a flag, a particular value), (more). It checks the flag (obj->4, a type
# / a flag, a particular type): if it is 3 (a particular type, a float type / a particular value type), it performs a
# float-to-int conversion (it reads the float (obj->0, a float, a particular value), converts it to an int (fctiwz
# f13, f0, a float-to-int conversion, a round toward zero, a particular conversion), and performs a range check (0 <
# the value <= 1024, a 0-1023 range, a particular range, a below/above-threshold check)). It then performs a byte
# manipulation (a loop, with byte operations, a particular conversion, an 8-byte stride, rlwinm r11, r11, 24, 8, 31, a
# byte extraction, a byte packing, a particular conversion, converting the int to a particular byte representation). If
# the flag is not 3, it takes one path (a no-op / a return, a particular state). So it's a "process (a conversion:
# (if a flag is 3, a particular type) perform a float-to-int conversion (with a range check, 0 < the value <= 1024, a
# 0-1023 range), and perform a byte manipulation (a loop, with byte operations, a particular conversion, an 8-byte
# stride, a byte packing / a byte extraction))" routine (a conversion process, part of the 0x82BC9 cluster, a callee
# of sub_82BCA180, a sibling of ParseFloatValue_82B47F98). 90 lines, no direct callees (pure data manipulation). 1
# caller: sub_82BCA180. The object's class (obj, ->0 = a float, ->4 = a type / a flag, a particular value, a float /
# int pair, a particular structure) and the flag's meaning (a flag, a type, a particular type, a float type) aren't
# resolvable, so the name states the mechanism: a conversion process ((if a flag is 3, a particular type) perform a
# float-to-int conversion (with a range check, 0 < the value <= 1024, a 0-1023 range), and perform a byte manipulation
# (a loop, with byte operations, a particular conversion, an 8-byte stride, a byte packing / a byte extraction)).
[entrypoint.functions."0x82BC9D70"]
name = "ProcessConversion_82BC9D70"

# A process (a range operation, with bounds checks and a complex computation, a grow, the sibling of GrowAndCopy_82
# 370550 / the caller of CompareU16_822F85C8): (if a value, c, is non-zero) perform a range check (b against obj->20,
# a limit, a below/above-threshold check), and (if b is in range) return b, a value; (otherwise) perform a complex
# computation (a grow, a range check, a 16-byte element, obj->24, a size / a count, and a comparison, CompareU16_822F85
# C8, a 16-bit comparison).

# Args: obj (r3, r11, ->4 = a base, ->20 = a limit, ->24 = a size / a count, a 16-byte element), a (r4, r25, a value)
# , b (r5, a value), c (r6, r28, a value), (more). It checks a value (c, r28, a value, a particular argument): if it
# is non-zero (a non-empty state), it performs a range check (b, r5, a value, against obj->20, a limit, a particular
# bound, a below/above-threshold check). If b is in range (b <= obj->20, a below-threshold check), it returns b (r3 =
# b, a value, a particular result). If b is above the limit (b > obj->20, an above-threshold check), it performs a
# complex computation (a grow, a range check, a 16-byte element, obj->24, a size / a count, a particular element, and
# a comparison, CompareU16_822F85C8, sub_822F85C8, a 16-bit comparison, on a value, a particular comparison). If c is
# zero (an empty state), it takes one path (a no-op / a return, a particular state). So it's a "process (a range
# operation: (if a value is non-zero) perform a range check (b against obj->20, a limit), and (if b is in range)
# return b, a value; (otherwise) perform a complex computation (a grow, a range check, a 16-byte element, obj->24, a
# size / a count, and a comparison))" routine (a range/grow process, part of the 0x8217A allocation cluster, a
# sibling of GrowAndCopy_82370550, a caller of CompareU16_822F85C8). 169 lines, 1 callee (CompareU16_822F85C8, sub
# _822F85C8, a 16-bit comparison). 2 callers: sub_822F2238, sub_8217AA20 (siblings in the 0x822F2/0x8217A clusters).
# The object's class (obj, ->4 = a base, ->20 = a limit, ->24 = a size / a count, a 16-byte element, a particular
# structure, a range / a grow object) and the values' meaning (a, b, c, particular values, a particular range) aren't
# resolvable, so the name states the mechanism: a range/grow process ((if a value is non-zero) perform a range check
# (b against obj->20, a limit), and (if b is in range) return b, a value; (otherwise) perform a complex computation
# (a grow, a range check, a 16-byte element, obj->24, a size / a count, and a comparison)).
[entrypoint.functions."0x8217A858"]
name = "ProcessRangeGrow_8217A858"

# A getter (a pointer offset getter, the sibling of GetOffset8_82BABC60 / GetPointer_82C23858): return a pointer (a +
# 36, a 36-byte offset, a particular field / a particular element, a pointer offset, a particular offset into an object
# ).

# Args: a (r3, a pointer, a particular object / a particular structure). It returns a pointer (r3 + 36, a 36-byte
# offset, a particular field / a particular element, a pointer offset, a particular offset into the object, a specific
# field / a specific element). So it's a "get a pointer offset (a pointer offset getter: return a pointer (a + 36, a
# 36-byte offset, a particular field / a particular element))" routine (a pointer offset getter, part of the 0x82295
# cluster, a sibling of GetOffset8_82BABC60). ~5 lines, no direct callees (a trivial pointer offset getter). 3 callers:
# sub_82C6ABE8, sub_82C6B640, sub_82C6AAB8 (siblings in the 0x82C6A/0x82C6B clusters). The object's class (a pointer,
# a particular object / a particular structure, a particular layout, a 36-byte offset to a particular field) and the
# offset's meaning (a 36-byte offset, a particular field / a particular element) aren't resolvable, so the name states
# the mechanism: a pointer offset getter (return a pointer (a + 36, a 36-byte offset, a particular field / a
# particular element)). The "36" refers to the 36-byte offset (a particular field / a particular element, a specific
# offset into the object).
[entrypoint.functions."0x822957E8"]
name = "GetOffset36_822957E8"

# A process (a range/element access, with a special value and a bounds check, the caller of sub_8217A9A8 / the sibling
# of GetIndexedPointer_82BC5448): (if a value, obj->0, is -2, a special value, a particular state) take one path, and
# (if it is 0, a null state) raise a trap (trap 22, a particular error, a null / a particular invalid state), and
# (otherwise) perform a range/element access (a 2-byte element stride, index * 2, with a bounds check, obj->4 and a
# base, a particular range, a 2-byte element, a particular element).

# Args: obj (r3, ->0 = a value, ->4 = a base, a pointer), index (r4, an index, a particular element), (more). It checks
# a value (obj->0, a value, a particular state): if it is -2 (a special value, a particular state, a below-threshold / a
# particular flag), it takes one path (a particular state, a particular result). If it is 0 (a null state, a particular
# invalid state), it raises a trap (ppc_trap 22, a particular error, a null / a particular invalid state, a particular
# assertion). Otherwise (a valid state), it performs a range/element access (a 2-byte element stride, rlwinm r9, r4, 1,
# 0, 30, a left shift by 1, a multiplication by 2, a 2-byte element stride, an index (r4, an index, a particular
# element), with a bounds check (obj->4, a base, a particular bound, and a base (r7, obj->4 or obj + 4, a base, a
# particular offset), a particular range, a 2-byte element, a particular element). So it's a "process (a range/element
# access: (if a value is -2, a special value) take one path, and (if it is 0, a null state) raise a trap (trap 22, a
# particular error), and (otherwise) perform a range/element access (a 2-byte element stride, index * 2, with a bounds
# check, obj->4 and a base, a particular range))" routine (a range/element access process, part of the 0x8217A
# allocation cluster, a caller of sub_8217A9A8, a sibling of GetIndexedPointer_82BC5448). 79 lines, no direct callees
# (pure data manipulation). 1 caller: sub_8217A9A8 (a sibling in the 0x8217A cluster). The object's class (obj, ->0 = a
# value, ->4 = a base, a pointer, a particular structure, a range / a table object) and the index's meaning (an index,
# a particular element, a particular offset) aren't resolvable, so the name states the mechanism: a range/element
# access process ((if a value is -2, a special value) take one path, and (if it is 0, a null state) raise a trap (trap
# 22), and (otherwise) perform a range/element access (a 2-byte element stride, index * 2, with a bounds check)).
[entrypoint.functions."0x8217A400"]
name = "ProcessElementAccess_8217A400"

# A process (a validation, with a bounds check and a call to ValidateIndex_822F6EE8, the sibling of ProcessElementAcce
# ss_8217A400): compute a pointer (a base (value->4, or value + 4, a base, a particular offset), plus a 2-byte element
# stride (value->20 * 2, a size, a 2-byte element, a particular range)), and call a validation (ValidateIndex_822F6EE8
# , on the pointer and an object, a particular index / a particular validation).

# Args: obj (r3, r31, an object), value (r4, r5, ->4 = a base, ->20 = a size, ->24 = a size, a particular structure),
# (more). It checks a size (value->24, a size, a particular bound): if it is >= 8 (a particular threshold, a particular
# size), it reads a base (value->4, a base, a particular offset); otherwise it uses a base (value + 4, a base, a
# particular offset, a relative offset). It computes a pointer (a base (r11, a base, a particular offset) plus a 2-byte
# element stride (r10, value->20 * 2, a size (r10, value->20, a size, a particular range), a 2-byte element, rlwinm r1
# 0, r10, 1, 0, 30, a left shift by 1, a multiplication by 2, a 2-byte element stride, a particular range), a pointer,
# a particular element). It calls a validation (ValidateIndex_822F6EE8, sub_822F6EE8, an index validation, on the
# pointer (r4, a pointer, a particular element) and the object (r3, r31, an object, a particular structure), a
# particular index / a particular validation). So it's a "process (a validation: compute a pointer (a base, plus a 2-
# byte element stride, value->20 * 2, a size, a particular range), and call a validation (ValidateIndex_822F6EE8, on
# the pointer and an object, a particular index / a particular validation))" routine (a validation process, part of the
# 0x8217A allocation cluster, a sibling of ProcessElementAccess_8217A400). 56 lines, 1 callee (ValidateIndex_822F6EE8,
# sub_822F6EE8, an index validation). 2 callers: sub_822F2238, sub_8217AA20 (siblings in the 0x822F2/0x8217A
# clusters). The object's class (obj, an object, a particular structure, a range / a table object) and the value's
# meaning (a value, a base, a size, a particular structure, a particular range) aren't resolvable, so the name states
# the mechanism: a validation process (compute a pointer (a base, plus a 2-byte element stride, value->20 * 2, a size,
# a particular range), and call a validation (ValidateIndex_822F6EE8, on the pointer and an object)).
[entrypoint.functions."0x8217A7F8"]
name = "ValidateElementAccess_8217A7F8"

# A process (a vcall loop, with a 16-bit element, the caller of ProcessElementAccess_8217A400 / the sibling of
# ProcessVcallSequence5_82C26940): compute a count (a range, b - a, divided by 2, a 16-bit element count, a particular
# count), call a validation (ProcessElementAccess_8217A400, on the object, the base, and the count, a particular
# validation), and loop (a vcall loop, calling a virtual method (d, r7, a vcall, a particular virtual method), on each
# 16-bit element (a, a 16-bit value, lhz, a 16-bit load), and storing the result (a 16-bit value, sthx, a 16-bit
# store)).

# Args: obj (r3, r27, an object), a (r4, r31, a base, a 16-bit element array), b (r5, r30, a limit, a particular end),
# c (r6, a value), d (r7, r29, a vcall, a particular virtual method), (more). It computes a count (a range (b - a, a
# range, a size), divided by 2 (srawi r4, r11, 1, a right shift by 1, a division by 2, a 16-bit element count, a
# particular count), a count, a particular number of 16-bit elements). It calls a validation (ProcessElementAccess
# _8217A400, sub_8217A400, a range/element access, on the object (r3, r27, an object), the base (r31, a base, a
# particular offset), and the count (r4, a count, a particular count), a particular validation). It loops (a vcall
# loop: it reads a 16-bit element (lhz r3, a, a 16-bit value, a particular element), calls a virtual method (bctrl d, r
# 7, a vcall, a particular virtual method, on the 16-bit value, a particular operation), and stores the result (st hx
# r3, r28, a, a 16-bit store, a particular element, a particular result), advancing to the next 16-bit element, a
# particular loop). So it's a "process (a vcall loop: compute a count (a range, b - a, divided by 2, a 16-bit element
# count), call a validation (ProcessElementAccess_8217A400, on the object, the base, and the count), and loop (a vcall
# loop, calling a virtual method (d, a vcall), on each 16-bit element (a, a 16-bit value), and storing the result))"
# routine (a vcall loop process, part of the 0x8217A allocation cluster, a caller of ProcessElementAccess_8217A400, a
# sibling of ProcessVcallSequence5_82C26940). 72 lines, 1 callee (ProcessElementAccess_8217A400, sub_8217A400, a
# range/element access). 2 callers: sub_822F2238, sub_8217AA20 (siblings in the 0x822F2/0x8217A clusters). The
# object's class (obj, an object, a particular structure, a range / a table object) and the 16-bit element's meaning (a
# 16-bit value, a particular element, a particular quantity) aren't resolvable, so the name states the mechanism: a
# vcall loop process (compute a count (a range, b - a, divided by 2, a 16-bit element count), call a validation
# (ProcessElementAccess_8217A400), and loop (a vcall loop, calling a virtual method (d, a vcall), on each 16-bit
# element, and storing the result)).
[entrypoint.functions."0x8217A9A8"]
name = "ProcessVcallLoop_8217A9A8"

# A validation (a bounds check, with a 4-byte element stride and traps, the sibling of ValidateIndex_822F6EE8 / the
# caller of sub_82BEC540): (if obj->0 is 0, a null state) raise a trap (trap 22, a particular error, a null / a
# particular invalid state), and (otherwise) perform a bounds check (a 4-byte element stride, index * 4, with a base
# (obj->0->4, a base, a particular offset) and a limit (obj->0->8, a limit, a particular bound), and (if the pointer is
# below the base or above the limit, or above the limit, a below/above-threshold check) raise a trap (trap 22, a
# particular error, an out-of-range / a particular invalid state)).

# Args: obj (r3, ->0 = a value, a 64-bit, a pointer), index (r4, an index, a particular element), (more). It checks a
# value (obj->0, a value, a 64-bit, a pointer, a particular state): if it is 0 (a null state, a particular invalid
# state), it raises a trap (ppc_trap 22, a particular error, a null / a particular invalid state, a particular
# assertion). Otherwise (a valid state), it performs a bounds check (a 4-byte element stride, rlwinm r10, r4, 2, 0, 29
# , a left shift by 2, a multiplication by 4, a 4-byte element stride, an index (r4, an index, a particular element),
# with a base (r9, obj->0->4, a base, a particular offset) and a limit (r11, obj->0->8, a limit, a particular bound), a
# particular range, a 4-byte element, a particular element). It computes a pointer (r3 = r10 + r9, a pointer, a
# particular element) and checks it (if it is above the limit (r3 > r11, an above-threshold check), or below the base
# (r3 < obj->0->4, a below-threshold check), or above the limit (r3 >= r11, an above-threshold check), it raises a trap
# (ppc_trap 22, a particular error, an out-of-range / a particular invalid state, a particular assertion)). So it's a
# "validate (a bounds check: (if obj->0 is 0, a null state) raise a trap (trap 22), and (otherwise) perform a bounds
# check (a 4-byte element stride, index * 4, with a base (obj->0->4) and a limit (obj->0->8), and (if the pointer is
# below the base or above the limit) raise a trap (trap 22)))" routine (a bounds check validation, part of the 0x82BEC
# cluster, a sibling of ValidateIndex_822F6EE8). ~45 lines, no direct callees (pure data manipulation, with traps). 10
# + callers: sub_82BEC540, sub_82BF1A40, sub_82BEF360, sub_82BEC8B0, sub_82BF07C0, sub_82BEDC10, sub_82BF06F8, sub
# _82BECFA0, sub_82BED548, sub_82BF1268 (a widely-called bounds check validation, part of the game's data validation
# subsystem). The object's class (obj, ->0 = a value, a 64-bit, a pointer, a particular structure, a range / a table
# object) and the index's meaning (an index, a particular element, a particular offset) aren't resolvable, so the name
# states the mechanism: a bounds check validation ((if obj->0 is 0, a null state) raise a trap (trap 22), and
# (otherwise) perform a bounds check (a 4-byte element stride, index * 4, with a base (obj->0->4) and a limit (obj
# ->0->8), and (if the pointer is below the base or above the limit) raise a trap (trap 22))).
[entrypoint.functions."0x82BEC300"]
name = "ValidateBounds4_82BEC300"

# A process (a dispatch, with a type check, the caller of DispatchValue_821CF230 / HashTable_Find3_821CF1D0): (if a
# flag, value->4, is 5, a type / a flag, a particular type) call a dispatch (DispatchValue_821CF230, on a value and
# value->0, a particular dispatch), and (in paths) call a hash lookup (HashTable_Find3_821CF1D0, on a value, a
# particular lookup) and sibling operations (sub_82BC9640, sub_82BC9860, sub_82BCBF50, sub_82BCD5E0 -- siblings in the
# 0x82BC9/0x82BCB/0x82BCD clusters).

# Args: obj (r3, r28, an object), value (r4, r29, ->0 = a value, ->4 = a type / a flag, a particular value), a (r5, r2
# 6, a value), b (r6, r23, a value), (more). It checks a flag (value->4, a type / a flag, a particular type): if it is
# 5 (a particular type, a particular value type), it calls a dispatch (DispatchValue_821CF230, sub_821CF230, a value
# dispatch, on a value (r4, a, a particular argument) and the value (r3, value->0, a particular value), a particular
# dispatch, storing the result to r30, a value). If the result's flag (r30->4, a flag, a particular state) is zero (a
# particular state), it takes one path (a process, with value->0->8, a particular value). In paths it calls a hash
# lookup (HashTable_Find3_821CF1D0, sub_821CF1D0, a hash table lookup, on a value, a particular lookup) and sibling
# operations (sub_82BC9640, sub_82BC9860, sub_82BCBF50, sub_82BCD5E0 -- siblings in the 0x82BC9/0x82BCB/0x82BCD
# clusters). So it's a "process (a dispatch: (if a flag is 5, a particular type) call a dispatch (DispatchValue
# _821CF230, on a value and value->0), and (in paths) call a hash lookup (HashTable_Find3_821CF1D0, on a value) and
# sibling operations)" routine (a dispatch process, part of the 0x821CF cluster, a caller of DispatchValue_821CF230 and
# HashTable_Find3_821CF1D0). 202 lines, 6 callees (DispatchValue_821CF230, HashTable_Find3_821CF1D0, sub_82BC9640, sub
# _82BC9860, sub_82BCBF50, sub_82BCD5E0). 4 callers: sub_82BDA2C8, sub_822DF280, sub_82BD3FD0, sub_821E1408 (siblings
# in the 0x82BDA/0x822DF/0x82BD3/0x821E1 clusters). The object's class (obj, an object, a particular structure, a
# dispatch / a value object) and the value's meaning (a value, a particular argument, a particular value) aren't
# resolvable, so the name states the mechanism: a dispatch process ((if a flag is 5, a particular type) call a dispatch
# (DispatchValue_821CF230, on a value and value->0), and (in paths) call a hash lookup (HashTable_Find3_821CF1D0, on a
# value) and sibling operations). The "Process" suffix distinguishes it from ProcessDispatchValue_82BCD308 (a different
# dispatch, in the 0x82BCD cluster).
[entrypoint.functions."0x821CF320"]
name = "DispatchValueProcess_821CF320"

# A getter (a size-bucket search, with a loop, the sibling of HeapAllocate_83231590 / the callee of PoolAllocate_8240D
# AA8 and Free_SizeBucketed_83231BE8): (if a size is 0, a null state) take one path, and (otherwise) search for a size
# bucket (a loop, over a bucket list (obj->24, a bucket list, a set of size buckets), comparing the size against the
# buckets, finding the first bucket that is large enough, a particular size bucket, a particular pool / a particular
# allocation bucket).

# Args: obj (r3, ->4 = a base, ->24 = a bucket list, a set of size buckets), size (r4, a size, a particular size / a
# particular allocation), (more). It checks the size (r4, a size, a particular size / a particular allocation): if it
# is 0 (a null state, a particular invalid state), it takes one path (a no-op / a return, a particular state). Otherwise
# (a valid size), it searches for a size bucket (a loop, over a bucket list (obj->24, a bucket list, a set of size
# buckets, a particular set of size buckets): it reads a bucket (r5->0, a size, a particular size bucket, a particular
# bucket), compares the size (r4, a size, a particular size) against the bucket (r5->0, a size, a particular size
# bucket, a particular bucket), and (if the size is large enough, a size >= the bucket, a particular size / a
# particular allocation) finds the bucket (a particular size bucket, a particular pool / a particular allocation bucket
# , returning the bucket, a particular result). If the size is not large enough (a size < the bucket, a particular size
# / a particular allocation), it advances to the next bucket (r5 = r5->8, a next, a particular bucket), and continues
# the loop (a particular search, a particular size bucket search). If the bucket list is exhausted (r5 = 0, a null
# state, a particular end), it returns 0 (a not-found, a particular state, a particular result). So it's a "get a size
# bucket (a size-bucket search: (if a size is 0, a null state) take one path, and (otherwise) search for a size bucket
# (a loop, over a bucket list (obj->24, a bucket list, a set of size buckets), comparing the size against the buckets,
# finding the first bucket that is large enough, a particular size bucket))" routine (a size-bucket search, part of the
# 0x83231 heap cluster, a sibling of HeapAllocate_83231590, a callee of PoolAllocate_8240DAA8 and Free_SizeBucketed
# _83231BE8). ~50 lines, no direct callees (pure data manipulation). 3 callers: PoolAllocate_8240DAA8 (sub_8240DAA8, a
# pool allocation), Free_SizeBucketed_83231BE8 (sub_83231BE8, a size-bucketed free), sub_828456C8 (a sibling in the
# 0x82845 cluster). The object's class (obj, ->4 = a base, ->24 = a bucket list, a set of size buckets, a particular
# structure, a heap / a pool object) and the size's meaning (a size, a particular size / a particular allocation)
# aren't resolvable, so the name states the mechanism: a size-bucket search ((if a size is 0, a null state) take one
# path, and (otherwise) search for a size bucket (a loop, over a bucket list (obj->24, a bucket list, a set of size
# buckets), comparing the size against the buckets, finding the first bucket that is large enough)).
[entrypoint.functions."0x83231518"]
name = "FindSizeBucket_83231518"

# A process (a vector hash/computation, with vector operations and constants, the sibling of ProcessVector2_8223BD98
# / the caller of sub_8223BB38): load vector constants (from a fixed .data address, a set of vector constants, a
# particular set of magic constants, a vector of constants), and perform a vector hash/computation (vector permutes,
# a vector XOR, a SIMD computation, a particular hash / a particular comparison, a particular vector operation).

# Args: obj (r3, an object), a (r4, a value), b (r5, a value), c (v2, a vector, a particular value), (more). It loads
# vector constants (from a fixed .data address, a set of vector constants, a particular set of magic constants, a
# vector of constants: lvx128 v0, r11, a vector constant, and vpermwi128 v6, v0, 228, a vector permute, a particular
# permutation, and vpermwi128 v5, v0, 177, a vector permute, a particular permutation, and vpermwi128 v4, v0, 78, a
# vector permute, a particular permutation). It loads another vector constant (lvx128 v0, r8, a vector constant, a
# particular value). It performs a vector hash/computation (a vector XOR, vxor v2, v6, v0, a vector XOR, a particular
# operation, a particular hash / a particular comparison, and vxor v31, v5, v0, a vector XOR, a particular operation, a
# particular hash / a particular comparison, and vxor v30, v4, v0, a vector XOR, a particular operation, a particular
# hash / a particular comparison, a SIMD computation, a particular vector operation). So it's a "process (a vector
# hash/computation: load vector constants (from a fixed .data address, a set of vector constants, a particular set of
# magic constants, a vector of constants), and perform a vector hash/computation (vector permutes, a vector XOR, a
# SIMD computation, a particular hash / a particular comparison, a particular vector operation))" routine (a vector
# hash/computation, part of the 0x8222E cluster, a sibling of ProcessVector2_8223BD98). 150 lines, no direct callees
# (pure data manipulation, with vector operations). 10+ callers: sub_8223BB38, sub_829BEB18, sub_82A0B288, sub
# _826EBE68, sub_826E8078, sub_821D3000, sub_82A9C2F8, sub_829BEFA8, sub_8223D148, sub_822AA498 (a widely-called
# vector hash/computation, part of the game's vector/SIMD subsystem). The object's class (obj, an object, a particular
# structure, a hash / a value object) and the vector's meaning (a vector, a particular value, a particular quantity)
# aren't resolvable, so the name states the mechanism: a vector hash/computation (load vector constants (from a fixed
# .data address, a set of vector constants, a particular set of magic constants, a vector of constants), and perform a
# vector hash/computation (vector permutes, a vector XOR, a SIMD computation, a particular hash / a particular
# comparison)).
[entrypoint.functions."0x8222E6C0"]
name = "ComputeVectorHash_8222E6C0"

# A process (a size computation, with a special case, the caller of sub_822F1F00 / the sibling of ComputeClamped_83230
# A48): (if a size is 0, a null state) take one path, and (otherwise) perform a special case check (a divide, -1 / size
# , a particular value, a below/above-threshold check, >= 24, a particular threshold), and (in a path) call a process
# (sub_822F1F00, on a constant and a stack value, a particular operation), and compute a size (size * 24, a 24x size,
# a particular size, a block size / a particular allocation size).

# Args: obj (r3, an object), size (r4, r31, a size, a particular size / a particular count), (more). It checks the size
# (r4, r31, a size, a particular size / a particular count): if it is 0 (a null state, a particular invalid state), it
# takes one path (a no-op / a return, a particular state). Otherwise (a valid size), it performs a special case check
# (a divide, -1 / size (divwu r10, r11, r31, a divide, -1 (r11, a constant, a particular value) / the size (r31, a
# size, a particular size), a particular value, a particular quotient), and (if the quotient is >= 24 (a particular
# threshold, a particular size, a particular count), it takes one path (a particular state, a particular result)). In a
# path it calls a process (sub_822F1F00, on a constant (r9, a constant, a particular value, from a fixed .data address,
# a particular string / a particular key) and a stack value (r3, a stack buffer, a particular value), a particular
# operation, a string / a key operation). It computes a size (size * 24, a 24x size, a particular size, a block size /
# a particular allocation size: rlwinm r11, r31, 1, 0, 30, a left shift by 1, a multiplication by 2, a particular
# stride, and add r11, r31, r11, a size * 3, a 3x size, and rlwinm r3, r11, 3, 0, 28, a left shift by 3, a multiplication
# by 8, a 8x size, a 24x size, a particular size, a block size / a particular allocation size). So it's a "process (a
# size computation: (if a size is 0, a null state) take one path, and (otherwise) perform a special case check (a
# divide, -1 / size, a particular value, >= 24, a particular threshold), and (in a path) call a process (sub_822F1F00,
# on a constant and a stack value), and compute a size (size * 24, a 24x size, a particular size, a block size / a
# particular allocation size))" routine (a size computation process, part of the 0x825BB cluster, a sibling of
# ComputeClamped_83230A48). 70 lines, 2 callees (Allocate_OrDbgBreak_8221F388, sub_8221F388, a size-bucketed allocation
# ; sub_822F1F00, a string / a key operation). 10+ callers: sub_82B134D8, sub_821BA6D0, sub_82A04C38, sub_82668848,
# sub_82344EC0, sub_825F7BE8, sub_82917988, sub_823FE080, sub_82A12EC0, sub_82A7FBC0 (a widely-called size computation
# process, part of the game's size / allocation subsystem). The object's class (obj, an object, a particular structure,
# a size / a value object) and the size's meaning (a size, a particular size / a particular count) aren't resolvable, so
# the name states the mechanism: a size computation process ((if a size is 0, a null state) take one path, and
# (otherwise) perform a special case check (a divide, -1 / size, a particular value, >= 24, a particular threshold),
# and (in a path) call a process (sub_822F1F00, on a constant and a stack value), and compute a size (size * 24, a 24x
# size, a particular size, a block size / a particular allocation size)).
[entrypoint.functions."0x825BBAD0"]
name = "ProcessSizeCompute_825BBAD0"

# A process (a vector hash, with a vector operation and a counter increment, the caller of ComputeVectorHash_8222E6C0
# ): load vector constants (a vector constant, from the stack, and a vector constant, from obj->80, a particular value
# ), call a vector hash (ComputeVectorHash_8222E6C0, on the vectors, a SIMD computation, a particular hash / a
# particular comparison), and increment a counter (obj->172, a counter, a particular count / a particular statistic).

# Args: obj (r3, r31, ->80 = a vector, a particular value, ->172 = a counter, a particular count / a particular
# statistic), a (r4, a value), b (r5, a value), (more). It loads a float constant (from a fixed .data address, a magic
# constant, a particular value). It loads a vector constant (from the stack, a vector constant, a particular value: lvl
# x v0, r10, a vector constant, from the stack, and vspltw v2, v0, 0, a vector splat, a particular value). It loads a
# vector constant (from obj->80, a vector constant, a particular value: lvx128 v1, r31 + 80, a vector constant, from obj
# , a particular value). It calls a vector hash (ComputeVectorHash_8222E6C0, sub_8222E6C0, a vector hash/computation,
# on the vectors (v2, a vector constant, from the stack, and v1, a vector constant, from obj), a SIMD computation, a
# particular hash / a particular comparison, a particular vector operation). It increments a counter (obj->172, a
# counter, a particular count / a particular statistic: it reads the counter (r11, obj->172, a counter, a particular
# count), and stores it (r11 + 1, an increment, a particular count, a particular statistic)). So it's a "process (a
# vector hash: load vector constants (a vector constant, from the stack, and a vector constant, from obj->80, a
# particular value), call a vector hash (ComputeVectorHash_8222E6C0, on the vectors, a SIMD computation, a particular
# hash / a particular comparison), and increment a counter (obj->172, a counter, a particular count / a particular
# statistic))" routine (a vector hash process, part of the 0x82A0B cluster, a caller of ComputeVectorHash_8222E6C0). 74
# lines, 1 callee (sub_8222E6C0, ComputeVectorHash_8222E6C0, a vector hash/computation). 4 callers: sub_8321ED18, sub
# _8321F668, sub_8321F790, sub_8321FC38 (siblings in the 0x8321E/0x8321F clusters). The object's class (obj, ->80 = a
# vector, a particular value, ->172 = a counter, a particular count / a particular statistic, a hash / a value object)
# and the value's meaning (a value, a particular argument) aren't resolvable, so the name states the mechanism: a
# vector hash process (load vector constants (a vector constant, from the stack, and a vector constant, from obj->80, a
# particular value), call a vector hash (ComputeVectorHash_8222E6C0, on the vectors), and increment a counter (obj
# ->172, a counter, a particular count / a particular statistic)).
[entrypoint.functions."0x82A0B288"]
name = "ProcessVectorHash_82A0B288"

# A process (a copy, with a 24-byte element stride, the callee of ProcessFloat_83234418 / the sibling of CopyRecords
# 20_82A13138): (if a size, obj->4, is 0, a null state) take one path, and (otherwise) compute a count (a 24-byte
# element stride, (obj->12 - obj->4) / 24, a size, a 24-byte element, a particular count), and (if a count (obj->8 -
# obj->4) / 24 is >= the first count, a below/above-threshold check) take one path, and (otherwise) copy a 24-byte
# element (r6->0, a value, to obj->8, a base, a 24-byte element, a particular copy).

# Args: obj (r3, ->4 = a size, ->8 = a base, ->12 = a base, a particular structure), index (r4, r6, ->0 = a value, ->4
# = a float, a 24-byte element, a particular value), (more). It checks a size (obj->4, a size, a particular size / a
# particular count): if it is 0 (a null state, a particular invalid state), it takes one path (a no-op / a return, a
# particular state). Otherwise (a valid size), it computes a count (a 24-byte element stride, (obj->12 - obj->4, a
# size, a 24-byte element, a particular count), divided by 24 (divw r9, r9, r8, a divide, a 24-byte element stride, a
# particular count), a count, a particular number of 24-byte elements). It computes another count (a 24-byte element
# stride, (obj->8 - obj->4, a size, a 24-byte element, a particular count), divided by 24 (divw r8, r10, r8, a divide,
# a 24-byte element stride, a particular count), a count, a particular number of 24-byte elements). If the second count
# is >= the first count (a below/above-threshold check, a particular range, a particular size), it takes one path (a
# particular state, a particular result). Otherwise (a valid range, a particular size), it copies a 24-byte element (r6
# ->0, a value, a 24-byte element, a particular value, to obj->8, a base, a 24-byte element, a particular copy: stw r10
# , 0, r11, a store, a 24-byte element, a particular copy, and lfs f0, 4, r6, a float, a 24-byte element, a particular
# value, and a store, a 24-byte element, a particular copy). So it's a "process (a copy: (if a size, obj->4, is 0, a
# null state) take one path, and (otherwise) compute a count (a 24-byte element stride, (obj->12 - obj->4) / 24, a
# size, a 24-byte element, a particular count), and (if a count (obj->8 - obj->4) / 24 is >= the first count, a
# below/above-threshold check) take one path, and (otherwise) copy a 24-byte element (r6->0, a value, to obj->8, a
# base, a 24-byte element, a particular copy))" routine (a copy process, part of the 0x82A12 cluster, a callee of
# ProcessFloat_83234418, a sibling of CopyRecords20_82A13138). 128 lines, 1 callee (sub_82A12EC0, a sibling in the
# 0x82A12 cluster). 3 callers: ProcessFloat_83234418 (sub_83234418, a float process), sub_83233268, sub_83233B40
# (siblings in the 0x83233 cluster). The object's class (obj, ->4 = a size, ->8 = a base, ->12 = a base, a particular
# structure, a range / a copy object) and the 24-byte element's meaning (a 24-byte element, a particular value, a
# particular quantity) aren't resolvable, so the name states the mechanism: a copy process ((if a size, obj->4, is 0, a
# null state) take one path, and (otherwise) compute a count (a 24-byte element stride, (obj->12 - obj->4) / 24, a size
# , a 24-byte element, a particular count), and (if a count (obj->8 - obj->4) / 24 is >= the first count) take one
# path, and (otherwise) copy a 24-byte element (r6->0, a value, to obj->8, a base, a 24-byte element, a particular
# copy)).
[entrypoint.functions."0x82A12D68"]
name = "ProcessCopyElement_82A12D68"

# A process (a copy loop, with a 20-byte element, the callee of sub_82A12EC0 / the sibling of CopyRecords20_82A13138
# ): (if a count is 0, a null state) take one path, and (otherwise) loop (a copy loop, with a counter, a 20-byte
# element count, copying a 20-byte element (a value, a float, a float, a float, four bytes, a particular 20-byte
# element) from a source (r6, a source, a 20-byte element, a particular value) to a base (a, a base, a 20-byte element,
# a particular target), advancing to the next 20-byte element, a particular loop).

# Args: obj (r3, an object), a (r4, r11, a base, a 20-byte element, a particular target), count (r5, a count, a 20-byte
# element count, a particular count), source (r6, ->0 = a value, ->4 = a float, ->8 = a float, ->12 = a float, ->16 = a
# byte, ->17 = a byte, ->18 = a byte, ->19 = a byte, a 20-byte element, a particular source), (more). It checks a count
# (r5, a count, a 20-byte element count, a particular count): if it is 0 (a null state, a particular invalid state), it
# takes one path (a no-op / a return, a particular state). Otherwise (a valid count), it loops (a copy loop, with a
# counter, a 20-byte element count: it computes a counter (addic. r10, r11, -8, a counter, a 20-byte element count, a
# particular count), and (if the counter is 0, a particular end, a particular state) takes one path (a no-op / a
# return, a particular state). Otherwise (a valid count, a particular count), it copies a 20-byte element (r6->0, a
# value, a 20-byte element, a particular value, to a, a base, a 20-byte element, a particular target: stw r10, -8, r11
# , a store, a 20-byte element, a particular copy, and lfs f0, 4, r6, a float, a 20-byte element, a particular value,
# and a store, a 20-byte element, a particular copy, and lfs f13, 8, r6, a float, a 20-byte element, a particular
# value, and a store, a 20-byte element, a particular copy, and lfs f12, 12, r6, a float, a 20-byte element, a
# particular value, and a store, a 20-byte element, a particular copy, and lbz r9, 16, r6, a byte, a 20-byte element,
# a particular value, and a store, a 20-byte element, a particular copy, and lbz r8, 17, r6, a byte, a 20-byte element,
# a particular value, and a store, a 20-byte element, a particular copy, and lbz r7, 18, r6, a byte, a 20-byte element
# , a particular value, and a store, a 20-byte element, a particular copy, and lbz r3, 19, r6, a byte, a 20-byte
# element, a particular value, and a store, a 20-byte element, a particular copy), advancing to the next 20-byte
# element, a particular loop). So it's a "process (a copy loop: (if a count is 0, a null state) take one path, and
# (otherwise) loop (a copy loop, with a counter, a 20-byte element count, copying a 20-byte element (a value, a float,
# a float, a float, four bytes, a particular 20-byte element) from a source (r6, a source, a 20-byte element, a
# particular value) to a base (a, a base, a 20-byte element, a particular target), advancing to the next 20-byte
# element))" routine (a copy loop process, part of the 0x82A12 cluster, a callee of sub_82A12EC0, a sibling of
# CopyRecords20_82A13138). 80 lines, no direct callees (pure data manipulation). 1 caller: sub_82A12EC0 (a sibling in
# the 0x82A12 cluster, a callee of ProcessCopyElement_82A12D68, sub_82A12D68, a copy process, which calls this to copy
# the elements). The object's class (obj, an object, a particular structure, a copy / a range object) and the 20-byte
# element's meaning (a 20-byte element, a particular value, a particular quantity) aren't resolvable, so the name
# states the mechanism: a copy loop process ((if a count is 0, a null state) take one path, and (otherwise) loop (a copy
# loop, with a counter, a 20-byte element count, copying a 20-byte element (a value, a float, a float, a float, four
# bytes, a particular 20-byte element) from a source (r6, a source, a 20-byte element, a particular value) to a base (a
# , a base, a 20-byte element, a particular target), advancing to the next 20-byte element)).
[entrypoint.functions."0x82A12E40"]
name = "CopyElement20_82A12E40"

# A process (a copy/grow, with a 20-byte element and a 24-byte stride, the callee of ProcessCopyElement_82A12D68 / the
# caller of CopyElement20_82A12E40): copy a 20-byte element (a value, a float, a float, a float, four bytes, a
# particular 20-byte element, from a source (r6, a source, a 20-byte element, a particular value) to the stack, a
# particular copy), and perform a complex computation (a 24-byte element stride, a grow, calling CopyElement20
# _82A12E40, a copy loop; CopyRecords20_82A13138, a copy; Free_SizeBucketed_Thunk_8221BE68, a free; ProcessSizeCompute
# _825BBAD0, a size computation; sub_82A131B8, sub_82A97648 -- siblings in the 0x82A13/0x82A97 clusters).

# Args: obj (r3, r31, ->4 = a size, a particular structure), a (r4, a value), source (r6, ->0 = a value, ->4 = a float,
# ->8 = a float, ->12 = a float, ->16 = a byte, ->17 = a byte, ->18 = a byte, ->19 = a byte, ->20 = a byte, a 20-byte
# element, a particular source), (more). It copies a 20-byte element (a value (r10, r6->0, a value, a 20-byte element,
# a particular value), a float (f0, r6->4, a float, a 20-byte element, a particular value), a float (f13, r6->8, a
# float, a 20-byte element, a particular value), a float (f12, r6->12, a float, a 20-byte element, a particular value)
# , four bytes (r9, r6->16, a byte, a 20-byte element, a particular value; r8, r6->17, a byte, a 20-byte element, a
# particular value; r7, r6->18, a byte, a 20-byte element, a particular value; r5, r6->19, a byte, a 20-byte element, a
# particular value; r4, r6->20, a byte, a 20-byte element, a particular value), from a source (r6, a source, a 20-byte
# element, a particular value) to the stack (a particular copy: stw r10, 80, r1, a store, a 20-byte element, a
# particular copy, and stfs f0, 84, r1, a store, a 20-byte element, a particular copy, and stfs f13, 88, r1, a store, a
# 20-byte element, a particular copy, and stfs f12, 92, r1, a store, a 20-byte element, a particular copy, and stb r9,
# 96, r1, a store, a 20-byte element, a particular copy). It performs a complex computation (a 24-byte element stride, a
# grow, a particular size / a particular allocation, calling CopyElement20_82A12E40, sub_82A12E40, a copy loop; CopyRe
# cords20_82A13138, sub_82A13138, a copy; Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a free; ProcessSizeCompute
# _825BBAD0, sub_825BBAD0, a size computation; sub_82A131B8, sub_82A97648 -- siblings in the 0x82A13/0x82A97 clusters
# , a particular copy/grow, a particular size / a particular allocation). So it's a "process (a copy/grow: copy a 20-
# byte element (a value, a float, a float, a float, four bytes, a particular 20-byte element, from a source (r6, a
# source, a 20-byte element, a particular value) to the stack, a particular copy), and perform a complex computation
# (a 24-byte element stride, a grow, calling CopyElement20_82A12E40, a copy loop; CopyRecords20_82A13138, a copy;
# Free_SizeBucketed_Thunk_8221BE68, a free; ProcessSizeCompute_825BBAD0, a size computation; sub_82A131B8, sub
# _82A97648))" routine (a copy/grow process, part of the 0x82A12 cluster, a callee of ProcessCopyElement_82A12D68, a
# caller of CopyElement20_82A12E40). 365 lines, 6 callees (CopyElement20_82A12E40, CopyRecords20_82A13138,
# Free_SizeBucketed_Thunk_8221BE68, ProcessSizeCompute_825BBAD0, sub_82A131B8, sub_82A97648). 1 caller:
# ProcessCopyElement_82A12D68 (sub_82A12D68, a copy process, which calls this to copy and grow the elements). The
# object's class (obj, ->4 = a size, a particular structure, a range / a copy object) and the 20-byte element's meaning
# (a 20-byte element, a particular value, a particular quantity) aren't resolvable, so the name states the mechanism:
# a copy/grow process (copy a 20-byte element (a value, a float, a float, a float, four bytes, a particular 20-byte
# element, from a source (r6, a source, a 20-byte element, a particular value) to the stack, a particular copy), and
# perform a complex computation (a 24-byte element stride, a grow, calling CopyElement20_82A12E40, a copy loop;
# CopyRecords20_82A13138, a copy; Free_SizeBucketed_Thunk_8221BE68, a free; ProcessSizeCompute_825BBAD0, a size
# computation)).
[entrypoint.functions."0x82A12EC0"]
name = "ProcessCopyGrow_82A12EC0"

# A process (a grow/append, with a 24-byte element stride and a copy, the caller of sub_8323FF48 / the sibling of
# ProcessCopyGrow_82A12EC0): (if a size, obj->100, is >= 4, a particular threshold, a particular size) return 0, a
# failure / a particular state, and (otherwise) grow/append (a 24-byte element stride, a copy, a particular size / a
# particular allocation, copying a 24-byte element (a value, a float, a float, a float, four bytes, a particular 20-byte
# element) from a source (r4, a source, a 20-byte element, a particular value) to a base (obj, a base, a 24-byte element
# , a particular target), advancing to the next 24-byte element, a particular grow/append).

# Args: obj (r3, r10, ->100 = a size, a particular structure, a range / a grow object), source (r4, ->0 = a value, ->4
# = a float, ->8 = a float, ->12 = a float, ->16 = a byte, a 20-byte element, a particular source), (more). It checks a
# size (obj->100, a size, a particular size / a particular count): if it is >= 4 (a particular threshold, a particular
# size, a particular count, a particular state), it returns 0 (a failure / a particular state, a particular result). If
# it is < 4 (a particular threshold, a particular size, a particular count, a particular state), it grows/appends (a 24
# -byte element stride, a copy, a particular size / a particular allocation: it computes a size (r9, obj->100 * 2, a
# size, a 2-byte element stride, a particular count), and a base (r7, obj->100 + r9, a size, a 3x size, a particular
# count), and a pointer (r11, r7 * 8, a size, a 24-byte element stride, a particular count, a base, a particular target
# ), and (r11 + r10, a base, a particular target), and copies a 24-byte element (r4->0, a value, a 20-byte element, a
# particular value, to obj, a base, a 24-byte element, a particular target: stw r8, 4, r11, a store, a 24-byte element
# , a particular copy, and lfs f0, 4, r4, a float, a 20-byte element, a particular value, and a store, a 24-byte
# element, a particular copy, and lfs f13, 8, r4, a float, a 20-byte element, a particular value, and a store, a 24-byte
# element, a particular copy, and lfs f12, 12, r4, a float, a 20-byte element, a particular value, and a store, a 24-byte
# element, a particular copy, and lbz r6, 16, r4, a byte, a 20-byte element, a particular value, and a store, a 24-byte
# element, a particular copy), advancing to the next 24-byte element, a particular grow/append). So it's a "process (a
# grow/append: (if a size, obj->100, is >= 4, a particular threshold, a particular size) return 0, a failure / a
# particular state, and (otherwise) grow/append (a 24-byte element stride, a copy, a particular size / a particular
# allocation, copying a 24-byte element (a value, a float, a float, a float, four bytes, a particular 20-byte element)
# from a source (r4, a source, a 20-byte element, a particular value) to a base (obj, a base, a 24-byte element, a
# particular target), advancing to the next 24-byte element))" routine (a grow/append process, part of the 0x82BE4
# cluster, a sibling of ProcessCopyGrow_82A12EC0). 81 lines, no direct callees (pure data manipulation). 3 callers: sub
# _8323FF48, sub_8323FA78, sub_8323FCA8 (siblings in the 0x8323F cluster). The object's class (obj, ->100 = a size, a
# particular structure, a range / a grow object) and the 20-byte element's meaning (a 20-byte element, a particular
# value, a particular quantity) aren't resolvable, so the name states the mechanism: a grow/append process ((if a size,
# obj->100, is >= 4, a particular threshold, a particular size) return 0, a failure / a particular state, and
# (otherwise) grow/append (a 24-byte element stride, a copy, a particular size / a particular allocation, copying a 24-
# byte element (a value, a float, a float, a float, four bytes, a particular 20-byte element) from a source (r4, a
# source, a 20-byte element, a particular value) to a base (obj, a base, a 24-byte element, a particular target),
# advancing to the next 24-byte element)).
[entrypoint.functions."0x82BE4AA8"]
name = "ProcessGrowAppend_82BE4AA8"

# A process (a grow/append, with a byte-by-byte copy and a 4-byte element, the sibling of ProcessGrowAppend_82BE4AA8
# / the caller of sub_82A1B480): (if a size, obj->20, is >= 4, a particular threshold, a particular size) grow/append
# (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte element (four
# bytes, a particular 4-byte element) from a base (obj->12, a base, a 4-byte element, a particular source) to a stack
# (a particular target), advancing to the next 4-byte element, a particular grow/append), and (in a path) call a
# process (sub_82A1B480, on a value, a particular operation).

# Args: obj (r3, r31, ->4 = a base, ->12 = a base, ->20 = a size, a particular structure, a range / a grow object), value
# (r4, r30, a value, a particular argument), (more). It checks a size (obj->20, a size, a particular size / a particular
# count): if it is >= 4 (a particular threshold, a particular size, a particular count, a particular state), it
# grows/appends (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation: it computes a size
# (r9, obj->20 - 4, a size, a 4-byte element, a particular count), and a base (r11, obj->12, a base, a 4-byte element,
# a particular source), and (r7, r11 + 4, a base, a 4-byte element, a particular source), and copies a 4-byte element
# (four bytes, a particular 4-byte element: lbz r5, 0, r11, a byte, a 4-byte element, a particular value, and stb r5, 0,
# r8, a store, a 4-byte element, a particular copy, and lbz r4, 1, r11, a byte, a 4-byte element, a particular value, and
# stb r4, 1, r8, a store, a 4-byte element, a particular copy, and lbz r3, 2, r11, a byte, a 4-byte element, a
# particular value, and stb r3, 2, r8, a store, a 4-byte element, a particular copy, and lbz r11, 3, r11, a byte, a 4-byte
# element, a particular value, and stb r11, 3, r8, a store, a 4-byte element, a particular copy), advancing to the next
# 4-byte element, a particular grow/append). If it is < 4 (a particular threshold, a particular size, a particular count
# , a particular state), it takes one path (a no-op / a return, a particular state). In a path it calls a process (sub
# _82A1B480, on a value, a particular operation, a particular grow/append, a particular size / a particular allocation).
# So it's a "process (a grow/append: (if a size, obj->20, is >= 4, a particular threshold, a particular size) grow
# /append (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte element
# (four bytes, a particular 4-byte element) from a base (obj->12, a base, a 4-byte element, a particular source) to a
# stack (a particular target), advancing to the next 4-byte element), and (in a path) call a process (sub_82A1B480, on a
# value))" routine (a grow/append process, part of the 0x8321F cluster, a sibling of ProcessGrowAppend_82BE4AA8, a
# caller of sub_82A1B480). 290 lines, 1 callee (sub_82A1B480, a sibling in the 0x82A1B cluster). 3 callers: sub
# _8321F548, sub_83225760, sub_83225600 (siblings in the 0x8321F/0x83225 clusters). The object's class (obj, ->4 = a
# base, ->12 = a base, ->20 = a size, a particular structure, a range / a grow object) and the 4-byte element's meaning
# (a 4-byte element, a particular value, a particular quantity) aren't resolvable, so the name states the mechanism: a
# grow/append process ((if a size, obj->20, is >= 4, a particular threshold, a particular size) grow/append (a byte-by-
# byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte element (four bytes, a
# particular 4-byte element) from a base (obj->12, a base, a 4-byte element, a particular source) to a stack (a
# particular target), advancing to the next 4-byte element), and (in a path) call a process (sub_82A1B480, on a value)
# ). The "2" distinguishes it from ProcessGrowAppend_82BE4AA8.
[entrypoint.functions."0x8321F328"]
name = "ProcessGrowAppend2_8321F328"

# A 32-byte constructor (the sibling of ConstructNode32_82BACFB0 / Construct32Byte_82B722E8, with a vector operation):
# initialize the object's fields (obj->4 = value, obj->8 = 0, obj->0 = a constant, a 32-byte object), and perform a
# vector operation (with a constant, a SIMD computation, calling ProcessVector2_8223BD98, a vector process, a
# particular vector operation).

# Args: obj (r3, r31, ->0 = a field, a constant, a class id / a marker, ->4 = a field, a value, ->8 = a field, a count / a
# size, a 32-byte object), value (r4, a value, a particular argument), (more). It initializes the object's fields (obj
# ->4 = value, a field, a value, obj->8 = 0, a field, a count / a size, a zero, obj->0 = a constant, a class id / a
# marker, from a fixed .data address, a particular class / a particular marker). It performs a vector operation (with a
# constant, a SIMD computation: it loads a vector constant (lvx128 v0, r9, a vector constant, from a fixed .data
# address, a particular vector constant), and a float constant (f0, a constant, from a fixed .data address, a
# particular value), and (a vector operation, with a constant, a SIMD computation, calling ProcessVector2_8223BD98, sub
# _8223BD98, a vector process, a particular vector operation). So it's a "construct a 32-byte object (initialize the
# fields, obj->4 = value, obj->8 = 0, obj->0 = a constant, a 32-byte object, and perform a vector operation (with a
# constant, a SIMD computation, calling ProcessVector2_8223BD98, a vector process, a particular vector operation))"
# routine (a 32-byte constructor, with a vector operation, part of the 0x8321F cluster, a sibling of ConstructNode32
# _82BACFB0). 132 lines, 1 callee (ProcessVector2_8223BD98, sub_8223BD98, a vector process). 9 callers: sub_8323C7F0,
# sub_8323BC80, sub_8323AA90, sub_83216720, sub_83217228, sub_8323BCC8, sub_8323B220, sub_8323F1B8, sub_83220478
# (siblings in the 0x8323C/0x8323B/0x8323A/0x83216/0x83217/0x8323F/0x83220 clusters). The object's class (obj, ->0 = a
# field, a constant, a class id / a marker, ->4 = a field, a value, ->8 = a field, a count / a size, a 32-byte object, a
# particular structure) and the value's meaning (a value, a particular argument) aren't resolvable, so the name states
# the mechanism: a 32-byte constructor (with a vector operation) (initialize the fields, obj->4 = value, obj->8 = 0,
# obj->0 = a constant, a 32-byte object, and perform a vector operation (with a constant, a SIMD computation, calling
# ProcessVector2_8223BD98, a vector process)). The "Vector" refers to the vector operation (a SIMD computation, a
# particular vector operation).
[entrypoint.functions."0x8321FB58"]
name = "Construct32ByteVector_8321FB58"

# A process (a grow/append, with a byte-by-byte copy and a 4-byte element, the sibling of ProcessGrowAppend2_8321F328
# / ProcessGrowAppend_82BE4AA8, the caller of ProcessVectorHash_82A0B288 / CopyStructData_83235608 / CopyStructData4_8
# 2A1BEA8): (if a size, value->20, is >= 4, a particular threshold, a particular size) grow/append (a byte-by-byte
# copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte element (four bytes, a
# particular 4-byte element) from a base (value->12, a base, a 4-byte element, a particular source) to a stack (a
# particular target), advancing to the next 4-byte element, a particular grow/append), and (in paths) call a vector
# hash (ProcessVectorHash_82A0B288, on a value, a SIMD computation, a particular hash / a particular comparison) and
# copy (CopyStructData_83235608, CopyStructData4_82A1BEA8, a struct data copy, a particular copy).

# Args: obj (r3, r30, an object), value (r4, r31, ->4 = a base, ->12 = a base, ->20 = a size, a particular structure, a
# range / a grow object), a (r5, r28, a value), b (r6, r27, a value), (more). It checks a size (value->20, a size, a
# particular size / a particular count): if it is >= 4 (a particular threshold, a particular size, a particular count, a
# particular state), it grows/appends (a byte-by-byte copy, a 4-byte element, a particular size / a particular
# allocation: it computes a size (r9, value->20 - 4, a size, a 4-byte element, a particular count), and a base (r11,
# value->12, a base, a 4-byte element, a particular source), and (r7, r11 + 4, a base, a 4-byte element, a particular
# source), and copies a 4-byte element (four bytes, a particular 4-byte element: lbz r5, 0, r11, a byte, a 4-byte
# element, a particular value, and stb r5, 0, r8, a store, a 4-byte element, a particular copy, and lbz r4, 1, r11, a
# byte, a 4-byte element, a particular value, and stb r4, 1, r8, a store, a 4-byte element, a particular copy, and lbz
# r3, 2, r11, a byte, a 4-byte element, a particular value, and stb r3, 2, r8, a store, a 4-byte element, a particular
# copy, and lbz r11, 3, r11, a byte, a 4-byte element, a particular value, and stb r11, 3, r8, a store, a 4-byte
# element, a particular copy), advancing to the next 4-byte element, a particular grow/append). In paths it calls a
# vector hash (ProcessVectorHash_82A0B288, sub_82A0B288, a vector hash, on a value, a SIMD computation, a particular
# hash / a particular comparison, a particular vector operation) and copies (CopyStructData_83235608, sub_83235608, a
# struct data copy, on a value, a particular copy, and CopyStructData4_82A1BEA8, sub_82A1BEA8, a struct data copy, on a
# value, a particular copy, a particular size / a particular allocation). So it's a "process (a grow/append: (if a
# size, value->20, is >= 4, a particular threshold, a particular size) grow/append (a byte-by-byte copy, a 4-byte
# element, a particular size / a particular allocation, copying a 4-byte element (four bytes, a particular 4-byte
# element) from a base (value->12, a base, a 4-byte element, a particular source) to a stack (a particular target),
# advancing to the next 4-byte element), and (in paths) call a vector hash (ProcessVectorHash_82A0B288, on a value, a
# SIMD computation) and copy (CopyStructData_83235608, CopyStructData4_82A1BEA8, a struct data copy, a particular
# copy))" routine (a grow/append process, part of the 0x8321F cluster, a sibling of ProcessGrowAppend2_8321F328, a
# caller of ProcessVectorHash_82A0B288, CopyStructData_83235608, and CopyStructData4_82A1BEA8). 815 lines, 4 callees
# (CopyStructData4_82A1BEA8, CopyStructData_83235608, ProcessVectorHash_82A0B288, sub_82A1B480). 7 callers: sub
# _8323F5B0, sub_8323C0C0, sub_8323CB98, sub_83216BB0, sub_83217688, sub_8323AE00, sub_8323B650 (siblings in the
# 0x8323F/0x8323C/0x83216/0x83217/0x8323A/0x8323B clusters). The object's class (obj, an object, a particular
# structure, a range / a grow object) and the 4-byte element's meaning (a 4-byte element, a particular value, a
# particular quantity) aren't resolvable, so the name states the mechanism: a grow/append process ((if a size, value
# ->20, is >= 4, a particular threshold, a particular size) grow/append (a byte-by-byte copy, a 4-byte element, a
# particular size / a particular allocation, copying a 4-byte element (four bytes, a particular 4-byte element) from a
# base (value->12, a base, a 4-byte element, a particular source) to a stack (a particular target), advancing to the
# next 4-byte element), and (in paths) call a vector hash (ProcessVectorHash_82A0B288, on a value, a SIMD computation)
# and copy (CopyStructData_83235608, CopyStructData4_82A1BEA8, a struct data copy, a particular copy)). The "3"
# distinguishes it from ProcessGrowAppend2_8321F328 and ProcessGrowAppend_82BE4AA8.
[entrypoint.functions."0x8321FC38"]
name = "ProcessGrowAppend3_8321FC38"

# A process (a state dispatch, with a switch, the sibling of StateDispatch3_82BCD9E8 / StateDispatch2_82BCD7B0):
# initialize the object's fields (obj->0/->4 = 0, two zeros, a particular initialization), and (if a state is <= 9, a
# particular range, a particular state) perform a state dispatch (a switch, over 0-9 states, a particular state
# dispatch, a particular state / a particular transition), and (in paths) call sibling operations (sub_823BE9C0, sub
# _82A0C2E0, sub_82A0C960, sub_82A0CFC8, sub_82A0D630, sub_832167D0, sub_832172A8, sub_83220478, sub_8323B270, sub
# _8323BCC8, sub_8323E260, sub_8323F238 -- siblings in the 0x823BE/0x82A0C/0x82A0D/0x83216/0x83217/0x83220/0x8323B/0
# x8323E/0x8323F clusters, a particular state / a particular transition).

# Args: obj (r3, r31, ->0 = a field, a particular value, ->4 = a field, a particular value, a particular structure),
# state (r4, r11, a state, a particular state / a particular value), a (r5, a value, a particular argument), (more). It
# initializes the object's fields (obj->0 = 0, a field, a particular value, obj->4 = 0, a field, a particular value, two
# zeros, a particular initialization). It checks a state (r4, r11, a state, a particular state / a particular value): if
# it is <= 9 (a particular range, a particular state, a particular value), it performs a state dispatch (a switch, over
# 0-9 states, a particular state dispatch, a particular state / a particular transition: case 0, a particular state, a
# particular transition; case 1, a particular state, a particular transition; case 2, a particular state, a particular
# transition; case 3, a particular state, a particular transition; case 4, a particular state, a particular transition;
# case 5, a particular state, a particular transition; case 6, a particular state, a particular transition; case 7, a
# particular state, a particular transition; case 8, a particular state, a particular transition; case 9, a particular
# state, a particular transition). If the state is > 9 (a particular range, a particular state, a particular value), it
# takes one path (a no-op / a return, a particular state). In paths it calls sibling operations (sub_823BE9C0, sub
# _82A0C2E0, sub_82A0C960, sub_82A0CFC8, sub_82A0D630, sub_832167D0, sub_832172A8, sub_83220478, sub_8323B270, sub
# _8323BCC8, sub_8323E260, sub_8323F238 -- siblings in the 0x823BE/0x82A0C/0x82A0D/0x83216/0x83217/0x83220/0x8323B/0
# x8323E/0x8323F clusters, a particular state / a particular transition, a particular operation). So it's a "process (a
# state dispatch: initialize the object's fields (obj->0/->4 = 0, two zeros, a particular initialization), and (if a
# state is <= 9, a particular range, a particular state) perform a state dispatch (a switch, over 0-9 states, a
# particular state dispatch, a particular state / a particular transition), and (in paths) call sibling operations)"
# routine (a state dispatch process, part of the 0x83220 cluster, a sibling of StateDispatch3_82BCD9E8). 599 lines, 13
# callees (Release_RefCounted_829FF648, sub_823BE9C0, sub_82A0C2E0, sub_82A0C960, sub_82A0CFC8, sub_82A0D630, sub
# _832167D0, sub_832172A8, sub_83220478, sub_8323B270, sub_8323BCC8, sub_8323E260, sub_8323F238). 3 callers: sub
# _83225760, sub_832256A0, sub_83225600 (siblings in the 0x83225 cluster). The object's class (obj, ->0 = a field, a
# particular value, ->4 = a field, a particular value, a particular structure, a state / a value object) and the
# state's meaning (a state, a particular state / a particular value) aren't resolvable, so the name states the
# mechanism: a state dispatch process (initialize the object's fields (obj->0/->4 = 0, two zeros, a particular
# initialization), and (if a state is <= 9, a particular range, a particular state) perform a state dispatch (a switch,
# over 0-9 states, a particular state dispatch, a particular state / a particular transition), and (in paths) call
# sibling operations). The "4" distinguishes it from StateDispatch3_82BCD9E8 and StateDispatch2_82BCD7B0.
[entrypoint.functions."0x83220888"]
name = "StateDispatch4_83220888"

# A process (a reference count, with a release and an increment, the caller of Release_RefCounted_829FF648 /
# LookupAndAssign_8323A1E8): (if a value, obj->4, is non-zero, a particular value) release a refcounted value
# (Release_RefCounted_829FF648, on a value, a particular release), and increment a reference count (a value (obj->4, a
# value, a particular value), a reference count, a particular count), and (otherwise) take one path, and call a lookup
# (LookupAndAssign_8323A1E8, on a value and a constant, a particular lookup).

# Args: obj (r3, ->0 = a value, ->4 = a value, a reference count, a particular structure), (more). It checks a value
# (obj->4, a value, a reference count, a particular value): if it is non-zero (a particular value, a particular state),
# it releases a refcounted value (Release_RefCounted_829FF648, sub_829FF648, a refcounted release, on a value (stack
# [80], a value, a particular value), a particular release, a particular count), and increments a reference count (a
# value (r31, obj->4, a value, a reference count, a particular value), a reference count, a particular count: it reads
# the count (r11, r31->0, a reference count, a particular count), and stores it (r11 + 1, an increment, a particular
# count, a particular reference count). If it is zero (a null state, a particular state), it takes one path (a no-op /
# a return, a particular state). It calls a lookup (LookupAndAssign_8323A1E8, sub_8323A1E8, a lookup and assign, on a
# value (stack[80], a value, a particular value) and a constant (a constant, from a fixed .data address, a particular
# value, a particular key), a particular lookup, a particular assignment). So it's a "process (a reference count: (if a
# value, obj->4, is non-zero, a particular value) release a refcounted value (Release_RefCounted_829FF648, on a value,
# a particular release), and increment a reference count (a value (obj->4, a value, a reference count, a particular
# value), a reference count, a particular count), and (otherwise) take one path, and call a lookup (LookupAndAssign
# _8323A1E8, on a value and a constant, a particular lookup))" routine (a reference count process, part of the 0x83225
# cluster, a caller of Release_RefCounted_829FF648 and LookupAndAssign_8323A1E8). 73 lines, 2 callees
# (LookupAndAssign_8323A1E8, Release_RefCounted_829FF648). 10+ callers: sub_8323BCC8, sub_8323B270, sub_82A0C2E0, sub
# _832172A8, sub_82A0CFC8, sub_8323E260, sub_82A0C960, sub_832167D0, sub_8323F238, sub_82A0D630 (a widely-called
# reference count process, part of the game's reference counting subsystem). The object's class (obj, ->0 = a value, ->
# 4 = a value, a reference count, a particular structure, a reference counted object) and the value's meaning (a value,
# a reference count, a particular value, a particular count) aren't resolvable, so the name states the mechanism: a
# reference count process ((if a value, obj->4, is non-zero, a particular value) release a refcounted value
# (Release_RefCounted_829FF648, on a value, a particular release), and increment a reference count (a value (obj->4, a
# value, a reference count, a particular value), a reference count, a particular count), and (otherwise) take one path,
# and call a lookup (LookupAndAssign_8323A1E8, on a value and a constant, a particular lookup)).
[entrypoint.functions."0x83225538"]
name = "ProcessRefCount_83225538"

# A getter (with a check, the sibling of GetSingleton_829F9090 / GetObjectAtOffset_821782D8): read a global (from a
# fixed .data address, a particular value, a particular singleton / a particular flag), and (if it is non-zero, a
# particular state) return another global (a flag, from a fixed .data address, a particular value, a particular flag /
# a particular state), and (otherwise) return 0, a zero / a particular state.

# No args. It reads a global (r11, from a fixed .data address, a particular value, a particular singleton / a
# particular flag). It checks the value: if it is non-zero (a particular state, a particular value), it returns another
# global (r3, from a fixed .data address, a particular value, a particular flag / a particular state, a particular
# value); if it is zero (a null state, a particular state), it returns 0 (a zero / a particular state, a particular
# value). So it's a "get a flag (with a check: read a global, and (if it is non-zero, a particular state) return
# another global (a flag, from a fixed .data address, a particular value, a particular flag / a particular state), and
# (otherwise) return 0)" routine (a getter, with a check, part of the 0x82226 cluster). 23 lines, no direct callees
# (pure data manipulation). 8 callers: sub_822FAD38, sub_82184D38, sub_8230C328, sub_82815E00, sub_8230BB78, sub
# _8217D1F8, sub_822D1410, sub_822C5390 (a widely-called getter, part of the game's subsystems). The global's meaning
# (a global, from a fixed .data address, a particular value, a particular singleton / a particular flag, a particular
# flag / a particular state) and the flag's meaning (a flag, from a fixed .data address, a particular value, a
# particular flag / a particular state) aren't resolvable, so the name states the mechanism: a getter (with a check) (a
# flag, from a fixed .data address, a particular value, a particular flag / a particular state, with a check, a
# particular state).
[entrypoint.functions."0x82226ED8"]
name = "GetFlag_82226ED8"

# A process (a dispatch, with a loop and a vcall, the caller of ProcessDispatchValue_8229A488 / HashTable_Find3_821CF1
# D0): loop over a list (a list, r27, a particular list / a particular sequence, a dispatch object), and for each item
# (a type, list->4, a type, a particular value, a particular item), (if the type is 5, a particular type, a particular
# state) dispatch a value (ProcessDispatchValue_8229A488, on a value, a particular dispatch, a particular value /
# a particular operation), and (if the result's type is non-zero, a particular state) take one path (a no-op / a return
# , a particular state), and (in paths) call a hash table lookup (HashTable_Find3_821CF1D0, on a value and a constant, a
# particular lookup, a particular key).

# Args: obj (r3, r31, an object), list (r4, r27, ->0 = a value, ->4 = a type, a particular list / a particular
# sequence, a dispatch object), a (r5, r26, a value, a particular argument), b (r6, r24, a value, a particular
# argument), (more). It loops over a list (r27, a list, a particular list / a particular sequence, a dispatch object, a
# particular count / a particular size): for each item (r11, list->4, a type, a particular value, a particular item), it
# checks the type: if it is 5 (a particular type, a particular state, a particular value), it dispatches a value
# (ProcessDispatchValue_8229A488, sub_8229A488, a dispatch, on a value (obj, r31, an object, a particular argument, a
# dispatch object), and a (r26, a value, a particular argument), and r29, list->0, a value, a particular argument, a
# particular dispatch, a particular value / a particular operation). If the result's type (r28, result, a type, a
# particular value, a particular state) is non-zero (a particular state, a particular value), it takes one path (a no-op
# / a return, a particular state). In paths it calls a hash table lookup (HashTable_Find3_821CF1D0, sub_821CF1D0, a
# hash table lookup, on a value and a constant, a particular lookup, a particular key, a particular value / a particular
# operation). So it's a "process (a dispatch, with a loop and a vcall: loop over a list (a list, r27, a particular list
# / a particular sequence, a dispatch object), and for each item (a type, list->4, a type, a particular value, a
# particular item), (if the type is 5, a particular type, a particular state) dispatch a value (ProcessDispatchValue
# _8229A488, on a value, a particular dispatch, a particular value / a particular operation), and (in paths) call a hash
# table lookup (HashTable_Find3_821CF1D0, on a value and a constant, a particular lookup))" routine (a dispatch loop
# process, part of the 0x8229A cluster, a caller of ProcessDispatchValue_8229A488 and HashTable_Find3_821CF1D0). 312
# lines, 7 callees (HashTable_Find3_821CF1D0, ProcessDispatchValue_8229A488, sub_8219EE00, sub_82BC9640, sub_82BC9860,
# sub_82BCCD58, sub_82BCD5E0). 10 callers: sub_82BBB3A8, sub_82BC7328, sub_82BB46A8, sub_82BB5C90, sub_82BC5E60, sub
# _82BC0210, sub_82BBFA28, sub_82BC5EB0, sub_82BC0A60, sub_822DF280 (siblings in the 0x82BBB/0x82BC7/0x82BB4/0x82BB5/0
# x82BC5/0x82BC0/0x82BBF/0x82BC5/0x82BC0/0x822DF clusters). The object's class (obj, an object, a particular structure,
# a dispatch object) and the list's meaning (a list, r27, a particular list / a particular sequence, a dispatch object)
# aren't resolvable, so the name states the mechanism: a dispatch loop process (loop over a list (a list, r27, a
# particular list / a particular sequence, a dispatch object), and for each item (a type, list->4, a type, a
# particular value, a particular item), (if the type is 5, a particular type, a particular state) dispatch a value
# (ProcessDispatchValue_8229A488, on a value, a particular dispatch, a particular value / a particular operation), and
# (in paths) call a hash table lookup (HashTable_Find3_821CF1D0, on a value and a constant, a particular lookup)).
[entrypoint.functions."0x8229A518"]
name = "ProcessDispatchLoop_8229A518"

# A process (a vcall, with a construct and a complex computation, the caller of ConstructRefCounted_8222CF18):
# perform a vcall (a method call, on a value, a particular vcall / a particular operation), construct a refcounted
# value (ConstructRefCounted_8222CF18, on a value, a particular construct / a particular allocation), and (in paths)
# perform a complex computation (a reference count, a particular count / a particular state, a particular size / a
# particular allocation).

# Args: obj (r3, r30, ->0 = a value, a particular structure, a refcounted object), a (r4, r31, ->0 = a vtable, ->28 = a
# value, a particular vcall object, a particular operation object), (more). It performs a vcall (a method call, on a
# value (a, r31, a particular vcall object, a particular operation object), a particular vcall / a particular operation
# : it loads a vtable (r11, a->0, a vtable, a particular vtable), and a method (r10, r11->8, a method, a particular
# method), and calls it (bctrl, a vcall, a method call, a particular vcall / a particular operation, a particular value
# / a particular operation)). It constructs a refcounted value (ConstructRefCounted_8222CF18, sub_8222CF18, a
# refcounted construct, on a value (obj, r30, a particular structure, a refcounted object), and a + result, a particular
# value / a particular allocation, and -1, a constant, a particular value, a particular construct / a particular
# allocation). In paths it performs a complex computation (a reference count, a particular count / a particular state,
# a particular size / a particular allocation: it reads a value (r11, obj->0, a value, a particular value), and (if it
# is non-zero, a particular state) reads another value (r11->4, a value, a particular value), and increments it (r29,
# r11 + 1, an increment, a particular count / a particular state, a particular reference count). So it's a "process (a
# vcall, with a construct and a complex computation: perform a vcall (a method call, on a value, a particular vcall /
# a particular operation), construct a refcounted value (ConstructRefCounted_8222CF18, on a value, a particular
# construct / a particular allocation), and (in paths) perform a complex computation (a reference count, a particular
# count / a particular state))" routine (a vcall+construct process, part of the 0x82A1D cluster, a caller of
# ConstructRefCounted_8222CF18). 98 lines, 1 callee (ConstructRefCounted_8222CF18, sub_8222CF18, a refcounted
# construct). 3 callers: sub_82B80D70, sub_82AAAC20, sub_82B28398 (siblings in the 0x82B80/0x82AAA/0x82B28 clusters)
# . The object's class (obj, r30, ->0 = a value, a particular structure, a refcounted object, a particular vcall
# object) and the value's meaning (a, r31, ->0 = a vtable, ->28 = a value, a particular vcall object, a particular
# operation object) aren't resolvable, so the name states the mechanism: a vcall+construct process (perform a vcall (a
# method call, on a value, a particular vcall / a particular operation), construct a refcounted value
# (ConstructRefCounted_8222CF18, on a value, a particular construct / a particular allocation), and (in paths) perform
# a complex computation (a reference count, a particular count / a particular state)).
[entrypoint.functions."0x82A1D5C8"]
name = "ProcessVcallConstruct_82A1D5C8"

# A process (a vcall, with a complex computation, the caller of BinarySearch_4Stride_82C40EC0): perform a vcall (a
# method call, on a value, a particular vcall / a particular operation), and (if the result is non-zero, a particular
# state) perform a complex computation (a binary search, a particular search / a particular lookup, calling
# BinarySearch_4Stride_82C40EC0, on a value and a constant, a particular search, a particular key), and (otherwise)
# return 0, a zero / a particular state.

# Args: a (r3, a value), obj (r4, ->0 = a vtable, a particular vcall object, a particular operation object), b (r5, a
# value), (more). It performs a vcall (a method call, on a value (obj, r4, a particular vcall object, a particular
# operation object), a particular vcall / a particular operation: it loads a vtable (r11, obj->0, a vtable, a
# particular vtable), and a method (r9, r11->4, a method, a particular method), and calls it (bctrl, a vcall, a
# method call, a particular vcall / a particular operation, a particular value / a particular operation)). If the
# result (r3, result, a value, a particular value, a particular state) is non-zero (a particular state, a particular
# value), it performs a complex computation (a binary search, a particular search / a particular lookup, calling
# BinarySearch_4Stride_82C40EC0, sub_82C40EC0, a binary search, on a value (r31, result + 4, a value, a particular
# value, a particular base) and a constant (a constant, from a fixed .data address, a particular value, a particular
# key), a particular search, a particular key, a particular size / a particular allocation). If the result is zero (a
# null state, a particular state), it returns 0 (a zero / a particular state, a particular value). So it's a "process
# (a vcall, with a complex computation: perform a vcall (a method call, on a value, a particular vcall / a particular
# operation), and (if the result is non-zero, a particular state) perform a complex computation (a binary search, a
# particular search / a particular lookup, calling BinarySearch_4Stride_82C40EC0, on a value and a constant, a
# particular search, a particular key), and (otherwise) return 0)" routine (a vcall process, part of the 0x82C26
# cluster, a caller of BinarySearch_4Stride_82C40EC0). 83 lines, 1 callee (BinarySearch_4Stride_82C40EC0, sub
# _82C40EC0, a binary search). 2 callers: sub_82C27538, sub_82C265A8 (siblings in the 0x82C27/0x82C26 clusters). The
# object's class (obj, r4, ->0 = a vtable, a particular vcall object, a particular operation object, a particular
# structure) and the value's meaning (a, r3, a value; b, r5, a value, a particular argument) aren't resolvable, so the
# name states the mechanism: a vcall process (perform a vcall (a method call, on a value, a particular vcall / a
# particular operation), and (if the result is non-zero, a particular state) perform a complex computation (a binary
# search, a particular search / a particular lookup, calling BinarySearch_4Stride_82C40EC0, on a value and a constant,
# a particular search, a particular key), and (otherwise) return 0).
[entrypoint.functions."0x82C26518"]
name = "ProcessVcallBinary_82C26518"

# A process (a vcall, with a complex computation, the sibling of ProcessVcallBinary_82C26518 / the caller of
# ProcessVcallBinary_82C26518): perform a vcall (a method call, on a value, a particular vcall / a particular
# operation, calling ProcessVcallBinary_82C26518, a vcall process, a particular vcall), and (in paths) perform a
# complex computation (a binary search, a particular search / a particular lookup, calling sub_82BFF710, sub_82BFF750,
# sub_82BFF7D8, sub_82BFF848, sub_82BFFAA8 -- siblings in the 0x82BFF cluster, a particular search / a particular
# lookup, a particular value / a particular operation).

# Args: a (r3, a value), obj (r4, r31, ->0 = a value, a particular vcall object, a particular operation object), b (r5,
# r30, a value), (more). It performs a vcall (a method call, on a value (obj, r4, r31, a particular vcall object, a
# particular operation object), a particular vcall / a particular operation, calling ProcessVcallBinary_82C26518, sub
# _82C26518, a vcall process, a particular vcall, a particular value / a particular operation). In paths it performs a
# complex computation (a binary search, a particular search / a particular lookup: it calls sub_82BFF710, on a value
# (stack[80], a value, a particular value, a particular stack), a particular operation; sub_82BFF7D8, on a value
# (stack[80], a value, a particular value, a particular stack) and a constant (a constant, from a fixed .data address,
# a particular value, a particular key), a particular operation; sub_82BFF848, on a value (stack[80], a value, a
# particular value, a particular stack) and b (r30, a value, a particular argument), a particular operation; sub
# _82BFF7D8, on a value (stack[80], a value, a particular value, a particular stack) and a constant (a constant, from a
# fixed .data address, a particular value, a particular key), a particular operation; sub_82BFF750, sub_82BFFAA8 --
# siblings in the 0x82BFF cluster, a particular search / a particular lookup, a particular value / a particular
# operation). So it's a "process (a vcall, with a complex computation: perform a vcall (a method call, on a value, a
# particular vcall / a particular operation, calling ProcessVcallBinary_82C26518, a vcall process, a particular
# vcall), and (in paths) perform a complex computation (a binary search, a particular search / a particular lookup,
# calling sub_82BFF710, sub_82BFF750, sub_82BFF7D8, sub_82BFF848, sub_82BFFAA8 -- siblings in the 0x82BFF cluster, a
# particular search / a particular lookup))" routine (a vcall process, part of the 0x82C26 cluster, a sibling of
# ProcessVcallBinary_82C26518, a caller of ProcessVcallBinary_82C26518). 97 lines, 6 callees (sub_82BFF710, sub
# _82BFF750, sub_82BFF7D8, sub_82BFF848, sub_82BFFAA8, sub_82C26518). No direct callers (it's called via a vtable / it
# is registered-but-uncalled, part of the game's subsystems). The object's class (obj, r4, r31, ->0 = a value, a
# particular vcall object, a particular operation object, a particular structure) and the value's meaning (a, r3, a
# value; b, r5, r30, a value, a particular argument) aren't resolvable, so the name states the mechanism: a vcall
# process (perform a vcall (a method call, on a value, a particular vcall / a particular operation, calling
# ProcessVcallBinary_82C26518, a vcall process, a particular vcall), and (in paths) perform a complex computation (a
# binary search, a particular search / a particular lookup, calling sub_82BFF710, sub_82BFF750, sub_82BFF7D8, sub
# _82BFF848, sub_82BFFAA8 -- siblings in the 0x82BFF cluster, a particular search / a particular lookup)). The "2"
# distinguishes it from ProcessVcallBinary_82C26518.
[entrypoint.functions."0x82C265A8"]
name = "ProcessVcallBinary2_82C265A8"

# A dispatch (a vcall, with a switch, the sibling of StateDispatch4_83220888 / StateDispatch3_82BCD9E8): (if a value,
# obj->0, is non-zero, a particular value) perform a vcall (a method call, on a value, a particular vcall / a
# particular operation), and (otherwise) perform a dispatch (a switch, over 6-7 states, a particular state dispatch, a
# particular state / a particular transition, calling a vcall (a method call, on a value, a particular vcall / a
# particular operation, based on a type (obj->10, a type, a particular value, a particular state))).

# Args: a (r3, a value), obj (r4, ->0 = a value, ->4 = a value, ->10 = a type, a particular dispatch object, a
# particular vcall object), (more). It checks a value (obj->0, a value, a particular value): if it is non-zero (a
# particular value, a particular state), it performs a vcall (a method call, on a value (r3, obj->0, a value, a
# particular value, a particular vcall object), a particular vcall / a particular operation: it loads a vtable (r11,
# r3->0, a vtable, a particular vtable), and a method (r10, r11->12, a method, a particular method), and calls it
# (bctrl, a vcall, a method call, a particular vcall / a particular operation, a particular value / a particular
# operation)). If it is zero (a null state, a particular state), it performs a dispatch (a switch, over 6-7 states, a
# particular state dispatch, a particular state / a particular transition: it checks a type (r11, obj->10, a type, a
# particular value, a particular state): if it is 6 (a particular state, a particular value, a particular type), it
# performs a vcall (a method call, on a value (r4, obj->4, a value, a particular value, a particular vcall object), a
# particular vcall / a particular operation); if it is 7 (a particular state, a particular value, a particular type), it
# performs another vcall (a method call, on a value, a particular vcall / a particular operation); otherwise it returns
# (a no-op / a return, a particular state). So it's a "dispatch (a vcall, with a switch: (if a value, obj->0, is non
# -zero, a particular value) perform a vcall (a method call, on a value, a particular vcall / a particular operation),
# and (otherwise) perform a dispatch (a switch, over 6-7 states, a particular state dispatch, a particular state / a
# particular transition, calling a vcall (a method call, on a value, a particular vcall / a particular operation, based
# on a type (obj->10, a type, a particular value, a particular state))))" routine (a vcall dispatch, part of the
# 0x82188 cluster, a sibling of StateDispatch4_83220888). 87 lines, no direct callees (pure vcall dispatch, using bctrl
# ). No direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (obj, r4, ->0 = a value, ->4 = a value, ->10 = a type, a particular dispatch object, a particular
# vcall object, a particular structure) and the value's meaning (a, r3, a value, a particular argument) aren't
# resolvable, so the name states the mechanism: a vcall dispatch ((if a value, obj->0, is non-zero, a particular value)
# perform a vcall (a method call, on a value, a particular vcall / a particular operation), and (otherwise) perform a
# dispatch (a switch, over 6-7 states, a particular state dispatch, a particular state / a particular transition,
# calling a vcall (a method call, on a value, a particular vcall / a particular operation, based on a type (obj->10, a
# type, a particular value, a particular state)))).
[entrypoint.functions."0x82188680"]
name = "DispatchVcallType_82188680"

# A compute (a complex computation, with a count and a constant, the sibling of DispatchVcallType_82188680):
# (if a count, obj->9, is zero, a particular count / a particular state) return (a no-op / a return, a particular
# state), and (otherwise) compute a complex computation (a count, obj->9, a count, a particular count / a particular
# value, a constant, from a fixed .data address, a particular value, a particular result, a particular size / a
# particular allocation, using a base (obj->4, a base, a particular value, a particular source), and a byte (obj->4->31
# , a byte, a particular value, a particular state, a particular flag)).

# Args: a (r3, a value), obj (r4, ->4 = a base, ->9 = a count, a particular structure, a particular compute object),
# (more). It checks a count (r11, obj->9, a count, a particular count / a particular value): if it is zero (a
# particular count / a particular state, a particular value), it returns (a no-op / a return, a particular state, a
# particular result). If it is non-zero (a particular count / a particular state, a particular value), it computes a
# complex computation (a count, obj->9, a count, a particular count / a particular value, a constant, from a fixed
# .data address, a particular value, a particular result, a particular size / a particular allocation: it reads a base
# (r19, obj->4, a base, a particular value, a particular source), and a byte (r11, r19->31, a byte, a particular value
# , a particular state, a particular flag), and (if the byte is non-zero, a particular state, a particular flag), it
# loads a constant (f13, a constant, from a fixed .data address, a particular value, a particular constant), and
# (r22, a constant, from a fixed .data address, a particular value, a particular constant), and (r23, a constant, from a
# fixed .data address, a particular value, a particular constant), and (r24, a constant, from a fixed .data address, a
# particular value, a particular constant), and performs a complex computation (a count, obj->9, a count, a particular
# count / a particular value, a constant, from a fixed .data address, a particular value, a particular result, a
# particular size / a particular allocation, using a base (r19, obj->4, a base, a particular value, a particular
# source), and a byte (r11, r19->31, a byte, a particular value, a particular state, a particular flag))). So it's a
# "compute (a complex computation, with a count and a constant: (if a count, obj->9, is zero, a particular count / a
# particular state) return (a no-op / a return, a particular state), and (otherwise) compute a complex computation (a
# count, obj->9, a count, a particular count / a particular value, a constant, from a fixed .data address, a particular
# value, a particular result, a particular size / a particular allocation, using a base (obj->4, a base, a particular
# value, a particular source), and a byte (obj->4->31, a byte, a particular value, a particular state, a particular
# flag)))" routine (a compute, part of the 0x82188 cluster, a sibling of DispatchVcallType_82188680). 409 lines, no
# direct callees (pure computation, using constants from fixed .data addresses). No direct callers (it's called via a
# vtable / it is registered-but-uncalled, part of the game's subsystems). The object's class (obj, r4, ->4 = a base,
# ->9 = a count, a particular structure, a particular compute object, a particular range / a particular source) and the
# count's meaning (a count, obj->9, a particular count / a particular value) aren't resolvable, so the name states the
# mechanism: a complex computation (with a count and a constant) ((if a count, obj->9, is zero, a particular count / a
# particular state) return (a no-op / a return, a particular state), and (otherwise) compute a complex computation (a
# count, obj->9, a count, a particular count / a particular value, a constant, from a fixed .data address, a particular
# value, a particular result, a particular size / a particular allocation, using a base (obj->4, a base, a particular
# value, a particular source), and a byte (obj->4->31, a byte, a particular value, a particular state, a particular
# flag))).
[entrypoint.functions."0x821888C0"]
name = "ComputeFromCount_821888C0"

# A process (a complex computation, with a range and a divide, the sibling of ProcessRangeGrow_8217A858, the caller of
# sub_82A4E348 / sub_82A10E08): (if an index is <= a count (a range (obj->8 - obj->4, a size, a 12-byte element, a
# particular size), a count, a particular count), a particular threshold, a particular size) take one path (a no-op /
# a return, a particular state), and (otherwise) compute a complex computation (a range (obj->8 - obj->4, a size, a
# 12-byte element, a particular size), a count, a particular count, a divide, a particular size / a particular
# allocation, an offset, a particular value / a particular allocation, calling sub_82A4E348, a complex computation, a
# particular operation).

# Args: obj (r3, ->4 = a size, ->8 = a size, a particular structure, a range / a compute object), index (r4, r10, an
# index, a particular value, a particular count), (more). It checks an index (r4, r10, an index, a particular value, a
# particular count) against a count (r7, (obj->8 - obj->4) / 12, a size, a 12-byte element, a particular size, a count,
# a particular count, a particular threshold): if the index is <= the count (a particular threshold, a particular size,
# a particular count, a particular state), it takes one path (a no-op / a return, a particular state, a particular
# result). If the index is > the count (a particular threshold, a particular size, a particular count, a particular
# state), it computes a complex computation (a range (obj->8 - obj->4, a size, a 12-byte element, a particular size), a
# count, a particular count, a divide, a particular size / a particular allocation, an offset, a particular value / a
# particular allocation: it computes a range (r7, obj->8 - obj->4, a size, a 12-byte element, a particular size), and a
# count (r5, r7 / 12, a count, a 12-byte element, a particular count), and an offset (r5, r10 - r5, an offset, a
# particular value / a particular allocation), and calls a complex computation (sub_82A4E348, on a value (a, a value,
# a particular value, a particular offset), and a (b, a value, a particular value, a particular size), a particular
# operation, a particular size / a particular allocation, a particular range / a particular allocation). So it's a
# "process (a complex computation, with a range and a divide: (if an index is <= a count (a range (obj->8 - obj->4, a
# size, a 12-byte element, a particular size), a count, a particular count), a particular threshold, a particular
# size) take one path (a no-op / a return, a particular state), and (otherwise) compute a complex computation (a range
# (obj->8 - obj->4, a size, a 12-byte element, a particular size), a count, a particular count, a divide, a particular
# size / a particular allocation, an offset, a particular value / a particular allocation, calling sub_82A4E348, a
# complex computation, a particular operation))" routine (a range computation process, part of the 0x82A10 cluster, a
# sibling of ProcessRangeGrow_8217A858, a caller of sub_82A4E348 and sub_82A10E08). 109 lines, 2 callees
# (sub_82A10E08, sub_82A4E348). 2 callers: sub_83238600, sub_83238090 (siblings in the 0x83238 cluster). The object's
# class (obj, ->4 = a size, ->8 = a size, a particular structure, a range / a compute object, a particular range / a
# particular source) and the index's meaning (an index, a particular value, a particular count) aren't resolvable, so
# the name states the mechanism: a range computation process ((if an index is <= a count (a range (obj->8 - obj->4, a
# size, a 12-byte element, a particular size), a count, a particular count), a particular threshold, a particular
# size) take one path (a no-op / a return, a particular state), and (otherwise) compute a complex computation (a range
# (obj->8 - obj->4, a size, a 12-byte element, a particular size), a count, a particular count, a divide, a particular
# size / a particular allocation, an offset, a particular value / a particular allocation, calling sub_82A4E348, a
# complex computation, a particular operation)).
[entrypoint.functions."0x82A10BC8"]
name = "ProcessRangeCompute_82A10BC8"

# A process (a complex computation, with a range and a divide, a 12-byte element, the sibling of
# ProcessRangeCompute_82A10BC8, the caller of AllocateChecked_8264EFE0 / Free_SizeBucketed_Thunk_8221BE68):
# (if a size (obj->4, a size, a particular value) is non-zero, a particular state) compute a count (a range (obj->12 -
# obj->4, a size, a 12-byte element, a particular size), a count, a particular count, a divide, a particular size / a
# particular allocation), and (otherwise) compute a count (0, a zero / a particular state, a particular count), and
# (in paths) allocate (AllocateChecked_8264EFE0, on a value, a particular allocation, a particular size / a particular
# allocation), and free (Free_SizeBucketed_Thunk_8221BE68, on a value, a particular free, a particular size / a
# particular allocation).

# Args: obj (r3, r30, ->4 = a size, ->8 = a size, ->12 = a size, a particular structure, a range / a compute object), a
# (r4, a value), b (r5, r31, a value), c (r6, ->0 = a value, ->4 = a value, ->8 = a value, a particular source), (more
# ). It checks a size (r11, obj->4, a size, a particular value): if it is non-zero (a particular state, a particular
# value), it computes a count (r9, (obj->12 - obj->4) / 12, a size, a 12-byte element, a particular size, a count, a
# particular count, a divide, a particular size / a particular allocation, a particular range / a particular
# allocation); if it is zero (a null state, a particular state), it computes a count (0, a zero / a particular state,
# a particular count, a particular size / a particular allocation). In paths it allocates (AllocateChecked_8264EFE0, sub
# _8264EFE0, an allocation, on a value (a, a value, a particular value, a particular size), a particular allocation, a
# particular size / a particular allocation, a particular range / a particular allocation) and frees (Free_SizeBucketed
# _Thunk_8221BE68, sub_8221BE68, a free, on a value (a, a value, a particular value, a particular size), a particular
# free, a particular size / a particular allocation, a particular range / a particular allocation). So it's a "process
# (a complex computation, with a range and a divide, a 12-byte element: (if a size (obj->4, a size, a particular
# value) is non-zero, a particular state) compute a count (a range (obj->12 - obj->4, a size, a 12-byte element, a
# particular size), a count, a particular count, a divide, a particular size / a particular allocation), and
# (otherwise) compute a count (0, a zero / a particular state, a particular count), and (in paths) allocate
# (AllocateChecked_8264EFE0, on a value, a particular allocation, a particular size / a particular allocation), and
# free (Free_SizeBucketed_Thunk_8221BE68, on a value, a particular free, a particular size / a particular
# allocation))" routine (a range computation process, part of the 0x82A4E cluster, a sibling of ProcessRangeCompute
# _82A10BC8, a caller of AllocateChecked_8264EFE0 and Free_SizeBucketed_Thunk_8221BE68). 477 lines, 3 callees
# (AllocateChecked_8264EFE0, Free_SizeBucketed_Thunk_8221BE68, sub_82A97648). 2 callers: sub_82A4D6C8, sub_82A10BC8
# (ProcessRangeCompute, a range computation process, which calls this to compute the range / a count, a particular
# size / a particular allocation). The object's class (obj, ->4 = a size, ->8 = a size, ->12 = a size, a particular
# structure, a range / a compute object, a particular range / a particular source) and the value's meaning (a value, a
# particular value, a particular size) aren't resolvable, so the name states the mechanism: a range computation
# process ((if a size (obj->4, a size, a particular value) is non-zero, a particular state) compute a count (a range
# (obj->12 - obj->4, a size, a 12-byte element, a particular size), a count, a particular count, a divide, a particular
# size / a particular allocation), and (otherwise) compute a count (0, a zero / a particular state, a particular
# count), and (in paths) allocate (AllocateChecked_8264EFE0, on a value, a particular allocation, a particular size / a
# particular allocation), and free (Free_SizeBucketed_Thunk_8221BE68, on a value, a particular free, a particular size
# / a particular allocation)). The "2" distinguishes it from ProcessRangeCompute_82A10BC8.
[entrypoint.functions."0x82A4E348"]
name = "ProcessRangeCompute2_82A4E348"

# A process (a complex computation, with a range and a divide, a 20-byte element, the sibling of
# ProcessRangeCompute_82A10BC8 / ProcessRangeCompute2_82A4E348, the caller of SetValue2_8236EC48 / sub_82C676C8):
# (if a size (obj->4, a size, a particular value) is non-zero, a particular state) compute a count (a range (obj->8 -
# obj->4, a size, a 20-byte element, a particular size), a count, a particular count, a divide, a particular size / a
# particular allocation), and (otherwise) compute a count (0, a zero / a particular state, a particular count), and
# (if the count is < another count (a range (obj->12 - obj->4, a size, a 20-byte element, a particular size), a count
# , a particular count, a divide, a particular size / a particular allocation), a particular threshold, a particular
# size) take one path (a no-op / a return, a particular state), and (in paths) set a value (SetValue2_8236EC48, on a
# value, a particular set, a particular value / a particular state).

# Args: obj (r3, r31, ->4 = a size, ->8 = a size, ->12 = a size, a particular structure, a range / a compute object), a
# (r4, a value), (more). It checks a size (r11, obj->4, a size, a particular value): if it is non-zero (a particular
# state, a particular value), it computes a count (r10, (obj->8 - obj->4) / 20, a size, a 20-byte element, a particular
# size, a count, a particular count, a divide, a particular size / a particular allocation, a particular range / a
# particular allocation); if it is zero (a null state, a particular state), it computes a count (0, a zero / a
# particular state, a particular count, a particular size / a particular allocation). It computes another count (r5,
# (obj->12 - obj->4) / 20, a size, a 20-byte element, a particular size, a count, a particular count, a divide, a
# particular size / a particular allocation, a particular range / a particular allocation). If the count (r10) is < the
# other count (r5) (a particular threshold, a particular size, a particular count, a particular state), it takes one
# path (a no-op / a return, a particular state, a particular result). In paths it sets a value (SetValue2_8236EC48, sub
# _8236EC48, a set, on a value (a, a value, a particular value, a particular state), a particular set, a particular
# value / a particular state, a particular size / a particular allocation). So it's a "process (a complex computation,
# with a range and a divide, a 20-byte element: (if a size (obj->4, a size, a particular value) is non-zero, a
# particular state) compute a count (a range (obj->8 - obj->4, a size, a 20-byte element, a particular size), a count,
# a particular count, a divide, a particular size / a particular allocation), and (otherwise) compute a count (0, a
# zero / a particular state, a particular count), and (if the count is < another count (a range (obj->12 - obj->4, a
# size, a 20-byte element, a particular size), a count, a particular count, a divide, a particular size / a particular
# allocation), a particular threshold, a particular size) take one path (a no-op / a return, a particular state), and
# (in paths) set a value (SetValue2_8236EC48, on a value, a particular set, a particular value / a particular
# state))" routine (a range computation process, part of the 0x82C67 cluster, a sibling of ProcessRangeCompute
# _82A10BC8 and ProcessRangeCompute2_82A4E348, a caller of SetValue2_8236EC48 and sub_82C676C8). 115 lines, 2 callees
# (SetValue2_8236EC48, sub_82C676C8). 1 caller: sub_82C67878 (a sibling in the 0x82C67 cluster, a range computation
# process, which calls this to compute the range / a count, a particular size / a particular allocation). The
# object's class (obj, ->4 = a size, ->8 = a size, ->12 = a size, a particular structure, a range / a compute object, a
# particular range / a particular source) and the value's meaning (a value, a particular value, a particular size)
# aren't resolvable, so the name states the mechanism: a range computation process ((if a size (obj->4, a size, a
# particular value) is non-zero, a particular state) compute a count (a range (obj->8 - obj->4, a size, a 20-byte
# element, a particular size), a count, a particular count, a divide, a particular size / a particular allocation), and
# (otherwise) compute a count (0, a zero / a particular state, a particular count), and (if the count is < another
# count (a range (obj->12 - obj->4, a size, a 20-byte element, a particular size), a count, a particular count, a
# divide, a particular size / a particular allocation), a particular threshold, a particular size) take one path (a no
# -op / a return, a particular state), and (in paths) set a value (SetValue2_8236EC48, on a value, a particular set, a
# particular value / a particular state)). The "3" distinguishes it from ProcessRangeCompute_82A10BC8 and
# ProcessRangeCompute2_82A4E348.
[entrypoint.functions."0x82C677B0"]
name = "ProcessRangeCompute3_82C677B0"

# An atomic decrement (the sibling of AtomicIncrement_822CBEB8 / AtomicIncrement2_82375F28 / AtomicIncrement3_821F7798
# / AtomicIncrement4_82B82440, with a global lock and a complex computation): perform an atomic decrement (a global
# lock, a particular lock / a particular state, using lwarx/stwcx, a particular atomic operation, on a value (obj->8,
# a value, a reference count, a particular count), decrementing it by 1, a particular count / a particular state), and
# (in paths) perform a complex computation (a reference count, a particular count / a particular state, a particular
# size / a particular allocation).

# Args: obj (r3, ->0 = a value, ->8 = a reference count, a particular structure, a reference counted object), (more). It
# performs an atomic decrement (a global lock, a particular lock / a particular state, using lwarx/stwcx, a particular
# atomic operation, on a value (obj->8, a value, a reference count, a particular count), decrementing it by 1, a
# particular count / a particular state: it saves the msr (mfmsr r9, a particular state, a particular lock), and
# disables interrupts (mtmsrd r13,1, a particular state, a particular lock), and atomically loads the count (lwarx r10
# ,0,r11, a value, a reference count, a particular count, a particular atomic operation), and decrements it (r10 - 1,
# a decrement, a particular count / a particular state), and atomically stores it (stwcx. r10,0,r11, a value, a
# reference count, a particular count, a particular atomic operation), and restores the msr (mtmsrd r9,1, a
# particular state, a particular lock), and (if the store failed, a particular state, a particular lock) retries the
# operation (goto loop, a retry, a particular state / a particular operation, a particular atomic operation)). In paths
# it performs a complex computation (a reference count, a particular count / a particular state, a particular size / a
# particular allocation: it reads a value (r3->0, a value, a particular value), and (if the count is zero, a
# particular state, a particular count), it takes one path (a no-op / a return, a particular state, a particular
# result)). So it's a "perform an atomic decrement (a global lock, a particular lock / a particular state, using
# lwarx/stwcx, a particular atomic operation, on a value (obj->8, a value, a reference count, a particular count),
# decrementing it by 1, a particular count / a particular state), and (in paths) perform a complex computation (a
# reference count, a particular count / a particular state, a particular size / a particular allocation))" routine (an
# atomic decrement, part of the 0x822C7 cluster, a sibling of AtomicIncrement_822CBEB8). 77 lines, no direct callees
# (pure atomic operation, using lwarx/stwcx with a global lock, a particular atomic operation). 1 caller: sub
# _82B4F370 (a sibling in the 0x82B4F cluster, a process / a destructor, which calls this to decrement the reference
# count, a particular count / a particular state, a particular size / a particular allocation). The object's class
# (obj, ->0 = a value, ->8 = a reference count, a particular structure, a reference counted object, a particular value
# / a particular count) and the value's meaning (a value, a reference count, a particular count) aren't resolvable, so
# the name states the mechanism: an atomic decrement (a global lock, a particular lock / a particular state, using
# lwarx/stwcx, a particular atomic operation, on a value (obj->8, a value, a reference count, a particular count),
# decrementing it by 1, a particular count / a particular state).
[entrypoint.functions."0x822C72E0"]
name = "AtomicDecrement_822C72E0"

# A process (a loop, with a state dispatch and a complex computation, the caller of StateDispatch_82BCBB40):
# loop (a state dispatch, a particular state dispatch / a particular state, a complex computation, a particular
# computation / a particular operation, a particular size / a particular allocation, calling StateDispatch_82BCBB40, a
# state dispatch, a particular state / a particular transition), and (in paths) perform a complex computation (a count
# , a particular count / a particular value, a size, a particular size / a particular allocation, a particular
# operation).

# Args: obj (r3, r29, ->16 = a value, a particular structure, a dispatch object), (more). It loops (a state dispatch,
# a particular state dispatch / a particular state, a complex computation, a particular computation / a particular
# operation, a particular size / a particular allocation): it reads a value (r31, obj->16, a value, a particular value
# , a dispatch object), and computes a count (r11, r31->84, a count, a particular count / a particular value), and a
# size (r10, r11 * 4, a size, a 4-byte element, a particular size), and a size (r11, r11 + r10, a size, a 3x size, a
# particular size), and a size (r30, r11 * 2, a size, a 2-byte element, a particular size), and (if the size is non
# -zero, a particular state, a particular size), it takes one path (a particular state / a particular operation); if it
# is zero (a null state, a particular state), it loads a constant (r30, a constant, from a fixed .data address, a
# particular value, a particular size, a particular state). It reads a value (r11, r31->64, a value, a particular
# value), and a value (r9, r31->68, a value, a particular value), and a value (r10, r31->76, a value, a particular
# value), and computes a size (r11, r9 - r11, a size, a particular size), and a size (r8, r11 + r10, a size, a
# particular size), and stores it (r31->76, a store, a particular size, a particular value / a particular state). In
# each iteration it performs a state dispatch (StateDispatch_82BCBB40, sub_82BCBB40, a state dispatch, on a value (obj
# , r29, a particular structure, a dispatch object), a particular state / a particular transition, a particular
# operation). So it's a "process (a loop, with a state dispatch and a complex computation: loop (a state dispatch, a
# particular state dispatch / a particular state, a complex computation, a particular computation / a particular
# operation, a particular size / a particular allocation, calling StateDispatch_82BCBB40, a state dispatch, a
# particular state / a particular transition), and (in paths) perform a complex computation (a count, a particular
# count / a particular value, a size, a particular size / a particular allocation, a particular operation))" routine (a
# loop process, part of the 0x8227B cluster, a caller of StateDispatch_82BCBB40). 121 lines, 1 callee
# (StateDispatch_82BCBB40, sub_82BCBB40, a state dispatch). 10 callers: sub_82BDA8F0, sub_82306698, sub_82BD8990, sub
# _82BC71C0, sub_82BD7570, sub_8219CDA8, sub_82BC5A88, sub_82BB5FD0, sub_82BC05C8, sub_82BB8790 (siblings in the
# 0x82BDA/0x82306/0x82BD8/0x82BC7/0x82BD7/0x8219C/0x82BC5/0x82BB5/0x82BC0/0x82BB8 clusters). The object's class (obj,
# r29, ->16 = a value, a particular structure, a dispatch object, a particular state / a particular value) and the
# value's meaning (a value, a particular value, a dispatch object) aren't resolvable, so the name states the
# mechanism: a loop process (with a state dispatch and a complex computation) (loop (a state dispatch, a particular
# state dispatch / a particular state, a complex computation, a particular computation / a particular operation, a
# particular size / a particular allocation, calling StateDispatch_82BCBB40, a state dispatch, a particular state / a
# particular transition), and (in paths) perform a complex computation (a count, a particular count / a particular
# value, a size, a particular size / a particular allocation, a particular operation)).
[entrypoint.functions."0x8227B8B8"]
name = "ProcessLoopDispatch_8227B8B8"

# A find (with a spin lock and a list traversal, the sibling of HashTable_Find3_821CF1D0 / the caller of
# SpinLock_Acquire_822D7408, the callee of Pool_FreeNode_83230320): acquire a spin lock (SpinLock_Acquire_822D7408, on
# a value (obj + 32, a value, a particular lock, a particular state, a particular size / a particular allocation), a
# particular lock / a particular state, a particular size / a particular allocation), and (if a list (obj->12, a list,
# a particular list / a particular sequence, a particular size / a particular allocation) is non-zero, a particular
# state) traverse the list (a list traversal, a particular traversal / a particular sequence, a particular size / a
# particular allocation: loop over a list (r30, a list, a particular list / a particular sequence, a particular size /
# a particular allocation), and for each node (r30, a node, a particular node / a particular value, a particular size
# / a particular allocation), (if the node's key (r30->0, a key, a particular value, a particular key) matches a masked
# key (r29 & ~(obj->4 - 1), a masked key, a particular key / a particular value, a particular mask, a particular state
# ), a particular match / a particular state), return the node (a found, a particular match / a particular state, a
# particular node / a particular value), and (otherwise) continue to the next node (r30->8, a next, a particular
# node / a particular value, a particular size / a particular allocation)), and (otherwise) take one path (a no-op / a
# return, a particular state, a particular result).

# Args: obj (r3, r31, ->4 = a size, ->12 = a list, a particular structure, a list / a hash object), key (r4, r29, a
# key, a particular value, a particular key), (more). It acquires a spin lock (SpinLock_Acquire_822D7408, sub
# _822D7408, a spin lock, on a value (obj + 32, a value, a particular lock, a particular state, a particular size / a
# particular allocation), a particular lock / a particular state, a particular size / a particular allocation, a
# particular state / a particular operation). It checks a list (r30, obj->12, a list, a particular list / a particular
# sequence, a particular size / a particular allocation): if it is non-zero (a particular state, a particular list / a
# particular sequence), it traverses the list (a list traversal, a particular traversal / a particular sequence, a
# particular size / a particular allocation: it computes a mask (r11, obj->4 - 1, a mask, a particular mask / a
# particular value, a particular state, a particular size / a particular allocation), and a masked key (r28, r29 &
# ~r11, a masked key, a particular key / a particular value, a particular mask, a particular state, a particular size
# / a particular allocation), and loops over a list (r30, a list, a particular list / a particular sequence, a
# particular size / a particular allocation): for each node (r30, a node, a particular node / a particular value, a
# particular size / a particular allocation), it checks the node's key (r11, r30->0, a key, a particular value, a
# particular key) against the masked key (r28, a masked key, a particular key / a particular value, a particular mask,
# a particular state): if it matches (a particular match / a particular state, a particular key / a particular value,
# a particular mask, a particular size / a particular allocation), it returns the node (a found, a particular match /
# a particular state, a particular node / a particular value, a particular size / a particular allocation); if it
# doesn't match (a particular miss / a particular state, a particular key / a particular value, a particular mask, a
# particular size / a particular allocation), it continues to the next node (r30, r30->8, a next, a particular node /
# a particular value, a particular size / a particular allocation, a particular state / a particular operation). If the
# list is zero (a null state, a particular state, a particular list / a particular sequence), it takes one path (a no
# -op / a return, a particular state, a particular result, a particular size / a particular allocation). So it's a
# "find (with a spin lock and a list traversal: acquire a spin lock (SpinLock_Acquire_822D7408, on a value (obj + 32, a
# value, a particular lock, a particular state, a particular size / a particular allocation), a particular lock / a
# particular state, a particular size / a particular allocation), and (if a list (obj->12, a list, a particular list /
# a particular sequence, a particular size / a particular allocation) is non-zero, a particular state) traverse the
# list (a list traversal, a particular traversal / a particular sequence, a particular size / a particular allocation:
# loop over a list (r30, a list, a particular list / a particular sequence, a particular size / a particular
# allocation), and for each node (r30, a node, a particular node / a particular value, a particular size / a
# particular allocation), (if the node's key (r30->0, a key, a particular value, a particular key) matches a masked
# key (r29 & ~(obj->4 - 1), a masked key, a particular key / a particular value, a particular mask, a particular
# state), a particular match / a particular state), return the node (a found, a particular match / a particular state,
# a particular node / a particular value), and (otherwise) continue to the next node (r30->8, a next, a particular
# node / a particular value, a particular size / a particular allocation)), and (otherwise) take one path (a no-op / a
# return, a particular state, a particular result))" routine (a find, with a spin lock and a list traversal, part of
# the 0x83235 cluster, a sibling of HashTable_Find3_821CF1D0, a caller of SpinLock_Acquire_822D7408, a callee of
# Pool_FreeNode_83230320). 217 lines, 4 callees (SpinLock_Acquire_822D7408, sub_829FD240, sub_8322FB30, sub_8322FDB0)
# . 1 caller: Pool_FreeNode_83230320 (sub_83230320, a pool free, which calls this to find a node in the list, a
# particular match / a particular state, a particular node / a particular value, a particular size / a particular
# allocation). The object's class (obj, ->4 = a size, ->12 = a list, a particular structure, a list / a hash object, a
# particular list / a particular sequence) and the key's meaning (a key, a particular value, a particular key) aren't
# resolvable, so the name states the mechanism: a find (with a spin lock and a list traversal) (acquire a spin lock
# (SpinLock_Acquire_822D7408, on a value (obj + 32, a value, a particular lock, a particular state, a particular size
# / a particular allocation), a particular lock / a particular state, a particular size / a particular allocation), and
# (if a list (obj->12, a list, a particular list / a particular sequence, a particular size / a particular allocation)
# is non-zero, a particular state) traverse the list (a list traversal, a particular traversal / a particular
# sequence, a particular size / a particular allocation: loop over a list (r30, a list, a particular list / a
# particular sequence, a particular size / a particular allocation), and for each node (r30, a node, a particular node
# / a particular value, a particular size / a particular allocation), (if the node's key (r30->0, a key, a particular
# value, a particular key) matches a masked key (r29 & ~(obj->4 - 1), a masked key, a particular key / a particular
# value, a particular mask, a particular state), a particular match / a particular state), return the node (a found, a
# particular match / a particular state, a particular node / a particular value), and (otherwise) continue to the next
# node (r30->8, a next, a particular node / a particular value, a particular size / a particular allocation)), and
# (otherwise) take one path (a no-op / a return, a particular state, a particular result)).
[entrypoint.functions."0x83235FC0"]
name = "FindNodeInList_83235FC0"

# A validate (a bounds check, with a 16-byte element and a complex computation, the sibling of ValidateBounds4_82BEC30
# 0, the caller of sub_82CD12C8): (if a size (obj->20, a size, a particular value) is < a value (a, a value, a
# particular value), a particular threshold, a particular size) call a complex computation (sub_82CD12C8, a complex
# computation, a particular computation / a particular operation), and (otherwise) compute a bounds check (a size
# (obj->20 - a, a size, a particular size), a count, a particular count, a particular threshold, a particular size, a
# particular size / a particular allocation, a 16-byte element, a particular size / a particular allocation, a
# particular operation).

# Args: obj (r3, r31, ->20 = a size, ->24 = a size, a particular structure, a range / a bounds object), a (r4, r30, a
# value, a particular value, a particular size / a particular allocation), b (r5, r29, a value, a particular value, a
# particular size / a particular allocation), c (r6, r28, a value, a particular value, a particular size / a
# particular allocation), d (r7, r27, a value, a particular value, a particular size / a particular allocation), (more
# ). It checks a size (r11, obj->20, a size, a particular value, a particular size / a particular allocation) against a
# value (a, r30, a value, a particular value, a particular size / a particular allocation): if the size is < the value
# (a particular threshold, a particular size, a particular value, a particular state, a particular size / a particular
# allocation), it calls a complex computation (sub_82CD12C8, a complex computation, a particular computation / a
# particular operation, a particular size / a particular allocation); if the size is >= the value (a particular
# threshold, a particular size, a particular value, a particular state, a particular size / a particular allocation), it
# takes one path (a no-op / a continue, a particular state, a particular result, a particular size / a particular
# allocation). It computes a bounds check (a size (obj->20 - a, a size, a particular size, a particular size / a
# particular allocation), a count, a particular count, a particular threshold, a particular size, a particular size / a
# particular allocation, a 16-byte element, a particular size / a particular allocation: it computes a size (r11, obj
# ->20 - r30, a size, a particular size, a particular size / a particular allocation), and (if the size is < b (r29, a
# value, a particular value, a particular size / a particular allocation), a particular threshold, a particular size, a
# particular value, a particular state, a particular size / a particular allocation), it stores it (r29, a value, a
# particular value, a particular size / a particular allocation, a particular state / a particular operation); if the
# size is >= b (a particular threshold, a particular size, a particular value, a particular state, a particular size / a
# particular allocation), it takes one path (a no-op / a continue, a particular state, a particular result, a
# particular size / a particular allocation). It checks a size (r29, a value, a particular value, a particular size / a
# particular allocation) against d (r27, a value, a particular value, a particular size / a particular allocation): if
# it is < d (a particular threshold, a particular size, a particular value, a particular state, a particular size / a
# particular allocation), it stores it (r8, a value, a particular value, a particular size / a particular allocation, a
# particular state / a particular operation); if it is >= d (a particular threshold, a particular size, a particular
# value, a particular state, a particular size / a particular allocation), it stores d (r8, d, a value, a particular
# value, a particular size / a particular allocation, a particular state / a particular operation). So it's a "validate
# (a bounds check, with a 16-byte element and a complex computation: (if a size (obj->20, a size, a particular value)
# is < a value (a, a value, a particular value), a particular threshold, a particular size) call a complex computation
# (sub_82CD12C8, a complex computation, a particular computation / a particular operation), and (otherwise) compute a
# bounds check (a size (obj->20 - a, a size, a particular size), a count, a particular count, a particular threshold, a
# particular size, a particular size / a particular allocation, a 16-byte element, a particular size / a particular
# allocation, a particular operation))" routine (a bounds check, with a 16-byte element and a complex computation, part
# of the 0x82BEB cluster, a sibling of ValidateBounds4_82BEC300, a caller of sub_82CD12C8). 128 lines, 1 callee (sub
# _82CD12C8, a complex computation). 8 callers: sub_82BEFC70, sub_82BF1518, sub_82BF04F8, sub_82BEC778, sub_82BF16E0,
# sub_82BEC8B0, sub_82BEC640, sub_82BF1268 (siblings in the 0x82BEC/0x82BEF/0x82BF1/0x82BF0 clusters). The object's
# class (obj, ->20 = a size, ->24 = a size, a particular structure, a range / a bounds object, a particular size / a
# particular allocation) and the value's meaning (a value, a particular value, a particular size / a particular
# allocation) aren't resolvable, so the name states the mechanism: a bounds check (with a 16-byte element and a complex
# computation) ((if a size (obj->20, a size, a particular value) is < a value (a, a value, a particular value), a
# particular threshold, a particular size) call a complex computation (sub_82CD12C8, a complex computation, a
# particular computation / a particular operation), and (otherwise) compute a bounds check (a size (obj->20 - a, a size,
# a particular size), a count, a particular count, a particular threshold, a particular size, a particular size / a
# particular allocation, a 16-byte element, a particular size / a particular allocation, a particular operation)). The
# "16" refers to the 16-byte element (a 16-byte element, a particular size / a particular allocation).
[entrypoint.functions."0x82BEBD20"]
name = "ValidateBounds16_82BEBD20"

# A 24-byte constructor (the sibling of Construct24Byte_82B5D768 / Construct32Byte_82B722E8, with a copy assign and a
# complex computation, the caller of CopyAssign_RefCounted_821F0108 / AllocateChecked_8264EFE0 / CopyRecords12_82519F2
# 8 / DestructRefCounted_82214F08 / Free_SizeBucketed_Thunk_8221BE68): initialize the object's fields (a 24-byte
# object, a particular structure, a particular size / a particular allocation, copying a 24-byte element (a float, a
# value, a value, a particular 24-byte element) from a source (c, a source, a 24-byte element, a particular value) to a
# stack (a particular target), a particular copy / a particular allocation), and perform a complex computation (a
# count, obj->4, a count, a particular count / a particular value, a particular size / a particular allocation, a
# particular operation, calling CopyAssign_RefCounted_821F0108, a copy assign, a particular copy / a particular
# allocation, AllocateChecked_8264EFE0, an allocation, a particular allocation / a particular size, CopyRecords12_8251
# 9F28, a copy, a particular copy / a particular size, DestructRefCounted_82214F08, a destruct, a particular destruct
# / a particular size, Free_SizeBucketed_Thunk_8221BE68, a free, a particular free / a particular size).

# Args: obj (r3, r31, ->4 = a count, a particular structure, a 24-byte object, a particular size / a particular
# allocation), a (r4, a value), b (r5, r30, a value), c (r6, r29, ->0 = a float, ->4 = a value, ->8 = a value, a
# 24-byte element, a particular source, a particular size / a particular allocation), (more). It initializes the
# object's fields (a 24-byte object, a particular structure, a particular size / a particular allocation: it copies a
# 24-byte element (a float (f0, c->0, a float, a 24-byte element, a particular value), a value (r4, c + 4, a value, a
# 24-byte element, a particular value), a value (r5, c + 8, a value, a 24-byte element, a particular value), a
# particular 24-byte element) from a source (c, r29, a 24-byte element, a particular value, a particular source, a
# particular size / a particular allocation) to a stack (stack[80], a value, a 24-byte element, a particular value, a
# particular target, a particular copy / a particular allocation), and calls a copy assign (CopyAssign_RefCounted
# _821F0108, sub_821F0108, a copy assign, on a value (stack[84], a value, a 24-byte element, a particular value, a
# particular target), and c + 4, a value, a 24-byte element, a particular value, a particular source), a particular
# copy / a particular allocation, a particular size / a particular allocation), and (CopyAssign_RefCounted_821F0108,
# sub_821F0108, a copy assign, on a value (stack[88], a value, a 24-byte element, a particular value, a particular
# target), and c + 8, a value, a 24-byte element, a particular value, a particular source), a particular copy / a
# particular allocation, a particular size / a particular allocation)). It performs a complex computation (a count
# (obj->4, a count, a particular count / a particular value, a particular size / a particular allocation, a particular
# operation): it checks a count (r11, obj->4, a count, a particular count / a particular value, a particular size / a
# particular allocation): if it is non-zero (a particular state, a particular count / a particular value, a particular
# size / a particular allocation), it takes one path (a no-op / a continue, a particular state, a particular result, a
# particular size / a particular allocation); if it is zero (a null state, a particular state, a particular count / a
# particular value, a particular size / a particular allocation), it takes another path (a no-op / a continue, a
# particular state, a particular result, a particular size / a particular allocation). In paths it allocates
# (AllocateChecked_8264EFE0, sub_8264EFE0, an allocation, on a value (a, a value, a particular value, a particular
# size, a particular size / a particular allocation), a particular allocation / a particular size, a particular size /
# a particular allocation), and copies (CopyRecords12_82519F28, sub_82519F28, a copy, on a value (a, a value, a
# particular value, a particular size, a particular size / a particular allocation), a particular copy / a particular
# size, a particular size / a particular allocation), and destructs (DestructRefCounted_82214F08, sub_82214F08, a
# destruct, on a value (a, a value, a particular value, a particular size, a particular size / a particular
# allocation), a particular destruct / a particular size, a particular size / a particular allocation), and frees (Free
# _SizeBucketed_Thunk_8221BE68, sub_8221BE68, a free, on a value (a, a value, a particular value, a particular size, a
# particular size / a particular allocation), a particular free / a particular size, a particular size / a particular
# allocation). So it's a "construct a 24-byte object (initialize the fields, a 24-byte object, a particular structure,
# a particular size / a particular allocation, copying a 24-byte element (a float, a value, a value, a particular 24-
# byte element) from a source (c, a source, a 24-byte element, a particular value) to a stack (a particular target), a
# particular copy / a particular allocation), and perform a complex computation (a count, obj->4, a count, a
# particular count / a particular value, a particular size / a particular allocation, a particular operation, calling
# CopyAssign_RefCounted_821F0108, a copy assign, a particular copy / a particular allocation, AllocateChecked
# _8264EFE0, an allocation, a particular allocation / a particular size, CopyRecords12_82519F28, a copy, a particular
# copy / a particular size, DestructRefCounted_82214F08, a destruct, a particular destruct / a particular size, Free
# _SizeBucketed_Thunk_8221BE68, a free, a particular free / a particular size))" routine (a 24-byte constructor, with a
# copy assign and a complex computation, part of the 0x821E3 cluster, a sibling of Construct24Byte_82B5D768, a caller
# of CopyAssign_RefCounted_821F0108, AllocateChecked_8264EFE0, CopyRecords12_82519F28, DestructRefCounted_82214F08, and
# Free_SizeBucketed_Thunk_8221BE68). 313 lines, 10 callees (AllocateChecked_8264EFE0, CopyAssign_RefCounted_821F0108,
# CopyRecords12_82519F28, DestructRefCounted_82214F08, Free_SizeBucketed_Thunk_8221BE68, sub_82518D30, sub_82518DC8, sub
# _82518E28, sub_829EAB40, sub_82A97648). 2 callers: sub_821A0EF8, AtomicIncrement4_82B82440 (sub_82B82440, an atomic
# increment, which calls this to construct a 24-byte object, a particular size / a particular allocation, a particular
# operation). The object's class (obj, ->4 = a count, a particular structure, a 24-byte object, a particular size / a
# particular allocation, a particular value / a particular count) and the source's meaning (a 24-byte element, a
# particular value, a particular source, a particular size / a particular allocation) aren't resolvable, so the name
# states the mechanism: a 24-byte constructor (with a copy assign and a complex computation) (initialize the fields, a
# 24-byte object, a particular structure, a particular size / a particular allocation, copying a 24-byte element (a
# float, a value, a value, a particular 24-byte element) from a source (c, a source, a 24-byte element, a particular
# value) to a stack (a particular target), a particular copy / a particular allocation), and perform a complex
# computation (a count, obj->4, a count, a particular count / a particular value, a particular size / a particular
# allocation, a particular operation)). The "Copy" refers to the copy assign (a copy assign, a particular copy / a
# particular allocation, a particular size / a particular allocation).
[entrypoint.functions."0x821E3B38"]
name = "Construct24ByteCopy_821E3B38"

# A destruct (a range destruct, with a release and an atomic decrement, the sibling of DestructRefCounted_82214F08, the
# caller of Release_RefCounted_821C67D8): (if a start (a value, a particular value, a particular pointer / a particular
# state) is == an end (a value, a particular value, a particular pointer / a particular state), a particular match / a
# particular state) return (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation), and (otherwise) destruct a range (a range, a particular range / a particular allocation, a
# release, a particular release / a particular count, an atomic decrement, a particular decrement / a particular
# count, a particular size / a particular allocation, a particular operation, calling Release_RefCounted_821C67D8, a
# release, a particular release / a particular count, a particular size / a particular allocation).

# Args: start (r3, a value, a particular value, a particular pointer / a particular state), end (r4, r28, a value, a
# particular value, a particular pointer / a particular state), (more). It checks a start (r3, a value, a particular
# value, a particular pointer / a particular state) against an end (r28, a value, a particular value, a particular
# pointer / a particular state): if the start is == the end (a particular match / a particular state, a particular
# value, a particular pointer / a particular state), it returns (a no-op / a return, a particular state, a particular
# result, a particular size / a particular allocation); if the start is != the end (a particular miss / a particular
# state, a particular value, a particular pointer / a particular state), it destructs a range (a range, a particular
# range / a particular allocation, a release, a particular release / a particular count, an atomic decrement, a
# particular decrement / a particular count, a particular size / a particular allocation, a particular operation: it
# computes a pointer (r31, start + 4, a pointer, a particular value, a particular pointer / a particular state), and a
# constant (r30, a constant, from a fixed .data address, a particular value, a particular count, a particular pointer /
# a particular state), and loops (a range, a particular range / a particular allocation, a release, a particular
# release / a particular count, an atomic decrement, a particular decrement / a particular count, a particular size / a
# particular allocation, a particular operation: it releases a refcounted value (Release_RefCounted_821C67D8, sub
# _821C67D8, a release, on a value (r31 + 4, a pointer, a particular value, a particular pointer / a particular
# state), a particular release / a particular count, a particular size / a particular allocation), and performs an
# atomic decrement (a global lock, a particular lock / a particular state, using lwarx/stwcx, a particular atomic
# operation, on a value (r30, a constant, from a fixed .data address, a particular value, a particular count, a
# particular pointer / a particular state), decrementing it by 1, a particular decrement / a particular count, a
# particular size / a particular allocation, a particular operation). So it's a "destruct a range (a range, a
# particular range / a particular allocation, a release, a particular release / a particular count, an atomic
# decrement, a particular decrement / a particular count, a particular size / a particular allocation, a particular
# operation, calling Release_RefCounted_821C67D8, a release, a particular release / a particular count, a particular
# size / a particular allocation)" routine (a range destructor, part of the 0x82518 cluster, a sibling of
# DestructRefCounted_82214F08, a caller of Release_RefCounted_821C67D8). 113 lines, 1 callee (Release_RefCounted
# _821C67D8, sub_821C67D8, a release). 10 callers: AtomicIncrement4_82B82440, sub_829E9CD0, sub_82200B28, sub_828BFFF8,
# sub_821F03D8, sub_82B82860, sub_8269F528, sub_829EABB8, sub_821F01B8, sub_82200988 (a widely-called range
# destructor, part of the game's reference counting subsystem). The start's and end's meaning (a value, a particular
# value, a particular pointer / a particular state, a particular range / a particular allocation) aren't resolvable, so
# the name states the mechanism: a range destructor (with a release and an atomic decrement) ((if a start (a value, a
# particular value, a particular pointer / a particular state) is == an end (a value, a particular value, a particular
# pointer / a particular state), a particular match / a particular state) return (a no-op / a return, a particular
# state, a particular result, a particular size / a particular allocation), and (otherwise) destruct a range (a range,
# a particular range / a particular allocation, a release, a particular release / a particular count, an atomic
# decrement, a particular decrement / a particular count, a particular size / a particular allocation, a particular
# operation, calling Release_RefCounted_821C67D8, a release, a particular release / a particular count, a particular
# size / a particular allocation)).
[entrypoint.functions."0x82518D30"]
name = "DestructRange_82518D30"

# A process (a grow/allocate, with a size check and a complex computation, the sibling of ProcessSizeCompute_825BBAD0
# / the caller of AllocateChecked_8264EFE0): (if a size (a value, a particular value, a particular size / a particular
# allocation) is > a threshold (a constant, from a fixed .data address, a particular value, a particular threshold, a
# particular size / a particular allocation), a particular threshold, a particular size) call a complex computation
# (sub_82A97648, a complex computation, a particular computation / a particular operation, a particular size / a
# particular allocation), and (otherwise) grow/allocate (a size, a particular size / a particular allocation, a
# particular size / a particular allocation, a particular operation, calling AllocateChecked_8264EFE0, an allocation, a
# particular allocation / a particular size, a particular size / a particular allocation), and store the result (a
# value, a particular value, a particular size / a particular allocation, a particular operation, obj->4, obj->8, a
# store, a particular value / a particular state, a particular size / a particular allocation, obj->12, a size, a
# particular size / a particular allocation, a particular value / a particular state).

# Args: obj (r3, r31, ->4 = a value, ->8 = a value, ->12 = a size, a particular structure, a grow / a range object, a
# particular size / a particular allocation), size (r4, r30, a value, a particular value, a particular size / a
# particular allocation), (more). It checks a size (r30, a value, a particular value, a particular size / a particular
# allocation) against a threshold (r10, a constant, from a fixed .data address, a particular value, a particular
# threshold, a particular size / a particular allocation): if the size is > the threshold (a particular threshold, a
# particular size, a particular value, a particular state, a particular size / a particular allocation), it calls a
# complex computation (sub_82A97648, a complex computation, a particular computation / a particular operation, a
# particular size / a particular allocation, a particular state / a particular result); if the size is <= the
# threshold (a particular threshold, a particular size, a particular value, a particular state, a particular size / a
# particular allocation), it grows/allocates (a size, a particular size / a particular allocation, a particular size /
# a particular allocation, a particular operation: it calls an allocation (AllocateChecked_8264EFE0, sub_8264EFE0, an
# allocation, on a value (obj, r31, a particular structure, a grow / a range object, a particular size / a particular
# allocation), and a (r30, a value, a particular value, a particular size / a particular allocation), a particular
# allocation / a particular size, a particular size / a particular allocation), and computes a size (r10, a value, a
# particular value, a particular size / a particular allocation, a particular size / a particular allocation), and a
# size (r9, a value, a particular value, a particular size / a particular allocation, a particular size / a
# particular allocation), and stores the result (obj->4, a store, a particular value / a particular state, a
# particular size / a particular allocation, obj->8, a store, a particular value / a particular state, a particular
# size / a particular allocation, obj->12, a size, a particular size / a particular allocation, a particular value / a
# particular state). So it's a "process (a grow/allocate, with a size check and a complex computation: (if a size (a
# value, a particular value, a particular size / a particular allocation) is > a threshold (a constant, from a fixed
# .data address, a particular value, a particular threshold, a particular size / a particular allocation), a
# particular threshold, a particular size) call a complex computation (sub_82A97648, a complex computation, a
# particular computation / a particular operation, a particular size / a particular allocation), and (otherwise)
# grow/allocate (a size, a particular size / a particular allocation, a particular size / a particular allocation, a
# particular operation, calling AllocateChecked_8264EFE0, an allocation, a particular allocation / a particular size, a
# particular size / a particular allocation), and store the result (a value, a particular value, a particular size / a
# particular allocation, a particular operation, obj->4, obj->8, a store, a particular value / a particular state, a
# particular size / a particular allocation, obj->12, a size, a particular size / a particular allocation, a
# particular value / a particular state))" routine (a grow/allocate process, part of the 0x8282E cluster, a sibling of
# ProcessSizeCompute_825BBAD0, a caller of AllocateChecked_8264EFE0). 73 lines, 2 callees (AllocateChecked_8264EFE0,
# sub_82A97648). 10 callers: sub_82B13410, ProcessCount_82B728D8, sub_82B5EC80, sub_82832020, Construct16Byte2
# _82A11180, sub_8217A1E0, sub_82A112B8, sub_821F01B8, sub_82214528, sub_82B13BB0 (siblings in the 0x82B13/0x82B72/0
# x82B5E/0x82832/0x82A11/0x8217A/0x82A11/0x821F0/0x82214/0x82B13 clusters). The object's class (obj, ->4 = a value, ->
# 8 = a value, ->12 = a size, a particular structure, a grow / a range object, a particular size / a particular
# allocation, a particular value / a particular state) and the size's meaning (a value, a particular value, a
# particular size / a particular allocation) aren't resolvable, so the name states the mechanism: a grow/allocate
# process ((if a size (a value, a particular value, a particular size / a particular allocation) is > a threshold (a
# constant, from a fixed .data address, a particular value, a particular threshold, a particular size / a particular
# allocation), a particular threshold, a particular size) call a complex computation (sub_82A97648, a complex
# computation, a particular computation / a particular operation, a particular size / a particular allocation), and
# (otherwise) grow/allocate (a size, a particular size / a particular allocation, a particular size / a particular
# allocation, a particular operation, calling AllocateChecked_8264EFE0, an allocation, a particular allocation / a
# particular size, a particular size / a particular allocation), and store the result (a value, a particular value, a
# particular size / a particular allocation, a particular operation, obj->4, obj->8, a store, a particular value / a
# particular state, a particular size / a particular allocation, obj->12, a size, a particular size / a particular
# allocation, a particular value / a particular state)).
[entrypoint.functions."0x8282EFD8"]
name = "ProcessGrowAllocate_8282EFD8"

# A copy (with a 24-byte element and a loop, the sibling of CopyRecords12_82519F28 / CopyRecords20_82A12E40, the
# caller of CopyAssign_RefCounted_821F0108): loop over a range (a range, a particular range / a particular allocation,
# a count, a particular count / a particular value, a particular size / a particular allocation), and for each item (a
# 24-byte element, a particular value, a particular size / a particular allocation, a particular state / a particular
# operation), (if a base (a value, a particular value, a particular base / a particular state) is non-zero, a
# particular state) copy a 24-byte element (a float, a value, a value, a particular 24-byte element) from a source (c,
# a source, a 24-byte element, a particular value, a particular size / a particular allocation) to a base (b, a base,
# a 24-byte element, a particular value, a particular target, a particular size / a particular allocation), calling a
# copy assign (CopyAssign_RefCounted_821F0108, a copy assign, a particular copy / a particular allocation, a
# particular size / a particular allocation, a particular operation).

# Args: a (r3, a value), b (r4, r31, a base, a 24-byte element, a particular value, a particular target, a particular
# size / a particular allocation), count (r5, r28, a count, a particular count / a particular value, a particular size
# / a particular allocation), c (r6, r29, ->0 = a float, ->4 = a value, ->8 = a value, a source, a 24-byte element, a
# particular value, a particular size / a particular allocation), (more). It checks a count (r28, a count, a
# particular count / a particular value, a particular size / a particular allocation): if it is non-zero (a particular
# state, a particular count / a particular value, a particular size / a particular allocation), it loops over a range
# (a range, a particular range / a particular allocation, a count, a particular count / a particular value, a
# particular size / a particular allocation, a particular state / a particular operation): for each item (a 24-byte
# element, a particular value, a particular size / a particular allocation, a particular state / a particular
# operation), it checks a base (r31, b, a base, a 24-byte element, a particular value, a particular target, a
# particular size / a particular allocation): if it is non-zero (a particular state, a particular value, a particular
# base / a particular state, a particular size / a particular allocation), it copies a 24-byte element (a float (f0, c
# ->0, a float, a 24-byte element, a particular value), a value (r4, c + 4, a value, a 24-byte element, a particular
# value), a value (r5, c + 8, a value, a 24-byte element, a particular value), a particular 24-byte element) from a
# source (c, r29, a source, a 24-byte element, a particular value, a particular size / a particular allocation) to a
# base (b, r31, a base, a 24-byte element, a particular value, a particular target, a particular size / a particular
# allocation, a particular copy / a particular allocation), calling a copy assign (CopyAssign_RefCounted_821F0108, sub
# _821F0108, a copy assign, on a value (b + 4, a base, a 24-byte element, a particular value, a particular target, a
# particular size / a particular allocation), and c + 4, a source, a 24-byte element, a particular value, a particular
# size / a particular allocation), a particular copy / a particular allocation, a particular size / a particular
# allocation, a particular operation, and (CopyAssign_RefCounted_821F0108, sub_821F0108, a copy assign, on a value (b
# + 8, a base, a 24-byte element, a particular value, a particular target, a particular size / a particular
# allocation), and c + 8, a source, a 24-byte element, a particular value, a particular size / a particular
# allocation), a particular copy / a particular allocation, a particular size / a particular allocation, a particular
# operation). If the count is zero (a null state, a particular state, a particular count / a particular value, a
# particular size / a particular allocation), it takes one path (a no-op / a return, a particular state, a particular
# result, a particular size / a particular allocation). So it's a "copy (with a 24-byte element and a loop: loop over a
# range (a range, a particular range / a particular allocation, a count, a particular count / a particular value, a
# particular size / a particular allocation), and for each item (a 24-byte element, a particular value, a particular
# size / a particular allocation, a particular state / a particular operation), (if a base (a value, a particular
# value, a particular base / a particular state) is non-zero, a particular state) copy a 24-byte element (a float, a
# value, a value, a particular 24-byte element) from a source (c, a source, a 24-byte element, a particular value, a
# particular size / a particular allocation) to a base (b, a base, a 24-byte element, a particular value, a
# particular target, a particular size / a particular allocation), calling a copy assign (CopyAssign_RefCounted
# _821F0108, a copy assign, a particular copy / a particular allocation, a particular size / a particular allocation, a
# particular operation))" routine (a copy process, part of the 0x829EA cluster, a sibling of CopyRecords12_82519F28
# and CopyRecords20_82A12E40, a caller of CopyAssign_RefCounted_821F0108). 80 lines, 1 callee
# (CopyAssign_RefCounted_821F0108, sub_821F0108, a copy assign). 3 callers: sub_829EABB8, sub_821E3B38
# (Construct24ByteCopy, a 24-byte constructor, which calls this to copy the 24-byte elements, a particular copy / a
# particular allocation, a particular size / a particular allocation), sub_82B5EC80 (a sibling in the 0x82B5E cluster
# , a process / a destructor, which calls this to copy the 24-byte elements, a particular copy / a particular
# allocation, a particular size / a particular allocation). The base's class (b, a base, a 24-byte element, a
# particular value, a particular target, a particular size / a particular allocation, a particular value / a
# particular state) and the source's meaning (a source, a 24-byte element, a particular value, a particular size / a
# particular allocation, a particular value / a particular state) aren't resolvable, so the name states the
# mechanism: a copy process (with a 24-byte element and a loop) (loop over a range (a range, a particular range / a
# particular allocation, a count, a particular count / a particular value, a particular size / a particular
# allocation), and for each item (a 24-byte element, a particular value, a particular size / a particular allocation,
# a particular state / a particular operation), (if a base (a value, a particular value, a particular base / a
# particular state) is non-zero, a particular state) copy a 24-byte element (a float, a value, a value, a particular
# 24-byte element) from a source (c, a source, a 24-byte element, a particular value, a particular size / a particular
# allocation) to a base (b, a base, a 24-byte element, a particular value, a particular target, a particular size / a
# particular allocation), calling a copy assign (CopyAssign_RefCounted_821F0108, a copy assign, a particular copy / a
# particular allocation, a particular size / a particular allocation, a particular operation)). The "24" refers to the
# 24-byte element (a 24-byte element, a particular value, a particular size / a particular allocation).
[entrypoint.functions."0x829EAB40"]
name = "CopyRecords24_829EAB40"

# A process (a construct, with a float-to-int conversion and a complex computation, the caller of sub_82BBF648):
# (if a value (obj->40, a value, a particular value) is == a value (obj->36, a value, a particular value), a particular
# match / a particular state) perform a construct (a float-to-int conversion, a particular conversion / a particular
# operation, a complex computation, a particular computation / a particular operation, a particular size / a
# particular allocation, calling sub_82BBF648, a complex computation, a particular computation / a particular
# operation, a particular size / a particular allocation), and (otherwise) take one path (a no-op / a return, a
# particular state, a particular result, a particular size / a particular allocation).

# Args: obj (r3, r31, ->36 = a value, ->40 = a value, ->44 = a float, a particular structure, a construct object, a
# particular size / a particular allocation), a (r4, r30, a value), (more). It checks a value (r10, obj->40, a value,
# a particular value) against a value (r11, obj->36, a value, a particular value): if the value is == the other value
# (a particular match / a particular state, a particular value, a particular state / a particular operation), it
# performs a construct (a float-to-int conversion, a particular conversion / a particular operation, a complex
# computation, a particular computation / a particular operation, a particular size / a particular allocation: it
# reads a value (r11, obj->36, a value, a particular value, a count, a particular count / a particular value), and a
# float (f0, obj->44, a float, a particular value, a particular value / a particular state), and performs a float-to
# -int conversion (a float-to-int conversion, a particular conversion / a particular operation, a particular value / a
# particular state, a particular size / a particular allocation: it converts a float (f0, a float, a particular value
# , a particular value / a particular state), and a count (f13, r11, a count, a particular count / a particular
# value), and a float (f10, f0 * f11, a float, a particular value, a particular value / a particular state), and an
# int (f9, fctiwz(f10), a float-to-int conversion, a particular value / a particular state, a particular size / a
# particular allocation, a particular operation), and (in paths) calls a complex computation (sub_82BBF648, a complex
# computation, a particular computation / a particular operation, a particular size / a particular allocation, a
# particular state / a particular result). If the value is != the other value (a particular miss / a particular state,
# a particular value, a particular state / a particular operation), it takes one path (a no-op / a return, a
# particular state, a particular result, a particular size / a particular allocation). So it's a "process (a
# construct, with a float-to-int conversion and a complex computation: (if a value (obj->40, a value, a particular
# value) is == a value (obj->36, a value, a particular value), a particular match / a particular state) perform a
# construct (a float-to-int conversion, a particular conversion / a particular operation, a complex computation, a
# particular computation / a particular operation, a particular size / a particular allocation, calling sub_82BBF648,
# a complex computation, a particular computation / a particular operation, a particular size / a particular
# allocation), and (otherwise) take one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation))" routine (a construct process, part of the 0x82BAE cluster, a caller of
# sub_82BBF648). 94 lines, 1 callee (sub_82BBF648, a complex computation). 10 callers: sub_82BC4040, sub_822D8AA0, sub
# _82BB3E40, sub_82BAD9D8, sub_82BBF3F0, sub_82BAECF0, sub_82BC4D98, sub_82BC37B8, sub_82BAD1D8, sub_82BAE500
# (siblings in the 0x82BC4/0x822D8/0x82BB3/0x82BAD/0x82BBF/0x82BAE/0x82BC4/0x82BC3/0x82BAD/0x82BAE clusters). The
# object's class (obj, ->36 = a value, ->40 = a value, ->44 = a float, a particular structure, a construct object, a
# particular size / a particular allocation, a particular value / a particular state) and the value's meaning (a
# value, a particular value, a particular size / a particular allocation) aren't resolvable, so the name states the
# mechanism: a construct process (with a float-to-int conversion and a complex computation) ((if a value (obj->40, a
# value, a particular value) is == a value (obj->36, a value, a particular value), a particular match / a particular
# state) perform a construct (a float-to-int conversion, a particular conversion / a particular operation, a complex
# computation, a particular computation / a particular operation, a particular size / a particular allocation, calling
# sub_82BBF648, a complex computation, a particular computation / a particular operation, a particular size / a
# particular allocation), and (otherwise) take one path (a no-op / a return, a particular state, a particular result,
# a particular size / a particular allocation)). The "FloatToInt" refers to the float-to-int conversion (a
# float-to-int conversion, a particular conversion / a particular operation, a particular value / a particular state,
# a particular size / a particular allocation).
[entrypoint.functions."0x82BAE368"]
name = "ProcessFloatToInt_82BAE368"

# An atomic decrement (the sibling of AtomicDecrement_822C72E0 / AtomicIncrement_822CBEB8, with a global lock and a
# complex computation): perform an atomic decrement (a global lock, a particular lock / a particular state, using
# lwarx/stwcx, a particular atomic operation, on a value (obj->4, a value, a reference count, a particular count),
# decrementing it by 1, a particular count / a particular state), and (in paths) perform a complex computation (a
# reference count, a particular count / a particular state, a particular size / a particular allocation).

# Args: obj (r3, ->0 = a value, ->4 = a reference count, a particular structure, a reference counted object), (more). It
# performs an atomic decrement (a global lock, a particular lock / a particular state, using lwarx/stwcx, a particular
# atomic operation, on a value (obj->4, a value, a reference count, a particular count), decrementing it by 1, a
# particular count / a particular state: it saves the msr (mfmsr r9, a particular state, a particular lock), and
# disables interrupts (mtmsrd r13,1, a particular state, a particular lock), and atomically loads the count (lwarx r10
# ,0,r11, a value, a reference count, a particular count, a particular atomic operation), and decrements it (r10 - 1,
# a decrement, a particular count / a particular state), and atomically stores it (stwcx. r10,0,r11, a value, a
# reference count, a particular count, a particular atomic operation), and restores the msr (mtmsrd r9,1, a
# particular state, a particular lock), and (if the store failed, a particular state, a particular lock) retries the
# operation (goto loop, a retry, a particular state / a particular operation, a particular atomic operation)). In paths
# it performs a complex computation (a reference count, a particular count / a particular state, a particular size / a
# particular allocation: it reads a value (r3->0, a value, a particular value), and (if the count is zero, a
# particular state, a particular count), it takes one path (a no-op / a return, a particular state, a particular
# result)). So it's a "perform an atomic decrement (a global lock, a particular lock / a particular state, using
# lwarx/stwcx, a particular atomic operation, on a value (obj->4, a value, a reference count, a particular count),
# decrementing it by 1, a particular count / a particular state), and (in paths) perform a complex computation (a
# reference count, a particular count / a particular state, a particular size / a particular allocation))" routine (an
# atomic decrement, part of the 0x826C0 cluster, a sibling of AtomicDecrement_822C72E0). 77 lines, no direct callees
# (pure atomic operation, using lwarx/stwcx with a global lock, a particular atomic operation). No direct callers (it's
# called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The object's class (obj, ->0 =
# a value, ->4 = a reference count, a particular structure, a reference counted object, a particular value / a
# particular count) and the value's meaning (a value, a reference count, a particular count) aren't resolvable, so the
# name states the mechanism: an atomic decrement (a global lock, a particular lock / a particular state, using
# lwarx/stwcx, a particular atomic operation, on a value (obj->4, a value, a reference count, a particular count),
# decrementing it by 1, a particular count / a particular state). The "2" distinguishes it from AtomicDecrement
# _822C72E0.
[entrypoint.functions."0x826C0B30"]
name = "AtomicDecrement2_826C0B30"

# A 32-byte constructor (the sibling of Construct32ByteVector_8321FB58 / ConstructNode32_82BACFB0, with a vector
# operation, the caller of Construct32ByteVector_8321FB58): perform a 32-byte constructor (with a vector operation, a
# SIMD computation, calling Construct32ByteVector_8321FB58, a 32-byte constructor, a particular 32-byte object / a
# particular size, a particular size / a particular allocation), and initialize the object's fields (obj->0 = a
# constant, from a fixed .data address, a class id / a marker, a particular class / a particular marker, obj->192 = 0, a
# field, a count / a size, a zero, obj->196 = -1, a field, a value, a particular value, obj->216 = a constant, from a
# fixed .data address, a float, a particular value, obj->221 = 0, a field, a count / a size, a zero, a 32-byte object,
# a particular structure, a particular size / a particular allocation).

# Args: obj (r3, r31, ->0 = a field, a constant, a class id / a marker, ->192 = a field, a count / a size, ->196 = a
# field, a value, ->216 = a field, a float, ->221 = a field, a count / a size, a 32-byte object), (more). It performs a
# 32-byte constructor (with a vector operation, a SIMD computation, calling Construct32ByteVector_8321FB58, sub
# _8321FB58, a 32-byte constructor, on a value (obj, r31, a 32-byte object, a particular structure, a particular size
# / a particular allocation), a particular 32-byte object / a particular size, a particular size / a particular
# allocation, a particular operation). It initializes the object's fields (obj->0 = a constant, from a fixed .data
# address, a class id / a marker, a particular class / a particular marker, obj->192 = 0, a field, a count / a size, a
# zero, obj->196 = -1, a field, a value, a particular value, obj->216 = a constant, from a fixed .data address, a
# float, a particular value, obj->221 = 0, a field, a count / a size, a zero, a 32-byte object, a particular
# structure, a particular size / a particular allocation, a particular initialization). So it's a "construct a 32-byte
# object (perform a 32-byte constructor (with a vector operation, a SIMD computation, calling Construct32ByteVector
# _8321FB58, a 32-byte constructor, a particular 32-byte object / a particular size, a particular size / a particular
# allocation), and initialize the object's fields (obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a particular marker, obj->192 = 0, a field, a count / a size, a zero, obj->196 = -1, a
# field, a value, a particular value, obj->216 = a constant, from a fixed .data address, a float, a particular value,
# obj->221 = 0, a field, a count / a size, a zero, a 32-byte object, a particular structure, a particular size / a
# particular allocation))" routine (a 32-byte constructor, with a vector operation, part of the 0x8323C cluster, a
# sibling of Construct32ByteVector_8321FB58, a caller of Construct32ByteVector_8321FB58). 83 lines, 1 callee
# (Construct32ByteVector_8321FB58, sub_8321FB58, a 32-byte constructor). 8 callers: sub_82A0ECC8, sub_82A0ED30, sub
# _82A0C2E0, sub_82A0D7F8, sub_8323EAE8, sub_82A0D630, sub_8323DB30, sub_82A0C4A8 (siblings in the 0x82A0E/0x82A0C/0
# x82A0D/0x8323E/0x8323D clusters). The object's class (obj, ->0 = a field, a constant, a class id / a marker, ->192 =
# a field, a count / a size, ->196 = a field, a value, ->216 = a field, a float, ->221 = a field, a count / a size, a
# 32-byte object, a particular structure, a particular size / a particular allocation) and the value's meaning (a
# value, a particular value, a particular size / a particular allocation) aren't resolvable, so the name states the
# mechanism: a 32-byte constructor (with a vector operation) (perform a 32-byte constructor (with a vector operation, a
# SIMD computation, calling Construct32ByteVector_8321FB58, a 32-byte constructor, a particular 32-byte object / a
# particular size, a particular size / a particular allocation), and initialize the object's fields (obj->0 = a
# constant, from a fixed .data address, a class id / a marker, a particular class / a particular marker, obj->192 = 0,
# a field, a count / a size, a zero, obj->196 = -1, a field, a value, a particular value, obj->216 = a constant, from a
# fixed .data address, a float, a particular value, obj->221 = 0, a field, a count / a size, a zero, a 32-byte object,
# a particular structure, a particular size / a particular allocation)). The "2" distinguishes it from
# Construct32ByteVector_8321FB58.
[entrypoint.functions."0x8323C7F0"]
name = "Construct32ByteVector2_8323C7F0"

# A process (a grow/append, with a byte copy and a 1-byte element, the sibling of ProcessGrowAppend_82BE4AA8 /
# ProcessGrowAppend2_8321F328 / ProcessGrowAppend3_8321FC38, the caller of ProcessFloatData_832206E8 /
# ProcessGrowAppend3_8321FC38 / sub_82A1B480): (if a count (a->20, a count, a particular count / a particular value)
# is >= 1, a particular threshold, a particular count) grow/append (a byte copy, a 1-byte element, a particular size /
# a particular allocation, copying a byte (a byte, a particular value, a 1-byte element, a particular value) from a
# base (a->12, a base, a 1-byte element, a particular value, a particular source) to a stack (a particular target), a
# particular copy / a particular allocation), and (in paths) call a process (ProcessFloatData_832206E8, on a value, a
# particular process, a particular operation), and a grow/append (ProcessGrowAppend3_8321FC38, on a value, a
# particular grow/append, a particular size / a particular allocation).

# Args: obj (r3, r30, an object), a (r4, r31, ->4 = a base, ->12 = a base, ->20 = a count, a particular structure, a
# range / a grow object), b (r5, r28, a value), c (r6, r27, a value), (more). It checks a count (r11, a->20, a count, a
# particular count / a particular value): if it is >= 1 (a particular threshold, a particular count, a particular
# value, a particular state), it grows/appends (a byte copy, a 1-byte element, a particular size / a particular
# allocation: it computes a count (r9, a->20 - 1, a count, a 1-byte element, a particular count / a particular value),
# and a base (r11, a->12, a base, a 1-byte element, a particular value, a particular source), and (r7, r11 + 1, a
# base, a 1-byte element, a particular value, a particular source), and copies a byte (r5, r11->0, a byte, a 1-byte
# element, a particular value, a particular source, to a stack (r8, a stack, a particular value, a particular target,
# a particular copy / a particular allocation), a particular copy / a particular allocation, advancing to the next 1-byte
# element, a particular grow/append, a particular size / a particular allocation). If the count is < 1 (a particular
# threshold, a particular count, a particular value, a particular state), it takes one path (a no-op / a return, a
# particular state, a particular result, a particular size / a particular allocation). In paths it calls a process
# (ProcessFloatData_832206E8, sub_832206E8, a process, on a value (a, a value, a particular value, a particular
# argument), a particular process, a particular operation, a particular size / a particular allocation), and a
# grow/append (ProcessGrowAppend3_8321FC38, sub_8321FC38, a grow/append, on a value (a, a value, a particular value, a
# particular argument), a particular grow/append, a particular size / a particular allocation, a particular operation).
# So it's a "process (a grow/append, with a byte copy and a 1-byte element: (if a count (a->20, a count, a particular
# count / a particular value) is >= 1, a particular threshold, a particular count) grow/append (a byte copy, a 1-byte
# element, a particular size / a particular allocation, copying a byte (a byte, a particular value, a 1-byte element, a
# particular value) from a base (a->12, a base, a 1-byte element, a particular value, a particular source) to a stack
# (a particular target), a particular copy / a particular allocation), and (in paths) call a process (ProcessFloatData
# _832206E8, on a value, a particular process, a particular operation), and a grow/append (ProcessGrowAppend3
# _8321FC38, on a value, a particular grow/append, a particular size / a particular allocation))" routine (a
# grow/append process, part of the 0x8323C cluster, a sibling of ProcessGrowAppend_82BE4AA8, a caller of
# ProcessFloatData_832206E8 and ProcessGrowAppend3_8321FC38). 808 lines, 3 callees (ProcessFloatData_832206E8,
# ProcessGrowAppend3_8321FC38, sub_82A1B480). 2 callers: sub_8323EB40, sub_8323DBB0 (siblings in the 0x8323E/0x8323D
# clusters, a range computation process, which calls this to grow/append, a particular size / a particular allocation,
# a particular operation). The object's class (obj, r30, an object, a particular structure, a range / a grow object, a
# particular size / a particular allocation) and the value's meaning (a value, a particular value, a particular size /
# a particular allocation) aren't resolvable, so the name states the mechanism: a grow/append process ((if a count
# (a->20, a count, a particular count / a particular value) is >= 1, a particular threshold, a particular count) grow
# /append (a byte copy, a 1-byte element, a particular size / a particular allocation, copying a byte (a byte, a
# particular value, a 1-byte element, a particular value) from a base (a->12, a base, a 1-byte element, a particular
# value, a particular source) to a stack (a particular target), a particular copy / a particular allocation), and (in
# paths) call a process (ProcessFloatData_832206E8, on a value, a particular process, a particular operation), and a
# grow/append (ProcessGrowAppend3_8321FC38, on a value, a particular grow/append, a particular size / a particular
# allocation)). The "4" distinguishes it from ProcessGrowAppend_82BE4AA8, ProcessGrowAppend2_8321F328, and
# ProcessGrowAppend3_8321FC38.
[entrypoint.functions."0x8323CB98"]
name = "ProcessGrowAppend4_8323CB98"

# A process (an element access, with a bounds check and a complex computation, a 2-byte element, the sibling of
# ProcessElementAccess_8217A400 / ValidateElementAccess_8217A7F8, the caller of ProcessValue_FlagNonZero_8217A5E0 /
# ClampAndUpdate_822F6380 / Buffer_CopyOrZero_Error_82CA3730): (if a size (obj->24, a size, a particular value) is < 8,
# a particular threshold, a particular size) take one path (a no-op / a return, a particular state, a particular
# result, a particular size / a particular allocation), and (otherwise) perform an element access (a bounds check, a
# particular check / a particular state, a complex computation, a particular computation / a particular operation, a
# 2-byte element, a particular size / a particular allocation, a particular value / a particular operation), and (in
# paths) call a process (ProcessValue_FlagNonZero_8217A5E0, on a value, a particular process, a particular operation, a
# particular size / a particular allocation), and clamp/update (ClampAndUpdate_822F6380, on a value, a particular
# clamp/update, a particular size / a particular allocation, a particular operation).

# Args: obj (r3, r31, ->4 = a base, ->20 = a size, ->24 = a size, a particular structure, a range / a bounds object),
# index (r4, r28, an index, a particular value, a particular count), a (r5, r27, a value), (more). It checks a size (r9
# , obj->24, a size, a particular value, a particular size / a particular allocation): if it is < 8 (a particular
# threshold, a particular size, a particular value, a particular state, a particular size / a particular allocation), it
# takes one path (a no-op / a return, a particular state, a particular result, a particular size / a particular
# allocation); if it is >= 8 (a particular threshold, a particular size, a particular value, a particular state, a
# particular size / a particular allocation), it performs an element access (a bounds check, a particular check / a
# particular state, a complex computation, a particular computation / a particular operation, a 2-byte element, a
# particular size / a particular allocation, a particular value / a particular operation: it checks an index (r28, an
# index, a particular value, a particular count) against a base (r11, obj->4, a base, a particular value, a particular
# base / a particular state, a particular size / a particular allocation): if the index is < the base (a particular
# threshold, a particular size, a particular value, a particular state, a particular size / a particular allocation), it
# takes one path (a no-op / a return, a particular state, a particular result, a particular size / a particular
# allocation); if the index is >= the base (a particular threshold, a particular size, a particular value, a particular
# state, a particular size / a particular allocation), it performs an element access (a complex computation, a
# particular computation / a particular operation, a 2-byte element, a particular size / a particular allocation, a
# particular value / a particular operation: it computes a size (r10, obj->20, a size, a particular value, a particular
# size / a particular allocation), and a size (r10 * 2, a size, a 2-byte element, a particular size / a particular
# allocation), and a pointer (r8, r10 + r11, a pointer, a particular value, a particular pointer / a particular state,
# a particular size / a particular allocation), and (in paths) calls a process (ProcessValue_FlagNonZero_8217A5E0, sub
# _8217A5E0, a process, on a value (a, a value, a particular value, a particular size / a particular allocation), a
# particular process, a particular operation, a particular size / a particular allocation), and clamps/updates
# (ClampAndUpdate_822F6380, sub_822F6380, a clamp/update, on a value (a, a value, a particular value, a particular size
# / a particular allocation), a particular clamp/update, a particular size / a particular allocation, a particular
# operation). So it's a "process (an element access, with a bounds check and a complex computation, a 2-byte element:
# (if a size (obj->24, a size, a particular value) is < 8, a particular threshold, a particular size) take one path (a
# no-op / a return, a particular state, a particular result, a particular size / a particular allocation), and
# (otherwise) perform an element access (a bounds check, a particular check / a particular state, a complex
# computation, a particular computation / a particular operation, a 2-byte element, a particular size / a particular
# allocation, a particular value / a particular operation), and (in paths) call a process (ProcessValue_FlagNonZero
# _8217A5E0, on a value, a particular process, a particular operation, a particular size / a particular allocation), and
# clamp/update (ClampAndUpdate_822F6380, on a value, a particular clamp/update, a particular size / a particular
# allocation, a particular operation))" routine (an element access process, part of the 0x8217A cluster, a sibling of
# ProcessElementAccess_8217A400 and ValidateElementAccess_8217A7F8, a caller of ProcessValue_FlagNonZero_8217A5E0, a
# particular process, a particular operation). 182 lines, 3 callees (Buffer_CopyOrZero_Error_82CA3730, ClampAndUpdate
# _822F6380, ProcessValue_FlagNonZero_8217A5E0). 10 callers: sub_82C72640, sub_822F21B8, sub_82C68B50, sub_82C809C8, sub
# _82C66A58, sub_82C7F458, sub_82C7C778, sub_82C7FB28, sub_82BFD228, sub_82C7A220 (siblings in the 0x82C72/0x822F2/0
# x82C68/0x82C80/0x82C66/0x82C7F/0x82C7C/0x82C7F/0x82BFD/0x82C7A clusters). The object's class (obj, ->4 = a base, ->
# 20 = a size, ->24 = a size, a particular structure, a range / a bounds object, a particular size / a particular
# allocation, a particular value / a particular state) and the index's meaning (an index, a particular value, a
# particular count) aren't resolvable, so the name states the mechanism: an element access process (with a bounds check
# and a complex computation, a 2-byte element) ((if a size (obj->24, a size, a particular value) is < 8, a particular
# threshold, a particular size) take one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation), and (otherwise) perform an element access (a bounds check, a particular
# check / a particular state, a complex computation, a particular computation / a particular operation, a 2-byte
# element, a particular size / a particular allocation, a particular value / a particular operation), and (in paths) call
# a process (ProcessValue_FlagNonZero_8217A5E0, on a value, a particular process, a particular operation, a particular
# size / a particular allocation), and clamp/update (ClampAndUpdate_822F6380, on a value, a particular clamp/update, a
# particular size / a particular allocation, a particular operation)). The "2" distinguishes it from
# ProcessElementAccess_8217A400.
[entrypoint.functions."0x8217A6C0"]
name = "ProcessElementAccess2_8217A6C0"

# A process (a float data process, with a dispatch and a complex computation, the caller of ProcessFloatData_832206E8
# / BuildAndDispatch_83220548 / ProcessGrowAppend4_8323CB98): perform a float data process (a float, a particular
# value, a particular size / a particular allocation, a particular operation, calling ProcessFloatData_832206E8, on a
# value and a constant (4, a constant, a particular value, a particular index / a particular state), a particular
# process, a particular operation, a particular size / a particular allocation), and (in paths) build and dispatch (a
# build, a particular build / a particular operation, a dispatch, a particular dispatch / a particular state, calling
# BuildAndDispatch_83220548, on a value and a constant (0, a constant, a particular value, a particular index / a
# particular state), a particular build, a particular operation, a particular size / a particular allocation), and a
# grow/append (a byte copy, a 1-byte element, a particular size / a particular allocation, calling ProcessGrowAppend4
# _8323CB98, on a value, a particular grow/append, a particular size / a particular allocation, a particular
# operation).

# Args: obj (r3, r30, an object, a float data object, a particular structure, a particular size / a particular
# allocation), a (r4, r31, a value, a float data, a particular value, a particular size / a particular allocation), b
# (r5, r28, a value), c (r6, r27, a value), d (r7, r29, a value), (more). It performs a float data process (a float, a
# particular value, a particular size / a particular allocation, a particular operation, calling ProcessFloatData
# _832206E8, sub_832206E8, a process, on a value (obj, r30, an object, a float data object, a particular structure, a
# particular size / a particular allocation), and a (r31, a value, a float data, a particular value, a particular size
# / a particular allocation), and a constant (4, a constant, a particular value, a particular index / a particular
# state), a particular process, a particular operation, a particular size / a particular allocation, and (ProcessFloat
# Data_832206E8, sub_832206E8, a process, on a value (obj, r30, an object, a float data object, a particular structure,
# a particular size / a particular allocation), and a (r31, a value, a float data, a particular value, a particular
# size / a particular allocation), and a constant (5, a constant, a particular value, a particular index / a
# particular state), a particular process, a particular operation, a particular size / a particular allocation)). In
# paths it builds and dispatches (a build, a particular build / a particular operation, a dispatch, a particular
# dispatch / a particular state, calling BuildAndDispatch_83220548, sub_83220548, a build and dispatch, on a value
# (obj, r30, an object, a float data object, a particular structure, a particular size / a particular allocation), and
# a (r31, a value, a float data, a particular value, a particular size / a particular allocation), and a constant (0, a
# constant, a particular value, a particular index / a particular state), a particular build, a particular operation, a
# particular size / a particular allocation), and grows/appends (a byte copy, a 1-byte element, a particular size / a
# particular allocation, calling ProcessGrowAppend4_8323CB98, sub_8323CB98, a grow/append, on a value (a, a value, a
# particular value, a particular size / a particular allocation), a particular grow/append, a particular size / a
# particular allocation, a particular operation). So it's a "process (a float data process, with a dispatch and a
# complex computation: perform a float data process (a float, a particular value, a particular size / a particular
# allocation, a particular operation, calling ProcessFloatData_832206E8, on a value and a constant (4, a constant, a
# particular value, a particular index / a particular state), a particular process, a particular operation, a
# particular size / a particular allocation), and (in paths) build and dispatch (a build, a particular build / a
# particular operation, a dispatch, a particular dispatch / a particular state, calling BuildAndDispatch_83220548, on a
# value and a constant (0, a constant, a particular value, a particular index / a particular state), a particular
# build, a particular operation, a particular size / a particular allocation), and a grow/append (a byte copy, a 1-byte
# element, a particular size / a particular allocation, calling ProcessGrowAppend4_8323CB98, on a value, a particular
# grow/append, a particular size / a particular allocation, a particular operation))" routine (a float data process,
# part of the 0x8323E cluster, a caller of ProcessFloatData_832206E8, BuildAndDispatch_83220548, and
# ProcessGrowAppend4_8323CB98). 362 lines, 4 callees (BuildAndDispatch_83220548, ProcessFloatData_832206E8,
# ProcessGrowAppend4_8323CB98, sub_82A1B480). 4 callers: sub_82A0D1D0, sub_82A0CB68, sub_82A0D878, sub_82A0C528
# (siblings in the 0x82A0D/0x82A0C clusters, a process, which calls this to process the float data, a particular size
# / a particular allocation, a particular operation). The object's class (obj, r30, an object, a float data object, a
# particular structure, a particular size / a particular allocation, a particular value / a particular state) and the
# value's meaning (a, r31, a value, a float data, a particular value, a particular size / a particular allocation)
# aren't resolvable, so the name states the mechanism: a float data process (with a dispatch and a complex computation)
# (perform a float data process (a float, a particular value, a particular size / a particular allocation, a
# particular operation, calling ProcessFloatData_832206E8, on a value and a constant (4, a constant, a particular
# value, a particular index / a particular state), a particular process, a particular operation, a particular size / a
# particular allocation), and (in paths) build and dispatch (a build, a particular build / a particular operation, a
# dispatch, a particular dispatch / a particular state, calling BuildAndDispatch_83220548, on a value and a constant
# (0, a constant, a particular value, a particular index / a particular state), a particular build, a particular
# operation, a particular size / a particular allocation), and a grow/append (a byte copy, a 1-byte element, a
# particular size / a particular allocation, calling ProcessGrowAppend4_8323CB98, on a value, a particular
# grow/append, a particular size / a particular allocation, a particular operation)). The "Dispatch" refers to the
# dispatch (a dispatch, a particular dispatch / a particular state, a particular operation, a particular size / a
# particular allocation).
[entrypoint.functions."0x8323EB40"]
name = "ProcessFloatDataDispatch_8323EB40"

# A destruct (a destructor, with a free and a complex computation, the sibling of Destruct20Byte_832156E0, the caller
# of Destruct20Byte_832156E0 / Free_SizeBucketed_Thunk_8221BE68): initialize the object's fields (obj->0 = a constant
# , from a fixed .data address, a class id / a marker, a particular class / a particular marker, a 20-byte object, a
# particular structure, a particular size / a particular allocation), and perform a destructor (a free, a particular
# free / a particular count, a complex computation, a particular computation / a particular operation, a particular
# size / a particular allocation, calling Destruct20Byte_832156E0, a destructor, a particular destruct / a particular
# size, a particular size / a particular allocation, and Free_SizeBucketed_Thunk_8221BE68, a free, a particular free /
# a particular count, a particular size / a particular allocation).

# Args: obj (r3, r31, ->0 = a field, a constant, a class id / a marker, a 20-byte object, a particular structure, a
# particular size / a particular allocation), a (r4, r30, a value, a flag, a particular value, a particular size / a
# particular allocation), (more). It initializes the object's fields (obj->0 = a constant, from a fixed .data address,
# a class id / a marker, a particular class / a particular marker, a 20-byte object, a particular structure, a
# particular size / a particular allocation, a particular initialization). It performs a destructor (a free, a
# particular free / a particular count, a complex computation, a particular computation / a particular operation, a
# particular size / a particular allocation: it calls a destructor (Destruct20Byte_832156E0, sub_832156E0, a
# destructor, on a value (obj, r31, a 20-byte object, a particular structure, a particular size / a particular
# allocation), a particular destruct / a particular size, a particular size / a particular allocation, a particular
# operation), and checks a flag (r9, a & 1, a flag, a particular value, a particular state, a particular size / a
# particular allocation): if the flag is non-zero (a particular state, a particular value, a particular size / a
# particular allocation), it frees (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a free, on a value (obj, r31, a 20
# -byte object, a particular structure, a particular size / a particular allocation), a particular free / a particular
# count, a particular size / a particular allocation, a particular operation); if the flag is zero (a null state, a
# particular state, a particular size / a particular allocation), it takes one path (a no-op / a return, a particular
# state, a particular result, a particular size / a particular allocation). So it's a "destruct (a destructor, with a
# free and a complex computation: initialize the object's fields (obj->0 = a constant, from a fixed .data address, a
# class id / a marker, a particular class / a particular marker, a 20-byte object, a particular structure, a
# particular size / a particular allocation), and perform a destructor (a free, a particular free / a particular
# count, a complex computation, a particular computation / a particular operation, a particular size / a particular
# allocation, calling Destruct20Byte_832156E0, a destructor, a particular destruct / a particular size, a particular
# size / a particular allocation, and Free_SizeBucketed_Thunk_8221BE68, a free, a particular free / a particular
# count, a particular size / a particular allocation))" routine (a destructor, part of the 0x82A11 cluster, a sibling
# of Destruct20Byte_832156E0, a caller of Destruct20Byte_832156E0 and Free_SizeBucketed_Thunk_8221BE68). 56 lines, 2
# callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). No direct callers (it's called via a vtable / it
# is registered-but-uncalled, part of the game's subsystems). The object's class (obj, ->0 = a field, a constant, a
# class id / a marker, a 20-byte object, a particular structure, a particular size / a particular allocation, a
# particular value / a particular state) and the value's meaning (a, r30, a value, a flag, a particular value, a
# particular size / a particular allocation) aren't resolvable, so the name states the mechanism: a destructor (with a
# free and a complex computation) (initialize the object's fields (obj->0 = a constant, from a fixed .data address, a
# class id / a marker, a particular class / a particular marker, a 20-byte object, a particular structure, a
# particular size / a particular allocation), and perform a destructor (a free, a particular free / a particular
# count, a complex computation, a particular computation / a particular operation, a particular size / a particular
# allocation, calling Destruct20Byte_832156E0, a destructor, a particular destruct / a particular size, a particular
# size / a particular allocation, and Free_SizeBucketed_Thunk_8221BE68, a free, a particular free / a particular
# count, a particular size / a particular allocation)). The "20ByteFree" refers to the 20-byte destructor (a
# destructor, a particular destruct / a particular size, a particular size / a particular allocation) and the free (a
# free, a particular free / a particular count, a particular size / a particular allocation).
[entrypoint.functions."0x82A11F90"]
name = "Destruct20ByteFree_82A11F90"

# A process (a grow/append, with a byte-by-byte copy and a 4-byte element, the sibling of ProcessGrowAppend2_8321F328
# / ProcessGrowAppend_82BE4AA8, the caller of CopyStructData2_82A1BD30 / sub_82A1B480 / sub_83211D48): (if a count
# (a->20, a count, a particular count / a particular value) is >= 4, a particular threshold, a particular count) grow
# /append (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte
# element (four bytes, a particular 4-byte element, a 4-byte element, a particular value) from a base (a->12, a base,
# a 4-byte element, a particular value, a particular source) to a stack (a particular target), a particular copy / a
# particular allocation), and (in paths) copy (CopyStructData2_82A1BD30, on a value, a particular copy / a particular
# size, a particular size / a particular allocation, a particular operation).

# Args: obj (r3, r30, an object), a (r4, r31, ->4 = a base, ->12 = a base, ->20 = a count, a particular structure, a
# range / a grow object), b (r5, r29, a value), (more). It checks a count (r11, a->20, a count, a particular count /
# a particular value): if it is >= 4 (a particular threshold, a particular count, a particular value, a particular
# state), it grows/appends (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation: it
# computes a count (r9, a->20 - 4, a count, a 4-byte element, a particular count / a particular value), and a base
# (r11, a->12, a base, a 4-byte element, a particular value, a particular source), and (r7, r11 + 4, a base, a 4-byte
# element, a particular value, a particular source), and copies a 4-byte element (four bytes, a particular 4-byte
# element, a 4-byte element, a particular value: lbz r5, 0, r11, a byte, a 4-byte element, a particular value, and stb
# r5, 0, r8, a store, a 4-byte element, a particular copy / a particular allocation, and lbz r4, 1, r11, a byte, a 4
# -byte element, a particular value, and stb r4, 1, r8, a store, a 4-byte element, a particular copy / a particular
# allocation, and lbz r3, 2, r11, a byte, a 4-byte element, a particular value, and stb r3, 2, r8, a store, a 4-byte
# element, a particular copy / a particular allocation, and lbz r11, 3, r11, a byte, a 4-byte element, a particular
# value, and stb r11, 3, r8, a store, a 4-byte element, a particular copy / a particular allocation), advancing to the
# next 4-byte element, a particular grow/append, a particular size / a particular allocation). If the count is < 4 (a
# particular threshold, a particular count, a particular value, a particular state), it takes one path (a no-op / a
# return, a particular state, a particular result, a particular size / a particular allocation). In paths it copies
# (CopyStructData2_82A1BD30, sub_82A1BD30, a copy, on a value (a, a value, a particular value, a particular size / a
# particular allocation), a particular copy / a particular size, a particular size / a particular allocation, a
# particular operation). So it's a "process (a grow/append, with a byte-by-byte copy and a 4-byte element: (if a count
# (a->20, a count, a particular count / a particular value) is >= 4, a particular threshold, a particular count) grow
# /append (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte
# element (four bytes, a particular 4-byte element, a 4-byte element, a particular value) from a base (a->12, a base,
# a 4-byte element, a particular value, a particular source) to a stack (a particular target), a particular copy / a
# particular allocation), and (in paths) copy (CopyStructData2_82A1BD30, on a value, a particular copy / a particular
# size, a particular size / a particular allocation, a particular operation))" routine (a grow/append process, part of
# the 0x83212 cluster, a sibling of ProcessGrowAppend2_8321F328, a caller of CopyStructData2_82A1BD30). 211 lines, 3
# callees (CopyStructData2_82A1BD30, sub_82A1B480, sub_83211D48). No direct callers (it's called via a vtable / it is
# registered-but-uncalled, part of the game's subsystems). The object's class (obj, r30, an object, a particular
# structure, a range / a grow object, a particular size / a particular allocation) and the value's meaning (a, r31, ->
# 4 = a base, ->12 = a base, ->20 = a count, a particular structure, a range / a grow object, a particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a grow/append process ((if a count
# (a->20, a count, a particular count / a particular value) is >= 4, a particular threshold, a particular count) grow
# /append (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte
# element (four bytes, a particular 4-byte element, a 4-byte element, a particular value) from a base (a->12, a base,
# a 4-byte element, a particular value, a particular source) to a stack (a particular target), a particular copy / a
# particular allocation), and (in paths) copy (CopyStructData2_82A1BD30, on a value, a particular copy / a particular
# size, a particular size / a particular allocation, a particular operation)). The "5" distinguishes it from
# ProcessGrowAppend_82BE4AA8, ProcessGrowAppend2_8321F328, ProcessGrowAppend3_8321FC38, and ProcessGrowAppend4
# _8323CB98.
[entrypoint.functions."0x83212DC0"]
name = "ProcessGrowAppend5_83212DC0"

# A 20-byte constructor (the sibling of Construct20Byte_83215640, with a constant and a complex computation, the
# caller of Construct20Byte_83215640): perform a 20-byte constructor (a 20-byte object, a particular structure, a
# particular size / a particular allocation, a particular operation, calling Construct20Byte_83215640, a 20-byte
# constructor, a particular 20-byte object / a particular size, a particular size / a particular allocation), and
# initialize the object's fields (obj->0 = a constant, from a fixed .data address, a class id / a marker, a
# particular class / a particular marker, obj->16 = a constant, from a fixed .data address, a float, a particular
# value, obj->20 = a constant, from a fixed .data address, a float, a particular value, obj->24 = a constant, from a
# fixed .data address, a float, a particular value, obj->28 = a constant, from a fixed .data address, a float, a
# particular value, obj->32 = a constant, from a fixed .data address, a float, a particular value, a 20-byte object,
# a particular structure, a particular size / a particular allocation).

# Args: obj (r3, r31, ->0 = a field, a constant, a class id / a marker, ->16 = a field, a float, ->20 = a field, a
# float, ->24 = a field, a float, ->28 = a field, a float, ->32 = a field, a float, a 20-byte object), (more). It
# performs a 20-byte constructor (a 20-byte object, a particular structure, a particular size / a particular
# allocation, a particular operation, calling Construct20Byte_83215640, sub_83215640, a 20-byte constructor, on a
# value (obj, r31, a 20-byte object, a particular structure, a particular size / a particular allocation), a
# particular 20-byte object / a particular size, a particular size / a particular allocation, a particular operation)
# . It initializes the object's fields (obj->0 = a constant, from a fixed .data address, a class id / a marker, a
# particular class / a particular marker, obj->16 = a constant, from a fixed .data address, a float, a particular
# value, obj->20 = a constant, from a fixed .data address, a float, a particular value, obj->24 = a constant, from a
# fixed .data address, a float, a particular value, obj->28 = a constant, from a fixed .data address, a float, a
# particular value, obj->32 = a constant, from a fixed .data address, a float, a particular value, a 20-byte object,
# a particular structure, a particular size / a particular allocation, a particular initialization). So it's a
# "construct a 20-byte object (perform a 20-byte constructor (a 20-byte object, a particular structure, a particular
# size / a particular allocation, a particular operation, calling Construct20Byte_83215640, a 20-byte constructor, a
# particular 20-byte object / a particular size, a particular size / a particular allocation), and initialize the
# object's fields (obj->0 = a constant, from a fixed .data address, a class id / a marker, a particular class / a
# particular marker, obj->16 = a constant, from a fixed .data address, a float, a particular value, obj->20 = a
# constant, from a fixed .data address, a float, a particular value, obj->24 = a constant, from a fixed .data address,
# a float, a particular value, obj->28 = a constant, from a fixed .data address, a float, a particular value, obj->32
# = a constant, from a fixed .data address, a float, a particular value, a 20-byte object, a particular structure, a
# particular size / a particular allocation))" routine (a 20-byte constructor, with a constant and a complex
# computation, part of the 0x83215 cluster, a sibling of Construct20Byte_83215640, a caller of Construct20Byte
# _83215640). 69 lines, 1 callee (Construct20Byte_83215640, sub_83215640, a 20-byte constructor). 2 callers:
# ProcessTreeNode_83218C10, sub_83219588 (siblings in the 0x83218/0x83219 clusters, a process, which calls this to
# construct a 20-byte object, a particular size / a particular allocation, a particular operation). The object's class
# (obj, ->0 = a field, a constant, a class id / a marker, ->16 = a field, a float, ->20 = a field, a float, ->24 = a
# field, a float, ->28 = a field, a float, ->32 = a field, a float, a 20-byte object, a particular structure, a
# particular size / a particular allocation, a particular value / a particular state) and the value's meaning (a
# value, a particular value, a particular size / a particular allocation) aren't resolvable, so the name states the
# mechanism: a 20-byte constructor (with a constant and a complex computation) (perform a 20-byte constructor (a 20-
# byte object, a particular structure, a particular size / a particular allocation, a particular operation, calling
# Construct20Byte_83215640, a 20-byte constructor, a particular 20-byte object / a particular size, a particular size
# / a particular allocation), and initialize the object's fields (obj->0 = a constant, from a fixed .data address, a
# class id / a marker, a particular class / a particular marker, obj->16 = a constant, from a fixed .data address, a
# float, a particular value, obj->20 = a constant, from a fixed .data address, a float, a particular value, obj->24 = a
# constant, from a fixed .data address, a float, a particular value, obj->28 = a constant, from a fixed .data address,
# a float, a particular value, obj->32 = a constant, from a fixed .data address, a float, a particular value, a 20-byte
# object, a particular structure, a particular size / a particular allocation)). The "2" distinguishes it from
# Construct20Byte_83215640.
[entrypoint.functions."0x832158F8"]
name = "Construct20Byte2_832158F8"

# A destruct (a destructor, with a release and a free and a complex computation, the sibling of DestructRange_82518D30
# / Destruct20ByteFree_82A11F90, the caller of Release_RefCounted_829FF648 / Free_SizeBucketed_Thunk_8221BE68):
# (if a base (obj->4, a base, a particular value, a particular pointer / a particular state) is non-zero, a particular
# state) loop over a range (a range, a particular range / a particular allocation, a base, a particular value, a
# particular pointer / a particular state, a limit, obj->8, a limit, a particular value, a particular size / a
# particular allocation, a particular state / a particular operation), and for each item (a value, a particular value
# , a particular size / a particular allocation, a particular state / a particular operation), release a refcounted
# value (Release_RefCounted_829FF648, on a value (r31, a value, a particular value, a particular pointer / a
# particular state), a particular release / a particular count, a particular size / a particular allocation, a
# particular operation), and free the array (Free_SizeBucketed_Thunk_8221BE68, on a value (obj->4, a base, a
# particular value, a particular pointer / a particular state), a particular free / a particular count, a particular
# size / a particular allocation, a particular operation), and (otherwise) take one path (a no-op / a return, a
# particular state, a particular result, a particular size / a particular allocation), and clear the array's fields
# (obj->4 = 0, obj->8 = 0, obj->12 = 0, three zeros, a particular initialization, a particular size / a particular
# allocation).

# Args: obj (r3, r29, ->4 = a base, ->8 = a limit, ->12 = a size, a particular structure, an array / a range object, a
# particular size / a particular allocation), (more). It checks a base (r31, obj->4, a base, a particular value, a
# particular pointer / a particular state): if it is non-zero (a particular state, a particular value, a particular
# pointer / a particular state), it loops over a range (a range, a particular range / a particular allocation, a base
# (r31, obj->4, a base, a particular value, a particular pointer / a particular state), a limit (r30, obj->8, a limit,
# a particular value, a particular size / a particular allocation), a particular state / a particular operation: for
# each item (a value (r31, a value, a particular value, a particular pointer / a particular state), a particular size
# / a particular allocation, a particular state / a particular operation), it releases a refcounted value
# (Release_RefCounted_829FF648, sub_829FF648, a release, on a value (r31, a value, a particular value, a particular
# pointer / a particular state), a particular release / a particular count, a particular size / a particular
# allocation, a particular operation), and advances to the next item (r31, r31 + 8, a pointer, a particular value, a
# particular pointer / a particular state, a particular state / a particular operation); when the loop is done (a
# particular state / a particular result, a particular size / a particular allocation), it frees the array (Free_Size
# Bucketed_Thunk_8221BE68, sub_8221BE68, a free, on a value (obj->4, a base, a particular value, a particular pointer /
# a particular state), a particular free / a particular count, a particular size / a particular allocation, a
# particular operation). If the base is zero (a null state, a particular state, a particular value, a particular
# pointer / a particular state), it takes one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation). It clears the array's fields (obj->4 = 0, a base, a particular value, a
# particular pointer / a particular state, obj->8 = 0, a limit, a particular value, a particular size / a particular
# allocation, obj->12 = 0, a size, a particular value, a particular size / a particular allocation, three zeros, a
# particular initialization, a particular size / a particular allocation). So it's a "destruct (a destructor, with a
# release and a free and a complex computation: (if a base (obj->4, a base, a particular value, a particular pointer /
# a particular state) is non-zero, a particular state) loop over a range (a range, a particular range / a particular
# allocation, a base, a particular value, a particular pointer / a particular state, a limit, obj->8, a limit, a
# particular value, a particular size / a particular allocation, a particular state / a particular operation), and for
# each item (a value, a particular value, a particular size / a particular allocation, a particular state / a
# particular operation), release a refcounted value (Release_RefCounted_829FF648, on a value (r31, a value, a
# particular value, a particular pointer / a particular state), a particular release / a particular count, a
# particular size / a particular allocation, a particular operation), and free the array (Free_SizeBucketed_Thunk
# _8221BE68, on a value (obj->4, a base, a particular value, a particular pointer / a particular state), a particular
# free / a particular count, a particular size / a particular allocation, a particular operation), and (otherwise) take
# one path (a no-op / a return, a particular state, a particular result, a particular size / a particular allocation),
# and clear the array's fields (obj->4 = 0, obj->8 = 0, obj->12 = 0, three zeros, a particular initialization, a
# particular size / a particular allocation))" routine (a destructor, part of the 0x82356 cluster, a sibling of
# DestructRange_82518D30 and Destruct20ByteFree_82A11F90, a caller of Release_RefCounted_829FF648 and
# Free_SizeBucketed_Thunk_8221BE68). 60 lines, 2 callees (Free_SizeBucketed_Thunk_8221BE68, Release_RefCounted
# _829FF648). 10 callers: sub_82808AC0, sub_82922CE0, sub_82923448, sub_82723598, sub_82494378, sub_82581968, sub
# _82919220, sub_82B63A10, sub_82925D20, sub_82808888 (a widely-called destructor, part of the game's subsystems). The
# object's class (obj, ->4 = a base, ->8 = a limit, ->12 = a size, a particular structure, an array / a range object, a
# particular size / a particular allocation, a particular value / a particular state) and the value's meaning (a
# value, a particular value, a particular size / a particular allocation) aren't resolvable, so the name states the
# mechanism: a destructor (with a release and a free and a complex computation) ((if a base (obj->4, a base, a
# particular value, a particular pointer / a particular state) is non-zero, a particular state) loop over a range (a
# range, a particular range / a particular allocation, a base, a particular value, a particular pointer / a particular
# state, a limit, obj->8, a limit, a particular value, a particular size / a particular allocation, a particular state
# / a particular operation), and for each item (a value, a particular value, a particular size / a particular
# allocation, a particular state / a particular operation), release a refcounted value (Release_RefCounted_829FF648, on
# a value (r31, a value, a particular value, a particular pointer / a particular state), a particular release / a
# particular count, a particular size / a particular allocation, a particular operation), and free the array
# (Free_SizeBucketed_Thunk_8221BE68, on a value (obj->4, a base, a particular value, a particular pointer / a
# particular state), a particular free / a particular count, a particular size / a particular allocation, a
# particular operation), and (otherwise) take one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation), and clear the array's fields (obj->4 = 0, obj->8 = 0, obj->12 = 0, three
# zeros, a particular initialization, a particular size / a particular allocation)).
[entrypoint.functions."0x82356698"]
name = "DestructArray_82356698"

# A process (a grow/append, with a byte-by-byte copy and a 4-byte element, the sibling of ProcessGrowAppend2_8321F328
# / ProcessGrowAppend5_83212DC0): (if a count (a->20, a count, a particular count / a particular value) is >= 4, a
# particular threshold, a particular count) grow/append (a byte-by-byte copy, a 4-byte element, a particular size / a
# particular allocation, copying a 4-byte element (four bytes, a particular 4-byte element, a 4-byte element, a
# particular value) from a base (a->12, a base, a 4-byte element, a particular value, a particular source) to a stack
# (a particular target), a particular copy / a particular allocation), and (otherwise) take one path (a no-op / a
# return, a particular state, a particular result, a particular size / a particular allocation), and (in paths) call a
# process (sub_82A1B480, on a value, a particular process, a particular operation, a particular size / a particular
# allocation).

# Args: obj (r3, r28, an object), a (r4, r31, ->4 = a base, ->12 = a base, ->20 = a count, a particular structure, a
# range / a grow object), (more). It checks a count (r11, a->20, a count, a particular count / a particular value): if
# it is >= 4 (a particular threshold, a particular count, a particular value, a particular state), it grows/appends (a
# byte-by-byte copy, a 4-byte element, a particular size / a particular allocation: it computes a count (r9, a->20 - 4
# , a count, a 4-byte element, a particular count / a particular value), and a base (r11, a->12, a base, a 4-byte
# element, a particular value, a particular source), and (r7, r11 + 4, a base, a 4-byte element, a particular value, a
# particular source), and copies a 4-byte element (four bytes, a particular 4-byte element, a 4-byte element, a
# particular value: lbz r5, 0, r11, a byte, a 4-byte element, a particular value, and stb r5, 0, r8, a store, a 4-byte
# element, a particular copy / a particular allocation, and lbz r4, 1, r11, a byte, a 4-byte element, a particular
# value, and stb r4, 1, r8, a store, a 4-byte element, a particular copy / a particular allocation, and lbz r3, 2, r11
# , a byte, a 4-byte element, a particular value, and stb r3, 2, r8, a store, a 4-byte element, a particular copy / a
# particular allocation, and lbz r11, 3, r11, a byte, a 4-byte element, a particular value, and stb r11, 3, r8, a
# store, a 4-byte element, a particular copy / a particular allocation), advancing to the next 4-byte element, a
# particular grow/append, a particular size / a particular allocation). If the count is < 4 (a particular threshold, a
# particular count, a particular value, a particular state), it takes one path (a no-op / a return, a particular
# state, a particular result, a particular size / a particular allocation). In paths it calls a process (sub_82A1B480,
# on a value (a, a value, a particular value, a particular size / a particular allocation), a particular process, a
# particular operation, a particular size / a particular allocation). So it's a "process (a grow/append, with a
# byte-by-byte copy and a 4-byte element: (if a count (a->20, a count, a particular count / a particular value) is >=
# 4, a particular threshold, a particular count) grow/append (a byte-by-byte copy, a 4-byte element, a particular size
# / a particular allocation, copying a 4-byte element (four bytes, a particular 4-byte element, a 4-byte element, a
# particular value) from a base (a->12, a base, a 4-byte element, a particular value, a particular source) to a stack
# (a particular target), a particular copy / a particular allocation), and (otherwise) take one path (a no-op / a
# return, a particular state, a particular result, a particular size / a particular allocation), and (in paths) call a
# process (sub_82A1B480, on a value, a particular process, a particular operation, a particular size / a particular
# allocation))" routine (a grow/append process, part of the 0x83216 cluster, a sibling of ProcessGrowAppend2_8321F328
# and ProcessGrowAppend5_83212DC0). 368 lines, 1 callee (sub_82A1B480, a sibling in the 0x82A1B cluster). 1 caller: sub
# _83221548 (a sibling in the 0x83221 cluster, a process, which calls this to grow/append, a particular size / a
# particular allocation, a particular operation). The object's class (obj, r28, an object, a particular structure, a
# range / a grow object, a particular size / a particular allocation) and the value's meaning (a, r31, ->4 = a base,
# ->12 = a base, ->20 = a count, a particular structure, a range / a grow object, a particular size / a particular
# allocation) aren't resolvable, so the name states the mechanism: a grow/append process ((if a count (a->20, a count,
# a particular count / a particular value) is >= 4, a particular threshold, a particular count) grow/append (a
# byte-by-byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte element (four
# bytes, a particular 4-byte element, a 4-byte element, a particular value) from a base (a->12, a base, a 4-byte
# element, a particular value, a particular source) to a stack (a particular target), a particular copy / a particular
# allocation), and (otherwise) take one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation), and (in paths) call a process (sub_82A1B480, on a value, a particular
# process, a particular operation, a particular size / a particular allocation)). The "6" distinguishes it from
# ProcessGrowAppend_82BE4AA8, ProcessGrowAppend2_8321F328, ProcessGrowAppend3_8321FC38, ProcessGrowAppend4_8323CB98,
# and ProcessGrowAppend5_83212DC0.
[entrypoint.functions."0x83216060"]
name = "ProcessGrowAppend6_83216060"

# A process (a grow/append, with a byte-by-byte copy and a 4-byte element, the sibling of ProcessGrowAppend2_8321F328
# / ProcessGrowAppend5_83212DC0 / ProcessGrowAppend6_83216060): (if a count (obj->20, a count, a particular count /
# a particular value) is >= 4, a particular threshold, a particular count) grow/append (a byte-by-byte copy, a 4-byte
# element, a particular size / a particular allocation, copying a 4-byte element (four bytes, a particular 4-byte
# element, a 4-byte element, a particular value) from a base (obj->12, a base, a 4-byte element, a particular value, a
# particular source) to a stack (a particular target), a particular copy / a particular allocation), and (otherwise)
# take one path (a no-op / a return, a particular state, a particular result, a particular size / a particular
# allocation), and (in paths) call a process (sub_82A1B480, on a value, a particular process, a particular operation, a
# particular size / a particular allocation).

# Args: obj (r3, r31, ->4 = a base, ->12 = a base, ->20 = a count, a particular structure, a range / a grow object), a
# (r4, r30, a value), (more). It checks a count (r10, obj->20, a count, a particular count / a particular value): if it
# is >= 4 (a particular threshold, a particular count, a particular value, a particular state), it grows/appends (a
# byte-by-byte copy, a 4-byte element, a particular size / a particular allocation: it computes a count (r7, obj->20 -
# 4, a count, a 4-byte element, a particular count / a particular value), and a base (r11, obj->12, a base, a 4-byte
# element, a particular value, a particular source), and (r6, r11 + 4, a base, a 4-byte element, a particular value, a
# particular source), and copies a 4-byte element (four bytes, a particular 4-byte element, a 4-byte element, a
# particular value: lbz r4, 0, r11, a byte, a 4-byte element, a particular value, and stb r4, 0, r8, a store, a 4-byte
# element, a particular copy / a particular allocation, and lbz r3, 1, r11, a byte, a 4-byte element, a particular
# value, and stb r3, 1, r8, a store, a 4-byte element, a particular copy / a particular allocation, and lbz r10, 2,
# r11, a byte, a 4-byte element, a particular value, and stb r10, 2, r8, a store, a 4-byte element, a particular copy /
# a particular allocation, and lbz r9, 3, r11, a byte, a 4-byte element, a particular value, and stb r9, 3, r8, a
# store, a 4-byte element, a particular copy / a particular allocation), advancing to the next 4-byte element, a
# particular grow/append, a particular size / a particular allocation). If the count is < 4 (a particular threshold, a
# particular count, a particular value, a particular state), it takes one path (a no-op / a return, a particular
# state, a particular result, a particular size / a particular allocation). In paths it calls a process (sub_82A1B480,
# on a value (a, a value, a particular value, a particular size / a particular allocation), a particular process, a
# particular operation, a particular size / a particular allocation). So it's a "process (a grow/append, with a
# byte-by-byte copy and a 4-byte element: (if a count (obj->20, a count, a particular count / a particular value) is >=
# 4, a particular threshold, a particular count) grow/append (a byte-by-byte copy, a 4-byte element, a particular size
# / a particular allocation, copying a 4-byte element (four bytes, a particular 4-byte element, a 4-byte element, a
# particular value) from a base (obj->12, a base, a 4-byte element, a particular value, a particular source) to a
# stack (a particular target), a particular copy / a particular allocation), and (otherwise) take one path (a no-op / a
# return, a particular state, a particular result, a particular size / a particular allocation), and (in paths) call a
# process (sub_82A1B480, on a value, a particular process, a particular operation, a particular size / a particular
# allocation))" routine (a grow/append process, part of the 0x83221 cluster, a sibling of ProcessGrowAppend2
# _8321F328, ProcessGrowAppend5_83212DC0, and ProcessGrowAppend6_83216060). 122 lines, 1 callee (sub_82A1B480, a
# sibling in the 0x82A1B cluster). 3 callers: sub_83225A20, sub_832212D0, sub_83225BC0 (siblings in the 0x83225/0
# x83221 clusters, a process, which calls this to grow/append, a particular size / a particular allocation, a
# particular operation). The object's class (obj, ->4 = a base, ->12 = a base, ->20 = a count, a particular
# structure, a range / a grow object, a particular size / a particular allocation) and the value's meaning (a, r30, a
# value, a particular size / a particular allocation) aren't resolvable, so the name states the mechanism: a
# grow/append process ((if a count (obj->20, a count, a particular count / a particular value) is >= 4, a particular
# threshold, a particular count) grow/append (a byte-by-byte copy, a 4-byte element, a particular size / a particular
# allocation, copying a 4-byte element (four bytes, a particular 4-byte element, a 4-byte element, a particular value)
# from a base (obj->12, a base, a 4-byte element, a particular value, a particular source) to a stack (a particular
# target), a particular copy / a particular allocation), and (otherwise) take one path (a no-op / a return, a
# particular state, a particular result, a particular size / a particular allocation), and (in paths) call a process
# (sub_82A1B480, on a value, a particular process, a particular operation, a particular size / a particular
# allocation)). The "7" distinguishes it from ProcessGrowAppend_82BE4AA8, ProcessGrowAppend2_8321F328,
# ProcessGrowAppend3_8321FC38, ProcessGrowAppend4_8323CB98, ProcessGrowAppend5_83212DC0, and ProcessGrowAppend6
# _83216060.
[entrypoint.functions."0x83221138"]
name = "ProcessGrowAppend7_83221138"

# A process (a grow/append, with a complex computation and a constant, the sibling of ProcessGrowAppend2_8321F328 / the
# caller of Allocate_OrDbgBreak_8221F388 / ComputeWrapped_8222C3E8 / SetValue3_82A09FB0 / sub_83221320 / sub_832351B0 /
# sub_82AACDE0): (if a size (a->8 - a->4, a size, a 8-byte element, a particular size) is non-zero, a particular
# state) grow/append (a complex computation, a particular computation / a particular operation, a constant, from a
# fixed .data address, a particular value, a particular size / a particular allocation, a particular operation, calling
# Allocate_OrDbgBreak_8221F388, an allocation, a particular allocation / a particular size, a particular size / a
# particular allocation, and ComputeWrapped_8222C3E8, a compute, a particular computation / a particular operation, a
# particular size / a particular allocation, and SetValue3_82A09FB0, a set, a particular set / a particular value, a
# particular size / a particular allocation), and (otherwise) take one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation).

# Args: obj (r3, r29, ->12 = a base, ->28 = a value, ->32 = a value, ->36 = a value, a particular structure, a grow /
# a range object), a (r4, r28, ->4 = a base, ->8 = a limit, a particular structure, a range / a grow object), (more). It
# checks a size (r8, a->8 - a->4, a size, a 8-byte element, a particular size, a particular size / a particular
# allocation): if it is non-zero (a particular state, a particular size, a particular value, a particular size / a
# particular allocation), it grows/appends (a complex computation, a particular computation / a particular operation, a
# constant, from a fixed .data address, a particular value, a particular size / a particular allocation, a particular
# operation: it calls a complex computation (sub_83221320, a complex computation, a particular computation / a
# particular operation, a particular size / a particular allocation, a particular state / a particular result), and a
# complex computation (sub_832351B0, on a value (a, a value, a particular value, a particular size / a particular
# allocation), and obj->36, a value, a particular value, a particular size / a particular allocation, and obj->32, a
# value, a particular value, a particular size / a particular allocation), a particular computation / a particular
# operation, a particular size / a particular allocation, a particular state / a particular result), and a complex
# computation (sub_82AACDE0, on a value (obj + 12, a base, a particular value, a particular size / a particular
# allocation), a particular computation / a particular operation, a particular size / a particular allocation, a
# particular state / a particular result), and (in paths) allocates (Allocate_OrDbgBreak_8221F388, sub_8221F388, an
# allocation, on a value (a, a value, a particular value, a particular size, a particular size / a particular
# allocation), a particular allocation / a particular size, a particular size / a particular allocation, a particular
# state / a particular result), and computes (ComputeWrapped_8222C3E8, sub_8222C3E8, a compute, on a value (a, a
# value, a particular value, a particular size, a particular size / a particular allocation), a particular
# computation / a particular operation, a particular size / a particular allocation, a particular state / a particular
# result), and sets (SetValue3_82A09FB0, sub_82A09FB0, a set, on a value (a, a value, a particular value, a
# particular size, a particular size / a particular allocation), a particular set / a particular value, a particular
# size / a particular allocation, a particular state / a particular result). If the size is zero (a null state, a
# particular state, a particular size, a particular value, a particular size / a particular allocation), it takes one
# path (a no-op / a return, a particular state, a particular result, a particular size / a particular allocation). So
# it's a "process (a grow/append, with a complex computation and a constant: (if a size (a->8 - a->4, a size, a 8-byte
# element, a particular size) is non-zero, a particular state) grow/append (a complex computation, a particular
# computation / a particular operation, a constant, from a fixed .data address, a particular value, a particular size /
# a particular allocation, a particular operation, calling Allocate_OrDbgBreak_8221F388, an allocation, a particular
# allocation / a particular size, a particular size / a particular allocation, and ComputeWrapped_8222C3E8, a compute,
# a particular computation / a particular operation, a particular size / a particular allocation, and SetValue3
# _82A09FB0, a set, a particular set / a particular value, a particular size / a particular allocation), and
# (otherwise) take one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation))" routine (a grow/append process, part of the 0x83221 cluster, a sibling of
# ProcessGrowAppend2_8321F328, a caller of Allocate_OrDbgBreak_8221F388, ComputeWrapped_8222C3E8, and SetValue3
# _82A09FB0). 277 lines, 10 callees (Allocate_OrDbgBreak_8221F388, ComputeWrapped_8222C3E8, SetValue3_82A09FB0, sub
# _82AACDE0, sub_83221320, sub_832351B0, sub_8323FA78, sub_8323FCA8, sub_8323FF48, sub_832402B8). 2 callers: sub
# _832217E8, sub_83221548 (siblings in the 0x83221 cluster, a process, which calls this to grow/append, a particular
# size / a particular allocation, a particular operation). The object's class (obj, ->12 = a base, ->28 = a value, ->
# 32 = a value, ->36 = a value, a particular structure, a grow / a range object, a particular size / a particular
# allocation) and the value's meaning (a, r28, ->4 = a base, ->8 = a limit, a particular structure, a range / a grow
# object, a particular size / a particular allocation) aren't resolvable, so the name states the mechanism: a
# grow/append process ((if a size (a->8 - a->4, a size, a 8-byte element, a particular size) is non-zero, a particular
# state) grow/append (a complex computation, a particular computation / a particular operation, a constant, from a
# fixed .data address, a particular value, a particular size / a particular allocation, a particular operation, calling
# Allocate_OrDbgBreak_8221F388, an allocation, a particular allocation / a particular size, a particular size / a
# particular allocation, and ComputeWrapped_8222C3E8, a compute, a particular computation / a particular operation, a
# particular size / a particular allocation, and SetValue3_82A09FB0, a set, a particular set / a particular value, a
# particular size / a particular allocation), and (otherwise) take one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation)). The "8" distinguishes it from ProcessGrowAppend
# _82BE4AA8, ProcessGrowAppend2_8321F328, ProcessGrowAppend3_8321FC38, ProcessGrowAppend4_8323CB98, ProcessGrowAppend
# 5_83212DC0, ProcessGrowAppend6_83216060, and ProcessGrowAppend7_83221138.
[entrypoint.functions."0x83221378"]
name = "ProcessGrowAppend8_83221378"

# A process (a grow/append, with a byte-by-byte copy and a 4-byte element, the sibling of ProcessGrowAppend2_8321F328
# / ProcessGrowAppend6_83216060, the caller of ProcessGrowAppend6_83216060 / ProcessGrowAppend8_83221378 / Allocate
# _OrDbgBreak_8221F388 / ConstructNode20_82A12B08 / CopyStructData_83235608): (if a count (a->20, a count, a
# particular count / a particular value) is >= 4, a particular threshold, a particular count) grow/append (a
# byte-by-byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte element (four
# bytes, a particular 4-byte element, a 4-byte element, a particular value) from a base (a->12, a base, a 4-byte
# element, a particular value, a particular source) to a stack (a particular target), a particular copy / a
# particular allocation), and (otherwise) take one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation), and (in paths) call a grow/append (ProcessGrowAppend6_83216060, on a
# value, a particular grow/append, a particular size / a particular allocation, a particular operation), and a
# grow/append (ProcessGrowAppend8_83221378, on a value, a particular grow/append, a particular size / a particular
# allocation, a particular operation), and allocate (Allocate_OrDbgBreak_8221F388, on a value, a particular
# allocation / a particular size, a particular size / a particular allocation, a particular operation), and construct
# (ConstructNode20_82A12B08, on a value, a particular construct / a particular size, a particular size / a particular
# allocation, a particular operation).

# Args: obj (r3, r22, an object), a (r4, r30, ->4 = a base, ->12 = a base, ->20 = a count, a particular structure, a
# range / a grow object), b (r5, r24, a value), c (r6, r23, a value), (more). It checks a count (r11, a->20, a count, a
# particular count / a particular value): if it is >= 4 (a particular threshold, a particular count, a particular
# value, a particular state), it grows/appends (a byte-by-byte copy, a 4-byte element, a particular size / a
# particular allocation: it computes a count (r9, a->20 - 4, a count, a 4-byte element, a particular count / a
# particular value), and a base (r11, a->12, a base, a 4-byte element, a particular value, a particular source), and
# (r7, r11 + 4, a base, a 4-byte element, a particular value, a particular source), and copies a 4-byte element (four
# bytes, a particular 4-byte element, a 4-byte element, a particular value: lbz r5, 0, r11, a byte, a 4-byte element,
# a particular value, and stb r5, 0, r8, a store, a 4-byte element, a particular copy / a particular allocation, and
# lbz r4, 1, r11, a byte, a 4-byte element, a particular value, and stb r4, 1, r8, a store, a 4-byte element, a
# particular copy / a particular allocation, and lbz r3, 2, r11, a byte, a 4-byte element, a particular value, and stb
# r3, 2, r8, a store, a 4-byte element, a particular copy / a particular allocation, and lbz r11, 3, r11, a byte, a 4
# -byte element, a particular value, and stb r11, 3, r8, a store, a 4-byte element, a particular copy / a particular
# allocation), advancing to the next 4-byte element, a particular grow/append, a particular size / a particular
# allocation). If the count is < 4 (a particular threshold, a particular count, a particular value, a particular
# state), it takes one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation). In paths it calls a grow/append (ProcessGrowAppend6_83216060, sub_83216060, a grow/append,
# on a value (a, a value, a particular value, a particular size / a particular allocation), a particular grow/append,
# a particular size / a particular allocation, a particular operation), and a grow/append (ProcessGrowAppend8
# _83221378, sub_83221378, a grow/append, on a value (a, a value, a particular value, a particular size / a
# particular allocation), a particular grow/append, a particular size / a particular allocation, a particular
# operation), and allocates (Allocate_OrDbgBreak_8221F388, sub_8221F388, an allocation, on a value (a, a value, a
# particular value, a particular size, a particular size / a particular allocation), a particular allocation / a
# particular size, a particular size / a particular allocation, a particular operation), and constructs (ConstructNode
# 20_82A12B08, sub_82A12B08, a construct, on a value (a, a value, a particular value, a particular size, a particular
# size / a particular allocation), a particular construct / a particular size, a particular size / a particular
# allocation, a particular operation). So it's a "process (a grow/append, with a byte-by-byte copy and a 4-byte
# element: (if a count (a->20, a count, a particular count / a particular value) is >= 4, a particular threshold, a
# particular count) grow/append (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation,
# copying a 4-byte element (four bytes, a particular 4-byte element, a 4-byte element, a particular value) from a base
# (a->12, a base, a 4-byte element, a particular value, a particular source) to a stack (a particular target), a
# particular copy / a particular allocation), and (otherwise) take one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation), and (in paths) call a grow/append
# (ProcessGrowAppend6_83216060, on a value, a particular grow/append, a particular size / a particular allocation, a
# particular operation), and a grow/append (ProcessGrowAppend8_83221378, on a value, a particular grow/append, a
# particular size / a particular allocation, a particular operation), and allocate (Allocate_OrDbgBreak_8221F388, on a
# value, a particular allocation / a particular size, a particular size / a particular allocation, a particular
# operation), and construct (ConstructNode20_82A12B08, on a value, a particular construct / a particular size, a
# particular size / a particular allocation, a particular operation))" routine (a grow/append process, part of the
# 0x83221 cluster, a sibling of ProcessGrowAppend2_8321F328, a caller of ProcessGrowAppend6_83216060 and
# ProcessGrowAppend8_83221378). 373 lines, 11 callees (Allocate_OrDbgBreak_8221F388, Allocate_SizeBucketed_Thunk
# _8221F3F0, ConstructNode20_82A12B08, CopyStructData_83235608, Release_RefCounted_829FF648, SetValue4_829FD7B8, sub
# _82356698, sub_82A1B480, sub_82CBBB58, sub_83216060, sub_83221378). No direct callers (it's called via a vtable / it
# is registered-but-uncalled, part of the game's subsystems). The object's class (obj, r22, an object, a particular
# structure, a range / a grow object, a particular size / a particular allocation) and the value's meaning (a, r30, ->
# 4 = a base, ->12 = a base, ->20 = a count, a particular structure, a range / a grow object, a particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a grow/append process ((if a count
# (a->20, a count, a particular count / a particular value) is >= 4, a particular threshold, a particular count) grow
# /append (a byte-by-byte copy, a 4-byte element, a particular size / a particular allocation, copying a 4-byte
# element (four bytes, a particular 4-byte element, a 4-byte element, a particular value) from a base (a->12, a base,
# a 4-byte element, a particular value, a particular source) to a stack (a particular target), a particular copy / a
# particular allocation), and (otherwise) take one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation), and (in paths) call a grow/append (ProcessGrowAppend6_83216060, on a
# value, a particular grow/append, a particular size / a particular allocation, a particular operation), and a
# grow/append (ProcessGrowAppend8_83221378, on a value, a particular grow/append, a particular size / a particular
# allocation, a particular operation), and allocate (Allocate_OrDbgBreak_8221F388, on a value, a particular
# allocation / a particular size, a particular size / a particular allocation, a particular operation), and construct
# (ConstructNode20_82A12B08, on a value, a particular construct / a particular size, a particular size / a particular
# allocation, a particular operation)). The "9" distinguishes it from ProcessGrowAppend_82BE4AA8, ProcessGrowAppend2
# _8321F328, ProcessGrowAppend3_8321FC38, ProcessGrowAppend4_8323CB98, ProcessGrowAppend5_83212DC0, ProcessGrowAppend
# 6_83216060, ProcessGrowAppend7_83221138, and ProcessGrowAppend8_83221378.
[entrypoint.functions."0x83221548"]
name = "ProcessGrowAppend9_83221548"

# A 32-byte constructor (the sibling of Construct32Byte_82B722E8 / Construct32ByteVector_8321FB58, with a constant and
# a complex computation, the caller of Allocate_OrDbgBreak_8221F388 / SetRefcountedWrapper_8238A848): initialize the
# object's fields (a 32-byte object, a particular structure, a particular size / a particular allocation, obj->4 = a
# value, a field, a particular value, obj->8 = 0, a field, a count / a size, a zero, obj->0 = a constant, from a fixed
# .data address, a class id / a marker, a particular class / a particular marker, obj->16 = 0, a field, a count / a
# size, a zero, obj->20 = 0, a field, a count / a size, a zero, obj->24 = 0, a field, a count / a size, a zero, obj->32
# = 1, a field, a value, a one, obj->36 = 0, a field, a count / a size, a zero, obj->40 = 0, a field, a count / a size
# , a zero, obj->44 = a constant, from a fixed .data address, a float, a particular value, obj->48 = a constant, from a
# fixed .data address, a float, a particular value, a 32-byte object, a particular structure, a particular size / a
# particular allocation), and (in paths) allocate (Allocate_OrDbgBreak_8221F388, on a value, a particular allocation /
# a particular size, a particular size / a particular allocation, a particular operation), and set a refcounted
# wrapper (SetRefcountedWrapper_8238A848, on a value, a particular set / a particular wrapper, a particular size / a
# particular allocation, a particular operation).

# Args: obj (r3, r31, ->0 = a field, a constant, a class id / a marker, ->4 = a field, a value, ->8 = a field, a count
# / a size, ->16 = a field, a count / a size, ->20 = a field, a count / a size, ->24 = a field, a count / a size, ->32
# = a field, a value, ->36 = a field, a count / a size, ->40 = a field, a count / a size, ->44 = a field, a float, ->
# 48 = a field, a float, a 32-byte object), a (r4, a value), (more). It initializes the object's fields (a 32-byte
# object, a particular structure, a particular size / a particular allocation: obj->4 = a, a value, a particular
# value, obj->8 = 0, a field, a count / a size, a zero, obj->0 = a constant, from a fixed .data address, a class id /
# a marker, a particular class / a particular marker, obj->16 = 0, a field, a count / a size, a zero, obj->20 = 0, a
# field, a count / a size, a zero, obj->24 = 0, a field, a count / a size, a zero, obj->32 = 1, a field, a value, a
# one, obj->36 = 0, a field, a count / a size, a zero, obj->40 = 0, a field, a count / a size, a zero, obj->44 = a
# constant, from a fixed .data address, a float, a particular value, obj->48 = a constant, from a fixed .data address,
# a float, a particular value, a 32-byte object, a particular structure, a particular size / a particular
# allocation, a particular initialization). In paths it allocates (Allocate_OrDbgBreak_8221F388, sub_8221F388, an
# allocation, on a value (a, a value, a particular value, a particular size, a particular size / a particular
# allocation), a particular allocation / a particular size, a particular size / a particular allocation, a particular
# operation, a particular state / a particular result), and sets a refcounted wrapper (SetRefcountedWrapper_8238A848,
# sub_8238A848, a set, on a value (a, a value, a particular value, a particular size, a particular size / a
# particular allocation), a particular set / a particular wrapper, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result). So it's a "construct a 32-byte object (initialize
# the object's fields (a 32-byte object, a particular structure, a particular size / a particular allocation, obj->4 =
# a value, a field, a particular value, obj->8 = 0, a field, a count / a size, a zero, obj->0 = a constant, from a
# fixed .data address, a class id / a marker, a particular class / a particular marker, obj->16 = 0, a field, a count
# / a size, a zero, obj->20 = 0, a field, a count / a size, a zero, obj->24 = 0, a field, a count / a size, a zero,
# obj->32 = 1, a field, a value, a one, obj->36 = 0, a field, a count / a size, a zero, obj->40 = 0, a field, a count
# / a size, a zero, obj->44 = a constant, from a fixed .data address, a float, a particular value, obj->48 = a
# constant, from a fixed .data address, a float, a particular value, a 32-byte object, a particular structure, a
# particular size / a particular allocation), and (in paths) allocate (Allocate_OrDbgBreak_8221F388, on a value, a
# particular allocation / a particular size, a particular size / a particular allocation, a particular operation), and
# set a refcounted wrapper (SetRefcountedWrapper_8238A848, on a value, a particular set / a particular wrapper, a
# particular size / a particular allocation, a particular operation))" routine (a 32-byte constructor, with a
# constant and a complex computation, part of the 0x83221 cluster, a sibling of Construct32Byte_82B722E8 and
# Construct32ByteVector_8321FB58, a caller of Allocate_OrDbgBreak_8221F388 and SetRefcountedWrapper_8238A848). 117
# lines, 2 callees (Allocate_OrDbgBreak_8221F388, SetRefcountedWrapper_8238A848). 1 caller: sub_83221A60 (a sibling in
# the 0x83221 cluster, a process, which calls this to construct a 32-byte object, a particular size / a particular
# allocation, a particular operation). The object's class (obj, ->0 = a field, a constant, a class id / a marker, ->
# 4 = a field, a value, ->8 = a field, a count / a size, ->16 = a field, a count / a size, ->20 = a field, a count /
# a size, ->24 = a field, a count / a size, ->32 = a field, a value, ->36 = a field, a count / a size, ->40 = a
# field, a count / a size, ->44 = a field, a float, ->48 = a field, a float, a 32-byte object, a particular
# structure, a particular size / a particular allocation, a particular value / a particular state) and the value's
# meaning (a, a value, a particular value, a particular size / a particular allocation) aren't resolvable, so the
# name states the mechanism: a 32-byte constructor (with a constant and a complex computation) (initialize the
# object's fields (a 32-byte object, a particular structure, a particular size / a particular allocation, obj->4 = a
# value, a field, a particular value, obj->8 = 0, a field, a count / a size, a zero, obj->0 = a constant, from a
# fixed .data address, a class id / a marker, a particular class / a particular marker, obj->16 = 0, a field, a count
# / a size, a zero, obj->20 = 0, a field, a count / a size, a zero, obj->24 = 0, a field, a count / a size, a zero,
# obj->32 = 1, a field, a value, a one, obj->36 = 0, a field, a count / a size, a zero, obj->40 = 0, a field, a count
# / a size, a zero, obj->44 = a constant, from a fixed .data address, a float, a particular value, obj->48 = a
# constant, from a fixed .data address, a float, a particular value, a 32-byte object, a particular structure, a
# particular size / a particular allocation), and (in paths) allocate (Allocate_OrDbgBreak_8221F388, on a value, a
# particular allocation / a particular size, a particular size / a particular allocation, a particular operation), and
# set a refcounted wrapper (SetRefcountedWrapper_8238A848, on a value, a particular set / a particular wrapper, a
# particular size / a particular allocation, a particular operation)). The "2" distinguishes it from Construct32Byte
# _82B722E8 and Construct32ByteVector_8321FB58.
[entrypoint.functions."0x83221998"]
name = "Construct32Byte2_83221998"

# A process (a lookup, with a release and a complex computation, the caller of WalkChainFind_83239FA8 /
# Construct32Byte2_83221998 / Allocate_OrDbgBreak_8221F388 / SetRefcountedWrapper_8238A848 / Release_RefCounted_829FF
# 648): perform a lookup (a lookup, a particular lookup / a particular search, a particular size / a particular
# allocation, a particular operation, calling WalkChainFind_83239FA8, a lookup, a particular lookup / a particular
# search, a particular size / a particular allocation, a particular operation), and (if a flag (r3 & 0xFF, a flag, a
# particular value, a particular state, a particular size / a particular allocation) is non-zero, a particular state)
# release (Release_RefCounted_829FF648, on a value, a particular release / a particular count, a particular size / a
# particular allocation, a particular operation), and clear the object's fields (obj->0 = 0, obj->4 = 0, two zeros, a
# particular initialization, a particular size / a particular allocation, a particular state / a particular result),
# and (otherwise) construct (Construct32Byte2_83221998, on a value, a particular construct / a particular size, a
# particular size / a particular allocation, a particular operation), and allocate (Allocate_OrDbgBreak_8221F388, on a
# value, a particular allocation / a particular size, a particular size / a particular allocation, a particular
# operation), and set a refcounted wrapper (SetRefcountedWrapper_8238A848, on a value, a particular set / a
# particular wrapper, a particular size / a particular allocation, a particular operation).

# Args: obj (r3, r31, ->0 = a field, a value, ->4 = a field, a value, a particular structure, a lookup object, a
# particular size / a particular allocation), a (r4, r29, a value), (more). It performs a lookup (a lookup, a
# particular lookup / a particular search, a particular size / a particular allocation, a particular operation, calling
# WalkChainFind_83239FA8, sub_83239FA8, a lookup, on a value (stack[80], a value, a particular value, a particular
# size / a particular allocation, a particular target), and a constant (a constant, from a fixed .data address, a
# particular value, a particular key, a particular size / a particular allocation), a particular lookup / a
# particular search, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result). It checks a flag (r10, r3 & 0xFF, a flag, a particular value, a particular state, a particular
# size / a particular allocation): if the flag is non-zero (a particular state, a particular value, a particular size
# / a particular allocation), it releases (Release_RefCounted_829FF648, sub_829FF648, a release, on a value (stack[80]
# , a value, a particular value, a particular size / a particular allocation, a particular target), a particular
# release / a particular count, a particular size / a particular allocation, a particular operation, a particular
# state / a particular result), and clears the object's fields (obj->0 = 0, a field, a value, a zero, obj->4 = 0, a
# field, a value, a zero, two zeros, a particular initialization, a particular size / a particular allocation, a
# particular state / a particular result); if the flag is zero (a null state, a particular state, a particular size /
# a particular allocation), it constructs (Construct32Byte2_83221998, sub_83221998, a 32-byte constructor, on a value
# (a, a value, a particular value, a particular size, a particular size / a particular allocation), a particular
# construct / a particular size, a particular size / a particular allocation, a particular operation, a particular
# state / a particular result), and allocates (Allocate_OrDbgBreak_8221F388, sub_8221F388, an allocation, on a value
# (a, a value, a particular value, a particular size, a particular size / a particular allocation), a particular
# allocation / a particular size, a particular size / a particular allocation, a particular operation, a particular
# state / a particular result), and sets a refcounted wrapper (SetRefcountedWrapper_8238A848, sub_8238A848, a set, on
# a value (a, a value, a particular value, a particular size, a particular size / a particular allocation), a
# particular set / a particular wrapper, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result). So it's a "process (a lookup, with a release and a complex computation: perform
# a lookup (a lookup, a particular lookup / a particular search, a particular size / a particular allocation, a
# particular operation, calling WalkChainFind_83239FA8, a lookup, a particular lookup / a particular search, a
# particular size / a particular allocation, a particular operation), and (if a flag (r3 & 0xFF, a flag, a
# particular value, a particular state, a particular size / a particular allocation) is non-zero, a particular state)
# release (Release_RefCounted_829FF648, on a value, a particular release / a particular count, a particular size / a
# particular allocation, a particular operation), and clear the object's fields (obj->0 = 0, obj->4 = 0, two zeros, a
# particular initialization, a particular size / a particular allocation, a particular state / a particular result),
# and (otherwise) construct (Construct32Byte2_83221998, on a value, a particular construct / a particular size, a
# particular size / a particular allocation, a particular operation), and allocate (Allocate_OrDbgBreak_8221F388, on a
# value, a particular allocation / a particular size, a particular size / a particular allocation, a particular
# operation), and set a refcounted wrapper (SetRefcountedWrapper_8238A848, on a value, a particular set / a
# particular wrapper, a particular size / a particular allocation, a particular operation))" routine (a lookup process
# , part of the 0x83221 cluster, a sibling of Construct32Byte2_83221998, a caller of WalkChainFind_83239FA8,
# Construct32Byte2_83221998, Allocate_OrDbgBreak_8221F388, and SetRefcountedWrapper_8238A848). 137 lines, 6 callees
# (Allocate_OrDbgBreak_8221F388, Construct32Byte2_83221998, Release_RefCounted_829FF648, SetRefcountedWrapper_8238A848
# , WalkChainFind_83239FA8, sub_832259A0). 3 callers: sub_83225AE0, sub_83225A20, sub_83225BC0 (siblings in the
# 0x83225 cluster, a process, which calls this to perform the lookup, a particular size / a particular allocation, a
# particular operation). The object's class (obj, ->0 = a field, a value, ->4 = a field, a value, a particular
# structure, a lookup object, a particular size / a particular allocation, a particular value / a particular state) and
# the value's meaning (a, r29, a value, a particular size / a particular allocation) aren't resolvable, so the name
# states the mechanism: a lookup process (with a release and a complex computation) (perform a lookup (a lookup, a
# particular lookup / a particular search, a particular size / a particular allocation, a particular operation, calling
# WalkChainFind_83239FA8, a lookup, a particular lookup / a particular search, a particular size / a particular
# allocation, a particular operation), and (if a flag (r3 & 0xFF, a flag, a particular value, a particular state, a
# particular size / a particular allocation) is non-zero, a particular state) release (Release_RefCounted_829FF648, on
# a value, a particular release / a particular count, a particular size / a particular allocation, a particular
# operation), and clear the object's fields (obj->0 = 0, obj->4 = 0, two zeros, a particular initialization, a
# particular size / a particular allocation, a particular state / a particular result), and (otherwise) construct
# (Construct32Byte2_83221998, on a value, a particular construct / a particular size, a particular size / a
# particular allocation, a particular operation), and allocate (Allocate_OrDbgBreak_8221F388, on a value, a
# particular allocation / a particular size, a particular size / a particular allocation, a particular operation), and
# set a refcounted wrapper (SetRefcountedWrapper_8238A848, on a value, a particular set / a particular wrapper, a
# particular size / a particular allocation, a particular operation)). The "LookupConstruct" refers to the lookup (a
# lookup, a particular lookup / a particular search, a particular size / a particular allocation, a particular
# operation) and the construct (a construct, a particular construct / a particular size, a particular size / a
# particular allocation, a particular operation).
[entrypoint.functions."0x83221A60"]
name = "ProcessLookupConstruct_83221A60"

# A process (a reference count, with a release and an increment, the sibling of ProcessRefCount_83225538, the caller
# of Release_RefCounted_829FF648 / LookupAndAssign_8323A1E8): (if a value (obj->4, a value, a reference count, a
# particular value) is non-zero, a particular value) release a refcounted value (Release_RefCounted_829FF648, on a
# value, a particular release, a particular count), and increment a reference count (a value (obj->4, a value, a
# reference count, a particular value), a reference count, a particular count), and (otherwise) take one path, and
# call a lookup (LookupAndAssign_8323A1E8, on a value and a constant, a particular lookup).

# Args: obj (r3, ->0 = a value, ->4 = a value, a reference count, a particular structure, a reference counted object),
# (more). It checks a value (obj->4, a value, a reference count, a particular value): if it is non-zero (a
# particular value, a particular state), it releases a refcounted value (Release_RefCounted_829FF648, sub_829FF648,
# a refcounted release, on a value (stack[80], a value, a particular value), a particular release, a particular count
# ), and increments a reference count (a value (r31, obj->4, a value, a reference count, a particular value), a
# reference count, a particular count: it reads the count (r11, r31->0, a reference count, a particular count), and
# stores it (r11 + 1, an increment, a particular count, a particular reference count). If it is zero (a null state, a
# particular state), it takes one path (a no-op / a return, a particular state). It calls a lookup (LookupAndAssign
# _8323A1E8, sub_8323A1E8, a lookup and assign, on a value (stack[80], a value, a particular value) and a constant (a
# constant, from a fixed .data address, a particular value, a particular key), a particular lookup, a particular
# assignment). So it's a "process (a reference count: (if a value, obj->4, is non-zero, a particular value) release a
# refcounted value (Release_RefCounted_829FF648, on a value, a particular release), and increment a reference count
# (a value (obj->4, a value, a reference count, a particular value), a reference count, a particular count), and
# (otherwise) take one path, and call a lookup (LookupAndAssign_8323A1E8, on a value and a constant, a particular
# lookup))" routine (a reference count process, part of the 0x83225 cluster, a sibling of ProcessRefCount_83225538, a
# caller of Release_RefCounted_829FF648 and LookupAndAssign_8323A1E8). 73 lines, 2 callees (LookupAndAssign_8323A1E8,
# Release_RefCounted_829FF648). 1 caller: sub_83221A60 (ProcessLookupConstruct, a lookup process, which calls this to
# process the reference count, a particular count / a particular state, a particular size / a particular allocation).
# The object's class (obj, ->0 = a value, ->4 = a value, a reference count, a particular structure, a reference
# counted object) and the value's meaning (a value, a reference count, a particular value, a particular count) aren't
# resolvable, so the name states the mechanism: a reference count process ((if a value, obj->4, is non-zero, a
# particular value) release a refcounted value (Release_RefCounted_829FF648, on a value, a particular release), and
# increment a reference count (a value (obj->4, a value, a reference count, a particular value), a reference count, a
# particular count), and (otherwise) take one path, and call a lookup (LookupAndAssign_8323A1E8, on a value and a
# constant, a particular lookup)). The "2" distinguishes it from ProcessRefCount_83225538.
[entrypoint.functions."0x832259A0"]
name = "ProcessRefCount2_832259A0"

# A process (an allocation, with a check and a call, the caller of Allocate_OrDbgBreak_8221F388 / sub_822F1F00): (if a
# size (a size, a particular size, a particular value, a particular size / a particular allocation) is non-zero, a
# particular value) perform a check (a check, a particular check / a particular state, a particular size / a
# particular allocation, a particular operation, calling sub_822F1F00, on a value (stack[80], a value, a particular
# value, a particular size / a particular allocation, a particular target), and a constant (a constant, from a fixed
# .data address, a particular value, a particular size / a particular allocation, a particular state), a particular
# check / a particular state, a particular size / a particular allocation, a particular operation, a particular state
# / a particular result), and (otherwise) take one path (a no-op / a return, a particular state, a particular result,
# a particular size / a particular allocation), and allocate (Allocate_OrDbgBreak_8221F388, on a value (a, a value, a
# particular value, a particular size, a particular size / a particular allocation), a particular allocation / a
# particular size, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result).

# Args: a (r3, a value), size (r4, r31, a size, a particular size, a particular value, a particular size / a
# particular allocation), (more). It checks a size (r31, a, a size, a particular size, a particular value, a
# particular size / a particular allocation): if it is non-zero (a particular value, a particular state, a particular
# size / a particular allocation), it performs a check (a check, a particular check / a particular state, a
# particular size / a particular allocation, a particular operation, calling sub_822F1F00, sub_822F1F00, on a value
# (stack[80], a value, a particular value, a particular size / a particular allocation, a particular target), and a
# constant (a constant, from a fixed .data address, a particular value, a particular size / a particular allocation, a
# particular state), a particular check / a particular state, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result); if it is zero (a null state, a particular state, a
# particular size / a particular allocation), it takes one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation). It allocates (Allocate_OrDbgBreak_8221F388,
# sub_8221F388, an allocation, on a value (a, a value, a particular value, a particular size, a particular size / a
# particular allocation), a particular allocation / a particular size, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result). So it's a "process (an allocation, with a check and
# a call: (if a size (a size, a particular size, a particular value, a particular size / a particular allocation) is
# non-zero, a particular value) perform a check (a check, a particular check / a particular state, a particular size /
# a particular allocation, a particular operation, calling sub_822F1F00, on a value (stack[80], a value, a
# particular value, a particular size / a particular allocation, a particular target), and a constant (a constant,
# from a fixed .data address, a particular value, a particular size / a particular allocation, a particular state), a
# particular check / a particular state, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (otherwise) take one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation), and allocate (Allocate_OrDbgBreak_8221F388, on a
# value (a, a value, a particular value, a particular size, a particular size / a particular allocation), a
# particular allocation / a particular size, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result))" routine (an allocation check process, part of the 0x82301 cluster, a
# caller of Allocate_OrDbgBreak_8221F388 and sub_822F1F00). 66 lines, 2 callees (Allocate_OrDbgBreak_8221F388, sub
# _822F1F00). 4 callers: sub_82301240, sub_823012B0, sub_82B28F48, sub_82301878 (siblings in the 0x82301/0x82B28
# clusters, a process, which calls this to perform the allocation check, a particular size / a particular allocation,
# a particular operation). The value's meaning (a, a value, a particular value, a particular size / a particular
# allocation) isn't resolvable, so the name states the mechanism: an allocation check process (with a check and a
# call) ((if a size (a size, a particular size, a particular value, a particular size / a particular allocation) is
# non-zero, a particular value) perform a check (a check, a particular check / a particular state, a particular size /
# a particular allocation, a particular operation, calling sub_822F1F00, on a value (stack[80], a value, a
# particular value, a particular size / a particular allocation, a particular target), and a constant (a constant,
# from a fixed .data address, a particular value, a particular size / a particular allocation, a particular state), a
# particular check / a particular state, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (otherwise) take one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation), and allocate (Allocate_OrDbgBreak_8221F388, on a
# value (a, a value, a particular value, a particular size, a particular size / a particular allocation), a
# particular allocation / a particular size, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result)). The "AllocateCheck" refers to the allocation (an allocation, a
# particular allocation / a particular size, a particular size / a particular allocation, a particular operation) and
# the check (a check, a particular check / a particular state, a particular size / a particular allocation, a
# particular operation).
[entrypoint.functions."0x823014D0"]
name = "ProcessAllocateCheck_823014D0"

# A process (a grow/copy, with a range check and a call, the caller of sub_823012B0 / CheckedCopy_82CA3808, the
# sibling of ProcessAllocateCheck_823014D0): (if a value (a, a value, a particular value, a particular size / a
# particular allocation) is <= a size (obj->8 - obj->4, a size, a particular size, a particular size / a particular
# allocation), a particular size) perform a range check (a range check, a particular range / a particular check, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result), and
# (otherwise) grow/copy (a grow/copy, a particular grow / a particular copy, a particular size / a particular
# allocation, a particular operation, a particular state / a particular result, calling sub_823012B0, on a value
# (stack[80], a value, a particular value, a particular size / a particular allocation, a particular target), and a
# value (stack[84], a value, a particular value, a particular size / a particular allocation, a particular source),
# and a value (a, a value, a particular value, a particular size / a particular allocation, a particular size / a
# particular allocation), a particular grow / a particular copy, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and (in paths) copy (CheckedCopy_82CA3808, on a
# value, a particular copy, a particular size / a particular allocation, a particular operation, a particular state /
# a particular result).

# Args: obj (r3, r31, ->4 = a base, ->8 = a limit, a particular structure, a grow object), a (r4, a value, a
# particular value, a particular size / a particular allocation), b (r5, r6, a value, a particular value, a
# particular size / a particular allocation), (more). It checks a size (r9, obj->8 - obj->4, a size, a particular
# size, a particular size / a particular allocation): if a value (r4, a, a value, a particular value, a particular
# size / a particular allocation) is <= the size (a particular size, a particular state, a particular size / a
# particular allocation), it performs a range check (a range check, a particular range / a particular check, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result); if
# the value is > the size (a particular size, a particular state, a particular size / a particular allocation), it
# grows/copies (a grow/copy, a particular grow / a particular copy, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result, calling sub_823012B0, sub_823012B0, a grow/copy, on
# a value (stack[80], a value, a particular value, a particular size / a particular allocation, a particular
# target), and a value (stack[84], a value, a particular value, a particular size / a particular allocation, a
# particular source), and a value (a, a value, a particular value, a particular size / a particular allocation, a
# particular size / a particular allocation), a particular grow / a particular copy, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result). In paths it copies
# (CheckedCopy_82CA3808, sub_82CA3808, a checked copy, on a value (a, a value, a particular value, a particular size,
# a particular size / a particular allocation), a particular copy, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result). So it's a "process (a grow/copy, with a range check
# and a call: (if a value (a, a value, a particular value, a particular size / a particular allocation) is <= a size
# (obj->8 - obj->4, a size, a particular size, a particular size / a particular allocation), a particular size)
# perform a range check (a range check, a particular range / a particular check, a particular size / a particular
# allocation, a particular operation, a particular state / a particular result), and (otherwise) grow/copy (a
# grow/copy, a particular grow / a particular copy, a particular size / a particular allocation, a particular
# operation, a particular state / a particular result, calling sub_823012B0, on a value (stack[80], a value, a
# particular value, a particular size / a particular allocation, a particular target), and a value (stack[84], a
# value, a particular value, a particular size / a particular allocation, a particular source), and a value (a, a
# value, a particular value, a particular size / a particular allocation, a particular size / a particular
# allocation), a particular grow / a particular copy, a particular size / a particular allocation, a particular
# operation, a particular state / a particular result), and (in paths) copy (CheckedCopy_82CA3808, on a value, a
# particular copy, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result))" routine (a grow/copy process, part of the 0x82300 cluster, a sibling of
# ProcessAllocateCheck_823014D0, a caller of sub_823012B0 and CheckedCopy_82CA3808). 87 lines, 2 callees (sub
# _823012B0, CheckedCopy_82CA3808). 10+ callers: sub_82A855A8, sub_823C2148, sub_822FDF78, sub_82886658, sub_82B31CA0
# , sub_82B4A230, sub_82B88670, sub_82A4AE70, sub_824E7A50, sub_82B4A4F8 (processes, which call this to grow/copy, a
# particular size / a particular allocation, a particular operation). The object's class (obj, ->4 = a base, ->8 = a
# limit, a particular structure, a grow object, a particular size / a particular allocation, a particular value /
# a particular state) and the value's meaning (a, a value, a particular value, a particular size / a particular
# allocation, a particular size / a particular allocation) aren't resolvable, so the name states the mechanism: a
# grow/copy process (with a range check and a call) ((if a value (a, a value, a particular value, a particular size /
# a particular allocation) is <= a size (obj->8 - obj->4, a size, a particular size, a particular size / a
# particular allocation), a particular size) perform a range check (a range check, a particular range / a
# particular check, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and (otherwise) grow/copy (a grow/copy, a particular grow / a particular copy, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result, calling
# sub_823012B0, on a value (stack[80], a value, a particular value, a particular size / a particular allocation, a
# particular target), and a value (stack[84], a value, a particular value, a particular size / a particular
# allocation, a particular source), and a value (a, a value, a particular value, a particular size / a
# particular allocation, a particular size / a particular allocation), a particular grow / a particular copy, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result), and
# (in paths) copy (CheckedCopy_82CA3808, on a value, a particular copy, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result)). The "GrowCopy" refers to the grow/copy (a
# grow/copy, a particular grow / a particular copy, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result).
[entrypoint.functions."0x82300648"]
name = "ProcessGrowCopy_82300648"

# A process (a grow/append, with a range check and a copy, the sibling of ProcessGrowAppend_82BE4AA8 /
# ProcessGrowAppend2_8321F328 / ProcessGrowCopy_82300648, the caller of sub_82A97648 / CheckedCopy_82CA3808 / Free
# _SizeBucketed_Thunk_8221BE68 / ProcessAllocateCheck_823014D0): (if a base (obj->4, a base, a particular value, a
# particular size / a particular allocation) is non-zero, a particular value) compute a size (obj->12 - obj->4, a
# size, a particular size, a particular size / a particular allocation, a particular operation), and (if a count
# (obj->8 - obj->4 - 1, a count, a particular count, a particular size / a particular allocation) is >= a value
# (r29, a count, a particular count, a particular size / a particular allocation), a particular count) perform a
# range check (a range check, a particular range / a particular check, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and (otherwise) grow/append (a grow/append, a
# particular grow / a particular append, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result, calling sub_82A97648, on a value, a particular grow / a particular
# append, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and (in paths) copy (CheckedCopy_82CA3808, on a value, a particular copy, a particular size /
# a particular allocation, a particular operation, a particular state / a particular result), and free
# (Free_SizeBucketed_Thunk_8221BE68, on a value, a particular free / a particular size, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and allocate
# (ProcessAllocateCheck_823014D0, on a value, a particular allocation / a particular check, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result).

# Args: obj (r3, r28, ->4 = a base, ->8 = a limit, ->12 = a base, a particular structure, a grow object), a (r4, a
# value), count (r5, r29, a count, a particular count, a particular size / a particular allocation), b (r6, a value,
# a particular value, a particular size / a particular allocation), (more). It checks a base (r11, obj->4, a base, a
# particular value, a particular size / a particular allocation): if it is non-zero (a particular value, a
# particular state, a particular size / a particular allocation), it computes a size (r10, obj->12 - obj->4, a size, a
# particular size, a particular size / a particular allocation, a particular operation). It checks a count (r29, a
# count, a particular count, a particular size / a particular allocation): if it is zero (a null state, a
# particular state, a particular size / a particular allocation), it takes one path (a no-op / a return, a
# particular state, a particular result, a particular size / a particular allocation). If it is non-zero (a
# particular value, a particular state, a particular size / a particular allocation), it checks a count (r9, obj->8
# - obj->4 - 1, a count, a particular count, a particular size / a particular allocation): if it is >= the count (a
# particular count, a particular state, a particular size / a particular allocation), it performs a range check (a
# range check, a particular range / a particular check, a particular size / a particular allocation, a particular
# operation, a particular state / a particular result); if it is < the count (a particular count, a particular
# state, a particular size / a particular allocation), it grows/appends (a grow/append, a particular grow / a
# particular append, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling sub_82A97648, sub_82A97648, a grow/append, on a value (a, a value, a particular value,
# a particular size, a particular size / a particular allocation), a particular grow / a particular append, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result). In
# paths it copies (CheckedCopy_82CA3808, sub_82CA3808, a checked copy, on a value (a, a value, a particular value, a
# particular size, a particular size / a particular allocation), a particular copy, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), frees
# (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a free, on a value (a, a value, a particular value, a particular
# size, a particular size / a particular allocation), a particular free / a particular size, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and allocates
# (ProcessAllocateCheck_823014D0, sub_823014D0, an allocation, on a value (a, a value, a particular value, a
# particular size, a particular size / a particular allocation), a particular allocation / a particular check, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result). So
# it's a "process (a grow/append, with a range check and a copy: (if a base (obj->4, a base, a particular value, a
# particular size / a particular allocation) is non-zero, a particular value) compute a size (obj->12 - obj->4, a
# size, a particular size, a particular size / a particular allocation, a particular operation), and (if a count
# (obj->8 - obj->4 - 1, a count, a particular count, a particular size / a particular allocation) is >= a value
# (r29, a count, a particular count, a particular size / a particular allocation), a particular count) perform a
# range check (a range check, a particular range / a particular check, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and (otherwise) grow/append (a grow/append, a
# particular grow / a particular append, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result, calling sub_82A97648, on a value, a particular grow / a
# particular append, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and (in paths) copy (CheckedCopy_82CA3808, on a value, a particular copy, a particular size /
# a particular allocation, a particular operation, a particular state / a particular result), and free
# (Free_SizeBucketed_Thunk_8221BE68, on a value, a particular free / a particular size, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and allocate
# (ProcessAllocateCheck_823014D0, on a value, a particular allocation / a particular check, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result))" routine (a grow/append
# process, part of the 0x82301 cluster, a sibling of ProcessGrowCopy_82300648, a caller of CheckedCopy_82CA3808,
# Free_SizeBucketed_Thunk_8221BE68, and ProcessAllocateCheck_823014D0). 324 lines, 4 callees (CheckedCopy_82CA3808,
# Free_SizeBucketed_Thunk_8221BE68, sub_823014D0, sub_82A97648). 2 callers: sub_82B29020, sub_82300648 (ProcessGrow
# Copy, a grow/copy process, which calls this to grow/append, a particular size / a particular allocation, a
# particular operation). The object's class (obj, ->4 = a base, ->8 = a limit, ->12 = a base, a particular
# structure, a grow object, a particular size / a particular allocation, a particular value / a
# particular state) and the value's meaning (a, a value, a particular value, a particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a grow/append process (with a range
# check and a copy) ((if a base (obj->4, a base, a particular value, a particular size / a particular allocation) is
# non-zero, a particular value) compute a size (obj->12 - obj->4, a size, a particular size, a
# particular size / a particular allocation, a particular operation), and (if a count (obj->8 - obj->4 - 1, a
# count, a particular count, a particular size / a particular allocation) is >= a value (r29, a count, a
# particular count, a particular size / a particular allocation), a particular count) perform a range check (a
# range check, a particular range / a particular check, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and (otherwise) grow/append (a grow/append, a
# particular grow / a particular append, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result, calling sub_82A97648, on a value, a particular grow / a
# particular append, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and (in paths) copy (CheckedCopy_82CA3808, on a value, a particular copy, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result), and
# free (Free_SizeBucketed_Thunk_8221BE68, on a value, a particular free / a particular size, a
# particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and allocate (ProcessAllocateCheck_823014D0, on a value, a
# particular allocation / a particular check, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result)). The "10" distinguishes it from ProcessGrowAppend
# _82BE4AA8, ProcessGrowAppend2_8321F328, ProcessGrowAppend3_8321FC38, ProcessGrowAppend4_8323CB98, ProcessGrowAppend
# 5_83212DC0, ProcessGrowAppend6_83216060, ProcessGrowAppend7_83221138, ProcessGrowAppend8_83221378, and
# ProcessGrowAppend9_83221548.
[entrypoint.functions."0x823012B0"]
name = "ProcessGrowAppend10_823012B0"

# A process (an increment, with a copy, the caller of CheckedCopy_82CA3808, the sibling of ProcessGrowAppend10_82301
# 2B0): (if a base (obj->4, a base, a particular value, a particular size / a particular allocation) is != a limit
# (obj->8, a limit, a particular value, a particular size / a particular allocation), a particular value) perform an
# increment (an increment, a particular increment / a particular count, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and copy (CheckedCopy_82CA3808, on a value, a
# particular copy, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and store the new limit (obj->8 = r30, a store, a limit, a particular value, a particular size /
# a particular allocation, a particular state / a particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a particular result, a particular size / a particular allocation).

# Args: obj (r3, r31, ->4 = a base, ->8 = a limit, a particular structure, a grow object), (more). It checks a base
# (r3, obj->4, a base, a particular value, a particular size / a particular allocation): if it is != a limit (r5,
# obj->8, a limit, a particular value, a particular size / a particular allocation, a particular state), it performs an
# increment (an increment, a particular increment / a particular count, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and copies (CheckedCopy_82CA3808, sub_82CA3808, a
# checked copy, on a value (r6, a zero, a particular value, a particular size / a particular allocation, a
# particular source), a particular copy, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and stores the new limit (obj->8 = r30, a store, a limit, a particular
# value, a particular size / a particular allocation, a particular state / a particular result). If it is == the
# limit (a particular state, a particular value, a particular size / a particular allocation), it takes one path (a
# no-op / a return, a particular state, a particular result, a particular size / a particular allocation). So it's a
# "process (an increment, with a copy: (if a base (obj->4, a base, a particular value, a particular size / a
# particular allocation) is != a limit (obj->8, a limit, a particular value, a particular size / a particular
# allocation), a particular value) perform an increment (an increment, a particular increment / a
# particular count, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and copy (CheckedCopy_82CA3808, on a value, a particular copy, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and store the new limit
# (obj->8 = r30, a store, a limit, a particular value, a particular size / a particular allocation, a
# particular state / a particular result), and (otherwise) take one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation))" routine (an increment process, with a copy, part
# of the 0x8272E cluster, a sibling of ProcessGrowAppend10_823012B0, a caller of CheckedCopy_82CA3808). 55 lines, 1
# callee (CheckedCopy_82CA3808). 4 callers: sub_83232A70, sub_8272E610, sub_82840D20, sub_82B4A380 (processes, which
# call this to perform the increment, a particular count / a particular size, a particular size / a
# particular allocation, a particular operation). The object's class (obj, ->4 = a base, ->8 = a limit, a
# particular structure, a grow object, a particular size / a particular allocation, a particular value /
# a particular state) isn't resolvable, so the name states the mechanism: an increment process (with a copy) ((if a
# base (obj->4, a base, a particular value, a particular size / a particular allocation) is != a limit (obj->8, a
# limit, a particular value, a particular size / a particular allocation), a particular value) perform an increment
# (an increment, a particular increment / a particular count, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and copy (CheckedCopy_82CA3808, on a value, a
# particular copy, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and store the new limit (obj->8 = r30, a store, a limit, a particular value, a
# particular size / a particular allocation, a particular state / a particular result), and (otherwise) take one path
# (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation)). The "IncrementCopy" refers to the increment (an increment, a
# particular increment / a particular count, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result) and the copy (a copy, a
# particular copy, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result).
[entrypoint.functions."0x8272E7D0"]
name = "ProcessIncrementCopy_8272E7D0"

# A process (a recursive process, with a loop and a complex computation, the sibling of ProcessRecursive_8229A2A8 /
# ProcessIncrementCopy_8272E7D0, the caller of sub_82A0A4A0 / sub_82A0A2D0 (recursive) / sub_82A0A890 / sub_82A0ACE8 /
# sub_82A0AFB0): (if a size (stack[188] - stack[180], a size, an 8-byte element, a particular size, a
# particular size / a particular allocation) is > 32, a particular threshold, a particular size) perform a loop (a
# loop, a particular loop / a particular iteration, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and (in the loop) call a process (sub_82A0A4A0, on
# a value (stack[80], a value, a particular value, a particular size / a particular allocation, a
# particular target), and a value (a, a value, a particular value, a particular size / a
# particular allocation, a particular source), and a value (b, a value, a particular value, a
# particular size / a particular allocation, a particular source), and a value (c, a value, a
# particular value, a particular size / a particular allocation, a particular source), a particular process, a
# particular operation, a particular size / a particular allocation, a particular state / a
# particular result), and (in paths) recurse (sub_82A0A2D0, on a value, a particular process, a
# particular operation, a particular size / a particular allocation, a particular state / a
# particular result), and call a process (sub_82A0A890, on a value, a particular process, a
# particular operation, a particular size / a particular allocation, a particular state / a
# particular result), and a process (sub_82A0ACE8, on a value, a particular process, a
# particular operation, a particular size / a particular allocation, a particular state / a
# particular result), and a process (sub_82A0AFB0, on a value, a particular process, a
# particular operation, a particular size / a particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation).

# Args: a (r3, r27, a value, a particular value, a particular size / a particular allocation), b (r4, r26, a value,
# a particular value, a particular size / a particular allocation), count (r5, r31, a count, a
# particular count, a particular size / a particular allocation), c (r6, r29, a value, a
# particular value, a particular size / a particular allocation), (more). It checks a size (r11, stack[188] -
# stack[180], a size, an 8-byte element, a particular size, a particular size / a
# particular allocation): if it is > 32 (a particular threshold, a particular size, a
# particular value, a particular state, a particular size / a particular allocation), it performs a loop (a
# loop, a particular loop / a particular iteration, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). In the loop, it checks a count (r31, a count, a
# particular count, a particular size / a particular allocation): if it is <= 0 (a null state, a
# particular state, a particular size / a particular allocation), it takes one path (a no-op / a return, a
# particular state, a particular result, a particular size / a
# particular allocation); if it is > 0 (a particular value, a
# particular state, a particular size / a
# particular allocation), it calls a process (sub_82A0A4A0, sub_82A0A4A0, a process, on a
# value (stack[80], a value, a particular value, a particular size / a
# particular allocation, a particular target), and a value (a, a value, a
# particular value, a particular size / a
# particular allocation, a particular source), and a value (b, a value, a
# particular value, a particular size / a
# particular allocation, a particular source), and a value (c, a value, a
# particular value, a particular size / a
# particular allocation, a particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result). In paths it recurses (sub_82A0A2D0, sub_82A0A2D0, a process, on a
# value (a, a value, a particular value, a particular size,
# a particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and calls a process (sub_82A0A890, sub_82A0A890, a process, on a
# value (a, a value, a particular value, a particular size,
# a particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82A0ACE8, sub_82A0ACE8, a process, on a
# value (a, a value, a particular value, a particular size,
# a particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82A0AFB0, sub_82A0AFB0, a process, on a
# value (a, a value, a particular value, a particular size,
# a particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result). So it's a "process (a recursive process, with a loop and a complex computation: (if a
# size (stack[188] - stack[180], a size, an 8-byte element, a particular size, a
# particular size / a particular allocation) is > 32, a particular threshold, a
# particular size) perform a loop (a
# loop, a particular loop / a particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in the loop) call a process (sub_82A0A4A0, on
# a value (stack[80], a value, a particular value, a particular size / a
# particular allocation, a
# particular target), and a value (a, a value, a particular value, a
# particular size / a
# particular allocation, a particular source), and a value (b, a value, a
# particular value, a particular size / a
# particular allocation, a particular source), and a value (c, a value, a
# particular value, a particular size / a
# particular allocation, a particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (in paths) recurse (sub_82A0A2D0, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and call a process (sub_82A0A890, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82A0ACE8, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82A0AFB0, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a recursive process, part of the 0x82A0A cluster, a sibling of
# ProcessRecursive_8229A2A8, a caller of sub_82A0A4A0 and sub_82A0A2D0 (recursive)). 286 lines, 5 callees (sub
# _82A0A2D0, sub_82A0A4A0, sub_82A0A890, sub_82A0ACE8, sub_82A0AFB0). 1 caller: sub_83221320 (a process, which
# calls this to perform the recursive process, a particular size / a
# particular allocation, a
# particular operation). The values' meanings (a, r27, a value, a particular value, a
# particular size / a particular allocation, b, r26, a value, a particular value, a
# particular size / a
# particular allocation, c, r29, a value, a particular value, a particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a recursive process (with a
# loop and a complex computation) ((if a
# size (stack[188] - stack[180], a size, an 8-byte element, a particular size, a
# particular size / a particular allocation) is > 32, a particular threshold, a
# particular size) perform a loop (a
# loop, a particular loop / a particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in the loop) call a process (sub_82A0A4A0, on
# a value (stack[80], a value, a particular value, a particular size / a
# particular allocation, a
# particular target), and a value (a, a value, a particular value, a
# particular size / a
# particular allocation, a particular source), and a value (b, a value, a
# particular value, a particular size / a
# particular allocation, a particular source), and a value (c, a value, a
# particular value, a particular size / a
# particular allocation, a particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (in paths) recurse (sub_82A0A2D0, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and call a process (sub_82A0A890, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82A0ACE8, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82A0AFB0, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "2" distinguishes it from ProcessRecursive_8229A2A8.
[entrypoint.functions."0x82A0A2D0"]
name = "ProcessRecursive2_82A0A2D0"

# A process (a sort, with a loop and a comparison, the sibling of ProcessRecursive2_82A0A2D0, the caller of sub_82A0
# B0A0): perform a sort (a sort, a particular sort / a particular ordering, a particular size / a particular
# allocation, a particular operation, a particular state / a particular result, calling sub_82A0B0A0, on a value,
# a particular sort / a particular ordering, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (in the loop) compare (a comparison, a particular comparison / a
# particular ordering, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, a value (a->0, a value, a particular value, a particular size / a particular allocation, a
# particular element) and a value (b->0, a value, a particular value, a particular size / a particular allocation, a
# particular element)), and (in paths) recurse (sub_82A0B0A0, on a value, a particular process, a particular
# operation, a particular size / a particular allocation, a particular state / a particular result), and (otherwise)
# take one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation).

# Args: a (r3, r29, a value, a particular value, a particular size / a particular allocation, a particular
# structure, a range / a sort object), b (r4, a value, a particular value, a particular size / a
# particular allocation), (more). It performs a sort (a sort, a particular sort / a particular ordering, a
# particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling sub_82A0B0A0, sub_82A0B0A0, a sort, on a value (a, a value, a
# particular value, a particular size, a particular size / a
# particular allocation), a particular sort / a particular ordering, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). In the loop, it compares (a comparison, a particular comparison / a
# particular ordering, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result, a value (a->0, a value, a particular value, a particular size / a
# particular allocation, a particular element) and a value (b->0, a value, a
# particular value, a particular size / a
# particular allocation, a particular element): if a value (r8, r7->4, a value, a
# particular value, a particular size / a
# particular allocation, a particular key) is >= a value (r11, r10->4, a value, a
# particular value, a particular size / a
# particular allocation, a particular key), it takes one path (a no-op / a return, a
# particular state, a particular result, a particular size / a
# particular allocation); if it is < the value (a particular state, a
# particular value, a particular size / a
# particular allocation), it sets a flag (r11 = 1, a flag, a particular value, a
# particular state, a particular size / a
# particular allocation)). In paths it recurses (sub_82A0B0A0, sub_82A0B0A0, a process, on a
# value (a, a value, a particular value, a particular size,
# a particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result). So it's a "process (a sort, with a loop and a comparison: perform a sort (a
# sort, a particular sort / a particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling sub_82A0B0A0, on a value,
# a particular sort / a particular ordering, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (in the loop) compare (a comparison, a particular comparison / a
# particular ordering, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result, a value (a->0, a value, a particular value, a particular size / a
# particular allocation, a particular element) and a value (b->0, a value, a
# particular value, a particular size / a
# particular allocation, a particular element)), and (in paths) recurse (sub_82A0B0A0, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a sort process, part of the 0x82A0A cluster, a sibling of
# ProcessRecursive2_82A0A2D0, a caller of sub_82A0B0A0). 198 lines, 1 callee (sub_82A0B0A0). 1 caller: sub_82A0A2D0
# (ProcessRecursive2, a recursive process, which calls this to perform the sort, a particular size / a
# particular allocation, a
# particular operation). The values' meanings (a, r29, a value, a particular value, a
# particular size / a particular allocation, a particular structure, a range / a sort object, b, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a sort process (with a
# loop and a comparison) (perform a sort (a
# sort, a particular sort / a particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling sub_82A0B0A0, on a value,
# a particular sort / a particular ordering, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (in the loop) compare (a comparison, a particular comparison / a
# particular ordering, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result, a value (a->0, a value, a particular value, a particular size / a
# particular allocation, a particular element) and a value (b->0, a value, a
# particular value, a particular size / a
# particular allocation, a particular element)), and (in paths) recurse (sub_82A0B0A0, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "Sort" refers to the sort (a
# sort, a particular sort / a particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result).
[entrypoint.functions."0x82A0A890"]
name = "ProcessSort_82A0A890"

# A process (an increment, with a copy, the caller of CheckedCopy_82CA3808, the sibling of ProcessIncrementCopy_827
# 2E7D0): (if a base (obj->4, a base, a particular value, a particular size / a particular allocation) is != a
# limit (obj->8, a limit, a particular value, a particular size / a particular allocation), a particular value)
# perform an increment (an increment, a particular increment / a particular count, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and copy
# (CheckedCopy_82CA3808, on a value, a particular copy, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and store the new limit (obj->8 = r31, a
# store, a limit, a particular value, a particular size / a particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a particular state, a
# particular result, a particular size / a particular allocation).

# Args: obj (r3, r30, ->4 = a base, ->8 = a limit, a particular structure, a grow object), (more). It checks a
# base (r3, obj->4, a base, a particular value, a particular size / a particular allocation): if it is != a
# limit (r5, obj->8, a limit, a particular value, a particular size / a particular allocation, a
# particular state), it performs an increment (an increment, a particular increment / a
# particular count, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and copies (CheckedCopy_82CA3808, sub_82CA3808, a checked copy, on a
# value (r6, a zero, a particular value, a particular size / a
# particular allocation, a particular source), a particular copy, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and stores the new limit (obj->8 = r31, a store, a limit, a
# particular value, a particular size / a
# particular allocation, a particular state / a
# particular result). If it is == the limit (a particular state, a
# particular value, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (an increment, with a copy: (if a
# base (obj->4, a base, a particular value, a particular size / a
# particular allocation) is != a
# limit (obj->8, a limit, a particular value, a particular size / a
# particular allocation), a particular value)
# perform an increment (an increment, a particular increment / a
# particular count, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and copy
# (CheckedCopy_82CA3808, on a value, a particular copy, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and store the new limit (obj->8 = r31, a
# store, a limit, a particular value, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation))" routine (an increment process, with a copy, part
# of the 0x82AAC cluster, a sibling of ProcessIncrementCopy_8272E7D0, a caller of CheckedCopy_82CA3808). 60
# lines, 1
# callee (CheckedCopy_82CA3808). 10+ callers: sub_824ED390, sub_823442E0, sub_824CAFB0, sub_82AAB6C8, sub_82A4ACB0
# , ProcessGrowAppend8_83221378, sub_8235ABA0, sub_82B6E0D0, sub_8233F518, sub_82AAB590 (processes, which
# call this to perform the increment, a particular count / a particular size, a
# particular size / a
# particular allocation, a particular operation). The object's class (obj, ->4 = a base, ->8 = a
# limit, a
# particular structure, a grow object, a particular size / a
# particular allocation, a particular value /
# a particular state) isn't resolvable, so the name states the mechanism: an increment process (with a
# copy) ((if a
# base (obj->4, a base, a particular value, a particular size / a
# particular allocation) is != a
# limit (obj->8, a limit, a particular value, a particular size / a
# particular allocation), a particular value)
# perform an increment (an increment, a particular increment / a
# particular count, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and copy
# (CheckedCopy_82CA3808, on a value, a particular copy, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and store the new limit (obj->8 = r31, a
# store, a limit, a particular value, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation)). The "2" distinguishes it from ProcessIncrementCopy_8272E7D0.
[entrypoint.functions."0x82AACDE0"]
name = "ProcessIncrementCopy2_82AACDE0"

# A process (a wrapper, with a compute and a call, the caller of ProcessRecursive2_82A0A2D0, the sibling of
# ProcessGrowAppend8_83221378 / ProcessRecursive2_82A0A2D0): compute a size (obj->8 - obj->4, a size, an 8-byte
# element, a particular size, a particular size / a particular allocation, a particular operation), and a count (a
# size / 8, a count, an 8-byte element, a particular count, a particular size / a particular allocation, a
# particular operation), and call a process (ProcessRecursive2_82A0A2D0, on a value (stack[96], a value, a
# particular value, a particular size / a particular allocation, a particular target), and a value (0, a zero, a
# particular value, a particular size / a particular allocation, a particular source), and a value (0, a zero, a
# particular value, a particular size / a particular allocation, a particular source), and a value (stack[80], a
# value, a particular value, a particular size / a particular allocation, a particular target), and a value (obj, a
# value, a particular value, a particular size / a particular allocation, a particular source), and a value (r5, a
# count, a particular count, a particular size / a particular allocation, a particular count), a particular process,
# a particular operation, a particular size / a particular allocation, a particular state / a particular result).

# Args: obj (r4, ->4 = a base, ->8 = a limit, a particular structure, a grow object), (more). It computes a size (r7,
# obj->8 - obj->4, a size, an 8-byte element, a particular size, a particular size / a particular allocation, a
# particular operation), and a count (r5, r7 / 8, a count, an 8-byte element, a particular count, a particular
# size / a particular allocation, a particular operation). It calls a process (ProcessRecursive2_82A0A2D0, sub
# _82A0A2D0, a recursive process, on a value (stack[96], a value, a particular value, a particular size / a
# particular allocation, a particular target), and a value (0, a zero, a particular value, a particular size / a
# particular allocation, a particular source), and a value (0, a zero, a particular value, a particular size / a
# particular allocation, a particular source), and a value (stack[80], a value, a particular value, a particular
# size / a particular allocation, a particular target), and a value (obj, a value, a particular value, a particular
# size / a particular allocation, a particular source), and a value (r5, a count, a particular count, a
# particular size / a particular allocation, a particular count), a particular process, a particular operation, a
# particular size / a particular allocation, a particular state / a particular result). So it's a "process (a
# wrapper, with a compute and a call: compute a size (obj->8 - obj->4, a size, an 8-byte element, a
# particular size, a particular size / a particular allocation, a particular operation), and a count (a
# size / 8, a count, an 8-byte element, a particular count, a particular size / a
# particular allocation, a
# particular operation), and call a process (ProcessRecursive2_82A0A2D0, on a
# value (stack[96], a value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (0, a zero, a
# particular value, a particular size / a
# particular allocation, a
# particular source), and a value (0, a zero, a
# particular value, a particular size / a
# particular allocation, a
# particular source), and a value (stack[80], a
# value, a particular value, a particular size / a
# particular allocation, a
# particular target), and a value (obj, a
# value, a particular value, a particular size / a
# particular allocation, a
# particular source), and a value (r5, a
# count, a particular count, a particular size / a
# particular allocation, a particular count), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result))" routine (a wrapper process, part of the 0x83221 cluster, a sibling of
# ProcessGrowAppend8_83221378 and ProcessRecursive2_82A0A2D0, a caller of ProcessRecursive2_82A0A2D0). 53 lines,
# 1
# callee (sub_82A0A2D0, ProcessRecursive2). 1 caller: ProcessGrowAppend8_83221378 (a grow/append process, which
# calls this to perform the wrapper, a particular size / a
# particular allocation, a
# particular operation). The object's class (obj, ->4 = a base, ->8 = a limit, a
# particular structure, a grow object, a particular size / a
# particular allocation, a particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a wrapper process (with a
# compute and a call) (compute a size (obj->8 - obj->4, a size, an 8-byte element, a
# particular size, a particular size / a
# particular allocation, a particular operation), and a count (a
# size / 8, a count, an 8-byte element, a particular count, a
# particular size / a
# particular allocation, a
# particular operation), and call a process (ProcessRecursive2_82A0A2D0, on a
# value (stack[96], a value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (0, a zero, a
# particular value, a particular size / a
# particular allocation, a
# particular source), and a value (0, a zero, a
# particular value, a particular size / a
# particular allocation, a
# particular source), and a value (stack[80], a
# value, a particular value, a particular size / a
# particular allocation, a
# particular target), and a value (obj, a
# value, a particular value, a particular size / a
# particular allocation, a
# particular source), and a value (r5, a
# count, a particular count, a particular size / a
# particular allocation, a particular count), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result)). The "SortWrapper" refers to the wrapper (a
# wrapper, a particular wrapper / a particular process, a particular size / a
# particular allocation, a
# particular operation) and the sort (a
# sort, a particular sort / a particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling ProcessRecursive2_82A0A2D0, a recursive process, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result).
[entrypoint.functions."0x83221320"]
name = "ProcessSortWrapper_83221320"

# A process (a range compute, with a division and a loop, the caller of MemCpy_Aligned_82CA30E8, the sibling of
# ProcessRangeCompute_82A10BC8 / ProcessRangeCompute2_82A4E348 / ProcessRangeCompute3_82C677B0): compute a size
# (obj->40 - obj->36, a size, a 24-byte element, a particular size, a particular size / a particular allocation, a
# particular operation), and a count (a size / 24, a count, a 24-byte element, a particular count, a
# particular size / a particular allocation, a particular operation), and (if a count (r10, a count, a 24-byte
# element, a particular count, a particular size / a particular allocation) is > 0, a particular value) perform a
# loop (a loop, a particular loop / a particular iteration, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result, calling MemCpy_Aligned_82CA30E8, on a value
# (stack[144], a value, a particular value, a particular size / a particular allocation, a particular target), and
# a value (obj + 16, a base, a particular value, a particular size / a particular allocation, a
# particular source), and a value (r8, a size, a particular size, a particular size / a
# particular allocation), a particular copy, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r28, ->36 = a base, ->40 = a limit, a particular structure, a range object), (more). It computes a
# size (r8, obj->40 - obj->36, a size, a 24-byte element, a particular size, a particular size / a
# particular allocation, a particular operation), and a count (r10, r8 / 24, a count, a 24-byte element, a
# particular count, a particular size / a
# particular allocation, a
# particular operation). It checks a count (r10, a count, a 24-byte element, a
# particular count, a particular size / a
# particular allocation): if it is > 0 (a particular value, a
# particular state, a particular size / a
# particular allocation), it performs a loop (a
# loop, a particular loop / a particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling MemCpy_Aligned_82CA30E8, sub_82CA30E8, an aligned copy, on a
# value (stack[144], a value, a particular value, a particular size / a
# particular allocation, a
# particular target), and a value (obj + 16, a base, a particular value, a particular size / a
# particular allocation, a
# particular source), and a value (r8, a size, a particular size, a particular size / a
# particular allocation), a particular copy, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). If it is <= 0 (a null state, a
# particular state, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a range compute, with a division and a loop: compute a size
# (obj->40 - obj->36, a size, a 24-byte element, a particular size, a particular size / a
# particular allocation, a particular operation), and a count (a size / 24, a count, a 24-byte element, a
# particular count, a
# particular size / a
# particular allocation, a
# particular operation), and (if a count (r10, a count, a 24-byte
# element, a particular count, a particular size / a
# particular allocation) is > 0, a particular value) perform a
# loop (a
# loop, a particular loop / a particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling MemCpy_Aligned_82CA30E8, on a value
# (stack[144], a value, a particular value, a particular size / a particular allocation, a particular target), and
# a value (obj + 16, a base, a particular value, a particular size / a
# particular allocation, a
# particular source), and a value (r8, a size, a particular size, a particular size / a
# particular allocation), a particular copy, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a range compute process, part of the 0x83232 cluster, a sibling of
# ProcessRangeCompute_82A10BC8, a caller of MemCpy_Aligned_82CA30E8). 569 lines, 1
# callee (MemCpy_Aligned_82CA30E8). 1 caller: sub_832351B0 (a process, which
# calls this to perform the range compute, a particular size / a
# particular allocation, a
# particular operation). The object's class (obj, ->36 = a base, ->40 = a limit, a
# particular structure, a range object, a particular size / a
# particular allocation, a particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a range compute process (with a
# division and a loop) (compute a size
# (obj->40 - obj->36, a size, a 24-byte element, a particular size, a particular size / a
# particular allocation, a particular operation), and a count (a size / 24, a count, a 24-byte element, a
# particular count, a
# particular size / a
# particular allocation, a
# particular operation), and (if a count (r10, a count, a 24-byte
# element, a particular count, a particular size / a
# particular allocation) is > 0, a particular value) perform a
# loop (a
# loop, a particular loop / a particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling MemCpy_Aligned_82CA30E8, on a value
# (stack[144], a value, a particular value, a particular size / a particular allocation, a particular target), and
# a value (obj + 16, a base, a particular value, a particular size / a
# particular allocation, a
# particular source), and a value (r8, a size, a particular size, a particular size / a
# particular allocation), a particular copy, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "4" distinguishes it from ProcessRangeCompute_82A10BC8, ProcessRangeCompute2
# _82A4E348, and ProcessRangeCompute3_82C677B0.
[entrypoint.functions."0x83232678"]
name = "ProcessRangeCompute4_83232678"

# A process (a range grow, with a compute and a call, the caller of ProcessIncrementCopy_8272E7D0, the sibling of
# ProcessRangeGrow_8217A858 / ProcessRangeCompute4_83232678): grow a range (a range grow, a particular grow / a
# particular range, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling ProcessIncrementCopy_8272E7D0, on a value (obj + 16, a base, a particular value, a
# particular size / a particular allocation, a particular target), a particular increment / a particular copy, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result), and
# (if a base (obj->36, a base, a particular value, a particular size / a particular allocation) is != a limit
# (obj->40, a limit, a particular value, a particular size / a particular allocation), a particular value) perform a
# range grow (a range grow, a particular grow / a particular range, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result, computing a size (obj->40 - obj->36, a size, a
# 24-byte element, a particular size, a particular size / a particular allocation, a particular operation)), and
# (otherwise) take one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->16 = a base, ->36 = a base, ->40 = a limit, a particular structure, a range object), (more).
# It grows a range (a range grow, a particular grow / a particular range, a particular size / a particular
# allocation, a particular operation, a particular state / a particular result, calling ProcessIncrementCopy
# _8272E7D0, sub_8272E7D0, an increment process, on a value (obj + 16, a base, a particular value, a
# particular size / a particular allocation, a particular target), a particular increment / a particular copy, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result). It
# checks a base (r8, obj->36, a base, a particular value, a particular size / a particular allocation): if it is != a
# limit (r9, obj->40, a limit, a particular value, a particular size / a particular allocation, a particular state),
# it performs a range grow (a range grow, a particular grow / a particular range, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result, computing a size (r8,
# obj->40 - obj->36, a size, a 24-byte element, a particular size, a particular size / a particular allocation, a
# particular operation)). If it is == the limit (a particular state, a particular value, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation). So it's a "process (a range grow, with a compute and a call: grow a
# range (a range grow, a particular grow / a particular range, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result, calling ProcessIncrementCopy_8272E7D0, on a value
# (obj + 16, a base, a particular value, a particular size / a particular allocation, a particular target), a
# particular increment / a particular copy, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (if a base (obj->36, a base, a particular value, a particular size /
# a particular allocation) is != a limit (obj->40, a limit, a particular value, a particular size / a
# particular allocation), a particular value) perform a range grow (a range grow, a particular grow / a
# particular range, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, computing a size (obj->40 - obj->36, a size, a 24-byte element, a particular size, a
# particular size / a particular allocation, a particular operation)), and (otherwise) take one path (a no-op / a
# return, a particular state, a particular result, a particular size / a particular allocation))" routine (a range
# grow process, part of the 0x83232 cluster, a sibling of ProcessRangeGrow_8217A858, a caller of
# ProcessIncrementCopy_8272E7D0). 125 lines, 1 callee (ProcessIncrementCopy_8272E7D0). 2 callers: sub_832351B0,
# sub_83232B48 (processes, which call this to grow the range, a particular size / a particular allocation, a
# particular operation). The object's class (obj, ->16 = a base, ->36 = a base, ->40 = a limit, a particular
# structure, a range object, a particular size / a particular allocation, a particular value / a
# particular state) isn't resolvable, so the name states the mechanism: a range grow process (with a
# compute and a call) (grow a range (a range grow, a particular grow / a particular range, a
# particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling ProcessIncrementCopy_8272E7D0, on a value
# (obj + 16, a base, a particular value, a particular size / a
# particular allocation, a
# particular target), a particular increment / a particular copy, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (if a base (obj->36, a base, a particular value, a
# particular size / a
# particular allocation) is != a limit (obj->40, a limit, a particular value, a
# particular size / a
# particular allocation), a particular value) perform a range grow (a range grow, a particular grow / a
# particular range, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result, computing a size (obj->40 - obj->36, a size, a 24-byte element, a
# particular size, a particular size / a
# particular allocation, a particular operation)), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation)). The "2" distinguishes it from ProcessRangeGrow_8217A858.
[entrypoint.functions."0x83232A70"]
name = "ProcessRangeGrow2_83232A70"

# A process (a dispatch, with a switch and a complex computation, the sibling of ProcessDispatchValue_8229A488 /
# ProcessRangeGrow2_83232A70, the caller of ProcessRangeGrow2_83232A70 / ProcessRangeCompute4_83232678 /
# ProcessGrowCopy_82300648 / ComputeVector_832119D8): perform a dispatch (a dispatch, a particular dispatch / a
# particular value, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling ProcessRangeGrow2_83232A70, on a value (obj, a value, a particular value, a
# particular size / a particular allocation, a particular target), and a value (a, a value, a particular value, a
# particular size / a particular allocation, a particular source), a particular range grow, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (in a switch, on a
# value (r31, a type, a particular value, a particular state, a particular size / a particular allocation), a
# switch 0-3) take a path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation), and (in paths) call a process (ProcessRangeCompute4_83232678, on a value, a
# particular range compute, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and a process (ProcessGrowCopy_82300648, on a value, a
# particular grow/copy, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and a process (ComputeVector_832119D8, on a value, a
# particular compute, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result).

# Args: obj (r3, r29, a value, a particular value, a particular size / a particular allocation, a
# particular structure, a dispatch object), a (r4, r27, ->8 = a value, a particular value, a
# particular size / a particular allocation), type (r5, r31, a type, a particular value, a
# particular state, a particular size / a particular allocation), (more). It performs a dispatch (a
# dispatch, a particular dispatch / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling ProcessRangeGrow2_83232A70, sub_83232A70, a range grow process, on a
# value (obj, a value, a particular value, a particular size / a
# particular allocation, a
# particular target), and a value (a, a value, a
# particular value, a particular size / a
# particular allocation, a
# particular source), a particular range grow, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). It checks a value (r11, r31 - 1, a type, a particular value, a
# particular state, a particular size / a
# particular allocation): if it is > 3 (a particular state, a
# particular value, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is <= 3 (a particular state, a
# particular value, a particular size / a
# particular allocation), it takes a path (a switch, on a
# value (r31, a type, a particular value, a
# particular state, a particular size / a
# particular allocation), a switch 0-3, a particular dispatch, a
# particular operation, a particular state / a
# particular result). In paths it calls a process (ProcessRangeCompute4_83232678, sub_83232678, a
# range compute process, on a value (obj, a value, a particular value, a
# particular size / a
# particular allocation, a
# particular target), a particular range compute, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and a process (ProcessGrowCopy_82300648, sub_82300648, a
# grow/copy process, on a value (obj, a value, a particular value, a
# particular size / a
# particular allocation, a
# particular target), a particular grow/copy, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and a process (ComputeVector_832119D8, sub_832119D8, a
# compute, on a value (obj, a value, a particular value, a
# particular size / a
# particular allocation, a
# particular target), a particular compute, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). So it's a "process (a dispatch, with a switch and a complex computation: perform a
# dispatch (a dispatch, a particular dispatch / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling ProcessRangeGrow2_83232A70, on a value (obj, a value, a particular value, a
# particular size / a particular allocation, a particular target), and a value (a, a value, a particular value, a
# particular size / a particular allocation, a particular source), a particular range grow, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (in a switch, on a
# value (r31, a type, a particular value, a particular state, a particular size / a particular allocation), a
# switch 0-3) take a path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation), and (in paths) call a process (ProcessRangeCompute4_83232678, on a value, a
# particular range compute, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and a process (ProcessGrowCopy_82300648, on a value, a
# particular grow/copy, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and a process (ComputeVector_832119D8, on a value, a
# particular compute, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result))" routine (a dispatch process, part of the 0x83235 cluster, a
# sibling of ProcessDispatchValue_8229A488, a caller of ProcessRangeGrow2_83232A70, ProcessRangeCompute4
# _83232678, ProcessGrowCopy_82300648, and ComputeVector_832119D8). 335 lines, 11 callees (ComputeVector
# _832119D8, ProcessGrowCopy_82300648, sub_82A13220, sub_82A134B8, sub_82A13750, sub_82A139D0, sub_83211790,
# sub_83211860, sub_83211900, sub_83232678, sub_83232A70). 1 caller: ProcessGrowAppend8_83221378 (a grow/append
# process, which calls this to perform the dispatch, a particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r29, a value, a particular value, a
# particular size / a particular allocation, a particular structure, a dispatch object, a
# particular value / a
# particular state) and the value's meaning (a, r27, ->8 = a value, a particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a dispatch process (with a
# switch and a complex computation) (perform a
# dispatch (a dispatch, a particular dispatch / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling ProcessRangeGrow2_83232A70, on a value (obj, a value, a particular value, a
# particular size / a particular allocation, a particular target), and a value (a, a value, a particular value, a
# particular size / a particular allocation, a particular source), a particular range grow, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (in a switch, on a
# value (r31, a type, a particular value, a particular state, a particular size / a particular allocation), a
# switch 0-3) take a path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation), and (in paths) call a process (ProcessRangeCompute4_83232678, on a value, a
# particular range compute, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and a process (ProcessGrowCopy_82300648, on a value, a
# particular grow/copy, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and a process (ComputeVector_832119D8, on a value, a
# particular compute, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result)). The "2" distinguishes it from ProcessDispatchValue_8229A488.
[entrypoint.functions."0x832351B0"]
name = "ProcessDispatchValue2_832351B0"

# A process (a hash lookup, with a check and a compute, the caller of HashCstring_82188CF0 / LinearSearch_82284448,
# the sibling of ProcessHashLookup_82B5F0B8): perform a hash lookup (a hash lookup, a particular lookup / a
# particular hash, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling HashCstring_82188CF0, on a value (r3, a value, a particular value, a particular size,
# a particular size / a particular allocation, a particular key), a particular hash, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (in paths) search
# (LinearSearch_82284448, on a value, a particular search / a particular lookup, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (if a value (a, a
# value, a particular value, a particular size / a particular allocation) is zero, a null state) take one path (a
# no-op / a return, a particular state, a particular result, a particular size / a particular allocation), and (if a
# value (b, a value, a particular value, a particular size / a particular allocation) is zero, a null state) take one
# path (a no-op / a return, a particular state, a particular result, a particular size / a particular allocation),
# and (if a value (c->0, a value, a particular value, a particular size / a particular allocation) is zero, a
# null state) take one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation).

# Args: a (r3, r31, a value, a particular value, a particular size / a particular allocation), b (r4, r30, a value,
# a particular value, a particular size / a particular allocation), c (r5, ->0 = a value, a particular value, a
# particular size / a particular allocation), (more). It checks a value (r31, a, a value, a particular value, a
# particular size / a particular allocation): if it is zero (a null state, a particular state, a particular size /
# a particular allocation), it takes one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation). It checks a value (r30, b, a value, a particular value, a
# particular size / a particular allocation): if it is zero (a null state, a particular state, a particular size /
# a particular allocation), it takes one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation). It checks a value (r11, c->0, a value, a particular value, a
# particular size / a particular allocation): if it is zero (a null state, a particular state, a particular size /
# a particular allocation), it takes one path (a no-op / a return, a particular state, a particular result, a
# particular size / a particular allocation); if it is non-zero (a particular value, a particular state, a
# particular size / a particular allocation), it performs a hash lookup (a hash lookup, a particular lookup / a
# particular hash, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling HashCstring_82188CF0, sub_82188CF0, a hash, on a value (r3, a value, a
# particular value, a particular size, a particular size / a particular allocation, a particular key), a
# particular hash, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result). In paths it searches (LinearSearch_82284448, sub_82284448, a linear search, on a
# value (a, a value, a particular value, a particular size, a particular size / a
# particular allocation), a particular search / a
# particular lookup, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). So it's a "process (a hash lookup, with a check and a compute: perform a
# hash lookup (a hash lookup, a particular lookup / a
# particular hash, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling HashCstring_82188CF0, on a value (r3, a value, a particular value, a particular size,
# a particular size / a particular allocation, a particular key), a particular hash, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (in paths) search
# (LinearSearch_82284448, on a value, a particular search / a particular lookup, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (if a value (a, a
# value, a particular value, a particular size / a particular allocation) is zero, a null state) take one path (a
# no-op / a return, a particular state, a particular result, a particular size / a particular allocation), and (if a
# value (b, a value, a particular value, a particular size / a particular allocation) is zero, a null state) take one
# path (a no-op / a return, a particular state, a particular result, a particular size / a particular allocation),
# and (if a value (c->0, a value, a particular value, a particular size / a particular allocation) is zero, a
# null state) take one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation))" routine (a hash lookup process, part of the 0x823F7 cluster, a sibling of
# ProcessHashLookup_82B5F0B8, a caller of HashCstring_82188CF0 and LinearSearch_82284448). 122 lines, 2 callees
# (HashCstring_82188CF0, LinearSearch_82284448). 2 callers: sub_829DDE68, sub_828152D0 (processes, which
# call this to perform the hash lookup, a particular size / a
# particular allocation, a
# particular operation). The values' meanings (a, r31, a value, a particular value, a
# particular size / a
# particular allocation, b, r30, a value, a particular value, a
# particular size / a
# particular allocation, c, ->0 = a value, a particular value, a particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a hash lookup process (with a
# check and a compute) (perform a
# hash lookup (a hash lookup, a particular lookup / a
# particular hash, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling HashCstring_82188CF0, on a value (r3, a value, a particular value, a particular size,
# a particular size / a particular allocation, a particular key), a particular hash, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (in paths) search
# (LinearSearch_82284448, on a value, a particular search / a particular lookup, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (if a value (a, a
# value, a particular value, a particular size / a particular allocation) is zero, a null state) take one path (a
# no-op / a return, a particular state, a particular result, a particular size / a particular allocation), and (if a
# value (b, a value, a particular value, a particular size / a particular allocation) is zero, a null state) take one
# path (a no-op / a return, a particular state, a particular result, a particular size / a particular allocation),
# and (if a value (c->0, a value, a particular value, a particular size / a particular allocation) is zero, a
# null state) take one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation)). The "2" distinguishes it from ProcessHashLookup_82B5F0B8.
[entrypoint.functions."0x823F75A0"]
name = "ProcessHashLookup2_823F75A0"

# A process (an element access, with a check and a call, the caller of ValidateIndex_822F6EE8 / ValidateElement
# Access_8217A7F8 / ProcessRangeGrow_8217A858 / ProcessVcallLoop_8217A9A8, the sibling of ProcessElementAccess
# _8217A400 / ProcessElementAccess2_8217A6C0): perform an element access (an element access, a particular access / a
# particular element, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and (if a count (obj->24, a count, a particular count, a particular size / a particular
# allocation) is >= 8, a particular threshold, a particular count) take a base (obj->4, a base, a particular value, a
# particular size / a particular allocation, a particular element), and (otherwise) take a base (obj + 4, a base, a
# particular value, a particular size / a particular allocation, a particular element), and call a check
# (ValidateIndex_822F6EE8, on a value (stack[88], a value, a particular value, a particular size / a
# particular allocation, a particular target), and a base (r4, a base, a particular value, a particular size / a
# particular allocation, a particular element), and a value (obj, a value, a particular value, a particular size /
# a particular allocation, a particular element), a particular check / a particular index, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (in paths) validate
# (ValidateElementAccess_8217A7F8, on a value, a particular validate / a particular element, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and grow a range
# (ProcessRangeGrow_8217A858, on a value, a particular grow / a particular range, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and perform a loop
# (ProcessVcallLoop_8217A9A8, on a value, a particular loop / a particular vcall, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result).

# Args: obj (r3, r31, ->4 = a base, ->24 = a count, a particular structure, a range object), (more). It checks a
# count (r11, obj->24, a count, a particular count, a particular size / a particular allocation): if it is >= 8 (a
# particular threshold, a particular count, a particular value, a particular state, a particular size / a
# particular allocation), it takes a base (r4, obj->4, a base, a particular value, a particular size / a
# particular allocation, a particular element); if it is < 8 (a particular threshold, a particular count, a
# particular value, a particular state, a particular size / a particular allocation), it takes a base (r4, obj + 4, a
# base, a particular value, a particular size / a particular allocation, a particular element). It calls a check
# (ValidateIndex_822F6EE8, sub_822F6EE8, a check, on a value (stack[88], a value, a particular value, a
# particular size / a particular allocation, a particular target), and a base (r4, a base, a particular value, a
# particular size / a particular allocation, a particular element), and a value (obj, a value, a particular value, a
# particular size / a particular allocation, a particular element), a particular check / a particular index, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result). In
# paths it validates (ValidateElementAccess_8217A7F8, sub_8217A7F8, a validate, on a value (obj, a value, a
# particular value, a particular size, a particular size / a particular allocation), a particular validate / a
# particular element, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), grows a range (ProcessRangeGrow_8217A858, sub_8217A858, a range grow, on a value (obj, a
# value, a particular value, a particular size, a particular size / a particular allocation), a particular grow / a
# particular range, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and performs a loop (ProcessVcallLoop_8217A9A8, sub_8217A9A8, a vcall loop, on a value (obj, a
# value, a particular value, a particular size, a particular size / a particular allocation), a particular loop / a
# particular vcall, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result). So it's a "process (an element access, with a check and a call: perform an element access (an
# element access, a particular access / a particular element, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and (if a count (obj->24, a count, a
# particular count, a particular size / a particular allocation) is >= 8, a particular threshold, a
# particular count) take a base (obj->4, a base, a particular value, a particular size / a particular allocation, a
# particular element), and (otherwise) take a base (obj + 4, a base, a particular value, a particular size / a
# particular allocation, a particular element), and call a check (ValidateIndex_822F6EE8, on a value (stack[88], a
# value, a particular value, a particular size / a particular allocation, a particular target), and a base (r4, a
# base, a particular value, a particular size / a particular allocation, a particular element), and a value (obj, a
# value, a particular value, a particular size / a particular allocation, a particular element), a particular check /
# a particular index, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and (in paths) validate (ValidateElementAccess_8217A7F8, on a value, a particular validate / a
# particular element, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and grow a range (ProcessRangeGrow_8217A858, on a value, a particular grow / a
# particular range, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and perform a loop (ProcessVcallLoop_8217A9A8, on a value, a particular loop / a
# particular vcall, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result))" routine (an element access process, part of the 0x8217A cluster, a sibling of
# ProcessElementAccess_8217A400, a caller of ValidateIndex_822F6EE8, ValidateElementAccess_8217A7F8,
# ProcessRangeGrow_8217A858, and ProcessVcallLoop_8217A9A8). 156 lines, 5 callees (ProcessRangeGrow_8217A858,
# ProcessVcallLoop_8217A9A8, ValidateElementAccess_8217A7F8, ValidateIndex_822F6EE8, sub_822F52A8). 10+ callers: sub
# _822F27C0, sub_82C68B50, sub_82368830, sub_8217AB58, sub_822F38B0, LoadString_FastOrSlow_823725E8, sub_82372758, sub
# _82C69318, sub_82B3E560, sub_82B3EDD0 (processes, which call this to perform the element access, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, ->4 = a base, ->24 = a count, a
# particular structure, a range object, a particular size / a
# particular allocation, a particular value /
# a particular state) isn't resolvable, so the name states the mechanism: an element access process (with a
# check and a call) (perform an element access (an
# element access, a particular access / a particular element, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a count (obj->24, a count, a
# particular count, a particular size / a
# particular allocation) is >= 8, a particular threshold, a
# particular count) take a base (obj->4, a base, a particular value, a
# particular size / a
# particular allocation, a
# particular element), and (otherwise) take a base (obj + 4, a
# base, a particular value, a particular size / a
# particular allocation, a
# particular element), and call a check (ValidateIndex_822F6EE8, on a value (stack[88], a
# value, a particular value, a particular size / a
# particular allocation, a
# particular target), and a base (r4, a base, a particular value, a
# particular size / a
# particular allocation, a
# particular element), and a value (obj, a
# value, a particular value, a particular size / a
# particular allocation, a
# particular element), a particular check / a
# particular index, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (in paths) validate (ValidateElementAccess_8217A7F8, on a
# value, a particular validate / a
# particular element, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and grow a range (ProcessRangeGrow_8217A858, on a
# value, a particular grow / a
# particular range, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and perform a loop (ProcessVcallLoop_8217A9A8, on a
# value, a particular loop / a
# particular vcall, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result)). The "3" distinguishes it from ProcessElementAccess_8217A400 and ProcessElementAccess2
# _8217A6C0.
[entrypoint.functions."0x8217AA20"]
name = "ProcessElementAccess3_8217AA20"

# A process (a lookup, with a check and a comparison, the caller of LinearSearch_82284448 / FloatToIntProcess_8228
# 4810, the sibling of LinearSearch_82284448): perform a lookup (a lookup, a particular lookup / a
# particular search, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling LinearSearch_82284448, on a value (a, a value, a particular value, a
# particular size / a particular allocation, a particular target), and a value (b, a value, a
# particular value, a particular size / a particular allocation, a particular source), and a value (c, a
# value, a particular value, a particular size / a particular allocation, a particular key), a particular
# lookup / a particular search, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (if a result (r29, a value, a particular value, a
# particular size / a particular allocation, a particular result) is non-zero, a particular value) check a value
# (r29->4, a value, a particular value, a particular size / a particular allocation, a particular key): if it is != a
# value (c, a value, a particular value, a particular size / a particular allocation, a particular key), take one
# path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation); if it is == the value (a particular state, a particular value, a
# particular size / a
# particular allocation), check a flag (r29->0 & 0x1C, a flag, a particular value, a
# particular state, a particular size / a
# particular allocation): if it is != 20 (a particular state, a
# particular value, a particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == 20 (a particular state, a
# particular value, a particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) process (FloatToIntProcess_82284810, on a value, a
# particular process / a
# particular float-to-int, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result).

# Args: obj (r3, r31, an object), a (r4, r28, ->28 = a value, a particular value, a
# particular size / a
# particular allocation), b (r5, a value, a particular value, a
# particular size / a
# particular allocation), c (r6, r27, a value, a particular value, a
# particular size / a
# particular allocation), (more). It performs a lookup (a
# lookup, a particular lookup / a
# particular search, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling LinearSearch_82284448, sub_82284448, a linear search, on a
# value (a, a value, a particular value, a particular size / a
# particular allocation, a
# particular target), and a value (b, a value, a
# particular value, a particular size / a
# particular allocation, a
# particular source), and a value (c, a
# value, a particular value, a particular size / a
# particular allocation, a
# particular key), a particular
# lookup / a
# particular search, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). It checks a result (r29, a value, a
# particular value, a particular size / a
# particular allocation, a
# particular result): if it is non-zero (a particular value, a
# particular state, a particular size / a
# particular allocation), it checks a value (r11, r29->4, a
# value, a particular value, a particular size / a
# particular allocation, a
# particular key): if it is != a value (r27, c, a
# value, a particular value, a particular size / a
# particular allocation, a
# particular key), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == the value (a particular state, a
# particular value, a particular size / a
# particular allocation), it checks a flag (r10, r29->0 & 0x1C, a
# flag, a particular value, a
# particular state, a particular size / a
# particular allocation): if it is != 20 (a particular state, a
# particular value, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == 20 (a particular state, a
# particular value, a particular size / a
# particular allocation), it takes a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). If the result is zero (a null state, a
# particular state, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). In paths it processes (FloatToIntProcess_82284810, sub_82284810, a
# float-to-int process, on a value (a, a value, a
# particular value, a particular size, a particular size / a
# particular allocation), a particular process / a
# particular float-to-int, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). So it's a "process (a lookup, with a check and a comparison: perform a
# lookup (a lookup, a particular lookup / a
# particular search, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling LinearSearch_82284448, on a value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), and a value (b, a value, a
# particular value, a particular size / a
# particular allocation, a
# particular source), and a value (c, a
# value, a particular value, a particular size / a
# particular allocation, a
# particular key), a particular
# lookup / a
# particular search, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (if a result (r29, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular result) is non-zero, a particular value) check a value
# (r29->4, a value, a particular value, a particular size / a
# particular allocation, a particular key): if it is != a
# value (c, a value, a particular value, a particular size / a
# particular allocation, a
# particular key), take one
# path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == the value (a particular state, a
# particular value, a
# particular size / a
# particular allocation), check a flag (r29->0 & 0x1C, a flag, a
# particular value, a
# particular state, a particular size / a
# particular allocation): if it is != 20 (a particular state, a
# particular value, a particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == 20 (a particular state, a
# particular value, a particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) process (FloatToIntProcess_82284810, on a value, a
# particular process / a
# particular float-to-int, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result))" routine (a lookup process, with a check and a comparison, part
# of the 0x82284 cluster, a sibling of LinearSearch_82284448, a caller of LinearSearch_82284448 and
# FloatToIntProcess_82284810). 184 lines, 2 callees (FloatToIntProcess_82284810, LinearSearch_82284448). No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (obj, r31, an object, a
# particular structure, a lookup object, a particular size / a
# particular allocation, a
# particular value /
# a particular state) and the value's meaning (a, r28, ->28 = a value, a
# particular value, a
# particular size / a
# particular allocation, b, a value, a particular value, a
# particular size / a
# particular allocation, c, r27, a value, a particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a lookup process (with a
# check and a comparison) (perform a
# lookup (a lookup, a particular lookup / a
# particular search, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling LinearSearch_82284448, on a value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), and a value (b, a value, a
# particular value, a particular size / a
# particular allocation, a
# particular source), and a value (c, a
# value, a particular value, a particular size / a
# particular allocation, a
# particular key), a particular
# lookup / a
# particular search, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (if a result (r29, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular result) is non-zero, a particular value) check a value
# (r29->4, a value, a particular value, a particular size / a
# particular allocation, a particular key): if it is != a
# value (c, a value, a particular value, a particular size / a
# particular allocation, a
# particular key), take one
# path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == the value (a particular state, a
# particular value, a
# particular size / a
# particular allocation), check a flag (r29->0 & 0x1C, a flag, a
# particular value, a
# particular state, a particular size / a
# particular allocation): if it is != 20 (a particular state, a
# particular value, a particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == 20 (a particular state, a
# particular value, a particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) process (FloatToIntProcess_82284810, on a value, a
# particular process / a
# particular float-to-int, a particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result)). The "LookupCheck" refers to the lookup (a
# lookup, a particular lookup / a
# particular search, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result) and the check (a
# check, a particular check / a
# particular comparison, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result).
[entrypoint.functions."0x82284300"]
name = "ProcessLookupCheck_82284300"

# A count (a count, with a loop and a comparison, the sibling of ProcessLookupCheck_82284300): perform a count (a
# count, a particular count / a particular number, a particular size / a particular allocation, a particular
# operation, a particular state / a particular result), and (in a loop) check a value (base->0, a value, a
# particular value, a particular size / a particular allocation, a 2-byte element, a particular element): if it is
# zero (a null state, a particular state, a particular size / a particular allocation), take one path (a no-op / a
# return, a particular state, a particular result, a particular size / a particular allocation); if it is non-zero (a
# particular value, a particular state, a particular size / a particular allocation), increment a count (r11, a
# count, a particular count, a particular size / a particular allocation, a particular number), and advance a base
# (r3, a base, a particular value, a particular size / a particular allocation, a 2-byte element, a
# particular source), and (if a count (r11, a count, a particular count, a particular size / a
# particular allocation, a
# particular number) is < a value (r4, a count, a particular count, a particular size / a
# particular allocation, a
# particular number), a particular count) continue the loop (a loop, a particular loop / a
# particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and return the count (r11, a count, a particular count, a
# particular size / a
# particular allocation, a
# particular number).

# Args: base (r3, a value, a particular value, a particular size / a particular allocation, a 2-byte element, a
# particular source), count (r4, a count, a particular count, a particular size / a
# particular allocation, a
# particular number), (more). It performs a count (a
# count, a particular count / a
# particular number, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result). In the loop, it checks a value (r10, base->0, a
# value, a particular value, a particular size / a
# particular allocation, a 2-byte element, a
# particular element): if it is zero (a null state, a
# particular state, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a particular size / a
# particular allocation), it increments a count (r11, r11 + 1, a
# count, a particular count, a particular size / a
# particular allocation, a
# particular number), and advances a base (r3, r3 + 2, a
# base, a particular value, a particular size / a
# particular allocation, a 2-byte element, a
# particular source). It checks a count (r11, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number): if it is < a value (r4, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number), it continues the loop (a
# loop, a particular loop / a
# particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result); if it is >= the value (a particular state, a
# particular value, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). It returns the count (r11, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number). So it's a "count (a count, with a loop and a comparison: perform a
# count (a
# count, a particular count / a
# particular number, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a loop) check a value (base->0, a value, a
# particular value, a particular size / a
# particular allocation, a 2-byte element, a
# particular element): if it is
# zero (a null state, a particular state, a particular size / a
# particular allocation), take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a
# particular value, a
# particular state, a particular size / a
# particular allocation), increment a count (r11, a
# count, a particular count, a particular size / a
# particular allocation, a
# particular number), and advance a base
# (r3, a base, a particular value, a particular size / a
# particular allocation, a 2-byte element, a
# particular source), and (if a count (r11, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number) is < a value (r4, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number), a particular count) continue the loop (a loop, a particular loop / a
# particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and return the count (r11, a count, a
# particular count, a
# particular size / a
# particular allocation, a
# particular number))" routine (a count routine, part of the 0x822CA cluster, a sibling of
# ProcessLookupCheck_82284300). 30 lines, 0 callees. 2 callers: sub_822CA580, sub_82CA67D0 (processes, which
# call this to perform the count, a particular count / a
# particular number, a
# particular size / a
# particular allocation, a
# particular operation). The values' meanings (base, r3, a value, a
# particular value, a particular size / a
# particular allocation, a 2-byte element, a
# particular source, count, r4, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number) aren't resolvable, so the name states the mechanism: a count (with a
# loop and a comparison) (perform a
# count (a
# count, a particular count / a
# particular number, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a loop) check a value (base->0, a value, a
# particular value, a particular size / a
# particular allocation, a 2-byte element, a
# particular element): if it is
# zero (a null state, a particular state, a particular size / a
# particular allocation), take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a
# particular value, a
# particular state, a particular size / a
# particular allocation), increment a count (r11, a
# count, a particular count, a particular size / a
# particular allocation, a
# particular number), and advance a base
# (r3, a base, a particular value, a particular size / a
# particular allocation, a 2-byte element, a
# particular source), and (if a count (r11, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number) is < a value (r4, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number), a particular count) continue the loop (a loop, a particular loop / a
# particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and return the count (r11, a count, a
# particular count, a
# particular size / a
# particular allocation, a
# particular number)). The "NonZero" refers to the count (a
# count, a particular count / a
# particular number, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result) of non-zero values (a
# value, a particular value, a particular size / a
# particular allocation, a 2-byte element, a
# particular element, a
# particular value, a
# particular state).
[entrypoint.functions."0x822CA680"]
name = "CountNonZero_822CA680"

# A 32-byte constructor (the sibling of Construct32Byte_82B722E8 / Construct32Byte2_83221998, with a constant and
# a complex computation): initialize the object's fields (a 32-byte object, a particular structure, a
# particular size / a particular allocation, obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a particular marker, obj->4 = 0, a field, a count / a size, a zero, obj->20 = 1, a
# field, a value, a one, obj->8 = a constant, from a fixed .data address, a float, a particular value, obj->21 = 1, a
# field, a value, a one, obj->12 = a constant, from a fixed .data address, a float, a particular value, obj->22 = 0, a
# field, a count / a size, a zero, obj->16 = a constant, from a fixed .data address, a float, a particular value,
# obj->23 = 0, a field, a count / a size, a zero, obj->32 = a constant, from a fixed .data address, a float, a
# particular value, obj->24 = 0, a field, a count / a size, a zero, obj->36 = a constant, from a fixed .data address,
# a float, a particular value, obj->28 = 0, a field, a count / a size, a zero, obj->40 = a constant, from a fixed
# .data address, a float, a particular value, obj->44 = 1, a field, a value, a one, obj->56 = a constant, from a
# fixed .data address, a float, a particular value, a 32-byte object, a particular structure, a
# particular size / a
# particular allocation, a particular initialization).

# Args: obj (r3, ->0 = a field, a constant, a class id / a marker, ->4 = a field, a count / a size, ->8 = a field,
# a float, ->12 = a field, a float, ->16 = a field, a float, ->20 = a field, a value, ->21 = a field, a
# value, ->22 = a field, a count / a size, ->23 = a field, a count / a size, ->24 = a field, a
# count / a size, ->28 = a field, a count / a size, ->32 = a field, a float, ->36 = a field, a
# float, ->40 = a field, a float, ->44 = a field, a value, ->56 = a field, a float, a
# 32-byte object), (more). It initializes the object's fields (a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation: obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, obj->4 = 0, a field, a count / a size, a zero, obj->20 = 1, a
# field, a value, a one, obj->8 = a constant, from a fixed .data address, a float, a
# particular value, obj->21 = 1, a
# field, a value, a one, obj->12 = a constant, from a fixed .data address, a float, a
# particular value, obj->22 = 0, a
# field, a count / a size, a zero, obj->16 = a constant, from a fixed .data address, a float, a
# particular value, obj->23 = 0, a
# field, a count / a size, a zero, obj->32 = a constant, from a fixed .data address, a float, a
# particular value, obj->24 = 0, a
# field, a count / a size, a zero, obj->36 = a constant, from a fixed .data address, a float, a
# particular value, obj->28 = 0, a
# field, a count / a size, a zero, obj->40 = a constant, from a fixed .data address, a float, a
# particular value, obj->44 = 1, a
# field, a value, a one, obj->56 = a constant, from a fixed .data address, a float, a
# particular value, a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation, a
# particular initialization). So it's a "construct a 32-byte object (initialize the object's fields (a
# 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation, obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, obj->4 = 0, a field, a count / a size, a zero, obj->20 = 1, a
# field, a value, a one, obj->8 = a constant, from a fixed .data address, a float, a
# particular value, obj->21 = 1, a
# field, a value, a one, obj->12 = a constant, from a fixed .data address, a float, a
# particular value, obj->22 = 0, a
# field, a count / a size, a zero, obj->16 = a constant, from a fixed .data address, a float, a
# particular value, obj->23 = 0, a
# field, a count / a size, a zero, obj->32 = a constant, from a fixed .data address, a float, a
# particular value, obj->24 = 0, a
# field, a count / a size, a zero, obj->36 = a constant, from a fixed .data address, a float, a
# particular value, obj->28 = 0, a
# field, a count / a size, a zero, obj->40 = a constant, from a fixed .data address, a float, a
# particular value, obj->44 = 1, a
# field, a value, a one, obj->56 = a constant, from a fixed .data address, a float, a
# particular value, a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation))" routine (a 32-byte constructor, with a
# constant and a
# complex computation, part
# of the 0x82BE4 cluster, a sibling of Construct32Byte_82B722E8 and Construct32Byte2_83221998). 109
# lines, 0 callees. 3 callers: sub_8323FA78, sub_8323FCA8, sub_8323FF48 (siblings in the 0x8323F cluster, a
# process, which calls this to construct a 32-byte object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, ->0 = a field, a constant, a class id / a
# marker, ->4 = a field, a count / a size, ->8 = a field, a float, ->12 = a field, a
# float, ->16 = a field, a float, ->20 = a field, a value, ->21 = a field, a
# value, ->22 = a field, a count / a size, ->23 = a field, a count / a size, ->24 = a field, a
# count / a size, ->28 = a field, a count / a size, ->32 = a field, a float, ->36 = a field, a
# float, ->40 = a field, a float, ->44 = a field, a value, ->56 = a field, a float, a
# 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation, a
# particular value / a
# particular state) isn't resolvable, so the name states the mechanism: a 32-byte constructor (with a
# constant and a
# complex computation) (initialize the object's fields (a
# 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation, obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, obj->4 = 0, a field, a count / a size, a zero, obj->20 = 1, a
# field, a value, a one, obj->8 = a constant, from a fixed .data address, a float, a
# particular value, obj->21 = 1, a
# field, a value, a one, obj->12 = a constant, from a fixed .data address, a float, a
# particular value, obj->22 = 0, a
# field, a count / a size, a zero, obj->16 = a constant, from a fixed .data address, a float, a
# particular value, obj->23 = 0, a
# field, a count / a size, a zero, obj->32 = a constant, from a fixed .data address, a float, a
# particular value, obj->24 = 0, a
# field, a count / a size, a zero, obj->36 = a constant, from a fixed .data address, a float, a
# particular value, obj->28 = 0, a
# field, a count / a size, a zero, obj->40 = a constant, from a fixed .data address, a float, a
# particular value, obj->44 = 1, a
# field, a value, a one, obj->56 = a constant, from a fixed .data address, a float, a
# particular value, a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation)). The "3" distinguishes it from Construct32Byte_82B722E8 and Construct32Byte2_83221998.
[entrypoint.functions."0x82BE49F0"]
name = "Construct32Byte3_82BE49F0"

# An atomic decrement (with a release, the sibling of AtomicDecrement_822C72E0 / AtomicDecrement2_826C0B30): perform an
# atomic decrement (an atomic decrement, a particular decrement / a particular count, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (if a reference
# count (obj->4, a reference count, a particular count, a particular size / a particular allocation) is zero, a
# null state, a particular state) release a value (a value, a particular value, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (otherwise) take
# one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation).

# Args: obj (r3, ->0 = a value, a particular value, a particular size / a particular allocation, ->4 = a reference
# count, a particular count, a particular size / a particular allocation, a reference counted object), (more). It
# performs an atomic decrement (an atomic decrement, a particular decrement / a particular count, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result: it reads a reference count (r10, obj->4, a reference count, a
# particular count, a particular size / a
# particular allocation, a
# particular value), and stores it (r10 - 1, an atomic decrement, a
# particular count, a particular size / a
# particular allocation, a
# particular value)). It checks a reference count (r31, obj->4, a
# reference count, a
# particular count, a particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a particular size / a
# particular allocation), it releases a value (a
# value, a particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result); if it is non-zero (a particular value, a
# particular state, a particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's an "atomic decrement (with a release: perform an
# atomic decrement (an atomic decrement, a particular decrement / a
# particular count, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a reference
# count (obj->4, a reference count, a particular count, a particular size / a
# particular allocation) is zero, a
# null state, a particular state) release a value (a value, a particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (otherwise) take
# one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation))" routine (an atomic decrement, with a
# release, part of the 0x82375 cluster, a sibling of AtomicDecrement_822C72E0 and
# AtomicDecrement2_826C0B30). 77 lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (obj, ->0 = a value, a particular value, a particular size / a
# particular allocation, ->4 = a reference
# count, a
# particular count, a particular size / a
# particular allocation, a reference counted object, a
# particular value / a
# particular state) isn't resolvable, so the name states the mechanism: an atomic decrement (with a
# release) (perform an
# atomic decrement (an atomic decrement, a particular decrement / a
# particular count, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a reference
# count (obj->4, a reference count, a particular count, a particular size / a
# particular allocation) is zero, a
# null state, a particular state) release a value (a value, a particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (otherwise) take
# one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation)). The "3" distinguishes it from AtomicDecrement_822C72E0 and AtomicDecrement2_826C0B30.
[entrypoint.functions."0x82375F50"]
name = "AtomicDecrement3_82375F50"

# A process (a dispatch, with a switch and a call, the sibling of AtomicDecrement3_82375F50 / ProcessDispatch
# _82BCD7B0, the caller of sub_82815CB0 / sub_82188B88 / sub_82815E00): perform a dispatch (a dispatch, a
# particular dispatch / a particular value, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (in a switch, on a value (a->10, a type, a particular value, a
# particular state, a particular size / a particular allocation), a switch on a type) take a path (a no-op / a
# return, a particular state, a particular result, a particular size / a particular allocation), and (in paths) call
# a process (sub_82815CB0, on a value, a particular process, a particular operation, a particular size / a
# particular allocation, a particular state / a particular result), and a process (sub_82188B88, on a value, a
# particular process, a particular operation, a particular size / a particular allocation, a particular state / a
# particular result), and a process (sub_82815E00, on a value, a particular process, a particular operation, a
# particular size / a particular allocation, a particular state / a particular result).

# Args: obj (r3, a value), a (r4, ->10 = a type, a particular value, a particular state, a particular size / a
# particular allocation, a dispatch object), (more). It performs a dispatch (a dispatch, a particular dispatch / a
# particular value, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result). In the switch, on a value (r11, a->10, a type, a particular value, a particular state, a
# particular size / a particular allocation), it takes a path: if the type is 6 (a particular state, a
# particular value, a particular size / a particular allocation), it calls a process (sub_82815CB0, sub_82815CB0, a
# process, on a value (obj, a value, a particular value, a particular size, a particular size / a
# particular allocation), a particular process, a particular operation, a particular size / a
# particular allocation, a particular state / a particular result); if the type is 2 (a particular state, a
# particular value, a particular size / a particular allocation), it calls a process (sub_82188B88, sub_82188B88, a
# process, on a value (obj, a value, a particular value, a particular size, a particular size / a
# particular allocation), a particular process, a particular operation, a particular size / a
# particular allocation, a particular state / a particular result); if the type is 7 (a particular state, a
# particular value, a particular size / a particular allocation), it calls a process (sub_82815E00, sub_82815E00, a
# process, on a value (obj, a value, a particular value, a particular size, a particular size / a
# particular allocation), a particular process, a particular operation, a particular size / a
# particular allocation, a particular state / a particular result); if the type is other (a particular state, a
# particular value, a particular size / a particular allocation), it takes one path (a no-op / a return, a
# particular state, a particular result, a particular size / a particular allocation). So it's a "process (a
# dispatch, with a switch and a call: perform a dispatch (a dispatch, a particular dispatch / a
# particular value, a particular size / a particular allocation, a particular operation, a
# particular state / a
# particular result), and (in a switch, on a value (a->10, a type, a
# particular value, a
# particular state, a particular size / a
# particular allocation), a switch on a type) take a path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) call
# a process (sub_82815CB0, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82188B88, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82815E00, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result))" routine (a dispatch process, part of the 0x82815 cluster, a sibling of
# AtomicDecrement3_82375F50 and ProcessDispatch_82BCD7B0, a caller of sub_82815CB0, sub_82188B88, and
# sub_82815E00). 29 lines, 3 callees (sub_82188B88, sub_82815CB0, sub_82815E00). No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (obj, r3, a value, a particular value, a particular size / a
# particular allocation, a
# particular structure, a dispatch object, a
# particular value /
# a particular state) and the value's meaning (a, r4, ->10 = a type, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation, a
# dispatch object) aren't resolvable, so the name states the mechanism: a dispatch process (with a
# switch and a call) (perform a dispatch (a dispatch, a particular dispatch / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a switch, on a value (a->10, a type, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation), a switch on a type) take a path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) call
# a process (sub_82815CB0, on a value, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82188B88, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82815E00, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result)). The "3" distinguishes it from ProcessDispatch_82BCD7B0 and ProcessDispatch2_82BCD9E8.
[entrypoint.functions."0x82815C28"]
name = "ProcessDispatch3_82815C28"

# A process (a lookup, with a check and a compare, the sibling of ProcessLookupCheck_82284300 / ProcessDispatch3
# _82815C28, the caller of no functions): perform a lookup (a lookup, a particular lookup / a
# particular search, a particular size / a particular allocation, a particular operation, a
# particular state / a
# particular result), and (in a loop) check a value (r9, a value, a particular value, a
# particular size / a
# particular allocation, a particular element): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == a value (r4, a value, a particular value, a
# particular size / a
# particular allocation, a
# particular key), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) trap (a trap, a particular trap / a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in paths) compare (a comparison, a particular comparison / a
# particular check, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (r11, a value, a particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (r7, a value, a particular value, a
# particular size / a
# particular allocation, a
# particular element)).

# Args: obj (r4, ->4 = a value, a particular value, a
# particular size / a
# particular allocation, a
# particular structure, a lookup object), (more). It performs a lookup (a
# lookup, a particular lookup / a
# particular search, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it checks a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == a value (r4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), it takes a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is other (a particular state, a
# particular value, a
# particular size / a
# particular allocation), it traps (a trap, a particular trap / a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result). In paths it compares (a
# comparison, a
# particular comparison / a
# particular check, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (r7, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)). So it's a "process (a lookup, with a check and a compare: perform a
# lookup (a lookup, a particular lookup / a
# particular search, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) check a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a particular element): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == a value (r4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) trap (a trap, a particular trap / a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in paths) compare (a comparison, a particular comparison / a
# particular check, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (r7, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element))" routine (a lookup process, with a
# check and a
# compare, part
# of the 0x82A0F cluster, a sibling of ProcessLookupCheck_82284300 and ProcessDispatch3_82815C28). 167
# lines, 0 callees. 1 caller: ReassignBlock_82A0F6B8 (a process, which
# calls this to perform the lookup, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r4, ->4 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular structure, a lookup object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a lookup process (with a
# check and a
# compare) (perform a
# lookup (a lookup, a particular lookup / a
# particular search, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) check a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a particular element): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is == a value (r4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) trap (a trap, a particular trap / a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in paths) compare (a comparison, a particular comparison / a
# particular check, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (r7, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)). The "2" distinguishes it from ProcessLookupCheck_82284300 and ProcessLookup_82B5E330.
[entrypoint.functions."0x82A0FB80"]
name = "ProcessLookup2_82A0FB80"

# A process (a count, with a check and a call, the caller of CountNonZero_822CA680 / sub_822F5150 / sub_822F6480, the
# sibling of CountNonZero_822CA680 / ProcessLookup2_82A0FB80): perform a count (a count, a particular count / a
# particular number, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling CountNonZero_822CA680, on a value (obj, a value, a particular value, a
# particular size / a particular allocation, a particular source), and a value (2048, a constant, a
# particular value, a particular size / a particular allocation, a particular count), a particular count, a
# particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and (if a count (r29, a count, a particular count, a particular size / a
# particular allocation, a
# particular number) is < 40, a particular threshold, a
# particular count) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) call a process (sub_822F5150, on a value (stack[96], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (obj, a
# value, a particular value, a
# particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_822F6480, on a
# value (stack[96], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (stack[80], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (in paths) call a process (sub_822F20F0, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_822F5208, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result).

# Args: obj (r3, r31, a value, a particular value, a particular size / a
# particular allocation, a
# particular structure, a count object), (more). It checks a value (r31, obj, a
# value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it performs a count (a
# count, a particular count / a
# particular number, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling CountNonZero_822CA680, sub_822CA680, a count, on a
# value (obj, a value, a particular value, a
# particular size / a
# particular allocation, a
# particular source), and a value (2048, a constant, a
# particular value, a particular size / a
# particular allocation, a
# particular count), a particular count, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). It checks a count (r29, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number): if it is < 40 (a particular threshold, a
# particular count, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is >= 40 (a particular threshold, a
# particular count, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (sub_822F5150, sub_822F5150, a
# process, on a
# value (stack[96], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (obj, a
# value, a particular value, a
# particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_822F6480, sub_822F6480, a
# process, on a
# value (stack[96], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (stack[80], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result). In paths it calls a process (sub_822F20F0, sub_822F20F0, a
# process, on a
# value (obj, a value, a particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_822F5208, sub_822F5208, a
# process, on a
# value (obj, a value, a particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result). So it's a "process (a count, with a check and a call: perform a
# count (a
# count, a particular count / a
# particular number, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling CountNonZero_822CA680, on a value (obj, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular source), and a value (2048, a constant, a
# particular value, a particular size / a
# particular allocation, a
# particular count), a particular count, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (if a count (r29, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number) is < 40, a particular threshold, a
# particular count) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) call a process (sub_822F5150, on a value (stack[96], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (obj, a
# value, a particular value, a
# particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_822F6480, on a
# value (stack[96], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (stack[80], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (in paths) call a process (sub_822F20F0, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_822F5208, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result))" routine (a count process, part of the 0x822CA cluster, a sibling of
# CountNonZero_822CA680, a caller of CountNonZero_822CA680, sub_822F5150, and sub_822F6480). 151
# lines, 5 callees (CountNonZero_822CA680, sub_822F20F0, sub_822F5150, sub_822F5208, sub_822F6480). 3
# callers: LazyGetData_822F2088, sub_82C66CC8, sub_82B4B2B8 (processes, which
# call this to perform the count, a
# particular count / a
# particular number, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular structure, a count object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a count process (with a
# check and a
# call) (perform a
# count (a
# count, a particular count / a
# particular number, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling CountNonZero_822CA680, on a value (obj, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular source), and a value (2048, a constant, a
# particular value, a particular size / a
# particular allocation, a
# particular count), a particular count, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (if a count (r29, a count, a
# particular count, a particular size / a
# particular allocation, a
# particular number) is < 40, a particular threshold, a
# particular count) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) call a process (sub_822F5150, on a value (stack[96], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (obj, a
# value, a particular value, a
# particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_822F6480, on a
# value (stack[96], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular target), and a value (stack[80], a
# value, a
# particular value, a particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (in paths) call a process (sub_822F20F0, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_822F5208, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result)). The "CountDispatch" refers to the count (a
# count, a particular count / a
# particular number, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result) and the dispatch (a
# dispatch, a
# particular dispatch / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling sub_822F5150, a
# process, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result, and sub_822F6480, a
# process, a
# particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result).
[entrypoint.functions."0x822CA580"]
name = "ProcessCountDispatch_822CA580"

# An atomic increment (with a check and a compare, the sibling of AtomicIncrement_822CBEB8 / AtomicIncrement2_8237
# 5F28 / AtomicIncrement3_821F7798 / AtomicIncrement4_82B82440, the caller of Allocate_OrDbgBreak_8221F388 /
# ReassignBuffer_822088B0 / Release_RefCounted_821C67D8): perform an atomic increment (an atomic increment, a
# particular increment / a particular count, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (if a value (obj->0, a value, a particular value, a particular size /
# a particular allocation) is zero, a null state, a particular state) take one path (a no-op / a return, a
# particular state, a particular result, a particular size / a particular allocation), and (otherwise) compute a
# base (obj->0 + 12, a base, a particular value, a particular size / a particular allocation, a reference count,
# a particular element), and (in a loop) increment a reference count (a reference count, a particular count, a
# particular size / a particular allocation, a particular value), and (if a reference count (r11, a reference
# count, a particular count, a particular size / a particular allocation, a particular value) is == 2, a
# particular value, a particular state) call a process (Allocate_OrDbgBreak_8221F388, on a value, a
# particular allocation, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (in paths) reassign a buffer (ReassignBuffer_822088B0, on a
# value, a particular reassign / a particular buffer, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and release a value (Release_RefCounted
# _821C67D8, on a value, a particular release, a particular count, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result).

# Args: obj (r3, r29, ->0 = a value, a particular value, a particular size / a particular allocation, a
# reference counted object), (more). It checks a value (r11, obj->0, a value, a particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it computes a base (r11, obj->0 + 12, a
# base, a particular value, a particular size / a
# particular allocation, a reference count,
# a particular element). In the loop, it performs an atomic increment (an atomic increment, a
# particular increment / a
# particular count, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result: it reads a reference count (r10, r11->0, a reference count, a
# particular count, a
# particular size / a
# particular allocation, a
# particular value), and stores it (r10 + 1, an atomic increment, a
# particular count, a
# particular size / a
# particular allocation, a
# particular value)). It checks a reference count (r11, a reference count, a
# particular count, a
# particular size / a
# particular allocation, a
# particular value): if it is == 2 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (Allocate_OrDbgBreak_8221F388, sub_8221F388, an
# allocation, on a value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular allocation, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result). In paths it reassigns a buffer (ReassignBuffer_822088B0, sub_822088B0, a
# reassign, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular reassign / a
# particular buffer, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and releases a value (Release_RefCounted_821C67D8, sub_821C67D8, a
# release, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular release, a
# particular count, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result). So it's an "atomic increment (with a check and a compare: perform an
# atomic increment (an atomic increment, a
# particular increment / a
# particular count, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a value (obj->0, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# null state, a particular state) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) compute a
# base (obj->0 + 12, a base, a particular value, a
# particular size / a
# particular allocation, a reference count,
# a particular element), and (in a loop) increment a reference count (a reference count, a
# particular count, a
# particular size / a
# particular allocation, a particular value), and (if a reference count (r11, a reference
# count, a particular count, a particular size / a
# particular allocation, a particular value) is == 2, a
# particular value, a particular state) call a process (Allocate_OrDbgBreak_8221F388, on a value, a
# particular allocation, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (in paths) reassign a buffer (ReassignBuffer_822088B0, on a
# value, a particular reassign / a particular buffer, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and release a value (Release_RefCounted
# _821C67D8, on a value, a particular release, a particular count, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result))" routine (an atomic
# increment, with a
# check and a
# compare, part
# of the 0x8222E cluster, a sibling of AtomicIncrement_822CBEB8, AtomicIncrement2_82375F28,
# AtomicIncrement3_821F7798, and AtomicIncrement4_82B82440, a caller of Allocate_OrDbgBreak_8221F388,
# ReassignBuffer_822088B0, and Release_RefCounted_821C67D8). 195 lines, 3 callees (Allocate_OrDbgBreak_8221F388,
# ReassignBuffer_822088B0, Release_RefCounted_821C67D8). 10+ callers: sub_82B3B468, sub_821DA550, sub_82B3A230, sub
# _82B3A1B8, sub_821EE858, sub_82B40708, sub_823F8A38, sub_82197488, sub_821EEA00, sub_821EC668 (processes, which
# call this to perform the atomic increment, a
# particular count / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r29, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# reference counted object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: an atomic increment (with a
# check and a
# compare) (perform an
# atomic increment (an atomic increment, a
# particular increment / a
# particular count, a
# particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a value (obj->0, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# null state, a particular state) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) compute a
# base (obj->0 + 12, a base, a particular value, a
# particular size / a
# particular allocation, a reference count,
# a particular element), and (in a loop) increment a reference count (a reference count, a
# particular count, a
# particular size / a
# particular allocation, a particular value), and (if a reference count (r11, a reference
# count, a particular count, a particular size / a
# particular allocation, a particular value) is == 2, a
# particular value, a particular state) call a process (Allocate_OrDbgBreak_8221F388, on a value, a
# particular allocation, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (in paths) reassign a buffer (ReassignBuffer_822088B0, on a
# value, a particular reassign / a particular buffer, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result), and release a value (Release_RefCounted
# _821C67D8, on a value, a particular release, a particular count, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result)). The "5" distinguishes
# it from AtomicIncrement_822CBEB8, AtomicIncrement2_82375F28, AtomicIncrement3_821F7798, and
# AtomicIncrement4_82B82440.
[entrypoint.functions."0x8222E840"]
name = "AtomicIncrement5_8222E840"

# A process (a float range, with a loop and a compute, the caller of ProcessFloat_83234418, the sibling of Process
# DispatchValue2_832351B0 / ProcessFloatDataDispatch_8323EB40): perform a float range (a float range, a
# particular range / a particular float, a particular size / a particular allocation, a particular operation, a
# particular state / a particular result), and (in a loop) compute a float (a float, a particular float / a
# particular value, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result, calling ProcessFloat_83234418, on a value, a particular float / a particular compute, a
# particular size / a particular allocation, a particular operation, a particular state / a particular result), and
# (if a count (a->8 - a->4, a count, an 8-byte element, a particular count, a particular size / a
# particular allocation) is > 0, a particular value) continue the loop (a loop, a particular loop / a
# particular iteration, a particular size / a particular allocation, a particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r30, a value, a particular value, a particular size / a
# particular allocation, a
# particular structure, a range object), a (r4, r29, ->4 = a base, ->8 = a limit, a
# particular value, a
# particular size / a
# particular allocation), b (r5, r27, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It performs a float range (a
# float range, a
# particular range / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result). In the loop, it computes a float (a
# float, a
# particular float / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling ProcessFloat_83234418, sub_83234418, a float, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular float / a
# particular compute, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result). It checks a count (r28, a->8 - a->4, a
# count, an 8-byte element, a particular count, a
# particular size / a
# particular allocation): if it is > 0 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it continues the loop (a
# loop, a particular loop / a
# particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result); if it is <= 0 (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a float range, with a loop and a compute: perform a
# float range (a
# float range, a
# particular range / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a loop) compute a float (a
# float, a
# particular float / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling ProcessFloat_83234418, on a value, a
# particular float / a
# particular compute, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (if a count (a->8 - a->4, a
# count, an 8-byte element, a particular count, a particular size / a
# particular allocation) is > 0, a particular value) continue the loop (a loop, a particular loop / a
# particular iteration, a particular size / a particular allocation, a particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a float range process, part of the 0x82A13 cluster, a sibling of
# ProcessDispatchValue2_832351B0, a caller of ProcessFloat_83234418). 377 lines, 1
# callee (ProcessFloat_83234418). 1 caller: ProcessDispatchValue2_832351B0 (a
# dispatch process, which
# calls this to perform the float range, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r30, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular structure, a range object, a
# particular value /
# a particular state) and the value's meaning (a, r29, ->4 = a base, ->8 = a limit, a
# particular value, a
# particular size / a
# particular allocation, b, r27, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a float range process (with a
# loop and a
# compute) (perform a
# float range (a
# float range, a
# particular range / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a loop) compute a float (a
# float, a
# particular float / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, calling ProcessFloat_83234418, on a value, a
# particular float / a
# particular compute, a
# particular size / a
# particular allocation, a particular operation, a particular state / a
# particular result), and (if a count (a->8 - a->4, a
# count, an 8-byte element, a particular count, a particular size / a
# particular allocation) is > 0, a particular value) continue the loop (a loop, a particular loop / a
# particular iteration, a particular size / a particular allocation, a particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation)). The "FloatRange" refers to the float range (a
# float range, a
# particular range / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result) and the loop (a
# loop, a particular loop / a
# particular iteration, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result).
[entrypoint.functions."0x82A13220"]
name = "ProcessFloatRange_82A13220"

# A 32-byte constructor (the sibling of Construct32Byte_82B722E8 / Construct32Byte2_83221998 / Construct32Byte3_82
# BE49F0, with a constant and a complex computation, the caller of Construct32Byte3_82BE49F0 / CopyRecords_82A13CA
# 0 / ProcessCopyRecords_82A13D30 / ProcessGrowAppend_82BE4AA8): initialize the object's fields (a 32-byte object,
# a particular structure, a particular size / a particular allocation, obj->0 = a constant, from a fixed .data
# address, a class id / a marker, a particular class / a particular marker, obj->8 = 0, a field, a count / a size,
# a zero, obj->12 = 0, a field, a count / a size, a zero, obj->16 = 0, a field, a count / a size, a zero, obj->32 =
# 0, a field, a count / a size, a zero, obj->36 = 1, a field, a value, a one, a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation, a particular initialization), and (in paths) construct (Construct32Byte3_82BE49F0, on a
# value, a particular construct / a particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and copy records (CopyRecords_82A13CA0, on a value, a
# particular copy / a particular record, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and copy records (ProcessCopyRecords_82A13D30, on a
# value, a particular copy / a particular record, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and grow/append (ProcessGrowAppend_82BE4AA8, on a
# value, a particular grow / a particular append, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->0 = a field, a constant, a class id / a marker, ->8 = a field, a
# count / a size, ->12 = a field, a count / a size, ->16 = a field, a count / a size, ->32 = a field, a
# count / a size, ->36 = a field, a value, a 32-byte object), a (r4, r25, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It initializes the object's fields (a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation: obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, obj->8 = 0, a field, a count / a size, a zero, obj->12 = 0, a
# field, a count / a size, a zero, obj->16 = 0, a
# field, a count / a size, a zero, obj->32 = 0, a
# field, a count / a size, a zero, obj->36 = 1, a
# field, a value, a one, a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation, a
# particular initialization). In paths it constructs (Construct32Byte3_82BE49F0, sub_82BE49F0, a
# 32-byte constructor, on a value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular construct / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), copies records (CopyRecords_82A13CA0, sub_82A13CA0, a copy, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular copy / a
# particular record, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), copies records (ProcessCopyRecords_82A13D30, sub_82A13D30, a
# copy, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular copy / a
# particular record, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and grows/appends (ProcessGrowAppend_82BE4AA8, sub_82BE4AA8, a
# grow/append, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular grow / a
# particular append, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "construct a 32-byte object (initialize the object's fields (a
# 32-byte object,
# a
# particular structure, a
# particular size / a
# particular allocation, obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, obj->8 = 0, a field, a count / a size, a zero, obj->12 = 0, a
# field, a count / a size, a zero, obj->16 = 0, a
# field, a count / a size, a zero, obj->32 = 0, a
# field, a count / a size, a zero, obj->36 = 1, a
# field, a value, a one, a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation), and (in paths) construct (Construct32Byte3_82BE49F0, on a
# value, a particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and copy records (CopyRecords_82A13CA0, on a value, a
# particular copy / a particular record, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and copy records (ProcessCopyRecords_82A13D30, on a
# value, a particular copy / a particular record, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and grow/append (ProcessGrowAppend_82BE4AA8, on a
# value, a particular grow / a particular append, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a 32-byte constructor, with a
# constant and a
# complex computation, part
# of the 0x8323F cluster, a sibling of Construct32Byte_82B722E8, Construct32Byte2_83221998, and
# Construct32Byte3_82BE49F0, a caller of Construct32Byte3_82BE49F0, CopyRecords_82A13CA0,
# ProcessCopyRecords_82A13D30, and ProcessGrowAppend_82BE4AA8). 526 lines, 4 callees (Construct32Byte3_82BE49F0,
# CopyRecords_82A13CA0, ProcessCopyRecords_82A13D30, ProcessGrowAppend_82BE4AA8). 1 caller: ProcessGrowAppend8
# _83221378 (a grow/append process, which
# calls this to construct a 32-byte object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, ->0 = a field, a constant, a class id / a
# marker, ->8 = a field, a
# count / a size, ->12 = a field, a count / a size, ->16 = a field, a count / a size, ->32 = a field, a
# count / a size, ->36 = a field, a value, a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation, a
# particular value /
# a particular state) and the value's meaning (a, r25, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a 32-byte constructor (with a
# constant and a
# complex computation) (initialize the object's fields (a
# 32-byte object,
# a
# particular structure, a
# particular size / a
# particular allocation, obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, obj->8 = 0, a field, a count / a size, a zero, obj->12 = 0, a
# field, a count / a size, a zero, obj->16 = 0, a
# field, a count / a size, a zero, obj->32 = 0, a
# field, a count / a size, a zero, obj->36 = 1, a
# field, a value, a one, a 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation), and (in paths) construct (Construct32Byte3_82BE49F0, on a
# value, a particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and copy records (CopyRecords_82A13CA0, on a value, a
# particular copy / a particular record, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and copy records (ProcessCopyRecords_82A13D30, on a
# value, a particular copy / a particular record, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and grow/append (ProcessGrowAppend_82BE4AA8, on a
# value, a particular grow / a particular append, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "4" distinguishes it from Construct32Byte_82B722E8, Construct32Byte2_83221998, and
# Construct32Byte3_82BE49F0.
[entrypoint.functions."0x8323FF48"]
name = "Construct32Byte4_8323FF48"

# A trampoline (a trampoline, with a call, the caller of sub_822D7068, the sibling of ProcessElementAccess3_8217AA
# 20 / ProcessVcallLoop_8217A9A8): forward a call (a trampoline, a particular forward / a
# particular call, a particular size / a particular allocation, a particular operation, a
# particular state / a
# particular result, calling sub_822D7068, on a value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), and a value (b, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and return the result (a
# result, a
# particular result, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: a (r3, a value, a particular value, a particular size / a
# particular allocation, a
# particular target), b (r4, a value, a particular value, a
# particular size / a
# particular allocation, a
# particular source), (more). It forwards a call (a
# trampoline, a particular forward / a
# particular call, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling sub_822D7068, sub_822D7068, a process, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), and a value (b, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result). It returns the result (a
# result, a
# particular result, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "trampoline (a
# trampoline, with a
# call: forward a call (a
# trampoline, a particular forward / a
# particular call, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling sub_822D7068, on a value (a, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), and a value (b, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and return the result (a
# result, a
# particular result, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a trampoline, part of the 0x8217A cluster, a sibling of
# ProcessElementAccess3_8217AA20 and ProcessVcallLoop_8217A9A8, a caller of sub_822D7068). 9
# lines, 1
# callee (sub_822D7068). 5 callers: sub_822F21B8, sub_8217AB58, sub_82BFD228, sub_82B3EB30, sub_82C68B50
# (processes, which
# call this to forward the call, a
# particular size / a
# particular allocation, a
# particular operation). The values' meanings (a, r3, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target, b, r4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular source) aren't resolvable, so the name states the mechanism: a trampoline (with a
# call) (forward a call (a
# trampoline, a particular forward / a
# particular call, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling sub_822D7068, on a value (a, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), and a value (b, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular source), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and return the result (a
# result, a
# particular result, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "Trampoline" refers to the trampoline (a
# trampoline, a particular forward / a
# particular call, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).
[entrypoint.functions."0x8217A978"]
name = "Trampoline_8217A978"

# A process (a set, with a check and a call, the caller of sub_82CAB770 / sub_82CAB630, the sibling of Trampoline
# _8217A978): perform a set (a set, a particular set / a particular value, a particular size / a particular
# allocation, a particular operation, a particular state / a particular result), and (if a value (obj, a value, a
# particular value, a particular size / a particular allocation) is zero, a null state, a particular state) take
# one path (a no-op / a return, a particular state, a particular result, a particular size / a
# particular allocation), and (if a value (b, a value, a particular value, a particular size / a
# particular allocation) is zero, a null state, a particular state) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (if a value (a, a value, a particular value, a
# particular size / a
# particular allocation) is zero, a null state, a particular state) call a process (sub_82CAB770, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and set a flag (r3->0 = 22, a flag, a particular value, a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and call a process (sub_82CAB630, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r11, ->0 = a value, a particular value, a particular size / a
# particular allocation, a
# particular structure, a set object), a (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r5, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It checks a value (r11, obj, a
# value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it checks a value (r5, b, a
# value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it sets a value (obj->0 = 0, a store, a zero, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). It checks a value (r4, a, a
# value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (sub_82CAB770, sub_82CAB770, a
# process, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and sets a flag (r3->0 = 22, a flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and calls a process (sub_82CAB630, sub_82CAB630, a
# process, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a set, with a check and a call: perform a
# set (a set, a particular set / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a value (obj, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a null state, a particular state) take
# one path (a no-op / a return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (if a value (b, a value, a
# particular value, a particular size / a
# particular allocation) is zero, a null state, a particular state) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (if a value (a, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a null state, a particular state) call a process (sub_82CAB770, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and set a flag (r3->0 = 22, a flag, a particular value, a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and call a process (sub_82CAB630, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a set process, part of the 0x822D6 cluster, a sibling of
# Trampoline_8217A978, a caller of sub_82CAB770 and sub_82CAB630). 122 lines, 2 callees (sub_82CAB630,
# sub_82CAB770). 2 callers: sub_822D7068, sub_82CA3970 (processes, which
# call this to perform the set, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r11, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular structure, a set object, a
# particular value /
# a particular state) and the value's meaning (a, r4, a value, a
# particular value, a
# particular size / a
# particular allocation, b, r5, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a set process (with a
# check and a
# call) (perform a
# set (a set, a particular set / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a value (obj, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a null state, a particular state) take
# one path (a no-op / a return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (if a value (b, a value, a
# particular value, a particular size / a
# particular allocation) is zero, a null state, a particular state) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation), and (if a value (a, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a null state, a particular state) call a process (sub_82CAB770, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and set a flag (r3->0 = 22, a flag, a particular value, a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and call a process (sub_82CAB630, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and (otherwise) take one path (a no-op / a
# return, a particular state, a
# particular result, a particular size / a
# particular allocation)). The "SetError" refers to the set (a
# set, a particular set / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result) and the error (a
# set, a particular set / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, setting a flag (r3->0 = 22, a flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)).
[entrypoint.functions."0x822D6F90"]
name = "ProcessSetError_822D6F90"

# A process (a set, with a check and a call, the caller of ProcessSetError_822D6F90 / sub_82CAB770 / sub_82CAB630, the
# sibling of ProcessSetError_822D6F90 / Trampoline_8217A978): perform a set (a set, a particular set / a
# particular value, a particular size / a particular allocation, a particular operation, a particular state / a
# particular result), and (if a value (a, a value, a particular value, a particular size / a
# particular allocation) is zero, a null state, a particular state) check a value (b, a value, a
# particular value, a particular size / a particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) check a value (b, a value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) call a process (ProcessSetError_822D6F90, on a
# value, a particular set / a
# particular error, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and a process (sub_82CAB770, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82CAB630, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result).

# Args: obj (r3, r27, a value, a particular value, a particular size / a
# particular allocation, a
# particular structure, a set object), a (r4, r31, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r5, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r6, r29, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It performs a set (a
# set, a particular set / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result). It checks a value (r31, a, a
# value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it checks a value (r30, b, a
# value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it checks a value (r30, b, a
# value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). In paths it calls a process (ProcessSetError_822D6F90, sub_822D6F90, a
# set process, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular set / a
# particular error, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and a process (sub_82CAB770, sub_82CAB770, a
# process, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82CAB630, sub_82CAB630, a
# process, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result). So it's a "process (a set, with a check and a call: perform a
# set (a set, a particular set / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a value (a, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a null state, a particular state) check a value (b, a value, a
# particular value, a particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) check a value (b, a value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) call a process (ProcessSetError_822D6F90, on a
# value, a particular set / a
# particular error, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and a process (sub_82CAB770, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82CAB630, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result))" routine (a set process, part of the 0x822D7 cluster, a sibling of
# ProcessSetError_822D6F90, a caller of ProcessSetError_822D6F90, sub_82CAB770, and sub_82CAB630). 184
# lines, 3 callees (sub_822D6F90, sub_82CAB630, sub_82CAB770). 1 caller: Trampoline_8217A978 (a
# trampoline, which
# calls this to perform the set, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r27, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular structure, a set object, a
# particular value /
# a particular state) and the value's meaning (a, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, b, r30, a value, a
# particular value, a
# particular size / a
# particular allocation, c, r29, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a set process (with a
# check and a
# call) (perform a
# set (a set, a particular set / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (if a value (a, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a null state, a particular state) check a value (b, a value, a
# particular value, a particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) check a value (b, a value, a
# particular value, a
# particular size / a
# particular allocation): if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), take a path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) call a process (ProcessSetError_822D6F90, on a
# value, a particular set / a
# particular error, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and a process (sub_82CAB770, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result), and a process (sub_82CAB630, on a
# value, a particular process, a
# particular operation, a particular size / a
# particular allocation, a particular state / a
# particular result)). The "2" distinguishes it from ProcessSetError_822D6F90.
[entrypoint.functions."0x822D7068"]
name = "ProcessSetError2_822D7068"

# A process (a sort compare, with a swap and a loop, the sibling of ProcessSetError2_822D7068 / ProcessSort_82A0A890):
# perform a sort compare (a sort compare, a particular compare / a particular ordering, a particular size / a
# particular allocation, a particular operation, a particular state / a particular result), and (in a loop) compare
# (a comparison, a particular comparison / a particular ordering, a particular size / a particular allocation, a
# particular operation, a particular state / a particular result, a value (a->0, a value, a particular value, a
# particular size / a particular allocation, a particular element) and a value (b->0, a value, a particular value, a
# particular size / a particular allocation, a particular element)), and (if a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key) is > a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), a particular key) swap (a swap, a particular swap / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) compare (a comparison, a
# particular comparison / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (c->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)).

# Args: a (r3, ->0 = a value, a particular value, a particular size / a
# particular allocation, a
# particular structure, a compare object), b (r4, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation), c (r5, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It performs a sort compare (a
# sort compare, a
# particular compare / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result). In the loop, it compares (a
# comparison, a
# particular comparison / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element): if a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key) is > a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), it swaps (a
# swap, a
# particular swap / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)). If a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key) is == a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), it checks a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key): if it is >= a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is < the value (a particular state, a
# particular value, a
# particular size / a
# particular allocation), it swaps (a
# swap, a
# particular swap / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)). In paths it compares (a
# comparison, a
# particular comparison / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (c->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)). So it's a "process (a sort compare, with a swap and a loop: perform a
# sort compare (a
# sort compare, a
# particular compare / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a loop) compare
# (a comparison, a particular comparison / a particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)), and (if a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key) is > a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), a particular key) swap (a swap, a particular swap / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) compare (a comparison, a
# particular comparison / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (c->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element))" routine (a sort compare process, part of the 0x82C22 cluster, a sibling of
# ProcessSetError2_822D7068 and ProcessSort_82A0A890). 89
# lines, 0 callees. 1 caller: sub_82C222F0 (a process, which
# calls this to perform the sort compare, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular structure, a compare object, a
# particular value /
# a particular state) and the value's meaning (a, r3, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, b, r4, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, c, r5, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a sort compare process (with a
# swap and a
# loop) (perform a
# sort compare (a
# sort compare, a
# particular compare / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a loop) compare
# (a comparison, a particular comparison / a particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)), and (if a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key) is > a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular key), a particular key) swap (a swap, a particular swap / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (in paths) compare (a comparison, a
# particular comparison / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result, a value (b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element) and a value (c->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular element)). The "SortCompare" refers to the sort compare (a
# sort compare, a
# particular compare / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result) and the swap (a
# swap, a
# particular swap / a
# particular ordering, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result).
[entrypoint.functions."0x82C22148"]
name = "ProcessSortCompare_82C22148"

# A lookup (a lookup, with a compute and a mask, the sibling of ProcessSortCompare_82C22148 / ProcessLookup2_82A0
# FB80): perform a lookup (a lookup, a particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a compute) compute an offset (an offset, a
# particular offset / a
# particular index, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, (index << 1) & 0x1FE, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a mask) mask a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a
# 2-byte element, a
# particular element, & 0xFFFF8000, a
# mask, a
# particular mask / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and return the value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# 2-byte element, a
# particular element, a
# particular state / a
# particular result).

# Args: index (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular index), (more). It performs a lookup (a
# lookup, a particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result). In the compute, it computes an offset (r10, (r3 << 1) & 0x1FE, an
# offset, a
# particular offset / a
# particular index, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). It masks a value (r11, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# 2-byte element, a
# particular element, & 0xFFFF8000, a
# mask, a
# particular mask / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). It returns the value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# 2-byte element, a
# particular element, a
# particular state / a
# particular result). So it's a "lookup (a
# lookup, with a
# compute and a
# mask: perform a lookup (a
# lookup, a particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a compute) compute an offset (an offset, a
# particular offset / a
# particular index, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, (index << 1) & 0x1FE, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a mask) mask a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a
# 2-byte element, a
# particular element, & 0xFFFF8000, a
# mask, a
# particular mask / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and return the value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# 2-byte element, a
# particular element, a
# particular state / a
# particular result))" routine (a lookup routine, part of the 0x8223F cluster, a sibling of
# ProcessSortCompare_82C22148 and ProcessLookup2_82A0FB80). 18
# lines, 0 callees. 9 callers: sub_82CB7B90, sub_82CAE4A8, sub_82CB7688, sub_830043D0, BuildBuffer_8223F990, sub
# _82CACCC0, sub_82CB3260, sub_82CB2038, sub_82CB1438 (processes, which
# call this to perform the lookup, a
# particular size / a
# particular allocation, a
# particular operation). The value's meaning (index, r3, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular index) isn't resolvable, so the name states the mechanism: a lookup (with a
# compute and a
# mask) (perform a lookup (a
# lookup, a particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result), and (in a compute) compute an offset (an offset, a
# particular offset / a
# particular index, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, (index << 1) & 0x1FE, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a mask) mask a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a
# 2-byte element, a
# particular element, & 0xFFFF8000, a
# mask, a
# particular mask / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and return the value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# 2-byte element, a
# particular element, a
# particular state / a
# particular result)). The "Masked" refers to the lookup (a
# lookup, a particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a particular state / a
# particular result) and the mask (a
# mask, a
# particular mask / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).
[entrypoint.functions."0x8223F970"]
name = "LookupMasked_8223F970"

# A destruct (a destruct, with a check and a free, the caller of Destruct20Byte_832156E0 / Free_SizeBucketed_Thunk
# _8221BE68, the sibling of Destruct20ByteFree_82A11F90): destruct a 20-byte object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Destruct20Byte_832156E0, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular destruct / a
# particular release, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (if a flag (a & 0x1, a flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->0 = a value, a particular value, a particular size / a
# particular allocation, a
# particular structure, a destruct object), a (r4, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It destructs a 20-byte object (a
# destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Destruct20Byte_832156E0, sub_832156E0, a destruct, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular destruct / a
# particular release, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). It checks a flag (r9, a & 0x1, a
# flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation): if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it frees a value (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a
# free, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result); if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "destruct a 20-byte object (a
# destruct, with a
# check and a
# free: destruct a 20-byte object (a
# destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Destruct20Byte_832156E0, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular destruct / a
# particular release, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (if a flag (a & 0x1, a flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a destruct routine, part of the 0x82A14 cluster, a sibling of
# Destruct20ByteFree_82A11F90, a caller of Destruct20Byte_832156E0 and Free_SizeBucketed_Thunk_8221BE68). 56
# lines, 2 callees (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (obj, r31, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular structure, a destruct object, a
# particular value /
# a particular state) and the value's meaning (a, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a destruct (with a
# check and a
# free) (destruct a 20-byte object (a
# destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Destruct20Byte_832156E0, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular destruct / a
# particular release, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (if a flag (a & 0x1, a flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "2" distinguishes it from Destruct20ByteFree_82A11F90.
[entrypoint.functions."0x82A14A20"]
name = "Destruct20ByteFree2_82A14A20"

# A 20-byte constructor (the sibling of Construct20Byte_82A11180 / Construct20Byte2_832158F8, with a copy and a check,
# the caller of CopyStructData2_82A1BD30 / sub_83211D48): construct a 20-byte object (a construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy struct data (a copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling CopyStructData2_82A1BD30, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), a particular copy / a
# particular struct, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->16 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->20 = r9, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a flag (b & 0xFF, a flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_83211D48, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->16 = a value, a particular value, a particular size / a
# particular allocation, ->20 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# 20-byte object), a (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r5, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It constructs a 20-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the copy, it copies struct data (a
# copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling CopyStructData2_82A1BD30, sub_82A1BD30, a copy, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), a particular copy / a
# particular struct, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). It stores values (obj->16 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->20 = r9, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). It checks a flag (r11, b & 0xFF, a
# flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation): if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (sub_83211D48, sub_83211D48, a
# process, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result); if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "construct a 20-byte object (a
# construct, with a
# copy and a
# check: construct a 20-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy struct data (a copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling CopyStructData2_82A1BD30, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), a particular copy / a
# particular struct, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->16 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->20 = r9, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a flag (b & 0xFF, a flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_83211D48, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a 20-byte constructor, with a
# copy and a
# check, part
# of the 0x83212 cluster, a sibling of Construct20Byte_82A11180 and Construct20Byte2_832158F8, a caller of
# CopyStructData2_82A1BD30 and sub_83211D48). 58 lines, 2 callees (CopyStructData2_82A1BD30, sub_83211D48). No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (obj, ->16 = a value, a particular value, a particular size / a
# particular allocation, ->20 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# 20-byte object, a
# particular structure, a
# particular size / a
# particular allocation, a
# particular value /
# a particular state) and the value's meaning (a, r4, a value, a
# particular value, a
# particular size / a
# particular allocation, b, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a 20-byte constructor (with a
# copy and a
# check) (construct a 20-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy struct data (a copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling CopyStructData2_82A1BD30, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular target), a particular copy / a
# particular struct, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->16 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->20 = r9, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a flag (b & 0xFF, a flag, a
# particular value, a
# particular state, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_83211D48, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "3" distinguishes it from Construct20Byte_82A11180 and Construct20Byte2_832158F8.
[entrypoint.functions."0x83212058"]
name = "Construct20Byte3_83212058"

# A 20-byte constructor (the sibling of Construct20Byte_82A11180 / Construct20Byte2_832158F8 / Construct20Byte3_83
# 212058, with a constant and a copy, the caller of Construct20Byte_83215640): construct a 20-byte object (a
# construct, a particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a constant (a copy, a
# particular copy / a
# particular constant, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Construct20Byte_83215640, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular construct / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->16 = a constant, from a fixed .data address, a float, a
# particular value, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->20 = a constant, from a fixed .data address, a float, a
# particular value, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->0 = a field, a constant, a class id / a marker, ->16 = a field, a
# float, ->20 = a field, a float, a
# 20-byte object), (more). It constructs a 20-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the copy, it copies a constant (a
# copy, a
# particular copy / a
# particular constant, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Construct20Byte_83215640, sub_83215640, a construct, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular construct / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). It stores values (obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->16 = a constant, from a fixed .data address, a float, a
# particular value, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->20 = a constant, from a fixed .data address, a float, a
# particular value, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "construct a 20-byte object (a
# construct, with a
# constant and a
# copy: construct a 20-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a constant (a copy, a
# particular copy / a
# particular constant, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Construct20Byte_83215640, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular construct / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->16 = a constant, from a fixed .data address, a float, a
# particular value, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->20 = a constant, from a fixed .data address, a float, a
# particular value, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a 20-byte constructor, with a
# constant and a
# copy, part
# of the 0x83215 cluster, a sibling of Construct20Byte_82A11180, Construct20Byte2_832158F8, and
# Construct20Byte3_83212058, a caller of Construct20Byte_83215640). 51 lines, 1
# callee (Construct20Byte_83215640). 2 callers: sub_83219588, ProcessTreeNode_83218C10 (processes, which
# call this to construct a 20-byte object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, ->0 = a field, a constant, a class id / a
# marker, ->16 = a field, a
# float, ->20 = a field, a float, a
# 20-byte object, a
# particular structure, a
# particular size / a
# particular allocation, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a 20-byte constructor (with a
# constant and a
# copy) (construct a 20-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a constant (a copy, a
# particular copy / a
# particular constant, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Construct20Byte_83215640, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular construct / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->0 = a constant, from a fixed .data address, a class id / a
# marker, a particular class / a
# particular marker, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->16 = a constant, from a fixed .data address, a float, a
# particular value, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->20 = a constant, from a fixed .data address, a float, a
# particular value, a
# store, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "4" distinguishes it from Construct20Byte_82A11180, Construct20Byte2_832158F8, and
# Construct20Byte3_83212058.
[entrypoint.functions."0x83215808"]
name = "Construct20Byte4_83215808"

# A convert (a convert, with a check and a compute, the sibling of ToLower_SdkRuntime / Construct20Byte4_83215808):
# convert a character (a convert, a particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (if a character (c, a character, a
# particular value, a
# particular size / a
# particular allocation) is < 97 (a lowercase 'a', a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (if a character (c, a character, a
# particular value, a
# particular size / a
# particular allocation) is > 122 (a lowercase 'z', a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) convert to uppercase (a
# convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, c - 32, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: c (r3, a character, a
# particular value, a
# particular size / a
# particular allocation), (more). It checks a character (r3, c, a
# character, a
# particular value, a
# particular size / a
# particular allocation): if it is < 97 (a lowercase 'a', a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); if it is > 122 (a lowercase 'z', a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). If it is in the range [97, 122] (a lowercase 'a' to a lowercase 'z', a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it converts to uppercase (a
# convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, c - 32, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "convert (a
# convert, with a
# check and a
# compute: convert a character (a convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (if a character (c, a character, a
# particular value, a
# particular size / a
# particular allocation) is < 97 (a lowercase 'a', a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (if a character (c, a character, a
# particular value, a
# particular size / a
# particular allocation) is > 122 (a lowercase 'z', a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) convert to uppercase (a
# convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, c - 32, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a convert routine, part of the 0x821EE cluster, a sibling of
# ToLower_SdkRuntime and Construct20Byte4_83215808). 16
# lines, 0 callees. 10+ callers: sub_82F12280, sub_82BD2300, sub_821C0848, sub_827C3FD0, sub_82A249E8, sub
# _82C5C340, sub_822F3D20, sub_830253B8, sub_821EEA90, sub_82548870 (processes, which
# call this to convert the character, a
# particular size / a
# particular allocation, a
# particular operation). The character's meaning (c, r3, a character, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a convert (with a
# check and a
# compute) (convert a character (a convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (if a character (c, a character, a
# particular value, a
# particular size / a
# particular allocation) is < 97 (a lowercase 'a', a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (if a character (c, a character, a
# particular value, a
# particular size / a
# particular allocation) is > 122 (a lowercase 'z', a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) convert to uppercase (a
# convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, c - 32, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "_SdkRuntime" suffix keeps the guest symbol distinct from the host libc's toupper.
[entrypoint.functions."0x821EEB10"]
name = "ToUpper_SdkRuntime_821EEB10"

# A constant return (a constant return, with a compute, the sibling of ReturnConstant_0x10000_82A14660 / ToUpper
# _SdkRuntime_821EEB10): return a constant (a constant return, a particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 1024, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (more). It returns a constant (a
# constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 1024, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "constant return (a
# constant return, with a
# compute: return a constant (a constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 1024, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a constant return routine, part of the 0x82A11 cluster, a sibling of
# ReturnConstant_0x10000_82A14660 and ToUpper_SdkRuntime_821EEB10). 8
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# constant's value (1024, a
# constant, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable beyond its value, so the name states the mechanism: a constant return (with a
# compute) (return a constant (a constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 1024, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "1024" refers to the constant (1024, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).
[entrypoint.functions."0x82A11E60"]
name = "ReturnConstant_1024_82A11E60"

# A compute (a compute, with a check and a call, the sibling of ProcessClamped_83231358 / ReturnConstant_1024_82A11
# E60, the caller of ComputeClamped_83230A48 / sub_82CC0CF0): do a compute (a compute, a particular compute / a
# particular operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, stack[80], a
# particular value, a
# particular state / a
# particular result), and (if a value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_82CC0CF0, on a
# value (stack[84], a value, a
# particular value, a
# particular size / a
# particular allocation, r30, a
# particular value, 16384, a
# particular value / a
# particular size), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and compute a value (obj->32 = obj->32 - r30, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->32 = a value, a particular value, a particular size / a
# particular allocation, a
# particular object), a (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r5, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r6, r8, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a compute (a
# compute, a
# particular compute / a
# particular operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, stack[80], a
# particular value, a
# particular state / a
# particular result). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (sub_82CC0CF0, sub_82CC0CF0, a
# process, on a
# value (stack[84], a value, a
# particular value, a
# particular size / a
# particular allocation, r30, a
# particular value, 16384, a
# particular value / a
# particular size), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and computes a value (obj->32 = obj->32 - r30, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result); if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "compute (a
# compute, with a
# check and a
# call: do a compute (a compute, a
# particular compute / a
# particular operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, stack[80], a
# particular value, a
# particular state / a
# particular result), and (if a value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_82CC0CF0, on a
# value (stack[84], a value, a
# particular value, a
# particular size / a
# particular allocation, r30, a
# particular value, 16384, a
# particular value / a
# particular size), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and compute a value (obj->32 = obj->32 - r30, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a compute routine, part of the 0x83230 cluster, a sibling of
# ProcessClamped_83231358 and ReturnConstant_1024_82A11E60, a caller of ComputeClamped_83230A48 and sub_82CC0CF0). 68
# lines, 2 callees (ComputeClamped_83230A48, sub_82CC0CF0). 1 caller: sub_83231240 (a process, which calls this to do
# the compute, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->32 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r4, a value, a
# particular value, a
# particular size / a
# particular allocation, b, r5, a value, a
# particular value, a
# particular size / a
# particular allocation, c, r8, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a compute (with a
# check and a
# call) (do a compute (a compute, a
# particular compute / a
# particular operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, stack[80], a
# particular value, a
# particular state / a
# particular result), and (if a value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_82CC0CF0, on a
# value (stack[84], a value, a
# particular value, a
# particular size / a
# particular allocation, r30, a
# particular value, 16384, a
# particular value / a
# particular size), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and compute a value (obj->32 = obj->32 - r30, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "83230C88" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x83230C88"]
name = "ProcessClamped_83230C88"

# A process (a process, with a compute and a check and an insert, the sibling of ProcessClamped_83230C88 / Process
# Insert_83231358, the caller of ComputeBlockSize_829FD1C8 / List_InsertSorted_829FD158 / ProcessClamped_83230C88): do
# a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) compute a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->36 = obj->36 - a->12, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r24, a
# particular value, r8, a
# particular value, a
# particular state / a
# particular result), and (if a value (r24, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) insert a value (List_InsertSorted_829FD158, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, a->12, a
# particular value, a
# particular size / a
# particular allocation), a particular insert / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r30, ->36 = a value, a particular value, a particular size / a
# particular allocation, ->4 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object), a (r4, r29, ->12 = a value, a particular value, a particular size / a
# particular allocation, ->8 = a value, a
# particular value, a
# particular size / a
# particular allocation, ->16 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object), b (r5, r25, ->0 = a value, a particular value, a particular size / a
# particular allocation, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the compute, it computes a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->36 = obj->36 - a->12, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r24, a
# particular value, r8, a
# particular value, a
# particular state / a
# particular result). If it is < a value (r24, a
# value, a
# particular value, a
# particular size / a
# particular allocation, r8, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); otherwise, it inserts a value (List_InsertSorted_829FD158, sub_829FD158, an
# insert, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, a->12, a
# particular value, a
# particular size / a
# particular allocation), a particular insert / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# compute and a
# check and an
# insert: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) compute a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->36 = obj->36 - a->12, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r24, a
# particular value, r8, a
# particular value, a
# particular state / a
# particular result), and (if a value (r24, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) insert a value (List_InsertSorted_829FD158, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, a->12, a
# particular value, a
# particular size / a
# particular allocation), a particular insert / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x83231 cluster, a sibling of
# ProcessClamped_83230C88 and ProcessInsert_83231358, a caller of ComputeBlockSize_829FD1C8, List_InsertSorted_829FD158,
# and ProcessClamped_83230C88). 405 lines, 5
# callees (ComputeBlockSize_829FD1C8, List_InsertSorted_829FD158, sub_83230BC0, sub_83230C18, ProcessClamped_83230C88).
# 1 caller: FindSizeBucket_83231518 (a process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r30, ->36 = a value, a
# particular value, a
# particular size / a
# particular allocation, ->4 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r29, ->12 = a value, a
# particular value, a
# particular size / a
# particular allocation, ->8 = a value, a
# particular value, a
# particular size / a
# particular allocation, ->16 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, b, r25, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object) aren't resolvable, so the name states the mechanism: a process (with a
# compute and a
# check and an
# insert) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) compute a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->36 = obj->36 - a->12, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r24, a
# particular value, r8, a
# particular value, a
# particular state / a
# particular result), and (if a value (r24, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) insert a value (List_InsertSorted_829FD158, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, a->12, a
# particular value, a
# particular size / a
# particular allocation), a particular insert / a
# particular value, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "83231240" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x83231240"]
name = "ProcessInsert_83231240"

# A return address (a return address, with a constant, the sibling of ReturnPointer_82C3C648 / ProcessInsert_83231
# 240): return a pointer (a return address, a particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19932, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (more). It returns a pointer (a
# return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19932, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "return address (a
# return address, with a
# constant: return a pointer (a return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19932, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a return address routine, part of the 0x82C3B cluster, a sibling of
# ReturnPointer_82C3C648 and ProcessInsert_83231240). 10
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# pointer's meaning (r3, a pointer, a
# particular value, a
# particular size / a
# particular allocation, from a fixed .data address + 19932, a
# particular value) isn't resolvable, so the name states the mechanism: a return address (with a
# constant) (return a pointer (a return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19932, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "82C3BE78" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82C3BE78"]
name = "ReturnPointer_82C3BE78"

# A process (a process, with a vcall and a check, the sibling of ProcessVcall_82C22258 / ProcessSortCompare_82C221
# 48, the caller of VcallTriple_AllSuccess_82C011E8 / AtomicIncrement3_821F7798 / DestructRefCounted_82214F08):
# do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on b->0->20, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3 & 0xFF, a
# particular value, a
# particular state / a
# particular result), and (if a value (r3 & 0xFF, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) do another vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling VcallTriple_AllSuccess_82C011E8, on a
# value (b, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, a + 4, a
# particular value), a particular vcall / a
# particular method, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: a (r3, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, r31, ->0 = a value, a particular value, a particular size / a
# particular allocation, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the vcall, it does a vcall (a
# vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on b->0->20, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r3 & 0xFF, a
# particular value, a
# particular state / a
# particular result). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it does another vcall (a
# vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling VcallTriple_AllSuccess_82C011E8, sub_82C011E8, a vcall, on a
# value (b, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, a + 4, a
# particular value), a particular vcall / a
# particular method, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result); if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# vcall and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on b->0->20, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3 & 0xFF, a
# particular value, a
# particular state / a
# particular result), and (if a value (r3 & 0xFF, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) do another vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling VcallTriple_AllSuccess_82C011E8, on a
# value (b, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, a + 4, a
# particular value), a particular vcall / a
# particular method, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x82C21 cluster, a sibling of
# ProcessVcall_82C22258 and ProcessSortCompare_82C22148, a caller of VcallTriple_AllSuccess_82C011E8, AtomicIncrement3_821F7798,
# and DestructRefCounted_82214F08). 302 lines, 5
# callees (AtomicIncrement3_821F7798, DestructRefCounted_82214F08, ProcessVcallSequence3_82C14D70, VcallTriple_AllSuccess_82C011E8,
# sub_8228A4C0). 1 caller: sub_82C22258 (a process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (b, r31, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# vcall and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on b->0->20, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3 & 0xFF, a
# particular value, a
# particular state / a
# particular result), and (if a value (r3 & 0xFF, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) do another vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling VcallTriple_AllSuccess_82C011E8, on a
# value (b, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, a + 4, a
# particular value), a particular vcall / a
# particular method, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "82C21F38" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82C21F38"]
name = "ProcessVcall_82C21F38"

# A process (a process, with a vcall and a check, the sibling of ProcessVcall_82C21F38 / ProcessVcall3_82C223E8, the
# caller of ProcessVcall_82C21F38): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on obj->0->24, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3 & 0xFF, a
# particular value, a
# particular state / a
# particular result), and (if a value (r3 & 0xFF, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (ProcessVcall_82C21F38, on a
# value (b, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, obj, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->0 = a value, a particular value, a particular size / a
# particular allocation, a
# particular object), a (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r5, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the vcall, it does a vcall (a
# vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on obj->0->24, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r3 & 0xFF, a
# particular value, a
# particular state / a
# particular result). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (ProcessVcall_82C21F38, sub_82C21F38, a
# process, on a
# value (b, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, obj, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result); if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# vcall and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on obj->0->24, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3 & 0xFF, a
# particular value, a
# particular state / a
# particular result), and (if a value (r3 & 0xFF, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (ProcessVcall_82C21F38, on a
# value (b, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, obj, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x82C22 cluster, a sibling of
# ProcessVcall_82C21F38 and ProcessVcall3_82C223E8, a caller of ProcessVcall_82C21F38). 86 lines, 1
# callee (ProcessVcall_82C21F38). 1 caller: sub_82C223E8 (a process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->0 = a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r4, a value, a
# particular value, a
# particular size / a
# particular allocation, b, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# vcall and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on obj->0->24, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3 & 0xFF, a
# particular value, a
# particular state / a
# particular result), and (if a value (r3 & 0xFF, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (ProcessVcall_82C21F38, on a
# value (b, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, obj, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "2" distinguishes it from ProcessVcall_82C21F38.
[entrypoint.functions."0x82C22258"]
name = "ProcessVcall2_82C22258"

# A 32-byte copy constructor (the sibling of Construct24ByteCopy_821E3B38 / Construct32Byte_82B722E8, with a copy and a check,
# the caller of Allocate_OrDbgBreak_8221F388): construct a 32-byte object (a construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a 32-byte object (a copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation: obj->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = src->4, a float, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = src->8, a float, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->16 = src->16, a vector, 16 bytes, a vector load / a vector store, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (src->0, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) allocate a value (Allocate_OrDbgBreak_8221F388, 160 bytes, a size, a
# particular value, a
# particular size / a
# particular allocation, on a
# value (160, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular allocate / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->0 = a field, a value, ->4 = a field, a float, ->8 = a field, a
# float, ->16 = a field, a vector, a
# 32-byte object), src (r4, r30, ->0 = a field, a value, ->4 = a field, a float, ->8 = a
# field, a float, ->16 = a field, a vector, a
# 32-byte object), (more). It constructs a 32-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the copy, it copies a 32-byte object (a
# copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation: obj->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = src->4, a float, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = src->8, a float, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->16 = src->16, a vector, 16 bytes, a vector load / a vector store, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). It checks a value (r10, src->0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state): if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it allocates a value (Allocate_OrDbgBreak_8221F388, sub_8221F388, an
# allocate, 160 bytes, a size, a
# particular value, a
# particular size / a
# particular allocation, on a
# value (160, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular allocate / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result); if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "construct a 32-byte object (a
# construct, with a
# copy and a
# check: construct a 32-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a 32-byte object (a copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation: obj->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = src->4, a float, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = src->8, a float, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->16 = src->16, a vector, 16 bytes, a vector load / a vector store, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (src->0, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) allocate a value (Allocate_OrDbgBreak_8221F388, 160 bytes, a size, a
# particular value, a
# particular size / a
# particular allocation, on a
# value (160, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular allocate / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a 32-byte copy constructor, with a
# copy and a
# check, part
# of the 0x82A0C cluster, a sibling of Construct24ByteCopy_821E3B38 and Construct32Byte_82B722E8, a caller of
# Allocate_OrDbgBreak_8221F388). 188 lines, 1
# callee (Allocate_OrDbgBreak_8221F388). 4 callers: sub_82A0BDA0, sub_82A0B668, sub_8321F968, sub_82A0B930 (processes,
# which call this to construct the 32-byte object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->0 = a field, a value, ->4 = a field, a
# float, ->8 = a field, a
# float, ->16 = a field, a vector, a
# 32-byte object, a
# particular structure, a
# particular size / a
# particular allocation, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a 32-byte copy constructor (with a
# copy and a
# check) (construct a 32-byte object (a
# construct, a
# particular construct / a
# particular size, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a 32-byte object (a copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation: obj->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = src->4, a float, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = src->8, a float, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->16 = src->16, a vector, 16 bytes, a vector load / a vector store, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (src->0, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) allocate a value (Allocate_OrDbgBreak_8221F388, 160 bytes, a size, a
# particular value, a
# particular size / a
# particular allocation, on a
# value (160, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular allocate / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "Copy" refers to the copy (a
# copy, a
# particular copy / a
# particular struct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82A0C1B8"]
name = "Construct32ByteCopy_82A0C1B8"

# A process (a process, with a loop and a compute and a recursion, the sibling of ProcessRecursive_8229A2A8 / Process
# Recursive2_82A0A2D0, the caller of sub_82C202A0 / sub_82C20680): do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over n, a count, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a compute) compute a value (a value, a
# particular value, a
# particular size / a
# particular allocation, (src - dst) >> 2, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value ((src - dst) >> 2, a value, a
# particular value, a
# particular size / a
# particular allocation) is > 32, a
# particular value) call a process (sub_82C202A0, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, dst, a
# particular value, a
# particular size / a
# particular allocation, src, a
# particular value, a
# particular size / a
# particular allocation, a, a
# particular value), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: dst (r3, r30, a value, a
# particular value, a
# particular size / a
# particular allocation, a destination), src (r4, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, a source), n (r5, r29, a count, a
# particular value, a
# particular size / a
# particular allocation), a (r6, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over n, a count, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the compute, it computes a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, (src - dst) >> 2, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). If it is > 32 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (sub_82C202A0, sub_82C202A0, a
# process, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, dst, a
# particular value, a
# particular size / a
# particular allocation, src, a
# particular value, a
# particular size / a
# particular allocation, a, a
# particular value), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result); if it is <= 32 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# loop and a
# compute and a
# recursion: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over n, a count, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a compute) compute a value (a value, a
# particular value, a
# particular size / a
# particular allocation, (src - dst) >> 2, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value ((src - dst) >> 2, a value, a
# particular value, a
# particular size / a
# particular allocation) is > 32, a
# particular value) call a process (sub_82C202A0, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, dst, a
# particular value, a
# particular size / a
# particular allocation, src, a
# particular value, a
# particular size / a
# particular allocation, a, a
# particular value), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x82C20 cluster, a sibling of
# ProcessRecursive_8229A2A8 and ProcessRecursive2_82A0A2D0, a caller of sub_82C202A0 and sub_82C20680 (self-recursive)). 176
# lines, 5 callees (sub_82C1FE90, sub_82C202A0, sub_82C204F0, sub_82C20618, sub_82C20680 (self-recursive)). 2
# callers: sub_82C208E8, sub_82C20878 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (dst, r30, a value, a
# particular value, a
# particular size / a
# particular allocation, a destination, a
# particular value /
# a particular state) and the value's meaning (src, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, a source, n, r29, a count, a
# particular value, a
# particular size / a
# particular allocation, a, r28, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# loop and a
# compute and a
# recursion) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over n, a count, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a compute) compute a value (a value, a
# particular value, a
# particular size / a
# particular allocation, (src - dst) >> 2, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value ((src - dst) >> 2, a value, a
# particular value, a
# particular size / a
# particular allocation) is > 32, a
# particular value) call a process (sub_82C202A0, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, dst, a
# particular value, a
# particular size / a
# particular allocation, src, a
# particular value, a
# particular size / a
# particular allocation, a, a
# particular value), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "3" distinguishes it from ProcessRecursive_8229A2A8 and ProcessRecursive2_82A0A2D0.
[entrypoint.functions."0x82C20680"]
name = "ProcessRecursive3_82C20680"

# A process (a process, with an atomic increment and a check, the sibling of ProcessAtomicIncrement_821E6408 / Process
# Recursive3_82C20680, the caller of AllocateString_822079D8 / Release_RefCounted_821C67D8): do a process (a
# process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in an atomic increment) do an atomic increment (a
# process, a particular atomic increment / a
# particular counter, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a global counter, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, lwarx/stwcx, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->0, a
# particular value, a
# particular state / a
# particular result), and (if a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (Release_RefCounted_821C67D8, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->0 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), a (r4, ->0 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the atomic increment, it does an atomic increment (a
# process, a
# particular atomic increment / a
# particular counter, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a global counter, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, lwarx/stwcx, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a->0, a
# particular value, a
# particular state / a
# particular result). If it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (Release_RefCounted_821C67D8, sub_821C67D8, a
# process, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result); if it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with an
# atomic increment and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in an atomic increment) do an atomic increment (a
# process, a particular atomic increment / a
# particular counter, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a global counter, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, lwarx/stwcx, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->0, a
# particular value, a
# particular state / a
# particular result), and (if a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (Release_RefCounted_821C67D8, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x821E3 cluster, a sibling of
# ProcessAtomicIncrement_821E6408 and ProcessRecursive3_82C20680, a caller of AllocateString_822079D8 and Release_RefCounted_821C67D8). 123
# lines, 2 callees (AllocateString_822079D8, Release_RefCounted_821C67D8). 10+ callers: sub_821F03D8, sub_8285D078, sub
# _82A6DC38, sub_822F38B0, sub_82414F40, sub_828BFFF8, sub_82C02FC8, sub_82265500, sub_82281EE0, sub_821E6408
# (processes, which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->0 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r4, ->0 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object) aren't resolvable, so the name states the mechanism: a process (with an
# atomic increment and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in an atomic increment) do an atomic increment (a
# process, a
# particular atomic increment / a
# particular counter, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a global counter, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, lwarx/stwcx, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->0, a
# particular value, a
# particular state / a
# particular result), and (if a value (a->0, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (Release_RefCounted_821C67D8, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "821E3950" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x821E3950"]
name = "ProcessAtomicIncrement_821E3950"

# A process (a process, with a loop and a comparison, the sibling of ProcessLoop_82C202A0 / ProcessAtomicIncrement_821
# E3950, the caller of Process2DRegion_82C3C010): do a process (a process, a particular process, a particular size /
# a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a comparison) compare a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r9, a
# particular value, r10, a
# particular value, a
# particular state / a
# particular result), and (if a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) call a process (Process2DRegion_82C3C010, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: a (r3, r29, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the comparison, it compares a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r9, a
# particular value, r10, a
# particular value, a
# particular state / a
# particular result). If it is < a value (r9, a
# value, a
# particular value, a
# particular size / a
# particular allocation, r10, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it continues the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation); otherwise, it calls a process (Process2DRegion_82C3C010, sub_82C3C010, a
# process, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# comparison: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a comparison) compare a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r9, a
# particular value, r10, a
# particular value, a
# particular state / a
# particular result), and (if a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) call a process (Process2DRegion_82C3C010, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82C20 cluster, a sibling of
# ProcessLoop_82C202A0 and ProcessAtomicIncrement_821E3950, a caller of Process2DRegion_82C3C010). 115
# lines, 1 callee (Process2DRegion_82C3C010). 1 caller: sub_82C20680 (ProcessRecursive3_82C20680, a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r29, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular value /
# a particular state) and the value's meaning (b, r28, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# loop and a
# comparison) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a comparison) compare a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r9, a
# particular value, r10, a
# particular value, a
# particular state / a
# particular result), and (if a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) call a process (Process2DRegion_82C3C010, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "82C204F0" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82C204F0"]
name = "ProcessLoop_82C204F0"

# A construct (a construct, with a copy and a check, the sibling of ConstructString_821E6408 / ProcessLoop_82C204F0, the
# caller of StrLenUTF16_8217AB30 / ProcessElementAccess2_8217A6C0 / ProcessElementAccess3_8217AA20): construct a string (a
# construct, a particular construct / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a string (a copy, a
# particular copy / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation: obj->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->28 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->32 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a compute) compute a string length (a
# compute, a
# particular compute / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling StrLenUTF16_8217AB30, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string), a particular compute / a
# particular string, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->20 = a field, a value, ->24 = a field, a value, ->4 = a field, a
# value, ->28 = a field, a value, ->32 = a field, a value, a
# particular object), src (r4, r6, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string), (more). It constructs a string (a
# construct, a
# particular construct / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the copy, it copies a string (a
# copy, a
# particular copy / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation: obj->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->28 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->32 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the compute, it computes a string length (a
# compute, a
# particular compute / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling StrLenUTF16_8217AB30, sub_8217AB30, a compute, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string), a particular compute / a
# particular string, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "construct a string (a
# construct, with a
# copy and a
# check: construct a string (a
# construct, a
# particular construct / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a string (a copy, a
# particular copy / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation: obj->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->28 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->32 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a compute) compute a string length (a
# compute, a
# particular compute / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling StrLenUTF16_8217AB30, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string), a particular compute / a
# particular string, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a construct routine, part of the 0x8217A cluster, a sibling of
# ConstructString_821E6408 and ProcessLoop_82C204F0, a caller of StrLenUTF16_8217AB30, ProcessElementAccess2_8217A6C0, and
# ProcessElementAccess3_8217AA20). 77 lines, 4
# callees (ProcessElementAccess2_8217A6C0, ProcessElementAccess3_8217AA20, StrLenUTF16_8217AB30, Trampoline_8217A978). 10+
# callers: sub_8233A8D8, sub_822F4368, sub_822F3D20, sub_82BFD600, sub_825B27D0, sub_82C67C70, sub_824E93A8, sub_82C65310,
# sub_82C07D80, sub_82374240 (processes, which
# call this to construct the string, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->20 = a field, a value, ->24 = a field, a value, ->4 = a field, a
# value, ->28 = a field, a value, ->32 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (src, r6, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string) aren't resolvable, so the name states the mechanism: a construct (with a
# copy and a
# check) (construct a string (a
# construct, a
# particular construct / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a string (a copy, a
# particular copy / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from src, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation: obj->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->28 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->32 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a compute) compute a string length (a
# compute, a
# particular compute / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling StrLenUTF16_8217AB30, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string), a particular compute / a
# particular string, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "8217AB58" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8217AB58"]
name = "ConstructString_8217AB58"

# A return address (a return address, with a constant, the sibling of ReturnPointer_82C3BE78 / ConstructString_8217AB58):
# return a pointer (a return address, a particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19868, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (more). It returns a pointer (a
# return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19868, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "return address (a
# return address, with a
# constant: return a pointer (a return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19868, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a return address routine, part of the 0x82C2E cluster, a sibling of
# ReturnPointer_82C3BE78 and ConstructString_8217AB58). 10
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# pointer's meaning (r3, a pointer, a
# particular value, a
# particular size / a
# particular allocation, from a fixed .data address + 19868, a
# particular value) isn't resolvable, so the name states the mechanism: a return address (with a
# constant) (return a pointer (a return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19868, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "2" distinguishes it from ReturnPointer_82C3BE78.
[entrypoint.functions."0x82C2EDE8"]
name = "ReturnPointer2_82C2EDE8"

# A process (a process, with a copy and a loop, the sibling of ProcessToLower_821EEA90 / ReturnPointer2_82C2EDE8, the
# caller of CopyAssign_RefCounted_821F0108 / AtomicIncrement5_8222E840 / ToLower_Ascii_821EE9E8): do a process (a
# process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a refcounted object (a copy, a
# particular copy / a
# particular refcounted object, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling CopyAssign_RefCounted_821F0108, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular copy / a
# particular refcounted object, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a to-lower) convert to lowercase (a
# convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ToLower_Ascii_821EE9E8, on a
# value (a byte, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular convert / a
# particular character, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r30, ->0 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the copy, it copies a refcounted object (a
# copy, a
# particular copy / a
# particular refcounted object, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling CopyAssign_RefCounted_821F0108, sub_821F0108, a copy, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular copy / a
# particular refcounted object, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the to-lower, it converts to lowercase (a
# convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ToLower_Ascii_821EE9E8, sub_821EE9E8, a convert, on a
# value (a byte, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular convert / a
# particular character, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# copy and a
# loop: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a refcounted object (a copy, a
# particular copy / a
# particular refcounted object, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling CopyAssign_RefCounted_821F0108, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular copy / a
# particular refcounted object, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a to-lower) convert to lowercase (a
# convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ToLower_Ascii_821EE9E8, on a
# value (a byte, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular convert / a
# particular character, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x821EE cluster, a sibling of
# ProcessToLower_821EEA90 and ReturnPointer2_82C2EDE8, a caller of CopyAssign_RefCounted_821F0108, AtomicIncrement5_8222E840,
# and ToLower_Ascii_821EE9E8). 77 lines, 4
# callees (AtomicIncrement5_8222E840, CopyAssign_RefCounted_821F0108, GetSubObjField4_821EEA78, ToLower_Ascii_821EE9E8). 10+
# callers: sub_82835508, sub_824C7FB8, sub_82699F18, sub_8271E7E0, sub_82871858, sub_82C635D8, sub_83243AC0, sub_82612F38,
# sub_82A93090, sub_83242C90 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r30, ->0 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a process (with a
# copy and a
# loop) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a copy) copy a refcounted object (a copy, a
# particular copy / a
# particular refcounted object, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling CopyAssign_RefCounted_821F0108, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation), a particular copy / a
# particular refcounted object, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a to-lower) convert to lowercase (a
# convert, a
# particular convert / a
# particular character, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ToLower_Ascii_821EE9E8, on a
# value (a byte, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular convert / a
# particular character, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "821EEA00" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x821EEA00"]
name = "ProcessToLower_821EEA00"

# A chain search (a process, with a lookup and a loop, the sibling of ChainSearch_82B3D880 / ChainSearch2_82B5D548 / Chain
# Search5_8236E4A0): do a chain search (a process, a particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a->4, a
# particular value, a
# particular state / a
# particular result), and (in a loop) loop over a chain (a loop, a particular loop / a
# particular chain, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a chain, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is != a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) return a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r10, a
# particular value, a
# particular state / a
# particular result).

# Args: a (r3, ->4 = a field, a value, ->0 = a field, a value, a
# particular object), b (r4, ->4 = a field, a value, ->8 = a field, a
# value, a
# particular object), (more). It does a chain search (a
# process, a
# particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the lookup, it does a lookup (a
# lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a->4, a
# particular value, a
# particular state / a
# particular result). In the loop, it loops over a chain (a
# loop, a
# particular loop / a
# particular chain, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a chain, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). If a value (r11, a
# value, a
# particular value, a
# particular size / a
# particular allocation) is != a value (r9, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it continues the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation); otherwise, it returns a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r10, a
# particular value, a
# particular state / a
# particular result). So it's a "chain search (a
# process, with a
# lookup and a
# loop: do a chain search (a process, a
# particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a->4, a
# particular value, a
# particular state / a
# particular result), and (in a loop) loop over a chain (a loop, a
# particular loop / a
# particular chain, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a chain, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is != a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) return a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r10, a
# particular value, a
# particular state / a
# particular result))" routine (a chain search routine, part of the 0x82C46 cluster, a sibling of
# ChainSearch_82B3D880, ChainSearch2_82B5D548, and ChainSearch5_8236E4A0). 65 lines, 0
# callees. 2 callers: sub_82C46F80, Tree_ProcessNode_82C47318 (processes, which
# call this to do the chain search, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r3, ->4 = a field, a value, ->0 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (b, r4, ->4 = a field, a value, ->8 = a field, a
# value, a
# particular object) aren't resolvable, so the name states the mechanism: a chain search (with a
# lookup and a
# loop) (do a chain search (a process, a
# particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a->4, a
# particular value, a
# particular state / a
# particular result), and (in a loop) loop over a chain (a loop, a
# particular loop / a
# particular chain, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a chain, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is != a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) return a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r10, a
# particular value, a
# particular state / a
# particular result)). The "6" distinguishes it from ChainSearch_82B3D880, ChainSearch2_82B5D548, and ChainSearch5_8236E4A0.
[entrypoint.functions."0x82C46BA0"]
name = "ChainSearch6_82C46BA0"

# A process (a process, with a vcall and a chain search, the sibling of ProcessVcall_82C21F38 / ProcessVcall2_82C22258,
# the caller of ChainSearch6_82C46BA0): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a->0->16, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a chain search) do a chain search (a
# process, a particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ChainSearch6_82C46BA0, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r3 + r30, a
# particular value), a particular process / a
# particular lookup, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object), a (r4, ->0 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), b (r5, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the vcall, it does a vcall (a
# vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a->0->16, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the chain search, it does a chain search (a
# process, a
# particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ChainSearch6_82C46BA0, sub_82C46BA0, a process, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r3 + r30, a
# particular value), a particular process / a
# particular lookup, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# vcall and a
# chain search: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a->0->16, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a chain search) do a chain search (a
# process, a particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ChainSearch6_82C46BA0, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r3 + r30, a
# particular value), a particular process / a
# particular lookup, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82C46 cluster, a sibling of
# ProcessVcall_82C21F38 and ProcessVcall2_82C22258, a caller of ChainSearch6_82C46BA0). 53 lines, 1
# callee (ChainSearch6_82C46BA0). 10+ callers: sub_82C13628, sub_82C55AD8, sub_82C50130, sub_82C136C0, sub_82C50198, sub
# _82C1BBA0, sub_82C1BE80, sub_82C0AF58, sub_82C52A80, sub_82C55BC8 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r4, ->0 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, b, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# vcall and a
# chain search) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a vcall) do a vcall (a vcall, a
# particular vcall / a
# particular method, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a->0->16, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a chain search) do a chain search (a
# process, a
# particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ChainSearch6_82C46BA0, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r3 + r30, a
# particular value), a particular process / a
# particular lookup, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "3" distinguishes it from ProcessVcall_82C21F38 and ProcessVcall2_82C22258.
[entrypoint.functions."0x82C46F80"]
name = "ProcessVcall3_82C46F80"

# A process (a process, with a loop and a comparison and a heap insert, the sibling of ProcessHeapInsert_82C202A0 / Process
# Vcall3_82C46F80): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a comparison) compare a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r6, a
# particular value, r8, a
# particular value, a
# particular state / a
# particular result), and (if a value (r6, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) insert a value (a store, a
# particular store / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a->r11 = r9, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, a->0 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: a (r3, ->0 = a field, a value, ->r11 = a field, a value, ->r7 = a field, a
# value, a
# particular object), b (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r5, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the comparison, it compares a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r6, a
# particular value, r8, a
# particular value, a
# particular state / a
# particular result). If it is < a value (r6, a
# value, a
# particular value, a
# particular size / a
# particular allocation, r8, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it inserts a value (a
# store, a
# particular store / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a->r11 = r9, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, a->0 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result); otherwise, it continues the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# loop and a
# comparison and a
# heap insert: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a comparison) compare a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r6, a
# particular value, r8, a
# particular value, a
# particular state / a
# particular result), and (if a value (r6, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) insert a value (a store, a
# particular store / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a->r11 = r9, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, a->0 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x82C1F cluster, a sibling of
# ProcessHeapInsert_82C202A0 and ProcessVcall3_82C46F80). 298 lines, 0
# callees. 1 caller: sub_82C202A0 (a process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r3, ->0 = a field, a value, ->r11 = a field, a value, ->r7 = a field, a
# value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (b, r4, a value, a
# particular value, a
# particular size / a
# particular allocation, c, r5, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# loop and a
# comparison and a
# heap insert) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a comparison) compare a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r6, a
# particular value, r8, a
# particular value, a
# particular state / a
# particular result), and (if a value (r6, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r8, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) insert a value (a store, a
# particular store / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a->r11 = r9, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, a->0 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "82C1FC68" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82C1FC68"]
name = "ProcessHeapInsert_82C1FC68"

# A process (a process, with a heap insert and a loop, the sibling of ProcessHeapInsert_82C1FC68 / ProcessHeap_82C20330,
# the caller of ProcessHeapInsert_82C1FC68): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a heap insert) do a heap insert (a
# process, a particular process / a
# particular insert, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ProcessHeapInsert_82C1FC68, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r31, a
# particular value), a particular process / a
# particular insert, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a range, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r30, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r28, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object), dst (r4, r30, a value, a
# particular value, a
# particular size / a
# particular allocation, a destination), src (r5, r29, a value, a
# particular value, a
# particular size / a
# particular allocation, a source), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the heap insert, it does a heap insert (a
# process, a
# particular process / a
# particular insert, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ProcessHeapInsert_82C1FC68, sub_82C1FC68, a process, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r31, a
# particular value), a particular process / a
# particular insert, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a range, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). If a value (r30, a
# value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r31, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it continues the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation); otherwise, it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# heap insert and a
# loop: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a heap insert) do a heap insert (a
# process, a particular process / a
# particular insert, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ProcessHeapInsert_82C1FC68, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r31, a
# particular value), a particular process / a
# particular insert, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a range, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r30, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x82C20 cluster, a sibling of
# ProcessHeapInsert_82C1FC68 and ProcessHeap_82C20330, a caller of ProcessHeapInsert_82C1FC68). 326 lines, 1
# callee (ProcessHeapInsert_82C1FC68). 1 caller: ProcessRecursive3_82C20680 (a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r28, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (dst, r30, a value, a
# particular value, a
# particular size / a
# particular allocation, a destination, src, r29, a value, a
# particular value, a
# particular size / a
# particular allocation, a source) aren't resolvable, so the name states the mechanism: a process (with a
# heap insert and a
# loop) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a heap insert) do a heap insert (a
# process, a
# particular process / a
# particular insert, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ProcessHeapInsert_82C1FC68, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r31, a
# particular value), a particular process / a
# particular insert, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a range, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r30, a value, a
# particular value, a
# particular size / a
# particular allocation) is < a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular value) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "82C202A0" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82C202A0"]
name = "ProcessHeap_82C202A0"

# A constant return (a constant return, with a constant, the sibling of ReturnConstant_1024_82A11E60 / ReturnConstant
# _0x10000_82A14660): return a constant (a constant return, a particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 131072, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (more). It returns a constant (a
# constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 131072, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "constant return (a
# constant return, with a
# constant: return a constant (a constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 131072, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a constant return routine, part of the 0x82A14 cluster, a sibling of
# ReturnConstant_1024_82A11E60 and ReturnConstant_0x10000_82A14660). 8
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# constant's value (131072, a
# constant, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable beyond its value, so the name states the mechanism: a constant return (with a
# constant) (return a constant (a constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 131072, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "131072" refers to the constant (131072, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).
[entrypoint.functions."0x82A14668"]
name = "ReturnConstant_131072_82A14668"

# A destruct (a destruct, with a check and a free, the sibling of Destruct_822C97D0 / Destruct2_82B11990 / Destruct
# 20ByteFree2_82A14A20, the caller of Free_SizeBucketed_Thunk_8221BE68): destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->24, a
# particular value, a
# particular state / a
# particular result), and (if a value (obj->24, a value, a
# particular value, a
# particular size / a
# particular allocation) is < 8, a
# particular value) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (obj->4, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and store values (obj->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->24 = a field, a value, ->4 = a field, a value, ->20 = a field, a
# value, a
# particular object), (more). It destructs an object (a
# destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->24, a
# particular value, a
# particular state / a
# particular result). If it is < 8 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it frees a value (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a
# free, on a
# value (obj->4, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result); if it is >= 8 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). It stores values (obj->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "destruct an object (a
# destruct, with a
# check and a
# free: destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->24, a
# particular value, a
# particular state / a
# particular result), and (if a value (obj->24, a value, a
# particular value, a
# particular size / a
# particular allocation) is < 8, a
# particular value) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (obj->4, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and store values (obj->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a destruct routine, part of the 0x822F5 cluster, a sibling of
# Destruct_822C97D0, Destruct2_82B11990, and Destruct20ByteFree2_82A14A20, a caller of Free_SizeBucketed_Thunk_8221BE68). 49
# lines, 1 callee (Free_SizeBucketed_Thunk_8221BE68). 10+ callers: sub_82374808, sub_823728E0, sub_82C638C0, sub_82C7AC28,
# sub_82C68B50, sub_82C71050, sub_82C70A00, sub_82EE2A48, sub_824EA150, sub_82C07AB0 (processes, which
# call this to destruct the object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->24 = a field, a value, ->4 = a field, a value, ->20 = a field, a
# value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a destruct (with a
# check and a
# free) (destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->24, a
# particular value, a
# particular state / a
# particular result), and (if a value (obj->24, a value, a
# particular value, a
# particular size / a
# particular allocation) is < 8, a
# particular value) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (obj->4, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and store values (obj->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "3" distinguishes it from Destruct_822C97D0 and Destruct2_82B11990.
[entrypoint.functions."0x822F5208"]
name = "Destruct3_822F5208"

# A process (a process, with a check and an allocate, the sibling of ProcessAllocate_83230900 / Destruct3_822F5208, the
# caller of sub_82CC0CA0): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->8, a
# particular value, a
# particular state / a
# particular result), and (if a value (obj->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) allocate a value (sub_82CC0CA0, on a
# value (0, a value, a
# particular value, a
# particular size / a
# particular allocation, 4096, a
# particular value, 4, a
# particular value, 128 | 4096, a
# particular value), a particular allocate / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->8 = a field, a value, ->0 = a field, a value, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->8, a
# particular value, a
# particular state / a
# particular result). If it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it allocates a value (sub_82CC0CA0, sub_82CC0CA0, an
# allocate, on a
# value (0, a value, a
# particular value, a
# particular size / a
# particular allocation, 4096, a
# particular value, 4, a
# particular value, 128 | 4096, a
# particular value), a particular allocate / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result); if it is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# check and an
# allocate: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->8, a
# particular value, a
# particular state / a
# particular result), and (if a value (obj->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) allocate a value (sub_82CC0CA0, on a
# value (0, a value, a
# particular value, a
# particular size / a
# particular allocation, 4096, a
# particular value, 4, a
# particular value, 128 | 4096, a
# particular value), a particular allocate / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x8322F cluster, a sibling of
# ProcessAllocate_83230900 and Destruct3_822F5208, a caller of sub_82CC0CA0). 145 lines, 1
# callee (sub_82CC0CA0). 4 callers: sub_83230900, ProcessPoolAcquire_83236230, sub_83230C18, FindNodeInList_83235FC0
# (processes, which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->8 = a field, a value, ->0 = a field, a value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a process (with a
# check and an
# allocate) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->8, a
# particular value, a
# particular state / a
# particular result), and (if a value (obj->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) allocate a value (sub_82CC0CA0, on a
# value (0, a value, a
# particular value, a
# particular size / a
# particular allocation, 4096, a
# particular value, 4, a
# particular value, 128 | 4096, a
# particular value), a particular allocate / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "8322FB30" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8322FB30"]
name = "ProcessAllocate_8322FB30"

# A process (a process, with a decrement and a lookup, the sibling of ProcessDecrement_83230BC0 / ProcessAllocate_8322
# FB30, the caller of sub_82CC0CF0): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a decrement) do a decrement (a
# process, a particular process / a
# particular decrement, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, r11->8 = r11->8 - 1, a
# store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r11->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) unlink a value (a store, a
# particular store / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, r9->4 = r11->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = r11->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and tail-call a process (sub_82CC0CF0, on a
# value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation, 0, a
# particular value, 32768, a
# particular value), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, ->8 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), a (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the decrement, it does a decrement (a
# process, a
# particular process / a
# particular decrement, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, r11->8 = r11->8 - 1, a
# store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the lookup, it does a lookup (a
# lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). If a value (r11->8, a
# value, a
# particular value, a
# particular size / a
# particular allocation) is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it unlinks a value (a
# store, a
# particular store / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, r9->4 = r11->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = r11->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and tail-calls a process (sub_82CC0CF0, sub_82CC0CF0, a
# process, on a
# value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation, 0, a
# particular value, 32768, a
# particular value), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# decrement and a
# lookup: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a decrement) do a decrement (a
# process, a particular process / a
# particular decrement, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, r11->8 = r11->8 - 1, a
# store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r11->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) unlink a value (a store, a
# particular store / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, r9->4 = r11->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = r11->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and tail-call a process (sub_82CC0CF0, on a
# value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation, 0, a
# particular value, 32768, a
# particular value), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x8322F cluster, a sibling of
# ProcessDecrement_83230BC0 and ProcessAllocate_8322FB30, a caller of sub_82CC0CF0). 84 lines, 1
# callee (sub_82CC0CF0). 4 callers: sub_83230BC0, sub_829FD240, sub_83230FA0, sub_83230848 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r3, ->8 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r4, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# decrement and a
# lookup) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a decrement) do a decrement (a
# process, a
# particular process / a
# particular decrement, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, r11->8 = r11->8 - 1, a
# store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->8, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (if a value (r11->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) unlink a value (a store, a
# particular store / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, r9->4 = r11->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = r11->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and tail-call a process (sub_82CC0CF0, on a
# value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation, 0, a
# particular value, 32768, a
# particular value), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "8322FAA0" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8322FAA0"]
name = "ProcessDecrement_8322FAA0"

# A process (a process, with a switch and a lookup, the sibling of ProcessDispatch_82BCD7B0 / ProcessDispatch3_82815C28,
# the caller of ConstructRefCounted_8222CF18 / DestructRefCounted_82214F08 / ProcessToLower_821EEA00): do a process (a
# process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a switch) switch on a type (a switch, a
# particular switch / a
# particular type, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over type, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a table, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and store a value (obj->524 = r9->r7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->524 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), type (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the switch, it switches on a type (a
# switch, a
# particular switch / a
# particular type, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over type, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the lookup, it does a lookup (a
# lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a table, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). It stores a value (obj->524 = r9->r7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# switch and a
# lookup: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a switch) switch on a type (a switch, a
# particular switch / a
# particular type, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over type, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a table, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and store a value (obj->524 = r9->r7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82816 cluster, a sibling of
# ProcessDispatch_82BCD7B0 and ProcessDispatch3_82815C28, a caller of ConstructRefCounted_8222CF18, DestructRefCounted_82214F08,
# and ProcessToLower_821EEA00). 172 lines, 5
# callees (ConstructRefCounted_8222CF18, DestructRefCounted_82214F08, ProcessToLower_821EEA00, sub_821EC668, sub_82BFD780).
# 2 callers: sub_82188B88, sub_828152D0 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->524 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (type, r4, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# switch and a
# lookup) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a switch) switch on a type (a switch, a
# particular switch / a
# particular type, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over type, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a lookup) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a table, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and store a value (obj->524 = r9->r7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "4" distinguishes it from ProcessDispatch_82BCD7B0 and ProcessDispatch3_82815C28.
[entrypoint.functions."0x828164E8"]
name = "ProcessDispatch4_828164E8"

# A constant return (a constant return, with a constant, the sibling of ReturnConstant_1024_82A11E60 / ReturnConstant
# _131072_82A14668): return a constant (a constant return, a particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 4096, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (more). It returns a constant (a
# constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 4096, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "constant return (a
# constant return, with a
# constant: return a constant (a constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 4096, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a constant return routine, part of the 0x82A11 cluster, a sibling of
# ReturnConstant_1024_82A11E60 and ReturnConstant_131072_82A14668). 8
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# constant's value (4096, a
# constant, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable beyond its value, so the name states the mechanism: a constant return (with a
# constant) (return a constant (a constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 4096, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "4096" refers to the constant (4096, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).
[entrypoint.functions."0x82A11F28"]
name = "ReturnConstant_4096_82A11F28"

# A trampoline (a trampoline, with a single call, the sibling of Trampoline_8217A978 / ReturnConstant_4096_82A11F28, the
# caller of LazyGetData_822F2088): do a trampoline (a trampoline, a particular trampoline, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a call) call a process (LazyGetData_822F2088, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a trampoline (a
# trampoline, a
# particular trampoline, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the call, it calls a process (LazyGetData_822F2088, sub_822F2088, a
# process, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "trampoline (a
# trampoline, with a
# single call: do a trampoline (a trampoline, a
# particular trampoline, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a call) call a process (LazyGetData_822F2088, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a trampoline routine, part of the 0x822C8 cluster, a sibling of
# Trampoline_8217A978 and ReturnConstant_4096_82A11F28, a caller of LazyGetData_822F2088). 7
# lines, 1 callee (LazyGetData_822F2088). 4 callers: sub_82C651D8, sub_82C65270, sub_82C65310, sub_82BFD780 (processes,
# which call this to do the trampoline, a
# particular size / a
# particular allocation, a
# particular operation). The value's meaning (r3, a value, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a trampoline (with a
# single call) (do a trampoline (a trampoline, a
# particular trampoline, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a call) call a process (LazyGetData_822F2088, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "2" distinguishes it from Trampoline_8217A978.
[entrypoint.functions."0x822C88A8"]
name = "Trampoline2_822C88A8"

# A process (a process, with a check and a construct and a free, the sibling of ProcessConstructString_82C651D8 / Tramp
# oline2_822C88A8, the caller of ConstructString_8217AB58 / Trampoline2_822C88A8 / Free_Thunk_824FE010): do a process (a
# process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->0, a
# particular value, a
# particular state / a
# particular result), and (in a construct) construct a string (a
# construct, a particular construct / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ConstructString_8217AB58, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value), a particular construct / a
# particular string, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a free) free a value (Free_Thunk_824FE010, on a
# value (stack[84], a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, ->0 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0, a
# particular value, a
# particular state / a
# particular result). In the construct, it constructs a string (a
# construct, a
# particular construct / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ConstructString_8217AB58, sub_8217AB58, a construct, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value), a particular construct / a
# particular string, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the free, it frees a value (Free_Thunk_824FE010, sub_824FE010, a
# free, on a
# value (stack[84], a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# check and a
# construct and a
# free: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->0, a
# particular value, a
# particular state / a
# particular result), and (in a construct) construct a string (a
# construct, a particular construct / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ConstructString_8217AB58, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value), a particular construct / a
# particular string, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a free) free a value (Free_Thunk_824FE010, on a
# value (stack[84], a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82BFD cluster, a sibling of
# ProcessConstructString_82C651D8 and Trampoline2_822C88A8, a caller of ConstructString_8217AB58, Trampoline2_822C88A8, and
# Free_Thunk_824FE010). 67 lines, 3
# callees (ConstructString_8217AB58, Free_Thunk_824FE010, sub_822C88A8). 10+ callers: sub_82557DE8, sub_82AAA530, sub
# _828815D8, sub_83260198, sub_83249CD0, sub_83249E50, sub_8240BA80, sub_82A1C7F0, sub_82819E70, sub_8235C3C0 (processes,
# which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r3, ->0 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a process (with a
# check and a
# construct and a
# free) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->0, a
# particular value, a
# particular state / a
# particular result), and (in a construct) construct a string (a
# construct, a
# particular construct / a
# particular string, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ConstructString_8217AB58, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value), a particular construct / a
# particular string, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a free) free a value (Free_Thunk_824FE010, on a
# value (stack[84], a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "82BFD780" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82BFD780"]
name = "ProcessConstructString_82BFD780"

# A process (a process, with a check and a string operation, the sibling of ProcessString_826C3EF0 / ProcessConstruct
# String_82BFD780, the caller of SetString_822F2020 / Allocate_OrDbgBreak_8221F388 / CopyAssign_RefCounted_821F0108):
# do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->8, a
# particular value, a
# particular state / a
# particular result), and (if a value (a->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is >= a constant (r9, a
# particular value, a
# particular size / a
# particular allocation), a particular value) do a string operation (a
# string operation, a particular string operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling SetString_822F2020, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a constant, a
# particular value), a particular string operation, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r24, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object), a (r4, r29, ->8 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), b (r5, r26, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r6, r31, a value, a
# particular value, a
# particular size / a
# particular allocation), d (r7, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a->8, a
# particular value, a
# particular state / a
# particular result). If it is >= a constant (a->8, a
# value, a
# particular value, a
# particular size / a
# particular allocation, r9, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state), it does a string operation (a
# string operation, a
# particular string operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling SetString_822F2020, sub_822F2020, a string operation, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a constant, a
# particular value), a particular string operation, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result); if it is < a constant (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# check and a
# string operation: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->8, a
# particular value, a
# particular state / a
# particular result), and (if a value (a->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is >= a constant (r9, a
# particular value, a
# particular size / a
# particular allocation), a particular value) do a string operation (a
# string operation, a particular string operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling SetString_822F2020, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a constant, a
# particular value), a particular string operation, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x8246C cluster, a sibling of
# ProcessString_826C3EF0 and ProcessConstructString_82BFD780, a caller of SetString_822F2020, Allocate_OrDbgBreak_8221F388,
# and CopyAssign_RefCounted_821F0108). 343 lines, 9
# callees (Allocate_OrDbgBreak_8221F388, CopyAssign_RefCounted_821F0108, SetString_822F2020, sub_82171810, sub_822F1F00,
# sub_82301AE0, sub_8234D268, sub_826C3EF0, sub_826C3FA8). 1 caller: LookupString2_8246C010 (a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r24, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r29, ->8 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, b, r26, a value, a
# particular value, a
# particular size / a
# particular allocation, c, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, d, r28, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# check and a
# string operation) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->8, a
# particular value, a
# particular state / a
# particular result), and (if a value (a->8, a value, a
# particular value, a
# particular size / a
# particular allocation) is >= a constant (r9, a
# particular value, a
# particular size / a
# particular allocation), a particular value) do a string operation (a
# string operation, a
# particular string operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling SetString_822F2020, on a
# value (stack[80], a value, a
# particular value, a
# particular size / a
# particular allocation, a constant, a
# particular value), a particular string operation, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "8246CF80" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8246CF80"]
name = "ProcessString_8246CF80"

# A hash lookup (a process, with a hash and a check, the sibling of HashLookup_82B49320 / ProcessString_8246CF80): do a
# hash lookup (a process, a particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a hash) do a hash (a hash, a
# particular hash / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, obj->36 ^ key, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11->60, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11->60, a value, a
# particular value, a
# particular size / a
# particular allocation) is != 1, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a table, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, ->36 = a field, a value, ->8 = a field, a value, a
# particular object), key (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a hash lookup (a
# process, a
# particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the hash, it does a hash (a
# hash, a
# particular hash / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, obj->36 ^ key, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r11->60, a
# particular value, a
# particular state / a
# particular result). If it is != 1 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); otherwise, it does a lookup (a
# lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a table, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "hash lookup (a
# process, with a
# hash and a
# check: do a hash lookup (a process, a
# particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a hash) do a hash (a hash, a
# particular hash / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, obj->36 ^ key, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11->60, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11->60, a value, a
# particular value, a
# particular size / a
# particular allocation) is != 1, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a table, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a hash lookup routine, part of the 0x82B49 cluster, a sibling of
# HashLookup_82B49320 and ProcessString_8246CF80). 100 lines, 0
# callees. 6 callers: sub_82B48FE0, sub_82B497B8, sub_82B49600, sub_82B49320, sub_82B49730,
# ProcessChunk_TableDispatch_82B49110 (processes, which
# call this to do the hash lookup, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r3, ->36 = a field, a value, ->8 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (key, r4, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a hash lookup (with a
# hash and a
# check) (do a hash lookup (a process, a
# particular process / a
# particular lookup, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a hash) do a hash (a hash, a
# particular hash / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, obj->36 ^ key, a
# compute, a
# particular compute / a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11->60, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11->60, a value, a
# particular value, a
# particular size / a
# particular allocation) is != 1, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) do a lookup (a lookup, a
# particular lookup / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a table, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "82B49678" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82B49678"]
name = "HashLookup_82B49678"

# A constant return (a constant return, with a constant, the sibling of ReturnConstant_1024_82A11E60 / ReturnConstant
# _4096_82A11F28): return a constant (a constant return, a particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 16384, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (more). It returns a constant (a
# constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 16384, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "constant return (a
# constant return, with a
# constant: return a constant (a constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 16384, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a constant return routine, part of the 0x82A14 cluster, a sibling of
# ReturnConstant_1024_82A11E60 and ReturnConstant_4096_82A11F28). 8
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# constant's value (16384, a
# constant, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable beyond its value, so the name states the mechanism: a constant return (with a
# constant) (return a constant (a constant return, a
# particular return / a
# particular value, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, 16384, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "16384" refers to the constant (16384, a
# constant, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).
[entrypoint.functions."0x82A145F0"]
name = "ReturnConstant_16384_82A145F0"

# A process (a process, with a check and a call, the sibling of Process_82227934 / ReturnConstant_16384_82A145F0):
# do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, d, a
# particular value, a
# particular state / a
# particular result), and (if a value (b, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) or (if a value (d, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_828456C8, on a
# value (r30->27520, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation, d, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: a (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r5, r27, a value, a
# particular value, a
# particular size / a
# particular allocation), d (r6, r29, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, d, a
# particular value, a
# particular state / a
# particular result). If a value (b, a
# value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation) or a value (d, a
# value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it calls a process (sub_828456C8, sub_828456C8, a
# process, on a
# value (r30->27520, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation, d, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result); otherwise, it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# check and a
# call: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, d, a
# particular value, a
# particular state / a
# particular result), and (if a value (b, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) or (if a value (d, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_828456C8, on a
# value (r30->27520, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation, d, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x82227 cluster, a sibling of
# Process_82227934 and ReturnConstant_16384_82A145F0). 127 lines, 5
# callees (sub_82195930, sub_824149C8, sub_828456C8, sub_828457F0, sub_82CBBB58). No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# value's meaning (a, r3, a value, a
# particular value, a
# particular size / a
# particular allocation, b, r28, a value, a
# particular value, a
# particular size / a
# particular allocation, c, r27, a value, a
# particular value, a
# particular size / a
# particular allocation, d, r29, a value, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a process (with a
# check and a
# call) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, d, a
# particular value, a
# particular state / a
# particular result), and (if a value (b, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) or (if a value (d, a value, a
# particular value, a
# particular size / a
# particular allocation) is non-zero, a
# particular value) call a process (sub_828456C8, on a
# value (r30->27520, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation, d, a
# particular value, a
# particular size / a
# particular allocation), a particular process, a
# particular operation, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (otherwise) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "82227868" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82227868"]
name = "Process_82227868"

# A process (a process, with a check and an acquire, the sibling of PoolAllocate_828457F0 / Process_82227868, the caller
# of Pool_AcquireNode_83230688 / HeapAllocate_83231590 / FindSizeBucket_83231518): do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, size, a
# particular value, a
# particular state / a
# particular result), and (if a value (size, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) do an acquire (a
# process, a particular process / a
# particular acquire, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Pool_AcquireNode_83230688, on a
# value (r10->r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular process / a
# particular acquire, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r5, r27, a value, a
# particular value, a
# particular size / a
# particular allocation), size (r6, r31, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, size, a
# particular value, a
# particular state / a
# particular result). If it is zero (a null state, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); otherwise, it does an acquire (a
# process, a
# particular process / a
# particular acquire, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Pool_AcquireNode_83230688, sub_83230688, a process, on a
# value (r10->r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular process / a
# particular acquire, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# check and an
# acquire: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, size, a
# particular value, a
# particular state / a
# particular result), and (if a value (size, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) do an acquire (a
# process, a particular process / a
# particular acquire, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Pool_AcquireNode_83230688, on a
# value (r10->r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular process / a
# particular acquire, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82845 cluster, a sibling of
# PoolAllocate_828457F0 and Process_82227868, a caller of Pool_AcquireNode_83230688, HeapAllocate_83231590, and
# FindSizeBucket_83231518). 171 lines, 5
# callees (FindSizeBucket_83231518, HeapAllocate_83231590, MemCpy_SdkRuntime_82CA2C60, Pool_AcquireNode_83230688,
# Pool_FreeByAddress_832304E8). 1 caller: sub_82227868 (Process_82227868, a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The value's meaning (a, r3, a value, a
# particular value, a
# particular size / a
# particular allocation, b, r28, a value, a
# particular value, a
# particular size / a
# particular allocation, c, r27, a value, a
# particular value, a
# particular size / a
# particular allocation, size, r31, a value, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a process (with a
# check and an
# acquire) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, size, a
# particular value, a
# particular state / a
# particular result), and (if a value (size, a value, a
# particular value, a
# particular size / a
# particular allocation) is zero, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) do an acquire (a
# process, a
# particular process / a
# particular acquire, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling Pool_AcquireNode_83230688, on a
# value (r10->r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular process / a
# particular acquire, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "828456C8" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x828456C8"]
name = "PoolAllocate_828456C8"

# A process (a process, with a loop and a check, the sibling of ProcessString_8246CF80 / PoolAllocate_828456C8, the
# caller of GetSubObjField4_821EEA78 / DestructRefCounted_82214F08 / ProcessAtomicIncrement_821E3950): do a process (a
# process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 32, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 9, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 13, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r30, ->0 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result). If it is == 32 (a particular value, a
# particular state, a
# particular size / a
# particular allocation) or == 9 (a particular value, a
# particular state, a
# particular size / a
# particular allocation) or == 13 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); otherwise, it continues the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 32, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 9, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 13, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x82B3A cluster, a sibling of
# ProcessString_8246CF80 and PoolAllocate_828456C8, a caller of GetSubObjField4_821EEA78, DestructRefCounted_82214F08,
# and ProcessAtomicIncrement_821E3950). 169 lines, 4
# callees (DestructRefCounted_82214F08, GetSubObjField4_821EEA78, ProcessAtomicIncrement_821E3950, Release_RefCounted_821C67D8). 9
# callers: sub_825F45A8, sub_82359BE8, sub_8281A568, sub_82355C08, sub_825F5238, sub_82607260, sub_82816618, sub_826935F0,
# sub_82817D28 (processes, which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r30, ->0 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 32, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 9, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 13, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "2" distinguishes it from ProcessString_8246CF80.
[entrypoint.functions."0x82B3A448"]
name = "ProcessString2_82B3A448"

# A process (a process, with a loop and a check, the sibling of ProcessString_8246CF80 / ProcessString2_82B3A448, the
# caller of DestructRefCounted_82214F08 / ProcessAtomicIncrement_821E3950 / Release_RefCounted_821C67D8): do a process (a
# process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 32, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 9, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 13, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 10, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation).

# Args: obj (r3, r31, ->0 = a field, a value, a particular value, a particular size / a
# particular allocation, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result). If it is == 32 (a particular value, a
# particular state, a
# particular size / a
# particular allocation) or == 9 (a particular value, a
# particular state, a
# particular size / a
# particular allocation) or == 13 (a particular value, a
# particular state, a
# particular size / a
# particular allocation) or == 10 (a particular value, a
# particular state, a
# particular size / a
# particular allocation), it takes one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation); otherwise, it continues the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 32, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 9, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 13, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 10, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation))" routine (a process routine, part of the 0x82B3A cluster, a sibling of
# ProcessString_8246CF80 and ProcessString2_82B3A448, a caller of DestructRefCounted_82214F08, ProcessAtomicIncrement_821E3950,
# and Release_RefCounted_821C67D8). 158 lines, 3
# callees (DestructRefCounted_82214F08, ProcessAtomicIncrement_821E3950, Release_RefCounted_821C67D8). 10 callers: sub_82817D28,
# sub_825F5238, sub_82355C08, sub_825F45A8, sub_82C47BC0, sub_826935F0, sub_8281A568, sub_82607260, sub_82359BE8,
# sub_82816618 (processes, which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->0 = a field, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a string, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 32, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 9, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 13, a
# particular value) or (if a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation) is == 10, a
# particular value) take one path (a no-op / a return, a
# particular state, a
# particular result, a particular size / a
# particular allocation), and (otherwise) continue the loop (a
# particular state, a
# particular result, a particular size / a
# particular allocation)). The "3" distinguishes it from ProcessString_8246CF80 and ProcessString2_82B3A448.
[entrypoint.functions."0x82B3A560"]
name = "ProcessString3_82B3A560"

# A process (a process, with a scan and a construct, the sibling of ProcessLine_82B47128 / ProcessString3_82B3A560, the
# caller of ScanLineLength_82B477F0 / ConstructRefCounted_8222CF18): do a process (a process, a particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a scan) do a scan (a
# scan, a particular scan / a
# particular line, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ScanLineLength_82B477F0, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation), a particular scan / a
# particular line, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a construct) construct a refcounted object (a
# construct, a particular construct / a
# particular refcounted object, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ConstructRefCounted_8222CF18, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation, r5, a
# particular value, a
# particular size / a
# particular allocation), a particular construct / a
# particular refcounted object, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object), a (r4, r5, ->0 = a field, a value, ->36 = a field, a value, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the scan, it does a scan (a
# scan, a
# particular scan / a
# particular line, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ScanLineLength_82B477F0, sub_82B477F0, a scan, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation), a particular scan / a
# particular line, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the construct, it constructs a refcounted object (a
# construct, a
# particular construct / a
# particular refcounted object, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ConstructRefCounted_8222CF18, sub_8222CF18, a construct, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation, r5, a
# particular value, a
# particular size / a
# particular allocation), a particular construct / a
# particular refcounted object, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# scan and a
# construct: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a scan) do a scan (a
# scan, a particular scan / a
# particular line, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ScanLineLength_82B477F0, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation), a particular scan / a
# particular line, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a construct) construct a refcounted object (a
# construct, a particular construct / a
# particular refcounted object, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ConstructRefCounted_8222CF18, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation, r5, a
# particular value, a
# particular size / a
# particular allocation), a particular construct / a
# particular refcounted object, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82B47 cluster, a sibling of
# ProcessLine_82B47128 and ProcessString3_82B3A560, a caller of ScanLineLength_82B477F0 and ConstructRefCounted_8222CF18). 69
# lines, 2 callees (ConstructRefCounted_8222CF18, ScanLineLength_82B477F0). 8 callers: sub_82A18998, sub_82607260, sub
# _828152D0, sub_825F5238, sub_826935F0, sub_82817D28, sub_82359BE8, sub_82355C08 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r5, ->0 = a field, a value, ->36 = a field, a value, a
# particular object) aren't resolvable, so the name states the mechanism: a process (with a
# scan and a
# construct) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a scan) do a scan (a
# scan, a
# particular scan / a
# particular line, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ScanLineLength_82B477F0, on a
# value (a, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation), a particular scan / a
# particular line, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a construct) construct a refcounted object (a
# construct, a
# particular construct / a
# particular refcounted object, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, calling ConstructRefCounted_8222CF18, on a
# value (obj, a value, a
# particular value, a
# particular size, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation, r5, a
# particular value, a
# particular size / a
# particular allocation), a particular construct / a
# particular refcounted object, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "82B470D0" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82B470D0"]
name = "ProcessLine_82B470D0"

# A process (a process, with a check and a list manipulation, the sibling of ListUnlink_82B5D930 / ProcessLine_82B470D0):
# do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r10->21, a
# particular value, a
# particular state / a
# particular result), and (in a list manipulation) do a list manipulation (a
# list manipulation, a particular list manipulation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, node->8 = r11->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->4 = node->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->0 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, node->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: a (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), node (r4, ->8 = a field, a value, ->4 = a field, a
# value, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r10->21, a
# particular value, a
# particular state / a
# particular result). In the list manipulation, it does a list manipulation (a
# list manipulation, a
# particular list manipulation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, node->8 = r11->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->4 = node->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->0 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, node->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# check and a
# list manipulation: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r10->21, a
# particular value, a
# particular state / a
# particular result), and (in a list manipulation) do a list manipulation (a
# list manipulation, a particular list manipulation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, node->8 = r11->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->4 = node->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->0 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, node->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a list manipulation routine, part of the 0x82B5E cluster, a sibling of
# ListUnlink_82B5D930 and ProcessLine_82B470D0). 71 lines, 0
# callees. 2 callers: sub_82B5D930, ProcessChain4_82B5E558 (processes, which
# call this to do the list manipulation, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (node, r4, ->8 = a field, a value, ->4 = a field, a
# value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r3, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# check and a
# list manipulation) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r10->21, a
# particular value, a
# particular state / a
# particular result), and (in a list manipulation) do a list manipulation (a
# list manipulation, a
# particular list manipulation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, node->8 = r11->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->4 = node->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->0 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, node->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "82B5E7F8" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82B5E7F8"]
name = "ListUnlink_82B5E7F8"

# A destruct (a destruct, with a loop and a free, the sibling of Destruct_822C97D0 / Destruct3_822F5208 / ListUnlink
# _82B5E7F8, the caller of Free_SizeBucketed_Thunk_8221BE68): destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a free) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (r3->0, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->8 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->4 = a field, a value, ->8 = a field, a value, a
# particular object), (more). It destructs an object (a
# destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the free, it frees a value (Free_SizeBucketed_Thunk_8221BE68, sub_8221BE68, a
# free, on a
# value (r3->0, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). It stores values (obj->8 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "destruct an object (a
# destruct, with a
# loop and a
# free: destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a free) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (r3->0, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->8 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a destruct routine, part of the 0x8264E cluster, a sibling of
# Destruct_822C97D0, Destruct3_822F5208, and ListUnlink_82B5E7F8, a caller of Free_SizeBucketed_Thunk_8221BE68). 63
# lines, 1 callee (Free_SizeBucketed_Thunk_8221BE68). 10 callers: sub_826928F8, sub_82B3BAC0, sub_832ABDC0, sub_82817D28,
# sub_82768B70, sub_82A45F58, sub_8268A9A0, sub_82647328, sub_82A46EA8, sub_82359BE8 (processes, which
# call this to destruct the object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->4 = a field, a value, ->8 = a field, a value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a destruct (with a
# loop and a
# free) (destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a free) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (r3->0, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (obj->8 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "4" distinguishes it from Destruct_822C97D0 and Destruct3_822F5208.
[entrypoint.functions."0x8264EDF8"]
name = "Destruct4_8264EDF8"

# A return address (a return address, with a constant, the sibling of ReturnPointer_82C3BE78 / ReturnPointer2_82C2EDE8):
# return a pointer (a return address, a particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19784, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (more). It returns a pointer (a
# return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19784, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "return address (a
# return address, with a
# constant: return a pointer (a return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19784, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a return address routine, part of the 0x82C21 cluster, a sibling of
# ReturnPointer_82C3BE78 and ReturnPointer2_82C2EDE8). 10
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# pointer's meaning (r3, a pointer, a
# particular value, a
# particular size / a
# particular allocation, from a fixed .data address + 19784, a
# particular value) isn't resolvable, so the name states the mechanism: a return address (with a
# constant) (return a pointer (a return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19784, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "3" distinguishes it from ReturnPointer_82C3BE78 and ReturnPointer2_82C2EDE8.
[entrypoint.functions."0x82C21F28"]
name = "ReturnPointer3_82C21F28"

# A process (a process, with a loop and a check, the sibling of ProcessAtomicIncrement_821E3950 / ReturnPointer3
# _82C21F28, the caller of Allocate_OrDbgBreak_8221F388 / CopyAssign_RefCounted2_82265160): do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over r7->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r7->0, a
# particular value, a
# particular state / a
# particular result), and (in an atomic increment) increment a value (r7->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, via a global lock, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->8 = a field, a value, a
# particular object), a (r4, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r6, r26, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over r7->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r7->0, a
# particular value, a
# particular state / a
# particular result). In the atomic increment, it increments a value (r7->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, via a global lock, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over r7->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r7->0, a
# particular value, a
# particular state / a
# particular result), and (in an atomic increment) increment a value (r7->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, via a global lock, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82354 cluster, a sibling of
# ProcessAtomicIncrement_821E3950 and ReturnPointer3_82C21F28, a caller of Allocate_OrDbgBreak_8221F388 and
# CopyAssign_RefCounted2_82265160). 168 lines, 2
# callees (Allocate_OrDbgBreak_8221F388, CopyAssign_RefCounted2_82265160). 10 callers: sub_82A185C8, sub_82B3BAC0,
# sub_826928F8, sub_828152D0, sub_8281A568, sub_82A18998, sub_825F45A8, sub_826939A0, sub_82466098, sub_824E3FA0 (processes,
# which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->8 = a field, a value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over r7->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r7->0, a
# particular value, a
# particular state / a
# particular result), and (in an atomic increment) increment a value (r7->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, via a global lock, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "2" distinguishes it from ProcessAtomicIncrement_821E3950.
[entrypoint.functions."0x82354880"]
name = "ProcessAtomic2_82354880"

# A process (a process, with a loop and a check, the sibling of Process12Byte_82601D80 / ProcessAtomic2_82354880, the
# caller of sub_82601D80): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a 12-byte element, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r8, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a->8->0 = b->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, a->8->4 = b->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: a (r3, ->4 = a field, a value, ->8 = a field, a value, ->12 = a field, a
# value, a
# particular object), b (r4, r6, ->0 = a field, a value, ->4 = a field, a
# value, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a 12-byte element, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r8, a
# particular value, a
# particular state / a
# particular result). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a->8->0 = b->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, a->8->4 = b->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a 12-byte element, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r8, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a->8->0 = b->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, a->8->4 = b->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82601 cluster, a sibling of
# Process12Byte_82601D80 and ProcessAtomic2_82354880, a caller of sub_82601D80). 126 lines, 1
# callee (sub_82601D80). 10 callers: sub_829CABD0, sub_82364EB8, sub_8281AE60, sub_82365458, sub_825F86A8, sub_82363150,
# sub_825F5238, sub_8226BE28, sub_8281A568, sub_82921518 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r3, ->4 = a field, a value, ->8 = a field, a value, ->12 = a field, a
# value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (b, r6, ->0 = a field, a value, ->4 = a field, a value, a
# particular object) aren't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a 12-byte element, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r8, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a->8->0 = b->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, a->8->4 = b->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "826016B0" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x826016B0"]
name = "Process12Byte_826016B0"

# A process (a process, with a loop and a check, the sibling of ProcessSize_82BC9860 / Process12Byte_826016B0, the
# caller of DispatchAndAdjustPos_8227BB58 / sub_82BC9860): do a process (a process, a particular process, a particular
# size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, b->16 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: a (r3, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, r29, ->16 = a field, a value, a
# particular object), c (r5, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular state / a
# particular result). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, b->16 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, b->16 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82BC9 cluster, a sibling of
# ProcessSize_82BC9860 and Process12Byte_826016B0, a caller of DispatchAndAdjustPos_8227BB58 and sub_82BC9860). 172
# lines, 2 callees (DispatchAndAdjustPos_8227BB58, sub_82BC9860). 2 callers: sub_82BC9FD0, sub_82BCA340 (processes,
# which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (b, r29, ->16 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r28, a value, a
# particular value, a
# particular size / a
# particular allocation; c, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, b->16 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "82BC9EA0" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82BC9EA0"]
name = "ProcessSize_82BC9EA0"

# A process (a process, with a check and a call, the sibling of ProcessFloat_82BC4328 / ProcessSize_82BC9EA0, the
# caller of MemCpy_SdkRuntime_82CA2C60 / sub_82BC4328): do a process (a process, a particular process, a particular
# size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->40, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_82BC4328, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, f0, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, f10 = f0 * f11, a float multiply, a
# value, a
# particular value, a
# particular size / a
# particular allocation).

# Args: a (r3, r30, ->36 = a field, a value, ->40 = a field, a value, ->44 = a float, a
# particular object), b (r4, r29, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a->40, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a process (sub_82BC4328, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, f0, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the compute, it does a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, f10 = f0 * f11, a float multiply, a
# value, a
# particular value, a
# particular size / a
# particular allocation). So it's a "process (a
# process, with a
# check and a
# call: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->40, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_82BC4328, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, f0, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, f10 = f0 * f11, a float multiply, a
# value, a
# particular value, a
# particular size / a
# particular allocation))" routine (a process routine, part of the 0x8229D cluster, a sibling of
# ProcessFloat_82BC4328 and ProcessSize_82BC9EA0, a caller of MemCpy_SdkRuntime_82CA2C60 and sub_82BC4328). 122
# lines, 2 callees (MemCpy_SdkRuntime_82CA2C60, sub_82BC4328). 2 callers: sub_8229DEE0, sub_82BC3248 (processes,
# which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r30, ->36 = a field, a value, ->40 = a field, a value, ->44 = a float, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (b, r29, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# check and a
# call) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a->40, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_82BC4328, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, f0, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, f10 = f0 * f11, a float multiply, a
# value, a
# particular value, a
# particular size / a
# particular allocation)). The "8229DE10" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8229DE10"]
name = "ProcessFloat_8229DE10"

# A process (a process, with a loop and a check, the sibling of ProcessSpecial_82239E88 / ProcessFloat_8229DE10, the
# caller of ComputeSpecialFunction_82239E88 / ComputeSpecialFunction2_82239F68): do a process (a process, a particular
# process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3, a
# particular value, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float / a
# particular bit, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a bit operation, a
# value, a
# particular value, a
# particular size / a
# particular allocation).

# Args: a (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r5, a value, a
# particular value, a
# particular size / a
# particular allocation), d (r6, r31, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r3, a
# particular value, a
# particular state / a
# particular result). In the compute, it does a compute (a
# compute, a
# particular compute / a
# particular float / a
# particular bit, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a bit operation, a
# value, a
# particular value, a
# particular size / a
# particular allocation). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3, a
# particular value, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float / a
# particular bit, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a bit operation, a
# value, a
# particular value, a
# particular size / a
# particular allocation))" routine (a process routine, part of the 0x821E4 cluster, a sibling of
# ProcessSpecial_82239E88 and ProcessFloat_8229DE10, a caller of ComputeSpecialFunction_82239E88 and
# ComputeSpecialFunction2_82239F68). 412 lines, 2
# callees (ComputeSpecialFunction_82239E88, ComputeSpecialFunction2_82239F68). 1 caller: sub_822D8FF8 (a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r3, a value, a
# particular value, a
# particular size / a
# particular allocation; b, r4, a value, a
# particular value, a
# particular size / a
# particular allocation; c, r5, a value, a
# particular value, a
# particular size / a
# particular allocation; d, r31, a value, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3, a
# particular value, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float / a
# particular bit, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a bit operation, a
# value, a
# particular value, a
# particular size / a
# particular allocation)). The "821E4EB8" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x821E4EB8"]
name = "ProcessSpecial_821E4EB8"

# A process (a process, with a float matrix multiply, the sibling of ProcessMatrix_8229DEE0 / ProcessFloat_8229DE10,
# the caller of MemCpy_SdkRuntime_82CA2C60): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f10 = f12 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f11 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f5 = f9 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r31, ->16 = a float, ->20 = a float, ->24 = a float, ->28 = a
# float, a
# particular object), b (r4, ->0 = a float, ->4 = a float, ->8 = a float, ->16 = a float, ->20 = a
# float, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the compute, it does a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f10 = f12 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f11 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f5 = f9 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# float matrix multiply: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f10 = f12 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f11 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f5 = f9 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x8229D cluster, a sibling of
# ProcessMatrix_8229DEE0 and ProcessFloat_8229DE10, a caller of MemCpy_SdkRuntime_82CA2C60). 356
# lines, 1 callee (MemCpy_SdkRuntime_82CA2C60). 3 callers: sub_82BC3990, sub_8229DEE0, sub_82BBC1A0 (processes,
# which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r31, ->16 = a float, ->20 = a float, ->24 = a float, ->28 = a
# float, a
# particular object, a
# particular value /
# a particular state; b, r4, ->0 = a float, ->4 = a float, ->8 = a float, ->16 = a float, ->20 = a
# float, a
# particular object) isn't resolvable, so the name states the mechanism: a process (with a
# float matrix multiply) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f10 = f12 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f11 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f5 = f9 * f0, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "8229DBD8" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8229DBD8"]
name = "ProcessMatrix_8229DBD8"

# A process (a process, with a loop and a check, the sibling of Process2D_8229DFE8 / ProcessMatrix_8229DBD8, the
# caller of MemCpy_SdkRuntime_82CA2C60 / ProcessMatrix_8229DBD8 / ProcessFloat_8229DE10): do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->251, a
# particular value, a
# particular state / a
# particular result), and (in a copy) copy a value (MemCpy_SdkRuntime_82CA2C60, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r31, a
# particular value, a
# particular size / a
# particular allocation, 64, a
# particular size / a
# particular allocation), a particular copy / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r29, ->32 = a field, a value, ->40 = a field, a value, a
# particular object), b (r4, r30, ->251 = a byte, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, b->251, a
# particular value, a
# particular state / a
# particular result). In the copy, it copies a value (MemCpy_SdkRuntime_82CA2C60, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r31, a
# particular value, a
# particular size / a
# particular allocation, 64, a
# particular size / a
# particular allocation), a particular copy / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->251, a
# particular value, a
# particular state / a
# particular result), and (in a copy) copy a value (MemCpy_SdkRuntime_82CA2C60, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r31, a
# particular value, a
# particular size / a
# particular allocation, 64, a
# particular size / a
# particular allocation), a particular copy / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x8229D cluster, a sibling of
# Process2D_8229DFE8 and ProcessMatrix_8229DBD8, a caller of MemCpy_SdkRuntime_82CA2C60, ProcessMatrix_8229DBD8, and
# ProcessFloat_8229DE10). 156 lines, 3
# callees (MemCpy_SdkRuntime_82CA2C60, ProcessMatrix_8229DBD8, ProcessFloat_8229DE10). 1 caller: sub_8229DFE8 (a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r29, ->32 = a field, a value, ->40 = a field, a value, a
# particular object, a
# particular value /
# a particular state; b, r30, ->251 = a byte, a
# particular object) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->251, a
# particular value, a
# particular state / a
# particular result), and (in a copy) copy a value (MemCpy_SdkRuntime_82CA2C60, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r31, a
# particular value, a
# particular size / a
# particular allocation, 64, a
# particular size / a
# particular allocation), a particular copy / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "8229DEE0" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8229DEE0"]
name = "Process2D_8229DEE0"

# A process (a process, with a check and a call, the sibling of Process2D_8229DEE0, the caller of Process2D_8229DEE0
# / sub_8229E148 / sub_822D8AA0): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->251, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_8229DEE0, on a
# value (a + 44, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, and sub_8229E148, on a
# value (b + 60, a value, a
# particular value, a
# particular size / a
# particular allocation, b + 12, a
# particular value, a
# particular size / a
# particular allocation, b + 24, a
# particular value, a
# particular size / a
# particular allocation, b + 36, a
# particular value, a
# particular size / a
# particular allocation, b + 48, a
# particular value, a
# particular size / a
# particular allocation, b->248, a
# particular value, a
# particular size / a
# particular allocation, b->249, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r30, ->44 = a field, a value, a
# particular object), b (r4, r31, ->12 = a field, a value, ->24 = a field, a value, ->36 = a field, a value, ->48 = a
# field, a value, ->60 = a field, a value, ->248 = a byte, ->249 = a byte, ->251 = a
# byte, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, b->251, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a process (sub_8229DEE0, on a
# value (a + 44, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, and sub_8229E148, on a
# value (b + 60, a value, a
# particular value, a
# particular size / a
# particular allocation, b + 12, a
# particular value, a
# particular size / a
# particular allocation, b + 24, a
# particular value, a
# particular size / a
# particular allocation, b + 36, a
# particular value, a
# particular size / a
# particular allocation, b + 48, a
# particular value, a
# particular size / a
# particular allocation, b->248, a
# particular value, a
# particular size / a
# particular allocation, b->249, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# check and a
# call: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->251, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_8229DEE0, on a
# value (a + 44, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, and sub_8229E148, on a
# value (b + 60, a value, a
# particular value, a
# particular size / a
# particular allocation, b + 12, a
# particular value, a
# particular size / a
# particular allocation, b + 24, a
# particular value, a
# particular size / a
# particular allocation, b + 36, a
# particular value, a
# particular size / a
# particular allocation, b + 48, a
# particular value, a
# particular size / a
# particular allocation, b->248, a
# particular value, a
# particular size / a
# particular allocation, b->249, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x8229D cluster, a sibling of
# Process2D_8229DEE0, a caller of Process2D_8229DEE0, sub_8229E148, and sub_822D8AA0). 101
# lines, 3 callees (Process2D_8229DEE0, sub_8229E148, sub_822D8AA0). No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (a, r30, ->44 = a field, a value, a
# particular object, a
# particular value /
# a particular state; b, r31, ->12 = a field, a value, ->24 = a field, a value, ->36 = a field, a value, ->48 = a
# field, a value, ->60 = a field, a value, ->248 = a byte, ->249 = a byte, ->251 = a
# byte, a
# particular object) isn't resolvable, so the name states the mechanism: a process (with a
# check and a
# call) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->251, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_8229DEE0, on a
# value (a + 44, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, and sub_8229E148, on a
# value (b + 60, a value, a
# particular value, a
# particular size / a
# particular allocation, b + 12, a
# particular value, a
# particular size / a
# particular allocation, b + 24, a
# particular value, a
# particular size / a
# particular allocation, b + 36, a
# particular value, a
# particular size / a
# particular allocation, b + 48, a
# particular value, a
# particular size / a
# particular allocation, b->248, a
# particular value, a
# particular size / a
# particular allocation, b->249, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "2" distinguishes it from Process2D_8229DEE0.
[entrypoint.functions."0x8229DFE8"]
name = "Process2D2_8229DFE8"

# A process (a process, with a loop and a check, the sibling of ProcessIndirect_8229DFE8 / Process2D2_8229DFE8):
# do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over b->220, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r30, ->0 = a field, a value, a
# particular object), b (r4, r31, ->220 = a field, a value, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over b->220, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over b->220, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x8229E cluster, a sibling of
# ProcessIndirect_8229DFE8 and Process2D2_8229DFE8). 90
# lines, 0 callees (it does indirect calls via bctrl, not direct calls). No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (a, r30, ->0 = a field, a value, a
# particular object, a
# particular value /
# a particular state; b, r31, ->220 = a field, a value, a
# particular object) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over b->220, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r11, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "8229E090" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8229E090"]
name = "ProcessIndirect_8229E090"

# A decrement (a decrement, with a store, the sibling of Decrement_8229E090 / ProcessIndirect_8229E090): decrement a
# field (a decrement, a particular decrement, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, obj->84 = obj->84 - 1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, ->84 = a field, a value, a
# particular object), (more). It decrements a field (a
# decrement, a
# particular decrement, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, obj->84 = obj->84 - 1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "decrement a field (a
# decrement, with a
# store: decrement a field (a decrement, a
# particular decrement, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, obj->84 = obj->84 - 1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a decrement routine, part of the 0x8229E cluster, a sibling of
# Decrement_8229E090 and ProcessIndirect_8229E090). 7
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (obj, r3, ->84 = a field, a value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a decrement (with a
# store) (decrement a field (a decrement, a
# particular decrement, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, obj->84 = obj->84 - 1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "8229E130" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8229E130"]
name = "DecrementField_8229E130"

# A process (a process, with a float matrix multiply, the sibling of ProcessMatrix2_8229E848 / ProcessMatrix
# _8229DBD8, the caller of MemCpy_SdkRuntime_82CA2C60 / sub_8229E848 / sub_822D8FF8): do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f11 = f0 * f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f8 = f12 * f10, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f9 * f7, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r31, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r5, r29, a value, a
# particular value, a
# particular size / a
# particular allocation), d (r6, r28, ->0 = a float, ->4 = a float, ->8 = a
# float, a
# particular object), e (r7, ->0 = a float, ->4 = a float, ->8 = a
# float, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the compute, it does a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f11 = f0 * f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f8 = f12 * f10, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f9 * f7, a
# value, a
# particular value, a
# particular size / a
# particular allocation). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# float matrix multiply: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f11 = f0 * f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f8 = f12 * f10, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f9 * f7, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x8229E cluster, a sibling of
# ProcessMatrix2_8229E848 and ProcessMatrix_8229DBD8, a caller of MemCpy_SdkRuntime_82CA2C60, sub_8229E848, and
# sub_822D8FF8). 264 lines, 3
# callees (MemCpy_SdkRuntime_82CA2C60, sub_8229E848, sub_822D8FF8). 2 callers: Process2D2_8229DFE8, sub_82BC3990
# (processes, which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r31, a value, a
# particular value, a
# particular size / a
# particular allocation; b, r30, a value, a
# particular value, a
# particular size / a
# particular allocation; c, r29, a value, a
# particular value, a
# particular size / a
# particular allocation; d, r28, ->0 = a float, ->4 = a float, ->8 = a
# float, a
# particular object; e, r7, ->0 = a float, ->4 = a float, ->8 = a
# float, a
# particular object) isn't resolvable, so the name states the mechanism: a process (with a
# float matrix multiply) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f11 = f0 * f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f8 = f12 * f10, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f9 * f7, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "2" distinguishes it from ProcessMatrix_8229DBD8.
[entrypoint.functions."0x8229E148"]
name = "ProcessMatrix2_8229E148"

# A process (a process, with a float matrix multiply, the sibling of ProcessTransform_8229E148 / ProcessMatrix2
# _8229E148, the caller of MemCpy_SdkRuntime_82CA2C60): do a process (a process, a particular process, a particular
# size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f5 = f9 * f7, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f3 = f6 * f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r31, ->4 = a float, ->8 = a float, a
# particular object), b (r4, r11, ->0 = a float, ->8 = a float, ->16 = a float, ->20 = a float, ->36 = a
# float, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the compute, it does a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f5 = f9 * f7, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f3 = f6 * f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# float matrix multiply: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f5 = f9 * f7, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f3 = f6 * f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x8229E cluster, a sibling of
# ProcessTransform_8229E148 and ProcessMatrix2_8229E148, a caller of MemCpy_SdkRuntime_82CA2C60). 264
# lines, 1 callee (MemCpy_SdkRuntime_82CA2C60). 1 caller: ProcessMatrix2_8229E148 (a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r31, ->4 = a float, ->8 = a float, a
# particular object, a
# particular value /
# a particular state; b, r11, ->0 = a float, ->8 = a float, ->16 = a float, ->20 = a float, ->36 = a
# float, a
# particular object) isn't resolvable, so the name states the mechanism: a process (with a
# float matrix multiply) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float multiply, f5 = f9 * f7, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f3 = f6 * f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a particular store, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "8229E848" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x8229E848"]
name = "ProcessTransform_8229E848"

# A process (a process, with a check and a call, the sibling of ProcessDispatch4_828164E8 / ProcessTransform
# _8229E848, the caller of LinearSearch_82284448 / ProcessFloatToInt_82BAE368): do a process (a process, a particular
# process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->250, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, r29, ->10 = a byte, ->250 = a byte, a
# particular object), c (r5, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, b->250, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# check and a
# call: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->250, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x822D8 cluster, a sibling of
# ProcessDispatch4_828164E8 and ProcessTransform_8229E848, a caller of LinearSearch_82284448 and
# ProcessFloatToInt_82BAE368). 258 lines, 2
# callees (LinearSearch_82284448, ProcessFloatToInt_82BAE368). 1 caller: Process2D2_8229DFE8 (a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r30, a value, a
# particular value, a
# particular size / a
# particular allocation; b, r29, ->10 = a byte, ->250 = a byte, a
# particular object, a
# particular value /
# a particular state; c, r28, a value, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a process (with a
# check and a
# call) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, b->250, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r9, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "5" distinguishes it from ProcessDispatch4_828164E8.
[entrypoint.functions."0x822D8AA0"]
name = "ProcessDispatch5_822D8AA0"

# A dispatch (a dispatch, with a switch, the sibling of ProcessDispatch5_822D8AA0 / ProcessSpecial_821E4EB8, the
# caller of ProcessSpecial_821E4EB8): dispatch a value (a dispatch, a particular dispatch / a
# particular case, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a switch) do a switch (a
# switch, a
# particular switch, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a
# value (c, a value, a
# particular value, a
# particular size / a
# particular allocation), with cases 0-11, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a call) call a process (ProcessSpecial_821E4EB8, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r31, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r5, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It dispatches a value (a
# dispatch, a
# particular dispatch / a
# particular case, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the switch, it does a switch (a
# switch, a
# particular switch, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a
# value (c, a value, a
# particular value, a
# particular size / a
# particular allocation), with cases 0-11, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the call, it calls a process (ProcessSpecial_821E4EB8, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "dispatch a value (a
# dispatch, with a
# switch: dispatch a value (a dispatch, a
# particular dispatch / a
# particular case, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a switch) do a switch (a
# switch, a
# particular switch, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a
# value (c, a value, a
# particular value, a
# particular size / a
# particular allocation), with cases 0-11, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a call) call a process (ProcessSpecial_821E4EB8, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a dispatch routine, part of the 0x822D8 cluster, a sibling of
# ProcessDispatch5_822D8AA0 and ProcessSpecial_821E4EB8, a caller of ProcessSpecial_821E4EB8). 304
# lines, 1 callee (ProcessSpecial_821E4EB8). 2 callers: sub_82BBBDB0, ProcessMatrix2_8229E148 (processes,
# which call this to dispatch the value, a
# particular size / a
# particular allocation, a
# particular operation). The value's meaning (a, r31, a value, a
# particular value, a
# particular size / a
# particular allocation; b, r4, a value, a
# particular value, a
# particular size / a
# particular allocation; c, r5, a value, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a dispatch (with a
# switch) (dispatch a value (a dispatch, a
# particular dispatch / a
# particular case, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a switch) do a switch (a
# switch, a
# particular switch, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, on a
# value (c, a value, a
# particular value, a
# particular size / a
# particular allocation), with cases 0-11, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a call) call a process (ProcessSpecial_821E4EB8, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, b, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "6" distinguishes it from ProcessDispatch5_822D8AA0.
[entrypoint.functions."0x822D8FF8"]
name = "ProcessDispatch6_822D8FF8"

# A list manipulation (a list manipulation, with a check and a store, the sibling of ListUnlink2_82B5E7F8 / ListUnlink
# _82B5E7F8): do a list manipulation (a
# list manipulation, a particular list manipulation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, node->0 = r11->8, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->4 = node->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->8 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, node->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: node (r4, ->0 = a field, a value, ->4 = a field, a value, ->8 = a field, a
# value, a
# particular object), (more). It does a list manipulation (a
# list manipulation, a
# particular list manipulation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, node->0 = r11->8, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->4 = node->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->8 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, node->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "list manipulation (a
# list manipulation, with a
# check and a
# store: do a list manipulation (a
# list manipulation, a particular list manipulation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, node->0 = r11->8, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->4 = node->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->8 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, node->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a list manipulation routine, part of the 0x82B5E cluster, a sibling of
# ListUnlink2_82B5E7F8 and ListUnlink_82B5E7F8). 71
# lines, 0 callees. 2 callers: sub_82B5D930, ProcessChain4_82B5E558 (processes, which
# call this to do the list manipulation, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (node, r4, ->0 = a field, a value, ->4 = a field, a value, ->8 = a field, a
# value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a list manipulation (with a
# check and a
# store) (do a list manipulation (a
# list manipulation, a
# particular list manipulation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, node->0 = r11->8, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->4 = node->4, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r10->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r11->8 = node, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, node->4 = r11, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "2" distinguishes it from ListUnlink_82B5E7F8.
[entrypoint.functions."0x82B5E878"]
name = "ListUnlink2_82B5E878"

# A construct (a construct, with a store and a call, the sibling of Construct8_82A3B330 / ConstructString_8217AB58,
# the caller of sub_82A3B330): construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->544 = -1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->548 = 1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a call) call a process (sub_82A3B330, on a
# value (obj + 16, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->0 = a field, a value, ->16 = a field, a value, ->544 = a field, a value, ->548 = a field, a
# value, ->549 = a byte, ->550 = a byte, ->552 = a field, a value, ->556 = a field, a value, ->560 = a field, a
# value, ->564 = a field, a value, a
# particular object), (more). It constructs an object (a
# construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->544 = -1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->548 = 1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the call, it calls a process (sub_82A3B330, on a
# value (obj + 16, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "construct an object (a
# construct, with a
# store and a
# call: construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->544 = -1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->548 = 1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a call) call a process (sub_82A3B330, on a
# value (obj + 16, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a construct routine, part of the 0x82814 cluster, a sibling of
# Construct8_82A3B330 and ConstructString_8217AB58, a caller of sub_82A3B330). 92
# lines, 1 callee (sub_82A3B330). 2 callers: sub_8281AE60, sub_8281A568 (processes, which
# call this to construct the object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->0 = a field, a value, ->16 = a field, a value, ->544 = a
# field, a value, ->548 = a field, a value, ->549 = a byte, ->550 = a byte, ->552 = a field, a value, ->556 = a
# field, a value, ->560 = a field, a value, ->564 = a field, a value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a construct (with a
# store and a
# call) (construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->544 = -1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->548 = 1, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a call) call a process (sub_82A3B330, on a
# value (obj + 16, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "82814A38" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82814A38"]
name = "Construct8_82814A38"

# A process (a process, with a float operation, the sibling of ProcessFloatOp_82814A38 / Construct8_82814A38):
# do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float add, f10 = f2 + f4, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f11 = f1 + f3, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a float sub, f13 = f12 - f4, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f7 = f8 - f3, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f9 - f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a float abs, f5 = fabs(f7), a
# value, a
# particular value, a
# particular size / a
# particular allocation, f12 = f12 - f10, a
# value, a
# particular value, a
# particular size / a
# particular allocation).

# Args: a (r3, ->48 = a field, a value, a
# particular object), f1 (a float), f2 (a float), f3 (a float), f4 (a float), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the compute, it does a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float add, f10 = f2 + f4, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f11 = f1 + f3, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a float sub, f13 = f12 - f4, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f7 = f8 - f3, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f9 - f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a float abs, f5 = fabs(f7), a
# value, a
# particular value, a
# particular size / a
# particular allocation, f12 = f12 - f10, a
# value, a
# particular value, a
# particular size / a
# particular allocation). So it's a "process (a
# process, with a
# float operation: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float add, f10 = f2 + f4, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f11 = f1 + f3, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a float sub, f13 = f12 - f4, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f7 = f8 - f3, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f9 - f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a float abs, f5 = fabs(f7), a
# value, a
# particular value, a
# particular size / a
# particular allocation, f12 = f12 - f10, a
# value, a
# particular value, a
# particular size / a
# particular allocation))" routine (a process routine, part of the 0x82814 cluster, a sibling of
# ProcessFloatOp_82814A38 and Construct8_82814A38). 234
# lines, 0 callees. 3 callers: sub_82841120, sub_82815E00, sub_828152D0 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r3, ->48 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (f1, a float; f2, a float; f3, a float; f4, a float) aren't
# resolvable, so the name states the mechanism: a process (with a
# float operation) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a compute) do a compute (a
# compute, a
# particular compute / a
# particular float, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, a float add, f10 = f2 + f4, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f11 = f1 + f3, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a float sub, f13 = f12 - f4, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f7 = f8 - f3, a
# value, a
# particular value, a
# particular size / a
# particular allocation, f6 = f9 - f13, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a float abs, f5 = fabs(f7), a
# value, a
# particular value, a
# particular size / a
# particular allocation, f12 = f12 - f10, a
# value, a
# particular value, a
# particular size / a
# particular allocation)). The "82814BA8" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82814BA8"]
name = "ProcessFloatOp_82814BA8"

# A process (a process, with a loop and a check, the sibling of ProcessVector_82814F40 / ProcessFloatOp_82814BA8):
# do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (obj->64, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->552, a
# particular value, a
# particular state / a
# particular result), and (in a vector operation) do a vector operation (a
# vector operation, a particular vector operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, lvx128, a
# particular size / a
# particular allocation, a
# particular operation).

# Args: obj (r3, ->552 = a field, a value, ->64 = a field, a value, a
# particular object), a (r4, ->0 = a byte, ->1 = a byte, ->2 = a byte, ->3 = a
# byte, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (obj->64, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->552, a
# particular value, a
# particular state / a
# particular result). In the vector operation, it does a vector operation (a
# vector operation, a
# particular vector operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, lvx128, a
# particular size / a
# particular allocation, a
# particular operation). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (obj->64, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->552, a
# particular value, a
# particular state / a
# particular result), and (in a vector operation) do a vector operation (a
# vector operation, a particular vector operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, lvx128, a
# particular size / a
# particular allocation, a
# particular operation))" routine (a process routine, part of the 0x82814 cluster, a sibling of
# ProcessVector_82814F40 and ProcessFloatOp_82814BA8). 269
# lines, 0 callees. 2 callers: sub_828152D0, sub_823F80F0 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r3, ->552 = a field, a value, ->64 = a field, a value, a
# particular object, a
# particular value /
# a particular state; a, r4, ->0 = a byte, ->1 = a byte, ->2 = a byte, ->3 = a
# byte, a
# particular object) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (obj->64, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->552, a
# particular value, a
# particular state / a
# particular result), and (in a vector operation) do a vector operation (a
# vector operation, a
# particular vector operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, lvx128, a
# particular size / a
# particular allocation, a
# particular operation)). The "82814F40" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82814F40"]
name = "ProcessVector_82814F40"

# A process (a process, with a loop and a check, the sibling of ProcessParse_82814F40 / ProcessVector_82814F40, the
# caller of ScanLineLength_82B477F0 / ProcessLine_82B470D0 / Destruct4_8264EDF8): do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, b->36, a
# value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (ScanLineLength_82B477F0, on a
# value (b, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: a (r3, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, r31, ->0 = a field, a value, ->36 = a field, a
# value, a
# particular object), c (r6, r27, a value, a
# particular value, a
# particular size / a
# particular allocation), d (r7, r26, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, b->36, a
# value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a
# particular value, a
# particular state / a
# particular result). In the call, it calls a process (ScanLineLength_82B477F0, on a
# value (b, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, b->36, a
# value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (ScanLineLength_82B477F0, on a
# value (b, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82815 cluster, a sibling of
# ProcessParse_82814F40 and ProcessVector_82814F40, a caller of ScanLineLength_82B477F0, ProcessLine_82B470D0, and
# Destruct4_8264EDF8). 596 lines, 19
# callees (ConstructRefCounted_8222CF18, CopyAssign_RefCounted2_82265160, Ctype_Pred_Bit2_82CA6B10, Destruct4_8264EDF8,
# DestructRefCounted_82214F08, HashLookupCompare_823F9620, HashLookup_823F74D0, ParseLine_82B47218,
# ProcessAtomic2_82354880, ProcessDispatch4_828164E8, ProcessHashLookup2_823F75A0, ProcessLineParse_82B47148,
# ProcessLine_82B470D0, ProcessVector2_8223BD98, ScanLineLength_82B477F0, ScanQuoteParity_82B47A40,
# SkipCtypeChars_82B47610, ProcessFloatOp_82814BA8, ProcessVector_82814F40). No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# object's class (a, r30, a value, a
# particular value, a
# particular size / a
# particular allocation; b, r31, ->0 = a field, a value, ->36 = a field, a
# value, a
# particular object, a
# particular value /
# a particular state; c, r27, a value, a
# particular value, a
# particular size / a
# particular allocation; d, r26, a value, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over b->0, a value, a
# particular value, a
# particular size / a
# particular allocation, b->36, a
# value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a value (r31, a value, a
# particular value, a
# particular size / a
# particular allocation), a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (ScanLineLength_82B477F0, on a
# value (b, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "828152D0" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x828152D0"]
name = "ProcessParse_828152D0"

# A construct (a construct, with a loop and a call, the sibling of Construct9_82A3B1E8 / Construct8_82814A38, the
# caller of ProcessVector_82A3B1E8): construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->16, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a call) call a process (ProcessVector_82A3B1E8, on a
# value (obj + 16, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store a value (obj->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->0 = a field, a value, ->16 = a field, a value, a
# particular object), (more). It constructs an object (a
# construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->16, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the call, it calls a process (ProcessVector_82A3B1E8, on a
# value (obj + 16, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). It stores a value (obj->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "construct an object (a
# construct, with a
# loop and a
# call: construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->16, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a call) call a process (ProcessVector_82A3B1E8, on a
# value (obj + 16, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store a value (obj->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a construct routine, part of the 0x82A3B cluster, a sibling of
# Construct9_82A3B1E8 and Construct8_82814A38, a caller of ProcessVector_82A3B1E8). 426
# lines, 1 callee (ProcessVector_82A3B1E8). 9 callers: sub_82A608F8, Construct8_82814A38, sub_8235B570, sub_8235B708,
# sub_823FF000, sub_82900B28, sub_82900688, sub_82410D98, sub_82817748 (processes, which
# call this to construct the object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->0 = a field, a value, ->16 = a field, a value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a construct (with a
# loop and a
# call) (construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->16, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a call) call a process (ProcessVector_82A3B1E8, on a
# value (obj + 16, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store a value (obj->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "9" distinguishes it from Construct8_82814A38.
[entrypoint.functions."0x82A3B330"]
name = "Construct9_82A3B330"

# A string operation (a string operation, with a loop and a check, the sibling of StringCopyN_822CD828 / Construct9
# _82A3B330): do a string operation (a
# string operation, a particular string operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over n, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, src->0, a
# particular value, a
# particular state / a
# particular result), and (in a copy) copy a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, dst->0 = src->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and store a value (dst->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: dst (r3, r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string), src (r4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string), n (r5, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a string operation (a
# string operation, a
# particular string operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over n, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, src->0, a
# particular value, a
# particular state / a
# particular result). In the copy, it copies a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, dst->0 = src->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). It stores a value (dst->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "string operation (a
# string operation, with a
# loop and a
# check: do a string operation (a
# string operation, a particular string operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over n, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, src->0, a
# particular value, a
# particular state / a
# particular result), and (in a copy) copy a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, dst->0 = src->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and store a value (dst->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a string operation routine, part of the 0x822CD cluster, a sibling of
# StringCopyN_822CD828 and Construct9_82A3B330). 56
# lines, 0 callees. 10 callers: sub_82E7DCA0, sub_82E7D8F8, sub_82D3AE00, sub_82C5BCC0, sub_82D51970, sub_82BA1048,
# sub_827FFE08, sub_8231AB20, sub_82BF9040, sub_82D114F8 (processes, which
# call this to do the string operation, a
# particular size / a
# particular allocation, a
# particular operation). The string's meaning (dst, r11, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string; src, r4, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular string; n, r5, a value, a
# particular value, a
# particular size / a
# particular allocation) isn't resolvable, so the name states the mechanism: a string operation (with a
# loop and a
# check) (do a string operation (a
# string operation, a
# particular string operation, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over n, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, src->0, a
# particular value, a
# particular state / a
# particular result), and (in a copy) copy a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, dst->0 = src->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and store a value (dst->0 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "822CD828" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x822CD828"]
name = "StringCopyN_822CD828"

# A process (a process, with a loop and a check, the sibling of ProcessObject_82BC2860 / StringCopyN_822CD828, the
# caller of StringCopyN_822CD828): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3->42, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->8 = a field, a value, ->12 = a field, a value, ->32 = a field, a value, ->36 = a field, a
# value, a
# particular object), a (r5, r28, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r6, r29, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r7, r27, a value, a
# particular value, a
# particular size / a
# particular allocation), d (r8, r26, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r3->42, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3->42, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82BAD cluster, a sibling of
# ProcessObject_82BC2860 and StringCopyN_822CD828, a caller of StringCopyN_822CD828). 175
# lines, 1 callee (StringCopyN_822CD828). 1 caller: sub_82BC2860 (a
# process, which calls this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->8 = a field, a value, ->12 = a field, a value, ->32 = a
# field, a value, ->36 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r28, a value, a
# particular value, a
# particular size / a
# particular allocation; b, r29, a value, a
# particular value, a
# particular size / a
# particular allocation; c, r27, a value, a
# particular value, a
# particular size / a
# particular allocation; d, r26, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3->42, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r10, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "82BAD8A8" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82BAD8A8"]
name = "ProcessObject_82BAD8A8"

# A return address (a return address, with a constant, the sibling of ReturnPointer4_82C2AA48 / ReturnPointer3
# _82C21F28): return a pointer (a return address, a particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19848, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: (more). It returns a pointer (a
# return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19848, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "return address (a
# return address, with a
# constant: return a pointer (a return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19848, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a return address routine, part of the 0x82C2A cluster, a sibling of
# ReturnPointer4_82C2AA48 and ReturnPointer3_82C21F28). 5
# lines, 0 callees. No
# direct callers (it's called via a vtable / it is registered-but-uncalled, part of the game's subsystems). The
# pointer's meaning (r3, a pointer, a
# particular value, a
# particular size / a
# particular allocation, from a fixed .data address + 19848, a
# particular value) isn't resolvable, so the name states the mechanism: a return address (with a
# constant) (return a pointer (a return address, a
# particular return / a
# particular pointer, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, from a fixed .data address + 19848, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "4" distinguishes it from ReturnPointer3_82C21F28.
[entrypoint.functions."0x82C2AA48"]
name = "ReturnPointer4_82C2AA48"

# A process (a process, with a check and a call, the sibling of ProcessRangeCheck_82A0F120 / ReturnPointer4
# _82C2AA48, the caller of RaiseIfOutOfRange_82461DA0): do a process (a process, a particular process, a particular
# size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (RaiseIfOutOfRange_82461DA0, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->8 = r11 - c, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: a (r3, a value, a
# particular value, a
# particular size / a
# particular allocation), b (r4, a value, a
# particular value, a
# particular size / a
# particular allocation), obj (r5, r31, ->8 = a field, a value, a
# particular object), c (r8, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, a, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a process (RaiseIfOutOfRange_82461DA0, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->8 = r11 - c, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# check and a
# call: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (RaiseIfOutOfRange_82461DA0, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->8 = r11 - c, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82A0F cluster, a sibling of
# ProcessRangeCheck_82A0F120 and ReturnPointer4_82C2AA48, a caller of RaiseIfOutOfRange_82461DA0). 85
# lines, 1 callee (RaiseIfOutOfRange_82461DA0). 6 callers: sub_82A0F120, sub_82874358, sub_827D90C0, Build
# Structure_82A118B0, sub_828740A8, sub_827D7F00 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->8 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (a, r3, a value, a
# particular value, a
# particular size / a
# particular allocation; b, r4, a value, a
# particular value, a
# particular size / a
# particular allocation; c, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# check and a
# call) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, a, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (RaiseIfOutOfRange_82461DA0, on a
# value (a, a value, a
# particular value, a
# particular size / a
# particular allocation, c, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->8 = r11 - c, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "82A0F290" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82A0F290"]
name = "ProcessRangeCheck_82A0F290"

# A process (a process, with a loop and a check, the sibling of ProcessField_82B44360 / ProcessRangeCheck
# _82A0F290, the caller of sub_82B44360): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (obj->28, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->40, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_82B44360, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, obj->20, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->20 = a field, a value, ->28 = a field, a value, ->40 = a field, a
# value, a
# particular object), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (obj->28, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->40, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a process (sub_82B44360, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, obj->20, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# loop and a
# check: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (obj->28, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->40, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_82B44360, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, obj->20, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82B51 cluster, a sibling of
# ProcessField_82B44360 and ProcessRangeCheck_82A0F290, a caller of sub_82B44360). 135
# lines, 1 callee (sub_82B44360). 3 callers: sub_82B50E70, sub_82B51188, sub_82B51080 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->20 = a field, a value, ->28 = a field, a value, ->40 = a
# field, a value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a process (with a
# loop and a
# check) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over a value (obj->28, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular loop / a
# particular range, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->40, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (sub_82B44360, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, obj->20, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result)). The "82B51380" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82B51380"]
name = "ProcessField_82B51380"

# A process (a process, with a check and a call, the sibling of ProcessVirtual_821E51D0 / ProcessField_82B51380, the
# caller of sub_821E51D0): do a process (a process, a particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r31->68 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: a (r3, r28, ->16 = a field, a value, a
# particular object), b (r5, r29, a value, a
# particular value, a
# particular size / a
# particular allocation), c (r6, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r3, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r31->68 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# check and a
# call: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r31->68 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82227 cluster, a sibling of
# ProcessVirtual_821E51D0 and ProcessField_82B51380, a caller of sub_821E51D0). 60
# lines, 1 callee (sub_821E51D0). 10 callers: sub_82A2DEA8, sub_821EC538, sub_822A0F58, sub_82A30568, sub_82BF51D8,
# sub_82A36648, sub_82A2D408, sub_82A2C600, sub_8222B4C0, sub_82A2F750 (processes, which
# call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (a, r28, ->16 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (b, r29, a value, a
# particular value, a
# particular size / a
# particular allocation; c, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# check and a
# call) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, r3, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a function (a
# call, a
# particular call / a
# particular function, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, via an indirect call, bctrl, on a
# value (r11, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular function, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r31->68 = r10, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "82227940" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82227940"]
name = "ProcessVirtual_82227940"

# A process (a process, with a check and a call, the sibling of ProcessError_822F2020 / ProcessVirtual_82227940, the
# caller of SetString_822F2020 / sub_826C3EF0 / sub_822F1F00): do a process (a process, a particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, n, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (SetString_822F2020, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r1->112 = r29, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->8 = a field, a value, a
# particular object), n (r4, r30, a value, a
# particular value, a
# particular size / a
# particular allocation), (more). It does a process (a
# process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, n, a
# particular value, a
# particular state / a
# particular result). In the call, it calls a process (SetString_822F2020, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r1->112 = r29, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "process (a
# process, with a
# check and a
# call: do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, n, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (SetString_822F2020, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r1->112 = r29, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a process routine, part of the 0x82A0F cluster, a sibling of
# ProcessError_822F2020 and ProcessVirtual_82227940, a caller of SetString_822F2020, sub_826C3EF0, and sub_822F1F00).
# 81 lines, 5
# callees (SetString_822F2020, sub_82171810, sub_822F1F00, sub_826C3EF0, sub_826C3FA8). 2 callers: sub_82A0F528,
# sub_82A0F328 (processes, which call this to do the process, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->8 = a field, a value, a
# particular object, a
# particular value /
# a particular state) and the value's meaning (n, r30, a value, a
# particular value, a
# particular size / a
# particular allocation) aren't resolvable, so the name states the mechanism: a process (with a
# check and a
# call) (do a process (a process, a
# particular process, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, n, a
# particular value, a
# particular state / a
# particular result), and (in a call) call a process (SetString_822F2020, on a
# value (r3, a value, a
# particular value, a
# particular size / a
# particular allocation, r4, a
# particular value, a
# particular size / a
# particular allocation), a particular call / a
# particular process, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, r1->112 = r29, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "82A0F630" refers to the address (a
# particular address, a
# particular size / a
# particular allocation).
[entrypoint.functions."0x82A0F630"]
name = "ProcessError_82A0F630"

# A destruct (a destruct, with a loop and a check, the sibling of Destruct5_82B41570 / Destruct4_8264EDF8, the caller
# of Free_SizeBucketed_Thunk_8221BE68): destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->28, a
# particular value, a
# particular state / a
# particular result), and (in a free) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (r31->4, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (r31->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r31->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r31->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, r30, ->0 = a field, a value, ->4 = a field, a value, ->20 = a field, a value, ->24 = a field, a
# value, ->28 = a field, a value, a
# particular object), (more). It destructs an object (a
# destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the loop, it loops over a range (a
# loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->28, a
# particular value, a
# particular state / a
# particular result). In the free, it frees a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (r31->4, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). It stores values (r31->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r31->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r31->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "destruct an object (a
# destruct, with a
# loop and a
# check: destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->28, a
# particular value, a
# particular state / a
# particular result), and (in a free) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (r31->4, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (r31->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r31->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r31->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a destruct routine, part of the 0x82B41 cluster, a sibling of
# Destruct5_82B41570 and Destruct4_8264EDF8, a caller of Free_SizeBucketed_Thunk_8221BE68). 71
# lines, 1 callee (Free_SizeBucketed_Thunk_8221BE68). 4 callers: sub_82B42080, sub_82B412B8, sub_82B42518,
# sub_82B41570 (processes, which call this to destruct the object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r30, ->0 = a field, a value, ->4 = a field, a value, ->20 = a
# field, a value, ->24 = a field, a value, ->28 = a field, a value, a
# particular object, a
# particular value /
# a particular state) isn't resolvable, so the name states the mechanism: a destruct (with a
# loop and a
# check) (destruct an object (a destruct, a
# particular destruct / a
# particular release, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a loop) loop over a range (a loop, a
# particular loop / a
# particular range, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result, over obj->0, a value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, obj->28, a
# particular value, a
# particular state / a
# particular result), and (in a free) free a value (Free_SizeBucketed_Thunk_8221BE68, on a
# value (r31->4, a value, a
# particular value, a
# particular size / a
# particular allocation), a particular free / a
# particular size, a
# particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and store values (r31->20 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r31->24 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, r31->4 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "5" distinguishes it from Destruct4_8264EDF8.
[entrypoint.functions."0x82B414F8"]
name = "Destruct5_82B414F8"

# A construct (a construct, with a check and a store, the sibling of Construct10_82B42518 / Destruct5_82B414F8, the
# caller of ClampAndUpdate_822F6380): construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, src->0, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0 = src->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->28 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result).

# Args: obj (r3, r31, ->0 = a field, a value, ->8 = a field, a value, ->24 = a field, a value, ->28 = a field, a
# value, a
# particular object), src (r4, r30, ->0 = a field, a value, ->4 = a field, a
# value, a
# particular object), (more). It constructs an object (a
# construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result). In the check, it checks a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, src->0, a
# particular value, a
# particular state / a
# particular result). In the store, it stores a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0 = src->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->28 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result). So it's a "construct an object (a
# construct, with a
# check and a
# store: construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, src->0, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0 = src->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->28 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result))" routine (a construct routine, part of the 0x82B42 cluster, a sibling of
# Construct10_82B42518 and Destruct5_82B414F8, a caller of ClampAndUpdate_822F6380). 78
# lines, 1 callee (ClampAndUpdate_822F6380). 3 callers: sub_82B42390, sub_82B42518, sub_82B412B8 (processes,
# which call this to construct the object, a
# particular size / a
# particular allocation, a
# particular operation). The object's class (obj, r31, ->0 = a field, a value, ->8 = a field, a value, ->24 = a
# field, a value, ->28 = a field, a value, a
# particular object, a
# particular value /
# a particular state; src, r30, ->0 = a field, a value, ->4 = a field, a value, a
# particular object) isn't resolvable, so the name states the mechanism: a construct (with a
# check and a
# store) (construct an object (a construct, a
# particular construct, a particular size / a
# particular allocation, a
# particular operation, a
# particular state / a
# particular result), and (in a check) check a value (a value, a
# particular value, a
# particular size / a
# particular allocation, src->0, a
# particular value, a
# particular state / a
# particular result), and (in a store) store a value (a
# value, a
# particular value, a
# particular size / a
# particular allocation, obj->0 = src->0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->24 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->28 = 7, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result, obj->8 = 0, a store, a
# value, a
# particular value, a
# particular size / a
# particular allocation, a
# particular state / a
# particular result)). The "10" distinguishes it from Construct9_82A3B330.
[entrypoint.functions."0x82B42918"]
name = "Construct10_82B42918"

# Returns the constant 2048 (0x800). Tiny leaf function: `r3 = 2048; return;`.
# Part of the 0x82323 cluster (sibling of the 0x82323E38 allocation helper below it).
# No direct callers found in the recompiled code (likely reached via a vtable or the
# function-pointer table). Sibling of ReturnConstant_16384_82A145F0 / ReturnConstant_4096_82A11F28.
[entrypoint.functions."0x82323C60"]
name = "ReturnConstant_2048_82323C60"

# Processes a small value buffer (e.g. an RGB/ARGB or 4-field struct): reads the element size
# flag at obj->24, picks the base pointer (obj->4 when size==8, else obj+4), bounds-checks the
# index, and computes the element address from obj->20 (stride, shifted left by 1) + offset.
# On the slow/error paths it calls Buffer_CopyOrZero_Error_82CA3730 and
# ProcessValue_FlagNonZero_8217A5E0, then dispatches to sub_822F7E28 / sub_82CD11D0.
# Part of the 0x822F6 cluster (sibling of ProcessValue_FlagNonZero_8217A5E0's family).
# 210 lines, 4 callees. 2 callers: sub_822F5258, LoadString_FastOrSlow_823725E8.
[entrypoint.functions."0x822F6CB0"]
name = "ProcessBuffer_822F6CB0"

# Allocates virtual memory by calling the kernel's NtAllocateVirtualMemory. Takes the size in
# r3 and a (pre-zeroed) address-out pointer in r4; on success returns the new base address,
# and on failure (status < 0) reports the error via sub_82CC1C38 and returns 0.
# Part of the 0x82CC0 memory/kernel cluster. 50 lines, callees: __imp__NtAllocateVirtualMemory,
# sub_82CC1C38 (error path). 10 callers, incl. ProcessAllocate_8322FB30 and ComputeClamped2_83230D00
# (the 0x8322F/0x83230 pool/heap cluster that sits on top of this allocator).
[entrypoint.functions."0x82CC0CA0"]
name = "VirtualAllocate_82CC0CA0"

# Destructor for an object that owns a 20-byte member at offset 0. It re-points the member's
# vtable/first field to the default 0x82A11E68 target, calls Destruct20Byte_832156E0 on it, and
# then frees the owning object (Free_SizeBucketed_Thunk_8221BE68) only when the low bit of the
# `flag` argument (r4) is set.
# Part of the 0x82A11 cluster (sibling of ReturnConstant_1024_82A11E60). 55 lines, 2 callees
# (Destruct20Byte_832156E0, Free_SizeBucketed_Thunk_8221BE68). No direct callers found in the
# recompiled code (likely invoked via a vtable / function-pointer table).
[entrypoint.functions."0x82A11E68"]
name = "Destruct6_82A11E68"

# Reads a 4-byte element from a buffered stream object. Checks that the remaining count at b->20
# is at least 4; if so, it copies the 4 bytes at b->12 out to a local scratch area (r1+84),
# advances the read pointer (b->12) and the base (b->4) past those bytes, then dispatches the
# decoded element to sub_82A1B480 / sub_83211D48 based on further state.
# Part of the 0x83213 cluster. 128 lines, 2 callees (sub_82A1B480, sub_83211D48). No direct
# callers found in the recompiled code (likely reached via a vtable / function-pointer table).
[entrypoint.functions."0x83213650"]
name = "ProcessRead_83213650"

# Constructor for an object that contains a 20-byte member. It calls Construct20Byte_83215640 to
# initialize the 20-byte part, then sets obj->0 to a fixed target pointer (0x83215A28 region) and
# obj->16 to a constant float loaded from a .data table. The pair (obj->0, obj->16) is the same
# layout the destructor Destruct6_82A11E68 tears down.
# Part of the 0x83215 cluster (sibling of Construct20Byte_83215640 / Destruct20Byte_832156E0).
# 55 lines, 1 callee (Construct20Byte_83215640). 3 callers: sub_83219588, ProcessTreeNode_83218C10,
# sub_83218630.
[entrypoint.functions."0x83215A28"]
name = "Construct11_83215A28"

# Grows a growable buffer of 4-byte elements. obj->4 holds the current capacity; if it is already
# >= n (r4) it just returns. Otherwise it computes the new byte size as (n << 2) clamped to 65535,
# allocates a fresh block (Allocate_OrDbgBreak_8221F388), copies the existing obj->8 bytes over with
# MemCpy_SdkRuntime_82CA2C60, frees the old block (Free_Thunk_824FE010), and updates the pointer.
# Part of the 0x82C21 cluster (sibling of ProcessVcall_82C21F38 / ReturnPointer3_82C21F28).
# 84 lines, 3 callees. 10 callers (the 0x82C1x/0x82C2x/0x82C3x container cluster).
[entrypoint.functions."0x82C21C90"]
name = "ProcessGrow_82C21C90"

# Compacts a growable array of pointers in place. obj->4 is the array base and obj->12 the element
# count; it walks the array and copies every non-zero element to the front (in order), then writes
# back the new (reduced) count into obj->12. This is the "defragment / remove nulls" step that
# container code runs before shrinking. Pure loop, no calls.
# Part of the 0x82C23 cluster (sibling of ProcessGrow_82C21C90). 50 lines, 0 callees. 10 callers
# (the 0x82C1x/0x82C2x/0x82C3x container cluster).
[entrypoint.functions."0x82C23DD0"]
name = "ArrayCompact_82C23DD0"

# Bounds-checked element accessor for an 8-byte-element array. obj->28 is the element count and
# obj->12 the array base; for a valid index (r4 - 1 < obj->28) it returns the pointer to that
# element (obj->12 + (r4 << 3) - 8). For an out-of-range / sentinel index it falls through to a
# fallback path that computes a small value from obj->7 / obj->16 and a constant float.
# Part of the 0x821E1 cluster (sibling of ProcessAtomicIncrement_821E3950 / ConstructString_8217AB58's
# element-access family). ~100 lines, 0 callees. 10 callers (incl. DispatchValue_821CF230 and the
# 0x82BBx/0x82BCx container cluster).
[entrypoint.functions."0x821E1498"]
name = "ElementAccess_821E1498"

# Removes / erases an entry from a ref-counted chain. It looks the entry up with
# WalkChainFind_83239FA8; on a hit (low byte of the result non-zero) it zeroes the found node
# (obj->0 / obj->4), releases the held reference (Release_RefCounted_829FF648), and on the
# rebuild path re-allocates (Allocate_OrDbgBreak_8221F388) and reconstructs the 32-byte vector
# (Construct32ByteVector2_8323C7F0), adjusting ref counts via ProcessRefCount_83225538.
# Part of the 0x82A0C cluster (sibling of Construct32ByteCopy_82A0C1B8). 239 lines, 5 callees.
# 1 caller: StateDispatch4_83220888.
[entrypoint.functions."0x82A0C2E0"]
name = "ProcessErase_82A0C2E0"

# Reads / decodes a 4-byte element from a buffered stream. It first invokes a vtable method
# (b->0->8, called through the method pointer), then checks that the remaining count (b->20) is
# at least 4 and copies the 4 bytes at b->12 out to a local scratch area, advancing the read and
# base pointers past the element. The decoded element is then dispatched to
# ProcessFloatDataDispatch_8323EB40 / sub_82A1B480 (the same sub_82A1B480 used by
# ProcessRead_83213650).
# Part of the 0x82A0C cluster (sibling of ProcessErase_82A0C2E0 / Construct32ByteCopy_82A0C1B8).
# 244 lines, 2 callees. No direct callers found in the recompiled code (likely reached via a
# vtable / function-pointer table).
[entrypoint.functions."0x82A0C528"]
name = "ProcessRead2_82A0C528"

# Allocates and initializes a heap array of 20-byte elements. Given an element count n (r4), it
# computes the byte size as n * 20 (the (n<<2)+n = n*5, then <<2 = n*20 sequence); for small non-zero
# n it first seeds a header (sub_822F1F00 with a default 0x8273FA80-region value), then requests
# the block from Allocate_OrDbgBreak_8221F388. Returns the allocated base (or 0 for n == 0).
# Part of the 0x8273F cluster. 70 lines, 2 callees (sub_822F1F00, Allocate_OrDbgBreak_8221F388).
# 10 callers (spread across the 0x821x/0x823x/0x824x/0x825x/0x826x/0x829x/0x82Ax containers).
[entrypoint.functions."0x8273FA80"]
name = "Allocate20ByteArray_8273FA80"

# Grows a 20-byte-element buffer to fit n elements. It computes the current element count from
# the stored byte span ((obj->8 - obj->4) / 20); if n (r4) exceeds that count it allocates extra
# space and shifts the payload, then updates the read/write pointers (obj->4 / obj->8) via the
# sibling helpers sub_82A10D50, sub_82A10E80, and sub_82A11248. This is the "reserve capacity"
# counterpart of Allocate20ByteArray_8273FA80.
# Part of the 0x82A10 cluster. 106 lines, 3 callees (sub_82A10D50, sub_82A10E80, sub_82A11248).
# 2 callers: sub_83238090, sub_83238600 (the 0x83238 container cluster).
[entrypoint.functions."0x82A10C90"]
name = "ProcessGrow2_82A10C90"

# Existing Functions

# The main per-frame render/present loop (one call per frame: ~30/s at the
# 30 fps cap, ~60/s with the 60 FPS patch). Presents the frame via VdSwap and
# calls the frame limiter (0x82242628), the per-frame helpers, and the
# 60 FPS dispatch function (0x82B9C7F8). fps_meter.h hooks this symbol.
[entrypoint.functions."0x82B9CD68"]
name = "MainRenderLoop_82B9CD68"
[entrypoint.functions."0x8222B3C8"]
name = "SwitchDispatch_8222B3C8"
size = 0x00F8

[entrypoint.functions."0x822A2AB0"]
name = "VtableSlotX3173_822A2AB0"
size = 0x00F0

[entrypoint.functions."0x822D7C50"]
name = "SwitchDispatch_822D7C50"
size = 0x0060

[entrypoint.functions."0x822D7CB0"]
name = "SwitchDispatch_822D7CB0"
size = 0x0078

[entrypoint.functions."0x822D7D58"]
name = "SwitchDispatch_822D7D58"
size = 0x00A0

[entrypoint.functions."0x822D7DF8"]
name = "SwitchDispatch_822D7DF8"
size = 0x00A8

[entrypoint.functions."0x822D7EA0"]
name = "SwitchDispatch_822D7EA0"
size = 0x00E8

[entrypoint.functions."0x822EAA98"]
name = "SwitchDispatch_822EAA98"
size = 0x0098

[entrypoint.functions."0x823A5A28"]
name = "SwitchDispatch_823A5A28"
size = 0x0118

[entrypoint.functions."0x824063B0"]
name = "SwitchDispatch_824063B0"
size = 0x00F0

[entrypoint.functions."0x82415A20"]
name = "SwitchDispatch_82415A20"
size = 0x01B8

[entrypoint.functions."0x8241BAA8"]
name = "SwitchDispatch_8241BAA8"
size = 0x0120

[entrypoint.functions."0x8242C5E0"]
name = "SwitchDispatch_8242C5E0"
size = 0x0088

[entrypoint.functions."0x824B7E70"]
name = "SwitchDispatch_824B7E70"
size = 0x0090

[entrypoint.functions."0x824BDE60"]
name = "SwitchDispatch_824BDE60"
size = 0x00A8

[entrypoint.functions."0x824CAC90"]
name = "SwitchDispatch_824CAC90"
size = 0x0158

[entrypoint.functions."0x824D2800"]
name = "SwitchDispatch_824D2800"
size = 0x0080

[entrypoint.functions."0x8250B778"]
name = "SwitchDispatch_8250B778"
size = 0x0058

[entrypoint.functions."0x82520C00"]
name = "SwitchDispatch_82520C00"
size = 0x0070

[entrypoint.functions."0x82522E18"]
name = "SwitchDispatch_82522E18"
size = 0x0178

[entrypoint.functions."0x82592C80"]
name = "SwitchDispatch_82592C80"
size = 0x0088

[entrypoint.functions."0x8259AF48"]
name = "SwitchDispatch_8259AF48"
size = 0x0080

[entrypoint.functions."0x825A0B58"]
name = "SwitchDispatch_825A0B58"
size = 0x0090

[entrypoint.functions."0x82622360"]
name = "SwitchDispatch_82622360"
size = 0x00C0

[entrypoint.functions."0x826224B8"]
name = "SwitchDispatch_826224B8"
size = 0x00B0

[entrypoint.functions."0x8272B2C0"]
name = "SwitchDispatch_8272B2C0"
size = 0x0168

[entrypoint.functions."0x82771250"]
name = "SwitchDispatch_82771250"
size = 0x0070

[entrypoint.functions."0x827D30C0"]
name = "SwitchDispatch_827D30C0"
size = 0x02E0

[entrypoint.functions."0x8283A008"]
name = "SwitchDispatch_8283A008"
size = 0x0088

[entrypoint.functions."0x8283FF98"]
name = "SwitchDispatch_8283FF98"
size = 0x00D0

[entrypoint.functions."0x8289FB40"]
name = "SwitchDispatch_8289FB40"
size = 0x02C8

[entrypoint.functions."0x8299F668"]
name = "SwitchDispatch_8299F668"
size = 0x0090

[entrypoint.functions."0x82A2F3E8"]
name = "SwitchDispatch_82A2F3E8"
size = 0x0118

[entrypoint.functions."0x82A34680"]
name = "SwitchDispatch_82A34680"
size = 0x0480

[entrypoint.functions."0x82B38270"]
name = "SwitchDispatch_82B38270"
size = 0x0110

[entrypoint.functions."0x82B57148"]
name = "SwitchDispatch_82B57148"
size = 0x02E0

[entrypoint.functions."0x82B809E8"]
name = "SwitchDispatch_82B809E8"
size = 0x0230

[entrypoint.functions."0x82B8F5E8"]
name = "SwitchDispatch_82B8F5E8"
size = 0x0128

[entrypoint.functions."0x82BC8260"]
name = "SwitchDispatch_82BC8260"
size = 0x0080

[entrypoint.functions."0x82BC9B28"]
name = "SwitchDispatch_82BC9B28"
size = 0x0128

[entrypoint.functions."0x82BE0100"]
name = "SwitchDispatch_82BE0100"
size = 0x00A8

[entrypoint.functions."0x82BE9668"]
name = "TailCall_82BE9668"
size = 0x0018  # was 0x08D4 in XenonRecomp; trimmed to avoid overlap with 0x82BE9680

[entrypoint.functions."0x82BE9680"]
name = "VtableSlotX1_82BE9680"
size = 0x08BC

[entrypoint.functions."0x82C56828"]
name = "SwitchDispatch_82C56828"
size = 0x0078

[entrypoint.functions."0x82C89840"]
name = "SwitchDispatch_82C89840"
size = 0x0078  # was 0x01D8 in XenonRecomp; trimmed to avoid overlap with 0x82C898B8

[entrypoint.functions."0x82C898B8"]
name = "SwitchDispatch_82C898B8"
size = 0x00A8  # was 0x0154 in XenonRecomp; trimmed to avoid overlap with 0x82C89960

[entrypoint.functions."0x82C89960"]
name = "SwitchDispatch_82C89960"
size = 0x00AC

[entrypoint.functions."0x82C8C9F0"]
name = "SwitchDispatch_82C8C9F0"
size = 0x0150

[entrypoint.functions."0x82C8CF00"]
name = "SwitchDispatch_82C8CF00"
size = 0x0100

[entrypoint.functions."0x82C8D3F0"]
name = "SwitchDispatch_82C8D3F0"
size = 0x01E8  # original XenonRecomp size; nested 0x82C8D4E8 entry removed (see comment above)

[entrypoint.functions."0x82C8D630"]
name = "Str_BoundedCopy_82C8D630"
size = 0x00D8

[entrypoint.functions."0x82C8D750"]
name = "SwitchDispatch_82C8D750"
size = 0x00D0

[entrypoint.functions."0x82C8DAE8"]
name = "SwitchDispatch_82C8DAE8"
size = 0x00F8

[entrypoint.functions."0x82C97120"]
name = "SwitchDispatch_82C97120"
size = 0x0098

[entrypoint.functions."0x82C99CB8"]
name = "SwitchDispatch_82C99CB8"
size = 0x0180

[entrypoint.functions."0x82C99ED0"]
name = "SwitchDispatch_82C99ED0"
size = 0x0190

[entrypoint.functions."0x82C9A118"]
name = "SwitchDispatch_82C9A118"
size = 0x0190

[entrypoint.functions."0x82C9B420"]
name = "SwitchDispatch_82C9B420"
size = 0x00D0

[entrypoint.functions."0x82C9D280"]
name = "SwitchDispatch_82C9D280"
size = 0x0198

[entrypoint.functions."0x82C9DB00"]
name = "SwitchDispatch_82C9DB00"
size = 0x0130

[entrypoint.functions."0x82C9E878"]
name = "SwitchDispatch_82C9E878"
size = 0x00E0

[entrypoint.functions."0x82C9E9B8"]
name = "SwitchDispatch_82C9E9B8"
size = 0x0108

[entrypoint.functions."0x82CA1A00"]
name = "SwitchDispatch_82CA1A00"
size = 0x0118

[entrypoint.functions."0x82CA1E98"]
name = "SwitchDispatch_82CA1E98"
size = 0x0140

[entrypoint.functions."0x82D7F5B0"]
name = "SwitchDispatch_82D7F5B0"
size = 0x0158

[entrypoint.functions."0x82D82B48"]
name = "SwitchDispatch_82D82B48"
size = 0x01A0

[entrypoint.functions."0x82D97098"]
name = "SwitchDispatch_82D97098"
size = 0x03F0

[entrypoint.functions."0x82D9E998"]
name = "SwitchDispatch_82D9E998"
size = 0x0058

[entrypoint.functions."0x82D9E9F0"]
name = "SwitchDispatch_82D9E9F0"
size = 0x0074

[entrypoint.functions."0x82DA60B0"]
name = "SwitchDispatch_82DA60B0"
size = 0x0320

[entrypoint.functions."0x82DDC208"]
name = "SwitchDispatch_82DDC208"
size = 0x0250

[entrypoint.functions."0x82DE4C10"]
name = "SwitchDispatch_82DE4C10"
size = 0x01E8

[entrypoint.functions."0x82DE6750"]
name = "SwitchDispatch_82DE6750"
size = 0x00F8

[entrypoint.functions."0x82E0F3A8"]
name = "SwitchDispatch_82E0F3A8"
size = 0x00D0

[entrypoint.functions."0x82E33B90"]
name = "SwitchDispatch_82E33B90"
size = 0x00B8

[entrypoint.functions."0x82FE7850"]
name = "VtableSlotX2_82FE7850"
size = 0x0108

[entrypoint.functions."0x83046BE8"]
name = "VtableSlotX2_83046BE8"
size = 0x0118

[entrypoint.functions."0x83095890"]
name = "IndirectCall_83095890"
size = 0x0110

[entrypoint.functions."0x8310FC68"]
name = "IndirectCall_8310FC68"
size = 0x0080

[entrypoint.functions."0x831B31B8"]
name = "IndirectCall_831B31B8"
size = 0x0078

[entrypoint.functions."0x831FBA60"]
name = "IndirectCall_831FBA60"
size = 0x0068

[entrypoint.functions."0x831FBAC8"]
name = "IndirectCall_831FBAC8"
size = 0x0068

[entrypoint.functions."0x831FBB30"]
name = "IndirectCall_831FBB30"
size = 0x0060

[entrypoint.functions."0x831FBB90"]
name = "IndirectCall_831FBB90"
size = 0x0198

# Gap targets: branches from discovered functions land at addresses the
# auto-discovery never registered as functions. Seeded here without a size so
# the scanner determines the extent (added 2026-09-19 after
# "Validation failed: 12 unresolved calls").
[entrypoint.functions."0x82BEA25C"]
name = "TailCall_82BEA25C"
[entrypoint.functions."0x82CD7948"]
name = "Func_82CD7948"
[entrypoint.functions."0x82C000F8"]
name = "TailCall_82C000F8"
[entrypoint.functions."0x82C00108"]
name = "TailCall_82C00108"
[entrypoint.functions."0x82C00118"]
name = "TailCall_82C00118"
[entrypoint.functions."0x82C14D58"]
name = "TailCall_82C14D58"
[entrypoint.functions."0x82F279D8"]
name = "RetConst1_82F279D8"
[entrypoint.functions."0x82E7E4F8"]
name = "TailCall_82E7E4F8"
[entrypoint.functions."0x82C0B680"]
name = "TailCall_82C0B680"
[entrypoint.functions."0x82C00120"]
name = "TailCall_82C00120"
[entrypoint.functions."0x82C00110"]
name = "TailCall_82C00110"
[entrypoint.functions."0x82C00128"]
name = "TailCall_82C00128"
[entrypoint.functions."0x82C0B688"]
name = "TailCall_82C0B688"


# Additional function-pointer-table entries (broader scan: in-body code-range
# pointers stored within +-20B of >=2 registered starts). Fixes the 0x829FCB00
# FATAL (2026-09-02) and pre-seeds similar table entries.
[entrypoint.functions."0x82238828"]
name = "Heap_AllocateBlock_82238828"
[entrypoint.functions."0x82238FCC"]
name = "TailCall_82238FCC"
[entrypoint.functions."0x822394F0"]
name = "Heap_FreeBlock_822394F0"
[entrypoint.functions."0x822396C8"]
name = "TailCall_822396C8"
[entrypoint.functions."0x829FCB00"]
name = "VtableSlotX3_829FCB00"
[entrypoint.functions."0x82B387D0"]
name = "Func_82B387D0"
[entrypoint.functions."0x82B387E8"]
name = "Func_82B387E8"
[entrypoint.functions."0x82C59698"]
name = "Func_82C59698"
[entrypoint.functions."0x82C5AC4C"]
name = "Func_82C5AC4C"
[entrypoint.functions."0x82C5CFBC"]
name = "TailCall_82C5CFBC"
[entrypoint.functions."0x82C5DA68"]
name = "Func_82C5DA68"
[entrypoint.functions."0x82C5DA94"]
name = "Func_82C5DA94"
[entrypoint.functions."0x82C89A0C"]
name = "Func_82C89A0C"
[entrypoint.functions."0x82CA33AC"]
name = "Func_82CA33AC"
[entrypoint.functions."0x82CA33E4"]
name = "Func_82CA33E4"
[entrypoint.functions."0x82CA3614"]
name = "Func_82CA3614"
[entrypoint.functions."0x82CA36C4"]
name = "TailCall_82CA36C4"
[entrypoint.functions."0x82CA47C4"]
name = "Func_82CA47C4"
[entrypoint.functions."0x82CA4814"]
name = "Func_82CA4814"
[entrypoint.functions."0x82CA4A64"]
name = "Func_82CA4A64"
[entrypoint.functions."0x82CA4A70"]
name = "Func_82CA4A70"
[entrypoint.functions."0x82CA4DE4"]
name = "Func_82CA4DE4"
[entrypoint.functions."0x82CA4DFC"]
name = "TailCall_82CA4DFC"
[entrypoint.functions."0x82CA4FB0"]
name = "Func_82CA4FB0"
[entrypoint.functions."0x82CA5004"]
name = "Func_82CA5004"
[entrypoint.functions."0x82CA5064"]
name = "TailCall_82CA5064"
[entrypoint.functions."0x82CA507C"]
name = "Func_82CA507C"
[entrypoint.functions."0x82CA51C0"]
name = "Func_82CA51C0"
[entrypoint.functions."0x82CA51CC"]
name = "Func_82CA51CC"
[entrypoint.functions."0x82CA53BC"]
name = "Func_82CA53BC"
[entrypoint.functions."0x82CA53D0"]
name = "TailCall_82CA53D0"
[entrypoint.functions."0x82CA56DC"]
name = "Func_82CA56DC"
[entrypoint.functions."0x82CA56E8"]
name = "Func_82CA56E8"
[entrypoint.functions."0x82CA71C8"]
name = "Func_82CA71C8"
[entrypoint.functions."0x82CA7300"]
name = "TailCall_82CA7300"
[entrypoint.functions."0x82CA7C70"]
name = "Func_82CA7C70"
[entrypoint.functions."0x82CA7C90"]
name = "TailCall_82CA7C90"
[entrypoint.functions."0x82CA7C9C"]
name = "TailCall_82CA7C9C"
[entrypoint.functions."0x82CA8038"]
name = "Func_82CA8038"
[entrypoint.functions."0x82CA8060"]
name = "TailCall_82CA8060"
[entrypoint.functions."0x82CA806C"]
name = "TailCall_82CA806C"
[entrypoint.functions."0x82CA8128"]
name = "Func_82CA8128"
[entrypoint.functions."0x82CA8154"]
name = "TailCall_82CA8154"
[entrypoint.functions."0x82CA8248"]
name = "Func_82CA8248"
[entrypoint.functions."0x82CA827C"]
name = "TailCall_82CA827C"
[entrypoint.functions."0x82CA8B14"]
name = "Func_82CA8B14"
[entrypoint.functions."0x82CA8B44"]
name = "Func_82CA8B44"
[entrypoint.functions."0x82CA9064"]
name = "Func_82CA9064"
[entrypoint.functions."0x82CA91E8"]
name = "TailCall_82CA91E8"
[entrypoint.functions."0x82CA9660"]
name = "BugCheck_82CA9660"
[entrypoint.functions."0x82CA9710"]
name = "BugCheck_82CA9710"
[entrypoint.functions."0x82CAB2B8"]
name = "Func_82CAB2B8"
[entrypoint.functions."0x82CAB2D4"]
name = "TailCall_82CAB2D4"
[entrypoint.functions."0x82CAF2C0"]
name = "Sync_82CAF2C0"
[entrypoint.functions."0x82CAF40C"]
name = "TailCall_82CAF40C"
[entrypoint.functions."0x82CAF620"]
name = "Func_82CAF620"
[entrypoint.functions."0x82CAF658"]
name = "TailCall_82CAF658"
[entrypoint.functions."0x82CAFAB0"]
name = "Func_82CAFAB0"
[entrypoint.functions."0x82CAFAFC"]
name = "TailCall_82CAFAFC"
[entrypoint.functions."0x82CAFC28"]
name = "Func_82CAFC28"
[entrypoint.functions."0x82CAFC88"]
name = "TailCall_82CAFC88"
[entrypoint.functions."0x82CAFE98"]
name = "Func_82CAFE98"
[entrypoint.functions."0x82CAFEE4"]
name = "TailCall_82CAFEE4"
[entrypoint.functions."0x82CB0138"]
name = "Func_82CB0138"
[entrypoint.functions."0x82CB0184"]
name = "TailCall_82CB0184"
[entrypoint.functions."0x82CB0FB0"]
name = "Func_82CB0FB0"
[entrypoint.functions."0x82CB1040"]
name = "Func_82CB1040"
[entrypoint.functions."0x82CB1390"]
name = "Func_82CB1390"
[entrypoint.functions."0x82CB13EC"]
name = "Func_82CB13EC"
[entrypoint.functions."0x82CB4938"]
name = "Func_82CB4938"
[entrypoint.functions."0x82CB4974"]
name = "Func_82CB4974"
[entrypoint.functions."0x82CB49B0"]
name = "TailCall_82CB49B0"
[entrypoint.functions."0x82CB49C0"]
name = "Func_82CB49C0"
[entrypoint.functions."0x82CB49DC"]
name = "Func_82CB49DC"
[entrypoint.functions."0x82CB4B1C"]
name = "Func_82CB4B1C"
[entrypoint.functions."0x82CB4B28"]
name = "Func_82CB4B28"
[entrypoint.functions."0x82CB4B38"]
name = "Func_82CB4B38"
[entrypoint.functions."0x82CB4CB8"]
name = "Func_82CB4CB8"
[entrypoint.functions."0x82CB4E9C"]
name = "Func_82CB4E9C"
[entrypoint.functions."0x82CB4EAC"]
name = "TailCall_82CB4EAC"
[entrypoint.functions."0x82CB4F18"]
name = "Func_82CB4F18"
[entrypoint.functions."0x82CB4FDC"]
name = "TailCall_82CB4FDC"
[entrypoint.functions."0x82CB4FEC"]
name = "TailCall_82CB4FEC"
[entrypoint.functions."0x82CB56B0"]
name = "Func_82CB56B0"
[entrypoint.functions."0x82CB56BC"]
name = "Func_82CB56BC"
[entrypoint.functions."0x82CB56CC"]
name = "Func_82CB56CC"
[entrypoint.functions."0x82CB57CC"]
name = "BugCheck_82CB57CC"
[entrypoint.functions."0x82CB57D4"]
name = "BugCheck_82CB57D4"
[entrypoint.functions."0x82CB57E0"]
name = "BugCheck_82CB57E0"
[entrypoint.functions."0x82CB582C"]
name = "Func_82CB582C"
[entrypoint.functions."0x82CB5834"]
name = "Func_82CB5834"
[entrypoint.functions."0x82CB5840"]
name = "Func_82CB5840"
[entrypoint.functions."0x82CB5894"]
name = "Func_82CB5894"
[entrypoint.functions."0x82CB5918"]
name = "TailCall_82CB5918"
[entrypoint.functions."0x82CB5BB4"]
name = "Func_82CB5BB4"
[entrypoint.functions."0x82CB5BC0"]
name = "Func_82CB5BC0"
[entrypoint.functions."0x82CB5BD0"]
name = "Func_82CB5BD0"
[entrypoint.functions."0x82CB68A8"]
name = "Func_82CB68A8"
[entrypoint.functions."0x82CB68F4"]
name = "TailCall_82CB68F4"
[entrypoint.functions."0x82CB6C48"]
name = "Func_82CB6C48"
[entrypoint.functions."0x82CB6C90"]
name = "TailCall_82CB6C90"
[entrypoint.functions."0x82CB89E8"]
name = "Func_82CB89E8"
[entrypoint.functions."0x82CB8A08"]
name = "Func_82CB8A08"
[entrypoint.functions."0x82CB8D48"]
name = "Sync_82CB8D48"
[entrypoint.functions."0x82CB8D7C"]
name = "Sync_82CB8D7C"
[entrypoint.functions."0x82CB8E64"]
name = "Sync_82CB8E64"
[entrypoint.functions."0x82CB8ECC"]
name = "Sync_82CB8ECC"
[entrypoint.functions."0x82CB8F00"]
name = "Sync_82CB8F00"
[entrypoint.functions."0x82CB9018"]
name = "TailCall_82CB9018"
[entrypoint.functions."0x82CBA360"]
name = "Func_82CBA360"
[entrypoint.functions."0x82CBA370"]
name = "TailCall_82CBA370"
[entrypoint.functions."0x82CBA76C"]
name = "Func_82CBA76C"
[entrypoint.functions."0x82CBA8D0"]
name = "TailCall_82CBA8D0"
[entrypoint.functions."0x82CBAE6C"]
name = "Func_82CBAE6C"
[entrypoint.functions."0x82CBAE88"]
name = "Func_82CBAE88"
[entrypoint.functions."0x82CBAF00"]
name = "Func_82CBAF00"
[entrypoint.functions."0x82CBAF0C"]
name = "Func_82CBAF0C"
[entrypoint.functions."0x82CBB98C"]
name = "FatalError_82CBB98C"
[entrypoint.functions."0x82CBBB24"]
name = "FatalError_82CBBB24"
[entrypoint.functions."0x82CBBB30"]
name = "FatalError_82CBBB30"
[entrypoint.functions."0x82CBC820"]
name = "FsVolume_82CBC820"
[entrypoint.functions."0x82CBC888"]
name = "Func_82CBC888"
[entrypoint.functions."0x82CBC930"]
name = "Func_82CBC930"
[entrypoint.functions."0x82CBC940"]
name = "Func_82CBC940"
[entrypoint.functions."0x82CBC97C"]
name = "Func_82CBC97C"
[entrypoint.functions."0x82CBF740"]
name = "MemMgr_82CBF740"
[entrypoint.functions."0x82CBF760"]
name = "MemMgr_82CBF760"
[entrypoint.functions."0x82CBF770"]
name = "MemMgr_82CBF770"
[entrypoint.functions."0x82CBFD74"]
name = "MemFree_82CBFD74"
[entrypoint.functions."0x82CBFD9C"]
name = "MemFree_82CBFD9C"
[entrypoint.functions."0x82CC02FC"]
name = "Exception_82CC02FC"
[entrypoint.functions."0x82CC031C"]
name = "Exception_82CC031C"
[entrypoint.functions."0x82CC032C"]
name = "Exception_82CC032C"
[entrypoint.functions."0x82CC0494"]
name = "TailCall_82CC0494"
[entrypoint.functions."0x82CC04A4"]
name = "Func_82CC04A4"
[entrypoint.functions."0x82CC04B0"]
name = "TailCall_82CC04B0"
[entrypoint.functions."0x82CCA428"]
name = "Thread_82CCA428"
[entrypoint.functions."0x82CCA448"]
name = "Thread_82CCA448"
[entrypoint.functions."0x82CCA454"]
name = "Thread_82CCA454"
[entrypoint.functions."0x82CD1690"]
name = "TailCall_82CD1690"
[entrypoint.functions."0x82CD182C"]
name = "Func_82CD182C"
[entrypoint.functions."0x82CD18AC"]
name = "Func_82CD18AC"
[entrypoint.functions."0x83000858"]
name = "Func_83000858"
[entrypoint.functions."0x830009AC"]
name = "Func_830009AC"
[entrypoint.functions."0x83000AF0"]
name = "Func_83000AF0"
[entrypoint.functions."0x83000CF4"]
name = "Func_83000CF4"
[entrypoint.functions."0x83001060"]
name = "Func_83001060"
[entrypoint.functions."0x83001184"]
name = "TailCall_83001184"
[entrypoint.functions."0x83001318"]
name = "Func_83001318"
[entrypoint.functions."0x830013A8"]
name = "TailCall_830013A8"
[entrypoint.functions."0x83001514"]
name = "Func_83001514"
[entrypoint.functions."0x8300164C"]
name = "TailCall_8300164C"
[entrypoint.functions."0x8300172C"]
name = "Func_8300172C"
[entrypoint.functions."0x830017A0"]
name = "TailCall_830017A0"
[entrypoint.functions."0x83002974"]
name = "Func_83002974"
[entrypoint.functions."0x8300298C"]
name = "Func_8300298C"
[entrypoint.functions."0x83002E84"]
name = "Func_83002E84"
[entrypoint.functions."0x83002F18"]
name = "TailCall_83002F18"
[entrypoint.functions."0x8300338C"]
name = "Func_8300338C"
[entrypoint.functions."0x830033E0"]
name = "Func_830033E0"
[entrypoint.functions."0x83003414"]
name = "TailCall_83003414"
[entrypoint.functions."0x8300342C"]
name = "TailCall_8300342C"
[entrypoint.functions."0x830046B4"]
name = "Func_830046B4"
[entrypoint.functions."0x830046C0"]
name = "Func_830046C0"
[entrypoint.functions."0x83040FEC"]
name = "Func_83040FEC"
[entrypoint.functions."0x8304100C"]
name = "Func_8304100C"
[entrypoint.functions."0x8304101C"]
name = "Func_8304101C"
[entrypoint.functions."0x830F07A8"]
name = "Func_830F07A8"
[entrypoint.functions."0x830F07F8"]
name = "Func_830F07F8"
[entrypoint.functions."0x830F0914"]
name = "Func_830F0914"
[entrypoint.functions."0x830F0980"]
name = "TailCall_830F0980"
[entrypoint.functions."0x830F1208"]
name = "Func_830F1208"
[entrypoint.functions."0x830F1228"]
name = "Func_830F1228"
[entrypoint.functions."0x830FB43C"]
name = "Func_830FB43C"
[entrypoint.functions."0x830FB490"]
name = "Func_830FB490"
[entrypoint.functions."0x830FB4A0"]
name = "Func_830FB4A0"
[entrypoint.functions."0x830FD788"]
name = "Func_830FD788"
[entrypoint.functions."0x830FD7A8"]
name = "Func_830FD7A8"
[entrypoint.functions."0x830FD7B8"]
name = "Func_830FD7B8"
[entrypoint.functions."0x830FD7C8"]
name = "Func_830FD7C8"
[entrypoint.functions."0x830FD7E4"]
name = "Func_830FD7E4"
[entrypoint.functions."0x830FD7F4"]
name = "Func_830FD7F4"
[entrypoint.functions."0x830FDCEC"]
name = "Func_830FDCEC"
[entrypoint.functions."0x830FDCFC"]
name = "Func_830FDCFC"
[entrypoint.functions."0x830FDD08"]
name = "Func_830FDD08"
[entrypoint.functions."0x830FE168"]
name = "Func_830FE168"
[entrypoint.functions."0x830FE220"]
name = "Func_830FE220"
[entrypoint.functions."0x830FE230"]
name = "Func_830FE230"
[entrypoint.functions."0x830FE2E8"]
name = "Func_830FE2E8"
[entrypoint.functions."0x830FE380"]
name = "Func_830FE380"
[entrypoint.functions."0x830FE390"]
name = "Func_830FE390"
[entrypoint.functions."0x83117EDC"]
name = "Func_83117EDC"
[entrypoint.functions."0x83117F88"]
name = "Func_83117F88"
[entrypoint.functions."0x83117F94"]
name = "Func_83117F94"

# Function-pointer-table entries (found by scanning the decrypted XEX image for
# data words in the code range that are not registered function starts). The
# dispatcher can only call registered starts, so these mid-function pointer
# targets are seeded as functions here (scanner sizes them). First hit:
# [FATAL] Call to invalid or unregistered function at 0x82CD8650 (2026-09-19).
[entrypoint.functions."0x82267568"]
name = "RetAddr_82267568"
[entrypoint.functions."0x82267C88"]
name = "RetAddr_82267C88"
[entrypoint.functions."0x824D56C8"]
name = "Math_FloatIndexStore_824D56C8"
[entrypoint.functions."0x824F0600"]
name = "VtableSlot29_824F0600"
[entrypoint.functions."0x825EF568"]
name = "VtableSlotX6922_825EF568"
[entrypoint.functions."0x8274B798"]
name = "RetConst67_8274B798"
[entrypoint.functions."0x82903F70"]
name = "VtableSlot5_82903F70"
[entrypoint.functions."0x82903F80"]
name = "VtableSlot2_82903F80"
[entrypoint.functions."0x82996258"]
name = "RetConst173_82996258"
[entrypoint.functions."0x82B56870"]
name = "VtableSlotX4_82B56870"
[entrypoint.functions."0x82B84350"]
name = "TailCall_82B84350"
[entrypoint.functions."0x82BBC898"]
name = "VtableSlotX4765_82BBC898"
[entrypoint.functions."0x82BFA4A8"]
name = "VtableSlot1_82BFA4A8"
[entrypoint.functions."0x82C09188"]
name = "VtableSlotX17_82C09188"
[entrypoint.functions."0x82C0B400"]
name = "VtableSlotX7_82C0B400"
[entrypoint.functions."0x82C1DC50"]
name = "VtableSlot7_82C1DC50"
[entrypoint.functions."0x82C1DC60"]
name = "VtableSlot6_82C1DC60"
[entrypoint.functions."0x82C1DC80"]
name = "VtableSlotX3_82C1DC80"
[entrypoint.functions."0x82C2F500"]
name = "VtableSlotX12_82C2F500"
[entrypoint.functions."0x82C4C2C8"]
name = "VtableSlotX10_82C4C2C8"
[entrypoint.functions."0x82C4C2E8"]
name = "VtableSlotX11_82C4C2E8"
[entrypoint.functions."0x82C4C300"]
name = "VtableSlotX6_82C4C300"
[entrypoint.functions."0x82C4C320"]
name = "VtableSlotX2_82C4C320"
[entrypoint.functions."0x82C4C5A8"]
name = "VtableSlotX2_82C4C5A8"
[entrypoint.functions."0x82C4C5C8"]
name = "VtableSlotX1_82C4C5C8"
[entrypoint.functions."0x82C4C5E8"]
name = "VtableSlotX1_82C4C5E8"
[entrypoint.functions."0x82C867E8"]
name = "TailCall_82C867E8"
[entrypoint.functions."0x82C8D820"]
name = "Func_82C8D820"
[entrypoint.functions."0x82CD8650"]
name = "Func_82CD8650"
[entrypoint.functions."0x82CDBAE0"]
name = "Func_82CDBAE0"
[entrypoint.functions."0x82CE1510"]
name = "TailCall_82CE1510"
[entrypoint.functions."0x82CE1520"]
name = "TailCall_82CE1520"
[entrypoint.functions."0x82CE1548"]
name = "TailCall_82CE1548"
[entrypoint.functions."0x82CE3750"]
name = "TailCall_82CE3750"
[entrypoint.functions."0x82CE3760"]
name = "TailCall_82CE3760"
[entrypoint.functions."0x82CE3768"]
name = "TailCall_82CE3768"
[entrypoint.functions."0x82CE3770"]
name = "TailCall_82CE3770"
[entrypoint.functions."0x82CE3798"]
name = "TailCall_82CE3798"
[entrypoint.functions."0x82CE37A0"]
name = "TailCall_82CE37A0"
[entrypoint.functions."0x82CE37A8"]
name = "TailCall_82CE37A8"
[entrypoint.functions."0x82CE5D50"]
name = "VtableSlotX4_82CE5D50"
[entrypoint.functions."0x82CE5D98"]
name = "Func_82CE5D98"
[entrypoint.functions."0x82E86188"]
name = "Func_82E86188"
[entrypoint.functions."0x82E87A10"]
name = "Func_82E87A10"
[entrypoint.functions."0x82E8F9E8"]
name = "RetAddr_82E8F9E8"
[entrypoint.functions."0x82EB7ED0"]
name = "RetAddr_82EB7ED0"
[entrypoint.functions."0x82EC1918"]
name = "RetAddr_82EC1918"
[entrypoint.functions."0x82EC4E58"]
name = "RetAddr_82EC4E58"
[entrypoint.functions."0x82EF41D8"]
name = "VtableSlotX3_82EF41D8"
[entrypoint.functions."0x82EF41F0"]
name = "VtableSlotX4_82EF41F0"
[entrypoint.functions."0x82EF8D20"]
name = "Func_82EF8D20"
[entrypoint.functions."0x82F04420"]
name = "VtableSlot26_82F04420"
[entrypoint.functions."0x82F04430"]
name = "RetConst0_82F04430"
[entrypoint.functions."0x82F0BAF8"]
name = "TailCall_82F0BAF8"
[entrypoint.functions."0x82F0BB08"]
name = "TailCall_82F0BB08"
[entrypoint.functions."0x82F1D508"]
name = "VtableSlotX10_82F1D508"
[entrypoint.functions."0x82F1D520"]
name = "VtableSlotX4_82F1D520"
[entrypoint.functions."0x82F1D538"]
name = "VtableSlotX11_82F1D538"
[entrypoint.functions."0x82F1D550"]
name = "VtableSlotX5_82F1D550"
[entrypoint.functions."0x82F1D568"]
name = "VtableSlotX6_82F1D568"
[entrypoint.functions."0x82F1D580"]
name = "VtableSlotX7_82F1D580"
[entrypoint.functions."0x82F1D598"]
name = "VtableSlotX8_82F1D598"
[entrypoint.functions."0x82F1D5B0"]
name = "VtableSlotX9_82F1D5B0"
[entrypoint.functions."0x82F2C2E8"]
name = "Func_82F2C2E8"
[entrypoint.functions."0x82F2C3A8"]
name = "VtableSlotX14_82F2C3A8"
[entrypoint.functions."0x82F2C3C0"]
name = "Func_82F2C3C0"
[entrypoint.functions."0x82F38DA0"]
name = "TailCall_82F38DA0"
[entrypoint.functions."0x82F435A8"]
name = "Func_82F435A8"
[entrypoint.functions."0x82F56690"]
name = "TailCall_82F56690"
[entrypoint.functions."0x82F67E18"]
name = "VtableSlotX3_82F67E18"
[entrypoint.functions."0x82F67E40"]
name = "VtableSlotX6_82F67E40"
[entrypoint.functions."0x82F67E68"]
name = "VtableSlotX7_82F67E68"
[entrypoint.functions."0x82FB7470"]
name = "Func_82FB7470"
[entrypoint.functions."0x82FE7EF8"]
name = "TailCall_82FE7EF8"
[entrypoint.functions."0x82FE7F00"]
name = "TailCall_82FE7F00"
[entrypoint.functions."0x83056810"]
name = "VtableSlot95_83056810"
[entrypoint.functions."0x83056BC0"]
name = "VtableSlotX133_83056BC0"
[entrypoint.functions."0x83056BE8"]
name = "VtableSlotX134_83056BE8"
[entrypoint.functions."0x83056C10"]
name = "VtableSlotX133_83056C10"
[entrypoint.functions."0x83056C38"]
name = "VtableSlotX134_83056C38"
[entrypoint.functions."0x83057068"]
name = "VtableSlotX151_83057068"
[entrypoint.functions."0x830ACB28"]
name = "Func_830ACB28"
[entrypoint.functions."0x831C5CD8"]
name = "VtableSlotX3_831C5CD8"
[entrypoint.functions."0x831C5D28"]
name = "Func_831C5D28"
[entrypoint.functions."0x831FFA08"]
name = "Func_831FFA08"


[entrypoint.functions."0x831FBD28"]
name = "IndirectCall_831FBD28"
size = 0x0170
